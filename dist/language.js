var __defProp = Object.defineProperty;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
const KEYWORDS_AKTION = /* @__PURE__ */ new Set([
  "function",
  // Module syntax for multi-file `.aktion` programs (resolved by the in-browser
  // linker / `linkProject`; a no-op for the streaming single-file runtime).
  // `from`/`as` are NOT keywords — they stay usable as ordinary identifiers and
  // are matched contextually by the import parser.
  "import",
  "export",
  // NOTE: `$effect` (the side-effect builtin) is `$`-prefixed, so it lexes as
  // a StateIdentifier and is recognised in the parser — it is NOT a keyword.
  "if",
  "else",
  "switch",
  "case",
  "break",
  "continue",
  "for",
  "while",
  "do",
  "of",
  "in",
  "let",
  "var",
  "const",
  "await",
  "async",
  "return",
  "default",
  "try",
  "catch",
  "finally",
  "throw",
  "new",
  "typeof",
  "instanceof",
  "delete",
  "void"
]);
const SINGLE_CHAR_PUNCT = /* @__PURE__ */ new Set(["(", ")", "[", "]", "{", "}", ",", ":", "?", "."]);
const KEYWORDS$2 = {
  true: "Boolean",
  false: "Boolean",
  null: "Null"
};
function tokenize(source) {
  const tokens = [];
  let i = 0;
  let line = 1;
  let column = 1;
  const peek = (offset = 0) => source[i + offset];
  const push = (type, value, startLine, startCol) => {
    tokens.push({ type, value, line: startLine, column: startCol });
  };
  const advance = () => {
    const ch = source[i];
    i += 1;
    if (ch === "\n") {
      line += 1;
      column = 1;
    } else {
      column += 1;
    }
    return ch;
  };
  const readHex = (count) => {
    for (let k = 0; k < count; k += 1) {
      if (!isHexDigit(peek(k) ?? "")) return null;
    }
    let hex = "";
    for (let k = 0; k < count; k += 1) hex += advance();
    return hex;
  };
  const decodeEscape = (esc) => {
    switch (esc) {
      case "n":
        return "\n";
      case "t":
        return "	";
      case "r":
        return "\r";
      case "b":
        return "\b";
      case "f":
        return "\f";
      case "v":
        return "\v";
      case "0":
        return peek() !== void 0 && peek() >= "0" && peek() <= "9" ? "0" : "\0";
      case "\\":
        return "\\";
      case '"':
        return '"';
      case "'":
        return "'";
      case "`":
        return "`";
      case "$":
        return "$";
      case "x": {
        const hex = readHex(2);
        return hex === null ? "x" : String.fromCharCode(parseInt(hex, 16));
      }
      case "u": {
        if (peek() === "{") {
          let off = 1;
          let digits = "";
          while (isHexDigit(peek(off) ?? "")) {
            digits += peek(off);
            off += 1;
          }
          const cp = digits.length > 0 ? parseInt(digits, 16) : NaN;
          if (peek(off) === "}" && Number.isFinite(cp) && cp <= 1114111) {
            for (let k = 0; k <= off; k += 1) advance();
            try {
              return String.fromCodePoint(cp);
            } catch {
              return "";
            }
          }
          return "u";
        }
        const hex = readHex(4);
        return hex === null ? "u" : String.fromCharCode(parseInt(hex, 16));
      }
      default:
        return esc ?? "";
    }
  };
  const regexAllowedHere = (toks) => {
    const last = toks[toks.length - 1];
    if (!last) return true;
    switch (last.type) {
      case "Identifier":
      case "Number":
      case "String":
      case "TemplateString":
      case "Boolean":
      case "Null":
      case "StateIdentifier":
      case "Regex":
        return false;
      case "Punctuation":
        return !(last.value === ")" || last.value === "]" || last.value === "}");
      default:
        return true;
    }
  };
  const scanRegexLiteral = () => {
    const savedI = i;
    const savedLine = line;
    const savedColumn = column;
    advance();
    let body = "";
    let inClass = false;
    while (i < source.length) {
      const c = peek();
      if (c === void 0 || c === "\n") break;
      if (c === "\\") {
        body += advance();
        if (peek() !== void 0 && peek() !== "\n") body += advance();
        continue;
      }
      if (c === "[") {
        inClass = true;
        body += advance();
        continue;
      }
      if (c === "]") {
        inClass = false;
        body += advance();
        continue;
      }
      if (c === "/" && !inClass) {
        advance();
        let flags = "";
        while (/[a-z]/i.test(peek() ?? "")) flags += advance();
        return { body, flags };
      }
      body += advance();
    }
    i = savedI;
    line = savedLine;
    column = savedColumn;
    return null;
  };
  while (i < source.length) {
    const ch = peek();
    if (ch === void 0) break;
    if (ch === "\n") {
      const startLine = line;
      const startCol = column;
      advance();
      push("Newline", "\n", startLine, startCol);
      continue;
    }
    if (ch === " " || ch === "	" || ch === "\r") {
      advance();
      continue;
    }
    if (ch === "/" && peek(1) === "/") {
      while (i < source.length && peek() !== "\n") advance();
      continue;
    }
    if (ch === "/" && peek(1) === "*") {
      advance();
      advance();
      while (i < source.length && !(peek() === "*" && peek(1) === "/")) {
        advance();
      }
      if (i < source.length) {
        advance();
        advance();
      }
      continue;
    }
    if (ch === "/" && regexAllowedHere(tokens)) {
      const startLine = line;
      const startCol = column;
      const scanned = scanRegexLiteral();
      if (scanned) {
        tokens.push({
          type: "Regex",
          value: scanned.body,
          flags: scanned.flags,
          line: startLine,
          column: startCol
        });
        continue;
      }
    }
    if (ch === ";") {
      const startLine = line;
      const startCol = column;
      advance();
      push("Semicolon", ";", startLine, startCol);
      continue;
    }
    if (ch === '"' || ch === "'") {
      const quote = ch;
      const startLine = line;
      const startCol = column;
      advance();
      let value = "";
      while (i < source.length && peek() !== quote) {
        if (peek() === "\\" && peek(1) !== void 0) {
          advance();
          value += decodeEscape(advance());
          continue;
        }
        if (peek() === "\n") {
          break;
        }
        value += advance();
      }
      if (peek() === quote) advance();
      push("String", value, startLine, startCol);
      continue;
    }
    if (ch === "`") {
      const startLine = line;
      const startCol = column;
      advance();
      const parts = [];
      let chunk = "";
      let sawExpr = false;
      while (i < source.length && peek() !== "`") {
        if (peek() === "\\" && peek(1) !== void 0) {
          advance();
          chunk += decodeEscape(advance());
          continue;
        }
        if (peek() === "$" && peek(1) === "{") {
          parts.push({ kind: "str", text: chunk });
          chunk = "";
          const exprLine = line;
          const exprCol = column;
          advance();
          advance();
          let depth = 1;
          let source2 = "";
          while (i < source.length && depth > 0) {
            const next = peek();
            if (next === void 0) break;
            if (next === "`") {
              source2 += advance();
              while (i < source.length && peek() !== "`") {
                if (peek() === "\\" && peek(1) !== void 0) {
                  source2 += advance();
                  source2 += advance();
                  continue;
                }
                source2 += advance();
              }
              if (peek() === "`") source2 += advance();
              continue;
            }
            if (next === '"' || next === "'") {
              const q = next;
              source2 += advance();
              while (i < source.length && peek() !== q) {
                if (peek() === "\\" && peek(1) !== void 0) {
                  source2 += advance();
                  source2 += advance();
                  continue;
                }
                if (peek() === "\n") break;
                source2 += advance();
              }
              if (peek() === q) source2 += advance();
              continue;
            }
            if (next === "{") {
              depth += 1;
              source2 += advance();
              continue;
            }
            if (next === "}") {
              depth -= 1;
              if (depth === 0) {
                advance();
                break;
              }
              source2 += advance();
              continue;
            }
            source2 += advance();
          }
          parts.push({ kind: "expr", source: source2, line: exprLine, column: exprCol });
          sawExpr = true;
          continue;
        }
        chunk += advance();
      }
      if (peek() === "`") advance();
      parts.push({ kind: "str", text: chunk });
      if (!sawExpr) {
        push("String", chunk, startLine, startCol);
        continue;
      }
      tokens.push({
        type: "TemplateString",
        value: "",
        line: startLine,
        column: startCol,
        parts
      });
      continue;
    }
    const lastToken = tokens[tokens.length - 1];
    const allowSignedNumber = !lastToken || lastToken.type === "Newline" || lastToken.type === "Semicolon" || lastToken.type === "Operator" || lastToken.type === "Punctuation" && (lastToken.value === "(" || lastToken.value === "[" || lastToken.value === "," || lastToken.value === ":" || lastToken.value === "?" || lastToken.value === "{");
    if (isDigit(ch) || ch === "-" && isDigit(peek(1) ?? "") && allowSignedNumber || ch === "." && isDigit(peek(1) ?? "") && allowSignedNumber) {
      const startLine = line;
      const startCol = column;
      let raw = "";
      if (ch === "-") raw += advance();
      const radixMark = peek(1);
      if (peek() === "0" && (radixMark === "x" || radixMark === "X" || radixMark === "b" || radixMark === "B" || radixMark === "o" || radixMark === "O")) {
        raw += advance();
        raw += advance();
        while (i < source.length) {
          const next = peek() ?? "";
          if (isHexDigit(next)) {
            raw += advance();
            continue;
          }
          if (next === "_" && isHexDigit(peek(1) ?? "")) {
            raw += advance();
            continue;
          }
          break;
        }
        push("Number", raw, startLine, startCol);
        continue;
      }
      let sawDot = false;
      let sawExp = false;
      while (i < source.length) {
        const next = peek() ?? "";
        if (isDigit(next)) {
          raw += advance();
          continue;
        }
        if (next === "_" && isDigit(peek(1) ?? "")) {
          raw += advance();
          continue;
        }
        if (next === "." && !sawDot && !sawExp && isDigit(peek(1) ?? "")) {
          sawDot = true;
          raw += advance();
          continue;
        }
        if ((next === "e" || next === "E") && !sawExp) {
          const afterE = peek(1) ?? "";
          const afterSign = afterE === "+" || afterE === "-" ? peek(2) ?? "" : afterE;
          if (isDigit(afterSign)) {
            sawExp = true;
            raw += advance();
            if (peek() === "+" || peek() === "-") raw += advance();
            continue;
          }
        }
        break;
      }
      push("Number", raw, startLine, startCol);
      continue;
    }
    if (ch === "$") {
      const startLine = line;
      const startCol = column;
      advance();
      let name = "";
      while (i < source.length && isIdentifierChar(peek() ?? "")) {
        name += advance();
      }
      push("StateIdentifier", name, startLine, startCol);
      continue;
    }
    if (isIdentifierStart(ch)) {
      const startLine = line;
      const startCol = column;
      let name = "";
      while (i < source.length && isIdentifierChar(peek() ?? "")) {
        name += advance();
      }
      const keyword = KEYWORDS$2[name];
      if (keyword === "Boolean") {
        push("Boolean", name, startLine, startCol);
      } else if (keyword === "Null") {
        push("Null", name, startLine, startCol);
      } else if (KEYWORDS_AKTION.has(name)) {
        push("Keyword", name, startLine, startCol);
      } else {
        push("Identifier", name, startLine, startCol);
      }
      continue;
    }
    if (ch === "." && peek(1) === "." && peek(2) === ".") {
      const startLine = line;
      const startCol = column;
      advance();
      advance();
      advance();
      push("Operator", "...", startLine, startCol);
      continue;
    }
    const two = ch + (peek(1) ?? "");
    const three = two + (peek(2) ?? "");
    const four = three + (peek(3) ?? "");
    if (four === ">>>=") {
      const startLine = line;
      const startCol = column;
      advance();
      advance();
      advance();
      advance();
      push("Operator", four, startLine, startCol);
      continue;
    }
    if (three === "===" || three === "!==" || three === "**=" || three === "??=" || three === "&&=" || three === "||=" || three === ">>>" || three === ">>=" || three === "<<=") {
      const startLine = line;
      const startCol = column;
      advance();
      advance();
      advance();
      push("Operator", three, startLine, startCol);
      continue;
    }
    if (two === "==" || two === "!=" || two === ">=" || two === "<=" || two === "&&" || two === "||" || two === "??" || two === "?." || two === "->" || two === "=>" || two === "**" || two === "%=" || two === "+=" || two === "-=" || two === "*=" || two === "/=" || two === "++" || two === "--" || two === "<<" || two === ">>" || two === "&=" || two === "|=" || two === "^=") {
      const startLine = line;
      const startCol = column;
      advance();
      advance();
      push("Operator", two, startLine, startCol);
      continue;
    }
    if ("+-*/%!=<>&|^~".includes(ch)) {
      const startLine = line;
      const startCol = column;
      advance();
      push("Operator", ch, startLine, startCol);
      continue;
    }
    if (SINGLE_CHAR_PUNCT.has(ch)) {
      const startLine = line;
      const startCol = column;
      advance();
      push("Punctuation", ch, startLine, startCol);
      continue;
    }
    advance();
  }
  tokens.push({ type: "EOF", value: "", line, column });
  return tokens;
}
function isDigit(ch) {
  return ch >= "0" && ch <= "9";
}
function isHexDigit(ch) {
  return ch >= "0" && ch <= "9" || ch >= "a" && ch <= "f" || ch >= "A" && ch <= "F";
}
function isIdentifierStart(ch) {
  return ch >= "a" && ch <= "z" || ch >= "A" && ch <= "Z" || ch === "_";
}
function isIdentifierChar(ch) {
  return isIdentifierStart(ch) || isDigit(ch);
}
function isNode(value) {
  if (typeof value !== "object" || value === null) return false;
  const kind = value.kind;
  if (typeof kind !== "string" || kind.length === 0) return false;
  const first = kind.charCodeAt(0);
  return first >= 65 && first <= 90;
}
function walkNode(root, visit) {
  visitNode(root, null, null, null, 0, visit);
}
function visitNode(node, parent, key, index, depth, visit) {
  if (visit({ node, parent, key, index, depth }) === false) return;
  for (const childKey of Object.keys(node)) {
    if (childKey === "loc") continue;
    const value = node[childKey];
    if (Array.isArray(value)) {
      for (let i = 0; i < value.length; i += 1) {
        const item = value[i];
        if (isNode(item)) visitNode(item, node, childKey, i, depth + 1, visit);
        else if (item && typeof item === "object") visitRecord(item, node, childKey, depth, visit);
      }
      continue;
    }
    if (isNode(value)) visitNode(value, node, childKey, null, depth + 1, visit);
    else if (value && typeof value === "object") {
      visitRecord(value, node, childKey, depth, visit);
    }
  }
}
function visitRecord(record, owner, key, depth, visit) {
  for (const inner of Object.keys(record)) {
    if (inner === "loc") continue;
    const value = record[inner];
    if (Array.isArray(value)) {
      for (let i = 0; i < value.length; i += 1) {
        const item = value[i];
        if (isNode(item)) visitNode(item, owner, key, i, depth + 1, visit);
        else if (item && typeof item === "object") visitRecord(item, owner, key, depth, visit);
      }
      continue;
    }
    if (isNode(value)) visitNode(value, owner, key, null, depth + 1, visit);
    else if (value && typeof value === "object") {
      visitRecord(value, owner, key, depth, visit);
    }
  }
}
function stampSourceIndex(root, index) {
  walkNode(root, ({ node }) => {
    const loc = node.loc;
    if (loc && loc.source === void 0) loc.source = index;
  });
}
function parse(source) {
  const tokens = tokenize(source);
  const ctx = new ParserContext(tokens);
  const statements = [];
  const errors = [];
  while (!ctx.isEnd()) {
    if (ctx.match("Newline") || ctx.match("Semicolon")) continue;
    try {
      const stmt = parseStatement(ctx, true);
      if (stmt) statements.push(stmt);
    } catch (err) {
      const error = err;
      errors.push(error);
      ctx.recoverToNextLine();
    }
  }
  return { statements, errors };
}
function parseStatement(ctx, _topLevel) {
  const head = ctx.peek();
  if (head.type === "StateIdentifier" && head.value === "effect" && ctx.peek(1).type === "Punctuation" && ctx.peek(1).value === "(") {
    return parseEffectStatement(ctx);
  }
  if (head.type === "Keyword") {
    switch (head.value) {
      case "function":
        return parseFunctionDecl(ctx);
      case "import":
        return parseImportStatement(ctx);
      case "export":
        return parseExportStatement(ctx);
      case "await":
        return parseAwait(ctx);
      case "async": {
        if (ctx.peek(1).type === "Keyword" && ctx.peek(1).value === "function") {
          ctx.consume();
          return parseFunctionDecl(ctx);
        }
        break;
      }
      case "return":
        return parseReturn(ctx);
      case "let":
      case "const":
      case "var":
        return parseVarDecl(ctx);
      case "if":
        return parseIfStatement(ctx);
      case "switch":
        return parseSwitchStatement(ctx);
      case "for":
        return parseForStatement(ctx);
      case "while":
        return parseWhileStatement(ctx);
      case "do":
        return parseDoWhileStatement(ctx);
      case "break":
        return parseBreakStatement(ctx);
      case "continue":
        return parseContinueStatement(ctx);
      case "throw":
        return parseThrowStatement(ctx);
      case "try":
        return parseTryStatement(ctx);
    }
  }
  const saved = ctx.snapshot();
  if (couldStartAssignment(ctx)) {
    try {
      return parseAssignment(ctx);
    } catch (err) {
      if (err && typeof err === "object" && err.__definitive) {
        throw err;
      }
      ctx.restore(saved);
    }
  }
  return parseExpressionStatement(ctx);
}
function couldStartAssignment(ctx) {
  const head = ctx.peek();
  if (head.type !== "Identifier" && head.type !== "StateIdentifier") {
    return false;
  }
  const next = ctx.peek(1);
  return next.type === "Operator" && next.value === "=";
}
function parseExpressionStatement(ctx) {
  const start = ctx.peek();
  let expression = parseExpression(ctx);
  const next = ctx.peek();
  if (next.type === "Operator" && isAssignmentOperator(next.value)) {
    if (isAssignableTarget(expression)) {
      ctx.consume();
      const value = parseExpression(ctx);
      expression = {
        kind: "BuiltinCall",
        name: "__rui_assign__",
        arguments: [
          expression,
          value,
          { kind: "Literal", value: next.value }
        ],
        loc: { line: next.line, column: next.column }
      };
    }
  } else if (next.type === "Operator" && (next.value === "++" || next.value === "--")) {
    if (isAssignableTarget(expression)) {
      ctx.consume();
      expression = {
        kind: "BuiltinCall",
        name: "__rui_postfix__",
        arguments: [expression, { kind: "Literal", value: next.value }],
        loc: { line: next.line, column: next.column }
      };
    }
  }
  skipTerminator(ctx);
  return {
    kind: "ExpressionStatement",
    expression,
    loc: { line: start.line, column: start.column }
  };
}
function isAssignmentOperator(value) {
  return value === "=" || value === "+=" || value === "-=" || value === "*=" || value === "/=" || value === "%=" || value === "**=" || value === "??=" || value === "&&=" || value === "||=" || value === "&=" || value === "|=" || value === "^=" || value === "<<=" || value === ">>=" || value === ">>>=";
}
function isAssignableTarget(expr) {
  if (expr.kind === "Member") return true;
  if (expr.kind === "StateRef") return true;
  if (expr.kind === "Identifier") return true;
  return false;
}
function parseFunctionDecl(ctx) {
  const start = ctx.expect("Keyword", "function");
  const isHook = ctx.peek().type === "StateIdentifier";
  const nameTok = isHook ? ctx.consume() : ctx.expect("Identifier");
  const params = parseFunctionParams(ctx);
  const body = parseBlock(ctx);
  skipTerminator(ctx);
  if (isHook) {
    return {
      kind: "HookDeclaration",
      name: nameTok.value,
      params,
      body,
      loc: { line: start.line, column: start.column }
    };
  }
  const isPascalCase2 = nameTok.value.length > 0 && nameTok.value[0] >= "A" && nameTok.value[0] <= "Z";
  if (isPascalCase2) {
    return {
      kind: "ComponentDeclaration",
      name: nameTok.value,
      params,
      slots: [],
      body,
      loc: { line: start.line, column: start.column }
    };
  }
  return {
    kind: "ActionDeclaration",
    name: nameTok.value,
    params,
    body,
    loc: { line: start.line, column: start.column }
  };
}
function parseFunctionParams(ctx) {
  ctx.expect("Punctuation", "(");
  const params = [];
  skipWhitespace(ctx);
  if (!(ctx.peek().type === "Punctuation" && ctx.peek().value === ")")) {
    while (true) {
      skipWhitespace(ctx);
      let isRest = false;
      if (ctx.peek().type === "Operator" && ctx.peek().value === "...") {
        ctx.consume();
        isRest = true;
      }
      const tok = ctx.peek();
      if (!isRest && tok.type === "Punctuation" && (tok.value === "{" || tok.value === "[")) {
        const pattern = parseDestructuringPattern(ctx);
        let defaultValue;
        if (ctx.peek().type === "Operator" && ctx.peek().value === "=") {
          ctx.consume();
          defaultValue = parseExpression(ctx);
        }
        const param = { name: "", pattern };
        if (defaultValue) param.defaultValue = defaultValue;
        params.push(param);
      } else if (tok.type === "Identifier" || tok.type === "Keyword") {
        const nameTok = ctx.consume();
        let defaultValue;
        if (!isRest && ctx.peek().type === "Operator" && ctx.peek().value === "=") {
          ctx.consume();
          defaultValue = parseExpression(ctx);
        }
        const param = { name: nameTok.value };
        if (defaultValue) param.defaultValue = defaultValue;
        if (isRest) param.rest = true;
        params.push(param);
      } else {
        throw {
          message: `Expected parameter name, got ${tok.type} "${tok.value}"`,
          line: tok.line,
          column: tok.column
        };
      }
      skipWhitespace(ctx);
      if (ctx.peek().type === "Punctuation" && ctx.peek().value === ",") {
        if (isRest) {
          throw {
            message: "Rest parameter `...name` must be the final parameter.",
            line: ctx.peek().line,
            column: ctx.peek().column
          };
        }
        ctx.consume();
        skipWhitespace(ctx);
        if (ctx.peek().type === "Punctuation" && ctx.peek().value === ")") break;
        continue;
      }
      break;
    }
  }
  ctx.expect("Punctuation", ")");
  return params;
}
function parseEffectStatement(ctx) {
  const start = ctx.consume();
  ctx.expect("Punctuation", "(");
  skipWhitespace(ctx);
  const callbackExpr = parseExpression(ctx);
  let body;
  if (callbackExpr.kind === "Lambda") {
    body = callbackExpr.body.kind === "Block" ? callbackExpr.body : { kind: "Block", body: [{ kind: "ExpressionStatement", expression: callbackExpr.body }] };
  } else {
    body = { kind: "Block", body: [] };
  }
  const triggers = [];
  let rateLimit;
  skipWhitespace(ctx);
  if (ctx.peek().type === "Punctuation" && ctx.peek().value === ",") {
    ctx.consume();
    skipWhitespace(ctx);
    if (ctx.peek().type === "Punctuation" && ctx.peek().value === "[") {
      ctx.consume();
      skipWhitespace(ctx);
      while (!(ctx.peek().type === "Punctuation" && ctx.peek().value === "]")) {
        parseEffectDep(ctx, triggers, (rl) => {
          rateLimit = rl;
        });
        skipWhitespace(ctx);
        if (ctx.peek().type === "Punctuation" && ctx.peek().value === ",") {
          ctx.consume();
          skipWhitespace(ctx);
        }
      }
      ctx.expect("Punctuation", "]");
    } else {
      parseExpression(ctx);
    }
  }
  skipWhitespace(ctx);
  ctx.expect("Punctuation", ")");
  skipTerminator(ctx);
  const decl = {
    kind: "EffectDeclaration",
    name: `__effect_L${start.line}_C${start.column}`,
    triggers,
    body,
    loc: { line: start.line, column: start.column }
  };
  if (rateLimit) decl.rateLimit = rateLimit;
  return decl;
}
function parseEffectDep(ctx, triggers, setRateLimit) {
  const head = ctx.peek();
  if (head.type === "StateIdentifier") {
    ctx.consume();
    let name = head.value;
    while (ctx.peek().type === "Punctuation" && ctx.peek().value === ".") {
      const prop2 = ctx.peek(1);
      if (prop2.type !== "Identifier" && prop2.type !== "Keyword") break;
      ctx.consume();
      ctx.consume();
      name += "." + prop2.value;
    }
    triggers.push({ kind: "state", name });
    return;
  }
  if (head.type === "String") {
    ctx.consume();
    const val = head.value;
    if (val === "mount" || val === "unmount") {
      triggers.push({ kind: "lifecycle", name: val });
      return;
    }
    const everyMatch = val.match(/^every\((\d+)\)$/);
    if (everyMatch) {
      triggers.push({ kind: "every", intervalMs: Number(everyMatch[1]) });
      return;
    }
    const debounceMatch = val.match(/^debounce\((\d+)\)$/);
    if (debounceMatch) {
      setRateLimit({ kind: "debounce", ms: Number(debounceMatch[1]) });
      return;
    }
    const throttleMatch = val.match(/^throttle\((\d+)\)$/);
    if (throttleMatch) {
      setRateLimit({ kind: "throttle", ms: Number(throttleMatch[1]) });
      return;
    }
    throw {
      message: `Unknown effect dependency string "${val}". Expected "mount", "unmount", "every(N)", "debounce(N)", or "throttle(N)".`,
      line: head.line,
      column: head.column
    };
  }
  throw {
    message: `Unexpected ${head.type} "${head.value}" inside effect dependency array. Expected $state or a string token ("mount", "unmount", "every(N)", etc.).`,
    line: head.line,
    column: head.column
  };
}
function parseVarDecl(ctx) {
  const start = ctx.consume();
  const head = ctx.peek();
  if (head.type === "Punctuation" && (head.value === "[" || head.value === "{")) {
    return parseDestructureDecl(ctx, start);
  }
  let identifier = "";
  let isState = false;
  if (head.type === "StateIdentifier") {
    identifier = ctx.consume().value;
    isState = true;
  } else if (head.type === "Identifier") {
    identifier = ctx.consume().value;
  } else {
    throw {
      message: `Expected identifier after "${start.value}", got ${head.type} "${head.value}"`,
      line: head.line,
      column: head.column
    };
  }
  ctx.expect("Operator", "=");
  const expression = parseExpression(ctx);
  skipTerminator(ctx);
  return {
    kind: "Assignment",
    identifier,
    isState,
    expression,
    loc: { line: start.line, column: start.column }
  };
}
function parseDestructureDecl(ctx, start) {
  const pattern = parseDestructuringPattern(ctx);
  ctx.expect("Operator", "=");
  const expression = parseExpression(ctx);
  skipTerminator(ctx);
  return {
    kind: "DestructureStatement",
    patternKind: pattern.kind,
    bindings: pattern.bindings,
    expression,
    loc: { line: start.line, column: start.column }
  };
}
function parseDestructuringPattern(ctx) {
  const head = ctx.consume();
  const patternKind = head.value === "[" ? "array" : "object";
  const bindings = [];
  if (patternKind === "array") {
    skipWhitespace(ctx);
    while (!(ctx.peek().type === "Punctuation" && ctx.peek().value === "]")) {
      if (ctx.peek().type === "Punctuation" && ctx.peek().value === ",") {
        bindings.push({ name: "" });
        ctx.consume();
        skipWhitespace(ctx);
        continue;
      }
      let isRest = false;
      if (ctx.peek().type === "Operator" && ctx.peek().value === "...") {
        ctx.consume();
        isRest = true;
      }
      if (!isRest && ctx.peek().type === "Punctuation" && (ctx.peek().value === "[" || ctx.peek().value === "{")) {
        const nested = parseDestructuringPattern(ctx);
        let nestedDefault;
        if (ctx.peek().type === "Operator" && ctx.peek().value === "=") {
          ctx.consume();
          nestedDefault = parseExpression(ctx);
        }
        const nestedBinding = { name: "", pattern: nested };
        if (nestedDefault) nestedBinding.defaultValue = nestedDefault;
        bindings.push(nestedBinding);
        skipWhitespace(ctx);
        if (ctx.peek().type === "Punctuation" && ctx.peek().value === ",") {
          ctx.consume();
          skipWhitespace(ctx);
          continue;
        }
        break;
      }
      const nameTok = ctx.expect("Identifier");
      let defaultValue;
      if (!isRest && ctx.peek().type === "Operator" && ctx.peek().value === "=") {
        ctx.consume();
        defaultValue = parseExpression(ctx);
      }
      const binding = { name: nameTok.value };
      if (isRest) binding.rest = true;
      if (defaultValue) binding.defaultValue = defaultValue;
      bindings.push(binding);
      skipWhitespace(ctx);
      if (ctx.peek().type === "Punctuation" && ctx.peek().value === ",") {
        ctx.consume();
        skipWhitespace(ctx);
        continue;
      }
      break;
    }
    ctx.expect("Punctuation", "]");
  } else {
    skipWhitespace(ctx);
    while (!(ctx.peek().type === "Punctuation" && ctx.peek().value === "}")) {
      let isRest = false;
      if (ctx.peek().type === "Operator" && ctx.peek().value === "...") {
        ctx.consume();
        isRest = true;
      }
      const keyTok = ctx.expect("Identifier");
      let alias = keyTok.value;
      let sourceKey;
      let nestedPattern;
      if (!isRest && ctx.peek().type === "Punctuation" && ctx.peek().value === ":") {
        ctx.consume();
        skipWhitespace(ctx);
        if (ctx.peek().type === "Punctuation" && (ctx.peek().value === "{" || ctx.peek().value === "[")) {
          sourceKey = keyTok.value;
          nestedPattern = parseDestructuringPattern(ctx);
        } else {
          const aliasTok = ctx.expect("Identifier");
          sourceKey = keyTok.value;
          alias = aliasTok.value;
        }
      }
      let defaultValue;
      if (!isRest && ctx.peek().type === "Operator" && ctx.peek().value === "=") {
        ctx.consume();
        defaultValue = parseExpression(ctx);
      }
      const binding = nestedPattern ? { name: "", sourceKey, pattern: nestedPattern } : { name: alias };
      if (!nestedPattern && sourceKey) binding.sourceKey = sourceKey;
      if (isRest) binding.rest = true;
      if (defaultValue) binding.defaultValue = defaultValue;
      bindings.push(binding);
      skipWhitespace(ctx);
      if (ctx.peek().type === "Punctuation" && ctx.peek().value === ",") {
        ctx.consume();
        skipWhitespace(ctx);
        continue;
      }
      break;
    }
    ctx.expect("Punctuation", "}");
  }
  return { kind: patternKind, bindings };
}
function collectPatternNames(pattern) {
  const names = [];
  for (const binding of pattern.bindings) {
    if (binding.pattern) {
      names.push(...collectPatternNames(binding.pattern));
    } else if (binding.name) {
      names.push(binding.name);
    }
  }
  return names;
}
function parseBlock(ctx) {
  const start = ctx.expect("Punctuation", "{");
  const body = [];
  skipWhitespace(ctx);
  while (!(ctx.peek().type === "Punctuation" && ctx.peek().value === "}")) {
    const stmt = parseStatement(ctx);
    if (stmt) body.push(stmt);
    skipWhitespace(ctx);
  }
  ctx.expect("Punctuation", "}");
  return {
    kind: "Block",
    body,
    loc: { line: start.line, column: start.column }
  };
}
function parseBlockOrSingleStatement(ctx) {
  skipWhitespace(ctx);
  if (ctx.peek().type === "Punctuation" && ctx.peek().value === "{") {
    return parseBlock(ctx);
  }
  const head = ctx.peek();
  const stmt = parseStatement(ctx);
  return {
    kind: "Block",
    body: stmt ? [stmt] : [],
    loc: { line: head.line, column: head.column }
  };
}
function parseAwait(ctx) {
  const start = ctx.expect("Keyword", "await");
  const argument = parseExpression(ctx);
  skipTerminator(ctx);
  return {
    kind: "Await",
    argument,
    loc: { line: start.line, column: start.column }
  };
}
function parseReturn(ctx) {
  const start = ctx.expect("Keyword", "return");
  let argument;
  const next = ctx.peek();
  if (next.type !== "Newline" && next.type !== "Semicolon" && !(next.type === "Punctuation" && next.value === "}")) {
    argument = parseExpression(ctx);
  }
  skipTerminator(ctx);
  return {
    kind: "Return",
    argument,
    loc: { line: start.line, column: start.column }
  };
}
class ParserContext {
  constructor(tokens) {
    __publicField(this, "index", 0);
    this.tokens = tokens;
  }
  isEnd() {
    return this.peek().type === "EOF";
  }
  peek(offset = 0) {
    return this.tokens[this.index + offset] ?? { type: "EOF", value: "", line: 0, column: 0 };
  }
  consume() {
    const tok = this.tokens[this.index] ?? { type: "EOF", value: "", line: 0, column: 0 };
    this.index += 1;
    return tok;
  }
  match(type, value) {
    const tok = this.peek();
    if (tok.type !== type) return false;
    if (value !== void 0 && tok.value !== value) return false;
    this.consume();
    return true;
  }
  expect(type, value) {
    const tok = this.peek();
    if (tok.type !== type || value !== void 0 && tok.value !== value) {
      throw {
        message: `Expected ${type}${value !== void 0 ? ` "${value}"` : ""} but got ${tok.type} "${tok.value}"`,
        line: tok.line,
        column: tok.column
      };
    }
    return this.consume();
  }
  recoverToNextLine() {
    while (!this.isEnd() && this.peek().type !== "Newline" && this.peek().type !== "Semicolon") this.consume();
    if (this.peek().type === "Newline" || this.peek().type === "Semicolon") this.consume();
  }
  snapshot() {
    return this.index;
  }
  restore(index) {
    this.index = index;
  }
}
function parseAssignment(ctx) {
  const head = ctx.peek();
  let identifier = "";
  let isState = false;
  if (head.type === "Identifier") {
    identifier = ctx.consume().value;
  } else if (head.type === "StateIdentifier") {
    identifier = ctx.consume().value;
    isState = true;
  } else {
    throw {
      message: `Expected identifier at start of statement, got ${head.type} "${head.value}"`,
      line: head.line,
      column: head.column
    };
  }
  const eq = ctx.expect("Operator", "=");
  const expression = parseExpression(ctx);
  skipTerminator(ctx);
  return {
    kind: "Assignment",
    identifier,
    isState,
    expression,
    loc: { line: eq.line, column: eq.column }
  };
}
function parseImportStatement(ctx) {
  const start = ctx.expect("Keyword", "import");
  ctx.expect("Punctuation", "{");
  const specifiers = [];
  skipWhitespace(ctx);
  while (!ctx.isEnd() && !(ctx.peek().type === "Punctuation" && ctx.peek().value === "}")) {
    const importedTok = ctx.peek();
    let imported;
    let isState = false;
    if (importedTok.type === "StateIdentifier") {
      imported = ctx.consume().value;
      isState = true;
    } else if (importedTok.type === "Identifier") {
      imported = ctx.consume().value;
    } else {
      throw {
        message: `Expected an import name, got ${importedTok.type} "${importedTok.value}"`,
        line: importedTok.line,
        column: importedTok.column
      };
    }
    let local = imported;
    if (ctx.peek().type === "Identifier" && ctx.peek().value === "as") {
      ctx.consume();
      const aliasTok = ctx.peek();
      let aliasIsState = false;
      if (aliasTok.type === "StateIdentifier") {
        local = ctx.consume().value;
        aliasIsState = true;
      } else if (aliasTok.type === "Identifier") {
        local = ctx.consume().value;
      } else {
        throw {
          message: `Expected an alias after \`as\`, got ${aliasTok.type} "${aliasTok.value}"`,
          line: aliasTok.line,
          column: aliasTok.column
        };
      }
      if (aliasIsState !== isState) {
        throw {
          message: "A `$state` import must keep its `$` across `as` (e.g. `{ $x as $y }`); a non-state import must not gain one.",
          line: aliasTok.line,
          column: aliasTok.column
        };
      }
    }
    specifiers.push(isState ? { imported, local, isState: true } : { imported, local });
    skipWhitespace(ctx);
    if (ctx.peek().type === "Punctuation" && ctx.peek().value === ",") {
      ctx.consume();
      skipWhitespace(ctx);
      continue;
    }
    break;
  }
  ctx.expect("Punctuation", "}");
  const fromTok = ctx.peek();
  if (!(fromTok.type === "Identifier" && fromTok.value === "from")) {
    throw {
      message: `Expected \`from\` after import specifiers, got ${fromTok.type} "${fromTok.value}"`,
      line: fromTok.line,
      column: fromTok.column
    };
  }
  ctx.consume();
  const sourceTok = ctx.expect("String");
  skipTerminator(ctx);
  return {
    kind: "Import",
    specifiers,
    source: sourceTok.value,
    loc: { line: start.line, column: start.column }
  };
}
function parseExportStatement(ctx) {
  const start = ctx.expect("Keyword", "export");
  const next = ctx.peek();
  if (next.type === "Punctuation" && next.value === "{") {
    throw {
      message: "`export { … }` lists are not supported yet — use inline `export <declaration>` (e.g. `export function Foo() {…}`, `export $count = 0`).",
      line: next.line,
      column: next.column
    };
  }
  let stmt;
  if (next.type === "Keyword" && next.value === "function") {
    stmt = parseFunctionDecl(ctx);
  } else if (next.type === "Keyword" && next.value === "async" && ctx.peek(1).type === "Keyword" && ctx.peek(1).value === "function") {
    ctx.consume();
    stmt = parseFunctionDecl(ctx);
  } else if (next.type === "Keyword" && (next.value === "let" || next.value === "const" || next.value === "var")) {
    stmt = parseVarDecl(ctx);
    if (stmt && stmt.kind === "DestructureStatement") {
      throw {
        message: "`export` of a destructuring declaration is not supported — export named bindings individually.",
        line: next.line,
        column: next.column
      };
    }
  } else if (couldStartAssignment(ctx)) {
    stmt = parseAssignment(ctx);
  } else {
    throw {
      message: "`export` must be followed by a declaration or assignment (`export function …`, `export let x = …`, `export $state = …`).",
      line: next.line,
      column: next.column
    };
  }
  if (stmt && (stmt.kind === "Assignment" || stmt.kind === "ComponentDeclaration" || stmt.kind === "ActionDeclaration" || stmt.kind === "HookDeclaration")) {
    stmt.exported = true;
    return stmt;
  }
  throw {
    message: "`export` must be followed by a declaration or assignment.",
    line: start.line,
    column: start.column
  };
}
function parseExpression(ctx) {
  return parseTernary(ctx);
}
function peekNonNewline(ctx) {
  let i = 0;
  while (true) {
    const t = ctx.peek(i);
    if (t.type === "Newline") {
      i += 1;
      continue;
    }
    return { token: t, skipped: i };
  }
}
function consumeNewlinesIfNext(ctx, predicate) {
  const { token, skipped } = peekNonNewline(ctx);
  if (!predicate(token)) return false;
  for (let i = 0; i < skipped; i += 1) ctx.consume();
  return true;
}
function operatorLoc(tok) {
  return { line: tok.line, column: tok.column };
}
const TEMPLATE_SUB_PREFIX = "__rui_tmpl__ = ";
function rebaseTemplateLocations(root, line, column) {
  const exprStartColumn = column + "${".length;
  walkNode(root, ({ node }) => {
    const loc = node.loc;
    if (!loc) return;
    if (loc.line === 1) {
      loc.column = exprStartColumn + (loc.column - (TEMPLATE_SUB_PREFIX.length + 1));
    }
    loc.line = line + (loc.line - 1);
  });
}
function parseTernary(ctx) {
  const test = parseLogicalOr(ctx);
  if (consumeNewlinesIfNext(ctx, (t) => t.type === "Punctuation" && t.value === "?")) {
    const question = ctx.consume();
    skipWhitespace(ctx);
    const consequent = parseExpression(ctx);
    skipWhitespace(ctx);
    ctx.expect("Punctuation", ":");
    skipWhitespace(ctx);
    const alternate = parseExpression(ctx);
    return { kind: "Ternary", test, consequent, alternate, loc: operatorLoc(question) };
  }
  return test;
}
function parseLogicalOr(ctx) {
  let left = parseLogicalAnd(ctx);
  while (consumeNewlinesIfNext(
    ctx,
    (t) => t.type === "Operator" && (t.value === "||" || t.value === "??")
  )) {
    const tok = ctx.consume();
    skipWhitespace(ctx);
    const right = parseLogicalAnd(ctx);
    left = { kind: "Binary", operator: tok.value, left, right, loc: operatorLoc(tok) };
  }
  return left;
}
function parseLogicalAnd(ctx) {
  let left = parseBitwiseOr(ctx);
  while (consumeNewlinesIfNext(ctx, (t) => t.type === "Operator" && t.value === "&&")) {
    const tok = ctx.consume();
    skipWhitespace(ctx);
    const right = parseBitwiseOr(ctx);
    left = { kind: "Binary", operator: "&&", left, right, loc: operatorLoc(tok) };
  }
  return left;
}
function parseBitwiseOr(ctx) {
  let left = parseBitwiseXor(ctx);
  while (consumeNewlinesIfNext(ctx, (t) => t.type === "Operator" && t.value === "|")) {
    const tok = ctx.consume();
    skipWhitespace(ctx);
    const right = parseBitwiseXor(ctx);
    left = { kind: "Binary", operator: "|", left, right, loc: operatorLoc(tok) };
  }
  return left;
}
function parseBitwiseXor(ctx) {
  let left = parseBitwiseAnd(ctx);
  while (consumeNewlinesIfNext(ctx, (t) => t.type === "Operator" && t.value === "^")) {
    const tok = ctx.consume();
    skipWhitespace(ctx);
    const right = parseBitwiseAnd(ctx);
    left = { kind: "Binary", operator: "^", left, right, loc: operatorLoc(tok) };
  }
  return left;
}
function parseBitwiseAnd(ctx) {
  let left = parseEquality(ctx);
  while (consumeNewlinesIfNext(ctx, (t) => t.type === "Operator" && t.value === "&")) {
    const tok = ctx.consume();
    skipWhitespace(ctx);
    const right = parseEquality(ctx);
    left = { kind: "Binary", operator: "&", left, right, loc: operatorLoc(tok) };
  }
  return left;
}
function parseEquality(ctx) {
  let left = parseComparison(ctx);
  while (consumeNewlinesIfNext(
    ctx,
    (t) => t.type === "Operator" && (t.value === "==" || t.value === "!=" || t.value === "===" || t.value === "!==")
  )) {
    const tok = ctx.consume();
    skipWhitespace(ctx);
    const right = parseComparison(ctx);
    left = {
      kind: "Binary",
      operator: tok.value,
      left,
      right,
      loc: operatorLoc(tok)
    };
  }
  return left;
}
function parseComparison(ctx) {
  let left = parseShift(ctx);
  while (true) {
    if (consumeNewlinesIfNext(
      ctx,
      (t) => t.type === "Operator" && (t.value === ">" || t.value === "<" || t.value === ">=" || t.value === "<=")
    )) {
      const tok = ctx.consume();
      skipWhitespace(ctx);
      const right = parseShift(ctx);
      left = {
        kind: "Binary",
        operator: tok.value,
        left,
        right,
        loc: operatorLoc(tok)
      };
      continue;
    }
    if (consumeNewlinesIfNext(ctx, (t) => t.type === "Keyword" && (t.value === "instanceof" || t.value === "in"))) {
      const tok = ctx.consume();
      skipWhitespace(ctx);
      const right = parseShift(ctx);
      left = {
        kind: "Binary",
        operator: tok.value,
        left,
        right,
        loc: operatorLoc(tok)
      };
      continue;
    }
    break;
  }
  return left;
}
function parseShift(ctx) {
  let left = parseAdditive(ctx);
  while (consumeNewlinesIfNext(
    ctx,
    (t) => t.type === "Operator" && (t.value === "<<" || t.value === ">>" || t.value === ">>>")
  )) {
    const tok = ctx.consume();
    skipWhitespace(ctx);
    const right = parseAdditive(ctx);
    left = {
      kind: "Binary",
      operator: tok.value,
      left,
      right,
      loc: operatorLoc(tok)
    };
  }
  return left;
}
function parseAdditive(ctx) {
  let left = parseMultiplicative(ctx);
  while (consumeNewlinesIfNext(ctx, (t) => t.type === "Operator" && (t.value === "+" || t.value === "-"))) {
    const tok = ctx.consume();
    skipWhitespace(ctx);
    const right = parseMultiplicative(ctx);
    left = { kind: "Binary", operator: tok.value, left, right, loc: operatorLoc(tok) };
  }
  return left;
}
function parseMultiplicative(ctx) {
  let left = parseExponent(ctx);
  while (consumeNewlinesIfNext(
    ctx,
    (t) => t.type === "Operator" && (t.value === "*" || t.value === "/" || t.value === "%")
  )) {
    const tok = ctx.consume();
    skipWhitespace(ctx);
    const right = parseExponent(ctx);
    left = {
      kind: "Binary",
      operator: tok.value,
      left,
      right,
      loc: operatorLoc(tok)
    };
  }
  return left;
}
function parseExponent(ctx) {
  const left = parseUnary(ctx);
  if (consumeNewlinesIfNext(ctx, (t) => t.type === "Operator" && t.value === "**")) {
    const tok = ctx.consume();
    skipWhitespace(ctx);
    const right = parseExponent(ctx);
    return { kind: "Binary", operator: "**", left, right, loc: operatorLoc(tok) };
  }
  return left;
}
function parseUnary(ctx) {
  const tok = ctx.peek();
  if (tok.type === "Operator" && (tok.value === "!" || tok.value === "-" || tok.value === "+" || tok.value === "~")) {
    ctx.consume();
    const argument = parseUnary(ctx);
    return { kind: "Unary", operator: tok.value, argument };
  }
  if (tok.type === "Keyword" && tok.value === "await") {
    ctx.consume();
    const argument = parseUnary(ctx);
    return {
      kind: "BuiltinCall",
      name: "__rui_await__",
      arguments: [argument],
      loc: { line: tok.line, column: tok.column }
    };
  }
  if (tok.type === "Operator" && (tok.value === "++" || tok.value === "--")) {
    ctx.consume();
    const argument = parseUnary(ctx);
    return {
      kind: "BuiltinCall",
      name: "__rui_prefix__",
      arguments: [
        argument,
        { kind: "Literal", value: tok.value }
      ],
      loc: { line: tok.line, column: tok.column }
    };
  }
  if (tok.type === "Keyword" && (tok.value === "typeof" || tok.value === "void" || tok.value === "delete")) {
    ctx.consume();
    const argument = parseUnary(ctx);
    return {
      kind: "Unary",
      operator: tok.value,
      argument
    };
  }
  if (tok.type === "Keyword" && tok.value === "new") {
    ctx.consume();
    let callee = parsePrimary(ctx);
    let args = [];
    if (callee.kind === "Call") {
      args = callee.arguments;
      callee = { kind: "Identifier", name: callee.callee, loc: callee.loc };
    } else {
      while (true) {
        const t = ctx.peek();
        if (t.type === "Punctuation" && t.value === ".") {
          ctx.consume();
          const propTok = ctx.consume();
          if (propTok.type !== "Identifier" && propTok.type !== "Keyword") {
            throw {
              message: `Expected Identifier but got ${propTok.type} "${propTok.value}"`,
              line: propTok.line,
              column: propTok.column
            };
          }
          callee = { kind: "Member", object: callee, property: propTok.value };
          continue;
        }
        if (t.type === "Punctuation" && t.value === "[") {
          ctx.consume();
          const computed = parseExpression(ctx);
          ctx.expect("Punctuation", "]");
          callee = { kind: "Member", object: callee, computed };
          continue;
        }
        break;
      }
      if (ctx.peek().type === "Punctuation" && ctx.peek().value === "(") {
        ctx.consume();
        args = parseCallArgs(ctx);
        ctx.expect("Punctuation", ")");
      }
    }
    const newNode = {
      kind: "New",
      callee,
      arguments: args,
      loc: { line: tok.line, column: tok.column }
    };
    return parsePostfixFrom(ctx, newNode);
  }
  return parsePostfixWithIncDec(ctx);
}
function parsePostfixWithIncDec(ctx) {
  const expr = parsePostfix(ctx);
  const tok = ctx.peek();
  if (tok.type === "Operator" && (tok.value === "++" || tok.value === "--")) {
    ctx.consume();
    return {
      kind: "BuiltinCall",
      name: "__rui_postfix__",
      arguments: [
        expr,
        { kind: "Literal", value: tok.value }
      ],
      loc: { line: tok.line, column: tok.column }
    };
  }
  return expr;
}
function parsePostfix(ctx) {
  return parsePostfixFrom(ctx, parsePrimary(ctx));
}
function parsePostfixFrom(ctx, base) {
  let expr = base;
  while (true) {
    consumeNewlinesIfNext(
      ctx,
      (t) => t.type === "Punctuation" && t.value === "." || t.type === "Operator" && t.value === "?."
    );
    const tok = ctx.peek();
    if (tok.type === "Punctuation" && tok.value === ".") {
      ctx.consume();
      const propTok = ctx.consume();
      if (propTok.type !== "Identifier" && propTok.type !== "Keyword" && propTok.type !== "StateIdentifier") {
        throw {
          message: `Expected Identifier but got ${propTok.type} "${propTok.value}"`,
          line: propTok.line,
          column: propTok.column
        };
      }
      const after = ctx.peek();
      if (after.type === "Punctuation" && after.value === "(") {
        ctx.consume();
        const args = parseCallArgs(ctx);
        ctx.expect("Punctuation", ")");
        expr = {
          kind: "MethodCall",
          object: expr,
          method: propTok.value,
          arguments: args,
          loc: { line: propTok.line, column: propTok.column }
        };
        continue;
      }
      expr = { kind: "Member", object: expr, property: propTok.value };
      continue;
    }
    if (tok.type === "Operator" && tok.value === "?.") {
      ctx.consume();
      if (ctx.peek().type === "Punctuation" && ctx.peek().value === "[") {
        ctx.consume();
        const computed = parseExpression(ctx);
        ctx.expect("Punctuation", "]");
        expr = { kind: "Member", object: expr, computed, optional: true };
      } else if (ctx.peek().type === "Punctuation" && ctx.peek().value === "(") {
        ctx.consume();
        const args = parseCallArgs(ctx);
        ctx.expect("Punctuation", ")");
        expr = {
          kind: "Invoke",
          callee: expr,
          arguments: args,
          optional: true,
          loc: { line: tok.line, column: tok.column }
        };
      } else {
        const propTok = ctx.consume();
        if (propTok.type !== "Identifier" && propTok.type !== "Keyword" && propTok.type !== "StateIdentifier") {
          throw {
            message: `Expected Identifier but got ${propTok.type} "${propTok.value}"`,
            line: propTok.line,
            column: propTok.column
          };
        }
        const after = ctx.peek();
        if (after.type === "Punctuation" && after.value === "(") {
          ctx.consume();
          const args = parseCallArgs(ctx);
          ctx.expect("Punctuation", ")");
          expr = {
            kind: "MethodCall",
            object: expr,
            method: propTok.value,
            arguments: args,
            optional: true,
            loc: { line: propTok.line, column: propTok.column }
          };
          continue;
        }
        expr = { kind: "Member", object: expr, property: propTok.value, optional: true };
      }
      continue;
    }
    if (tok.type === "Punctuation" && tok.value === "[") {
      ctx.consume();
      const computed = parseExpression(ctx);
      ctx.expect("Punctuation", "]");
      expr = { kind: "Member", object: expr, computed };
      continue;
    }
    if (tok.type === "Punctuation" && tok.value === "(") {
      ctx.consume();
      const args = parseCallArgs(ctx);
      ctx.expect("Punctuation", ")");
      expr = {
        kind: "Invoke",
        callee: expr,
        arguments: args,
        loc: { line: tok.line, column: tok.column }
      };
      continue;
    }
    break;
  }
  return expr;
}
function parsePrimary(ctx) {
  const tok = ctx.peek();
  if (tok.type === "Keyword" && (tok.value === "if" || tok.value === "for" || tok.value === "switch" || tok.value === "while" || tok.value === "try")) {
    const hint = tok.value === "if" ? "Use the ternary operator (`cond ? a : b`) when you need a value." : tok.value === "for" ? "Use `arr.map(x => …)` (or `.filter`, `.reduce`, …) to collect bodies into an array." : tok.value === "switch" ? "Use chained ternaries, an object lookup, or wrap the switch inside a `function`." : `Use the ${tok.value} statement inside a function / effect body.`;
    const err = {
      message: `\`${tok.value}\` is a statement, not an expression. ${hint}`,
      line: tok.line,
      column: tok.column
    };
    err.__definitive = true;
    throw err;
  }
  if (tok.type === "Keyword") {
    if (tok.value === "function") {
      const lookahead = ctx.peek(1);
      const lookahead2 = ctx.peek(2);
      const looksLikeFunctionExpr = lookahead.type === "Punctuation" && lookahead.value === "(" || lookahead.type === "Identifier" && lookahead2.type === "Punctuation" && lookahead2.value === "(";
      if (looksLikeFunctionExpr) {
        const start = tok;
        ctx.consume();
        if (ctx.peek().type === "Identifier") ctx.consume();
        const params = parseFunctionParams(ctx);
        const body = parseBlock(ctx);
        return {
          kind: "Lambda",
          params,
          body,
          loc: { line: start.line, column: start.column }
        };
      }
    }
    if (tok.value === "function" || tok.value === "let" || tok.value === "const" || tok.value === "var" || tok.value === "of" || tok.value === "in" || tok.value === "case" || tok.value === "break" || tok.value === "continue" || tok.value === "default") {
      ctx.consume();
      if (ctx.peek().type === "Punctuation" && ctx.peek().value === "(") {
        ctx.consume();
        const args = parseCallArgs(ctx);
        ctx.expect("Punctuation", ")");
        return {
          kind: "Call",
          callee: tok.value,
          arguments: args,
          loc: { line: tok.line, column: tok.column }
        };
      }
      return { kind: "Identifier", name: tok.value, loc: { line: tok.line, column: tok.column } };
    }
  }
  if (tok.type === "Number") {
    ctx.consume();
    return { kind: "Literal", value: numericLiteralValue(tok.value) };
  }
  if (tok.type === "String") {
    ctx.consume();
    return { kind: "Literal", value: tok.value };
  }
  if (tok.type === "Regex") {
    ctx.consume();
    const args = [{ kind: "Literal", value: tok.value }];
    if (tok.flags) args.push({ kind: "Literal", value: tok.flags });
    return {
      kind: "New",
      callee: { kind: "Identifier", name: "RegExp" },
      arguments: args,
      loc: { line: tok.line, column: tok.column }
    };
  }
  if (tok.type === "TemplateString") {
    ctx.consume();
    const parts = tok.parts ?? [];
    const quasis = [];
    const expressions = [];
    let pendingChunk = "";
    let hasPendingChunk = false;
    const flushChunk = () => {
      quasis.push(pendingChunk);
      pendingChunk = "";
      hasPendingChunk = false;
    };
    for (const part of parts) {
      if (part.kind === "str") {
        pendingChunk += part.text;
        hasPendingChunk = true;
        continue;
      }
      if (!hasPendingChunk) {
        quasis.push("");
      } else {
        flushChunk();
      }
      const sub = parse(`${TEMPLATE_SUB_PREFIX}${part.source}`);
      const firstStmt = sub.statements[0];
      if (firstStmt && firstStmt.kind === "Assignment") {
        rebaseTemplateLocations(firstStmt.expression, part.line, part.column);
        expressions.push(firstStmt.expression);
      } else {
        expressions.push({ kind: "Literal", value: "" });
      }
    }
    if (hasPendingChunk || quasis.length === 0) {
      quasis.push(pendingChunk);
    }
    while (quasis.length <= expressions.length) quasis.push("");
    return {
      kind: "Template",
      quasis,
      expressions,
      loc: { line: tok.line, column: tok.column }
    };
  }
  if (tok.type === "Boolean") {
    ctx.consume();
    return { kind: "Literal", value: tok.value === "true" };
  }
  if (tok.type === "Null") {
    ctx.consume();
    return { kind: "Literal", value: null };
  }
  if (tok.type === "StateIdentifier") {
    if (tok.value === "effect" && ctx.peek(1).type === "Punctuation" && ctx.peek(1).value === "(") {
      ctx.consume();
      return parseEffectCallAsExpr(ctx, tok);
    }
    ctx.consume();
    return { kind: "StateRef", name: tok.value };
  }
  if (tok.type === "Identifier") {
    ctx.consume();
    if (ctx.peek().type === "Operator" && ctx.peek().value === "=>") {
      ctx.consume();
      const body = parseLambdaBody(ctx);
      return {
        kind: "Lambda",
        params: [{ name: tok.value }],
        body,
        loc: { line: tok.line, column: tok.column }
      };
    }
    if (ctx.peek().type === "Punctuation" && ctx.peek().value === "(") {
      ctx.consume();
      const args = parseCallArgs(ctx);
      ctx.expect("Punctuation", ")");
      return {
        kind: "Call",
        callee: tok.value,
        arguments: args,
        loc: { line: tok.line, column: tok.column }
      };
    }
    return {
      kind: "Identifier",
      name: tok.value,
      loc: { line: tok.line, column: tok.column }
    };
  }
  if (tok.type === "Punctuation" && tok.value === "[") {
    ctx.consume();
    const elements = parseCallArgs(ctx);
    ctx.expect("Punctuation", "]");
    return { kind: "Array", elements };
  }
  if (tok.type === "Punctuation" && tok.value === "{") {
    ctx.consume();
    const properties = parseObjectProps(ctx);
    ctx.expect("Punctuation", "}");
    return { kind: "Object", properties };
  }
  if (tok.type === "Punctuation" && tok.value === "(") {
    const saved = ctx.snapshot();
    const lambda = tryParseLambdaFromParenList(ctx);
    if (lambda) return lambda;
    ctx.restore(saved);
    ctx.consume();
    const expr = parseExpression(ctx);
    ctx.expect("Punctuation", ")");
    return expr;
  }
  throw {
    message: `Unexpected token ${tok.type} "${tok.value}"`,
    line: tok.line,
    column: tok.column
  };
}
function parseEffectCallAsExpr(ctx, nameTok) {
  ctx.consume();
  const args = parseCallArgs(ctx);
  ctx.expect("Punctuation", ")");
  return {
    kind: "Call",
    callee: nameTok.value,
    arguments: args,
    loc: { line: nameTok.line, column: nameTok.column }
  };
}
function parseCallArgs(ctx) {
  const args = [];
  skipWhitespace(ctx);
  if (ctx.peek().type === "Punctuation" && (ctx.peek().value === ")" || ctx.peek().value === "]")) {
    return args;
  }
  args.push(parseArgItem(ctx));
  skipWhitespace(ctx);
  while (ctx.peek().type === "Punctuation" && ctx.peek().value === ",") {
    ctx.consume();
    skipWhitespace(ctx);
    if (ctx.peek().type === "Punctuation" && (ctx.peek().value === ")" || ctx.peek().value === "]")) {
      break;
    }
    args.push(parseArgItem(ctx));
    skipWhitespace(ctx);
  }
  skipWhitespace(ctx);
  return args;
}
function parseArgItem(ctx) {
  if (ctx.peek().type === "Operator" && ctx.peek().value === "...") {
    const tok = ctx.consume();
    const argument = parseExpression(ctx);
    return { kind: "Spread", argument, loc: { line: tok.line, column: tok.column } };
  }
  return parseExpression(ctx);
}
function parseIfStatement(ctx) {
  const start = ctx.expect("Keyword", "if");
  ctx.expect("Punctuation", "(");
  const test = parseExpression(ctx);
  ctx.expect("Punctuation", ")");
  const consequent = parseBlockOrSingleStatement(ctx);
  let alternate;
  skipWhitespace(ctx);
  if (ctx.peek().type === "Keyword" && ctx.peek().value === "else") {
    ctx.consume();
    skipWhitespace(ctx);
    if (ctx.peek().type === "Keyword" && ctx.peek().value === "if") {
      alternate = parseIfStatement(ctx);
    } else {
      alternate = parseBlockOrSingleStatement(ctx);
    }
  }
  skipTerminator(ctx);
  return {
    kind: "IfStatement",
    test,
    consequent,
    alternate,
    loc: { line: start.line, column: start.column }
  };
}
function parseSwitchStatement(ctx) {
  const start = ctx.expect("Keyword", "switch");
  ctx.expect("Punctuation", "(");
  const discriminant = parseExpression(ctx);
  ctx.expect("Punctuation", ")");
  ctx.expect("Punctuation", "{");
  const cases = [];
  skipWhitespace(ctx);
  while (!(ctx.peek().type === "Punctuation" && ctx.peek().value === "}")) {
    let test = null;
    if (ctx.peek().type === "Keyword" && ctx.peek().value === "case") {
      ctx.consume();
      test = parseExpression(ctx);
    } else if (ctx.peek().type === "Keyword" && ctx.peek().value === "default") {
      ctx.consume();
      test = null;
    } else {
      throw {
        message: `Expected "case" or "default" in switch body, got ${ctx.peek().type} "${ctx.peek().value}"`,
        line: ctx.peek().line,
        column: ctx.peek().column
      };
    }
    ctx.expect("Punctuation", ":");
    skipWhitespace(ctx);
    const body = [];
    while (!ctx.isEnd() && !(ctx.peek().type === "Keyword" && (ctx.peek().value === "case" || ctx.peek().value === "default")) && !(ctx.peek().type === "Punctuation" && ctx.peek().value === "}")) {
      const stmt = parseStatement(ctx);
      if (stmt) body.push(stmt);
      skipWhitespace(ctx);
    }
    cases.push({ test, body });
    skipWhitespace(ctx);
  }
  ctx.expect("Punctuation", "}");
  skipTerminator(ctx);
  return {
    kind: "SwitchStatement",
    discriminant,
    cases,
    loc: { line: start.line, column: start.column }
  };
}
function parseForStatement(ctx) {
  const start = ctx.expect("Keyword", "for");
  ctx.expect("Punctuation", "(");
  skipWhitespace(ctx);
  const headSnapshot = ctx.snapshot();
  let kind = "for-of";
  {
    let depth = 1;
    let i = 0;
    while (true) {
      const tok = ctx.peek(i);
      if (tok.type === "EOF") break;
      if (tok.type === "Punctuation" && tok.value === "(") depth += 1;
      else if (tok.type === "Punctuation" && tok.value === ")") {
        depth -= 1;
        if (depth === 0) break;
      } else if (depth === 1 && tok.type === "Semicolon") {
        kind = "classic";
        break;
      } else if (depth === 1 && tok.type === "Keyword" && tok.value === "of") {
        kind = "for-of";
        break;
      } else if (depth === 1 && tok.type === "Keyword" && tok.value === "in") {
        kind = "for-in";
        break;
      }
      i += 1;
    }
  }
  ctx.restore(headSnapshot);
  if (kind === "classic") return parseForClassic(ctx, start);
  if (ctx.peek().type === "Keyword" && (ctx.peek().value === "let" || ctx.peek().value === "const" || ctx.peek().value === "var")) {
    ctx.consume();
  }
  skipWhitespace(ctx);
  let item = "__row";
  let pattern;
  if (ctx.peek().type === "Punctuation" && (ctx.peek().value === "[" || ctx.peek().value === "{")) {
    pattern = parseDestructuringPattern(ctx);
  } else {
    item = ctx.expect("Identifier").value;
  }
  if (kind === "for-in") {
    ctx.expect("Keyword", "in");
  } else {
    ctx.expect("Keyword", "of");
  }
  const iterable = parseExpression(ctx);
  ctx.expect("Punctuation", ")");
  const body = parseBlockOrSingleStatement(ctx);
  skipTerminator(ctx);
  if (kind === "for-in") {
    return {
      kind: "ForInStatement",
      item,
      iterable,
      body,
      loc: { line: start.line, column: start.column }
    };
  }
  return {
    kind: "ForOfStatement",
    item,
    pattern,
    iterable,
    body,
    loc: { line: start.line, column: start.column }
  };
}
function parseForClassic(ctx, start) {
  let init;
  if (!(ctx.peek().type === "Semicolon")) {
    if (ctx.peek().type === "Keyword" && (ctx.peek().value === "let" || ctx.peek().value === "const" || ctx.peek().value === "var")) {
      const decl = parseVarDecl(ctx);
      if (decl.kind === "Assignment") init = decl;
      skipWhitespace(ctx);
    } else {
      const exprStart = ctx.peek();
      const expression = parseExpression(ctx);
      init = {
        kind: "ExpressionStatement",
        expression,
        loc: { line: exprStart.line, column: exprStart.column }
      };
      ctx.expect("Semicolon");
      skipWhitespace(ctx);
    }
  } else {
    ctx.expect("Semicolon");
    skipWhitespace(ctx);
  }
  let test;
  if (!(ctx.peek().type === "Semicolon")) {
    test = parseExpression(ctx);
  }
  ctx.expect("Semicolon");
  skipWhitespace(ctx);
  let update;
  if (!(ctx.peek().type === "Punctuation" && ctx.peek().value === ")")) {
    update = parseAssignmentLikeExpression(ctx);
  }
  ctx.expect("Punctuation", ")");
  const body = parseBlockOrSingleStatement(ctx);
  skipTerminator(ctx);
  return {
    kind: "ForClassicStatement",
    init,
    test,
    update,
    body,
    loc: { line: start.line, column: start.column }
  };
}
function parseWhileStatement(ctx) {
  const start = ctx.expect("Keyword", "while");
  ctx.expect("Punctuation", "(");
  const test = parseExpression(ctx);
  ctx.expect("Punctuation", ")");
  const body = parseBlockOrSingleStatement(ctx);
  skipTerminator(ctx);
  return {
    kind: "WhileStatement",
    test,
    body,
    loc: { line: start.line, column: start.column }
  };
}
function parseDoWhileStatement(ctx) {
  const start = ctx.expect("Keyword", "do");
  const body = parseBlockOrSingleStatement(ctx);
  skipWhitespace(ctx);
  ctx.expect("Keyword", "while");
  ctx.expect("Punctuation", "(");
  const test = parseExpression(ctx);
  ctx.expect("Punctuation", ")");
  skipTerminator(ctx);
  return {
    kind: "DoWhileStatement",
    test,
    body,
    loc: { line: start.line, column: start.column }
  };
}
function parseBreakStatement(ctx) {
  const start = ctx.expect("Keyword", "break");
  skipTerminator(ctx);
  return { kind: "BreakStatement", loc: { line: start.line, column: start.column } };
}
function parseContinueStatement(ctx) {
  const start = ctx.expect("Keyword", "continue");
  skipTerminator(ctx);
  return { kind: "ContinueStatement", loc: { line: start.line, column: start.column } };
}
function parseThrowStatement(ctx) {
  const start = ctx.expect("Keyword", "throw");
  const argument = parseExpression(ctx);
  skipTerminator(ctx);
  return {
    kind: "ThrowStatement",
    argument,
    loc: { line: start.line, column: start.column }
  };
}
function parseTryStatement(ctx) {
  const start = ctx.expect("Keyword", "try");
  const block = parseBlock(ctx);
  let catchParam;
  let catchBlock;
  let finallyBlock;
  skipWhitespace(ctx);
  if (ctx.peek().type === "Keyword" && ctx.peek().value === "catch") {
    ctx.consume();
    if (ctx.peek().type === "Punctuation" && ctx.peek().value === "(") {
      ctx.consume();
      if (ctx.peek().type === "Identifier") {
        catchParam = ctx.consume().value;
      }
      ctx.expect("Punctuation", ")");
    }
    catchBlock = parseBlock(ctx);
    skipWhitespace(ctx);
  }
  if (ctx.peek().type === "Keyword" && ctx.peek().value === "finally") {
    ctx.consume();
    finallyBlock = parseBlock(ctx);
  }
  skipTerminator(ctx);
  return {
    kind: "TryStatement",
    block,
    catchParam,
    catchBlock,
    finallyBlock,
    loc: { line: start.line, column: start.column }
  };
}
function tryParseLambdaFromParenList(ctx) {
  const start = ctx.peek();
  if (start.type !== "Punctuation" || start.value !== "(") return null;
  ctx.consume();
  const params = [];
  skipWhitespace(ctx);
  if (!(ctx.peek().type === "Punctuation" && ctx.peek().value === ")")) {
    while (true) {
      skipWhitespace(ctx);
      let isRest = false;
      if (ctx.peek().type === "Operator" && ctx.peek().value === "...") {
        ctx.consume();
        isRest = true;
      }
      const tok = ctx.peek();
      if (!isRest && tok.type === "Punctuation" && (tok.value === "{" || tok.value === "[")) {
        let pattern;
        try {
          pattern = parseDestructuringPattern(ctx);
        } catch {
          return null;
        }
        const param2 = { name: "", pattern };
        if (ctx.peek().type === "Operator" && ctx.peek().value === "=") {
          ctx.consume();
          try {
            param2.defaultValue = parseExpression(ctx);
          } catch {
            return null;
          }
        }
        params.push(param2);
        skipWhitespace(ctx);
        if (ctx.peek().type === "Punctuation" && ctx.peek().value === ",") {
          ctx.consume();
          continue;
        }
        break;
      }
      if (tok.type !== "Identifier") return null;
      ctx.consume();
      const param = { name: tok.value };
      if (isRest) param.rest = true;
      if (!isRest && ctx.peek().type === "Operator" && ctx.peek().value === "=") {
        ctx.consume();
        try {
          param.defaultValue = parseExpression(ctx);
        } catch {
          return null;
        }
      }
      params.push(param);
      skipWhitespace(ctx);
      if (ctx.peek().type === "Punctuation" && ctx.peek().value === ",") {
        if (isRest) return null;
        ctx.consume();
        continue;
      }
      break;
    }
  }
  skipWhitespace(ctx);
  if (!(ctx.peek().type === "Punctuation" && ctx.peek().value === ")")) {
    return null;
  }
  ctx.consume();
  if (!(ctx.peek().type === "Operator" && ctx.peek().value === "=>")) {
    return null;
  }
  ctx.consume();
  const body = parseLambdaBody(ctx);
  return {
    kind: "Lambda",
    params,
    body,
    loc: { line: start.line, column: start.column }
  };
}
function parseLambdaBody(ctx) {
  skipWhitespace(ctx);
  if (ctx.peek().type === "Punctuation" && ctx.peek().value === "{") {
    return parseBlock(ctx);
  }
  return parseAssignmentLikeExpression(ctx);
}
function parseAssignmentLikeExpression(ctx) {
  const expression = parseExpression(ctx);
  const next = ctx.peek();
  if (next.type === "Operator") {
    if (isAssignmentOperator(next.value)) {
      ctx.consume();
      const value = parseExpression(ctx);
      return {
        kind: "BuiltinCall",
        name: "__rui_assign__",
        arguments: [
          expression,
          value,
          { kind: "Literal", value: next.value }
        ],
        loc: { line: next.line, column: next.column }
      };
    }
    if (next.value === "++" || next.value === "--") {
      ctx.consume();
      return {
        kind: "BuiltinCall",
        name: "__rui_postfix__",
        arguments: [
          expression,
          { kind: "Literal", value: next.value }
        ],
        loc: { line: next.line, column: next.column }
      };
    }
  }
  return expression;
}
function parseObjectProps(ctx) {
  const props = [];
  skipWhitespace(ctx);
  if (ctx.peek().type === "Punctuation" && ctx.peek().value === "}") return props;
  while (true) {
    skipWhitespace(ctx);
    const keyTok = ctx.peek();
    if (keyTok.type === "Operator" && keyTok.value === "...") {
      ctx.consume();
      const value2 = parseExpression(ctx);
      props.push({ key: "", value: value2, spread: true });
      skipWhitespace(ctx);
      if (ctx.peek().type === "Punctuation" && ctx.peek().value === ",") {
        ctx.consume();
        skipWhitespace(ctx);
        if (ctx.peek().type === "Punctuation" && ctx.peek().value === "}") break;
        continue;
      }
      break;
    }
    let key;
    let computedKey;
    if (keyTok.type === "Punctuation" && keyTok.value === "[") {
      ctx.consume();
      skipWhitespace(ctx);
      computedKey = parseExpression(ctx);
      skipWhitespace(ctx);
      ctx.expect("Punctuation", "]");
      key = "";
    } else if (keyTok.type === "Number") {
      key = ctx.consume().value;
    } else if (keyTok.type === "Identifier" || keyTok.type === "String" || keyTok.type === "Keyword") {
      key = ctx.consume().value;
    } else {
      throw {
        message: `Expected object key, got ${keyTok.type} "${keyTok.value}"`,
        line: keyTok.line,
        column: keyTok.column
      };
    }
    const after = ctx.peek();
    let value;
    if (!computedKey && keyTok.type === "Identifier" && after.type === "Punctuation" && (after.value === "," || after.value === "}")) {
      value = { kind: "Identifier", name: key, loc: { line: keyTok.line, column: keyTok.column } };
    } else {
      ctx.expect("Punctuation", ":");
      value = parseExpression(ctx);
    }
    const prop2 = { key, value };
    if (computedKey) prop2.computedKey = computedKey;
    props.push(prop2);
    skipWhitespace(ctx);
    if (ctx.peek().type === "Punctuation" && ctx.peek().value === ",") {
      ctx.consume();
      skipWhitespace(ctx);
      if (ctx.peek().type === "Punctuation" && ctx.peek().value === "}") break;
      continue;
    }
    break;
  }
  skipWhitespace(ctx);
  return props;
}
function skipWhitespace(ctx) {
  while (ctx.match("Newline") || ctx.match("Semicolon")) {
  }
}
function skipTerminator(ctx) {
  if (!ctx.isEnd()) {
    ctx.match("Newline") || ctx.match("Semicolon");
  }
}
function numericLiteralValue(raw) {
  let s = raw.replace(/_/g, "");
  let sign = 1;
  if (s.startsWith("-")) {
    sign = -1;
    s = s.slice(1);
  } else if (s.startsWith("+")) {
    s = s.slice(1);
  }
  return sign * Number(s);
}
const indexCache = /* @__PURE__ */ new WeakMap();
function getIndex(library) {
  let index = indexCache.get(library.components);
  if (!index) {
    index = /* @__PURE__ */ new Map();
    for (const spec of library.components) {
      if (spec?.name) index.set(spec.name, spec);
    }
    indexCache.set(library.components, index);
  }
  return index;
}
function findComponent(library, name) {
  return getIndex(library).get(name);
}
const SVG_NS$2 = "http://www.w3.org/2000/svg";
const ALLOWED_ELEMENTS$1 = /* @__PURE__ */ new Set([
  "svg",
  "g",
  "defs",
  "symbol",
  "use",
  "title",
  "desc",
  "metadata",
  "switch",
  // Shapes
  "path",
  "rect",
  "circle",
  "ellipse",
  "line",
  "polyline",
  "polygon",
  // Text
  "text",
  "tspan",
  "textPath",
  // Paint servers / reusable paint
  "linearGradient",
  "radialGradient",
  "stop",
  "pattern",
  "marker",
  "clipPath",
  "mask",
  // Animation (attributeName is restricted separately — see SAFE_ANIMATABLE)
  "animate",
  "animateTransform",
  "animateMotion",
  "mpath",
  // Filters
  "filter",
  "feBlend",
  "feColorMatrix",
  "feComponentTransfer",
  "feComposite",
  "feConvolveMatrix",
  "feDiffuseLighting",
  "feDisplacementMap",
  "feDistantLight",
  "feDropShadow",
  "feFlood",
  "feFuncA",
  "feFuncB",
  "feFuncG",
  "feFuncR",
  "feGaussianBlur",
  "feMerge",
  "feMergeNode",
  "feMorphology",
  "feOffset",
  "fePointLight",
  "feSpecularLighting",
  "feSpotLight",
  "feTile",
  "feTurbulence"
]);
const ALLOWED_ATTRS$1 = /* @__PURE__ */ new Set([
  // Structure / identity
  "id",
  "class",
  "viewbox",
  "preserveaspectratio",
  "xmlns",
  "version",
  "width",
  "height",
  "x",
  "y",
  "dx",
  "dy",
  "rx",
  "ry",
  "cx",
  "cy",
  "r",
  "x1",
  "y1",
  "x2",
  "y2",
  "d",
  "points",
  "transform",
  "transform-origin",
  "gradienttransform",
  "patterntransform",
  // Paint
  "fill",
  "fill-opacity",
  "fill-rule",
  "stroke",
  "stroke-width",
  "stroke-linecap",
  "stroke-linejoin",
  "stroke-dasharray",
  "stroke-dashoffset",
  "stroke-opacity",
  "stroke-miterlimit",
  "opacity",
  "color",
  "stop-color",
  "stop-opacity",
  "offset",
  "gradientunits",
  "patternunits",
  "patterncontentunits",
  "spreadmethod",
  "fx",
  "fy",
  "fr",
  // Text
  "font-family",
  "font-size",
  "font-weight",
  "font-style",
  "font-variant",
  "letter-spacing",
  "word-spacing",
  "text-anchor",
  "dominant-baseline",
  "alignment-baseline",
  "baseline-shift",
  "text-decoration",
  "textlength",
  "lengthadjust",
  "writing-mode",
  "direction",
  "unicode-bidi",
  // Clipping / masking / markers
  "clip-path",
  "clip-rule",
  "mask",
  "maskunits",
  "maskcontentunits",
  "clippathunits",
  "marker-start",
  "marker-mid",
  "marker-end",
  "markerwidth",
  "markerheight",
  "markerunits",
  "refx",
  "refy",
  "orient",
  "overflow",
  "display",
  "visibility",
  "shape-rendering",
  "vector-effect",
  "paint-order",
  "mix-blend-mode",
  "isolation",
  // Filters
  "filter",
  "filterunits",
  "primitiveunits",
  "result",
  "in",
  "in2",
  "mode",
  "type",
  "values",
  "tablevalues",
  "slope",
  "intercept",
  "amplitude",
  "exponent",
  "operator",
  "radius",
  "stddeviation",
  "basefrequency",
  "numoctaves",
  "seed",
  "stitchtiles",
  "scale",
  "xchannelselector",
  "ychannelselector",
  "k1",
  "k2",
  "k3",
  "k4",
  "order",
  "kernelmatrix",
  "divisor",
  "bias",
  "targetx",
  "targety",
  "edgemode",
  "preservealpha",
  "surfacescale",
  "diffuseconstant",
  "specularconstant",
  "specularexponent",
  "azimuth",
  "elevation",
  "pointsatx",
  "pointsaty",
  "pointsatz",
  "limitingconeangle",
  "z",
  "flood-color",
  "flood-opacity",
  "lighting-color",
  "color-interpolation-filters",
  // Animation timing (attributeName is validated against SAFE_ANIMATABLE)
  "attributename",
  "attributetype",
  "from",
  "to",
  "by",
  "dur",
  "begin",
  "end",
  "repeatcount",
  "repeatdur",
  "fill-freeze",
  "calcmode",
  "keytimes",
  "keysplines",
  "additive",
  "accumulate",
  "restart",
  "path",
  "rotate",
  "min",
  "max",
  // Accessibility
  "role",
  "aria-label",
  "aria-labelledby",
  "aria-describedby",
  "aria-hidden"
]);
const SAFE_ANIMATABLE = /* @__PURE__ */ new Set([
  "x",
  "y",
  "cx",
  "cy",
  "r",
  "rx",
  "ry",
  "width",
  "height",
  "d",
  "points",
  "opacity",
  "fill",
  "fill-opacity",
  "stroke",
  "stroke-width",
  "stroke-opacity",
  "stroke-dasharray",
  "stroke-dashoffset",
  "transform",
  "offset",
  "stop-color",
  "stop-opacity",
  "visibility",
  "display",
  "font-size",
  "x1",
  "y1",
  "x2",
  "y2",
  "gradientTransform",
  "patternTransform"
]);
const FRAGMENT_ONLY_HREF = /* @__PURE__ */ new Set(["use", "mpath", "textPath"]);
const MAX_INPUT_LENGTH$1 = 64 * 1024;
const MAX_NODES$1 = 4096;
const MAX_DEPTH$1 = 32;
let inertDoc$1 = null;
function getInertDocument$1() {
  if (inertDoc$1) return inertDoc$1;
  try {
    if (typeof document === "undefined" || !document.implementation?.createHTMLDocument) return null;
    inertDoc$1 = document.implementation.createHTMLDocument("");
    return inertDoc$1;
  } catch {
    return null;
  }
}
function safeSvgHref(value, localName) {
  const cleaned = value.replace(/[\u0000-\u001F\u007F]/g, "").trim();
  if (!cleaned.startsWith("#")) return null;
  if (!FRAGMENT_ONLY_HREF.has(localName)) return null;
  if (!/^#[A-Za-z_][\w.:-]*$/.test(cleaned)) return null;
  return cleaned;
}
function safeSvgStyle(value) {
  const raw = value.trim();
  if (!raw || raw.length > 512) return null;
  if (/[<>{}@\\"']/.test(raw)) return null;
  if (/url\s*\(|expression\s*\(|behavior\s*:|image-set\s*\(|element\s*\(|--/i.test(raw)) return null;
  if (!/^[a-zA-Z0-9#%.,()\s:;+/*_-]+$/.test(raw)) return null;
  return raw;
}
function scrub$1(root) {
  let nodeCount = 0;
  const visit = (element, depth) => {
    if (depth > MAX_DEPTH$1) return false;
    nodeCount += 1;
    if (nodeCount > MAX_NODES$1) return false;
    for (const child of Array.from(element.children)) {
      const name = child.localName;
      if (child.namespaceURI !== SVG_NS$2 || !ALLOWED_ELEMENTS$1.has(name)) {
        child.remove();
        continue;
      }
      if (!visit(child, depth + 1)) return false;
    }
    for (const attrName of Array.from(element.getAttributeNames())) {
      const lower = attrName.toLowerCase();
      const value = element.getAttribute(attrName) ?? "";
      if (lower.startsWith("on")) {
        element.removeAttribute(attrName);
        continue;
      }
      if (lower === "href" || lower === "xlink:href") {
        const safe = safeSvgHref(value, element.localName);
        element.removeAttribute(attrName);
        if (safe) element.setAttribute("href", safe);
        continue;
      }
      if (lower === "style") {
        const safe = safeSvgStyle(value);
        element.removeAttribute(attrName);
        if (safe) element.setAttribute("style", safe);
        continue;
      }
      if (lower === "attributename") {
        if (!SAFE_ANIMATABLE.has(value.trim())) {
          element.remove();
          return true;
        }
        continue;
      }
      if (lower.startsWith("data-") || lower.startsWith("aria-")) continue;
      if (!ALLOWED_ATTRS$1.has(lower)) element.removeAttribute(attrName);
    }
    return true;
  };
  return visit(root, 0);
}
function sanitiseSvgMarkup(raw) {
  const source = typeof raw === "string" ? raw : "";
  if (!source || source.length > MAX_INPUT_LENGTH$1) return null;
  const doc = getInertDocument$1();
  if (!doc) return null;
  let host;
  try {
    host = doc.createElementNS(SVG_NS$2, "svg");
    host.innerHTML = source;
  } catch {
    return null;
  }
  let container = host;
  const rootAttrs = {};
  const onlyChild = host.children.length === 1 ? host.firstElementChild : null;
  if (onlyChild && onlyChild.localName === "svg" && onlyChild.namespaceURI === SVG_NS$2) {
    container = onlyChild;
  }
  if (!scrub$1(container)) return null;
  if (container !== host) {
    for (const name of container.getAttributeNames()) {
      const lower = name.toLowerCase();
      if (lower.startsWith("on")) continue;
      if (!ALLOWED_ATTRS$1.has(lower) && !lower.startsWith("data-") && !lower.startsWith("aria-")) continue;
      rootAttrs[lower] = container.getAttribute(name) ?? "";
    }
  }
  const children = [];
  for (const node of Array.from(container.childNodes)) {
    if (node.nodeType === 1) {
      children.push(document.importNode(node, true));
    } else if (node.nodeType === 3) {
      children.push(document.createTextNode(node.textContent ?? ""));
    }
  }
  if (children.length === 0) return null;
  return { children, rootAttrs };
}
const SUPPORTED_VARIANTS = /* @__PURE__ */ new Set(["solid", "regular", "brands"]);
const DEFAULT_VARIANT = "solid";
function resolveIconClasses(value) {
  if (typeof value !== "string") return [];
  const sanitized = stripInvisibleModifiers(value).trim();
  if (!sanitized) return [];
  if (!isAsciiIconName(sanitized)) return [];
  const [variant, name] = splitVariant(sanitized);
  if (!name) return [];
  return [`fa-${variant}`, `fa-${name}`];
}
const INVISIBLE_MODIFIER_RE = /[\uFE0E\uFE0F\u200D\u200C\uFEFF]/g;
function stripInvisibleModifiers(input) {
  return input.replace(INVISIBLE_MODIFIER_RE, "");
}
function splitVariant(input) {
  const idx = input.indexOf(":");
  if (idx === -1) return [DEFAULT_VARIANT, input];
  const variant = input.slice(0, idx).trim().toLowerCase();
  const name = input.slice(idx + 1).trim();
  if (!SUPPORTED_VARIANTS.has(variant)) return [DEFAULT_VARIANT, name || input];
  return [variant, name];
}
function isAsciiIconName(value) {
  return /^[a-zA-Z0-9:_-]+$/.test(value);
}
const ICON_SIZES = ["xs", "sm", "md", "lg", "xl"];
const customIcons = /* @__PURE__ */ new Map();
function getCustomIcon(name) {
  if (typeof name !== "string") return null;
  return customIcons.get(name.trim()) ?? null;
}
function hasCustomIcon(name) {
  return typeof name === "string" && customIcons.has(name.trim());
}
function el(tag, attrs, children) {
  const node = document.createElement(tag);
  if (attrs) {
    for (const [key, value] of Object.entries(attrs)) {
      if (value === null || value === void 0 || value === false) continue;
      if (key === "class") node.setAttribute("class", String(value));
      else if (value === true) node.setAttribute(key, "");
      else node.setAttribute(key, String(value));
    }
  }
  if (children) {
    for (const child of children) {
      if (child === null || child === void 0) continue;
      node.append(typeof child === "string" ? document.createTextNode(child) : child);
    }
  }
  return node;
}
function classNames(...parts) {
  return parts.filter(Boolean).join(" ");
}
function asArray(value) {
  if (Array.isArray(value)) return value;
  if (value === null || value === void 0) return [];
  return [value];
}
function asString(value, fallback = "") {
  if (value === null || value === void 0) return fallback;
  if (typeof value === "string") return value;
  return String(value);
}
function valueAttr(value) {
  if (value === null || value === void 0) return null;
  return typeof value === "string" ? value : String(value);
}
function asBoolean(value, fallback = false) {
  if (typeof value === "boolean") return value;
  if (value === "true") return true;
  if (value === "false") return false;
  if (value === null || value === void 0) return fallback;
  return Boolean(value);
}
function asNumber(value, fallback = 0) {
  if (typeof value === "number") return value;
  if (typeof value === "string" && value.trim() !== "") {
    const n = Number(value);
    if (!Number.isNaN(n)) return n;
  }
  return fallback;
}
const SPACING_TOKENS = ["none", "3xs", "2xs", "xs", "sm", "md", "lg", "xl", "2xl", "3xl"];
const LEGACY_SIZE_TOKEN_ALIASES = {
  s: "sm",
  m: "md",
  l: "lg",
  small: "sm",
  normal: "md",
  large: "lg"
};
function canonicalSizeToken(value) {
  const raw = typeof value === "string" ? value.trim() : "";
  return LEGACY_SIZE_TOKEN_ALIASES[raw] ?? raw;
}
function normalizeSpacingToken(value, fallback = "") {
  const token = canonicalSizeToken(value);
  return SPACING_TOKENS.includes(token) ? token : fallback;
}
function spacingCssValue(value) {
  const token = normalizeSpacingToken(value);
  if (!token) return "";
  if (token === "none") return "0";
  const varKey = token === "sm" ? "s" : token === "md" ? "m" : token === "lg" ? "l" : token;
  return `var(--rui-spacing-${varKey})`;
}
const RESPONSIVE_BREAKPOINTS = ["base", "sm", "md", "lg", "xl"];
function readResponsiveProp(value) {
  if (value == null || typeof value !== "object" || Array.isArray(value)) {
    return { kind: "single", value: value ?? null };
  }
  const entries = Object.entries(value);
  const values = {};
  let matched = false;
  for (const [key, raw] of entries) {
    if (RESPONSIVE_BREAKPOINTS.includes(key)) {
      values[key] = raw;
      matched = true;
    }
  }
  if (!matched) {
    return { kind: "single", value };
  }
  return { kind: "responsive", values };
}
const CSS_URL_FORBIDDEN = /["'\\\n\r<>;{}]/g;
function sanitiseCssUrl(raw) {
  if (!raw) return "";
  return raw.replace(CSS_URL_FORBIDDEN, "").trim();
}
const CSS_LENGTH_ALLOWED = /^[a-zA-Z0-9.%+\-*/\s(),]+$/;
function sanitiseCssLength(raw, fallback) {
  const trimmed = (asString(raw) ?? "").trim();
  if (!trimmed) return fallback;
  if (trimmed.length > 64) return fallback;
  if (!CSS_LENGTH_ALLOWED.test(trimmed)) return fallback;
  return trimmed;
}
const CSS_COLOR_ALLOWED = /^[a-zA-Z0-9#%.,()\s+\-]+$/;
function sanitiseCssColor(raw) {
  const trimmed = asString(raw).trim();
  if (!trimmed) return "";
  if (trimmed.length > 64) return "";
  if (!CSS_COLOR_ALLOWED.test(trimmed)) return "";
  if (/\burl\s*\(|\bexpression\s*\(|javascript\s*:|@import\b/i.test(trimmed)) return "";
  return trimmed;
}
const SAFE_HREF_SCHEMES = /* @__PURE__ */ new Set(["http", "https", "mailto", "tel"]);
function sanitiseHref(raw, fallback = "#") {
  const value = asString(raw).trim();
  if (!value) return fallback;
  const cleaned = value.replace(/[\u0000-\u001F\u007F]/g, "");
  if (!cleaned) return fallback;
  if (cleaned.startsWith("//")) return fallback;
  if (cleaned.startsWith("#") || cleaned.startsWith("/") || cleaned.startsWith("?") || cleaned.startsWith(".")) {
    return cleaned;
  }
  const schemeMatch = /^([a-zA-Z][a-zA-Z0-9+.\-]*):/.exec(cleaned);
  if (!schemeMatch) {
    return cleaned;
  }
  const scheme = schemeMatch[1].toLowerCase();
  if (!SAFE_HREF_SCHEMES.has(scheme)) return fallback;
  return cleaned;
}
const SAFE_IMAGE_SCHEMES = /* @__PURE__ */ new Set(["http", "https", "data", "blob"]);
function sanitiseImageSrc(raw) {
  const value = asString(raw).trim();
  if (!value) return "";
  const cleaned = value.replace(/[\u0000-\u001F\u007F]/g, "");
  if (!cleaned) return "";
  if (cleaned.startsWith("//")) return "";
  if (cleaned.startsWith("/") || cleaned.startsWith(".") || cleaned.startsWith("?") || cleaned.startsWith("#")) {
    return cleaned;
  }
  const schemeMatch = /^([a-zA-Z][a-zA-Z0-9+.\-]*):/.exec(cleaned);
  if (!schemeMatch) return cleaned;
  const scheme = schemeMatch[1].toLowerCase();
  if (!SAFE_IMAGE_SCHEMES.has(scheme)) return "";
  if (scheme === "data" && !/^data:image\//i.test(cleaned)) return "";
  return cleaned;
}
function isComponentNode(value) {
  if (value === null || typeof value !== "object") return false;
  const kind = value.__kind;
  return kind === "Component" || kind === "UserComponent";
}
function asColumnNodes(raw) {
  return asArray(raw).filter(
    (node) => node !== null && node !== void 0 && typeof node === "object"
  );
}
function fillTableCell(td, col, value, rowIndex, helpers, formatValue, row) {
  const format = col.format ?? "text";
  let content = value;
  if (typeof col.render === "function") {
    try {
      content = col.render(value, rowIndex, row);
    } catch (err) {
      console.error("[aktion] Col render() threw", err);
      content = null;
    }
  }
  const appendOne = (item) => {
    if (isComponentNode(item)) {
      td.append(helpers.renderNode(item));
    } else if (item !== null && item !== void 0) {
      td.append(document.createTextNode(formatValue(item, format)));
    }
  };
  if (Array.isArray(content)) {
    for (const item of content) appendOne(item);
  } else {
    appendOne(content);
  }
  if (typeof col.onClick === "function") {
    td.setAttribute("data-clickable", "true");
    td.setAttribute("role", "button");
    td.tabIndex = 0;
    const guardedFire = (event) => {
      const target = event.target;
      if (target?.closest("input,button,a,label,select,textarea")) return false;
      event.stopPropagation();
      helpers.invoke(col.onClick, value, rowIndex, row);
      return true;
    };
    td.onclick = (event) => {
      guardedFire(event);
    };
    td.onkeydown = (event) => {
      if (event.key !== "Enter" && event.key !== " ") return;
      if (guardedFire(event)) event.preventDefault();
    };
  }
}
let AUTO_ID_SEQ = 0;
function autoId(helpers, prefix) {
  const slot = helpers.useInstanceState("autoId", 0);
  if (slot.get() === 0) {
    AUTO_ID_SEQ += 1;
    slot.set(AUTO_ID_SEQ);
  }
  return `${prefix}-${slot.get()}`;
}
function renderIcon(value, options = {}) {
  const text2 = asString(value);
  if (!text2) return null;
  const color2 = options.color ? sanitiseCssColor(options.color) : "";
  const style = color2 ? `color:${color2};` : null;
  const wrapperClass = ["rui-icon", options.className].filter(Boolean).join(" ");
  const custom = getCustomIcon(text2);
  if (custom) {
    const span = el("span", {
      class: `${wrapperClass} rui-icon-custom`,
      "data-icon-size": options.size ?? null,
      style,
      "aria-hidden": "true"
    });
    const safe = sanitiseSvgMarkup(custom);
    if (safe) {
      const ns = "http://www.w3.org/2000/svg";
      const svg = document.createElementNS(ns, "svg");
      svg.setAttribute("viewBox", safe.rootAttrs.viewbox || "0 0 24 24");
      svg.setAttribute("fill", safe.rootAttrs.fill || "currentColor");
      for (const child of safe.children) svg.appendChild(child);
      span.appendChild(svg);
    }
    return span;
  }
  const classes = resolveIconClasses(text2);
  if (classes.length === 0) {
    return el("span", {
      class: wrapperClass,
      "data-icon-size": options.size ?? null,
      style
    }, [text2]);
  }
  return el("i", {
    class: `${wrapperClass} ${classes.join(" ")}`,
    "data-icon-size": options.size ?? null,
    style,
    "aria-hidden": "true"
  });
}
const BREAKPOINT_MIN = {
  base: 0,
  sm: 640,
  md: 768,
  lg: 1024,
  xl: 1280
};
const BP_ORDER = { base: 0, sm: 1, md: 2, lg: 3, xl: 4 };
let dynSheet;
const ruleCache = /* @__PURE__ */ new Map();
let classCounter = 0;
function getResponsiveSheet() {
  if (dynSheet !== void 0) return dynSheet;
  try {
    if (typeof CSSStyleSheet === "undefined" || !("replaceSync" in CSSStyleSheet.prototype) || typeof document === "undefined" || !("adoptedStyleSheets" in Document.prototype)) {
      dynSheet = null;
      return null;
    }
    dynSheet = new CSSStyleSheet();
    return dynSheet;
  } catch {
    dynSheet = null;
    return null;
  }
}
function responsiveClassFor(groups) {
  const sheet = getResponsiveSheet();
  if (!sheet) return null;
  const ordered = [...groups].filter((g) => g.decls.length > 0).sort((a, b) => (BP_ORDER[a.bp] ?? 0) - (BP_ORDER[b.bp] ?? 0));
  if (ordered.length === 0) return null;
  const cacheKey = ordered.map((g) => `${g.bp}{${g.decls.map(([p, v]) => `${p}:${v}`).join(";")}}`).join("|");
  const cached = ruleCache.get(cacheKey);
  if (cached) return cached;
  const cls = `ak-r${(classCounter++).toString(36)}`;
  const sel = `.${cls}.${cls}.${cls}`;
  for (const g of ordered) {
    const body = g.decls.map(([p, v]) => `${p}:${v}`).join(";");
    const min = BREAKPOINT_MIN[g.bp] ?? 0;
    const rule = min > 0 ? `@media (min-width:${min}px){${sel}{${body}}}` : `${sel}{${body}}`;
    try {
      sheet.insertRule(rule, sheet.cssRules.length);
    } catch {
    }
  }
  ruleCache.set(cacheKey, cls);
  return cls;
}
function isResponsiveMap(v) {
  if (!v || typeof v !== "object" || Array.isArray(v)) return false;
  const map = v;
  return "base" in map || "sm" in map || "md" in map || "lg" in map || "xl" in map;
}
const STATE_SELECTOR = {
  hover: ":hover",
  focus: ":focus",
  "focus-visible": ":focus-visible",
  "focus-within": ":focus-within",
  active: ":active",
  disabled: ":disabled, &[disabled], &[data-disabled='true']",
  checked: ":checked, &[aria-checked='true']",
  "group-hover": ""
  // handled specially below
};
function stateClassFor(groups) {
  const sheet = getResponsiveSheet();
  if (!sheet) return null;
  const valid = groups.filter((g) => g.decls.length > 0 && g.state in STATE_SELECTOR);
  if (valid.length === 0) return null;
  const cacheKey = "S|" + valid.map((g) => `${g.state}{${g.decls.map(([p, v]) => `${p}:${v}`).join(";")}}`).join("|");
  const cached = ruleCache.get(cacheKey);
  if (cached) return cached;
  const cls = `ak-s${(classCounter++).toString(36)}`;
  try {
    sheet.insertRule(
      `.${cls}{transition:background .15s ease,color .15s ease,box-shadow .15s ease,transform .15s ease,opacity .15s ease,border-color .15s ease;}`,
      sheet.cssRules.length
    );
  } catch {
  }
  for (const g of valid) {
    const body = g.decls.map(([p, v]) => `${p}:${v}`).join(";");
    let rule;
    if (g.state === "group-hover") {
      rule = `.ak-group:hover .${cls}{${body}}`;
    } else if (g.state === "disabled") {
      rule = `.${cls}:disabled,.${cls}[disabled],.${cls}[data-disabled="true"]{${body}}`;
    } else if (g.state === "checked") {
      rule = `.${cls}:checked,.${cls}[aria-checked="true"]{${body}}`;
    } else {
      rule = `.${cls}${STATE_SELECTOR[g.state]}{${body}}`;
    }
    try {
      sheet.insertRule(rule, sheet.cssRules.length);
    } catch {
    }
  }
  ruleCache.set(cacheKey, cls);
  return cls;
}
const SPACING = {
  none: "0",
  "3xs": "var(--rui-spacing-3xs)",
  "2xs": "var(--rui-spacing-2xs)",
  xs: "var(--rui-spacing-xs)",
  s: "var(--rui-spacing-s)",
  sm: "var(--rui-spacing-s)",
  m: "var(--rui-spacing-m)",
  md: "var(--rui-spacing-m)",
  l: "var(--rui-spacing-l)",
  lg: "var(--rui-spacing-l)",
  xl: "var(--rui-spacing-xl)",
  "2xl": "var(--rui-spacing-2xl)",
  "3xl": "var(--rui-spacing-3xl)",
  auto: "auto",
  // Safe-area insets (II.5) for notches / home indicators on mobile. Use the
  // directional tokens on single sides (`pb: "safe-bottom"`) and `safe` as the
  // 4-value shorthand for all-around padding/margin (`p: "safe"`).
  safe: "env(safe-area-inset-top) env(safe-area-inset-right) env(safe-area-inset-bottom) env(safe-area-inset-left)",
  "safe-top": "env(safe-area-inset-top)",
  "safe-right": "env(safe-area-inset-right)",
  "safe-bottom": "env(safe-area-inset-bottom)",
  "safe-left": "env(safe-area-inset-left)"
};
const COLORS = {
  bg: "var(--rui-color-bg)",
  "bg-subtle": "var(--rui-color-bg-subtle)",
  surface: "var(--rui-color-surface)",
  "surface-muted": "var(--rui-color-surface-muted)",
  border: "var(--rui-color-border)",
  "border-subtle": "var(--rui-color-border-subtle)",
  text: "var(--rui-color-text)",
  "text-muted": "var(--rui-color-text-muted)",
  muted: "var(--rui-color-text-muted)",
  primary: "var(--rui-color-primary)",
  "primary-hover": "var(--rui-color-primary-hover)",
  "primary-text": "var(--rui-color-primary-text)",
  accent: "var(--rui-color-accent)",
  success: "var(--rui-color-success)",
  warning: "var(--rui-color-warning)",
  danger: "var(--rui-color-danger)",
  info: "var(--rui-color-info)",
  transparent: "transparent",
  current: "currentColor"
};
const RADIUS = {
  none: "0",
  xs: "var(--rui-radius-xs)",
  sm: "var(--rui-radius-sm)",
  md: "var(--rui-radius-md)",
  lg: "var(--rui-radius-lg)",
  pill: "var(--rui-radius-pill)",
  full: "9999px",
  circle: "50%"
};
const SHADOW = {
  none: "none",
  sm: "var(--rui-shadow-sm)",
  md: "var(--rui-shadow-md)",
  lg: "var(--rui-shadow-lg)"
};
const SIZE_KEYWORDS = {
  full: "100%",
  half: "50%",
  screen: "100vh",
  "screen-w": "100vw",
  "screen-h": "100vh",
  dvh: "100dvh",
  min: "min-content",
  max: "max-content",
  fit: "fit-content",
  auto: "auto"
};
const ALIGN = {
  start: "flex-start",
  center: "center",
  end: "flex-end",
  stretch: "stretch",
  baseline: "baseline"
};
const JUSTIFY = {
  start: "flex-start",
  center: "center",
  end: "flex-end",
  between: "space-between",
  around: "space-around",
  evenly: "space-evenly",
  stretch: "stretch"
};
const Z_INDEX = {
  base: "var(--rui-z-base, 0)",
  raised: "var(--rui-z-raised, 10)",
  dropdown: "var(--rui-z-dropdown, 1000)",
  sticky: "var(--rui-z-sticky, 1100)",
  banner: "var(--rui-z-banner, 1200)",
  overlay: "var(--rui-z-overlay, 1250)",
  modal: "var(--rui-z-modal, 1300)",
  popover: "var(--rui-z-popover, 1350)",
  toast: "var(--rui-z-toast, 1400)",
  tooltip: "var(--rui-z-tooltip, 1500)"
};
const FONT_SIZE = {
  xs: "0.75rem",
  sm: "var(--rui-font-size-sm)",
  base: "var(--rui-font-size-base)",
  md: "var(--rui-font-size-base)",
  lg: "var(--rui-font-size-lg)",
  xl: "1.25rem",
  "2xl": "1.5rem",
  "3xl": "1.875rem",
  "4xl": "2.25rem"
};
const FONT_WEIGHT = /* @__PURE__ */ new Set(["100", "200", "300", "400", "500", "600", "700", "800", "900", "normal", "bold"]);
const DISPLAY = /* @__PURE__ */ new Set(["flex", "grid", "block", "inline", "inline-flex", "inline-block", "none", "contents"]);
const DIRECTION = /* @__PURE__ */ new Set(["row", "column", "row-reverse", "column-reverse"]);
const POSITION = /* @__PURE__ */ new Set(["relative", "absolute", "fixed", "sticky", "static"]);
const OVERFLOW = /* @__PURE__ */ new Set(["hidden", "auto", "scroll", "visible", "clip"]);
const CURSOR = /* @__PURE__ */ new Set(["pointer", "default", "not-allowed", "grab", "grabbing", "text", "move", "wait", "help", "none"]);
const TEXT_ALIGN = /* @__PURE__ */ new Set(["left", "center", "right", "justify", "start", "end"]);
function space(v) {
  const s = asString(v).trim();
  if (!s) return null;
  if (s in SPACING) return SPACING[s];
  return sanitiseCssLength(s, "") || null;
}
function size(v) {
  const s = asString(v).trim();
  if (!s) return null;
  if (s in SIZE_KEYWORDS) return SIZE_KEYWORDS[s];
  return sanitiseCssLength(s, "") || null;
}
function color(v) {
  const s = asString(v).trim();
  if (!s) return null;
  if (s in COLORS) return COLORS[s];
  if (s.startsWith("gradient.")) return `var(--rui-gradient-${cssIdent(s.slice("gradient.".length))})`;
  return sanitiseCssColor(s) || null;
}
function bg(v) {
  const s = asString(v).trim();
  if (s.startsWith("gradient.")) return `var(--rui-gradient-${cssIdent(s.slice("gradient.".length))})`;
  return color(v);
}
function cssIdent(raw) {
  return raw.replace(/[^a-zA-Z0-9-]/g, "").slice(0, 40);
}
function num(v) {
  if (typeof v === "number" && Number.isFinite(v)) return String(v);
  const s = asString(v).trim();
  if (/^-?\d+(\.\d+)?$/.test(s)) return s;
  return null;
}
function fontSize(v) {
  const s = asString(v).trim();
  if (!s) return null;
  if (s in FONT_SIZE) return FONT_SIZE[s];
  return sanitiseCssLength(s, "") || null;
}
function fontWeight(v) {
  const s = asString(v).trim();
  return FONT_WEIGHT.has(s) ? s : null;
}
function safeBgUrl(raw) {
  const safe = raw.replace(/["'\\\n\r<>;{})(]/g, "").trim();
  if (!safe || safe.length > 512) return null;
  if (/^(https?:)?\/\//i.test(safe)) return safe;
  if (/^data:image\//i.test(safe)) return safe;
  if (/^[a-zA-Z][a-zA-Z0-9+.-]*:/.test(safe)) return null;
  return safe;
}
function overlayLayer(v) {
  const s = asString(v).trim();
  if (!s) return null;
  if (s.startsWith("gradient.")) return `var(--rui-gradient-${cssIdent(s.slice("gradient.".length))})`;
  const c = color(s);
  return c ? `linear-gradient(${c}, ${c})` : null;
}
function resolveResponsive(v) {
  if (v && typeof v === "object" && !Array.isArray(v)) {
    const map = v;
    if ("base" in map || "sm" in map || "md" in map || "lg" in map || "xl" in map) {
      return map.base ?? map.sm ?? map.md ?? map.lg ?? map.xl;
    }
  }
  return v;
}
const RESPONSIVE_RESOLVERS = {
  p: (v) => pair("padding", space(v)),
  // `px`/`mx` use logical inline properties so RTL documents mirror (X.1) —
  // identical rendering in LTR, correct mirroring under `dir="rtl"`.
  px: (v) => pair("padding-inline", space(v)),
  py: (v) => {
    const s = space(v);
    return s ? [["padding-top", s], ["padding-bottom", s]] : [];
  },
  pt: (v) => pair("padding-top", space(v)),
  pr: (v) => pair("padding-right", space(v)),
  pb: (v) => pair("padding-bottom", space(v)),
  pl: (v) => pair("padding-left", space(v)),
  // Logical single-side spacing (X.1): `ps`/`pe` = inline start/end.
  ps: (v) => pair("padding-inline-start", space(v)),
  pe: (v) => pair("padding-inline-end", space(v)),
  m: (v) => pair("margin", space(v)),
  mx: (v) => pair("margin-inline", space(v)),
  my: (v) => {
    const s = space(v);
    return s ? [["margin-top", s], ["margin-bottom", s]] : [];
  },
  mt: (v) => pair("margin-top", space(v)),
  mr: (v) => pair("margin-right", space(v)),
  mb: (v) => pair("margin-bottom", space(v)),
  ml: (v) => pair("margin-left", space(v)),
  ms: (v) => pair("margin-inline-start", space(v)),
  me: (v) => pair("margin-inline-end", space(v)),
  gap: (v) => pair("gap", space(v)),
  w: (v) => pair("width", size(v)),
  h: (v) => pair("height", size(v)),
  minW: (v) => pair("min-width", size(v)),
  maxW: (v) => pair("max-width", size(v)),
  minH: (v) => pair("min-height", size(v)),
  maxH: (v) => pair("max-height", size(v)),
  bg: (v) => pair("background", bg(v)),
  color: (v) => pair("color", color(v)),
  radius: (v) => {
    const r = asString(v).trim();
    return pair("border-radius", r in RADIUS ? RADIUS[r] : sanitiseCssLength(r, "") || null);
  },
  shadow: (v) => {
    const s = asString(v).trim();
    return s in SHADOW ? [["box-shadow", SHADOW[s]]] : [];
  },
  display: (v) => {
    const d = asString(v).trim();
    return DISPLAY.has(d) ? [["display", d]] : [];
  },
  direction: (v) => {
    const d = asString(v).trim();
    return DIRECTION.has(d) ? [["flex-direction", d]] : [];
  },
  align: (v) => {
    const a = asString(v).trim();
    return a in ALIGN ? [["align-items", ALIGN[a]]] : [];
  },
  justify: (v) => {
    const j = asString(v).trim();
    return j in JUSTIFY ? [["justify-content", JUSTIFY[j]]] : [];
  },
  textAlign: (v) => {
    const t = asString(v).trim();
    return TEXT_ALIGN.has(t) ? [["text-align", t]] : [];
  },
  columns: (v) => {
    const c = num(v);
    return c ? [["grid-template-columns", `repeat(${c}, minmax(0, 1fr))`]] : [];
  },
  // Spec I.5 says EVERY sx value accepts a breakpoint map — cover the rest of
  // the bounded surface so responsive maps never silently no-op.
  border: (v) => resolveBorder(v),
  borderColor: (v) => pair("border-color", color(v)),
  opacity: (v) => pair("opacity", num(v)),
  zIndex: (v) => {
    const zs = asString(v).trim();
    return pair("z-index", zs in Z_INDEX ? Z_INDEX[zs] : num(zs));
  },
  overflow: (v) => {
    const o = asString(v).trim();
    return OVERFLOW.has(o) ? [["overflow", o]] : [];
  },
  grow: (v) => pair("flex-grow", num(v)),
  shrink: (v) => pair("flex-shrink", num(v)),
  basis: (v) => pair("flex-basis", size(v)),
  wrap: (v) => v === true || asString(v) === "true" ? [["flex-wrap", "wrap"]] : v === false || asString(v) === "false" ? [["flex-wrap", "nowrap"]] : [],
  position: (v) => {
    const p = asString(v).trim();
    return POSITION.has(p) ? [["position", p]] : [];
  },
  top: (v) => pair("top", size(v)),
  right: (v) => pair("right", size(v)),
  bottom: (v) => pair("bottom", size(v)),
  left: (v) => pair("left", size(v)),
  inset: (v) => {
    const i = asString(v).trim();
    return pair("inset", i === "0" ? "0" : sanitiseCssLength(i, "") || null);
  },
  fontSize: (v) => pair("font-size", fontSize(v)),
  weight: (v) => pair("font-weight", fontWeight(v)),
  textDecoration: (v) => {
    const t = asString(v).trim();
    return TEXT_DECORATION.has(t) ? [["text-decoration", t]] : [];
  }
};
const TEXT_DECORATION = /* @__PURE__ */ new Set(["underline", "none", "line-through", "overline"]);
function resolveBorder(v) {
  const b = asString(v).trim();
  if (!b) return [];
  if (b === "none") return [["border", "none"]];
  if (b === "subtle") return [["border", "1px solid var(--rui-color-border-subtle)"]];
  if (b === "strong") return [["border", "1px solid var(--rui-color-text)"]];
  if (b === "true" || b === "default") return [["border", "1px solid var(--rui-color-border)"]];
  const c = color(b);
  return c ? [["border", `1px solid ${c}`]] : [];
}
function pair(prop2, value) {
  return value ? [[prop2, value]] : [];
}
function applyResponsive(sx, classes) {
  const handled = /* @__PURE__ */ new Set();
  for (const [key, resolver] of Object.entries(RESPONSIVE_RESOLVERS)) {
    const value = sx[key];
    if (!isResponsiveMap(value)) continue;
    const groups = [];
    for (const bp of ["base", "sm", "md", "lg", "xl"]) {
      if (!(bp in value)) continue;
      const decls = resolver(value[bp]);
      if (decls.length > 0) groups.push({ bp, decls });
    }
    if (groups.length === 0) continue;
    const cls = responsiveClassFor(groups);
    if (cls) {
      classes.push(cls);
      handled.add(key);
    }
  }
  return handled;
}
function serializeSx(sxRaw) {
  const classes = [];
  if (!sxRaw || typeof sxRaw !== "object" || Array.isArray(sxRaw)) return { style: "", classes };
  const sx = sxRaw;
  const handledResponsive = applyResponsive(sx, classes);
  const decls = [];
  const get = (k) => handledResponsive.has(k) ? void 0 : resolveResponsive(sx[k]);
  decls.push(["padding", space(get("p"))]);
  decls.push(["padding-inline", space(get("px"))]);
  decls.push(["padding-top", space(get("py"))], ["padding-bottom", space(get("py"))]);
  decls.push(["padding-top", space(get("pt"))]);
  decls.push(["padding-right", space(get("pr"))]);
  decls.push(["padding-bottom", space(get("pb"))]);
  decls.push(["padding-left", space(get("pl"))]);
  decls.push(["padding-inline-start", space(get("ps"))]);
  decls.push(["padding-inline-end", space(get("pe"))]);
  decls.push(["margin", space(get("m"))]);
  decls.push(["margin-inline", space(get("mx"))]);
  decls.push(["margin-top", space(get("my"))], ["margin-bottom", space(get("my"))]);
  decls.push(["margin-top", space(get("mt"))]);
  decls.push(["margin-right", space(get("mr"))]);
  decls.push(["margin-bottom", space(get("mb"))]);
  decls.push(["margin-left", space(get("ml"))]);
  decls.push(["margin-inline-start", space(get("ms"))]);
  decls.push(["margin-inline-end", space(get("me"))]);
  decls.push(["gap", space(get("gap"))]);
  decls.push(["width", size(get("w"))]);
  decls.push(["height", size(get("h"))]);
  decls.push(["min-width", size(get("minW"))]);
  decls.push(["max-width", size(get("maxW"))]);
  decls.push(["min-height", size(get("minH"))]);
  decls.push(["max-height", size(get("maxH"))]);
  const bgVal = bg(get("bg"));
  if (bgVal) decls.push(["background", bgVal]);
  decls.push(["color", color(get("color"))]);
  const border = get("border");
  if (border != null) for (const d of resolveBorder(border)) decls.push(d);
  const bc = color(get("borderColor"));
  if (bc) decls.push(["border-color", bc]);
  const rad = get("radius");
  if (rad != null) {
    const r = asString(rad).trim();
    decls.push(["border-radius", r in RADIUS ? RADIUS[r] : sanitiseCssLength(r, "") || null]);
  }
  const sh = get("shadow");
  if (sh != null) {
    const s = asString(sh).trim();
    if (s in SHADOW) decls.push(["box-shadow", SHADOW[s]]);
  }
  const op = num(get("opacity"));
  if (op) decls.push(["opacity", op]);
  const disp = asString(get("display")).trim();
  if (DISPLAY.has(disp)) decls.push(["display", disp]);
  const dir = asString(get("direction")).trim();
  if (DIRECTION.has(dir)) decls.push(["flex-direction", dir]);
  const al = asString(get("align")).trim();
  if (al in ALIGN) decls.push(["align-items", ALIGN[al]]);
  const ju = asString(get("justify")).trim();
  if (ju in JUSTIFY) decls.push(["justify-content", JUSTIFY[ju]]);
  if (get("wrap") === true || asString(get("wrap")) === "true") decls.push(["flex-wrap", "wrap"]);
  const grow = num(get("grow"));
  if (grow) decls.push(["flex-grow", grow]);
  const shrink = num(get("shrink"));
  if (shrink) decls.push(["flex-shrink", shrink]);
  const flexBasis = size(get("basis"));
  if (flexBasis) decls.push(["flex-basis", flexBasis]);
  const cols = num(get("columns"));
  if (cols) decls.push(["grid-template-columns", `repeat(${cols}, minmax(0, 1fr))`]);
  const pos = asString(get("position")).trim();
  if (POSITION.has(pos)) decls.push(["position", pos]);
  decls.push(["top", size(get("top"))]);
  decls.push(["right", size(get("right"))]);
  decls.push(["bottom", size(get("bottom"))]);
  decls.push(["left", size(get("left"))]);
  const inset = get("inset");
  if (inset != null) {
    const i = asString(inset).trim();
    decls.push(["inset", i === "0" ? "0" : sanitiseCssLength(i, "") || null]);
  }
  const z = get("zIndex");
  if (z != null) {
    const zs = asString(z).trim();
    decls.push(["z-index", zs in Z_INDEX ? Z_INDEX[zs] : num(zs)]);
  }
  decls.push(["font-size", fontSize(get("fontSize"))]);
  decls.push(["font-weight", fontWeight(get("weight"))]);
  const deco = asString(get("textDecoration")).trim();
  if (TEXT_DECORATION.has(deco)) decls.push(["text-decoration", deco]);
  const ov = asString(get("overflow")).trim();
  if (OVERFLOW.has(ov)) decls.push(["overflow", ov]);
  const cur = asString(get("cursor")).trim();
  if (CURSOR.has(cur)) decls.push(["cursor", cur]);
  const ta = asString(get("textAlign")).trim();
  if (TEXT_ALIGN.has(ta)) decls.push(["text-align", ta]);
  const backdrop = asString(get("backdrop")).trim();
  if (backdrop === "blur") {
    decls.push(["backdrop-filter", "blur(12px)"]);
    decls.push(["-webkit-backdrop-filter", "blur(12px)"]);
  }
  const bgImage = asString(get("bgImage")).trim();
  const overlay = overlayLayer(get("bgOverlay"));
  if (bgImage) {
    const safe = safeBgUrl(bgImage);
    if (safe) {
      const url = `url("${safe}")`;
      decls.push(["background-image", overlay ? `${overlay}, ${url}` : url]);
      decls.push(["background-size", asString(get("bgSize")).trim() === "contain" ? "contain" : "cover"]);
      decls.push(["background-position", "center"]);
    }
  } else if (overlay) {
    decls.push(["background-image", overlay]);
  }
  collectStateClasses(get("hover"), "hover", classes);
  collectStateClasses(get("focus"), "focus", classes);
  collectArbitraryStateClasses(sx, classes);
  const style = decls.filter((d) => d[1] != null && d[1] !== "").map(([k, v]) => `${k}:${v}`).join(";");
  return { style, classes };
}
const STATE_EFFECTS = /* @__PURE__ */ new Set(["lift", "grow", "glow", "bright", "border", "underline", "scale"]);
function collectStateClasses(raw, state, classes) {
  if (raw == null) return;
  if (typeof raw === "string") {
    if (STATE_EFFECTS.has(raw)) classes.push(`ak-${state}-${raw}`);
    return;
  }
  if (typeof raw === "object" && !Array.isArray(raw)) {
    for (const [k, v] of Object.entries(raw)) {
      if (v && STATE_EFFECTS.has(k)) classes.push(`ak-${state}-${k}`);
    }
  }
}
const ARBITRARY_STATES = /* @__PURE__ */ new Set([
  "hover",
  "focus",
  "focus-visible",
  "focus-within",
  "active",
  "disabled",
  "checked",
  "group-hover"
]);
function collectArbitraryStateClasses(sx, classes) {
  const groups = [];
  const consider = (state, value) => {
    if (!value || typeof value !== "object" || Array.isArray(value)) return;
    const decls = resolveStateDecls(value);
    if (decls.length > 0) groups.push({ state, decls });
  };
  const states = sx.states;
  if (states && typeof states === "object" && !Array.isArray(states)) {
    for (const [state, value] of Object.entries(states)) {
      if (ARBITRARY_STATES.has(state)) consider(state, value);
    }
  }
  consider("hover", sx.hover);
  consider("focus", sx.focus);
  if (groups.length === 0) return;
  const cls = stateClassFor(groups);
  if (cls) classes.push(cls);
}
function resolveStateDecls(obj) {
  const out = [];
  const push = (prop2, value) => {
    if (value) out.push([prop2, value]);
  };
  const transforms = [];
  push("background", bg(obj.bg));
  push("color", color(obj.color));
  push("border-color", color(obj.borderColor));
  if (obj.shadow != null) {
    const s = asString(obj.shadow).trim();
    if (s in SHADOW) push("box-shadow", SHADOW[s]);
  }
  if (obj.radius != null) {
    const r = asString(obj.radius).trim();
    push("border-radius", r in RADIUS ? RADIUS[r] : sanitiseCssLength(r, "") || null);
  }
  const op = num(obj.opacity);
  if (op) push("opacity", op);
  const cur = asString(obj.cursor).trim();
  if (CURSOR.has(cur)) push("cursor", cur);
  if (obj.textDecoration != null) {
    const td = asString(obj.textDecoration).trim();
    if (["underline", "none", "line-through", "overline"].includes(td)) push("text-decoration", td);
  }
  const scale = num(obj.scale);
  if (scale) transforms.push(`scale(${scale})`);
  const ty = sanitiseCssLength(asString(obj.translateY), "");
  if (ty) transforms.push(`translateY(${ty})`);
  const tx = sanitiseCssLength(asString(obj.translateX), "");
  if (tx) transforms.push(`translateX(${tx})`);
  if (obj.rotate != null) {
    const deg = asNumber(obj.rotate);
    if (Number.isFinite(deg) && Math.abs(deg) <= 360) transforms.push(`rotate(${deg}deg)`);
  }
  if (transforms.length > 0) push("transform", transforms.join(" "));
  return out;
}
const ANIMATE_PRESETS = /* @__PURE__ */ new Set([
  "fade",
  "fade-up",
  "fade-down",
  "fade-left",
  "fade-right",
  "zoom",
  "zoom-in",
  "slide-up",
  "slide-down",
  "slide-left",
  "slide-right",
  "pulse",
  "float",
  "shimmer",
  "bounce",
  "spin",
  "ping",
  "wiggle"
]);
function resolveAnimate(raw) {
  const classes = [];
  const decls = [];
  if (!raw) return { classes, style: "" };
  let preset = "";
  let delay = null;
  let duration = null;
  let repeat = null;
  if (typeof raw === "string") {
    preset = raw.trim();
  } else if (typeof raw === "object" && !Array.isArray(raw)) {
    const o = raw;
    preset = asString(o.preset ?? o.name).trim();
    if (o.delay != null) delay = asNumber(o.delay);
    if (o.duration != null) duration = asNumber(o.duration);
    repeat = o.repeat;
  }
  if (!ANIMATE_PRESETS.has(preset)) return { classes, style: "" };
  classes.push("ak-anim", `ak-anim-${preset}`);
  if (delay != null && delay >= 0 && delay <= 2e4) decls.push(`animation-delay:${Math.round(delay)}ms`);
  if (duration != null && duration > 0 && duration <= 2e4) decls.push(`animation-duration:${Math.round(duration)}ms`);
  if (repeat === "infinite" || repeat === true) decls.push("animation-iteration-count:infinite");
  else {
    const n = num(repeat);
    if (n) decls.push(`animation-iteration-count:${n}`);
  }
  return { classes, style: decls.join(";") };
}
const ALLOWED_ROLES = /* @__PURE__ */ new Set([
  // Landmarks and document structure
  "banner",
  "complementary",
  "contentinfo",
  "form",
  "main",
  "navigation",
  "region",
  "search",
  "article",
  "group",
  "list",
  "listitem",
  "separator",
  "heading",
  "figure",
  "note",
  "definition",
  "term",
  // Live regions and status
  "status",
  "alert",
  "log",
  "timer",
  "marquee",
  "progressbar",
  "meter",
  // Common widgets
  "button",
  "link",
  "img",
  "toolbar",
  "tooltip",
  "dialog",
  "tab",
  "tabpanel",
  "tablist",
  "menuitem",
  "option",
  "checkbox",
  "radio",
  "switch",
  // Explicitly neutral
  "none",
  "presentation"
]);
const UNIVERSAL_PROP_NAMES = /* @__PURE__ */ new Set([
  "sx",
  "animate",
  "id",
  "anchor",
  "className",
  "class",
  "style",
  "aria",
  "data",
  "tooltip",
  "hidden",
  // Escape valve for ARIA defects that cannot be fixed from outside the library:
  // without it an app author has no way to correct a component's role.
  "role",
  // Second spelling of the `data` channel, for the components that declare a
  // `data` prop of their own (LineChart, JsonTree, Async, Draggable, Lottie,
  // QRCode). On those, the component prop wins and the universal channel was
  // unreachable, so `data-*` attributes could not be set at all.
  "dataAttrs",
  // A first-class spelling for the one `data-*` attribute every end-to-end test
  // runner looks for. `data: { testid: … }` could already do this, but not on the
  // six components that shadow `data`, and only if the author knew the attribute
  // name. `testid` is accepted alongside `testId` for the same reason `class`
  // sits beside `className`: both spellings are what people actually type, and an
  // unknown prop is a hard validation error, not a warning.
  "testId",
  "testid"
]);
const CLASS_TOKEN = /^[A-Za-z_][\w-]*$/;
function sanitiseClasses(raw) {
  const out = [];
  const push = (v) => {
    const s = asString(v).trim();
    for (const tok of s.split(/\s+/)) {
      if (tok && CLASS_TOKEN.test(tok)) out.push(tok);
    }
  };
  if (Array.isArray(raw)) raw.forEach(push);
  else push(raw);
  return out;
}
const STYLE_BLOCK_RE = /<\/?\w|expression\s*\(|javascript\s*:|@import\b|url\s*\(\s*['"]?\s*(javascript|data:text)/i;
function sanitiseStyleString(raw) {
  const s = asString(raw).trim();
  if (!s || s.length > 2048) return "";
  if (STYLE_BLOCK_RE.test(s)) return "";
  return s;
}
const ARIA_KEY = /^[a-z][a-z-]{1,32}$/;
const NEEDS_BOX_DECL = /(?:^|;)\s*(?:padding|margin|width|height|min-width|max-width|min-height|max-height|background|border|box-shadow|outline|overflow|gap|position|inset|top|right|bottom|left|aspect-ratio)\b/i;
const DECLARES_DISPLAY = /(?:^|;)\s*display\s*:/i;
const INLINE_CONTENTS = /(?:^|;)\s*display\s*:\s*contents\s*(?:;|$)/i;
const BOXLESS_HOST_CLASSES = ["rui-universal-host", "rui-route", "rui-transition", "rui-focus-trap"];
function stripDisplay(style) {
  return style.split(";").map((decl) => decl.trim()).filter((decl) => decl !== "" && !/^display\s*:/i.test(decl)).join(";");
}
function applyUniversal(node, universal) {
  if (!universal || !(node instanceof Element)) return;
  const elNode = node;
  const styleParts = [];
  const classes = [];
  if (universal.sx != null) {
    const { style, classes: sxClasses } = serializeSx(universal.sx);
    if (style) styleParts.push(style);
    classes.push(...sxClasses);
  }
  let needsBox = false;
  if (universal.animate != null) {
    const { classes: aClasses, style } = resolveAnimate(universal.animate);
    classes.push(...aClasses);
    if (style) styleParts.push(style);
    if (aClasses.length > 0) needsBox = true;
  }
  const rawStyle = sanitiseStyleString(universal.style);
  if (rawStyle) styleParts.push(rawStyle);
  const sxStyle = styleParts.join(";");
  const existing = elNode.getAttribute("style") ?? "";
  const boxless = INLINE_CONTENTS.test(existing) || BOXLESS_HOST_CLASSES.some((c) => elNode.classList.contains(c));
  let promoted = null;
  if (boxless) {
    if (universal.hidden === true) promoted = "none";
    else if (!DECLARES_DISPLAY.test(sxStyle) && (needsBox || NEEDS_BOX_DECL.test(sxStyle))) promoted = "block";
  }
  if (sxStyle || promoted) {
    const base = promoted ? stripDisplay(existing) : existing;
    const merged = [base, promoted ? `display:${promoted}` : "", sxStyle].filter(Boolean).join(";");
    elNode.setAttribute("style", merged);
  }
  classes.push(...sanitiseClasses(universal.className));
  classes.push(...sanitiseClasses(universal.class));
  for (const c of classes) elNode.classList.add(c);
  const id = asString(universal.id ?? universal.anchor).trim();
  if (id && /^[A-Za-z][\w-]*$/.test(id)) elNode.setAttribute("id", id);
  if (universal.hidden === true) elNode.setAttribute("hidden", "");
  const tooltip = asString(universal.tooltip).trim();
  if (tooltip) elNode.setAttribute("title", tooltip);
  const roleRaw = asString(universal.role).trim().toLowerCase();
  if (roleRaw && ALLOWED_ROLES.has(roleRaw)) elNode.setAttribute("role", roleRaw);
  if (universal.aria && typeof universal.aria === "object") {
    for (const [k, v] of Object.entries(universal.aria)) {
      const key = k.toLowerCase();
      if (ARIA_KEY.test(key) && v != null) {
        elNode.setAttribute(key.startsWith("aria-") ? key : `aria-${key}`, asString(v));
      }
    }
  }
  const dataBag = universal.dataAttrs && typeof universal.dataAttrs === "object" ? universal.dataAttrs : universal.data && typeof universal.data === "object" ? universal.data : null;
  if (dataBag) {
    for (const [k, v] of Object.entries(dataBag)) {
      const key = k.toLowerCase().replace(/[^a-z0-9-]/g, "");
      if (key && v != null) elNode.setAttribute(`data-${key}`, asString(v));
    }
  }
  const testId = asString(universal.testId ?? universal.testid).trim();
  if (testId) elNode.setAttribute("data-testid", testId);
}
function findPositionalIndex(spec) {
  const explicit = spec.props.findIndex((p) => p.positional === true);
  if (explicit >= 0) return explicit;
  return spec.props.length > 0 ? 0 : -1;
}
function findPositionalProp(spec) {
  const idx = findPositionalIndex(spec);
  return idx >= 0 ? spec.props[idx] : void 0;
}
function assertOnePositionalMax(specs) {
  for (const spec of specs) {
    const positional = spec.props.filter((p) => p.positional === true);
    if (positional.length > 1) {
      const names = positional.map((p) => p.name).join(", ");
      throw new SyntaxError(
        `Component "${spec.name}" declares ${positional.length} positional props (${names}). Aktion 0.5 §19.1 allows at most one.`
      );
    }
  }
}
function propExpectsObject(prop2) {
  return /\bobject\b|\bRecord\b|\{|\bany\b/i.test(prop2.type);
}
function knownPropNames(spec) {
  const names = /* @__PURE__ */ new Set();
  for (const p of spec.props) {
    names.add(p.name);
    if (p.aliases) for (const alias of p.aliases) names.add(alias);
  }
  names.add("key");
  return names;
}
function slotForNthPositional(spec, n) {
  const positionalIndex = findPositionalIndex(spec);
  if (positionalIndex < 0) return null;
  const filled = /* @__PURE__ */ new Set();
  let target = -1;
  for (let i = 0; i <= n; i += 1) {
    if (i === 0) {
      target = positionalIndex;
    } else {
      let cursor = 0;
      while (cursor < spec.props.length && filled.has(cursor)) cursor += 1;
      if (cursor >= spec.props.length) return null;
      target = cursor;
    }
    filled.add(target);
  }
  return spec.props[target] ?? null;
}
function chooseNamedBagIndex(args, spec) {
  let lastObj = -1;
  for (let i = args.length - 1; i >= 0; i -= 1) {
    if (args[i].objectKeys !== null) {
      lastObj = i;
      break;
    }
  }
  if (lastObj < 0) return -1;
  const keys = args[lastObj].objectKeys;
  const known = knownPropNames(spec);
  const knownCount = keys.reduce(
    (count, k) => count + (known.has(k) || UNIVERSAL_PROP_NAMES.has(k) ? 1 : 0),
    0
  );
  const allKnown = keys.length > 0 && knownCount === keys.length;
  const anyKnown = knownCount > 0;
  const positionalProp = findPositionalProp(spec);
  if (args.length === 1) {
    if (spec.props.length === 1) {
      return propExpectsObject(spec.props[0]) ? -1 : allKnown ? 0 : -1;
    }
    if (allKnown) return 0;
    if (!anyKnown && positionalProp && propExpectsObject(positionalProp)) return -1;
    return 0;
  }
  if (lastObj !== args.length - 1) {
    return anyKnown ? lastObj : -1;
  }
  if (anyKnown || keys.length === 0) return lastObj;
  const slot = slotForNthPositional(spec, args.length - 1);
  if (slot && propExpectsObject(slot)) return -1;
  return lastObj;
}
function callArgShapes(args) {
  return args.map((arg) => ({
    objectKeys: arg.kind === "Object" && arg.properties ? arg.properties.filter((p) => !p.spread).map((p) => p.key) : null
  }));
}
function mapPositionalArgs(spec, args) {
  const out = {};
  spec.props.forEach((prop2, index) => {
    if (index < args.length) out[prop2.name] = args[index];
  });
  return out;
}
function validateProgram(source, library) {
  const program = parse(source);
  const schemaErrors = validateProgramSchema(program, library);
  if (schemaErrors.length > 0) {
    program.errors = [...program.errors, ...schemaErrors];
  }
  return program;
}
function validateProgramSchema(program, library) {
  const errors = [];
  currentDeclaredComponents = collectDeclaredComponentNames(program);
  try {
    for (const stmt of program.statements) {
      walkStatement(stmt, library, errors);
    }
    validateRootBinding(program, errors);
  } finally {
    currentDeclaredComponents = EMPTY_NAME_SET;
  }
  return errors;
}
function validateRootBinding(program, out) {
  let root;
  for (const stmt of program.statements) {
    if (stmt.kind === "ExpressionStatement" && isAppCallExpr(stmt.expression)) {
      root = stmt.expression.arguments[0];
    } else if (stmt.kind === "Assignment" && !stmt.isState && stmt.identifier === "aktion") {
      root = stmt.expression;
    }
  }
  if (!root) return;
  const bare = bareRootValueKind(root);
  if (!bare) return;
  out.push({
    message: `The UI root must be a component tree (e.g. \`Text(...)\`, \`Column([...])\`), not a bare ${bare}. Wrap it — \`$app(Text(...))\` — or render a component. (root-not-renderable)`,
    line: root.loc?.line ?? 0,
    column: root.loc?.column ?? 0
  });
}
function isAppCallExpr(expr) {
  return expr.kind === "Invoke" && expr.callee.kind === "StateRef" && expr.callee.name === "app";
}
function bareRootValueKind(root) {
  if (root.kind === "Template") return "string";
  if (root.kind === "Literal") {
    const v = root.value;
    if (typeof v === "string") return "string";
    if (typeof v === "number") return "number";
    if (typeof v === "boolean") return "boolean";
  }
  return null;
}
const EMPTY_NAME_SET = /* @__PURE__ */ new Set();
let currentDeclaredComponents = EMPTY_NAME_SET;
function collectDeclaredComponentNames(program) {
  const names = /* @__PURE__ */ new Set();
  const scan = (node) => {
    if (!node || typeof node !== "object") return;
    if (Array.isArray(node)) {
      for (const entry of node) scan(entry);
      return;
    }
    const rec = node;
    if (rec.kind === "ComponentDeclaration" && typeof rec.name === "string") {
      names.add(rec.name);
    }
    for (const key of Object.keys(rec)) {
      if (key !== "loc") scan(rec[key]);
    }
  };
  scan(program.statements);
  return names;
}
function walkStatement(stmt, library, out) {
  switch (stmt.kind) {
    case "Assignment":
      walkExpression(stmt.expression, library, out);
      return;
    case "ExpressionStatement":
      walkExpression(stmt.expression, library, out);
      return;
    case "ComponentDeclaration":
      validateComponentDeclarationName(stmt, library, out);
      for (const inner of stmt.body.body) walkStatement(inner, library, out);
      return;
    case "ActionDeclaration":
    case "EffectDeclaration":
    case "HookDeclaration":
      for (const inner of stmt.body.body) walkStatement(inner, library, out);
      return;
    case "Return":
      if (stmt.argument) walkExpression(stmt.argument, library, out);
      return;
    case "Await":
      walkExpression(stmt.argument, library, out);
      return;
    case "IfStatement":
      walkExpression(stmt.test, library, out);
      for (const inner of stmt.consequent.body) walkStatement(inner, library, out);
      if (stmt.alternate) {
        if (stmt.alternate.kind === "IfStatement") {
          walkStatement(stmt.alternate, library, out);
        } else {
          for (const inner of stmt.alternate.body) walkStatement(inner, library, out);
        }
      }
      return;
    case "SwitchStatement":
      walkExpression(stmt.discriminant, library, out);
      for (const c of stmt.cases) {
        if (c.test) walkExpression(c.test, library, out);
        for (const inner of c.body) walkStatement(inner, library, out);
      }
      return;
    case "ForOfStatement":
      walkExpression(stmt.iterable, library, out);
      for (const inner of stmt.body.body) walkStatement(inner, library, out);
      return;
    case "ForClassicStatement":
      if (stmt.init) walkStatement(stmt.init, library, out);
      if (stmt.test) walkExpression(stmt.test, library, out);
      if (stmt.update) walkExpression(stmt.update, library, out);
      for (const inner of stmt.body.body) walkStatement(inner, library, out);
      return;
    case "WhileStatement":
    case "DoWhileStatement":
      walkExpression(stmt.test, library, out);
      for (const inner of stmt.body.body) walkStatement(inner, library, out);
      return;
    case "ForInStatement":
      walkExpression(stmt.iterable, library, out);
      for (const inner of stmt.body.body) walkStatement(inner, library, out);
      return;
    case "DestructureStatement":
      walkExpression(stmt.expression, library, out);
      for (const binding of stmt.bindings) {
        if (binding.defaultValue) walkExpression(binding.defaultValue, library, out);
      }
      return;
    case "TryStatement":
      for (const inner of stmt.block.body) walkStatement(inner, library, out);
      if (stmt.catchBlock) for (const inner of stmt.catchBlock.body) walkStatement(inner, library, out);
      if (stmt.finallyBlock) for (const inner of stmt.finallyBlock.body) walkStatement(inner, library, out);
      return;
    case "ThrowStatement":
      walkExpression(stmt.argument, library, out);
      return;
    default:
      return;
  }
}
function walkExpression(expr, library, out) {
  if (!expr) return;
  switch (expr.kind) {
    case "Call": {
      validateCall(expr, library, out);
      for (const arg of expr.arguments) walkExpression(arg, library, out);
      return;
    }
    case "MethodCall": {
      walkExpression(expr.object, library, out);
      for (const arg of expr.arguments) walkExpression(arg, library, out);
      return;
    }
    case "BuiltinCall": {
      for (const arg of expr.arguments) walkExpression(arg, library, out);
      return;
    }
    case "Array":
      for (const e of expr.elements) walkExpression(e, library, out);
      return;
    case "Object":
      for (const prop2 of expr.properties) walkExpression(prop2.value, library, out);
      return;
    case "Template":
      for (const e of expr.expressions) walkExpression(e, library, out);
      return;
    case "Binary":
      walkExpression(expr.left, library, out);
      walkExpression(expr.right, library, out);
      return;
    case "Unary":
      walkExpression(expr.argument, library, out);
      return;
    case "Ternary":
      walkExpression(expr.test, library, out);
      walkExpression(expr.consequent, library, out);
      walkExpression(expr.alternate, library, out);
      return;
    case "Member":
      walkExpression(expr.object, library, out);
      if (expr.computed) walkExpression(expr.computed, library, out);
      return;
    case "Spread":
      walkExpression(expr.argument, library, out);
      return;
    case "Invoke":
      if (expr.callee.kind === "StateRef" && expr.callee.name === "theme") {
        validateThemeCall(expr, out);
      }
      walkExpression(expr.callee, library, out);
      for (const arg of expr.arguments) walkExpression(arg, library, out);
      return;
    case "New":
      walkExpression(expr.callee, library, out);
      for (const arg of expr.arguments) walkExpression(arg, library, out);
      return;
    case "Lambda":
      if (expr.body.kind === "Block") {
        for (const inner of expr.body.body) walkStatement(inner, library, out);
      } else {
        walkExpression(expr.body, library, out);
      }
      return;
    case "Block":
      for (const inner of expr.body) walkStatement(inner, library, out);
      return;
    default:
      return;
  }
}
function validateComponentDeclarationName(stmt, library, out) {
  if (!findComponent(library, stmt.name)) return;
  if (subtreeCallsName(stmt.body, stmt.name)) return;
  out.push({
    message: `function ${stmt.name}(...) — "${stmt.name}" is a built-in component name. Rename the custom component (e.g. "App${stmt.name}"), or call ${stmt.name}(...) inside the body to wrap the built-in (inside its own body the name refers to the built-in component).`,
    line: stmt.loc?.line ?? 0,
    column: stmt.loc?.column ?? 0
  });
}
function subtreeCallsName(node, name) {
  if (!node || typeof node !== "object") return false;
  if (Array.isArray(node)) return node.some((entry) => subtreeCallsName(entry, name));
  const rec = node;
  if (rec.kind === "Call" && rec.callee === name) return true;
  for (const key of Object.keys(rec)) {
    if (key === "loc") continue;
    if (subtreeCallsName(rec[key], name)) return true;
  }
  return false;
}
function editDistance$1(a, b) {
  const al = a.length;
  const bl = b.length;
  if (al === 0) return bl;
  if (bl === 0) return al;
  let prev = Array.from({ length: bl + 1 }, (_, j) => j);
  for (let i = 1; i <= al; i += 1) {
    const curr = [i];
    for (let j = 1; j <= bl; j += 1) {
      const cost = a[i - 1] === b[j - 1] ? 0 : 1;
      curr[j] = Math.min(prev[j] + 1, curr[j - 1] + 1, prev[j - 1] + cost);
    }
    prev = curr;
  }
  return prev[bl];
}
function didYouMean(input, candidates) {
  const lower = input.toLowerCase();
  let best = null;
  let bestDist = Infinity;
  for (const c of candidates) {
    const d = editDistance$1(lower, c.toLowerCase());
    if (d < bestDist) {
      bestDist = d;
      best = c;
    }
  }
  if (best === null || best.toLowerCase() === lower) return null;
  return bestDist <= 2 && bestDist < Math.min(input.length, best.length) ? best : null;
}
function withSuggestion(base, input, candidates) {
  const guess = didYouMean(input, candidates);
  return guess ? `${base} Did you mean "${guess}"?` : base;
}
function validateCall(expr, library, out) {
  if (currentDeclaredComponents.has(expr.callee)) return;
  const spec = findComponent(library, expr.callee);
  if (!spec) return;
  const propNames = /* @__PURE__ */ new Set();
  for (const p of spec.props) {
    propNames.add(p.name);
    if (p.aliases) {
      for (const alias of p.aliases) propNames.add(alias);
    }
  }
  propNames.add("key");
  const args = expr.arguments;
  const trailingObjIdx = chooseNamedBagIndex(callArgShapes(args), spec);
  const trailingObj = trailingObjIdx >= 0 ? args[trailingObjIdx] : null;
  const positionalArgs = [];
  for (let i = 0; i < args.length; i++) {
    if (i === trailingObjIdx) continue;
    positionalArgs.push(args[i]);
  }
  if (positionalArgs.length > spec.props.length) {
    out.push({
      message: `${expr.callee}(...) — takes at most ${spec.props.length} positional argument${spec.props.length === 1 ? "" : "s"} (${spec.props.map((p) => p.name).join(", ")}); got ${positionalArgs.length}.`,
      line: expr.loc?.line ?? 0,
      column: expr.loc?.column ?? 0
    });
  }
  positionalArgs.forEach((arg, n) => {
    const prop2 = slotForNthPositional(spec, n);
    if (!prop2) return;
    if (prop2.enum && arg.kind === "Literal" && typeof arg.value === "string") {
      const value = arg.value;
      if (!prop2.enum.includes(value) && !prop2.enum.includes(canonicalSizeToken(value))) {
        out.push({
          message: withSuggestion(
            `<${expr.callee}> ${prop2.name}="${value}" — must be one of ${prop2.enum.map((v) => `"${v}"`).join(", ")}.`,
            value,
            prop2.enum
          ),
          line: arg.loc?.line ?? expr.loc?.line ?? 0,
          column: arg.loc?.column ?? expr.loc?.column ?? 0
        });
      }
    }
    if (arg.kind === "Object" && !propExpectsObject(prop2)) {
      out.push({
        message: `<${expr.callee}> — object passed positionally for "${prop2.name}" (${prop2.type}). Use named props from: ${spec.props.map((p) => p.name).join(", ")}.`,
        line: arg.loc?.line ?? expr.loc?.line ?? 0,
        column: arg.loc?.column ?? expr.loc?.column ?? 0
      });
    }
  });
  const namedEntries = [];
  if (trailingObj) {
    for (const prop2 of trailingObj.properties) {
      if (prop2.spread) continue;
      namedEntries.push({
        name: prop2.key,
        value: prop2.value,
        loc: prop2.value.loc ?? trailingObj.loc
      });
    }
  }
  for (const entry of namedEntries) {
    if (!propNames.has(entry.name)) {
      if (UNIVERSAL_PROP_NAMES.has(entry.name)) continue;
      out.push({
        message: withSuggestion(
          `Unknown prop "${entry.name}" on <${expr.callee}>. Known props: ${spec.props.map((p) => p.name).join(", ")}.`,
          entry.name,
          spec.props.map((p) => p.name)
        ),
        line: entry.loc?.line ?? expr.loc?.line ?? 0,
        column: entry.loc?.column ?? expr.loc?.column ?? 0
      });
      continue;
    }
    const prop2 = spec.props.find(
      (p) => p.name === entry.name || (p.aliases?.includes(entry.name) ?? false)
    );
    if (prop2?.enum && entry.value.kind === "Literal" && typeof entry.value.value === "string") {
      const value = entry.value.value;
      if (!prop2.enum.includes(value) && !prop2.enum.includes(canonicalSizeToken(value))) {
        out.push({
          message: withSuggestion(
            `<${expr.callee}> ${entry.name}="${value}" — must be one of ${prop2.enum.map((v) => `"${v}"`).join(", ")}.`,
            value,
            prop2.enum
          ),
          line: entry.loc?.line ?? expr.loc?.line ?? 0,
          column: entry.loc?.column ?? expr.loc?.column ?? 0
        });
      }
    }
  }
}
const STRUCTURED_THEME_GROUPS = /* @__PURE__ */ new Set(["colors", "radius", "font", "fonts", "spacing", "shadows", "gradients", "icons", "zIndex", "motion"]);
const THEME_METADATA_KEYS = /* @__PURE__ */ new Set(["name", "direction"]);
function validateThemeCall(expr, out) {
  const arg = expr.arguments[0];
  if (!arg || arg.kind !== "Object") return;
  for (const prop2 of arg.properties) {
    if (prop2.spread) continue;
    if (STRUCTURED_THEME_GROUPS.has(prop2.key)) continue;
    if (THEME_METADATA_KEYS.has(prop2.key)) continue;
    const suggestion = suggestStructuredKey(prop2.key);
    const message = prop2.key.startsWith("--") ? `$theme({"${prop2.key}": ...}) — free-form CSS variable keys are removed in Aktion 0.5. Use the structured form: $theme({ colors: {...}, radius: {...}, font: {...} }).` : `$theme({${prop2.key}: ...}) — legacy flat-shape token is removed in Aktion 0.5. Use ${suggestion}.`;
    out.push({
      message,
      line: expr.loc?.line ?? 0,
      column: expr.loc?.column ?? 0
    });
  }
}
function suggestStructuredKey(flatKey) {
  const groups = [
    { prefix: "color", group: "colors" },
    { prefix: "radius", group: "radius" },
    { prefix: "font", group: "font" }
  ];
  for (const { prefix, group } of groups) {
    if (flatKey === prefix) continue;
    if (flatKey.startsWith(prefix) && flatKey.length > prefix.length) {
      const tail = flatKey.slice(prefix.length);
      const inner = tail.charAt(0).toLowerCase() + tail.slice(1);
      return `$theme({ ${group}: { ${inner}: ... } })`;
    }
  }
  return `$theme({ colors: {...}, radius: {...}, font: {...} })`;
}
function editDistance(a, b) {
  const al = a.toLowerCase(), bl = b.toLowerCase();
  const m = al.length, n = bl.length;
  if (m === 0) return n;
  if (n === 0) return m;
  let prev = Array.from({ length: n + 1 }, (_, i) => i);
  let curr = new Array(n + 1);
  for (let i = 1; i <= m; i += 1) {
    curr[0] = i;
    for (let j = 1; j <= n; j += 1) {
      const cost = al[i - 1] === bl[j - 1] ? 0 : 1;
      curr[j] = Math.min(curr[j - 1] + 1, prev[j] + 1, prev[j - 1] + cost);
    }
    [prev, curr] = [curr, prev];
  }
  return prev[n];
}
function suggestComponent(name, library, limit = 3) {
  const query = String(name ?? "").trim();
  if (!query) return [];
  const names = collectSpecs(library).map((s) => s.name);
  if (names.includes(query)) return [query];
  const threshold = Math.max(2, Math.ceil(query.length / 3));
  return names.map((n) => ({ n, d: editDistance(query, n) })).filter((x) => x.d > 0 && x.d <= threshold).sort((a, b) => a.d - b.d || a.n.localeCompare(b.n)).slice(0, Math.max(1, limit)).map((x) => x.n);
}
function collectSpecs(library) {
  const anyLib = library;
  if (Array.isArray(anyLib.components)) return anyLib.components;
  if (typeof anyLib.list === "function") return anyLib.list();
  if (Array.isArray(anyLib.all)) return anyLib.all;
  const out = [];
  for (const v of Object.values(library)) {
    if (v && typeof v === "object" && typeof v.render === "function" && typeof v.name === "string") {
      out.push(v);
    }
  }
  return out;
}
const grammarSpec = {
  name: "aktion",
  atoms: ["true", "false", "null", "undefined"],
  // Full reserved-word set, kept in sync with `KEYWORDS_AKTION` in
  // `src/parser/lexer.ts` plus the reserved top-level handle `aktion` and the
  // router handle `route`. Aktion's `$`-prefixed builtins (`$effect`, `$emit`,
  // …) lex as StateIdentifiers, so they are not in this keyword list. Every
  // entry is documented in `keywordDocs` so the playground can show a popup.
  keywords: [
    "function",
    "import",
    "export",
    "if",
    "else",
    "switch",
    "case",
    "break",
    "continue",
    "for",
    "while",
    "do",
    "of",
    "in",
    "let",
    "var",
    "const",
    "await",
    "async",
    "return",
    "default",
    "try",
    "catch",
    "finally",
    "throw",
    "new",
    "typeof",
    "instanceof",
    "delete",
    "void",
    "aktion",
    "route"
  ],
  operators: ["+", "-", "*", "/", "%", "!", "=", "<", ">", "?", ":", ".", ","],
  // Long operators include `??` and `?.` (nullish coalescing + optional chain)
  // and `...` (spread). Order matters: longest match wins.
  operatorsLong: ["...", "==", "!=", ">=", "<=", "&&", "||", "??", "?."],
  brackets: [
    { open: "(", close: ")" },
    { open: "[", close: "]" },
    { open: "{", close: "}" }
  ],
  comments: { line: "//", blockStart: "/*", blockEnd: "*/" },
  strings: {
    singleLineQuotes: ['"', "'"],
    multiLineQuote: "`"
  },
  identifier: {
    start: /[A-Za-z_]/,
    part: /[A-Za-z0-9_]/
  },
  sigils: { state: "$" }
};
const keywordDocs = {
  aktion: {
    summary: "Legacy entry-point binding — prefer the `$app(...)` builtin to register the root of the rendered UI tree.",
    syntax: "$app(Component(...))",
    example: '$app(Stack([Heading("Hi"), Text("Welcome")]))'
  },
  route: {
    summary: "Reactive router handle — exposes path, params, query, and navigate().",
    syntax: "route.path · route.params · route.query · route.navigate(path)",
    example: 'Button("Home", () => route.navigate("/"))'
  },
  $emit: {
    summary: "Dispatch a custom DOM event from the host element.",
    syntax: '$emit("event-name", detail)',
    example: 'Button("Save", () => $emit("saved", { id: $id }))'
  },
  import: {
    summary: "Import named bindings (components, actions, hooks, `$state`) from another `.aktion` file. Multi-file projects are linked in the browser.",
    syntax: 'import { Name, Other as Alias, $shared } from "./module.aktion"',
    example: 'import { Button } from "./Button.aktion"\nimport { $count } from "./store.aktion"'
  },
  export: {
    summary: "Mark a top-level declaration importable from another file. Anything not exported is private to its file.",
    syntax: "export function Name(...) { ... } · export $state = ... · export name = ...",
    example: "export function Card2({ title }) {\n  return Card([CardHeader(title)])\n}"
  },
  function: {
    summary: "Declare a component or action — first-letter case does not matter.",
    syntax: "function name(params) { ... }",
    example: "function Greeting(name) {\n  return Text(`Hello ${name}`)\n}"
  },
  $effect: {
    summary: "Run a side-effect when dependencies change (timers, fetch, analytics).",
    syntax: "$effect(() => { ... }, [deps])",
    example: '$effect(() => {\n  $now = $util.now()\n}, ["every(1000)"])'
  },
  if: {
    summary: "Conditional statement — run a block when a condition is truthy.",
    syntax: "if (condition) { ... } else { ... }",
    example: "if (!$user) {\n  return Spinner()\n}"
  },
  else: {
    summary: "Fallback branch executed when the matching `if` condition is falsy.",
    syntax: "if (condition) { ... } else { ... }",
    example: "if ($ok) { return Done() } else { return Retry() }"
  },
  switch: {
    summary: "Multi-way branch on a value — pair each arm with `case` / `default`.",
    syntax: "switch (value) { case x: ...; default: ... }",
    example: 'switch (tab) {\n  case "list": return List()\n  default: return Empty()\n}'
  },
  case: {
    summary: "A labelled arm inside a `switch` statement.",
    syntax: "case value: statements; break",
    example: 'case "grid": return Grid($items)'
  },
  break: {
    summary: "Exit the nearest enclosing loop or `switch` immediately.",
    syntax: "break",
    example: "for (let i = 0; i < n; i++) {\n  if (done) break\n}"
  },
  continue: {
    summary: "Skip to the next iteration of the nearest enclosing loop.",
    syntax: "continue",
    example: "for (let x of items) {\n  if (!x.ok) continue\n}"
  },
  for: {
    summary: "Loop — classic `for`, `for…of` (values), or `for…in` (keys).",
    syntax: "for (let i = 0; i < n; i++) { ... }",
    example: "for (let item of $items) {\n  log(item)\n}"
  },
  while: {
    summary: "Loop while a condition stays truthy (checked before each pass).",
    syntax: "while (condition) { ... }",
    example: "while (i > 0) {\n  i = i - 1\n}"
  },
  do: {
    summary: "Run a block once, then repeat while a condition holds.",
    syntax: "do { ... } while (condition)",
    example: "do {\n  step()\n} while (hasMore)"
  },
  of: {
    summary: "Iterate the VALUES of an array/iterable in a `for…of` loop.",
    syntax: "for (let item of iterable) { ... }",
    example: "for (let row of $rows) { render(row) }"
  },
  in: {
    summary: "Iterate the KEYS of an object (`for…in`) or test key membership.",
    syntax: 'for (let key in obj) { ... }  ·  "key" in obj',
    example: "for (let k in $config) { use(k) }"
  },
  let: {
    summary: "Declare a block-scoped mutable variable (supports destructuring).",
    syntax: "let name = value  ·  let [a, b] = arr  ·  let { x } = obj",
    example: "let { name, age = 0 } = $user"
  },
  var: {
    summary: "Declare a variable (function-scoped) — `let` is preferred.",
    syntax: "var name = value",
    example: "var total = 0"
  },
  const: {
    summary: "Declare a block-scoped constant binding (supports destructuring).",
    syntax: "const name = value",
    example: "const [first, ...rest] = $items"
  },
  await: {
    summary: "Parses, but NEVER suspends — bodies are not async functions. In expression position the value is the PROMISE, not what it resolves to, so `if (await p)` is always true; as a bare statement it is dropped. Chain `.then(...)` instead, or use `$http(...).onDone`.",
    syntax: "await expression",
    example: '$util.copy($url).then(ok => { if (ok) $toast.success("Copied") })'
  },
  async: {
    summary: "Marks a function as async — accepted as a no-op modifier; it does NOT make the body suspend.",
    syntax: "async function name() { ... }",
    example: 'function load() {\n  fetch("/api/ping").then(res => { $status = res.status })\n}'
  },
  return: {
    summary: "Return a value from a component / action / helper body.",
    syntax: "return expression",
    example: 'return Card({ title: "Done" })'
  },
  default: {
    summary: "The fallback arm of a `switch` (or wildcard route in Router).",
    syntax: "default: statements",
    example: "default: return NotFound()"
  },
  try: {
    summary: "Run code that may throw, handling errors in a `catch` block.",
    syntax: "try { ... } catch (e) { ... } finally { ... }",
    example: "try {\n  risky()\n} catch (e) {\n  log(e)\n}"
  },
  catch: {
    summary: "Handle an error thrown inside the preceding `try` block.",
    syntax: "catch (error) { ... }",
    example: "catch (e) { $error = e }"
  },
  finally: {
    summary: "Run cleanup after `try` / `catch`, regardless of outcome.",
    syntax: "finally { ... }",
    example: "finally { $loading = false }"
  },
  throw: {
    summary: "Raise an error — caught by an enclosing `try` / `catch`.",
    syntax: "throw value",
    example: 'throw "invalid state"'
  },
  new: {
    summary: "Construct an instance of a host class (Date, Map, Set, …).",
    syntax: "new Constructor(args)",
    example: "let now = new Date()"
  },
  typeof: {
    summary: `Return a string naming a value's type ("number", "string", …).`,
    syntax: "typeof value",
    example: 'if (typeof x === "number") { ... }'
  },
  instanceof: {
    summary: "Test whether a value is an instance of a constructor.",
    syntax: "value instanceof Constructor",
    example: "if (d instanceof Date) { ... }"
  },
  delete: {
    summary: "Remove a property from an object.",
    syntax: "delete obj.prop",
    example: "delete $form.draft"
  },
  void: {
    summary: "Evaluate an expression and yield `undefined`.",
    syntax: "void expression",
    example: "void sideEffect()"
  }
};
function createStreamTokenizer(spec = grammarSpec) {
  const startState = () => ({ inBacktick: false, inBlockComment: false });
  const copyState = (state) => ({ ...state });
  const atomSet = new Set(spec.atoms);
  const keywordSet = new Set(spec.keywords);
  const componentCallRe = /^[A-Z][A-Za-z0-9_]*/;
  const lowerIdentRe = /^[a-z_][A-Za-z0-9_]*/;
  const numberRe = /^-?\d+(?:\.\d+)?/;
  const operatorChars = /* @__PURE__ */ new Set([...spec.operators.join(""), ..."=<>!&|"]);
  const consumeStringBody = (stream, quote) => {
    while (!stream.eol()) {
      const ch = stream.next();
      if (!ch) return;
      if (ch === "\\") {
        stream.next();
        continue;
      }
      if (ch === quote) return;
    }
  };
  const token = (stream, state) => {
    if (state.inBacktick) {
      while (!stream.eol()) {
        const ch = stream.next();
        if (ch === "\\") {
          stream.next();
          continue;
        }
        if (ch === "`") {
          state.inBacktick = false;
          return "string";
        }
      }
      return "string";
    }
    if (state.inBlockComment) {
      while (!stream.eol()) {
        if (stream.match(spec.comments.blockEnd, true)) {
          state.inBlockComment = false;
          return "comment";
        }
        stream.next();
      }
      return "comment";
    }
    if (stream.eatWhile(/[ \t]/)) return null;
    if (stream.match(spec.comments.line, true)) {
      stream.skipToEnd();
      return "comment";
    }
    if (stream.match(spec.comments.blockStart, true)) {
      state.inBlockComment = true;
      while (!stream.eol()) {
        if (stream.match(spec.comments.blockEnd, true)) {
          state.inBlockComment = false;
          return "comment";
        }
        stream.next();
      }
      return "comment";
    }
    const next = stream.peek();
    if (next === null || next === void 0) return null;
    if (spec.strings.singleLineQuotes.includes(next)) {
      const quote = next;
      stream.next();
      consumeStringBody(stream, quote);
      return "string";
    }
    if (next === spec.strings.multiLineQuote) {
      stream.next();
      while (!stream.eol()) {
        const ch = stream.next();
        if (ch === "\\") {
          stream.next();
          continue;
        }
        if (ch === "`") return "string";
      }
      state.inBacktick = true;
      return "string";
    }
    if (next >= "0" && next <= "9") {
      if (stream.match(numberRe, true)) return "number";
    }
    if (next === "-" && stream.match(/^-\d+(?:\.\d+)?/, false)) {
      if (stream.match(numberRe, true)) return "number";
    }
    if (next === spec.sigils.state) {
      stream.next();
      if (stream.peek() === spec.sigils.state) stream.next();
      stream.eatWhile(spec.identifier.part);
      return "state";
    }
    if (spec.identifier.start.test(next)) {
      const upper = stream.match(componentCallRe, true);
      if (upper) {
        return "component";
      }
      const lower = stream.match(lowerIdentRe, true);
      if (lower) {
        const text = lower[0];
        if (atomSet.has(text)) return "atom";
        if (keywordSet.has(text)) return "keyword";
        const ahead = stream.peek();
        if (ahead === ".") return "loopvar";
        return "identifier";
      }
    }
    for (const op of spec.operatorsLong) {
      if (stream.match(op, true)) return "operator";
    }
    if (next === ".") {
      stream.next();
      if (stream.peek() && spec.identifier.start.test(stream.peek())) {
        stream.eatWhile(spec.identifier.part);
        return "property";
      }
      return "punctuation";
    }
    if ("()[]{},:?".includes(next)) {
      stream.next();
      return "punctuation";
    }
    if (operatorChars.has(next)) {
      stream.next();
      return "operator";
    }
    stream.next();
    return null;
  };
  return {
    startState,
    copyState,
    token,
    languageData: {
      commentTokens: { line: spec.comments.line, block: { open: spec.comments.blockStart, close: spec.comments.blockEnd } },
      closeBrackets: { brackets: ["(", "[", "{", '"', "'", "`"] },
      indentOnInput: /^\s*[)\]}]$/
    }
  };
}
const defaultTagMap = {
  comment: "comment",
  string: "string",
  number: "number",
  atom: "atom",
  keyword: "keyword",
  state: "variableName.special",
  component: "typeName",
  identifier: "variableName",
  loopvar: "variableName.local",
  property: "propertyName",
  operator: "operator",
  punctuation: null
};
const builtinCatalog = [
  // Hooks (per-instance state — mirror React's use* family).
  {
    name: "state",
    sigil: "$state",
    category: "hook",
    signature: "$state(initial)",
    summary: "Hook: per-instance state → [value, setValue] (like React's useState)."
  },
  {
    name: "memo",
    sigil: "$memo",
    category: "hook",
    signature: "$memo(() => value, [deps])",
    summary: "Hook: value recomputed only when a dependency changes (like useMemo)."
  },
  {
    name: "ref",
    sigil: "$ref",
    category: "hook",
    signature: "$ref(initial)",
    summary: "Hook: stable { current } box; writing .current does not re-render (like useRef)."
  },
  {
    name: "reducer",
    sigil: "$reducer",
    category: "hook",
    signature: "$reducer((state, action) => next, initial)",
    summary: "Hook: [state, dispatch] (like useReducer)."
  },
  {
    name: "id",
    sigil: "$id",
    category: "hook",
    signature: "$id(prefix?)",
    summary: "Hook: stable unique id per component instance (like useId)."
  },
  // Effects + reactivity.
  {
    name: "effect",
    sigil: "$effect",
    category: "effect",
    signature: "$effect(() => { … }, [deps])",
    summary: 'Declarative side effect; deps mix $state, "mount"/"unmount", "every(N)", "debounce(N)".'
  },
  {
    name: "optimistic",
    sigil: "$optimistic",
    category: "effect",
    signature: "$optimistic(() => { … })",
    summary: "Run optimistic writes; auto-rolls back state if the callback throws or rejects."
  },
  {
    name: "store",
    sigil: "$store",
    category: "effect",
    signature: "$store({ ...state, ...methods })",
    summary: 'Global store: shared state + actions (like Zustand/Pinia). persist: "key" mirrors data to localStorage (persistIn: "session" for sessionStorage); history: true|depth adds undo()/redo()/clearHistory() + reactive canUndo/canRedo.'
  },
  {
    name: "form",
    sigil: "$form",
    category: "effect",
    signature: "$form({ values, rules, onSubmit })",
    summary: "Reactive form engine: values/errors/touched/dirty/valid/submitting/validating + field()/validate()/touch()/setField()/submit() (alias handleSubmit())/reset(). Async rules ($util.rules.asyncCustom) are awaited before submit; submitting stays true until an async onSubmit settles."
  },
  // Data layer.
  {
    name: "http",
    sigil: "$http",
    category: "data",
    signature: "$http({ url, method, … })",
    summary: "Reactive HTTP resource bag — { data, loading, error, refetch }."
  },
  {
    name: "query",
    sigil: "$query",
    category: "data",
    signature: "$query({ url, key, ttl })",
    summary: "Cached + deduplicated HTTP read. Polling via refetchInterval/refetchOnFocus/refetchOnReconnect; pagination via infinite: { param, limit, mode } (→ .loadMore()/.hasMore/.loadingMore); GraphQL via gql + variables."
  },
  {
    name: "mutation",
    sigil: "$mutation",
    category: "data",
    signature: "$mutation({ url, method })",
    summary: "Deferred write; fires on .mutate(overrides?). optimistic: (vars) => {…} applies instantly (auto-rollback on failure); invalidates: [keys] refetches matching cached queries; gql for GraphQL."
  },
  {
    name: "socket",
    sigil: "$socket",
    category: "data",
    signature: "$socket({ url, reconnect? })",
    summary: 'Reactive WebSocket — { status: "connecting"|"open"|"closed", connected, last, messages, attempts, send, close }. reconnect: true|n retries with exponential backoff; sends queue while connecting and flush on open; close() stops for good.'
  },
  {
    name: "sse",
    sigil: "$sse",
    category: "data",
    signature: "$sse({ url, event })",
    summary: 'Reactive Server-Sent Events stream — { status, connected, last, messages, close }. EventSource reconnects natively (status reads "connecting" while it does).'
  },
  {
    name: "script",
    sigil: "$script",
    category: "data",
    signature: "$script({ src, global? })",
    summary: "Load an external UMD/ESM script or stylesheet once → reactive { ready, loading, error, value }. `global` reads window[global] into `value` (e.g. window.Stripe). De-duplicated per src across the app; gate a third-party widget on `.ready`."
  },
  {
    name: "head",
    sigil: "$head",
    category: "app",
    signature: "$head({ title, meta, og, twitter, link, jsonLd })",
    summary: "Reactive document head: title, meta description, canonical/alternate links, Open Graph + Twitter cards, JSON-LD, <html> attrs. Per-route calls compose; renderToString emits the resolved <head> so SSR pages are crawlable."
  },
  // App / routing / theming / events.
  {
    name: "app",
    sigil: "$app",
    category: "app",
    signature: "$app(root)",
    summary: "Register the root of the rendered UI tree — every program needs one."
  },
  {
    name: "router",
    sigil: "$router",
    category: "routing",
    signature: "$router({ '/': Home(), default: NotFound() })",
    summary: "Outlet-first router: maps path patterns to component trees."
  },
  {
    name: "theme",
    sigil: "$theme",
    category: "theme",
    signature: "$theme({ colors, radius, font, … })",
    summary: "In-script theme override merged on top of the active base theme. Structured groups: colors/radius/font/spacing/shadows/gradients/zIndex/motion/fonts (web-font import)/icons (custom inline SVG), plus name/direction metadata."
  },
  {
    name: "emit",
    sigil: "$emit",
    category: "event",
    signature: "$emit('name', detail)",
    summary: "Dispatch a CustomEvent from the host element."
  },
  // Namespaces (members reached via `.`).
  {
    name: "util",
    sigil: "$util",
    category: "namespace",
    signature: "$util",
    summary: "Runtime helper + reactive-environment namespace: data helpers ($util.format/.sum/.range/.groupBy…), formatting ($util.slugify/.truncate/.initials/.currency/.percent/.bytes/.relativeTime), misc ($util.copy — async, resolves true on real success —/.sleep/.uuid/.debounceFn/.throttleFn — leading+trailing), styling ($util.style.cx/.gradient/.alpha/.clamp/.token/.toStyle), validators ($util.rules.required/.email/.url/.min/.max/.minLength/.maxLength/.pattern/.oneOf/.matches/.custom/.asyncCustom + .validate/.validateAll), computed ($util.derived(fn)), hooks ($util.onError/.onNavigate/.onRequest/.onResponse/.invalidate), reactive env ($util.scroll/.viewport/.breakpoint/.media/.mouse/.url incl. .url.setQuery/.removeQuery), device ($util.vibrate/.share/.readClipboard/.geolocate/.isOnline/.deviceType/.nativeShell/.isNativeApp), and platform ($util.worker/.registerServiceWorker/.webManifest).",
    namespace: true
  },
  {
    name: "storage",
    sigil: "$storage",
    category: "namespace",
    signature: "$storage",
    summary: "Persistent storage namespace ($storage.local, $storage.session, …).",
    namespace: true
  },
  {
    name: "console",
    sigil: "$console",
    category: "namespace",
    signature: "$console",
    summary: "Console namespace ($console.log, $console.warn, …).",
    namespace: true
  },
  {
    name: "toast",
    sigil: "$toast",
    category: "namespace",
    signature: "$toast",
    summary: "Imperative toasts: $toast.show/.success/.error/.dismiss; reactive .items.",
    namespace: true
  },
  {
    name: "dom",
    sigil: "$dom",
    category: "namespace",
    signature: "$dom",
    summary: "Managed DOM-observer namespace: $dom.onResize(node, cb) / .onIntersect(node, cb, opts?) / .onMutation(node, cb, opts?) (auto-disposed on replan) and .measure(node) → { rect, scroll, viewport }. Pair with Mount/OnMount node refs.",
    namespace: true
  },
  {
    name: "i18n",
    sigil: "$i18n",
    category: "data",
    signature: "$i18n({ translations, … })",
    summary: "Translation bundle → { t, setCurrentLanguage, getCurrentLanguage }."
  }
];
const builtinNames = new Set(
  builtinCatalog.map((b) => b.name)
);
const builtinsByName = Object.freeze(
  Object.fromEntries(builtinCatalog.map((b) => [b.name, b]))
);
function findBuiltin(bareName) {
  return builtinsByName[bareName];
}
function isBuiltinName(bareName) {
  return builtinNames.has(bareName);
}
const method = (name, signature, summary) => ({
  name,
  kind: "method",
  signature,
  summary
});
const prop = (name, summary) => ({
  name,
  kind: "property",
  signature: name,
  summary
});
const utilMembers = [
  // Aggregation
  method("count", "count(items)", "Number of items in an array."),
  method("sum", "sum(items)", "Sum of the numeric values."),
  method("avg", "avg(items)", "Arithmetic mean of the values."),
  method("min", "min(items)", "Smallest numeric value."),
  method("max", "max(items)", "Largest numeric value."),
  method("first", "first(items)", "First element, or null when empty."),
  method("last", "last(items)", "Last element, or null when empty."),
  // Reshaping
  method("filter", "filter(items, field?, op?, value?)", "Keep items where field matches. Operators: == != > < >= <= contains startsWith endsWith."),
  method("find", "find(items, field?, op?, value?)", "First item matching the comparator, or null."),
  method("sort", "sort(items, field?, direction?)", 'Sort by field, "asc" (default) or "desc".'),
  method("groupBy", "groupBy(items, field?)", "Group items into an object keyed by the field value."),
  method("slice", "slice(items, start?, end?)", "Subarray between start and end indices."),
  method("unique", "unique(items, field?)", "De-duplicate an array (optionally by a field)."),
  method("reverse", "reverse(items)", "Reversed copy of the array."),
  method("range", "range(start, end, step?)", "Array of numbers from start to end (inclusive)."),
  method("repeat", "repeat(value, n)", "Array with the value repeated n times."),
  method("pick", "pick(obj, keys)", "Object containing only the listed keys."),
  method("omit", "omit(obj, keys)", "Object without the listed keys."),
  method("chunk", "chunk(items, size)", "Split an array into chunks of the given size."),
  method("flatten", "flatten(items, depth?)", "Flatten nested arrays to the given depth (default 1)."),
  method("zip", "zip(...arrays)", "Combine arrays element-wise into tuples."),
  method("partition", "partition(items, field?, op?, value?)", "Split into [pass, fail] by a comparator."),
  method("keyBy", "keyBy(items, field?)", "Index an array into an object keyed by a field."),
  method("cloneDeep", "cloneDeep(value)", "Deep-clone a value (arrays / objects / dates)."),
  method("merge", "merge(target, ...sources)", "Deep-merge source objects onto a clone of the target."),
  // Formatting
  method("format", "format(value, mode?, options?)", 'Locale number format: "number" | "currency" | "percent" | "compact".'),
  method("formatDate", "formatDate(value, format?)", 'Format a date — a token pattern (MMM D, YYYY-MM-DD) or "relative"/"date"/"time"/"datetime"/"iso".'),
  method("plural", "plural(count, singular, plural?)", "Count + correctly pluralised noun: `3 items` / `1 item`."),
  method("capitalize", "capitalize(text)", "Capitalise the first letter."),
  method("lowercase", "lowercase(text)", "Lowercase the whole string."),
  method("uppercase", "uppercase(text)", "Uppercase the whole string."),
  method("titlecase", "titlecase(text)", "Title-case each word."),
  method("case", "case(text, kind?)", 'Recase: "camel" | "pascal" | "snake" | "kebab".'),
  // Date / time
  method("now", "now()", "Current time as epoch milliseconds."),
  method("today", "today()", "Start of today as an ISO string."),
  method("addDays", "addDays(date, days)", "Date shifted by n days (ISO)."),
  method("addHours", "addHours(date, hours)", "Date shifted by n hours (ISO)."),
  method("diffDays", "diffDays(start, end)", "Whole days between two dates."),
  method("startOfWeek", "startOfWeek(date)", "Start of the week containing the date (ISO)."),
  method("endOfMonth", "endOfMonth(date)", "End of the month containing the date (ISO)."),
  // String / regex
  method("join", "join(items, sep?)", "Join an array into a string with a separator."),
  method("split", "split(text, sep?)", "Split a string into an array on a separator."),
  method("trim", "trim(text)", "Trim surrounding whitespace."),
  method("replace", "replace(text, search, replacement?)", "Replace every occurrence of a substring."),
  method("substring", "substring(text, start, end?)", "Substring between two indices."),
  method("startsWith", "startsWith(text, prefix)", "True when the text starts with the prefix."),
  method("endsWith", "endsWith(text, suffix)", "True when the text ends with the suffix."),
  method("contains", "contains(text, needle)", "True when the text contains the needle."),
  method("match", "match(text, pattern)", "Test the text against a regular-expression pattern."),
  // Math
  method("round", "round(value, decimals?)", "Round to n decimal places."),
  method("floor", "floor(value)", "Round down to an integer."),
  method("ceil", "ceil(value)", "Round up to an integer."),
  method("abs", "abs(value)", "Absolute value."),
  method("clamp", "clamp(value, min, max)", "Constrain a number to a [min, max] range."),
  method("pow", "pow(base, exp)", "Exponentiation (base^exp)."),
  method("sqrt", "sqrt(value)", "Square root."),
  method("random", "random()", "Pseudo-random number in [0, 1)."),
  method("log", "log(value)", "Natural logarithm."),
  // Formatting / misc convenience
  method("slugify", "slugify(text)", 'URL-safe slug: "Hello World" → "hello-world".'),
  method("truncate", "truncate(text, length?, ellipsis?)", "Cut text to n chars with an ellipsis."),
  method("initials", "initials(name, max?)", "Up-to-`max` initials from a full name."),
  method("currency", "currency(value, code?, locale?)", "Locale currency string."),
  method("percent", "percent(value, decimals?)", 'Locale percent string (0.42 → "42%").'),
  method("bytes", "bytes(value)", 'Human-readable byte size (1536 → "1.5 KB").'),
  method("relativeTime", "relativeTime(value)", '"3 minutes ago" / "in 2 days" via Intl.'),
  method("copy", "copy(text)", "Copy to the clipboard. Returns a PROMISE that resolves true only when the write succeeds — `await` never suspends in Aktion, so read it with `$util.copy(x).then(ok => …)`."),
  method("sleep", "sleep(ms?)", "Pause, capped at 60s. Returns a promise — `await` never suspends, so continue in `$util.sleep(ms).then(() => …)`."),
  method("uuid", "uuid()", "Random UUID v4 string."),
  method("debounceFn", "debounceFn(fn, wait?)", "Wrap a function so it fires `wait` ms after the LAST call."),
  method("throttleFn", "throttleFn(fn, wait?)", "Wrap a function to fire at most once per `wait` ms (leading + one trailing)."),
  // Computed + program hooks
  method("derived", "derived(fn)", "Computed reactive value — recomputes from the atoms the lambda reads."),
  method("onError", "onError(fn)", "Program-level error sink — fires with { error, source } when an action throws."),
  method("onNavigate", "onNavigate(fn)", "Navigation guard: return false to block, a path string to redirect, anything else to allow."),
  method("onRequest", "onRequest(fn)", "HTTP request interceptor — a partial return merges over every outgoing request."),
  method("onResponse", "onResponse(fn)", "HTTP response interceptor — replace the response, or `return retry()` to re-issue once (the client awaits what you return; `await` inside the body does not suspend)."),
  method("invalidate", "invalidate(keys)", "Refetch every cached $query whose key contains one of the substrings."),
  // Reactive environment (listeners attach lazily on first read)
  prop("scroll", 'Reactive scroll: .x / .y / .progress (0–1) / .direction ("up"|"down").'),
  prop("viewport", "Reactive viewport: .width / .height."),
  prop("breakpoint", 'Reactive breakpoint: .active ("base"|"sm"|"md"|"lg"|"xl") + boolean .sm/.md/.lg/.xl.'),
  prop("media", "Reactive media flags: .prefersDark / .prefersReducedMotion / .online / .pointer / .portrait."),
  prop("mouse", "Reactive pointer position: .x / .y."),
  // URL + query-param state
  prop("url", "Reactive URL snapshot: .path / .params / .query / .hash + .navigate(to)."),
  method("url.setQuery", "url.setQuery(key, value)", 'Write a query param in place (null/"" drops it) — shareable filter/tab state.'),
  method("url.removeQuery", "url.removeQuery(key)", "Drop a query param from the URL."),
  // Styling helpers ($util.style)
  { name: "style", kind: "namespace", signature: "style", summary: "Safe styling helpers: .cx / .gradient / .alpha / .clamp / .token / .toStyle." },
  method("style.cx", "style.cx(...args)", "clsx-style class composer — strings, arrays, { name: cond } objects."),
  method("style.gradient", "style.gradient(stops, angle?)", "Safe linear-gradient() from color stops + angle."),
  method("style.alpha", "style.alpha(color, amount)", "color-mix transparency: token or color at 0–1 alpha."),
  method("style.clamp", "style.clamp(min, preferred, max)", "Responsive clamp(min, preferred, max) size."),
  method("style.token", "style.token(path)", 'Resolve a theme token path to its CSS var: "colors.primary" → var(--rui-color-primary).'),
  method("style.toStyle", "style.toStyle(obj)", "Serialise a CSS-declarations object to a sanitised style string."),
  // Validators ($util.rules)
  { name: "rules", kind: "namespace", signature: "rules", summary: "Composable validators — compose per field; run with .validate / .validateAll or hand to $form." },
  method("rules.required", "rules.required(message?)", "Non-empty value."),
  method("rules.email", "rules.email(message?)", "Valid email address."),
  method("rules.url", "rules.url(message?)", "Valid http(s) URL."),
  method("rules.min", "rules.min(n, message?)", "Number ≥ n."),
  method("rules.max", "rules.max(n, message?)", "Number ≤ n."),
  method("rules.minLength", "rules.minLength(n, message?)", "String length ≥ n."),
  method("rules.maxLength", "rules.maxLength(n, message?)", "String length ≤ n."),
  method("rules.pattern", "rules.pattern(re, message?)", "Match a regular expression."),
  method("rules.oneOf", "rules.oneOf(options, message?)", "Value is in the allowed list."),
  method("rules.matches", "rules.matches(other, message?)", "Equals another value (password confirmation)."),
  method("rules.custom", "rules.custom(fn, message?)", "Custom sync rule — return true/null (valid), false, or an error string."),
  method("rules.asyncCustom", "rules.asyncCustom(fn, message?)", "Async rule (Promise) — server-side checks; $form awaits it before submitting."),
  method("rules.validate", "rules.validate(value, validators)", "Run validators — first error message or null (a Promise when an async rule is hit)."),
  method("rules.validateAll", "rules.validateAll(values, schema)", "Validate an object against { field: [validators] } → { field: message }."),
  // Device / platform
  method("vibrate", "vibrate(pattern?)", "Haptic pulse (ms or pattern array) on supporting devices."),
  method("share", "share(data)", "Native share sheet (Web Share API) — resolves true on share."),
  method("readClipboard", "readClipboard()", "Read clipboard text (async, permission-gated)."),
  method("geolocate", "geolocate(options?)", "Resolve { lat, lng, accuracy } via the Geolocation API."),
  method("isOnline", "isOnline()", "Current navigator.onLine flag."),
  method("deviceType", "deviceType()", '"mobile" | "tablet" | "desktop" heuristic.'),
  method("worker", "worker(fn, ...args)", "Run a closure-free function in a Web Worker; resolves its return value."),
  method("registerServiceWorker", "registerServiceWorker(url, scope?)", "Register a service worker for PWA/offline."),
  method("webManifest", "webManifest(config)", "Build a sanitised web-app manifest (name, icons, themeColor…)."),
  method("nativeShell", "nativeShell()", 'Detect the wrapper: capacitor/cordova/tauri/electron/react-native or "web".'),
  method("isNativeApp", "isNativeApp()", "True when running inside a native shell.")
];
const storageBackendMembers = (prefix, label) => [
  method(`${prefix}set`, `${prefix}set(key, value, options?)`, `${label} write.`),
  method(`${prefix}get`, `${prefix}get(key)`, `${label} read. Returns null when missing.`),
  method(`${prefix}remove`, `${prefix}remove(key, options?)`, `Delete a key from ${label.toLowerCase()}.`),
  method(`${prefix}clear`, `${prefix}clear()`, `Wipe every ${label.toLowerCase()} entry.`)
];
const storageMembers = [
  ...storageBackendMembers("", "localStorage (default namespace)"),
  { name: "local", kind: "namespace", signature: "local", summary: "localStorage backend — set/get/remove/clear." },
  ...storageBackendMembers("local.", "localStorage"),
  { name: "session", kind: "namespace", signature: "session", summary: "sessionStorage backend (per-tab) — set/get/remove/clear." },
  ...storageBackendMembers("session.", "Per-tab sessionStorage"),
  { name: "cookies", kind: "namespace", signature: "cookies", summary: "Document cookies — set/get/remove/clear (options: expires, maxAge, path, domain, secure, sameSite)." },
  method("cookies.set", "cookies.set(key, value, options?)", "Set a cookie. Options: expires, maxAge, path, domain, secure, sameSite."),
  method("cookies.get", "cookies.get(key)", "Read a cookie value."),
  method("cookies.remove", "cookies.remove(key, options?)", "Delete a cookie. Path/domain must match the original set call."),
  method("cookies.clear", "cookies.clear()", "Clear every cookie on this document.")
];
const consoleMembers = [
  method("log", "log(...args)", "Log a message at the default level."),
  method("error", "error(...args)", "Log an error."),
  method("warn", "warn(...args)", "Log a warning."),
  method("info", "info(...args)", "Log an informational message."),
  method("debug", "debug(...args)", "Log a verbose debug message.")
];
const toastMembers = [
  method("show", "show(message, options?)", "Show a toast; returns its id. Options: { title?, tone?, duration? }."),
  method("success", "success(message, options?)", '`show` with tone "success".'),
  method("error", "error(message, options?)", '`show` with tone "danger".'),
  method("info", "info(message, options?)", '`show` with tone "info".'),
  method("warning", "warning(message, options?)", '`show` with tone "warning".'),
  method("dismiss", "dismiss(id)", "Remove a single toast by id."),
  method("clear", "clear()", "Remove every toast."),
  method("configure", "configure(options)", 'Settings for the auto-rendered stack, e.g. { position: "bottom-center" }. Call once at the top level of the program.'),
  prop("items", "Reactive list of live toasts (newest last). Treat as read-only.")
];
const domMembers = [
  method("onResize", "onResize(node, callback)", "Observe element size with a ResizeObserver; callback gets { width, height, entry }. Returns a disposer; auto-disposed on replan."),
  method("onIntersect", "onIntersect(node, callback, options?)", "Observe viewport intersection with an IntersectionObserver; callback gets the entry. Options: { root?, rootMargin?, threshold? }."),
  method("onMutation", "onMutation(node, callback, options?)", "Observe DOM mutations with a MutationObserver. Options: { childList?, attributes?, subtree?, characterData? }."),
  method("measure", "measure(node)", "One-shot read → { rect, scroll, viewport } (getBoundingClientRect + scroll offsets + window size).")
];
const namespaceCatalog = [
  { name: "util", sigil: "$util", summary: "Runtime helper + reactive-environment namespace.", members: utilMembers },
  { name: "storage", sigil: "$storage", summary: "Browser storage namespace (local / session / cookies).", members: storageMembers },
  { name: "console", sigil: "$console", summary: "Console namespace forwarding to the browser console.", members: consoleMembers },
  { name: "toast", sigil: "$toast", summary: "Imperative toast namespace.", members: toastMembers },
  { name: "dom", sigil: "$dom", summary: "Managed DOM-observer namespace (resize / intersection / mutation / measure).", members: domMembers }
];
const namespacesByName = Object.freeze(
  Object.fromEntries(namespaceCatalog.map((n) => [n.name, n]))
);
const httpResourceMembers = [
  prop("data", "Parsed response body — `null` until the request resolves."),
  prop("error", "`null` on success; `{ status, body }` on a non-2xx; the thrown error on network failure."),
  prop("status", "HTTP status code of the last response, e.g. `200`."),
  prop("loading", "`true` while a request is in flight."),
  prop("headers", "Response headers as a plain object."),
  prop("lastUpdated", "Epoch-ms of the last successful response."),
  method("refetch", "refetch()", "Re-issue the original request."),
  method("cancel", "cancel()", "Abort the in-flight request."),
  prop("onDone", "Settable callback fired each time the request settles (success or error).")
];
const queryResourceMembers = [
  ...httpResourceMembers,
  method("loadMore", "loadMore()", "Fetch the next page (infinite mode)."),
  prop("hasMore", "`true` while more pages are available (infinite mode)."),
  prop("loadingMore", "`true` while a `loadMore()` page is in flight."),
  prop("pages", "Raw page bodies loaded so far (infinite mode); `.data` is the flattened items.")
];
const mutationResourceMembers = [
  method("mutate", "mutate(overrides?)", "Fire the request; overrides shallow-merge over the config. `optimistic` applies instantly and rolls back on failure."),
  prop("data", "Response body of the last successful mutation."),
  prop("error", "`null` on success; error details on failure."),
  prop("loading", "`true` while the mutation request is in flight."),
  prop("status", "HTTP status code of the last response."),
  prop("onDone", "Settable callback fired when the mutation settles.")
];
const socketResourceMembers = [
  prop("status", 'Connection lifecycle: "connecting" | "open" | "closed".'),
  prop("connected", '`true` while status is "open".'),
  prop("last", "Most recent message (JSON auto-parsed), or null."),
  prop("messages", "Buffered messages, newest last (capped to bufferSize)."),
  prop("attempts", "Reconnect attempts in the current streak (resets on success)."),
  prop("error", "Last socket error event, if any."),
  method("send", "send(data)", "Send a message (objects JSON-stringified). Queues while connecting; flushes on open."),
  method("close", "close()", "Close for good — disables auto-reconnect.")
];
const sseResourceMembers = [
  prop("status", '"connecting" | "open" | "closed" (EventSource retries natively).'),
  prop("connected", "`true` while the stream is open."),
  prop("last", "Most recent event payload (JSON auto-parsed)."),
  prop("messages", "Buffered events, newest last (capped to bufferSize)."),
  prop("error", "Last stream error, if any."),
  method("close", "close()", "Close the stream.")
];
const formResourceMembers = [
  prop("values", "Reactive field values — two-way bind with an input's `value`."),
  prop("errors", "Per-field error messages (set after validate/touch/submit)."),
  prop("touched", "Per-field booleans — true once the user has interacted."),
  prop("dirty", "`true` once any value differs from the clean snapshot."),
  prop("valid", "`true` when the last validation pass found no errors."),
  prop("submitting", "`true` from submit() until an async onSubmit settles."),
  prop("validating", "`true` while async rules ($util.rules.asyncCustom) are in flight."),
  method("field", "field(name)", "Controlled prop bag: { value, error, name, onChange, onBlur } — spread onto an input."),
  method("touch", "touch(name)", "Mark a field touched + validate it (wire to `onBlur`)."),
  method("setField", "setField(name, value)", "Set one field value (clears its error)."),
  method("setValues", "setValues(values)", "Merge several field values at once."),
  method("validate", "validate()", "Validate every field → boolean (a Promise when async rules exist)."),
  method("validateField", "validateField(name)", "Validate one field → message | null (Promise for async rules)."),
  method("submit", "submit()", "Touch all → validate → onSubmit(values) when valid. Alias: handleSubmit()."),
  method("handleSubmit", "handleSubmit()", "Alias of submit()."),
  method("reset", "reset()", "Restore initial values; clears errors/touched/dirty.")
];
const scriptResourceMembers = [
  prop("ready", "`true` once the external script / stylesheet has loaded successfully."),
  prop("loading", "`true` while the resource is still downloading."),
  prop("error", "The load error, or `null` on success."),
  prop("value", "The resolved value — `window[global]` for a script with a `global` (e.g. window.Stripe), else `true`. `null` until ready.")
];
const storeResourceMembers = [
  method("undo", "undo()", "Undo the last change (`history: true|depth` stores)."),
  method("redo", "redo()", "Redo the last undone change."),
  prop("canUndo", "Reactive — `true` when an undo step is available."),
  prop("canRedo", "Reactive — `true` when a redo step is available."),
  method("clearHistory", "clearHistory()", "Drop the undo/redo stacks.")
];
const factoryResourceCatalog = [
  { factory: "http", summary: "Reactive HTTP resource bag.", members: httpResourceMembers },
  { factory: "query", summary: "Cached query bag (HTTP + pagination).", members: queryResourceMembers },
  { factory: "mutation", summary: "Deferred mutation bag (fires on .mutate()).", members: mutationResourceMembers },
  { factory: "socket", summary: "Reactive WebSocket bag.", members: socketResourceMembers },
  { factory: "sse", summary: "Reactive Server-Sent Events bag.", members: sseResourceMembers },
  { factory: "script", summary: "External script / stylesheet load bag.", members: scriptResourceMembers },
  { factory: "form", summary: "Managed form engine bag.", members: formResourceMembers },
  { factory: "store", summary: "Global store handle (built-in history methods).", members: storeResourceMembers }
];
const factoriesByName = Object.freeze(
  Object.fromEntries(factoryResourceCatalog.map((f) => [f.factory, f]))
);
const factoryResourceNames = new Set(
  factoryResourceCatalog.map((f) => f.factory)
);
const routeMembers = [
  prop("path", "Current URL path, e.g. `/users/42`."),
  prop("params", "Captured path segments from the matched route pattern (`route.params.id`)."),
  prop("query", "Parsed query-string parameters as an object."),
  prop("pattern", "The matched route pattern, or `null`."),
  method("navigate", "navigate(to)", "Imperatively navigate to a path.")
];
const i18nResultMembers = [
  method("t", "t(key, vars?)", "Translate a key for the current language; interpolates {name} placeholders + ICU plural/select."),
  method("setCurrentLanguage", "setCurrentLanguage(lang)", "Switch the active language."),
  method("getCurrentLanguage", "getCurrentLanguage()", "Return the active language code.")
];
function findNamespace(bareName) {
  return namespacesByName[bareName];
}
function isNamespaceName(bareName) {
  return bareName in namespacesByName;
}
function findFactoryResource(factory) {
  return factoriesByName[factory];
}
function namespaceMembersAt(bareName, path = []) {
  const ns = namespacesByName[bareName];
  if (!ns) return [];
  if (path.length === 0) return ns.members.slice();
  const prefix = `${path.join(".")}.`;
  const out = [];
  for (const m of ns.members) {
    if (m.name.startsWith(prefix)) {
      out.push({ ...m, name: m.name.slice(prefix.length) });
    }
  }
  return out;
}
function findNamespaceMember(bareName, memberPath) {
  const ns = namespacesByName[bareName];
  if (!ns) return void 0;
  return ns.members.find((m) => m.name === memberPath);
}
const cfg = (name, type, summary) => ({ name, type, summary });
const httpConfigKeys = [
  cfg("url", "string", "Absolute request URL."),
  cfg("method", 'enum: "GET" | "POST" | "PUT" | "PATCH" | "DELETE" | "HEAD" | "OPTIONS"', "HTTP method. Defaults to GET."),
  cfg("query", "object", "Object serialised into the URL querystring (`?k=v`)."),
  cfg("headers", "object", "Request headers as a plain object."),
  cfg("body", "object", "Request body. Objects are JSON-encoded automatically."),
  cfg("credentials", 'enum: "omit" | "same-origin" | "include"', "Fetch credentials mode."),
  cfg("mode", 'enum: "cors" | "no-cors" | "same-origin"', "Fetch request mode."),
  cfg("cache", 'enum: "default" | "no-store" | "reload" | "no-cache" | "force-cache"', "Fetch cache mode."),
  cfg("gql", "string", "GraphQL query — POSTs { query, variables }; `.data` is the unwrapped GraphQL data."),
  cfg("variables", "object", "GraphQL variables paired with `gql`.")
];
const queryConfigKeys = [
  ...httpConfigKeys,
  cfg("key", "string", "Cache key — identical keys share one in-flight request + cached bag."),
  cfg("ttl", "number", "Milliseconds before cached data is considered stale and auto-refetched."),
  cfg("refetchInterval", "number", "Poll interval in ms (live dashboards)."),
  cfg("refetchOnFocus", "boolean", "Refetch when the tab regains focus."),
  cfg("refetchOnReconnect", "boolean", "Refetch when the network reconnects."),
  cfg("infinite", "object", "Pagination config: { param?, start?, limit?, mode?, select? } → .loadMore()/.hasMore.")
];
const mutationConfigKeys = [
  cfg("url", "string", "Absolute request URL."),
  cfg("method", 'enum: "POST" | "PUT" | "PATCH" | "DELETE"', "HTTP method. Defaults to POST."),
  cfg("body", "object", "Default body; shallow-merged with `.mutate(overrides)`."),
  cfg("headers", "object", "Request headers as a plain object."),
  cfg("query", "object", "Object serialised into the URL querystring."),
  cfg("optimistic", "(vars) => void", "Runs synchronously before the request; auto-rolled-back on failure."),
  cfg("invalidates", "string[]", "Refetch every cached $query whose key contains a listed substring on success."),
  cfg("gql", "string", "GraphQL mutation document."),
  cfg("variables", "object", "GraphQL variables paired with `gql`.")
];
const socketConfigKeys = [
  cfg("url", "string", "WebSocket URL (ws:// or wss://)."),
  cfg("protocols", "string | string[]", "Optional sub-protocol(s)."),
  cfg("bufferSize", "number", "Max buffered messages kept in `.messages`."),
  cfg("onMessage", "(msg) => void", "Callback fired for each received message."),
  cfg("reconnect", "boolean | number", "Retry dropped connections (true, or a max-attempt count) with backoff.")
];
const sseConfigKeys = [
  cfg("url", "string", "EventSource URL."),
  cfg("event", "string", "Named event to listen for (defaults to message)."),
  cfg("withCredentials", "boolean", "Send credentials with the EventSource request."),
  cfg("bufferSize", "number", "Max buffered events kept in `.messages`.")
];
const scriptConfigKeys = [
  cfg("src", "string", "URL of the script (or stylesheet) to load. De-duplicated per src."),
  cfg("global", "string", 'Name of the window global the script defines — read into `.value` once ready (e.g. "Stripe").'),
  cfg("type", "string", 'Script type attribute (e.g. "module" for ESM).'),
  cfg("as", 'enum: "script" | "style"', "Force the resource kind. Inferred from a `.css` src otherwise."),
  cfg("attributes", "object", "Extra attributes to set on the injected <script>/<link> (e.g. crossorigin, integrity).")
];
const headConfigKeys = [
  cfg("title", "string", "Document title (sets document.title + <title>)."),
  cfg("titleTemplate", "string", 'Wrap `title` with a template, e.g. "%s — Acme".'),
  cfg("meta", "object", 'Named meta tags: { description, "theme-color", keywords, … } → <meta name content>.'),
  cfg("og", "object", 'Open Graph tags: { title, image, type, … } → <meta property="og:KEY">.'),
  cfg("twitter", "object", 'Twitter card tags: { card, site, … } → <meta name="twitter:KEY">.'),
  cfg("link", "object[]", 'Array of <link> descriptors, e.g. [{ rel: "canonical", href }].'),
  cfg("jsonLd", "object | object[]", 'JSON-LD structured data → <script type="application/ld+json">. @context defaults to schema.org.'),
  cfg("base", "string | object", "<base href> for the document."),
  cfg("htmlAttrs", "object", 'Attributes for the <html> element, e.g. { lang: "en", dir: "ltr" }.')
];
const formConfigKeys = [
  cfg("values", "object", "Initial field values — the clean snapshot."),
  cfg("rules", "object", "Per-field validator arrays: { field: [$util.rules.required(), …] }."),
  cfg("onSubmit", "(values) => void", "Called with the values once validation passes.")
];
const storeConfigKeys = [
  cfg("persist", "string", "Mirror the store's data to localStorage under this key (hydrates on first render)."),
  cfg("persistIn", 'enum: "local" | "session"', "Storage backend for `persist` (defaults to local)."),
  cfg("history", "boolean | number", "Enable undo()/redo()/clearHistory() + reactive canUndo/canRedo (number = depth).")
];
const themeConfigKeys = [
  cfg("name", "string", 'Selects a built-in base theme ("light", "dark", "shadcn"/"-light"/"-dark", "mui"/"-light"/"-dark", "heroui"/"-light"/"-dark", "soft").'),
  cfg("direction", 'enum: "ltr" | "rtl"', "Reading direction (metadata)."),
  cfg("colors", "object", "CSS color tokens: bg, surface, border, text, primary, accent, success, warning, danger, info, …."),
  cfg("radius", "object", "Border-radius tokens: xs, sm, md, lg, pill, button, input."),
  cfg("font", "object", "Font tokens: family, familyHeading, familyMono, sizeBase, weightBody, …."),
  cfg("spacing", "object", "Spacing scale tokens."),
  cfg("shadows", "object", "Box-shadow tokens."),
  cfg("gradients", "object", "Gradient color-stop arrays — referenced as gradient.<name>."),
  cfg("zIndex", "object", "Layer tokens (modal, toast, …) → sx.zIndex / --rui-z-*."),
  cfg("motion", "object", "Motion tokens: { fast, base, slow, ease } → --rui-motion-*."),
  cfg("fonts", "object", 'Web-font import: { import: ["Inter:400,700"] }.'),
  cfg("icons", "object", "Custom inline-SVG icons by name, usable anywhere an icon name is.")
];
const i18nConfigKeys = [
  cfg("defaultLanguage", "string", "Fallback language when a key is missing for the current language."),
  cfg("currentLanguage", "string", "Active language — drive from a reactive atom for live switching."),
  cfg("translations", "object", '{ key: { lang: "text {name}" } }. Supports ICU plural/select.')
];
const builtinConfigByName = Object.freeze({
  http: httpConfigKeys,
  query: queryConfigKeys,
  mutation: mutationConfigKeys,
  socket: socketConfigKeys,
  sse: sseConfigKeys,
  script: scriptConfigKeys,
  head: headConfigKeys,
  form: formConfigKeys,
  store: storeConfigKeys,
  theme: themeConfigKeys,
  i18n: i18nConfigKeys
});
function findBuiltinConfig(bareName) {
  return builtinConfigByName[bareName];
}
const DEFAULT_OFFSET = 6;
const VIEWPORT_MARGIN = 8;
const LAYER_Z = {
  dropdown: "var(--rui-z-dropdown, 1000)",
  popover: "var(--rui-z-popover, 1350)",
  tooltip: "var(--rui-z-tooltip, 1500)"
};
const OPEN = /* @__PURE__ */ new WeakMap();
function deferToPaint(fn) {
  let done = false;
  const run = () => {
    if (done) return;
    done = true;
    fn();
  };
  const raf = typeof requestAnimationFrame === "function" ? requestAnimationFrame(run) : 0;
  const timer = setTimeout(run, 0);
  return () => {
    done = true;
    if (raf) cancelAnimationFrame(raf);
    clearTimeout(timer);
  };
}
const supportsPopover = () => typeof HTMLElement !== "undefined" && typeof HTMLElement.prototype.showPopover === "function";
function ensureLayer(root) {
  const existing = root.querySelector(".rui-layer");
  if (existing) return existing;
  const layer = document.createElement("div");
  layer.className = "rui-layer";
  layer.style.cssText = "position:fixed;inset:0;pointer-events:none;z-index:var(--rui-z-popover,1350)";
  root.appendChild(layer);
  return layer;
}
function measure(panel, anchor, opts) {
  const side = opts.side ?? "bottom";
  const align = opts.align ?? "start";
  const offset = opts.offset ?? DEFAULT_OFFSET;
  const flip = opts.flip !== false;
  const shift = opts.shift !== false;
  const a = anchor.getBoundingClientRect();
  const p = panel.getBoundingClientRect();
  const vw = window.innerWidth || document.documentElement.clientWidth || 0;
  const vh = window.innerHeight || document.documentElement.clientHeight || 0;
  if (vw <= 0 || vh <= 0) {
    const fallbackSide = side;
    let fl;
    let ft;
    if (fallbackSide === "bottom" || fallbackSide === "top") {
      ft = fallbackSide === "bottom" ? a.bottom + offset : a.top - offset - p.height;
      fl = align === "center" ? a.left + a.width / 2 - p.width / 2 : align === "end" ? a.right - p.width : a.left;
    } else {
      fl = fallbackSide === "right" ? a.right + offset : a.left - offset - p.width;
      ft = align === "center" ? a.top + a.height / 2 - p.height / 2 : align === "end" ? a.bottom - p.height : a.top;
    }
    return { left: Math.round(fl), top: Math.round(ft), side: fallbackSide, maxHeight: null };
  }
  const room = {
    top: a.top - VIEWPORT_MARGIN,
    bottom: vh - a.bottom - VIEWPORT_MARGIN,
    left: a.left - VIEWPORT_MARGIN,
    right: vw - a.right - VIEWPORT_MARGIN
  };
  const need = {
    top: p.height + offset,
    bottom: p.height + offset,
    left: p.width + offset,
    right: p.width + offset
  };
  let chosen = side;
  if (flip && room[side] < need[side]) {
    const opposite = {
      top: "bottom",
      bottom: "top",
      left: "right",
      right: "left"
    };
    const alt = opposite[side];
    if (room[alt] > room[side]) chosen = alt;
  }
  let left = 0;
  let top = 0;
  if (chosen === "bottom" || chosen === "top") {
    top = chosen === "bottom" ? a.bottom + offset : a.top - offset - p.height;
    if (align === "start") left = a.left;
    else if (align === "center") left = a.left + a.width / 2 - p.width / 2;
    else left = a.right - p.width;
  } else {
    left = chosen === "right" ? a.right + offset : a.left - offset - p.width;
    if (align === "start") top = a.top;
    else if (align === "center") top = a.top + a.height / 2 - p.height / 2;
    else top = a.bottom - p.height;
  }
  if (shift) {
    const maxLeft = vw - p.width - VIEWPORT_MARGIN;
    left = Math.min(Math.max(left, VIEWPORT_MARGIN), Math.max(VIEWPORT_MARGIN, maxLeft));
    if (chosen === "left" || chosen === "right") {
      const maxTop = vh - p.height - VIEWPORT_MARGIN;
      top = Math.min(Math.max(top, VIEWPORT_MARGIN), Math.max(VIEWPORT_MARGIN, maxTop));
    }
  }
  let maxHeight = null;
  const cap = opts.maxHeight ?? "viewport";
  if (cap === "viewport") {
    const avail = chosen === "bottom" ? vh - (a.bottom + offset) - VIEWPORT_MARGIN : chosen === "top" ? a.top - offset - VIEWPORT_MARGIN : vh - 2 * VIEWPORT_MARGIN;
    maxHeight = Math.max(96, Math.floor(avail));
    if (p.height <= maxHeight) maxHeight = null;
    else if (chosen === "top") top = VIEWPORT_MARGIN;
  } else if (typeof cap === "number") {
    maxHeight = cap;
  }
  return { left: Math.round(left), top: Math.round(top), side: chosen, maxHeight };
}
function applyPosition(panel, m, opts) {
  const s = panel.style;
  s.setProperty("position", "fixed", "important");
  s.setProperty("inset", "auto", "important");
  s.setProperty("margin", "0", "important");
  s.setProperty("left", `${m.left}px`, "important");
  s.setProperty("top", `${m.top}px`, "important");
  s.setProperty("right", "auto", "important");
  s.setProperty("bottom", "auto", "important");
  s.setProperty("transform", "none", "important");
  if (m.maxHeight != null) {
    s.setProperty("max-height", `${m.maxHeight}px`, "important");
    s.setProperty("overflow-y", "auto", "important");
  } else {
    s.removeProperty("max-height");
    s.removeProperty("overflow-y");
  }
  panel.setAttribute("data-floating-side", m.side);
  if (!supportsPopover()) {
    s.setProperty("z-index", LAYER_Z[opts.layer ?? "dropdown"], "important");
  }
}
function openFloating(panel, opts) {
  const prior = OPEN.get(panel);
  if (prior) {
    prior.update();
    return prior;
  }
  const savedStyle = panel.getAttribute("style");
  const originalParent = panel.parentElement;
  const originalNext = panel.nextSibling;
  let promoted = "none";
  if (opts.matchAnchorWidth) {
    const w = Math.round(opts.anchor.getBoundingClientRect().width);
    const min = opts.minWidth ?? 0;
    panel.style.setProperty("width", `${Math.max(w, min)}px`, "important");
  }
  if (supportsPopover()) {
    try {
      panel.setAttribute("popover", "manual");
      panel.showPopover();
      promoted = "popover";
    } catch {
      panel.removeAttribute("popover");
    }
  }
  if (promoted === "none") {
    const root = panel.getRootNode();
    if (root instanceof ShadowRoot || root instanceof Document) {
      const shown = getComputedStyle(panel).display;
      const layer = ensureLayer(root);
      layer.appendChild(panel);
      panel.style.setProperty("display", shown === "none" ? "flex" : shown, "important");
      panel.style.setProperty("pointer-events", "auto", "important");
      promoted = "layer";
      warnIfTrapped(panel, "A floating panel");
    }
  }
  const update = () => {
    if (!panel.isConnected) return;
    applyPosition(panel, measure(panel, opts.anchor, opts), opts);
  };
  update();
  const cancelDeferred = deferToPaint(update);
  const onScroll = () => update();
  window.addEventListener("scroll", onScroll, { capture: true, passive: true });
  window.addEventListener("resize", onScroll, { passive: true });
  let ro = null;
  if (typeof ResizeObserver !== "undefined") {
    ro = new ResizeObserver(() => update());
    try {
      ro.observe(panel);
      ro.observe(opts.anchor);
    } catch {
    }
  }
  const reg = {
    panel,
    update,
    close: () => {
      if (!OPEN.has(panel)) return;
      OPEN.delete(panel);
      cancelDeferred();
      window.removeEventListener("scroll", onScroll, { capture: true });
      window.removeEventListener("resize", onScroll);
      ro?.disconnect();
      if (promoted === "popover") {
        try {
          panel.hidePopover();
        } catch {
        }
        panel.removeAttribute("popover");
      } else if (promoted === "layer" && originalParent) {
        if (originalNext && originalNext.parentNode === originalParent) {
          originalParent.insertBefore(panel, originalNext);
        } else {
          originalParent.appendChild(panel);
        }
      }
      panel.removeAttribute("data-floating-side");
      if (savedStyle == null) panel.removeAttribute("style");
      else panel.setAttribute("style", savedStyle);
    }
  };
  OPEN.set(panel, reg);
  return reg;
}
function closeFloating(panel) {
  if (!panel) return;
  OPEN.get(panel)?.close();
}
function updateFloating(panel) {
  if (!panel) return;
  OPEN.get(panel)?.update();
}
function isFloating(panel) {
  return !!panel && OPEN.has(panel);
}
const OVERLAY_UA_RESET = "width:auto;height:auto;max-width:none;max-height:none;margin:0;border:0;color:inherit";
const OVERLAY_FILL = "inset:0;padding:0;background:transparent;overflow:visible";
const OVERLAY_FILL_CLIP = "inset:0;padding:0;background:transparent;overflow:hidden";
const OVERLAY_CORNER = "padding:0;background:transparent;overflow:visible";
const PROMOTED_OVERLAYS = /* @__PURE__ */ new WeakMap();
function parentOrHost(node) {
  if (node.parentElement) return node.parentElement;
  const root = node.getRootNode();
  return typeof ShadowRoot !== "undefined" && root instanceof ShadowRoot ? root.host : null;
}
function trapsFixed(cs) {
  const get = (p) => cs.getPropertyValue(p) || "";
  const notNone = (p) => {
    const v = get(p).trim();
    return v !== "" && v !== "none";
  };
  return notNone("transform") || notNone("filter") || notNone("backdrop-filter") || notNone("perspective") || /transform|filter|perspective/.test(get("will-change")) || /\b(paint|layout|strict|content)\b/.test(get("contain"));
}
const TRAP_WARNED = /* @__PURE__ */ new WeakSet();
function warnIfTrapped(surface, what) {
  if (TRAP_WARNED.has(surface) || typeof getComputedStyle !== "function") return;
  let node = parentOrHost(surface);
  for (let hops = 0; node && hops < 64; hops += 1) {
    let trapped = false;
    try {
      trapped = trapsFixed(getComputedStyle(node));
    } catch {
      return;
    }
    if (trapped) {
      TRAP_WARNED.add(surface);
      const cls = node.getAttribute("class");
      console.warn(
        `[aktion] ${what} could not be promoted to the browser top layer, and the ancestor <${node.tagName.toLowerCase()}${cls ? ` class="${cls}"` : ""}> establishes a containing block (transform / filter / backdrop-filter / will-change / contain), so the overlay's \`position: fixed\` resolves against that element instead of the viewport. Remove the \`animate\`/\`sx\` transform from that ancestor, or move the overlay out of it.`
      );
      return;
    }
    node = parentOrHost(node);
  }
}
function promoteOverlay$1(surface, extraStyle) {
  if (PROMOTED_OVERLAYS.has(surface) || surface.hasAttribute("data-floating-side")) return true;
  const label = `<${surface.tagName.toLowerCase()}> overlay`;
  if (!supportsPopover()) {
    warnIfTrapped(surface, label);
    return false;
  }
  const saved = surface.getAttribute("style");
  try {
    surface.setAttribute("popover", "manual");
    surface.showPopover();
  } catch {
    surface.removeAttribute("popover");
    warnIfTrapped(surface, label);
    return false;
  }
  PROMOTED_OVERLAYS.set(surface, saved);
  const reset = extraStyle ? `${OVERLAY_UA_RESET};${extraStyle}` : OVERLAY_UA_RESET;
  surface.setAttribute("style", saved ? `${saved};${reset}` : reset);
  surface.setAttribute("data-floating-side", "overlay");
  return true;
}
function releaseOverlay(surface) {
  if (!surface || !PROMOTED_OVERLAYS.has(surface)) return;
  const saved = PROMOTED_OVERLAYS.get(surface) ?? null;
  PROMOTED_OVERLAYS.delete(surface);
  try {
    surface.hidePopover();
  } catch {
  }
  surface.removeAttribute("popover");
  surface.removeAttribute("data-floating-side");
  if (saved == null) surface.removeAttribute("style");
  else surface.setAttribute("style", saved);
}
function syncFloatingPanel(liveRoot, open, panelSelector, triggerSelector, opts = {}) {
  const panel = liveRoot.querySelector(panelSelector);
  const anchor = liveRoot.querySelector(triggerSelector) ?? liveRoot;
  if (!panel) return null;
  if (!open) {
    closeFloating(panel);
    return panel;
  }
  const side = liveRoot.getAttribute("data-side") ?? void 0;
  const align = liveRoot.getAttribute("data-align") ?? void 0;
  openFloating(panel, { anchor, side, align, ...opts });
  return panel;
}
const GRID_COLUMNS = 12;
function afterCommit(fn) {
  if (typeof queueMicrotask === "function") queueMicrotask(fn);
  else void Promise.resolve().then(fn);
}
function whenMounted(fresh, install) {
  afterCommit(() => {
    if (fresh.isConnected) install(fresh);
  });
}
function withLiveNode(fresh, slot, fn) {
  afterCommit(() => {
    const recorded = slot.get();
    const live = recorded && recorded.isConnected ? recorded : fresh.isConnected ? fresh : null;
    if (!live) return;
    slot.set(live);
    fn(live);
  });
}
const FLEX_ALIGN = {
  start: "flex-start",
  center: "center",
  end: "flex-end",
  stretch: "stretch"
};
const FLEX_JUSTIFY = {
  start: "flex-start",
  center: "center",
  end: "flex-end",
  between: "space-between",
  around: "space-around",
  evenly: "space-evenly"
};
function mapFlexAlign(token) {
  return FLEX_ALIGN[token] ?? "stretch";
}
function mapFlexJustify(token) {
  return FLEX_JUSTIFY[token] ?? "flex-start";
}
function applyDirectionWithReverse(value, reverse) {
  if (!reverse) return value;
  if (value === "row") return "row-reverse";
  if (value === "column") return "column-reverse";
  if (value.endsWith("-reverse")) return value;
  return `${value}-reverse`;
}
function emitResponsiveSpacingVars(styleParts, prop2, cssPrefix) {
  for (const bp of RESPONSIVE_BREAKPOINTS) {
    const v = prop2.values[bp];
    if (v) styleParts.push(`${cssPrefix}-${bp}:${spacingCssValue(v) || `var(--rui-spacing-${v}, ${v})`}`);
  }
}
function emitResponsiveFlexVars(styleParts, prop2, cssPrefix, mapper) {
  for (const bp of RESPONSIVE_BREAKPOINTS) {
    const v = prop2.values[bp];
    if (v) styleParts.push(`${cssPrefix}-${bp}:${mapper(String(v))}`);
  }
}
function applyResponsiveEnumProp(value, styleParts, attrs, options) {
  const parsed = readResponsiveProp(value);
  if (parsed.kind === "single") {
    const token = parsed.value ? String(parsed.value) : options.defaultToken;
    attrs[options.attrName] = token;
    return;
  }
  attrs[options.attrName] = "responsive";
  attrs[options.responsiveFlag] = "true";
  emitResponsiveFlexVars(styleParts, parsed, options.cssVarPrefix, options.mapper);
}
function isComponentNamed(value, name) {
  return Boolean(
    value && typeof value === "object" && value.__kind === "Component" && value.name === name
  );
}
function stackBaseDirection(props) {
  const direction = readResponsiveProp(props.direction);
  if (direction.kind === "single") {
    return direction.value ? String(direction.value) : "column";
  }
  return direction.values.base ? String(direction.values.base) : "column";
}
const StackItem = {
  name: "StackItem",
  description: "Per-child flex control inside a `Row`, `Column`, or `Stack`. Wraps one child so it can `grow` to fill leftover space, `shrink`, set a `basis`, override `alignSelf`, or change visual `order` — the building block for asymmetric rows like `Row([StackItem(searchInput, { grow: 1 }), saveButton])` (input expands, button hugs).",
  props: [
    { name: "child", aliases: ["children"], type: "Node", description: "Child node to wrap" },
    { name: "grow", type: "number", optional: true, description: "flex-grow (0 or 1 typical)" },
    { name: "shrink", type: "number", optional: true, description: "flex-shrink (0 or 1 typical)" },
    { name: "basis", type: "string", optional: true, description: "flex-basis (`auto`, `0`, or CSS length)" },
    { name: "alignSelf", type: "string", optional: true, enum: ["start", "center", "end", "stretch"], description: "Per-item cross-axis alignment" },
    { name: "order", type: "number", optional: true, description: "Visual order override" },
    { name: "minWidth", type: "string", optional: true, description: "CSS min-width" },
    { name: "maxWidth", type: "string", optional: true, description: "CSS max-width" }
  ],
  render: (_node, props, helpers) => {
    const attrs = { class: "rui-stack-item" };
    const styleParts = [];
    if (props.grow !== void 0 && props.grow !== null) {
      const grow = flexFactor(props.grow, 0);
      attrs["data-grow"] = String(grow);
      styleParts.push(`flex-grow:${grow}`);
    }
    if (props.shrink !== void 0 && props.shrink !== null) {
      const shrink = flexFactor(props.shrink, 1);
      attrs["data-shrink"] = String(shrink);
      styleParts.push(`flex-shrink:${shrink}`);
    }
    const basis = asString(props.basis);
    if (basis === "auto" || basis === "0") {
      attrs["data-basis"] = basis;
    } else if (basis) {
      styleParts.push(`flex-basis:${sanitiseCssLength(basis, "auto")}`);
    }
    const alignSelf = asString(props.alignSelf);
    if (alignSelf) attrs["data-align-self"] = alignSelf;
    if (props.order !== void 0 && props.order !== null) {
      const order = asNumber(props.order, 0);
      attrs["data-order"] = String(order);
      styleParts.push(`order:${order}`);
    }
    const minWidth = asString(props.minWidth);
    if (minWidth) styleParts.push(`min-width:${sanitiseCssLength(minWidth, "0")}`);
    const maxWidth = asString(props.maxWidth);
    if (maxWidth) styleParts.push(`max-width:${sanitiseCssLength(maxWidth, "none")}`);
    if (styleParts.length > 0) attrs.style = styleParts.join(";");
    const root = el("div", attrs);
    root.append(helpers.renderNode(props.child));
    return root;
  }
};
function flexFactor(value, fallback) {
  const n = asNumber(value, fallback);
  return Number.isFinite(n) && n >= 0 ? n : fallback;
}
function renderFlexContainer(directionValue, uniform, alignDefault, props, helpers) {
  const direction = readResponsiveProp(directionValue);
  const gap = readResponsiveProp(props.gap);
  const padding = readResponsiveProp(props.padding);
  const reverse = asBoolean(props.reverse);
  const attrs = {
    class: "rui-stack",
    "data-wrap": asBoolean(props.wrap) ? "true" : null,
    "data-reverse": reverse ? "true" : null,
    "data-uniform": uniform ? "true" : "false",
    "data-inline": asBoolean(props.inline) ? "true" : null
  };
  const styleParts = [];
  if (direction.kind === "single") {
    const dir = direction.value ? String(direction.value) : "column";
    attrs["data-direction"] = applyDirectionWithReverse(dir, reverse);
  } else {
    attrs["data-direction"] = "responsive";
    attrs["data-responsive-dir"] = "true";
    const rowAt = [];
    let effectiveDir = "column";
    for (const bp of RESPONSIVE_BREAKPOINTS) {
      const v = direction.values[bp];
      if (v) {
        effectiveDir = String(v);
        styleParts.push(`--rui-stack-dir-${bp}:${applyDirectionWithReverse(effectiveDir, reverse)}`);
      }
      if (effectiveDir === "row" || effectiveDir === "row-reverse") rowAt.push(bp);
    }
    if (rowAt.length > 0) attrs["data-row-at"] = rowAt.join(" ");
  }
  if (gap.kind === "single") {
    attrs["data-gap"] = gap.value ? normalizeSpacingToken(gap.value, String(gap.value)) : "md";
  } else {
    attrs["data-gap"] = "responsive";
    attrs["data-responsive-gap"] = "true";
    emitResponsiveSpacingVars(styleParts, gap, "--rui-stack-gap");
  }
  applyResponsiveEnumProp(props.align, styleParts, attrs, {
    attrName: "data-align",
    responsiveFlag: "data-responsive-align",
    cssVarPrefix: "--rui-stack-align",
    defaultToken: alignDefault,
    mapper: mapFlexAlign
  });
  applyResponsiveEnumProp(props.justify, styleParts, attrs, {
    attrName: "data-justify",
    responsiveFlag: "data-responsive-justify",
    cssVarPrefix: "--rui-stack-justify",
    defaultToken: "start",
    mapper: mapFlexJustify
  });
  const alignContent = asString(props.alignContent);
  if (alignContent) attrs["data-align-content"] = alignContent;
  if (padding.kind === "single") {
    const pad = padding.value ? normalizeSpacingToken(padding.value, String(padding.value)) : null;
    if (pad) attrs["data-padding"] = pad;
  } else {
    attrs["data-padding"] = "responsive";
    attrs["data-responsive-padding"] = "true";
    emitResponsiveSpacingVars(styleParts, padding, "--rui-stack-padding");
  }
  if (styleParts.length > 0) attrs.style = styleParts.join(";");
  const root = el("div", attrs);
  for (const child of asArray(props.children)) {
    root.append(helpers.renderNode(child));
  }
  return root;
}
const FLEX_GAP_PROP = { name: "gap", type: "string | object", optional: true, enum: SPACING_TOKENS, description: "Spacing between children. May be a responsive map." };
const FLEX_JUSTIFY_PROP = { name: "justify", type: "string | object", optional: true, enum: ["start", "center", "end", "between", "around", "evenly"], description: "Main-axis distribution. May be a responsive map." };
const FLEX_WRAP_PROP = { name: "wrap", type: "boolean", optional: true, description: "Wrap children onto multiple lines when they overflow" };
const FLEX_REVERSE_PROP = { name: "reverse", type: "boolean", optional: true, description: "Reverse the visual order of children" };
const FLEX_PADDING_PROP = { name: "padding", type: "string | object", optional: true, enum: SPACING_TOKENS, description: "Inner padding token. May be a responsive map." };
const FLEX_INLINE_PROP = { name: "inline", type: "boolean", optional: true, description: "Use inline-flex (shrink-to-fit) instead of a full-width block" };
const FLEX_ALIGN_CONTENT_PROP = { name: "alignContent", type: "string", optional: true, enum: ["start", "center", "end", "between", "around", "stretch"], description: "Alignment of wrapped lines (only when `wrap` is on)" };
const Row = {
  name: "Row",
  description: "Lay children out horizontally (left → right) with even spacing. The developer-friendly default: children keep their natural width and are vertically centered. Set `grow=true` to make children share the row equally (for equal-width columns prefer `Grid(columns: N)`); drop a `Spacer()` between children to push them apart; wrap a child in `StackItem` for per-child grow/shrink/alignment. Use `wrap=true` for chips/tags that should flow onto multiple lines.",
  props: [
    { name: "children", aliases: ["child"], type: "Node[]", description: "Children laid out left → right" },
    { name: "gap", type: "string | object", optional: true, enum: SPACING_TOKENS, description: "Horizontal spacing between children (default `md`). May be a responsive map." },
    { name: "align", type: "string | object", optional: true, enum: ["start", "center", "end", "stretch"], description: "Vertical alignment of children (default `center`). May be a responsive map." },
    { ...FLEX_JUSTIFY_PROP },
    { name: "grow", type: "boolean", optional: true, description: "Children share the row width equally (replaces the old `uniform`)" },
    { ...FLEX_WRAP_PROP },
    { ...FLEX_REVERSE_PROP },
    { ...FLEX_PADDING_PROP },
    { ...FLEX_INLINE_PROP },
    { ...FLEX_ALIGN_CONTENT_PROP }
  ],
  render: (_node, props, helpers) => renderFlexContainer("row", asBoolean(props.grow), "center", props, helpers)
};
const Column = {
  name: "Column",
  description: "Lay children out vertically (top → bottom) with even spacing — the most common page/section layout. Children stretch to the full width by default; set `align` to `start`/`center`/`end` to change that. This is the recommended root container for a page or a card body.",
  props: [
    { name: "children", aliases: ["child"], type: "Node[]", description: "Children laid out top → bottom" },
    { name: "gap", type: "string | object", optional: true, enum: SPACING_TOKENS, description: "Vertical spacing between children (default `md`). May be a responsive map." },
    { name: "align", type: "string | object", optional: true, enum: ["start", "center", "end", "stretch"], description: "Horizontal alignment of children (default `stretch`). May be a responsive map." },
    { ...FLEX_JUSTIFY_PROP },
    { ...FLEX_WRAP_PROP },
    { ...FLEX_REVERSE_PROP },
    { ...FLEX_PADDING_PROP },
    { ...FLEX_INLINE_PROP },
    { ...FLEX_ALIGN_CONTENT_PROP }
  ],
  render: (_node, props, helpers) => renderFlexContainer("column", false, "stretch", props, helpers)
};
const Stack = {
  name: "Stack",
  description: 'Low-level flex container with a configurable, optionally responsive `direction`. Reach for `Row` or `Column` first — they are clearer for fixed-direction layouts. Use `Stack` when the direction itself must change across breakpoints, e.g. `direction: {base: "column", md: "row"}` for a sidebar that stacks on mobile. `gap`, `align`, `justify`, and `padding` also accept responsive maps. NOTE: a `row` Stack grows its children equally by default (`uniform=true`); set `uniform=false` for natural widths (this is `Row`\'s default).',
  props: [
    { name: "children", aliases: ["child"], type: "Node[]", description: "Child components to stack" },
    { name: "direction", type: "string | object", optional: true, enum: ["column", "row"], description: 'Layout direction (default column). May be a responsive map like `{base: "column", md: "row"}`.' },
    { ...FLEX_GAP_PROP },
    { name: "align", type: "string | object", optional: true, enum: ["start", "center", "end", "stretch"], description: "Cross-axis alignment (default stretch). May be a responsive map." },
    { ...FLEX_JUSTIFY_PROP },
    { ...FLEX_ALIGN_CONTENT_PROP },
    { ...FLEX_WRAP_PROP },
    { ...FLEX_REVERSE_PROP },
    { name: "uniform", type: "boolean", optional: true, description: "Row children share space equally (default true for row stacks; use `Row` for natural widths)" },
    { ...FLEX_INLINE_PROP },
    { ...FLEX_PADDING_PROP }
  ],
  render: (_node, props, helpers) => {
    const baseDir = stackBaseDirection(props);
    const uniformDefault = baseDir === "row";
    const uniform = props.uniform === void 0 ? uniformDefault : asBoolean(props.uniform, uniformDefault);
    return renderFlexContainer(props.direction, uniform, "stretch", props, helpers);
  }
};
const Center = {
  name: "Center",
  description: 'Centers its children on both axes — the easy way to drop a spinner, an empty state, a hero call-to-action, or a modal body into the middle of a region. Give it `minHeight` (e.g. `"60vh"`) to center vertically inside a tall area, or `axis` to center on only one axis. Multiple children stack in a column and are centered as a group.',
  props: [
    { name: "children", aliases: ["child"], type: "Node[]", description: "Content to center" },
    { name: "axis", type: "string", optional: true, enum: ["both", "horizontal", "vertical"], description: "Which axis to center on (default both)" },
    { name: "minHeight", type: "string", optional: true, description: "CSS min-height — set to center vertically inside a tall region (e.g. `60vh`, `400px`)" },
    { name: "gap", type: "string", optional: true, enum: SPACING_TOKENS, description: "Spacing between stacked children (default `md`)" },
    { name: "padding", type: "string", optional: true, enum: SPACING_TOKENS, description: "Inner padding token" },
    { name: "inline", type: "boolean", optional: true, description: "Shrink to fit content instead of filling the available width" }
  ],
  render: (_node, props, helpers) => {
    const attrs = {
      class: "rui-center",
      "data-axis": asString(props.axis, "both"),
      "data-gap": normalizeSpacingToken(props.gap, "md"),
      "data-inline": asBoolean(props.inline) ? "true" : null
    };
    const padding = normalizeSpacingToken(props.padding, asString(props.padding));
    if (padding) attrs["data-padding"] = padding;
    const styleParts = [];
    const minHeight = asString(props.minHeight);
    if (minHeight) styleParts.push(`min-height:${sanitiseCssLength(minHeight, "auto")}`);
    if (styleParts.length > 0) attrs.style = styleParts.join(";");
    const root = el("div", attrs);
    for (const child of asArray(props.children)) root.append(helpers.renderNode(child));
    return root;
  }
};
const Card = {
  name: "Card",
  description: 'Vertical card container. Set `padding: "none"` for a full-bleed body (a Table, image, or list that should meet the card\'s edges). Pass `onClick` or `href` to make the whole card a single interactive target — it then renders as a `button` / `a` so it is keyboard-operable.',
  props: [
    { name: "children", aliases: ["child"], type: "Node[]", description: "Card contents" },
    { name: "variant", aliases: ["tone"], type: "string", optional: true, enum: ["default", "outlined", "elevated"] },
    { name: "padding", type: "string", optional: true, enum: SPACING_TOKENS, description: "Inner padding (default `lg`). Use `none` for a full-bleed body." },
    { name: "onClick", type: "callable", optional: true, aliases: ["onclick"], description: "Makes the whole card clickable (renders as a `button`)" },
    { name: "href", type: "string", optional: true, description: "Makes the whole card a link (renders as an `a`)" }
  ],
  render: (_node, props, helpers) => {
    const href = sanitiseHref(props.href, "");
    const clickable = Boolean(href) || props.onClick != null;
    const tag = href ? "a" : clickable ? "button" : "div";
    const padding = normalizeSpacingToken(props.padding, "");
    const root = el(tag, {
      class: "rui-card",
      "data-variant": asString(props.variant, "default"),
      "data-padding": padding || null,
      "data-clickable": clickable ? "true" : null,
      href: href || null,
      type: tag === "button" ? "button" : null
    });
    if (clickable) {
      root.onclick = () => {
        helpers.invoke(props.onClick);
      };
    }
    for (const child of asArray(props.children)) root.append(helpers.renderNode(child));
    return root;
  }
};
const CardHeader = {
  name: "CardHeader",
  description: 'Card header with title, an optional `eyebrow` line rendered ABOVE the title (category / kicker / pre-headline), and an optional `subtitle` rendered below it. `actions` puts nodes (a Badge, an overflow Menu, an "Edit" Button) on the trailing edge of the title row; `level` sets the heading tag so a page of cards keeps a sane document outline.',
  props: [
    { name: "title", type: "string" },
    { name: "subtitle", type: "string", optional: true },
    {
      name: "eyebrow",
      type: "string",
      optional: true,
      aliases: ["preheadline", "kicker"],
      description: "Short line shown above the title"
    },
    { name: "actions", type: "Node[]", optional: true, description: "Nodes pinned to the trailing edge of the title row (badge / menu / button)" },
    { name: "level", type: "number", optional: true, description: "Heading level for the title, 2–6 (default 3)" }
  ],
  render: (_node, props, helpers) => {
    const actions = asArray(props.actions);
    const root = el("header", {
      class: "rui-card-header",
      "data-has-actions": actions.length > 0 ? "true" : null
    });
    const textHost = actions.length > 0 ? el("div", { class: "rui-card-header-text" }) : root;
    const eyebrow = asString(props.eyebrow);
    if (eyebrow) textHost.append(el("p", { class: "rui-card-eyebrow" }, [eyebrow]));
    const level = Math.min(6, Math.max(2, Math.round(asNumber(props.level, 3))));
    textHost.append(el(`h${level}`, { class: "rui-card-title" }, [asString(props.title)]));
    const subtitle = asString(props.subtitle);
    if (subtitle) textHost.append(el("p", { class: "rui-card-subtitle" }, [subtitle]));
    if (textHost !== root) {
      root.append(textHost);
      const actionHost = el("div", { class: "rui-card-header-actions" });
      for (const item of actions) actionHost.append(helpers.renderNode(item));
      root.append(actionHost);
    }
    return root;
  }
};
const CARD_FOOTER_JUSTIFY = ["start", "center", "end", "between"];
const CardFooter = {
  name: "CardFooter",
  description: 'Card footer for actions. Defaults to trailing-aligned buttons; use `justify: "between"` for the "destructive action far left, confirm actions far right" shape, or `start` to left-align them.',
  props: [
    { name: "children", aliases: ["child"], type: "Node[]" },
    { name: "justify", type: "string", optional: true, enum: CARD_FOOTER_JUSTIFY, description: "Horizontal distribution of the actions (default `end`)" }
  ],
  render: (_node, props, helpers) => {
    const root = el("footer", {
      class: "rui-card-footer",
      "data-justify": asString(props.justify) || null
    });
    for (const child of asArray(props.children)) root.append(helpers.renderNode(child));
    return root;
  }
};
const CARD_SECTION_TONES = [
  "default",
  "activating",
  "success",
  "warning",
  "critical",
  "neutral",
  "corporate",
  "promoting"
];
const CardSection = {
  name: "CardSection",
  description: "Full-bleed, colour-coded band inside a `Card` that groups and semantically tints a chunk of the card's content (edge-to-edge tinted background with a rule above/below). Use to mark a region of a card as activating / success / warning / critical / neutral. For a standalone bordered notice use `Callout` instead.",
  props: [
    { name: "children", aliases: ["child"], type: "Node[]", positional: true },
    { name: "tone", type: "string", optional: true, enum: CARD_SECTION_TONES, aliases: ["variant", "status"] },
    { name: "align", type: "string", optional: true, enum: ["left", "center", "right"] }
  ],
  render: (_node, props, helpers) => {
    const root = el("section", {
      class: "rui-card-section",
      "data-tone": asString(props.tone, "default"),
      "data-align": asString(props.align) || null
    });
    for (const child of asArray(props.children)) root.append(helpers.renderNode(child));
    return root;
  }
};
const Separator = {
  name: "Separator",
  description: "Visual divider between content sections. Supports horizontal or vertical orientation, and an optional center `label` (lifted from the legacy `Divider`). Use `decorative=false` to expose the separator to assistive tech.",
  props: [
    { name: "orientation", type: "string", optional: true, enum: ["horizontal", "vertical"] },
    { name: "label", type: "string", optional: true, description: "Optional label rendered in the middle (horizontal only)" },
    { name: "decorative", type: "boolean", optional: true, description: "Hides the separator from assistive tech when true (default)" }
  ],
  render: (_node, props) => {
    const orientation = asString(props.orientation, "horizontal");
    const decorative = asBoolean(props.decorative, true);
    const label = asString(props.label);
    if (label && orientation === "horizontal") {
      return el("div", {
        class: "rui-separator-with-label",
        "data-orientation": orientation,
        role: decorative ? "presentation" : "separator",
        "aria-orientation": decorative ? null : orientation
      }, [
        el("span", { class: "rui-separator-line" }),
        el("span", { class: "rui-separator-label" }, [label]),
        el("span", { class: "rui-separator-line" })
      ]);
    }
    return el("div", {
      class: "rui-separator",
      "data-orientation": orientation,
      role: decorative ? "presentation" : "separator",
      "aria-orientation": decorative ? null : orientation
    });
  }
};
const STEP_STATUSES = ["pending", "active", "complete", "error"];
function resolveStepStatus(data) {
  const declared = asString(data.status).toLowerCase();
  if (STEP_STATUSES.includes(declared)) return declared;
  if (data.complete !== void 0 && data.complete !== null && asBoolean(data.complete)) return "complete";
  return asBoolean(data.active) ? "active" : "pending";
}
const renderStepLi = (title, details, status = "pending") => {
  const root = el("li", {
    class: "rui-steps-item",
    "data-active": status === "active" ? "true" : "false",
    "data-status": status,
    // `data-complete` mirrors what MultiStepForm's stepper already styles, so
    // a plain `Steps` gets the same check badge from one shared rule.
    "data-complete": status === "complete" ? "true" : null
  });
  root.append(el("div", { class: "rui-steps-title" }, [title]));
  if (details) root.append(el("div", { class: "rui-steps-details" }, [details]));
  return root;
};
const Steps = {
  name: "Steps",
  description: 'Numbered step-by-step guide. Pass items as `{title, details?, active?, status?}` objects. `active` marks the current step; `status` (`pending|active|complete|error`) additionally distinguishes finished and failed steps. `orientation: "horizontal"` lays the steps across the top of a wizard instead of down the page.',
  props: [
    { name: "items", type: "object[]" },
    { name: "orientation", type: "string", optional: true, enum: ["vertical", "horizontal"], description: "Layout direction (default `vertical`)" }
  ],
  render: (_node, props, helpers) => {
    const root = el("ol", {
      class: "rui-steps",
      "data-orientation": asString(props.orientation, "vertical")
    });
    for (const item of asArray(props.items)) {
      if (item && typeof item === "object" && item.__kind === "Component") {
        root.append(el("li", { class: "rui-steps-item", "data-bare": "true" }, [helpers.renderNode(item)]));
        continue;
      }
      if (item && typeof item === "object") {
        const data = item;
        root.append(renderStepLi(
          asString(data.title),
          asString(data.details),
          resolveStepStatus(data)
        ));
        continue;
      }
      root.append(renderStepLi(asString(item), ""));
    }
    return root;
  }
};
const TabItem = {
  name: "TabItem",
  description: "Single tab definition (used inside Tabs). Add `badge` for a count chip in the tab trigger, `icon` for a leading Font Awesome icon, and `disabled` for a tab that cannot be selected yet.",
  props: [
    { name: "value", type: "string", description: "Stable identifier for the tab" },
    { name: "label", type: "string", description: "Display label" },
    { name: "children", aliases: ["child"], type: "Node[]", description: "Tab content" },
    { name: "badge", type: "string", optional: true, description: "Trailing chip rendered in the tab trigger (count / status)" },
    { name: "icon", type: "string", optional: true, description: "Optional Font Awesome icon name shown before the label" },
    { name: "disabled", type: "boolean", optional: true, description: "Render the trigger as unavailable (not selectable, skipped by keyboard nav)" }
  ],
  render: (_node, props, helpers) => {
    const wrapper = el("div", {
      class: "rui-tab-content",
      role: "tabpanel",
      "data-value": asString(props.value),
      "data-label": asString(props.label) || null,
      "data-badge": asString(props.badge) || null,
      "data-icon": asString(props.icon) || null,
      "data-disabled": asBoolean(props.disabled) ? "true" : null,
      "data-active": "false"
    });
    for (const child of asArray(props.children)) wrapper.append(helpers.renderNode(child));
    return wrapper;
  }
};
let tabsIdSeq = 0;
function readTabEntry(item, idx, helpers) {
  const rendered = helpers.renderNode(item);
  let panel;
  if (rendered instanceof HTMLElement) {
    panel = rendered;
  } else {
    panel = el("div", { class: "rui-tab-content", role: "tabpanel" });
    panel.append(rendered);
  }
  const raw = item;
  const fromArgs = (slot) => asString(raw?.args?.[slot]);
  const data = panel.dataset;
  return {
    panel,
    value: data.value || fromArgs(0) || `tab-${idx}`,
    label: data.label || fromArgs(1) || `Tab ${idx + 1}`,
    badge: data.badge || fromArgs(3),
    icon: data.icon || fromArgs(4),
    disabled: data.disabled === "true" || asBoolean(raw?.args?.[5])
  };
}
const Tabs = {
  name: "Tabs",
  description: 'Tabbed container. Children must be TabItem components. Supports `orientation="vertical"` for sidebar-style tabs and built-in keyboard navigation (←/→ or ↑/↓, Home, End). Provide `onChange` to react when the user switches tabs (called with the new tab\'s value). Pass a `$variable` as `value` for a controlled strip — it is kept in sync both ways, so a button elsewhere on the page (or a route) can switch tabs and a user click updates the variable; `defaultValue` is the initial tab only. Set `fitted` for a strip that spans its container with equal-width triggers (the shape a two- or three-tab section header usually wants).',
  props: [
    { name: "items", type: "TabItem[]", description: "Tab definitions" },
    { name: "defaultValue", type: "string", optional: true, description: "Initially active tab value" },
    { name: "orientation", type: "string", optional: true, enum: ["horizontal", "vertical"], description: "Layout direction (default `horizontal`)" },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"], description: "Called with the newly-activated tab value when the user switches tabs" },
    { name: "value", type: "string", optional: true, description: "Active tab value — pass a `$variable` to control the strip from host state (written back when the user switches tabs)" },
    // Declared last on purpose: the controlled `value` channel reads its state
    // ref from argMeta slot 4, so nothing may be inserted ahead of it.
    { name: "fitted", type: "boolean", optional: true, description: 'Stretch the strip across its container and split it evenly between the triggers (ignored for `orientation="vertical"`)' }
  ],
  render: (node, props, helpers) => {
    const items = asArray(props.items);
    const orientation = asString(props.orientation, "horizontal");
    const fitted = asBoolean(props.fitted);
    const root = el("div", {
      class: "rui-tabs",
      "data-orientation": orientation,
      "data-fitted": fitted ? "true" : null
    });
    const tablist = el("div", {
      class: "rui-tab-list",
      role: "tablist",
      "aria-orientation": orientation
    });
    const panels = el("div", { class: "rui-tab-panels" });
    const entries = items.map((item, idx) => readTabEntry(item, idx, helpers));
    const idSlot = helpers.useInstanceState("rui-tabs-id", "");
    if (!idSlot.get()) idSlot.set(`rui-tabs-${tabsIdSeq += 1}`);
    const idPrefix = idSlot.get();
    const declaredDefault = asString(props.defaultValue);
    const firstSelectable = entries.find((e) => !e.disabled) ?? entries[0];
    const fallbackValue = declaredDefault || (firstSelectable ? firstSelectable.value : "");
    const activeSlot = helpers.useInstanceState("activeTab", fallbackValue);
    const seenDefault = helpers.useInstanceState("seenDefaultValue", null);
    if (declaredDefault && seenDefault.get() !== declaredDefault) {
      seenDefault.set(declaredDefault);
      activeSlot.set(declaredDefault);
    }
    const validValues = new Set(entries.map((e) => e.value));
    if (!validValues.has(activeSlot.get())) {
      activeSlot.set(fallbackValue);
    }
    const controlled = asString(props.value);
    if (controlled && validValues.has(controlled)) activeSlot.set(controlled);
    const valueStateRef = node.argMeta?.[4]?.stateRef;
    const setActive = (next, originBtn) => {
      const previous = activeSlot.get();
      activeSlot.set(next);
      if (valueStateRef) helpers.setState(valueStateRef, next);
      const liveRoot = originBtn.closest(".rui-tabs");
      if (liveRoot) {
        liveRoot.querySelectorAll(".rui-tab-trigger").forEach((b) => {
          const isActive = b.getAttribute("data-value") === next;
          b.setAttribute("aria-selected", isActive ? "true" : "false");
          b.tabIndex = isActive && !b.disabled ? 0 : -1;
        });
        liveRoot.querySelectorAll(".rui-tab-content").forEach((p) => {
          p.setAttribute("data-active", p.getAttribute("data-value") === next ? "true" : "false");
        });
      }
      if (previous !== next) helpers.invoke(props.onChange, next);
    };
    entries.forEach((entry, idx) => {
      const { value, label, badge, icon, disabled, panel } = entry;
      const isActive = value === activeSlot.get();
      const tabId = `${idPrefix}-tab-${idx}`;
      const panelId2 = `${idPrefix}-panel-${idx}`;
      const button = el(
        "button",
        {
          class: "rui-tab-trigger",
          role: "tab",
          type: "button",
          id: tabId,
          "aria-controls": panelId2,
          "data-value": value,
          "aria-selected": isActive ? "true" : "false",
          "aria-disabled": disabled ? "true" : null,
          disabled: disabled ? "" : null,
          tabindex: isActive && !disabled ? "0" : "-1"
        }
      );
      const iconNode = icon ? renderIconForTab(icon) : null;
      if (iconNode) button.append(iconNode);
      button.append(el("span", { class: "rui-tab-trigger-label" }, [label]));
      if (badge) button.append(el("span", { class: "rui-tab-trigger-badge" }, [badge]));
      button.onclick = (event) => {
        const origin = event.currentTarget ?? event.target;
        setActive(value, origin);
      };
      button.onkeydown = (event) => {
        const e = event;
        const horizontal = orientation !== "vertical";
        const isNext = horizontal ? e.key === "ArrowRight" : e.key === "ArrowDown";
        const isPrev = horizontal ? e.key === "ArrowLeft" : e.key === "ArrowUp";
        if (!isNext && !isPrev && e.key !== "Home" && e.key !== "End") return;
        e.preventDefault();
        const origin = e.currentTarget ?? e.target;
        const liveList = origin.closest(".rui-tab-list");
        if (!liveList) return;
        const triggers = Array.from(liveList.querySelectorAll(".rui-tab-trigger")).filter((t) => !t.disabled);
        if (triggers.length === 0) return;
        const currentIdx = triggers.indexOf(origin);
        let nextIdx = currentIdx;
        if (e.key === "Home") nextIdx = 0;
        else if (e.key === "End") nextIdx = triggers.length - 1;
        else if (isNext) nextIdx = (currentIdx + 1) % triggers.length;
        else if (isPrev) nextIdx = (currentIdx - 1 + triggers.length) % triggers.length;
        const target = triggers[nextIdx];
        if (!target) return;
        target.focus();
        const nextValue = target.getAttribute("data-value") ?? "";
        if (nextValue) setActive(nextValue, target);
      };
      tablist.append(button);
      panel.setAttribute("data-value", value);
      panel.setAttribute("data-active", isActive ? "true" : "false");
      panel.setAttribute("id", panelId2);
      panel.setAttribute("aria-labelledby", tabId);
      if (!panel.hasAttribute("tabindex")) panel.setAttribute("tabindex", "0");
      panels.append(panel);
    });
    root.append(tablist, panels);
    return root;
  }
};
function renderIconForTab(iconName) {
  return renderIcon(iconName, { className: "rui-tab-trigger-icon" });
}
const ACCORDION_VARIANTS = ["success", "warning", "danger", "neutral", "info"];
const AccordionItem = {
  name: "AccordionItem",
  description: 'Single accordion section. Pass a `$variable` as `open` to control it from host state (a "Collapse all" button then works); `onToggle` fires with the new open state, which is where lazy-loading a section\'s content belongs. `variant` paints a semantic left-edge stripe for check-list style accordions, and `disabled` marks a section that cannot be opened yet.',
  props: [
    { name: "title", type: "string" },
    { name: "children", aliases: ["child"], type: "Node[]" },
    { name: "open", type: "boolean", optional: true },
    { name: "subtitle", type: "string", optional: true, aliases: ["summary"], description: 'Second line inside the trigger, under the title — a preview of what the collapsed section holds ("Labels | Taints | Maintenance window")' },
    { name: "showArrow", type: "boolean", optional: true, description: "Show a chevron icon on the right (default false). Inherits from parent Accordion when unset." },
    { name: "variant", type: "string", optional: true, enum: ACCORDION_VARIANTS, aliases: ["tone"], description: "Semantic left-edge stripe (success / warning / danger / neutral / info)" },
    { name: "disabled", type: "boolean", optional: true, description: "Section cannot be expanded (e.g. a step that is not available yet)" },
    { name: "onToggle", type: "callable", optional: true, aliases: ["onOpenChange", "ontoggle"], description: "Called with the new open state whenever the section expands or collapses" }
  ],
  render: (node, props, helpers) => {
    const explicit = props.showArrow !== void 0 && props.showArrow !== null;
    const controlsOpen = props.open !== void 0 && props.open !== null;
    const isOpen = asBoolean(props.open);
    const disabled = asBoolean(props.disabled);
    const liveSlot = helpers.useInstanceState("rui-accordion-node", null);
    const assertedSlot = helpers.useInstanceState("rui-accordion-open", null);
    const recorded = liveSlot.get();
    const stateOpen = controlsOpen ? isOpen : Boolean(recorded?.isConnected && recorded.open);
    const details = el("details", {
      class: "rui-accordion-item",
      "data-show-arrow": explicit ? asBoolean(props.showArrow) ? "true" : "false" : null,
      "data-variant": asString(props.variant) || null,
      "data-disabled": disabled ? "true" : null,
      // The morph reconciler deliberately never strips `open` from a
      // `<details>` (it is user-toggleable state), so an `open: false` render
      // could never collapse an expanded item. `data-rui-open` is an attribute
      // morph DOES sync; the observer below mirrors it onto the live element.
      "data-rui-open": controlsOpen ? String(isOpen) : null,
      "data-state": stateOpen ? "open" : "closed"
    });
    if (isOpen) details.setAttribute("open", "");
    const summary = el("summary", { class: "rui-accordion-trigger" });
    const subtitle = asString(props.subtitle);
    if (subtitle) {
      summary.append(el("span", { class: "rui-accordion-heading" }, [
        el("span", { class: "rui-accordion-title" }, [asString(props.title)]),
        el("span", { class: "rui-accordion-subtitle" }, [subtitle])
      ]));
    } else {
      summary.append(el("span", { class: "rui-accordion-title" }, [asString(props.title)]));
    }
    summary.append(el("span", { class: "rui-accordion-chevron", "aria-hidden": "true" }));
    if (disabled) {
      summary.setAttribute("aria-disabled", "true");
      summary.onclick = (event) => {
        event.preventDefault();
      };
    }
    details.append(summary);
    const body = el("div", { class: "rui-accordion-body" });
    for (const child of asArray(props.children)) body.append(helpers.renderNode(child));
    details.append(body);
    const stateName = node.argMeta?.[2]?.stateRef;
    details.ontoggle = (event) => {
      const live = event.currentTarget ?? event.target;
      const nowOpen = live.open;
      live.setAttribute("data-state", nowOpen ? "open" : "closed");
      const asserted = live.getAttribute("data-rui-open");
      if (asserted !== null && asserted === String(nowOpen)) return;
      if (stateName) helpers.setState(stateName, nowOpen);
      helpers.invoke(props.onToggle, nowOpen);
    };
    if (!recorded?.isConnected) whenMounted(details, (live) => liveSlot.set(live));
    if (controlsOpen && assertedSlot.get() !== isOpen) {
      assertedSlot.set(isOpen);
      withLiveNode(details, liveSlot, (live) => {
        if (live.open !== isOpen) live.open = isOpen;
      });
    }
    return details;
  }
};
let accordionIdSeq = 0;
function accordionItemsIn(rendered) {
  if (rendered instanceof HTMLElement && rendered.classList.contains("rui-accordion-item")) {
    return [rendered];
  }
  const out = [];
  for (const child of Array.from(rendered.childNodes)) {
    if (child instanceof HTMLElement && child.classList.contains("rui-accordion-item")) out.push(child);
  }
  return out;
}
const Accordion = {
  name: "Accordion",
  description: 'Accordion container. Children must be AccordionItem components. Set `showArrow: true` to add a chevron indicator to every item; individual `AccordionItem`s can override via their own `showArrow` prop. `type: "single"` keeps only one section open at a time (the browser closes the previous one); `onChange` is called with the section\'s title and its new open state whenever any section toggles.',
  props: [
    { name: "items", type: "AccordionItem[]" },
    { name: "showArrow", type: "boolean", optional: true, description: "Show chevron icon on every item (default false)." },
    { name: "type", type: "string", optional: true, enum: ["single", "multiple"], description: "`single` allows one open section at a time; `multiple` (default) allows any number." },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"], description: "Called with (title, open) when any section toggles" }
  ],
  render: (_node, props, helpers) => {
    const single = asString(props.type, "multiple") === "single";
    const groupSlot = helpers.useInstanceState("rui-accordion-group", "");
    if (single && !groupSlot.get()) groupSlot.set(`rui-accordion-${accordionIdSeq += 1}`);
    const groupName = single ? groupSlot.get() : "";
    const root = el("div", {
      class: "rui-accordion",
      "data-show-arrow": asBoolean(props.showArrow) ? "true" : "false",
      "data-type": single ? "single" : "multiple"
    });
    const notify = props.onChange != null;
    for (const child of asArray(props.items)) {
      const rendered = helpers.renderNode(child);
      const itemEls = accordionItemsIn(rendered);
      for (const item of itemEls) {
        if (groupName) item.setAttribute("name", groupName);
        if (!notify) continue;
        const prior = item.ontoggle;
        item.ontoggle = (event) => {
          const live = event.currentTarget ?? event.target;
          if (prior) prior.call(live, event);
          const title = live.querySelector(".rui-accordion-title")?.textContent ?? "";
          helpers.invoke(props.onChange, title, live.open);
        };
      }
      root.append(rendered);
    }
    return root;
  }
};
const clampGridColumns = (n) => Math.max(1, Math.min(GRID_COLUMNS, Math.round(n)));
function resolveSpan(span) {
  if (span == null || span === "") return 12;
  const raw = String(span).trim();
  if (raw.includes("/")) {
    const [numPart, denPart] = raw.split("/");
    const num2 = Number(numPart);
    const den = Number(denPart);
    if (Number.isFinite(num2) && Number.isFinite(den) && den > 0) {
      return clampGridColumns(Math.round(GRID_COLUMNS * num2 / den));
    }
  }
  if (raw === "auto" || raw === "fit" || raw === "auto-fit" || raw === "full" || raw === "100%") return 12;
  return clampGridColumns(asNumber(span, 12));
}
function gridMinChildWidth(props) {
  return sanitiseCssLength(asString(props.minChildWidth) || "220px", "220px");
}
const GridItem = {
  name: "GridItem",
  description: 'Wraps a child in a 12-column grid cell with `span`, `offset`, `rowSpan`, and responsive `spanAt` maps. Parent `Grid` auto-enables 12-column mode when any child is a `GridItem`. Fraction spans like `"1/3"` resolve against the 12-column track. With no `span` the item takes one cell, so `GridItem` wrappers are safe inside a `Grid(columns: N)`.',
  props: [
    { name: "child", aliases: ["children"], type: "Node", description: "Child node to place in the grid" },
    { name: "span", type: "number | string", optional: true, description: 'Columns to span (1–12) or fraction like "1/2", "1/3" (default: one cell)' },
    { name: "offset", type: "number", optional: true, description: "Empty columns before this item (0–11)" },
    { name: "spanAt", type: "object", optional: true, description: "Responsive span map `{sm: 12, md: 6, lg: 4}`" },
    { name: "rowSpan", type: "number", optional: true, description: "Rows to span — the tall dashboard cell next to stacked KPI cards" }
  ],
  render: (_node, props, helpers) => {
    const hasSpan = props.span !== void 0 && props.span !== null && props.span !== "";
    const baseSpan = hasSpan ? resolveSpan(props.span) : null;
    const spanAt = readResponsiveProp(props.spanAt);
    const offset = props.offset === void 0 || props.offset === null ? 0 : Math.max(0, Math.min(GRID_COLUMNS - 1, Math.round(asNumber(props.offset, 0))));
    const attrs = {
      class: "rui-grid-item"
    };
    const styleParts = [];
    if (baseSpan !== null) {
      attrs["data-span"] = String(baseSpan);
      styleParts.push(`--rui-grid-item-span:${baseSpan}`);
    }
    if (offset > 0) {
      attrs["data-offset"] = String(offset);
      styleParts.push(`--rui-grid-item-offset:${offset}`);
    }
    const rowSpan = props.rowSpan === void 0 || props.rowSpan === null ? 0 : Math.max(1, Math.round(asNumber(props.rowSpan, 1)));
    if (rowSpan > 1) {
      attrs["data-row-span"] = String(rowSpan);
      styleParts.push(`--rui-grid-item-row-span:${rowSpan}`, `grid-row:span ${rowSpan}`);
    }
    if (spanAt.kind === "responsive") {
      attrs["data-responsive-span"] = "true";
      for (const bp of RESPONSIVE_BREAKPOINTS) {
        const v = spanAt.values[bp];
        if (v !== void 0) {
          const resolved = resolveSpan(v);
          styleParts.push(`--rui-grid-item-span-${bp}:${resolved}`);
        }
      }
    } else if (spanAt.kind === "single" && spanAt.value != null) {
      const resolved = resolveSpan(spanAt.value);
      styleParts.push(`--rui-grid-item-span:${resolved}`);
      attrs["data-span"] = String(resolved);
    }
    if (styleParts.length > 0) attrs.style = styleParts.join(";");
    const root = el("div", attrs);
    root.append(helpers.renderNode(props.child));
    return root;
  }
};
const BOX_RADIUS = {
  none: "0",
  sm: "var(--rui-radius-sm)",
  md: "var(--rui-radius-md)",
  lg: "var(--rui-radius-lg)",
  pill: "var(--rui-radius-pill)"
};
const Box = {
  name: "Box",
  description: "Spacing and surface wrapper for padding, margin, borders, semantic backgrounds, and max-width constraints. Use when a `Card` is too heavy but the content needs a subtle surface or inset. `radius` rounds the surface independently of `border`.",
  props: [
    { name: "children", aliases: ["child"], type: "Node[]" },
    { name: "padding", type: "string | object", optional: true, enum: SPACING_TOKENS, description: "Inner padding. May be a responsive map." },
    { name: "margin", type: "string | object", optional: true, enum: SPACING_TOKENS, description: "Outer margin. May be a responsive map." },
    { name: "border", type: "string", optional: true, enum: ["none", "subtle", "default"], description: "Border preset (default none)" },
    { name: "background", type: "string", optional: true, enum: ["none", "surface", "muted", "primary", "success", "warning", "danger", "info"], description: "Semantic background token" },
    { name: "maxWidth", type: "string", optional: true, description: "CSS max-width" },
    { name: "radius", type: "string", optional: true, enum: ["none", "sm", "md", "lg", "pill"], description: "Corner rounding — independent of `border`" }
  ],
  render: (_node, props, helpers) => {
    const padding = readResponsiveProp(props.padding);
    const margin = readResponsiveProp(props.margin);
    const border = asString(props.border, "none");
    const background = asString(props.background, "none");
    const radius = asString(props.radius) || (background !== "none" && border === "none" ? "md" : "");
    const attrs = {
      class: "rui-box",
      "data-border": border,
      "data-background": background,
      "data-radius": radius || null
    };
    const styleParts = [];
    if (radius && BOX_RADIUS[radius]) styleParts.push(`border-radius:${BOX_RADIUS[radius]}`);
    const maxWidth = asString(props.maxWidth);
    if (maxWidth) styleParts.push(`max-width:${sanitiseCssLength(maxWidth, "none")}`);
    if (padding.kind === "single") {
      const pad = padding.value ? normalizeSpacingToken(padding.value, String(padding.value)) : null;
      if (pad) attrs["data-padding"] = pad;
    } else {
      attrs["data-padding"] = "responsive";
      attrs["data-responsive-padding"] = "true";
      emitResponsiveSpacingVars(styleParts, padding, "--rui-box-padding");
    }
    if (margin.kind === "single") {
      const mar = margin.value ? normalizeSpacingToken(margin.value, String(margin.value)) : null;
      if (mar) attrs["data-margin"] = mar;
    } else {
      attrs["data-margin"] = "responsive";
      attrs["data-responsive-margin"] = "true";
      emitResponsiveSpacingVars(styleParts, margin, "--rui-box-margin");
    }
    if (styleParts.length > 0) attrs.style = styleParts.join(";");
    const root = el("div", attrs);
    for (const child of asArray(props.children)) root.append(helpers.renderNode(child));
    return root;
  }
};
const Fragment = {
  name: "Fragment",
  description: "Groups several siblings into one value WITHOUT adding a layout box — the children become direct children of the parent, so they participate in its flex/grid layout exactly as if they had been written inline. Use to return multiple nodes from a component, or to conditionally group siblings, without a stray `div` that would break a Grid/Stack's spacing. Because there is no element, universal style props (`sx`, `animate`, `id`) have nothing to attach to — wrap the group in a `Box` if you need those.",
  props: [
    { name: "children", aliases: ["child"], type: "Node[]", description: "Sibling nodes to group" }
  ],
  render: (_node, props, helpers) => {
    const frag = document.createDocumentFragment();
    for (const child of asArray(props.children)) frag.append(helpers.renderNode(child));
    return frag;
  }
};
const Grid = {
  name: "Grid",
  description: 'Two-dimensional grid with three modes:\n  1. AUTO-FIT (default, no `columns`): wraps as many equal columns as fit, each at least `minChildWidth` wide (default 220px) — perfect for card/KPI grids that should reflow on their own.\n  2. FIXED (`columns: N`, 1–12): exactly N equal columns.\n  3. SPAN (`columns: 12` or any `GridItem` children): a 12-track grid where each `GridItem` sets its own `span` (a number 1–12 or a fraction like `"1/3"`) for dashboards and asymmetric layouts.\n`columns` and `gap` accept responsive maps like `{base: 1, md: 2, lg: 4}`.',
  props: [
    { name: "children", aliases: ["child"], type: "Node[]" },
    { name: "columns", type: "number | object", optional: true, description: "Fixed column count 1–12. Omit for auto-fit; `12` (or `GridItem` children) enables the 12-track span system. May be a responsive map like `{base: 1, md: 3}`." },
    { name: "gap", type: "string | object", optional: true, enum: SPACING_TOKENS, description: "Gap on both axes (default `md`). May be a responsive map." },
    { name: "rowGap", type: "string | object", optional: true, enum: SPACING_TOKENS, description: "Row gap override. May be a responsive map." },
    { name: "columnGap", type: "string | object", optional: true, enum: SPACING_TOKENS, description: "Column gap override. May be a responsive map." },
    { name: "minChildWidth", type: "string", optional: true, aliases: ["minItemWidth"], description: 'Minimum column width for AUTO-FIT mode, e.g. `"240px"` (default 220px). Also caps the floor in FIXED mode.' },
    { name: "alignItems", type: "string", optional: true, enum: ["start", "center", "end", "stretch"], description: "Vertical alignment of items within their cells" },
    { name: "justifyItems", type: "string", optional: true, enum: ["start", "center", "end", "stretch"], description: "Horizontal alignment of items within their cells" },
    { name: "dense", type: "boolean", optional: true, description: "Dense auto-flow packing — let later items backfill earlier gaps" }
  ],
  render: (_node, props, helpers) => {
    const children = asArray(props.children);
    const hasGridItems = children.some((child) => isComponentNamed(child, "GridItem"));
    const columns = readResponsiveProp(props.columns);
    const gap = readResponsiveProp(props.gap);
    const rowGap = readResponsiveProp(props.rowGap);
    const columnGap = readResponsiveProp(props.columnGap);
    const attrs = {
      class: "rui-grid"
    };
    const styleParts = [];
    let twelveColMode = false;
    let explicitNonTwelve = false;
    const responsiveCols = columns.kind === "responsive";
    if (columns.kind === "single") {
      const requested = columns.value === null ? 0 : asNumber(columns.value, 0);
      const cols = requested > 0 ? clampGridColumns(requested) : 0;
      if (cols === GRID_COLUMNS) {
        twelveColMode = true;
      } else if (cols > 0) {
        explicitNonTwelve = true;
      }
      if (cols > 0) {
        attrs["data-columns"] = String(cols);
        const minChild = asString(props.minChildWidth);
        if (minChild) {
          attrs["data-min-child-width"] = "true";
          styleParts.push(`--rui-grid-min-child:${sanitiseCssLength(minChild, "220px")}`);
        }
      } else {
        styleParts.push(`--rui-grid-min-item:${gridMinChildWidth(props)}`);
      }
    } else {
      attrs["data-responsive-cols"] = "true";
      for (const bp of RESPONSIVE_BREAKPOINTS) {
        const v = columns.values[bp];
        if (v === void 0) continue;
        styleParts.push(`--rui-grid-cols-${bp}:${clampGridColumns(asNumber(v, 0))}`);
      }
      const minChild = asString(props.minChildWidth);
      if (minChild) {
        attrs["data-min-child-width"] = "true";
        styleParts.push(`--rui-grid-min-child:${sanitiseCssLength(minChild, "220px")}`);
      }
    }
    if (hasGridItems && !explicitNonTwelve && !responsiveCols) twelveColMode = true;
    if (twelveColMode) attrs["data-grid-mode"] = "12";
    if (gap.kind === "single") {
      attrs["data-gap"] = gap.value ? normalizeSpacingToken(gap.value, String(gap.value)) : "md";
    } else {
      attrs["data-gap"] = "responsive";
      attrs["data-responsive-gap"] = "true";
      emitResponsiveSpacingVars(styleParts, gap, "--rui-grid-gap");
    }
    if (rowGap.kind === "single" && rowGap.value) {
      attrs["data-row-gap"] = normalizeSpacingToken(rowGap.value, String(rowGap.value));
    } else if (rowGap.kind === "responsive") {
      attrs["data-row-gap"] = "responsive";
      attrs["data-responsive-row-gap"] = "true";
      emitResponsiveSpacingVars(styleParts, rowGap, "--rui-grid-row-gap");
    }
    if (columnGap.kind === "single" && columnGap.value) {
      attrs["data-column-gap"] = normalizeSpacingToken(columnGap.value, String(columnGap.value));
    } else if (columnGap.kind === "responsive") {
      attrs["data-column-gap"] = "responsive";
      attrs["data-responsive-column-gap"] = "true";
      emitResponsiveSpacingVars(styleParts, columnGap, "--rui-grid-column-gap");
    }
    const alignItems = asString(props.alignItems);
    if (alignItems) attrs["data-align-items"] = alignItems;
    const justifyItems = asString(props.justifyItems);
    if (justifyItems) attrs["data-justify-items"] = justifyItems;
    if (asBoolean(props.dense)) attrs["data-dense"] = "true";
    if (styleParts.length > 0) attrs.style = styleParts.join(";");
    const root = el("div", attrs);
    for (const child of children) root.append(helpers.renderNode(child));
    return root;
  }
};
const AspectRatio = {
  name: "AspectRatio",
  description: 'Container that constrains its child to a fixed aspect ratio (e.g. 16:9 for video embeds, 1:1 for thumbnails). The FIRST child fills the box; any further children are overlays positioned on top of it (a "LIVE" badge over a thumbnail), not stacked below.',
  props: [
    { name: "ratio", type: "string", description: "`width:height` (e.g. `16:9`, `4:3`) or a decimal like `1.78`" },
    { name: "children", aliases: ["child"], type: "Node[]" }
  ],
  render: (_node, props, helpers) => {
    const ratio = parseRatio$1(asString(props.ratio, "16:9"));
    const children = asArray(props.children);
    const root = el("div", {
      class: "rui-aspect-ratio",
      // Marks the overlay case for CSS: every direct child is sized to
      // 100%×100%, so a second child would otherwise be pushed out of the
      // clipped box and never seen.
      "data-overlay": children.length > 1 ? "true" : null,
      style: `aspect-ratio:${ratio};`
    });
    for (const child of children) root.append(helpers.renderNode(child));
    return root;
  }
};
function parseRatio$1(input) {
  if (input.includes(":")) {
    const [w, h] = input.split(":");
    const num2 = Number(w);
    const den = Number(h);
    if (Number.isFinite(num2) && num2 > 0 && Number.isFinite(den) && den > 0) return `${num2} / ${den}`;
  }
  const n = Number(input);
  return Number.isFinite(n) && n > 0 ? `${n} / 1` : "16 / 9";
}
const SCROLL_PINNED = /* @__PURE__ */ new WeakMap();
const STICK_THRESHOLD_PX = 24;
const isNearBottom = (node) => node.scrollHeight - node.clientHeight - node.scrollTop <= STICK_THRESHOLD_PX;
const ScrollArea = {
  name: "ScrollArea",
  description: "Bounded scroll container. Use to clip long lists / logs / chat panels to a fixed max height with a clean scrollbar. `height` gives the pane a stable box (it does not grow with its content); `maxHeight` lets it grow up to a cap. `stickToBottom` keeps the newest line in view as content is appended, until the user scrolls up.",
  props: [
    { name: "children", aliases: ["child"], type: "Node[]" },
    { name: "maxHeight", type: "string", optional: true, description: "CSS max-height — the pane grows with its content up to this cap (default 320px)" },
    { name: "direction", type: "string", optional: true, enum: ["vertical", "horizontal", "both"] },
    { name: "height", type: "string", optional: true, description: "CSS height — a FIXED box that neither grows nor shrinks with its content" },
    { name: "stickToBottom", type: "boolean", optional: true, description: "Auto-scroll to the newest content (logs / chat), until the user scrolls up" }
  ],
  render: (_node, props, helpers) => {
    const height = asString(props.height);
    const styleParts = [`max-height:${sanitiseCssLength(props.maxHeight, "320px")}`];
    if (height) styleParts.push(`height:${sanitiseCssLength(height, "auto")}`);
    const stick = asBoolean(props.stickToBottom);
    const root = el("div", {
      class: "rui-scroll-area",
      "data-direction": asString(props.direction, "vertical"),
      "data-stick-to-bottom": stick ? "true" : null,
      style: `${styleParts.join(";")};`
    });
    for (const child of asArray(props.children)) root.append(helpers.renderNode(child));
    if (stick) {
      root.onscroll = (event) => {
        const live = event.currentTarget ?? event.target;
        SCROLL_PINNED.set(live, isNearBottom(live));
      };
      if (typeof MutationObserver !== "undefined") {
        whenMounted(root, (live) => {
          const follow = () => {
            if (live.getAttribute("data-stick-to-bottom") !== "true") return;
            if (SCROLL_PINNED.get(live) === false) return;
            live.scrollTop = live.scrollHeight;
          };
          follow();
          const observer = new MutationObserver(follow);
          observer.observe(live, { childList: true, subtree: true, characterData: true });
          helpers.registerDisposer(() => observer.disconnect(), "rui-scroll-stick");
        });
      }
    }
    return root;
  }
};
const MODAL_SIZES = ["sm", "md", "lg", "xl", "full"];
const FOCUSABLE_SELECTOR$2 = 'a[href], button:not([disabled]), input:not([disabled]), select:not([disabled]), textarea:not([disabled]), [tabindex]:not([tabindex="-1"])';
let modalIdSeq = 0;
function focusablesIn(root) {
  const all = [...root.querySelectorAll(FOCUSABLE_SELECTOR$2)];
  const rendered = all.filter((n) => n.offsetParent !== null || n.getClientRects().length > 0);
  return rendered.length > 0 ? rendered : all;
}
function activeWithin(el2) {
  const root = el2.getRootNode();
  return root.activeElement ?? null;
}
const SCROLL_LOCKED = /* @__PURE__ */ new WeakSet();
let scrollLockCount = 0;
let savedBodyOverflow = "";
function lockBodyScroll(owner) {
  if (typeof document === "undefined" || !document.body) return;
  if (SCROLL_LOCKED.has(owner)) return;
  SCROLL_LOCKED.add(owner);
  scrollLockCount += 1;
  if (scrollLockCount === 1) {
    savedBodyOverflow = document.body.style.overflow;
    document.body.style.overflow = "hidden";
  }
}
function unlockBodyScroll(owner) {
  if (typeof document === "undefined" || !document.body) return;
  if (!SCROLL_LOCKED.delete(owner)) return;
  scrollLockCount = Math.max(0, scrollLockCount - 1);
  if (scrollLockCount === 0) {
    document.body.style.overflow = savedBodyOverflow;
    savedBodyOverflow = "";
  }
}
const MODAL_TEARDOWN = /* @__PURE__ */ new WeakSet();
function installModalTeardown(live, helpers) {
  if (MODAL_TEARDOWN.has(live)) return;
  MODAL_TEARDOWN.add(live);
  helpers.registerDisposer(() => {
    releaseOverlay(live);
    unlockBodyScroll(live);
    MODAL_TEARDOWN.delete(live);
  }, "rui-modal-teardown");
}
const Modal = {
  name: "Modal",
  description: "Dialog overlay shown when `open` is true. Pass a `$variable` as `open` to control it — the × button, Escape and the backdrop close the dialog by writing that variable, so a literal `true` or an expression needs `onRequestClose` instead or the dialog cannot be dismissed. The header always renders a × close button (disable via `closable: false`); the optional `footer` slot is the canonical place for action buttons. `closeOnBackdrop=true` opts in to backdrop-click dismissal. `onClose` fires once every time the modal actually closes, however it closed (× button, Escape, backdrop, or a state write from a Cancel/Save button). `lazy: true` skips rendering the body while closed. Accessible by default: the dialog is labelled by its title, renders in the browser's top layer so no transformed ancestor can clip it, moves focus into itself on open and restores it on close, traps Tab, locks background scrolling, and closes on Escape (unless `closable: false`).",
  props: [
    { name: "title", type: "string" },
    { name: "open", type: "boolean", description: "Open/closed state — usually a $variable" },
    { name: "children", aliases: ["child"], type: "Node[]" },
    { name: "size", type: "string", optional: true, enum: MODAL_SIZES, description: "Width preset (default `md`)" },
    { name: "footer", type: "Node[]", optional: true, description: "Footer slot — typically a row of action Buttons" },
    { name: "closable", type: "boolean", optional: true, description: "Render the header × button (default true)" },
    { name: "closeOnBackdrop", type: "boolean", optional: true, description: "Close when the overlay is clicked (default false)" },
    { name: "onClose", type: "callable", optional: true, aliases: ["onclose"], description: "Callable invoked once whenever the modal closes, by any route" },
    { name: "onRequestClose", type: "callable", optional: true, description: "Called when the user asks to close (× / Escape / backdrop). Required when `open` is not a plain $variable — that is the only way the dialog can be dismissed." },
    { name: "lazy", type: "boolean", optional: true, description: "Skip rendering `children`/`footer` while closed (defer charts, tables, timers)" }
  ],
  render: (node, props, helpers) => {
    const size2 = asString(props.size, "md");
    const closable = props.closable === void 0 ? true : asBoolean(props.closable);
    const isOpen = asBoolean(props.open);
    const overlay = el("div", {
      class: "rui-modal-overlay",
      "data-open": isOpen ? "true" : "false"
    });
    const titleIdSlot = helpers.useInstanceState("rui-modal-title-id", "");
    if (!titleIdSlot.get()) titleIdSlot.set(`rui-modal-title-${modalIdSeq += 1}`);
    const titleId = titleIdSlot.get();
    const dialog = el("div", {
      class: "rui-modal",
      role: "dialog",
      "aria-modal": "true",
      "aria-labelledby": titleId,
      // Focusable so we can move focus into the dialog on open.
      tabindex: "-1",
      "data-size": size2
    });
    const header = el("header", { class: "rui-modal-header" });
    header.append(el("h3", { class: "rui-modal-title", id: titleId }, [asString(props.title)]));
    const stateName = node.argMeta?.[1]?.stateRef;
    const requestClose = () => {
      if (stateName) helpers.setState(stateName, false);
      helpers.invoke(props.onRequestClose);
    };
    if (closable) {
      const closeBtn = el("button", {
        type: "button",
        class: "rui-modal-close",
        "aria-label": "Close dialog"
      }, ["×"]);
      closeBtn.onclick = (event) => {
        event.stopPropagation();
        requestClose();
      };
      header.append(closeBtn);
    }
    dialog.append(header);
    const renderContent = isOpen || !asBoolean(props.lazy);
    const body = el("div", { class: "rui-modal-body" });
    if (renderContent) {
      for (const child of asArray(props.children)) body.append(helpers.renderNode(child));
    }
    dialog.append(body);
    const footer = renderContent ? asArray(props.footer) : [];
    if (footer.length > 0) {
      const footRow = el("footer", { class: "rui-modal-footer" });
      for (const item of footer) footRow.append(helpers.renderNode(item));
      dialog.append(footRow);
    }
    overlay.append(dialog);
    if (asBoolean(props.closeOnBackdrop)) {
      overlay.onclick = (event) => {
        if (event.target === event.currentTarget) requestClose();
      };
    }
    dialog.onkeydown = (event) => {
      const live = event.currentTarget ?? event.target;
      if (event.key === "Escape" && closable) {
        event.stopPropagation();
        requestClose();
        return;
      }
      if (event.key !== "Tab") return;
      const items = focusablesIn(live);
      if (items.length === 0) {
        event.preventDefault();
        live.focus();
        return;
      }
      const first = items[0];
      const last = items[items.length - 1];
      const active = activeWithin(live);
      if (event.shiftKey && (active === first || !live.contains(active))) {
        event.preventDefault();
        last.focus();
      } else if (!event.shiftKey && active === last) {
        event.preventDefault();
        first.focus();
      }
    };
    const liveSlot = helpers.useInstanceState("rui-modal-node", null);
    if (!liveSlot.get()?.isConnected) whenMounted(overlay, (live) => liveSlot.set(live));
    const openSlot = helpers.useInstanceState(
      "rui-modal-focus",
      { open: false, prev: null }
    );
    const prevState = openSlot.get();
    if (isOpen !== prevState.open) {
      const recorded = liveSlot.get();
      const anchor = recorded && recorded.isConnected ? recorded : overlay;
      const toRestore = isOpen ? null : prevState.prev;
      openSlot.set({ open: isOpen, prev: isOpen ? activeWithin(anchor) : null });
      withLiveNode(overlay, liveSlot, (live) => {
        installModalTeardown(live, helpers);
        if (isOpen) {
          promoteOverlay$1(live);
          lockBodyScroll(live);
          const panel = live.querySelector(".rui-modal") ?? live;
          (focusablesIn(panel)[0] ?? panel).focus();
        } else {
          releaseOverlay(live);
          unlockBodyScroll(live);
          if (toRestore && typeof toRestore.focus === "function") toRestore.focus();
        }
      });
      if (!isOpen) helpers.invoke(props.onClose);
    }
    return overlay;
  }
};
const KEYWORDS$1 = {
  js: /* @__PURE__ */ new Set([
    "const",
    "let",
    "var",
    "function",
    "return",
    "if",
    "else",
    "for",
    "while",
    "do",
    "switch",
    "case",
    "break",
    "continue",
    "new",
    "class",
    "extends",
    "super",
    "this",
    "import",
    "export",
    "from",
    "default",
    "try",
    "catch",
    "finally",
    "throw",
    "typeof",
    "instanceof",
    "in",
    "of",
    "await",
    "async",
    "yield",
    "delete",
    "void",
    "null",
    "undefined",
    "true",
    "false",
    "interface",
    "type",
    "enum",
    "public",
    "private",
    "protected",
    "readonly",
    "static",
    "as",
    "implements",
    "namespace"
  ]),
  py: /* @__PURE__ */ new Set([
    "def",
    "return",
    "if",
    "elif",
    "else",
    "for",
    "while",
    "import",
    "from",
    "as",
    "class",
    "try",
    "except",
    "finally",
    "raise",
    "with",
    "lambda",
    "yield",
    "pass",
    "break",
    "continue",
    "and",
    "or",
    "not",
    "in",
    "is",
    "None",
    "True",
    "False",
    "global",
    "nonlocal",
    "async",
    "await",
    "self"
  ]),
  css: /* @__PURE__ */ new Set([]),
  json: /* @__PURE__ */ new Set(["true", "false", "null"])
};
function langFamily(lang) {
  const l = lang.toLowerCase();
  if (["js", "jsx", "ts", "tsx", "javascript", "typescript", "mjs", "cjs", "aktion"].includes(l)) return "js";
  if (["py", "python"].includes(l)) return "py";
  if (["css", "scss", "less"].includes(l)) return "css";
  if (["json", "jsonc"].includes(l)) return "json";
  if (["html", "xml", "svg", "vue"].includes(l)) return "html";
  return "generic";
}
const IDENT = /[A-Za-z_$][\w$]*/y;
const NUMBER = /0[xX][0-9a-fA-F]+|\d*\.?\d+(?:[eE][+-]?\d+)?/y;
const WS = /\s+/y;
const MAX_HIGHLIGHT_LINE_LENGTH = 4096;
function highlightLine(line, lang, state) {
  if (line.length > MAX_HIGHLIGHT_LINE_LENGTH) return [{ text: line, cls: null }];
  const family = langFamily(lang);
  if (family === "html") return highlightHtmlLine(line);
  const tokens = [];
  const keywords = KEYWORDS$1[family === "generic" ? "js" : family] ?? KEYWORDS$1.js;
  let i = 0;
  const n = line.length;
  const push = (text, cls) => {
    if (text) tokens.push({ text, cls });
  };
  while (i < n) {
    if (state.inBlockComment) {
      const close = line.indexOf("*/", i);
      if (close === -1) {
        push(line.slice(i), "comment");
        i = n;
        break;
      }
      push(line.slice(i, close + 2), "comment");
      i = close + 2;
      state.inBlockComment = false;
      continue;
    }
    const ch = line[i];
    const two = line.slice(i, i + 2);
    if (family === "py" && ch === "#" || (family === "js" || family === "css" || family === "generic") && two === "//") {
      push(line.slice(i), "comment");
      break;
    }
    if ((family === "js" || family === "css" || family === "generic") && two === "/*") {
      const close = line.indexOf("*/", i + 2);
      if (close === -1) {
        push(line.slice(i), "comment");
        state.inBlockComment = true;
        i = n;
        break;
      }
      push(line.slice(i, close + 2), "comment");
      i = close + 2;
      continue;
    }
    if (ch === '"' || ch === "'" || ch === "`") {
      let j = i + 1;
      while (j < n) {
        if (line[j] === "\\") {
          j += 2;
          continue;
        }
        if (line[j] === ch) {
          j += 1;
          break;
        }
        j += 1;
      }
      push(line.slice(i, j), "string");
      i = j;
      continue;
    }
    WS.lastIndex = i;
    const wsm = WS.exec(line);
    if (wsm && wsm.index === i) {
      push(wsm[0], null);
      i += wsm[0].length;
      continue;
    }
    NUMBER.lastIndex = i;
    const numm = NUMBER.exec(line);
    if (numm && numm.index === i && numm[0]) {
      push(numm[0], "number");
      i += numm[0].length;
      continue;
    }
    IDENT.lastIndex = i;
    const idm = IDENT.exec(line);
    if (idm && idm.index === i) {
      const word = idm[0];
      const after = line[i + word.length];
      if (keywords.has(word)) push(word, "keyword");
      else if (after === "(") push(word, "fn");
      else push(word, null);
      i += word.length;
      continue;
    }
    push(ch, "punct");
    i += 1;
  }
  return tokens;
}
function highlightHtmlLine(line) {
  const tokens = [];
  const re = /(<\/?[\w-]+)|(\/?>)|([\w-]+)(?==)|("[^"]*"|'[^']*')|(<!--.*?-->)/g;
  let last = 0;
  let m;
  while ((m = re.exec(line)) !== null) {
    if (m.index > last) tokens.push({ text: line.slice(last, m.index), cls: null });
    if (m[1]) tokens.push({ text: m[1], cls: "tag" });
    else if (m[2]) tokens.push({ text: m[2], cls: "tag" });
    else if (m[3]) tokens.push({ text: m[3], cls: "attr" });
    else if (m[4]) tokens.push({ text: m[4], cls: "string" });
    else if (m[5]) tokens.push({ text: m[5], cls: "comment" });
    last = re.lastIndex;
  }
  if (last < line.length) tokens.push({ text: line.slice(last), cls: null });
  return tokens;
}
function isHighlightable(lang) {
  return langFamily(lang) !== "generic" || lang.length > 0;
}
const ALLOWED_ELEMENTS = /* @__PURE__ */ new Set([
  "p",
  "br",
  "hr",
  "div",
  "span",
  "strong",
  "b",
  "em",
  "i",
  "u",
  "s",
  "strike",
  "mark",
  "small",
  "sub",
  "sup",
  "code",
  "pre",
  "kbd",
  "samp",
  "var",
  "abbr",
  "cite",
  "q",
  "ul",
  "ol",
  "li",
  "dl",
  "dt",
  "dd",
  "blockquote",
  "h1",
  "h2",
  "h3",
  "h4",
  "h5",
  "h6",
  "table",
  "thead",
  "tbody",
  "tfoot",
  "tr",
  "td",
  "th",
  "caption",
  "a",
  "img",
  "figure",
  "figcaption",
  "time"
]);
const ALLOWED_ATTRS = /* @__PURE__ */ new Set([
  "class",
  "title",
  "dir",
  "lang",
  "alt",
  "width",
  "height",
  "colspan",
  "rowspan",
  "headers",
  "scope",
  "abbr",
  "datetime",
  "cite",
  "loading",
  "decoding"
]);
const MAX_INPUT_LENGTH = 512 * 1024;
const MAX_NODES = 8192;
const MAX_DEPTH = 64;
let inertDoc = null;
function getInertDocument() {
  if (inertDoc) return inertDoc;
  try {
    if (typeof document === "undefined" || !document.implementation?.createHTMLDocument) return null;
    inertDoc = document.implementation.createHTMLDocument("");
    return inertDoc;
  } catch {
    return null;
  }
}
function scrub(root) {
  let nodeCount = 0;
  const visit = (element, depth) => {
    if (depth > MAX_DEPTH) return false;
    nodeCount += 1;
    if (nodeCount > MAX_NODES) return false;
    for (const child of Array.from(element.children)) {
      if (!ALLOWED_ELEMENTS.has(child.localName)) {
        const parent = child.parentNode;
        if (parent) {
          while (child.firstChild) parent.insertBefore(child.firstChild, child);
        }
        child.remove();
        continue;
      }
      if (!visit(child, depth + 1)) return false;
    }
    for (const attrName of Array.from(element.getAttributeNames())) {
      const lower = attrName.toLowerCase();
      const value = element.getAttribute(attrName) ?? "";
      if (lower.startsWith("on")) {
        element.removeAttribute(attrName);
        continue;
      }
      if (lower === "href") {
        element.removeAttribute(attrName);
        const safe = sanitiseHref(value, "");
        if (safe) element.setAttribute("href", safe);
        continue;
      }
      if (lower === "src") {
        element.removeAttribute(attrName);
        const safe = sanitiseImageSrc(value);
        if (safe) element.setAttribute("src", safe);
        continue;
      }
      if (lower === "target") {
        element.removeAttribute(attrName);
        if (value === "_blank") {
          element.setAttribute("target", "_blank");
          element.setAttribute("rel", "noopener noreferrer");
        }
        continue;
      }
      if (lower === "rel") continue;
      if (lower.startsWith("data-") || lower.startsWith("aria-")) continue;
      if (!ALLOWED_ATTRS.has(lower)) element.removeAttribute(attrName);
    }
    return true;
  };
  return visit(root, 0);
}
function sanitiseHtmlToNodes(raw) {
  const source = typeof raw === "string" ? raw : "";
  if (!source || source.length > MAX_INPUT_LENGTH) return [];
  const doc = getInertDocument();
  if (!doc) return [];
  let host;
  try {
    host = doc.createElement("div");
    host.innerHTML = source;
  } catch {
    return [];
  }
  if (!scrub(host)) return [];
  const out = [];
  for (const node of Array.from(host.childNodes)) {
    if (node.nodeType === 1) out.push(document.importNode(node, true));
    else if (node.nodeType === 3) out.push(document.createTextNode(node.textContent ?? ""));
  }
  return out;
}
function setSanitisedHtml(target, html) {
  target.replaceChildren(...sanitiseHtmlToNodes(html));
}
function readSanitisedHtml(target) {
  const doc = getInertDocument();
  if (!doc) return "";
  let host;
  try {
    host = doc.createElement("div");
    host.innerHTML = target.innerHTML;
  } catch {
    return "";
  }
  if (!scrub(host)) return "";
  return host.innerHTML;
}
function appendHighlightedLine(container, lineText, lang, state) {
  const tokens = highlightLine(lineText, lang, state);
  for (const tok of tokens) {
    if (tok.cls) container.append(el("span", { class: `rui-hl-${tok.cls}` }, [tok.text]));
    else container.append(document.createTextNode(tok.text));
  }
}
const ICON_VARIANTS = ["solid", "regular", "brands"];
const SIZE_ENUM = ["xs", "sm", "md", "lg", "xl"];
const TONE_ENUM = ["default", "neutral", "primary", "success", "warning", "danger", "info"];
function normaliseSize(value, fallback = "md") {
  const v = asString(value).trim().toLowerCase();
  if (!v) return fallback;
  const canonical = canonicalSizeToken(v);
  if (SIZE_ENUM.includes(canonical)) return canonical;
  return fallback;
}
function isSingleGlyph(value) {
  return Array.from(value.trim()).length === 1;
}
const Icon = {
  name: "Icon",
  description: 'Single Font Awesome icon. `name` is the FA name without the `fa-` prefix (e.g. `"house"`, `"chart-line"`). Use `variant` for non-solid styles (`regular`/`brands`) or prefix the name (`"regular:star"`). `color` accepts any CSS colour (`"#00ff00"`, `"tomato"`, `"var(--rui-color-primary)"`). Icons are decorative (hidden from screen readers) by default — pass `label` when the glyph carries the only meaning in its slot (a tick meaning "verified", a padlock next to a plan name) and it becomes an announced `role="img"`. `title` adds a native hover tooltip.',
  props: [
    { name: "name", type: "string", description: "FA name without the fa- prefix" },
    { name: "variant", type: "string", optional: true, aliases: ["tone"], enum: ICON_VARIANTS },
    { name: "size", type: "string", optional: true, enum: ICON_SIZES },
    { name: "color", type: "string", optional: true, description: "CSS colour applied to the glyph (hex, named, rgb()/hsl(), or var(--token))" },
    { name: "label", type: "string", optional: true, aliases: ["ariaLabel", "alt"], description: 'Accessible name — set when the icon is not decorative; announced as role="img"' },
    { name: "title", type: "string", optional: true, description: "Native hover tooltip (also used as the accessible name when `label` is omitted)" }
  ],
  render: (_node, props) => {
    const name = asString(props.name);
    const variant = asString(props.variant, "");
    const size2 = asString(props.size, "md");
    const color2 = asString(props.color, "");
    const composed = variant ? `${variant}:${name}` : name;
    const label = asString(props.label).trim();
    const title = asString(props.title).trim();
    const resolved = hasCustomIcon(composed) || resolveIconClasses(composed).length > 0;
    if (name && !resolved && !isSingleGlyph(name)) {
      return el("i", {
        class: "rui-icon rui-icon-unresolved",
        "data-icon-size": size2,
        "aria-hidden": "true"
      });
    }
    const node = renderIcon(composed, { size: size2, color: color2 });
    if (node) {
      const accessibleName = label || title;
      if (accessibleName) {
        node.removeAttribute("aria-hidden");
        node.setAttribute("role", "img");
        node.setAttribute("aria-label", accessibleName);
      } else if (!node.hasAttribute("aria-hidden")) {
        node.setAttribute("aria-hidden", "true");
      }
      if (title) node.setAttribute("title", title);
      return node;
    }
    return el("span", { class: "rui-icon", "data-icon-size": size2 }, [name]);
  }
};
const TEXT_VARIANTS = [
  "small",
  "small-heavy",
  "body",
  "body-heavy",
  "large",
  "large-heavy",
  "heading",
  "title"
];
function sanitiseInlineStyle$1(input) {
  const raw = asString(input).trim();
  if (!raw) return "";
  if (/[<>]/.test(raw)) return "";
  if (/\bexpression\s*\(|\bjavascript\s*:|\bbehavior\s*:|@import\b/i.test(raw)) {
    return "";
  }
  return raw;
}
const TEXT_TAGS = ["span", "p", "div", "h1", "h2", "h3", "h4", "h5", "h6"];
const VARIANT_ARIA_LEVEL = { title: "2", heading: "3" };
const TEXT_PROPS = [
  { name: "value", type: "string" },
  { name: "variant", type: "string", optional: true, enum: TEXT_VARIANTS },
  { name: "tone", type: "string", optional: true, enum: ["default", "muted", "primary", "success", "warning", "danger"], description: "Visual accent" },
  { name: "align", type: "string", optional: true, enum: ["left", "center", "right"], description: "Horizontal text alignment — the text becomes its own block-level line" },
  {
    name: "style",
    type: "string",
    optional: true,
    description: 'Inline CSS declarations applied to the rendered element (e.g. "font-size: 16px; font-weight: bold; color: #000;").'
  },
  { name: "as", type: "string", optional: true, enum: TEXT_TAGS, aliases: ["tag"], description: "Element to render (default `span`). Use `h1`..`h6` so a heading-styled variant is a real heading." },
  { name: "truncate", type: "boolean", optional: true, description: "Clip to a single line with a trailing ellipsis" },
  { name: "lines", type: "number", optional: true, aliases: ["clamp"], description: "Clamp to N lines with a trailing ellipsis (implies `truncate`)" }
];
function textClampStyle(truncate, lines) {
  if (lines > 1) {
    return `display:-webkit-box;-webkit-box-orient:vertical;-webkit-line-clamp:${lines};overflow:hidden`;
  }
  if (truncate || lines === 1) {
    return "display:block;white-space:nowrap;overflow:hidden;text-overflow:ellipsis";
  }
  return "";
}
const renderText = (_node, props) => {
  const variant = asString(props.variant, "body");
  const tone = asString(props.tone, "default");
  const align = asString(props.align);
  const authorStyle = sanitiseInlineStyle$1(props.style);
  const rawLines = Number(props.lines);
  const clampLines = Number.isFinite(rawLines) && rawLines > 0 ? Math.min(20, Math.floor(rawLines)) : 0;
  const clamp2 = textClampStyle(asBoolean(props.truncate), clampLines);
  const style = [clamp2, authorStyle].filter(Boolean).join(";");
  const requested = asString(props.as).trim().toLowerCase();
  const tag = TEXT_TAGS.includes(requested) ? requested : "span";
  const level = tag === "span" || tag === "p" || tag === "div" ? VARIANT_ARIA_LEVEL[variant] : void 0;
  return el(tag, {
    class: "rui-text",
    "data-variant": variant,
    "data-color": tone,
    "data-align": align || null,
    // A heading-styled variant on a generic element still needs heading
    // semantics, or rotor/heading navigation finds nothing.
    role: level ? "heading" : null,
    "aria-level": level ?? null,
    style: style || null
  }, [asString(props.value)]);
};
const Text = {
  name: "Text",
  description: 'Renders plain text with a typographic variant. `align` (left|center|right) sets the horizontal alignment (the text renders as its own block). `as` picks the element — pass `h1`..`h6` for real heading semantics (or use `Heading`); the `heading`/`title` variants otherwise get `role="heading"` so they still appear in the document outline. `truncate` clips to one line with an ellipsis and `lines: N` clamps to N lines. Optional `style` prop accepts a CSS declaration string (e.g. "font-size: 16px; color: #000;") applied directly to the rendered element.',
  props: TEXT_PROPS,
  render: renderText
};
const TextContent = {
  name: "TextContent",
  description: "Deprecated alias for `Text`. Prefer `Text(...)` — both render identically.",
  props: TEXT_PROPS,
  render: renderText
};
const IMAGE_FIT = ["cover", "contain", "fill", "none", "scale-down"];
const Image$1 = {
  name: "Image",
  description: 'Inline image. `ratio` constrains the box to a fixed aspect ratio (e.g. `16:9`, `1:1`) so callers do not need an outer `AspectRatio` (and it reserves space to avoid layout shift). `fit` controls how the image fills that box. `placeholder: "blur"` fades the image in once loaded; `sizes`/`srcset` enable responsive loading; `loading: "eager"` opts out of lazy-loading. When `src` is missing/unsafe or fails to load it shows the `fallback` text/icon.',
  props: [
    { name: "src", type: "string" },
    { name: "alt", type: "string", optional: true },
    { name: "caption", type: "string", optional: true },
    { name: "ratio", type: "string", optional: true, description: "Aspect ratio shorthand (e.g. `16:9`, `1:1`, `4:3`)" },
    { name: "fit", type: "string", optional: true, enum: IMAGE_FIT, description: "object-fit value (default `cover`)" },
    { name: "fallback", type: "string", optional: true, description: "Text label or Font Awesome icon shown when src is missing/unsafe/errored" },
    { name: "placeholder", type: "string", optional: true, enum: ["blur", "none"], description: "`blur` fades the image in on load" },
    { name: "loading", type: "string", optional: true, enum: ["lazy", "eager"], description: "Native loading strategy (default lazy)" },
    { name: "sizes", type: "string", optional: true, description: "Responsive `sizes` attribute" },
    { name: "srcset", type: "string", optional: true, aliases: ["srcSet"], description: "Responsive `srcset` candidates" },
    { name: "onClick", type: "callable", optional: true, aliases: ["onclick", "action"], description: "Makes the image activatable (gallery thumbnail, clickable avatar) — adds button semantics and keyboard activation" }
  ],
  render: (_node, props, helpers) => {
    const ratio = props.ratio ? parseImageRatio(asString(props.ratio)) : "";
    const clickable = props.onClick !== void 0 && props.onClick !== null;
    const alt = asString(props.alt);
    const wrapperStyle2 = [
      ratio ? `aspect-ratio:${ratio}` : "",
      // A ratio only reserves the box; without clipping, an image whose
      // intrinsic ratio differs spills over whatever follows.
      ratio ? "overflow:hidden" : "",
      clickable ? "cursor:pointer" : ""
    ].filter(Boolean).join(";");
    const wrapper = el("figure", {
      class: "rui-image",
      "data-fit": asString(props.fit, "cover"),
      "data-ratio": ratio ? "true" : null,
      style: wrapperStyle2 ? `${wrapperStyle2};` : null
    });
    const fillStyle = ratio ? "width:100%;height:100%;min-height:0;" : null;
    const safeSrc = sanitiseImageSrc(props.src);
    const renderFallback = () => {
      const fallback = asString(props.fallback);
      const name = alt || fallback;
      const placeholder = el("div", {
        class: "rui-image-placeholder",
        role: name ? "img" : "presentation",
        "aria-label": name || null,
        "aria-hidden": name ? null : "true",
        style: fillStyle
      });
      if (fallback) {
        const iconNode = renderIcon(fallback, { className: "rui-image-fallback-icon" });
        if (iconNode) placeholder.append(iconNode);
        else placeholder.append(el("span", { class: "rui-image-fallback-text" }, [fallback]));
      }
      return placeholder;
    };
    const failedSlot = helpers?.useInstanceState("image-failed-src", "");
    const failed = !!safeSrc && failedSlot?.get() === safeSrc;
    if (safeSrc && !failed) {
      const blur = asString(props.placeholder) === "blur";
      const loadedSlot = helpers?.useInstanceState("image-loaded", false);
      const img = el("img", {
        src: safeSrc,
        alt,
        loading: asString(props.loading, "lazy") === "eager" ? "eager" : "lazy",
        decoding: "async",
        sizes: asString(props.sizes) || null,
        srcset: asString(props.srcset) || null,
        "data-blur": blur ? "true" : null,
        "data-loaded": blur && loadedSlot?.get() === true ? "true" : null,
        style: fillStyle
      });
      if (blur && loadedSlot?.get() !== true) {
        if (img.complete) {
          loadedSlot?.set(true);
          img.setAttribute("data-loaded", "true");
        } else {
          img.onload = (event) => {
            const live = event.currentTarget ?? event.target;
            loadedSlot?.set(true);
            live?.setAttribute("data-loaded", "true");
          };
        }
      }
      img.onerror = (event) => {
        const live = typeof event === "string" ? null : event.currentTarget ?? event.target;
        failedSlot?.set(safeSrc);
        (live ?? img).replaceWith(renderFallback());
      };
      wrapper.append(img);
    } else {
      wrapper.append(renderFallback());
    }
    const cap = asString(props.caption);
    if (cap) wrapper.append(el("figcaption", { class: "rui-image-caption" }, [cap]));
    if (clickable) {
      wrapper.setAttribute("role", "button");
      wrapper.setAttribute("tabindex", "0");
      wrapper.setAttribute("aria-label", alt || cap || "Image");
      wrapper.onclick = () => {
        helpers?.invoke(props.onClick);
      };
      wrapper.onkeydown = (event) => {
        if (event.key !== "Enter" && event.key !== " ") return;
        event.preventDefault();
        helpers?.invoke(props.onClick);
      };
    }
    return wrapper;
  }
};
function parseImageRatio(input) {
  if (!input) return "auto";
  if (input.includes(":")) {
    const [w, h] = input.split(":");
    const num2 = Number(w);
    const den = Number(h);
    if (Number.isFinite(num2) && Number.isFinite(den) && den > 0) return `${num2} / ${den}`;
  }
  const n = Number(input);
  return Number.isFinite(n) && n > 0 ? `${n} / 1` : "auto";
}
const BADGE_VARIANTS = ["neutral", "primary", "success", "warning", "danger", "info"];
function renderBadgePill(label, tone, size2, icon) {
  const root = el("span", {
    class: "rui-badge",
    "data-variant": tone,
    "data-size": size2
  });
  const iconNode = renderIcon(icon, label ? { className: "rui-badge-icon" } : {});
  if (iconNode) root.append(iconNode);
  if (label) root.append(el("span", { class: "rui-badge-label" }, [label]));
  return root;
}
const Badge = {
  name: "Badge",
  description: "Small pill-style tag for status, counts, categories. Accepts an optional leading `icon` and a `size`.",
  props: [
    { name: "label", type: "string", positional: true },
    { name: "tone", type: "string", optional: true, enum: BADGE_VARIANTS, aliases: ["variant"], description: "Visual tone" },
    { name: "icon", type: "string", optional: true, description: 'Optional Font Awesome icon name (e.g. "star")' },
    { name: "size", type: "string", optional: true, enum: SIZE_ENUM }
  ],
  render: (_node, props) => renderBadgePill(
    asString(props.label),
    asString(props.tone, "neutral"),
    normaliseSize(props.size, "md"),
    props.icon
  )
};
const PILL_TONES = [
  "neutral",
  "activating",
  "success",
  "warning",
  "critical",
  "promoting",
  "corporate"
];
const PILL_TONE_ENUM = [
  ...PILL_TONES,
  "danger",
  "error",
  "info",
  "primary"
];
function normalisePillTone(value) {
  const raw = asString(value, "neutral").toLowerCase();
  if (raw === "danger" || raw === "error") return "critical";
  if (raw === "info") return "activating";
  if (raw === "primary") return "corporate";
  return PILL_TONES.includes(raw) ? raw : "neutral";
}
const Pill = {
  name: "Pill",
  description: 'Soft, tinted **state** label — pale semantic background with dark semantic text, regular weight, fully rounded. Use for the current state of a thing ("SSL active", "pending", "broken", "open ticket"). For a solid, high-attention marketing/status chip use `Badge` instead. Tones: neutral, activating, success, warning, critical, promoting, corporate (danger/error/info/primary are accepted as synonyms).',
  props: [
    { name: "label", type: "string", positional: true },
    { name: "tone", type: "string", optional: true, enum: PILL_TONE_ENUM, aliases: ["variant", "status"], description: "Semantic state tone" },
    { name: "icon", type: "string", optional: true, description: "Optional leading Font Awesome icon name" }
  ],
  render: (_node, props) => {
    const root = el("span", {
      class: "rui-pill",
      "data-tone": normalisePillTone(props.tone)
    });
    const iconNode = renderIcon(props.icon, { className: "rui-pill-icon" });
    if (iconNode) root.append(iconNode);
    const label = asString(props.label);
    if (label) root.append(el("span", { class: "rui-pill-label" }, [label]));
    return root;
  }
};
const BadgeList = {
  name: "BadgeList",
  description: "Cluster of Badge pills rendered from an array of strings. `tone` sets the whole cluster; `tones` / `icons` are index-aligned arrays that override it per item (so one item can be `success` and the next `danger`). `max` caps how many pills render and appends a `+N` overflow pill for the rest.",
  props: [
    { name: "labels", type: "string[]", positional: true, description: "Array of badge labels" },
    { name: "tone", type: "string", optional: true, enum: BADGE_VARIANTS, aliases: ["variant"] },
    { name: "size", type: "string", optional: true, enum: SIZE_ENUM },
    { name: "tones", type: "string[]", optional: true, aliases: ["variants"], description: "Per-item tones, index-aligned with `labels` (falls back to `tone`)" },
    { name: "icons", type: "string[]", optional: true, description: "Per-item leading Font Awesome icon names, index-aligned with `labels`" },
    { name: "max", type: "number", optional: true, description: "Render at most N pills, then a `+N` overflow pill" }
  ],
  render: (_node, props) => {
    const variant = asString(props.tone, "neutral");
    const size2 = normaliseSize(props.size, "md");
    const tones = asArray(props.tones);
    const icons = asArray(props.icons);
    const root = el("div", { class: "rui-badge-list" });
    const labels = asArray(props.labels).map((raw) => asString(raw)).filter((label) => label !== "");
    const rawMax = Number(props.max);
    const max = Number.isFinite(rawMax) && rawMax > 0 ? Math.floor(rawMax) : labels.length;
    const shown = labels.slice(0, max);
    shown.forEach((label, i) => {
      const itemTone = asString(tones[i]) || variant;
      root.append(renderBadgePill(label, itemTone, size2, icons[i]));
    });
    const hidden = labels.length - shown.length;
    if (hidden > 0) {
      const overflow = renderBadgePill(`+${hidden}`, variant, size2);
      overflow.setAttribute("data-overflow", "true");
      overflow.setAttribute("title", labels.slice(max).join(", "));
      overflow.setAttribute("aria-label", `${hidden} more: ${labels.slice(max).join(", ")}`);
      root.append(overflow);
    }
    return root;
  }
};
const CALLOUT_VARIANTS = ["neutral", "info", "success", "warning", "danger", "error"];
const Callout = {
  name: "Callout",
  description: 'Highlighted callout banner with variant, title, description, and leading icon. This is the library\'s alert primitive: it is announced to assistive tech by default (`role="alert"` for danger/error/warning, `role="status"` otherwise) — pass `live: false` for a static, decorative note that should stay quiet. Pass `compact: true` for a one-line inline-note rendering, `hideIcon: true` to drop the icon medallion, and `dismissible: true` (with an optional `onDismiss`) for a closable banner.',
  props: [
    { name: "tone", type: "string", optional: true, enum: CALLOUT_VARIANTS, aliases: ["variant"] },
    { name: "title", type: "string", positional: true, required: true },
    { name: "description", type: "string", optional: true, aliases: ["text"], description: "Body text" },
    { name: "icon", type: "string", optional: true, description: "Optional Font Awesome icon name" },
    { name: "compact", type: "boolean", optional: true, description: "Render with the dense, one-line note shape." },
    {
      name: "actions",
      type: "Node[]",
      optional: true,
      aliases: ["footer"],
      description: "Optional action row (buttons/links) rendered under the body"
    },
    { name: "hideIcon", type: "boolean", optional: true, aliases: ["noIcon"], description: "Render without the leading icon medallion" },
    { name: "live", type: "boolean", optional: true, description: "Announce the banner to assistive tech (default true; `false` for a static note)" },
    { name: "dismissible", type: "boolean", optional: true, aliases: ["closable"], description: "Render a × close button that removes the banner" },
    { name: "onDismiss", type: "callable", optional: true, aliases: ["onClose"], description: "Called when the banner is dismissed (implies `dismissible`)" }
  ],
  render: (_node, props, helpers) => {
    const variant = asString(props.tone, "info");
    const compact = asBoolean(props.compact);
    const dismissible = asBoolean(props.dismissible) || props.onDismiss !== void 0 && props.onDismiss !== null;
    const dismissedSlot = helpers?.useInstanceState("dismissed", false);
    if (dismissible && dismissedSlot?.get() === true) {
      return el("div", { class: "rui-callout", "data-dismissed": "true", hidden: true });
    }
    const announce = props.live === void 0 ? true : asBoolean(props.live);
    const urgent = variant === "danger" || variant === "error" || variant === "warning";
    const hideIcon = asBoolean(props.hideIcon) || props.icon === false;
    const iconName = hideIcon ? "" : asString(props.icon) || defaultCalloutIcon(variant);
    const iconNode = iconName ? renderIcon(iconName, { className: "rui-callout-icon" }) : null;
    const root = el("div", {
      class: "rui-callout",
      "data-variant": variant,
      "data-compact": compact ? "true" : "false",
      // Themes that place the icon out of flow have to indent the title past it,
      // and that indent must disappear when there is no icon. Publishing the fact
      // as an attribute keeps that a pure-CSS decision in every theme (and needs
      // no `:has()`), instead of each theme guessing from the DOM.
      "data-has-icon": iconNode ? "true" : "false",
      role: announce ? urgent ? "alert" : "status" : null,
      "aria-live": announce ? urgent ? "assertive" : "polite" : null,
      "aria-atomic": announce ? "true" : null
    });
    const section = el("div", { class: "rui-callout-section" });
    if (iconNode) section.append(iconNode);
    const body = el("div", { class: "rui-callout-body" });
    body.append(el("div", { class: "rui-callout-title" }, [asString(props.title)]));
    const desc = asString(props.description);
    if (desc) body.append(el("div", { class: "rui-callout-description" }, [desc]));
    const actions = asArray(props.actions);
    if (actions.length > 0 && helpers) {
      const footer = el("div", { class: "rui-callout-footer" });
      for (const action of actions) footer.append(helpers.renderNode(action));
      body.append(footer);
    }
    section.append(body);
    if (dismissible) {
      const closeBtn = el("button", {
        type: "button",
        class: "rui-callout-dismiss",
        "aria-label": "Dismiss"
      }, ["×"]);
      closeBtn.onclick = (event) => {
        const origin = event.currentTarget ?? event.target;
        const live = origin?.closest(".rui-callout") ?? (root.isConnected ? root : null);
        dismissedSlot?.set(true);
        live?.remove();
        helpers?.invoke(props.onDismiss);
      };
      section.append(closeBtn);
    }
    root.append(section);
    return root;
  }
};
const CodeBlock = {
  name: "CodeBlock",
  description: 'Read-only code block with a language label and a copy-to-clipboard button. Pass `showLineNumbers=true` to render a gutter; `highlightLines` accepts a string like `"3-5,8"` to emphasise specific lines. `header=false` renders a chromeless variant — no language label or copy button, no border or rounding — that fills its container (100%×100%), for embedding in your own frame (CodeWindow uses it). `filename` shows a file path in the header instead of the language token. `wrap=true` soft-wraps long lines instead of scrolling horizontally. `width`/`height` set an explicit size; overflowing code scrolls either way.',
  props: [
    { name: "language", type: "string", optional: true, description: "Display label + syntax highlighting (e.g. aktion, ts, bash)" },
    { name: "codeString", type: "string", positional: true, required: true, aliases: ["code"], description: "Raw source text" },
    { name: "showLineNumbers", type: "boolean", optional: true, description: "Render a left-side line-number gutter" },
    { name: "highlightLines", type: "string", optional: true, description: 'Highlight ranges, e.g. "3-5,8"' },
    { name: "highlight", type: "boolean", optional: true, description: "Syntax-highlight tokens when a language is set (default true)" },
    { name: "copy", type: "boolean", optional: true, description: "Show the copy-to-clipboard button (default true)" },
    { name: "header", type: "boolean", optional: true, description: "Show the header bar (default true); false = chromeless, fills its container" },
    { name: "width", type: "string", optional: true, description: "Explicit width (CSS length)" },
    { name: "height", type: "string", optional: true, description: "Explicit height (CSS length); overflowing code scrolls" },
    { name: "filename", type: "string", optional: true, aliases: ["title"], description: "File path shown in the header in place of the language token (e.g. `src/index.ts`)" },
    { name: "wrap", type: "boolean", optional: true, description: "Soft-wrap long lines instead of scrolling horizontally" }
  ],
  render: (_node, props, helpers) => {
    const language = asString(props.language);
    const code = asString(props.codeString);
    const showLineNumbers = asBoolean(props.showLineNumbers);
    const highlights = parseLineRanges(asString(props.highlightLines), code);
    const showCopy = props.copy === void 0 ? true : asBoolean(props.copy);
    const showHeader = props.header === void 0 ? true : asBoolean(props.header);
    const filename = asString(props.filename);
    const wrap = asBoolean(props.wrap);
    const width = sanitiseCssLength(props.width, "");
    const height = sanitiseCssLength(props.height, "");
    const sizeStyle = [width ? `width:${width}` : "", height ? `height:${height}` : ""].filter(Boolean).join(";");
    const root = el("div", {
      class: "rui-code-block",
      "data-headerless": showHeader ? null : "true",
      style: sizeStyle || null
    });
    if (showHeader && (filename || language || showCopy)) {
      const head = el("div", { class: "rui-code-block-head" });
      if (filename) {
        head.append(el("span", {
          class: "rui-code-block-filename",
          // The header uppercases and letter-spaces the language token; a file
          // path has to stay verbatim, so the reset rides with the one element
          // that needs it rather than adding a rule per theme.
          style: "font-family:var(--rui-font-family-mono);text-transform:none;letter-spacing:0;"
        }, [filename]));
      } else if (language) {
        head.append(el("span", { class: "rui-code-block-language" }, [language]));
      }
      if (showCopy) {
        const copiedSlot = helpers?.useInstanceState("copied", false);
        const copyBtn = el("button", {
          type: "button",
          class: "rui-code-block-copy",
          "aria-label": "Copy code",
          title: "Copy"
        });
        const copyIcon = renderIcon("copy", { className: "rui-code-block-copy-icon" });
        if (copyIcon) copyBtn.append(copyIcon);
        copyBtn.append(el("span", {
          class: "rui-code-block-copy-label",
          // Announce the Copy → Copied swap; the button's own name is static.
          "aria-live": "polite"
        }, [copiedSlot?.get() === true ? "Copied" : "Copy"]));
        copyBtn.onclick = (event) => {
          const origin = event.currentTarget ?? event.target;
          const live = origin.closest(".rui-code-block");
          const text = liveCodeText(live, code);
          const nav = typeof navigator !== "undefined" ? navigator : null;
          const clipboard = nav?.clipboard;
          if (clipboard?.writeText) {
            clipboard.writeText(text).catch(() => {
            });
          }
          const label = origin.querySelector(".rui-code-block-copy-label");
          if (label) {
            label.textContent = "Copied";
            copiedSlot?.set(true);
            const handle = setTimeout(() => {
              copiedSlot?.set(false);
              if (label.isConnected) label.textContent = "Copy";
            }, 1500);
            helpers?.registerDisposer(() => clearTimeout(handle));
          }
        };
        head.append(copyBtn);
      }
      root.append(head);
    }
    const pre = el("pre", {
      class: "rui-code-block-pre",
      "data-line-numbers": showLineNumbers ? "true" : "false",
      // A horizontally scrolling region has to be reachable without a mouse
      // (WCAG 2.1.1), and a focusable scroll container needs a name.
      tabindex: "0",
      role: "region",
      "aria-label": language ? `${language} code` : "Code",
      style: wrap ? "white-space:pre-wrap;overflow-wrap:anywhere;" : null
    });
    const wrapLineStyle = wrap ? "white-space:pre-wrap;overflow-wrap:anywhere;min-width:0;" : null;
    const wantHighlight = (props.highlight === void 0 ? true : asBoolean(props.highlight)) && !!language && isHighlightable(language);
    if (showLineNumbers || highlights.size > 0) {
      const lines = code.split(/\r?\n/);
      const codeEl = el("code", {});
      const hlState = { inBlockComment: false };
      lines.forEach((lineText, idx) => {
        const lineNumber = idx + 1;
        const line = el("span", {
          class: "rui-code-block-line",
          "data-line": String(lineNumber),
          "data-highlight": highlights.has(lineNumber) ? "true" : null
        });
        if (showLineNumbers) {
          line.append(el("span", { class: "rui-code-block-gutter" }, [String(lineNumber)]));
        }
        const codeSpan = el("span", { class: "rui-code-block-code", style: wrapLineStyle });
        if (wantHighlight) appendHighlightedLine(codeSpan, lineText, language, hlState);
        else codeSpan.append(document.createTextNode(lineText));
        line.append(codeSpan);
        codeEl.append(line);
      });
      pre.append(codeEl);
    } else if (wantHighlight) {
      const codeEl = el("code", {});
      const hlState = { inBlockComment: false };
      const lines = code.split(/\r?\n/);
      lines.forEach((lineText, idx) => {
        appendHighlightedLine(codeEl, lineText, language, hlState);
        if (idx < lines.length - 1) codeEl.append(document.createTextNode("\n"));
      });
      pre.append(codeEl);
    } else {
      pre.append(el("code", {}, [code]));
    }
    root.append(pre);
    return root;
  }
};
function liveCodeText(root, fallback) {
  const codeEl = root?.querySelector("code");
  if (!codeEl) return fallback;
  const lines = codeEl.querySelectorAll(".rui-code-block-code");
  if (lines.length === 0) return codeEl.textContent ?? fallback;
  return Array.from(lines).map((line) => line.textContent ?? "").join("\n");
}
function parseLineRanges(input, code = "") {
  const out = /* @__PURE__ */ new Set();
  if (!input) return out;
  const lineCount = code ? code.split(/\r?\n/).length : MAX_HIGHLIGHT_LINES;
  const ceiling = Math.min(lineCount, MAX_HIGHLIGHT_LINES);
  for (const segment of input.split(",")) {
    const trimmed = segment.trim();
    if (!trimmed) continue;
    if (trimmed.includes("-")) {
      const parts = trimmed.split("-").map((s) => Number(s.trim()));
      const a = parts[0] ?? NaN;
      const b = parts[1] ?? NaN;
      if (Number.isFinite(a) && Number.isFinite(b) && a <= b) {
        const start = Math.max(0, Math.floor(a));
        const end = Math.min(Math.floor(b), ceiling);
        for (let i = start; i <= end; i += 1) out.add(i);
      }
    } else {
      const n = Number(trimmed);
      if (Number.isFinite(n)) out.add(n);
    }
  }
  return out;
}
const MAX_HIGHLIGHT_LINES = 1e4;
function defaultCalloutIcon(variant) {
  switch (variant) {
    case "success":
      return "circle-check";
    case "warning":
      return "triangle-exclamation";
    case "danger":
    case "error":
      return "circle-xmark";
    case "info":
      return "circle-info";
    default:
      return "circle-info";
  }
}
const SKELETON_VARIANTS = ["paragraph", "card", "table-row", "avatar", "image"];
const SKELETON_SHAPES = ["rect", "circle"];
const Skeleton = {
  name: "Skeleton",
  description: 'Loading placeholder. Pass a `variant` for common shapes — `paragraph` (default), `card`, `table-row`, `avatar`, `image` — or use `shape` / `width` / `height` to build a custom one. All variants use a shimmer animation that respects `prefers-reduced-motion`. The placeholder is a polite live region announcing `label` (default "Loading"), so a screen reader hears the wait instead of silence — when you stack several Skeletons into one loading view, pass `live: false` on all but the first so the announcement happens once rather than once per placeholder.',
  props: [
    { name: "variant", aliases: ["tone"], type: "string", optional: true, enum: SKELETON_VARIANTS },
    { name: "lines", type: "number", optional: true, aliases: ["count", "columns"], description: "Lines for the `paragraph` variant (default 3) / cells for `table-row` (default 4)" },
    { name: "height", type: "number | string", optional: true, description: "Line height in px (paragraph) or CSS height for custom shape; a bare number is px" },
    { name: "shape", type: "string", optional: true, enum: SKELETON_SHAPES, description: "Force a primitive shape (rect/circle)" },
    { name: "width", type: "string", optional: true, description: "CSS width for shape-only skeletons" },
    { name: "label", type: "string", optional: true, description: 'What is loading, announced to assistive tech (default "Loading")' },
    { name: "live", type: "boolean", optional: true, description: "Announce the wait (default true); `false` keeps this placeholder silent when a sibling already announces" }
  ],
  render: (_node, props) => {
    const variant = asString(props.variant);
    const shape = asString(props.shape);
    if (shape) return renderShapeSkeleton(shape, props);
    if (variant && variant !== "paragraph") return renderVariantSkeleton(variant, props);
    const rawLines = Number(props.lines);
    const lines = Math.max(1, Math.min(50, Number.isFinite(rawLines) ? Math.floor(rawLines) : 3));
    const rawHeight = Number(props.height);
    const lineHeight = Number.isFinite(rawHeight) && rawHeight > 0 ? Math.min(200, Math.floor(rawHeight)) : 12;
    const root = skeletonRoot(props, { class: "rui-skeleton", "data-variant": "paragraph" });
    for (let i = 0; i < lines; i += 1) {
      root.append(el("div", { class: "rui-skeleton-line", style: `height:${lineHeight}px`, "aria-hidden": "true" }));
    }
    return root;
  }
};
function skeletonRoot(props, attrs) {
  const label = asString(props.label) || "Loading";
  const announce = props.live === void 0 ? true : asBoolean(props.live);
  const root = el("div", {
    ...attrs,
    role: "status",
    "aria-live": announce ? "polite" : "off",
    "aria-busy": "true",
    "aria-label": label
  });
  if (announce) root.append(el("span", { class: "rui-visually-hidden" }, [label]));
  return root;
}
function cssLengthProp(value, fallback) {
  if (typeof value === "number") {
    return Number.isFinite(value) ? `${value}px` : fallback;
  }
  const raw = asString(value).trim();
  if (!raw) return fallback;
  if (/^\d+(\.\d+)?$/.test(raw)) return `${raw}px`;
  return sanitiseCssLength(raw, fallback);
}
function renderShapeSkeleton(shape, props) {
  const width = cssLengthProp(props.width, "100%");
  const height = props.height !== void 0 && props.height !== null && asString(props.height) !== "" ? cssLengthProp(props.height, "16px") : shape === "circle" ? width : "16px";
  const kind = shape === "circle" ? "circle" : "rect";
  const root = skeletonRoot(props, {
    class: "rui-skeleton",
    "data-variant": "shape",
    "data-shape": kind,
    style: `width:${width};`
  });
  root.append(el("div", {
    class: "rui-skeleton-shape",
    "data-shape": kind,
    style: `width:100%;height:${height};`,
    "aria-hidden": "true"
  }));
  return root;
}
function renderVariantSkeleton(variant, props) {
  const root = skeletonRoot(props, { class: "rui-skeleton", "data-variant": variant });
  switch (variant) {
    case "avatar": {
      const size2 = cssLengthProp(props.width, "40px");
      root.append(el("div", {
        class: "rui-skeleton-shape",
        "data-shape": "circle",
        style: `width:${size2};height:${size2};`,
        "aria-hidden": "true"
      }));
      return root;
    }
    case "image": {
      const width = cssLengthProp(props.width, "100%");
      const height = cssLengthProp(props.height, "160px");
      root.append(el("div", {
        class: "rui-skeleton-shape",
        "data-shape": "rect",
        style: `width:${width};height:${height};`,
        "aria-hidden": "true"
      }));
      return root;
    }
    case "card": {
      root.append(el("div", { class: "rui-skeleton-shape", "data-shape": "rect", style: "width:100%;height:120px;", "aria-hidden": "true" }));
      root.append(el("div", { class: "rui-skeleton-line", style: "height:14px;width:70%;", "aria-hidden": "true" }));
      root.append(el("div", { class: "rui-skeleton-line", style: "height:12px;width:90%;", "aria-hidden": "true" }));
      root.append(el("div", { class: "rui-skeleton-line", style: "height:12px;width:60%;", "aria-hidden": "true" }));
      return root;
    }
    case "table-row": {
      const rawCells = Number(props.lines);
      const cells = Math.max(1, Math.min(8, Number.isFinite(rawCells) ? Math.floor(rawCells) : 4));
      const row = el("div", { class: "rui-skeleton-row", "aria-hidden": "true" });
      for (let i = 0; i < cells; i += 1) {
        row.append(el("div", { class: "rui-skeleton-line", style: "height:12px;flex:1;" }));
      }
      root.append(row);
      return root;
    }
    default:
      return root;
  }
}
const MARKDOWN_LINK_TARGETS = ["_self", "_blank"];
const Markdown = {
  name: "Markdown",
  description: "Render markdown-flavoured text. Supports **bold**, *italic*, `code`, headings (`#` through `######`), blockquotes (`>`), bullet (`-`/`*`) and numbered (`1.`) lists, thematic breaks (`---`), fenced code blocks (```), images (`![alt](src)`), inline links, and auto-linked bare URLs. Multi-line paragraphs collapse into `<p>` blocks. Links to a fragment or a root-relative path stay in the tab; absolute URLs open in a new one — pass `linkTarget` to force one or the other.",
  props: [
    { name: "content", type: "string" },
    { name: "linkTarget", type: "string", optional: true, enum: MARKDOWN_LINK_TARGETS, aliases: ["target"], description: "Force every link's target (default: `_self` for in-app links, `_blank` for absolute URLs)" }
  ],
  render: (_node, props) => {
    const value = asString(props.content);
    const requestedTarget = asString(props.linkTarget).trim();
    const linkTarget = MARKDOWN_LINK_TARGETS.includes(requestedTarget) ? requestedTarget : "";
    const html = renderMarkdown(value, linkTarget);
    const root = el("div", { class: "rui-markdown" });
    setSanitisedHtml(root, html);
    return root;
  }
};
const Container = {
  name: "Container",
  description: "Centered, max-width content wrapper. Use when a page is wider than comfortable reading width — landing pages, marketing sections, long documents. Picks a sensible default max-width per size; pass `maxWidth` to override with any CSS value.",
  props: [
    { name: "children", aliases: ["child"], type: "Node[]" },
    { name: "size", type: "string", optional: true, enum: ["sm", "md", "lg", "xl", "full"], description: "sm=640 / md=820 / lg=1040 / xl=1280 / full=100% (default lg)" },
    { name: "maxWidth", type: "string", optional: true, description: "Custom CSS max-width (overrides `size`)" },
    { name: "padding", type: "string", optional: true, enum: SPACING_TOKENS, description: "Horizontal padding (default md)" }
  ],
  render: (_node, props, helpers) => {
    const maxWidth = props.maxWidth ? sanitiseCssLength(props.maxWidth, "auto") : "";
    const root = el("div", {
      class: "rui-container",
      "data-size": canonicalSizeToken(asString(props.size, "lg")),
      "data-padding": normalizeSpacingToken(props.padding, "md"),
      style: `${maxWidth ? `max-width:${maxWidth};` : ""}margin-inline:auto;`
    });
    for (const child of asArray(props.children)) root.append(helpers.renderNode(child));
    return root;
  }
};
const Spacer = {
  name: "Spacer",
  description: 'Explicit space element for fine layout control. By default acts as a flex spacer that pushes following content to the far edge (use inside `Stack(direction="row")`). Pass `size` to render a fixed vertical/horizontal gap instead.',
  props: [
    { name: "size", type: "string", optional: true, enum: SPACING_TOKENS, description: "Fixed gap; omit to flex-grow" },
    { name: "flex", type: "boolean", optional: true, description: "Flex-grow even when size is set (default true when size omitted)" }
  ],
  render: (_node, props) => {
    const size2 = normalizeSpacingToken(props.size, asString(props.size));
    const flex = props.flex === void 0 ? !size2 : asBoolean(props.flex);
    return el("span", {
      class: "rui-spacer",
      "data-size": size2 || null,
      "data-flex": flex ? "true" : "false",
      "aria-hidden": "true"
    });
  }
};
const LoadingDots = {
  name: "LoadingDots",
  description: "Three dots that pulse in sequence — an inline indeterminate loader. Lighter and quieter than `Spinner`'s rotating ring; use inside buttons, beside labels, or in table cells. Pass `label` for an announced caption.",
  props: [
    { name: "label", type: "string", optional: true, positional: true, description: "Caption rendered beside the dots (also announced)" },
    { name: "size", type: "string", optional: true, enum: SIZE_ENUM, description: "Default `md`" },
    { name: "tone", aliases: ["variant"], type: "string", optional: true, enum: TONE_ENUM, description: "Visual accent (default `primary`)" }
  ],
  render: (_node, props) => {
    const label = asString(props.label);
    const root = el("span", {
      class: "rui-loading-dots",
      "data-size": normaliseSize(props.size, "md"),
      "data-tone": asString(props.tone, "primary"),
      role: "status",
      "aria-live": "polite",
      "aria-label": label || "Loading"
    });
    const dots = el("span", { class: "rui-loading-dots-track", "aria-hidden": "true" });
    for (let i = 0; i < 3; i += 1) dots.append(el("span", { class: "rui-loading-dots-dot" }));
    root.append(dots);
    if (label) root.append(el("span", { class: "rui-loading-dots-label" }, [label]));
    else root.append(el("span", { class: "rui-visually-hidden" }, ["Loading"]));
    return root;
  }
};
const Spinner = {
  name: "Spinner",
  description: "Indeterminate inline loader (a rotating ring). Use for tiny loading states inside buttons, toolbars, table cells, or chat bubbles where `Skeleton` and `Progress(indeterminate=true)` are too heavy. For a quieter three-dot pulse instead of a ring use `LoadingDots`. Pass `label` to render an inline caption beside the spinner (also announced via `aria-label`).",
  props: [
    { name: "size", type: "string", optional: true, enum: SIZE_ENUM, description: "Default `md`" },
    { name: "label", type: "string", optional: true, description: "Caption rendered beside the spinner (also announced)" },
    { name: "tone", aliases: ["variant"], type: "string", optional: true, enum: TONE_ENUM, description: "Visual accent (default `primary`)" }
  ],
  render: (_node, props) => {
    const size2 = normaliseSize(props.size, "md");
    const tone = asString(props.tone, "primary");
    const label = asString(props.label);
    const root = el("span", {
      class: "rui-spinner",
      "data-size": size2,
      "data-tone": tone,
      role: "status",
      "aria-live": "polite",
      "aria-label": label || "Loading"
    });
    root.append(el("span", { class: "rui-spinner-ring", "aria-hidden": "true" }));
    if (label) root.append(el("span", { class: "rui-spinner-label" }, [label]));
    else root.append(el("span", { class: "rui-visually-hidden" }, ["Loading"]));
    return root;
  }
};
const Quote = {
  name: "Quote",
  description: "Inline pull-quote with optional citation. Lighter than `Testimonial` — use inside articles, blog posts, marketing sections, or anywhere you need to highlight a sentence without the full quote/author/role + rating shape.",
  props: [
    { name: "text", type: "string" },
    { name: "cite", type: "string", optional: true, aliases: ["attribution", "author"], description: "Attribution text shown below the quote" },
    { name: "tone", type: "string", optional: true, aliases: ["variant"], enum: ["default", "primary", "success", "warning", "danger", "info"] }
  ],
  render: (_node, props) => {
    const root = el("figure", {
      class: "rui-quote",
      "data-tone": asString(props.tone, "default")
    });
    root.append(el("blockquote", { class: "rui-quote-text" }, [asString(props.text)]));
    const cite = asString(props.cite);
    if (cite) root.append(el("figcaption", { class: "rui-quote-cite" }, [cite]));
    return root;
  }
};
const SENTINEL = "\0";
const SENTINEL_RE = /\u0000(\d+)\u0000/g;
const MARKDOWN_MAX_LENGTH = 128 * 1024;
const MARKDOWN_MAX_LINE_LENGTH = 8 * 1024;
function renderMarkdown(value, linkTarget = "") {
  const escape = (s) => s.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;");
  const lines = value.slice(0, MARKDOWN_MAX_LENGTH).replace(/[\u0000-\u0008\u000B\u000C\u000E-\u001F]/g, "").split(/\r?\n/).map((line) => line.length > MARKDOWN_MAX_LINE_LENGTH ? line.slice(0, MARKDOWN_MAX_LINE_LENGTH) : line);
  const out = [];
  let listMode = null;
  let inQuote = false;
  let inCode = false;
  let codeLang = "";
  let codeBuf = [];
  const closeList = () => {
    if (listMode) {
      out.push(listMode === "ul" ? "</ul>" : "</ol>");
      listMode = null;
    }
  };
  const closeQuote = () => {
    if (inQuote) {
      out.push("</blockquote>");
      inQuote = false;
    }
  };
  const flushCode = () => {
    const lang = codeLang ? ` data-language="${escapeAttr(codeLang)}"` : "";
    out.push(`<pre class="rui-markdown-code"${lang}><code>${escape(codeBuf.join("\n"))}</code></pre>`);
    codeBuf = [];
    codeLang = "";
    inCode = false;
  };
  for (const rawLine of lines) {
    const fenceMatch = /^```\s*(.*)$/.exec(rawLine.trim());
    if (inCode) {
      if (fenceMatch) {
        flushCode();
        continue;
      }
      codeBuf.push(rawLine);
      continue;
    }
    if (fenceMatch) {
      closeList();
      closeQuote();
      inCode = true;
      codeLang = fenceMatch[1] ?? "";
      continue;
    }
    const heading = /^\s*(#{1,6})\s+(.+)$/.exec(rawLine);
    if (heading) {
      closeList();
      closeQuote();
      const level = Math.min(6, heading[1].length);
      out.push(`<h${level} class="rui-markdown-h${level}">${inline(escape(heading[2]))}</h${level}>`);
      continue;
    }
    if (/^\s*(?:-{3,}|\*{3,}|_{3,})\s*$/.test(rawLine)) {
      closeList();
      closeQuote();
      out.push('<hr class="rui-markdown-rule">');
      continue;
    }
    const quoteMatch = /^\s*>\s?(.*)$/.exec(rawLine);
    if (quoteMatch) {
      closeList();
      if (!inQuote) {
        out.push('<blockquote class="rui-markdown-quote">');
        inQuote = true;
      }
      out.push(`<p>${inline(escape(quoteMatch[1] ?? ""))}</p>`);
      continue;
    } else if (inQuote && rawLine.trim() !== "") {
      closeQuote();
    }
    const ulMatch = /^\s*[-*]\s+(.*)$/.exec(rawLine);
    if (ulMatch) {
      closeQuote();
      if (listMode !== "ul") {
        closeList();
        out.push("<ul>");
        listMode = "ul";
      }
      out.push(`<li>${inline(escape(ulMatch[1] ?? ""))}</li>`);
      continue;
    }
    const olMatch = /^\s*\d+\.\s+(.*)$/.exec(rawLine);
    if (olMatch) {
      closeQuote();
      if (listMode !== "ol") {
        closeList();
        out.push("<ol>");
        listMode = "ol";
      }
      out.push(`<li>${inline(escape(olMatch[1] ?? ""))}</li>`);
      continue;
    }
    if (rawLine.trim() === "") {
      closeList();
      closeQuote();
      continue;
    }
    closeList();
    closeQuote();
    out.push(`<p>${inline(escape(rawLine))}</p>`);
  }
  if (inCode) flushCode();
  closeList();
  closeQuote();
  return out.join("");
  function inline(s) {
    const slots = [];
    const park = (html) => {
      slots.push(html);
      return `${SENTINEL}${slots.length - 1}${SENTINEL}`;
    };
    const attr = (v) => v.replace(/"/g, "&quot;").replace(/'/g, "&#39;");
    const attrUrl = (v) => attr(v).replace(/&(?!(amp|quot|#39);)/g, "&amp;");
    const anchor = (href, body) => {
      const internal = href.startsWith("#") || href.startsWith("/") || href.startsWith("?");
      const target = linkTarget || (internal ? "_self" : "_blank");
      const rel = target === "_blank" ? ' rel="noopener noreferrer"' : "";
      return `<a class="rui-link" href="${href}" target="${target}"${rel}>${body}</a>`;
    };
    let result = s.replace(/!\[([^\]]*)\]\(([^)]+)\)/g, (_match, alt, rawSrc) => {
      const src = sanitiseImageSrc(decodeEntities(rawSrc));
      if (!src) return park(`<span class="rui-markdown-image-fallback">${alt}</span>`);
      return park(
        `<img class="rui-markdown-image" src="${attrUrl(src)}" alt="${attr(alt)}" loading="lazy">`
      );
    }).replace(/`([^`]+)`/g, (_m, code) => park(`<code>${code}</code>`)).replace(/\*\*([^*]+)\*\*/g, (_m, b) => park(`<strong>${b}</strong>`)).replace(/\*([^*]+)\*/g, (_m, i) => park(`<em>${i}</em>`)).replace(/\[([^\]]+)\]\(([^)]+)\)/g, (_match, label, rawHref) => {
      const href = sanitizeMarkdownHref(rawHref);
      return park(anchor(href, label));
    });
    result = result.replace(
      /\bhttps?:\/\/[a-zA-Z0-9._~:/?#@!$&'()*+,;=%\-]+/g,
      (url) => {
        const safe = sanitizeMarkdownHref(url);
        return park(anchor(safe, attr(url)));
      }
    );
    return result.replace(SENTINEL_RE, (_m, i) => slots[Number(i)] ?? "");
  }
}
function decodeEntities(raw) {
  let out = raw;
  for (let i = 0; i < 5; i += 1) {
    const next = decodeEntitiesOnce(out);
    if (next === out) break;
    out = next;
  }
  return out;
}
function decodeEntitiesOnce(raw) {
  return raw.replace(/&(#[0-9]{1,7}|#[xX][0-9a-fA-F]{1,6}|[a-zA-Z][a-zA-Z0-9]{1,31});?/g, (whole, body) => {
    if (body.startsWith("#")) {
      const hex = body[1] === "x" || body[1] === "X";
      const code = Number.parseInt(hex ? body.slice(2) : body.slice(1), hex ? 16 : 10);
      if (!Number.isFinite(code) || code < 0 || code > 1114111) return whole;
      try {
        return String.fromCodePoint(code);
      } catch {
        return whole;
      }
    }
    const named = {
      amp: "&",
      lt: "<",
      gt: ">",
      quot: '"',
      apos: "'",
      nbsp: " ",
      Tab: "	",
      NewLine: "\n",
      colon: ":",
      sol: "/",
      commat: "@"
    };
    return named[body] ?? whole;
  });
}
function sanitizeMarkdownHref(raw) {
  const trimmed = decodeEntities(raw).replace(/[\u0000-\u001F\u007F]/g, "").trim();
  if (!trimmed) return "#";
  if (trimmed.startsWith("//")) return "#";
  if (trimmed.startsWith("#") || trimmed.startsWith("/") || trimmed.startsWith("?")) {
    return escapeAttr(trimmed);
  }
  const schemeMatch = /^([a-zA-Z][a-zA-Z0-9+.-]*):/.exec(trimmed);
  if (!schemeMatch) return escapeAttr(trimmed);
  const scheme = schemeMatch[1].toLowerCase();
  const allowed = /* @__PURE__ */ new Set(["http", "https", "mailto", "tel"]);
  if (!allowed.has(scheme)) return "#";
  return escapeAttr(trimmed);
}
function escapeAttr(value) {
  return value.replace(/&/g, "&amp;").replace(/"/g, "&quot;").replace(/'/g, "&#39;").replace(/</g, "&lt;").replace(/>/g, "&gt;");
}
function renderAvatar(src, name, size2, helpers) {
  const trimmed = name.trim();
  const root = el("span", {
    class: "rui-avatar",
    "data-size": size2,
    role: trimmed ? "img" : null,
    "aria-label": trimmed || null,
    "aria-hidden": trimmed ? null : "true"
  });
  const safeSrc = sanitiseImageSrc(src);
  const errorSlot = safeSrc ? helpers.useInstanceState(`avatar-error:${safeSrc}`, false) : null;
  if (safeSrc && !errorSlot?.get()) {
    const img = el("img", { src: safeSrc, alt: name, loading: "lazy" });
    img.onerror = (event) => {
      errorSlot?.set(true);
      const ev = event;
      const live = ev.currentTarget ?? ev.target;
      live.replaceWith(el("span", { class: "rui-avatar-fallback" }, [initialsFor(name)]));
    };
    root.append(img);
  } else {
    root.append(el("span", { class: "rui-avatar-fallback" }, [initialsFor(name)]));
  }
  return root;
}
function initialsFor(name) {
  const trimmed = name.trim();
  if (!trimmed) return "?";
  const parts = trimmed.split(/\s+/).slice(0, 2);
  return parts.map((p) => p.charAt(0).toUpperCase()).join("") || trimmed.charAt(0).toUpperCase();
}
const DISMISS_REGISTRY = /* @__PURE__ */ new WeakMap();
const KEYED_REGISTRY = /* @__PURE__ */ new Map();
const OPEN_FLOATERS = [];
const HANDLED_ESCAPES = /* @__PURE__ */ new WeakSet();
function topFloater() {
  for (let i = OPEN_FLOATERS.length - 1; i >= 0; i -= 1) {
    const entry = OPEN_FLOATERS[i];
    if (entry.root.isConnected) return entry;
    entry.handle.dispose();
  }
  return null;
}
function installDismissListeners(opts) {
  const { liveRoot, onDismiss, key } = opts;
  DISMISS_REGISTRY.get(liveRoot)?.dispose();
  const prior = key ? KEYED_REGISTRY.get(key) : void 0;
  if (prior && !prior.root.isConnected) prior.handle.dispose();
  for (const stale of [...OPEN_FLOATERS]) {
    if (!stale.root.isConnected) stale.handle.dispose();
  }
  const host = liveRoot.getRootNode();
  let disposed = false;
  const onOutside = (event) => {
    const target = event.target;
    if (target && liveRoot.contains(target)) return;
    handle.dispose();
    onDismiss();
  };
  const onKey = (event) => {
    if (event.key !== "Escape") return;
    if (HANDLED_ESCAPES.has(event) || topFloater()?.handle !== handle) return;
    HANDLED_ESCAPES.add(event);
    event.preventDefault();
    event.stopPropagation();
    const path = typeof event.composedPath === "function" ? event.composedPath() : [];
    const fromInside = path.includes(liveRoot) || liveRoot.contains(event.target);
    handle.dispose();
    onDismiss();
    if (fromInside) liveRoot.querySelector("[aria-expanded]")?.focus?.();
  };
  const ownerDoc = liveRoot.ownerDocument;
  let observer = null;
  if (ownerDoc && typeof MutationObserver !== "undefined") {
    observer = new MutationObserver(() => {
      if (!liveRoot.isConnected) handle.dispose();
    });
    const observeRoot = liveRoot.getRootNode();
    if (observeRoot instanceof Element || observeRoot instanceof Document || observeRoot instanceof ShadowRoot) {
      observer.observe(observeRoot, { childList: true, subtree: true });
    }
  }
  const handle = {
    dispose: () => {
      if (disposed) return;
      disposed = true;
      host.removeEventListener("click", onOutside, true);
      host.removeEventListener("keydown", onKey, true);
      observer?.disconnect();
      DISMISS_REGISTRY.delete(liveRoot);
      const idx = OPEN_FLOATERS.indexOf(entry);
      if (idx >= 0) OPEN_FLOATERS.splice(idx, 1);
      if (key && KEYED_REGISTRY.get(key) === entry) KEYED_REGISTRY.delete(key);
    }
  };
  const entry = { root: liveRoot, key, handle };
  host.addEventListener("keydown", onKey, true);
  setTimeout(() => {
    if (disposed) return;
    host.addEventListener("click", onOutside, true);
  }, 0);
  DISMISS_REGISTRY.set(liveRoot, handle);
  if (key) KEYED_REGISTRY.set(key, entry);
  OPEN_FLOATERS.push(entry);
  return handle;
}
function disposeDismissListeners(liveRoot) {
  if (!liveRoot) return;
  const existing = DISMISS_REGISTRY.get(liveRoot);
  if (existing) existing.dispose();
}
const FOCUSABLE_SELECTOR$1 = "a[href], button:not([disabled]), input:not([disabled]), select:not([disabled]), textarea:not([disabled]), [tabindex]:not([tabindex='-1'])";
function dialogKeydownHandler(panelSelector, close) {
  return (event) => {
    const origin = event.currentTarget ?? event.target;
    if (!origin) return;
    if (event.key === "Escape") {
      event.stopPropagation();
      close(origin);
      return;
    }
    if (event.key !== "Tab") return;
    const panel = origin.querySelector(panelSelector);
    if (!panel) return;
    const focusables = Array.from(panel.querySelectorAll(FOCUSABLE_SELECTOR$1));
    if (focusables.length === 0) {
      event.preventDefault();
      return;
    }
    const doc = origin.getRootNode();
    const active = doc.activeElement;
    const first = focusables[0];
    const last = focusables[focusables.length - 1];
    if (event.shiftKey && (active === first || !panel.contains(active))) {
      event.preventDefault();
      last.focus();
    } else if (!event.shiftKey && active === last) {
      event.preventDefault();
      first.focus();
    }
  };
}
function wireDialogFocus(root, panelSelector, helpers) {
  if (typeof MutationObserver === "undefined") return;
  setTimeout(() => {
    if (!root.isConnected) return;
    let lastFocus = null;
    const onFlip = (isOpen) => {
      const doc = root.getRootNode();
      if (isOpen) {
        lastFocus = doc.activeElement ?? null;
        const panel = root.querySelector(panelSelector);
        if (panel && !panel.contains(doc.activeElement)) {
          (panel.querySelector(FOCUSABLE_SELECTOR$1) ?? panel).focus?.();
        }
      } else {
        lastFocus?.focus?.();
        lastFocus = null;
      }
    };
    let prevOpen = root.getAttribute("data-open") === "true";
    if (prevOpen) onFlip(true);
    const observer = new MutationObserver(() => {
      const nowOpen = root.getAttribute("data-open") === "true";
      if (nowOpen !== prevOpen) {
        prevOpen = nowOpen;
        onFlip(nowOpen);
      }
    });
    observer.observe(root, { attributes: true, attributeFilter: ["data-open"] });
    helpers.registerDisposer(() => observer.disconnect(), "rui-dialog-focus");
  }, 0);
}
const ICON_KEYWORD_RULES = [
  { match: /\b(revenue|sales|sale|income|payment|charges?|invoices?|billing)\b/i, icon: "sack-dollar" },
  { match: /\b(profit|earnings?|margin|roi)\b/i, icon: "chart-line" },
  { match: /\b(customers?|clients?|users?|members?|people|accounts?)\b/i, icon: "users" },
  { match: /\b(visitors?|sessions?|traffic|page-?views?|impressions?)\b/i, icon: "chart-line" },
  { match: /\b(orders?|carts?|purchases?|transactions?)\b/i, icon: "cart-shopping" },
  { match: /\b(products?|inventory|sku|stock|items?)\b/i, icon: "box" },
  { match: /\b(subscriptions?|plans?|tiers?|pricing)\b/i, icon: "credit-card" },
  { match: /\b(emails?|messages?|inbox|threads?|mail)\b/i, icon: "envelope" },
  { match: /\b(notifications?|alerts?|reminders?)\b/i, icon: "bell" },
  { match: /\b(growth|trend|increase|up)\b/i, icon: "arrow-trend-up" },
  { match: /\b(decline|drop|down|decrease|loss)\b/i, icon: "arrow-trend-down" },
  { match: /\b(reports?|analytics|insights?|dashboards?|metrics?|stats?|kpis?)\b/i, icon: "chart-pie" },
  { match: /\b(charts?|graphs?)\b/i, icon: "chart-column" },
  { match: /\b(tasks?|todos?|backlog|kanban|sprint)\b/i, icon: "list-check" },
  { match: /\b(projects?|workspaces?)\b/i, icon: "folder-open" },
  { match: /\b(files?|folders?|documents?|docs?|attachments?)\b/i, icon: "folder-open" },
  { match: /\b(images?|photos?|gallery|albums?)\b/i, icon: "image" },
  { match: /\b(videos?|clips?|recordings?)\b/i, icon: "video" },
  { match: /\b(audio|music|podcasts?|sounds?)\b/i, icon: "music" },
  { match: /\b(calendars?|schedule|events?|meetings?|appointments?)\b/i, icon: "calendar-days" },
  { match: /\b(comments?|replies|feedback|reviews?|ratings?)\b/i, icon: "comments" },
  { match: /\b(settings?|preferences?|config|configuration|options?)\b/i, icon: "gear" },
  { match: /\b(security|privacy|password|locks?|secure)\b/i, icon: "shield-halved" },
  { match: /\b(api|integrations?|webhooks?|connections?)\b/i, icon: "plug" },
  { match: /\b(database|storage|backups?|servers?)\b/i, icon: "database" },
  { match: /\b(speed|performance|latency|response\s?time)\b/i, icon: "gauge-high" },
  { match: /\b(uptime|availability|status|health)\b/i, icon: "heart-pulse" },
  { match: /\b(errors?|bugs?|failures?|exceptions?|incidents?)\b/i, icon: "circle-exclamation" },
  { match: /\b(success|complete|done|approved)\b/i, icon: "circle-check" },
  { match: /\b(warnings?|caution)\b/i, icon: "triangle-exclamation" },
  { match: /\b(search|results?|queries)\b/i, icon: "magnifying-glass" },
  { match: /\b(downloads?|exports?)\b/i, icon: "download" },
  { match: /\b(uploads?|imports?)\b/i, icon: "upload" },
  { match: /\b(time|hours?|duration|elapsed)\b/i, icon: "clock" },
  { match: /\b(locations?|maps?|addresses?|countries?|regions?)\b/i, icon: "location-dot" },
  { match: /\b(stars?|favourites?|favorites?|highlights?)\b/i, icon: "star" },
  { match: /\b(trophy|awards?|achievements?|badges?|gold)\b/i, icon: "trophy" },
  { match: /\b(targets?|goals?|objectives?|quotas?)\b/i, icon: "bullseye" },
  { match: /\b(teams?|departments?|orgs?|organisations?|organizations?)\b/i, icon: "people-group" },
  { match: /\b(tickets?|issues?|bugs?|requests?)\b/i, icon: "ticket" },
  { match: /\b(deploys?|builds?|releases?|versions?)\b/i, icon: "rocket" }
];
function pickIconForLabel(label) {
  if (!label) return null;
  for (const rule of ICON_KEYWORD_RULES) {
    if (rule.match.test(label)) return rule.icon;
  }
  return null;
}
const TONE_ICONS = {
  default: "circle-info",
  info: "circle-info",
  primary: "bolt",
  success: "circle-check",
  warning: "triangle-exclamation",
  danger: "circle-exclamation",
  error: "circle-exclamation",
  neutral: "circle-info"
};
function pickIconForTone(tone) {
  if (!tone) return null;
  return TONE_ICONS[tone.toLowerCase()] ?? null;
}
function dicebearUrlFor(name, style = "shapes") {
  const seed = name.trim() || "anon";
  const safeStyle = /^[a-z0-9-]+$/i.test(style) ? style : "shapes";
  return `https://api.dicebear.com/9.x/${safeStyle}/svg?seed=${encodeURIComponent(seed)}`;
}
function withFieldShell(control, props, options = {}) {
  if (asBoolean(props.disabled)) control.setAttribute("disabled", "");
  const fieldName = asString(props.name);
  if (fieldName) control.setAttribute("name", fieldName);
  const label = asString(props.label);
  const hint = asString(props.hint);
  const error = asString(props.error);
  const warning = asString(props.warning);
  const description = asString(props.description);
  const required = asBoolean(props.required);
  const optionalText = required ? "" : props.optional === true ? "(optional)" : asString(props.optional);
  const invalid = asBoolean(props.invalid) || Boolean(error);
  if (invalid) control.setAttribute("aria-invalid", "true");
  const authorDescribedBy = asString(props.describedBy).split(/\s+/).filter(Boolean);
  if (authorDescribedBy.length > 0) mergeDescribedBy(control, authorDescribedBy);
  if (!label && !hint && !error && !required && !description && !warning && !optionalText) return control;
  const id = asString(props[options.idKey ?? "id"]) || asString(control.getAttribute("id"));
  if (required) control.setAttribute("required", "");
  if (error) control.setAttribute("aria-invalid", "true");
  const root = el("div", {
    class: "rui-field",
    "data-invalid": invalid ? "true" : null,
    "data-warning": !invalid && warning ? "true" : null
  });
  const requiredMark = () => el("span", { class: "rui-field-required", "aria-hidden": "true" }, ["*"]);
  const optionalMark = () => el("span", { class: "rui-field-optional" }, [optionalText]);
  const labelHidden = asBoolean(props.labelHidden);
  const labelClass = labelHidden ? "rui-field-label rui-visually-hidden" : "rui-field-label";
  const implicitLabel = !!label && !id;
  if (label && !implicitLabel) {
    const lab = el("label", { class: labelClass, for: id }, [label]);
    if (required) lab.append(requiredMark());
    else if (optionalText) lab.append(optionalMark());
    root.append(lab);
  }
  const describedBy = [];
  const descriptionNode = description ? el("p", { class: "rui-field-description", id: id ? `${id}-description` : null }, [description]) : null;
  if (descriptionNode && id) describedBy.push(descriptionNode.getAttribute("id"));
  const message = error ? el("div", { class: "rui-field-error", id: id ? `${id}-error` : null, role: "alert" }, [error]) : warning ? el("div", { class: "rui-field-warning", id: id ? `${id}-warning` : null, role: "status", "aria-live": "polite" }, [warning]) : hint ? el("div", { class: "rui-field-hint", id: id ? `${id}-hint` : null }, [hint]) : null;
  if (message && id) describedBy.push(message.getAttribute("id"));
  if (describedBy.length) mergeDescribedBy(control, describedBy);
  if (implicitLabel) {
    const lab = el("label", { class: `${labelClass}`.trim(), "data-implicit": "true" }, [label]);
    if (required) lab.append(requiredMark());
    else if (optionalText) lab.append(optionalMark());
    if (descriptionNode) lab.append(descriptionNode);
    lab.append(control);
    root.append(lab);
  } else {
    if (descriptionNode) root.append(descriptionNode);
    root.append(control);
  }
  if (message) {
    if (!id && hint && !error && !label) control.setAttribute("aria-label", hint);
    root.append(message);
  }
  return root;
}
function mergeDescribedBy(control, ids) {
  const existing = (control.getAttribute("aria-describedby") ?? "").split(/\s+/).filter(Boolean);
  const merged = [.../* @__PURE__ */ new Set([...existing, ...ids])];
  control.setAttribute("aria-describedby", merged.join(" "));
}
const FIELD_SHELL_PROPS = [
  { name: "disabled", type: "boolean", optional: true, description: "Disable the control (non-editable, skipped by tab order)" },
  { name: "label", type: "string", optional: true, description: "Field label rendered above the control" },
  { name: "hint", type: "string", optional: true, description: "Helper text rendered below the control" },
  { name: "error", type: "string", optional: true, description: "Validation error rendered below the control (marks it invalid). Takes the message slot ahead of `warning` and `hint`" },
  { name: "warning", type: "string", optional: true, description: "Cautionary note below the control for a value that is accepted but probably not what was meant. Announced politely and does NOT mark the field invalid; takes the message slot ahead of `hint`" },
  { name: "description", type: "string", optional: true, description: "Guidance rendered BETWEEN the label and the control — what to put in the field, as opposed to `hint`, which is a note about the value below it" },
  { name: "required", type: "boolean", optional: true, description: "Mark the field required (adds a `*` and the `required` attribute)" },
  { name: "optional", type: "boolean | string", optional: true, description: 'Mark the field optional in its label: `true` for "(optional)", or a string to word it another way (e.g. a translation). Ignored when `required` is set' },
  { name: "invalid", type: "boolean", optional: true, aliases: ["ariaInvalid"], description: "Mark the control invalid without supplying a message — for a field whose explanation lives outside it, e.g. in a `RequirementList` or a form-level summary" },
  { name: "describedBy", type: "string", optional: true, aliases: ["ariaDescribedBy"], description: "Space-separated ids of elements that describe this control, merged into its `aria-describedby` alongside the shell's own message" },
  { name: "onBlur", type: "callable", optional: true, aliases: ["onblur"], description: "Called with the current value when focus leaves the control (validate-on-blur, `form.touch`)" },
  { name: "onFocus", type: "callable", optional: true, aliases: ["onfocus"], description: "Called when the control gains focus" },
  { name: "name", type: "string", optional: true, description: "Form field name submitted to the server (defaults to `id`)" },
  { name: "labelHidden", type: "boolean", optional: true, description: "Keep the label in the accessibility tree but hide it visually — for a field whose purpose is already clear from context" }
];
const SHELL_LATER_PROP_NAMES = [
  "description",
  "warning",
  "optional",
  "invalid",
  "describedBy"
];
function fieldShellExtraProps(exclude = []) {
  return FIELD_SHELL_PROPS.filter(
    (prop2) => SHELL_LATER_PROP_NAMES.includes(prop2.name) && !exclude.includes(prop2.name)
  );
}
function attachFocusHandlers(control, props, helpers, getValue = (node) => node.value) {
  const chain = (key, handler) => {
    const existing = control[key];
    control[key] = (event) => {
      if (typeof existing === "function") existing.call(control, event);
      handler(event);
    };
  };
  if (props.onBlur != null) {
    chain("onblur", (event) => {
      const live = event.currentTarget ?? event.target;
      helpers.invoke(props.onBlur, getValue(live));
    });
  }
  if (props.onFocus != null) {
    chain("onfocus", (event) => {
      const live = event.currentTarget ?? event.target;
      helpers.invoke(props.onFocus, getValue(live));
    });
  }
}
const VALUE_KEYS = ["value", "id", "key", "code"];
const LABEL_KEYS = ["label", "name", "title", "text", "description"];
function extractComboboxItems(raw) {
  const items = asArray(raw);
  return items.map((entry) => {
    if (entry && typeof entry === "object") {
      const node = entry;
      if (node.__kind === "Component" && Array.isArray(node.args)) {
        const value2 = asString(node.args[0]);
        return {
          value: value2,
          label: asString(node.args[1], value2),
          disabled: asBoolean(node.args[2]) || void 0,
          group: asString(node.args[3]) || void 0
        };
      }
      const vKey = VALUE_KEYS.find((k) => node[k] !== void 0);
      const lKey = LABEL_KEYS.find((k) => node[k] !== void 0);
      if (vKey || lKey) {
        const value2 = asString(vKey ? node[vKey] : node[lKey]);
        return {
          value: value2,
          label: asString(lKey ? node[lKey] : void 0, value2),
          disabled: asBoolean(node.disabled) || void 0,
          group: asString(node.group) || void 0
        };
      }
      return null;
    }
    const value = asString(entry);
    return { value, label: value };
  }).filter((item) => item !== null && (item.value !== "" || item.label !== ""));
}
const BUTTON_VARIANTS = ["primary", "secondary", "outline", "ghost", "link", "danger", "default"];
const BUTTON_SIZES = ["xs", "sm", "md", "lg", "xl"];
const INPUT_TYPES = [
  "text",
  "email",
  "password",
  "number",
  "tel",
  "url",
  "date",
  "time",
  "datetime-local"
];
function normaliseButtonSize(value) {
  const v = asString(value).trim().toLowerCase();
  if (v === "xs" || v === "extra-small") return "xs";
  if (v === "sm" || v === "small") return "sm";
  if (v === "lg" || v === "large") return "lg";
  if (v === "xl" || v === "extra-large") return "xl";
  return "md";
}
function bindChangeHandler(element, props, helpers, options) {
  composeHandler(element, `on${options.event}`, (event) => {
    const handler = props[options.prop ?? "onChange"];
    if (handler == null) return;
    const live = event.currentTarget ?? event.target ?? element;
    helpers.invoke(handler, options.getValue(live));
  });
}
function composeHandler(element, propKey, extra) {
  const record = element;
  const previous = record[propKey];
  record[propKey] = (event) => {
    previous?.call(element, event);
    extra(event);
  };
}
function relocateControlAria(wrapper, control) {
  const supportsRequired = control instanceof HTMLInputElement || control instanceof HTMLSelectElement || control instanceof HTMLTextAreaElement;
  const supportsDisabled = supportsRequired || control instanceof HTMLButtonElement;
  for (const attr of ["disabled", "required", "aria-invalid", "aria-describedby"]) {
    const value = wrapper.getAttribute(attr);
    if (value === null) continue;
    wrapper.removeAttribute(attr);
    if (attr === "required" && !supportsRequired) {
      control.setAttribute("aria-required", "true");
      continue;
    }
    if (attr === "disabled" && !supportsDisabled) {
      control.setAttribute("aria-disabled", "true");
      continue;
    }
    control.setAttribute(attr, value);
  }
}
function fallbackFieldId(prefix, label) {
  const slug = label.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "");
  return slug ? `${prefix}-${slug}` : "";
}
function localeTag$1(value) {
  const tag = asString(value).trim();
  if (!tag) return void 0;
  try {
    return Intl.getCanonicalLocales(tag)[0];
  } catch {
    return void 0;
  }
}
function formatDateInLocale(iso, locale) {
  const match = /^(\d{4})-(\d{2})-(\d{2})/.exec(asString(iso).trim());
  if (!match) return "";
  const date = new Date(Number(match[1]), Number(match[2]) - 1, Number(match[3]));
  if (Number.isNaN(date.getTime()) || date.getDate() !== Number(match[3])) return "";
  return date.toLocaleDateString(locale);
}
function wireDateReadout(input, rootSelector, readoutSelector, text) {
  const update = (event) => {
    const target = event.currentTarget ?? event.target;
    const liveRoot = target?.closest(rootSelector);
    const readout = liveRoot?.querySelector(readoutSelector);
    if (liveRoot && readout) readout.textContent = text(liveRoot);
  };
  composeHandler(input, "oninput", update);
  composeHandler(input, "onchange", update);
}
const isComponentNodeLike = (value) => !!value && typeof value === "object" && value.__kind === "Component";
function extractOptionItems(raw) {
  const out = [];
  for (const entry of asArray(raw)) {
    if (entry && typeof entry === "object") {
      const node = entry;
      if (node.__kind === "Component" && Array.isArray(node.args)) {
        const value2 = asString(node.args[0]);
        out.push({
          value: value2,
          label: asString(node.args[1], value2),
          disabled: asBoolean(node.args[2]),
          group: asString(node.args[3])
        });
        continue;
      }
      if (node.value !== void 0 || node.label !== void 0) {
        const value2 = asString(node.value);
        out.push({
          value: value2,
          label: asString(node.label, value2),
          disabled: asBoolean(node.disabled),
          group: asString(node.group)
        });
        continue;
      }
    }
    const value = asString(entry);
    out.push({ value, label: value, disabled: false, group: "" });
  }
  return out.filter((item) => item.value !== "" || item.label !== "");
}
function extractCheckItem(entry, groupName, index) {
  const fallbackName = `${groupName}-${index}`;
  if (isComponentNodeLike(entry)) {
    const args = entry.args ?? [];
    const name = asString(args[1], fallbackName);
    return {
      label: asString(args[0]),
      name,
      description: asString(args[2]),
      checked: asBoolean(args[3]),
      disabled: asBoolean(args[4]),
      value: asString(args[5], name)
    };
  }
  if (entry && typeof entry === "object") {
    const obj = entry;
    const name = asString(obj.name, fallbackName);
    return {
      label: asString(obj.label),
      name,
      description: asString(obj.description),
      checked: asBoolean(obj.defaultChecked ?? obj.checked),
      disabled: asBoolean(obj.disabled),
      value: asString(obj.value, name)
    };
  }
  const label = asString(entry);
  return { label, name: label || fallbackName, description: "", checked: false, disabled: false, value: label || fallbackName };
}
function extractSliderMarks(raw, min, max) {
  const out = [];
  for (const entry of asArray(raw)) {
    if (entry && typeof entry === "object") {
      const obj = entry;
      const value2 = asNumber(obj.value, NaN);
      if (!Number.isFinite(value2)) continue;
      out.push({ value: value2, label: asString(obj.label) });
      continue;
    }
    const value = asNumber(entry, NaN);
    if (!Number.isFinite(value)) continue;
    out.push({ value, label: "" });
  }
  return out.filter((mark) => mark.value >= min && mark.value <= max);
}
function formatFileSize(bytes) {
  if (!Number.isFinite(bytes) || bytes <= 0) return "";
  const units = ["B", "KB", "MB", "GB"];
  let n = bytes;
  let i = 0;
  while (n >= 1024 && i < units.length - 1) {
    n /= 1024;
    i += 1;
  }
  return `${n >= 10 || i === 0 ? Math.round(n) : n.toFixed(1)} ${units[i]}`;
}
const Button = {
  name: "Button",
  description: 'Clickable button. `onClick` (a callable) runs when the user presses the button. The legacy `action` prop is an alias and is still accepted. Pass `href` to navigate instead (renders an `<a>` that keeps the button styling — combine with `variant: "link"` for "Read the docs" / "View invoice" links).',
  props: [
    { name: "label", type: "string" },
    { name: "onClick", type: "callable", optional: true, aliases: ["action", "onclick"], description: "Callable invoked when the button is clicked" },
    { name: "variant", type: "string", optional: true, aliases: ["tone"], enum: BUTTON_VARIANTS, description: "Visual style — `primary` solid brand button (`default` is its alias), `secondary` surface + border, `outline` transparent with primary border, `ghost` bare, `link` text-only (underlines on hover), `danger` destructive" },
    { name: "type", type: "string", optional: true, enum: ["button", "submit", "reset"], description: "HTML button type — `submit` submits the enclosing Form, `reset` clears it" },
    { name: "size", type: "string", optional: true, enum: BUTTON_SIZES, description: "Size token `xs|sm|md|lg|xl`" },
    { name: "icon", type: "string", optional: true, description: "Optional Font Awesome icon name" },
    { name: "iconPosition", type: "string", optional: true, enum: ["leading", "trailing"], description: "Icon placement (default leading)" },
    { name: "iconOnly", type: "boolean", optional: true, description: "Hide the label visually (keeps aria-label)" },
    { name: "loading", type: "boolean", optional: true, description: "Show a spinner, announce `aria-busy` and swallow clicks" },
    { name: "fullWidth", type: "boolean", optional: true },
    { name: "disabled", type: "boolean", optional: true },
    { name: "href", type: "string", optional: true, description: 'Navigate to this URL — renders an `<a class="rui-button">` instead of a `<button>`' }
  ],
  render: (_node, props, helpers) => {
    const loading = asBoolean(props.loading);
    const disabled = asBoolean(props.disabled);
    const iconOnly = asBoolean(props.iconOnly);
    const iconPosition = asString(props.iconPosition, "leading");
    const labelText = asString(props.label);
    const size2 = normaliseButtonSize(props.size);
    const rawHref = asString(props.href);
    const shell = {
      class: "rui-button",
      "data-variant": asString(props.variant, "primary"),
      "data-size": size2,
      "data-icon-position": iconPosition,
      "data-icon-only": iconOnly ? "true" : null,
      "data-full-width": asBoolean(props.fullWidth) ? "true" : null,
      "data-loading": loading ? "true" : null,
      "aria-label": iconOnly ? labelText : null,
      // A busy button must stay focusable so the screen-reader user who
      // activated it is still on it when the state is announced — hence
      // `aria-busy` + `aria-disabled` rather than the `disabled` attribute
      // (which also silently swallows the announcement).
      "aria-busy": loading ? "true" : null,
      "aria-disabled": loading && !disabled ? "true" : null
    };
    const button = rawHref ? el("a", { ...shell, href: sanitiseHref(rawHref) }) : el("button", {
      ...shell,
      type: asString(props.type, "button"),
      disabled: disabled ? "" : null
    });
    const labelSpan = el("span", { class: "rui-button-label" }, [labelText]);
    const iconNode = renderIcon(props.icon, { className: "rui-button-icon" });
    const spinNode = loading ? el("span", { class: "rui-spinner rui-button-spinner", "data-size": size2, "aria-hidden": "true" }, [
      el("span", { class: "rui-spinner-ring" })
    ]) : null;
    const adornment = spinNode ?? iconNode;
    if (iconOnly) {
      if (adornment) button.append(adornment);
    } else if (iconPosition === "trailing") {
      button.append(labelSpan);
      if (adornment) button.append(adornment);
    } else {
      if (adornment) button.append(adornment);
      button.append(labelSpan);
    }
    button.onclick = (event) => {
      if (loading || disabled) {
        event.preventDefault();
        return;
      }
      helpers.invoke(props.onClick);
    };
    return button;
  }
};
const Buttons = {
  name: "Buttons",
  description: "Group of buttons laid out horizontally or vertically.",
  props: [
    { name: "items", type: "Button[]" },
    { name: "direction", type: "string", optional: true, enum: ["row", "column"] }
  ],
  render: (_node, props, helpers) => {
    const root = el("div", {
      class: "rui-buttons",
      "data-direction": asString(props.direction, "row")
    });
    for (const child of asArray(props.items)) root.append(helpers.renderNode(child));
    return root;
  }
};
const ButtonGroup = {
  name: "ButtonGroup",
  description: "Row of buttons joined edge-to-edge into a single continuous control — only the outer corners are rounded and adjoining borders are shared. Use for related actions that form one unit (segmented actions, split/paired buttons, view switchers with real actions). For a single-select pill track use `SegmentedControl`; for spaced-out independent actions use `Buttons`.",
  props: [
    { name: "items", type: "Button[]", positional: true },
    { name: "size", type: "string", optional: true, enum: ["sm", "md", "lg"], description: "Size token applied to every button in the group" },
    { name: "fullWidth", type: "boolean", optional: true, aliases: ["full"], description: "Stretch the group to fill its container, dividing width evenly" },
    { name: "ariaLabel", type: "string", optional: true, aliases: ["ariaLabelledBy", "label"], description: 'Accessible name for the group (e.g. "Time range") — the group role is anonymous without it' }
  ],
  render: (_node, props, helpers) => {
    const size2 = asString(props.size);
    const root = el("div", {
      class: "rui-button-group",
      "data-size": size2 || "md",
      "data-full-width": asBoolean(props.fullWidth) ? "true" : null,
      role: "group",
      "aria-label": asString(props.ariaLabel) || null
    });
    const items = asArray(props.items);
    items.forEach((child, i) => {
      const node = helpers.renderNode(child);
      if (node instanceof HTMLElement) {
        const pos = items.length === 1 ? "only" : i === 0 ? "start" : i === items.length - 1 ? "end" : "middle";
        node.setAttribute("data-pos", pos);
        node.classList.add("rui-button-group-item");
        if (size2) node.setAttribute("data-size", size2);
      }
      root.append(node);
    });
    return root;
  }
};
const InputGroup = {
  name: "InputGroup",
  description: "Single field wrapped in a shared bordered shell with an optional leading `icon` and an optional trailing `action` node (button / IconButton / short text suffix). The focus ring is drawn around the whole composite. Use for search fields, password reveal, copy-to-clipboard rows, and unit-suffixed inputs.",
  props: [
    { name: "field", type: "Node", positional: true, description: "The Input/Select/etc. to wrap" },
    { name: "icon", type: "string", optional: true, description: "Leading Font Awesome icon name" },
    { name: "action", type: "Node", optional: true, aliases: ["trailing"], description: "Trailing action node (Button / IconButton)" },
    { name: "suffix", type: "string", optional: true, description: 'Short trailing text (e.g. a unit like "GB")' },
    ...FIELD_SHELL_PROPS
  ],
  render: (_node, props, helpers) => {
    const disabled = asBoolean(props.disabled);
    const error = asString(props.error);
    const invalid = asBoolean(props.invalid) || Boolean(error);
    const warning = asString(props.warning);
    const root = el("div", {
      class: "rui-input-group",
      // The group owns the border and the focus ring, so the invalid, warning and
      // disabled states have to be readable on the shell itself — the nested
      // field's own chrome is stripped by the theme and can never show them.
      //
      // All three, not just `error`: the shell's own state selectors reach
      // `.rui-input` / `.rui-select` / `.rui-textarea`, none of which is what
      // carries the border here — so a group that did not mirror them onto itself
      // signalled a bad value by message text alone.
      "data-disabled": disabled ? "true" : null,
      "data-invalid": invalid ? "true" : null,
      "data-warning": !invalid && warning ? "true" : null
    });
    const iconNode = renderIcon(props.icon, { className: "rui-input-group-icon" });
    if (iconNode) root.append(iconNode);
    if (props.field) {
      const body = el("div", { class: "rui-input-group-field" });
      body.append(helpers.renderNode(props.field));
      root.append(body);
    }
    const suffix = asString(props.suffix);
    if (suffix) root.append(el("span", { class: "rui-input-group-suffix" }, [suffix]));
    if (props.action) {
      const act = el("div", { class: "rui-input-group-action" });
      act.append(helpers.renderNode(props.action));
      root.append(act);
    }
    const control = root.querySelector(
      ".rui-input-group-field input, .rui-input-group-field select, .rui-input-group-field textarea, .rui-input-group-field .rui-combobox-trigger"
    );
    if (control) {
      if (disabled) control.setAttribute("disabled", "");
      attachFocusHandlers(control, props, helpers);
      const controlId = control.getAttribute("id");
      if (controlId) root.setAttribute("id", `${controlId}-group`);
    }
    const shell = withFieldShell(root, props);
    if (control) {
      relocateControlAria(root, control);
      const controlId = control.getAttribute("id");
      const labelEl = shell === root ? null : shell.querySelector(".rui-field-label");
      if (labelEl && controlId) labelEl.setAttribute("for", controlId);
    }
    return shell;
  }
};
const Input = {
  name: "Input",
  description: "Text input field. Pass a $variable as `value` for two-way binding. `onChange(value)` fires on every keystroke with the current string. Pass `label`/`hint`/`error`/`required` to render a labelled field shell with validation messaging. Use `autocomplete` on sign-in and address fields so password managers and browser autofill work, and `readOnly` for a locked-but-selectable value.",
  props: [
    { name: "id", type: "string", description: "Input identifier" },
    { name: "placeholder", type: "string", optional: true },
    { name: "type", type: "string", optional: true, enum: INPUT_TYPES },
    { name: "validations", type: "any", optional: true, description: "Array or object of validation hints (`required`, `minLength:n`, `maxLength:n`, `pattern:re`, `email`)" },
    { name: "value", type: "any", optional: true, description: "Bound value (typically $variable)" },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"], description: "Called with the current value on every keystroke" },
    ...FIELD_SHELL_PROPS,
    { name: "readOnly", type: "boolean", optional: true, aliases: ["readonly"], description: "Value is visible and selectable but not editable (unlike `disabled`, it stays in tab order and is submitted)" },
    { name: "autocomplete", type: "string", optional: true, aliases: ["autoComplete"], description: "Native autofill hint (e.g. `email`, `current-password`, `postal-code`)" },
    { name: "maxLength", type: "number", optional: true, aliases: ["maxlength"], description: "Maximum number of characters accepted" }
  ],
  render: (node, props, helpers) => {
    const explicitType = asString(props.type);
    const maxLength = Math.floor(asNumber(props.maxLength, 0));
    const input = el("input", {
      class: "rui-input",
      id: asString(props.id),
      name: asString(props.name, asString(props.id)),
      type: explicitType || "text",
      placeholder: asString(props.placeholder),
      // `valueAttr`, not `asString`: an absent attribute tells the morph
      // reconciler this render asserts no value, so an onChange-only field is
      // not wiped on every unrelated re-render.
      value: valueAttr(props.value),
      readonly: asBoolean(props.readOnly) ? "" : null,
      autocomplete: asString(props.autocomplete) || null,
      maxlength: maxLength > 0 ? String(maxLength) : null
    });
    bindToStateAtArg(input, node, 4, helpers);
    bindChangeHandler(input, props, helpers, {
      event: "input",
      getValue: (n) => n.value
    });
    attachFocusHandlers(input, props, helpers);
    applyValidations(input, props.validations, explicitType !== "");
    return withFieldShell(input, props);
  }
};
const TextArea = {
  name: "TextArea",
  description: 'Multi-line text input. `onChange(value)` fires on every keystroke with the current text. Pass `label`/`hint`/`error`/`required` for a labelled field shell, `maxLength` to cap the length (drives a "120/280" counter), and `autoResize` for a composer that grows with its content.',
  props: [
    { name: "id", type: "string" },
    { name: "placeholder", type: "string", optional: true },
    { name: "rows", type: "number", optional: true },
    { name: "value", type: "any", optional: true },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"], description: "Called with the current value on every keystroke" },
    ...FIELD_SHELL_PROPS,
    { name: "maxLength", type: "number", optional: true, aliases: ["maxlength"], description: "Maximum number of characters accepted" },
    { name: "readOnly", type: "boolean", optional: true, aliases: ["readonly"], description: "Content is scrollable and selectable but not editable (stays in tab order, unlike `disabled`)" },
    { name: "autoResize", type: "boolean", optional: true, description: "Grow the box to fit its content instead of showing an inner scrollbar" }
  ],
  render: (node, props, helpers) => {
    const text = asString(props.value);
    const autoResize = asBoolean(props.autoResize);
    const baseRows = Number(props.rows ?? 4) || 4;
    const rows = autoResize ? Math.max(baseRows, text.split("\n").length) : baseRows;
    const maxLength = Math.floor(asNumber(props.maxLength, 0));
    const textarea = el("textarea", {
      class: "rui-textarea",
      id: asString(props.id),
      name: asString(props.id),
      placeholder: asString(props.placeholder),
      rows: String(rows),
      maxlength: maxLength > 0 ? String(maxLength) : null,
      readonly: asBoolean(props.readOnly) ? "" : null,
      "data-auto-resize": autoResize ? "true" : null
    });
    textarea.value = text;
    bindToStateAtArg(textarea, node, 3, helpers);
    bindChangeHandler(textarea, props, helpers, {
      event: "input",
      getValue: (n) => n.value
    });
    if (autoResize) {
      const fit = (target) => {
        target.style.height = "auto";
        target.style.height = `${target.scrollHeight}px`;
      };
      composeHandler(textarea, "oninput", (event) => {
        const live = event.currentTarget ?? event.target;
        if (live) fit(live);
      });
      const cancel = deferToPaint(() => {
        if (textarea.isConnected) fit(textarea);
      });
      helpers.registerDisposer(cancel, "textarea-autoresize");
    }
    attachFocusHandlers(textarea, props, helpers, (n) => n.value);
    return withFieldShell(textarea, props);
  }
};
const SelectItem = {
  name: "SelectItem",
  description: 'Single option for a Select/Radio/Combobox list. Set `disabled` for an option that must stay visible but unselectable ("Out of stock", "Enterprise plan — upgrade required"), and `group` to bucket long lists under `<optgroup>` headings.',
  props: [
    { name: "value", type: "string" },
    { name: "label", type: "string" },
    { name: "disabled", type: "boolean", optional: true, description: "Render the option greyed out and unselectable" },
    { name: "group", type: "string", optional: true, description: "Optgroup heading this option belongs to" }
  ],
  render: (_node, props) => {
    return el("option", {
      value: asString(props.value),
      disabled: asBoolean(props.disabled) ? "" : null,
      // Read back by Select to bucket the option under an `<optgroup>`.
      "data-group": asString(props.group) || null
    }, [asString(props.label)]);
  }
};
const Select = {
  name: "Select",
  description: "Dropdown select. Pass a `$variable` as `value` for two-way binding. Set `searchable: true` for a combobox-style filter UI on long option lists, or pass `onSearch` (which implies it) to fetch the matches from the server as the user types. `onChange(value)` fires with the newly-selected value. `items` accepts `SelectItem(value, label)` nodes, `{value, label}` objects and bare strings.",
  props: [
    { name: "id", type: "string" },
    { name: "items", type: "SelectItem[]", description: "Options; SelectItem(value, label) nodes, {value, label} objects or bare strings" },
    { name: "label", type: "string", optional: true },
    { name: "placeholder", type: "string", optional: true },
    { name: "value", type: "any", optional: true },
    { name: "searchable", type: "boolean", optional: true, description: "Render as a filterable combobox" },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"], description: "Called with the newly-selected value" },
    { name: "hint", type: "string", optional: true, description: "Helper text rendered below the control" },
    { name: "error", type: "string", optional: true, description: "Validation error rendered below the control (marks it invalid)" },
    { name: "required", type: "boolean", optional: true, description: "Mark the field required" },
    { name: "disabled", type: "boolean", optional: true, description: "Disable the control (non-editable, skipped by tab order)" },
    { name: "onBlur", type: "callable", optional: true, aliases: ["onblur"], description: "Called with the current value when focus leaves the control (validate-on-blur)" },
    { name: "onFocus", type: "callable", optional: true, aliases: ["onfocus"], description: "Called when the control gains focus" },
    { name: "loading", type: "boolean", optional: true, description: "Options are still being fetched — disables the control and shows a loading option instead of an empty list" },
    { name: "onSearch", type: "callable", optional: true, description: "Called with the query ~200ms after typing stops, for server-side search (implies `searchable`; supply the matches as `items`)" },
    { name: "labelHidden", type: "boolean", optional: true, description: "Keep the label in the accessibility tree but hide it visually — for a field whose purpose is already clear from context (a picker under a section heading that names it, a control in a table cell whose column header is the label)" },
    // Appended, not inserted: `value` is read from positional slot 4 by
    // `bindToStateAtArg`, so anything placed ahead of it silently breaks
    // two-way binding at every existing call site.
    { name: "emptyLabel", type: "string", optional: true, description: 'Shown in place of the options when there are none — the difference between "this list is genuinely empty" and "something failed to load". Defaults to "No options"' },
    ...fieldShellExtraProps()
  ],
  render: (node, props, helpers) => {
    if (asBoolean(props.searchable) || props.onSearch != null) {
      return renderSearchableSelect(node, props, helpers);
    }
    const loading = asBoolean(props.loading);
    const select = el("select", {
      class: "rui-select",
      id: asString(props.id),
      name: asString(props.id),
      "data-loading": loading ? "true" : null,
      // An empty dropdown is indistinguishable from "no results", so a loading
      // select locks itself rather than inviting a pick from nothing.
      disabled: loading ? "" : null
    });
    const placeholder = asString(props.placeholder);
    if (loading) {
      select.append(el("option", { value: "", disabled: "", selected: "" }, ["Loading…"]));
    } else if (placeholder) {
      select.append(el("option", { value: "", disabled: "", selected: "" }, [placeholder]));
    }
    const groups = /* @__PURE__ */ new Map();
    let optionCount = 0;
    const placeOption = (option) => {
      optionCount += 1;
      const group = option.getAttribute("data-group");
      if (!group) {
        select.append(option);
        return;
      }
      let bucket = groups.get(group);
      if (!bucket) {
        bucket = el("optgroup", { label: group });
        groups.set(group, bucket);
        select.append(bucket);
      }
      bucket.append(option);
    };
    for (const entry of asArray(props.items)) {
      if (isComponentNodeLike(entry)) {
        const rendered = helpers.renderNode(entry);
        if (rendered instanceof HTMLElement) placeOption(rendered);
        else select.append(rendered);
        continue;
      }
      for (const item of extractOptionItems([entry])) {
        placeOption(el("option", {
          value: item.value,
          disabled: item.disabled ? "" : null,
          "data-group": item.group || null
        }, [item.label]));
      }
    }
    if (optionCount === 0 && !loading) {
      select.append(el("option", { value: "", disabled: "", selected: "" }, [
        asString(props.emptyLabel) || "No options"
      ]));
    }
    select.value = asString(props.value);
    bindToStateAtArg(select, node, 4, helpers);
    bindChangeHandler(select, props, helpers, {
      event: "change",
      getValue: (n) => n.value
    });
    attachFocusHandlers(select, props, helpers, (n) => n.value);
    return withFieldShell(select, props);
  }
};
const Checkbox = {
  name: "Checkbox",
  description: 'Boolean checkbox. `onChange(checked)` fires with the new boolean state. Pass `required`/`error` for the "I accept the Terms" pattern, `description` for a secondary line, and `indeterminate` for a "select all" header over a partially-selected list.',
  props: [
    { name: "id", type: "string" },
    { name: "label", type: "string" },
    { name: "value", type: "boolean", optional: true, aliases: ["checked"] },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"], description: "Called with the new boolean value" },
    { name: "disabled", type: "boolean", optional: true, description: "Disable the control (non-editable, skipped by tab order)" },
    { name: "description", type: "string", optional: true, description: 'Secondary line under the label (e.g. "Sent every Monday at 9am")' },
    { name: "required", type: "boolean", optional: true, description: "Mark the checkbox required (native validation blocks submit until ticked)" },
    { name: "error", type: "string", optional: true, description: "Validation error rendered below the control (marks it invalid)" },
    { name: "hint", type: "string", optional: true, description: "Helper text rendered below the control" },
    { name: "indeterminate", type: "boolean", optional: true, description: 'Tri-state "partially checked" dash (a parent of a partly-selected list)' },
    { name: "labelHidden", type: "boolean", optional: true, description: "Keep the label in the accessibility tree but hide it visually — for a checkbox in a table cell whose column header already carries the name" },
    ...fieldShellExtraProps(["description"])
  ],
  render: (node, props, helpers) => {
    const disabled = asBoolean(props.disabled);
    const indeterminate = asBoolean(props.indeterminate);
    const description = asString(props.description);
    const wrapper = el("label", {
      class: "rui-checkbox",
      "data-disabled": disabled ? "true" : null,
      "data-has-description": description ? "true" : null
    });
    const isChecked = asBoolean(props.value);
    const input = el("input", {
      type: "checkbox",
      id: asString(props.id),
      name: asString(props.id),
      checked: isChecked ? "" : null,
      disabled: disabled ? "" : null,
      // The DOM property below is the thing browsers paint; the attribute is
      // what survives a morph pass, and it is what the deferred sync reads.
      "data-indeterminate": indeterminate ? "true" : null
    });
    input.checked = isChecked;
    input.indeterminate = indeterminate;
    const liveInput = helpers.useInstanceState("live-input", null);
    const cancelSync = deferToPaint(() => {
      const live = input.isConnected ? input : liveInput.get();
      if (!live?.isConnected) return;
      liveInput.set(live);
      live.indeterminate = indeterminate;
    });
    helpers.registerDisposer(cancelSync, "checkbox-indeterminate");
    bindToStateAtArg(input, node, 2, helpers);
    bindChangeHandler(input, props, helpers, {
      event: "change",
      getValue: (n) => n.checked
    });
    const labelSpan = el("span", {
      class: asBoolean(props.labelHidden) ? "rui-checkbox-label rui-visually-hidden" : "rui-checkbox-label"
    }, [asString(props.label)]);
    if (description) {
      const stack = el("span", { class: "rui-checkbox-item-text" });
      stack.append(labelSpan, el("span", { class: "rui-checkbox-item-description" }, [description]));
      wrapper.append(input, stack);
    } else {
      wrapper.append(input, labelSpan);
    }
    const shell = withFieldShell(wrapper, { ...props, label: null, description: null });
    relocateControlAria(wrapper, input);
    return shell;
  }
};
const CheckBoxItem = {
  name: "CheckBoxItem",
  description: "Single option inside a CheckBoxGroup. `disabled` locks one option (a scope the current plan tier does not include) while the rest stay editable; `value` is the string submitted for the option when it differs from its `name` key.",
  props: [
    { name: "label", type: "string" },
    { name: "name", type: "string", description: "Key inside the group's value object" },
    { name: "description", type: "string", optional: true },
    { name: "defaultChecked", type: "boolean", optional: true, aliases: ["checked"] },
    { name: "disabled", type: "boolean", optional: true, description: "Lock this option (greyed out, not togglable)" },
    { name: "value", type: "string", optional: true, description: "Submitted value for this option (defaults to `name`) — use when the group maps to an array of ids" }
  ],
  render: (_node, props) => {
    const itemName = asString(props.name);
    const label = asString(props.label);
    const description = asString(props.description);
    const isChecked = asBoolean(props.defaultChecked);
    const disabled = asBoolean(props.disabled);
    const wrapper = el("label", {
      class: "rui-checkbox-item",
      "data-name": itemName,
      "data-disabled": disabled ? "true" : null
    });
    const input = el("input", {
      type: "checkbox",
      name: itemName,
      value: asString(props.value, itemName),
      checked: isChecked ? "" : null,
      disabled: disabled ? "" : null
    });
    input.checked = isChecked;
    const text = el("div", { class: "rui-checkbox-item-text" });
    text.append(el("div", { class: "rui-checkbox-item-label" }, [label]));
    if (description) text.append(el("div", { class: "rui-checkbox-item-description" }, [description]));
    wrapper.append(input, text);
    return wrapper;
  }
};
const CheckBoxGroup = {
  name: "CheckBoxGroup",
  description: 'Group of checkboxes. Value is an object keyed by item name. Pass a `$variable` for two-way binding. `onChange(value)` fires with the full updated object. `items` accepts `CheckBoxItem(label, name, …)` nodes and plain `{label, name, description, checked, disabled}` objects. Pass `label` for the group heading ("Permissions") and `error` for "Select at least one scope".',
  props: [
    { name: "name", type: "string", description: "Group identifier" },
    { name: "items", type: "CheckBoxItem[]" },
    { name: "value", type: "any", optional: true, description: "Bound value (typically $variable)" },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"], description: "Called with the updated `{name: checked}` object when any item toggles" },
    { name: "label", type: "string", optional: true, description: "Group heading — also becomes the group's accessible name" },
    { name: "hint", type: "string", optional: true, description: "Helper text rendered below the group" },
    { name: "error", type: "string", optional: true, description: "Validation error rendered below the group (marks it invalid)" },
    { name: "required", type: "boolean", optional: true, description: "Mark the group required (adds a `*` to the heading)" },
    { name: "disabled", type: "boolean", optional: true, description: "Lock every item in the group" },
    { name: "labelHidden", type: "boolean", optional: true, description: "Keep the label in the accessibility tree but hide it visually — for a field whose purpose is already clear from context (a picker under a section heading that names it, a control in a table cell whose column header is the label)" },
    ...fieldShellExtraProps()
  ],
  render: (node, props, helpers) => {
    const groupName = asString(props.name);
    const groupDisabled = asBoolean(props.disabled);
    const groupLabel = asString(props.label);
    const root = el("div", {
      class: "rui-checkbox-group",
      role: "group",
      id: groupName ? `${groupName}-group` : null,
      "data-name": groupName,
      "data-disabled": groupDisabled ? "true" : null,
      // A bare `role="group"` announces nothing; replaced by `aria-labelledby`
      // below when the shell renders the visible heading.
      "aria-label": groupLabel || null
    });
    const valueObject = props.value && typeof props.value === "object" ? props.value : {};
    asArray(props.items).forEach((entry, idx) => {
      const item = extractCheckItem(entry, groupName, idx);
      const rendered = isComponentNodeLike(entry) ? helpers.renderNode(entry) : CheckBoxItem.render(entry, {
        label: item.label,
        name: item.name,
        description: item.description,
        defaultChecked: item.checked,
        disabled: item.disabled,
        value: item.value
      }, helpers);
      if (!(rendered instanceof HTMLElement)) {
        root.append(rendered);
        return;
      }
      const input = rendered.querySelector('input[type="checkbox"]');
      if (input) {
        const itemName = input.getAttribute("name") || item.name;
        const id = `${groupName}-${itemName}`;
        input.setAttribute("name", itemName);
        input.setAttribute("id", id);
        rendered.setAttribute("for", id);
        rendered.setAttribute("data-name", itemName);
        const isChecked = itemName in valueObject ? Boolean(valueObject[itemName]) : input.checked;
        input.checked = isChecked;
        if (isChecked) input.setAttribute("checked", "");
        else input.removeAttribute("checked");
        if (groupDisabled) {
          input.disabled = true;
          input.setAttribute("disabled", "");
          rendered.setAttribute("data-disabled", "true");
        }
      }
      root.append(rendered);
    });
    const stateName = node.argMeta?.[2]?.stateRef;
    const readGroupValue = (rootEl) => {
      const out = {};
      rootEl.querySelectorAll('input[type="checkbox"]').forEach((input) => {
        out[input.name] = input.checked;
      });
      return out;
    };
    if (stateName) {
      helpers.bindState(root, stateName, {
        event: "change",
        // Read from the *live* DOM rooted at the event target, never from
        // the closure's `inputs` array. After a morph re-render those
        // captured input elements are detached and report stale `checked`.
        getValue: readGroupValue
      });
    }
    bindChangeHandler(root, props, helpers, {
      event: "change",
      getValue: (n) => readGroupValue(n)
    });
    const shell = withFieldShell(root, props);
    if (shell !== root) {
      const labelEl = shell.querySelector(".rui-field-label");
      if (labelEl && groupName) {
        labelEl.removeAttribute("for");
        const labelId = `${groupName}-group-label`;
        labelEl.setAttribute("id", labelId);
        root.setAttribute("aria-labelledby", labelId);
        root.removeAttribute("aria-label");
      }
    }
    return shell;
  }
};
const Radio = {
  name: "Radio",
  description: 'Radio button group. `onChange(value)` fires with the newly-selected option value. Always pass `label` — the question being answered ("Shipping method") — and use `direction: "row"` for a Yes/No or Monthly/Yearly pair. `items` accepts `SelectItem(value, label)` nodes, `{value, label, disabled}` objects and bare strings. Pass `slots` to hang a control off an option\'s own row ("Fixed  [– 3 +]", "Card  [number]") — one entry per item, aligned by index; the slot renders OUTSIDE the `<label>`, so clicking the control does not select the option.',
  props: [
    { name: "id", type: "string" },
    { name: "items", type: "SelectItem[]", description: "Options; SelectItem(value, label) nodes, {value, label, disabled} objects or bare strings" },
    { name: "value", type: "any", optional: true },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"], description: "Called with the newly-selected radio value" },
    { name: "label", type: "string", optional: true, description: "The question the group answers — also its accessible name" },
    { name: "hint", type: "string", optional: true, description: "Helper text rendered below the group" },
    { name: "error", type: "string", optional: true, description: "Validation error rendered below the group (marks it invalid)" },
    { name: "required", type: "boolean", optional: true, description: "Mark the group required (adds a `*` to the label)" },
    { name: "disabled", type: "boolean", optional: true, description: "Lock every option in the group" },
    { name: "direction", type: "string", optional: true, enum: ["row", "column"], description: "Layout of the options (default `column`)" },
    { name: "labelHidden", type: "boolean", optional: true, description: "Keep the label in the accessibility tree but hide it visually — for a field whose purpose is already clear from context (a picker under a section heading that names it, a control in a table cell whose column header is the label)" },
    // Declared last: the controlled `value` channel reads its state ref from
    // argMeta slot 2, so nothing may be inserted ahead of it.
    { name: "slots", type: "Node[]", optional: true, description: "Per-option trailing content, aligned by index with `items` — rendered beside the option, outside its `<label>`, so a control in the slot stays independently clickable. Use `null` for an option that has none." },
    ...fieldShellExtraProps()
  ],
  render: (node, props, helpers) => {
    const groupName = asString(props.id);
    const groupLabel = asString(props.label);
    const groupDisabled = asBoolean(props.disabled);
    const isRow = asString(props.direction, "column") === "row";
    const slots = props.slots === void 0 || props.slots === null ? null : asArray(props.slots);
    const root = el("div", {
      class: "rui-radio-group",
      role: "radiogroup",
      id: groupName ? `${groupName}-group` : null,
      "data-direction": isRow ? "row" : "column",
      "data-slots": slots ? "true" : null,
      "data-disabled": groupDisabled ? "true" : null,
      // Named here so the group is not announced as an anonymous "radio group";
      // swapped for `aria-labelledby` when the shell renders a visible label.
      "aria-label": groupLabel || null,
      // The stylesheet hard-codes a column, so the inline override is what
      // actually makes `direction: "row"` work today.
      style: isRow ? "flex-direction: row; flex-wrap: wrap" : null
    });
    const current = asString(props.value);
    extractOptionItems(props.items).forEach((item, idx) => {
      const id = `${groupName}-${item.value || idx}`;
      const itemDisabled2 = groupDisabled || item.disabled;
      const itemRoot = el("label", {
        class: "rui-radio",
        for: id,
        "data-disabled": itemDisabled2 ? "true" : null
      });
      const isChecked = current !== "" && current === item.value;
      const input = el("input", {
        type: "radio",
        id,
        name: groupName,
        value: item.value,
        checked: isChecked ? "" : null,
        disabled: itemDisabled2 ? "" : null
      });
      input.checked = isChecked;
      itemRoot.append(input, el("span", { class: "rui-radio-label" }, [item.label]));
      if (!slots) {
        root.append(itemRoot);
        return;
      }
      const row = el("div", { class: "rui-radio-row" }, [itemRoot]);
      const slot = slots[idx];
      if (slot !== void 0 && slot !== null && slot !== false && slot !== "") {
        row.append(el("div", { class: "rui-radio-slot" }, [helpers.renderNode(slot)]));
      }
      root.append(row);
    });
    const stateName = node.argMeta?.[2]?.stateRef;
    if (stateName) {
      helpers.bindState(root, stateName, { event: "change", getValue: readRadioValue });
    }
    bindChangeHandler(root, props, helpers, {
      event: "change",
      getValue: (n) => readRadioValue(n)
    });
    const shell = withFieldShell(root, props);
    if (shell !== root) {
      const labelEl = shell.querySelector(".rui-field-label");
      if (labelEl && groupName) {
        labelEl.removeAttribute("for");
        const labelId = `${groupName}-group-label`;
        labelEl.setAttribute("id", labelId);
        root.setAttribute("aria-labelledby", labelId);
        root.removeAttribute("aria-label");
      }
    }
    return shell;
  }
};
function readRadioValue(scope) {
  if (scope instanceof HTMLInputElement) return scope.value;
  return scope.querySelector('input[type="radio"]:checked')?.value ?? "";
}
const FormControl = {
  name: "FormControl",
  description: "Labeled wrapper around a single form field. The label is associated with the nested control automatically (override with `for`), and `error`/`required` give a validation slot to fields that have none of their own (Checkbox, Radio, CheckBoxGroup).",
  props: [
    { name: "label", type: "string" },
    { name: "field", type: "Node", aliases: ["control"] },
    { name: "hint", type: "string", optional: true },
    { name: "error", type: "string", optional: true, description: "Validation error rendered below the field (marks the control invalid)" },
    { name: "required", type: "boolean", optional: true, description: "Mark the field required (adds a `*` and the `required` attribute)" },
    { name: "for", type: "string", optional: true, aliases: ["htmlFor"], description: "Id of the control the label points at — only needed when the nested field has no `id` of its own" },
    // The same five the field shell grew, implemented here rather than spread:
    // this component wires its own label/message and does not call
    // `withFieldShell`, so a spread would declare props nothing reads.
    ...fieldShellExtraProps()
  ],
  render: (_node, props, helpers) => {
    const labelText = asString(props.label);
    const hint = asString(props.hint);
    const error = asString(props.error);
    const warning = asString(props.warning);
    const descriptionNode = isComponentNodeLike(props.description) ? helpers.renderNode(props.description) : null;
    const description = descriptionNode ? "" : asString(props.description);
    const required = asBoolean(props.required);
    const invalid = asBoolean(props.invalid) || Boolean(error);
    const optionalText = required ? "" : props.optional === true ? "(optional)" : asString(props.optional);
    const root = el("div", {
      class: "rui-form-control",
      "data-invalid": invalid ? "true" : null,
      "data-warning": !invalid && warning ? "true" : null
    });
    const fieldEl = helpers.renderNode(props.field);
    const control = fieldEl instanceof HTMLElement ? fieldEl.matches("input, select, textarea") ? fieldEl : fieldEl.querySelector("input, select, textarea, .rui-combobox-trigger, .rui-multiselect-trigger") : null;
    const controlId = asString(props.for) || control?.getAttribute("id") || "";
    const labelEl = el("label", { class: "rui-form-label", for: controlId || null }, [labelText]);
    if (required) labelEl.append(el("span", { class: "rui-field-required", "aria-hidden": "true" }, ["*"]));
    else if (optionalText) labelEl.append(el("span", { class: "rui-field-optional" }, [optionalText]));
    root.append(labelEl);
    const describedByIds = [];
    if (description || descriptionNode) {
      const descriptionId = controlId ? `${controlId}-description` : "";
      const wrap = el(descriptionNode ? "div" : "p", {
        class: "rui-field-description",
        id: descriptionId || null
      });
      if (descriptionNode) wrap.append(descriptionNode);
      else wrap.append(document.createTextNode(description));
      root.append(wrap);
      if (descriptionId) describedByIds.push(descriptionId);
    }
    root.append(fieldEl);
    const message = error ? { node: el("div", { class: "rui-field-error", id: controlId ? `${controlId}-error` : null, role: "alert" }, [error]), id: controlId ? `${controlId}-error` : "" } : warning ? { node: el("div", { class: "rui-field-warning", id: controlId ? `${controlId}-warning` : null, role: "status", "aria-live": "polite" }, [warning]), id: controlId ? `${controlId}-warning` : "" } : hint ? { node: el("p", { class: "rui-form-hint", id: controlId ? `${controlId}-hint` : null }, [hint]), id: controlId ? `${controlId}-hint` : "" } : null;
    if (message) {
      root.append(message.node);
      if (message.id) describedByIds.push(message.id);
    }
    if (control) {
      if (required) control.setAttribute("required", "");
      if (invalid) control.setAttribute("aria-invalid", "true");
      const authorIds = asString(props.describedBy).split(/\s+/).filter(Boolean);
      const allIds = [...describedByIds, ...authorIds];
      if (allIds.length > 0) {
        const existing = (control.getAttribute("aria-describedby") ?? "").split(/\s+/).filter(Boolean);
        control.setAttribute("aria-describedby", [.../* @__PURE__ */ new Set([...existing, ...allIds])].join(" "));
      }
      if (!controlId && labelText) control.setAttribute("aria-label", labelText);
      else if (controlId && labelText && !control.matches("input, select, textarea, button")) {
        labelEl.setAttribute("id", `${controlId}-label`);
        control.setAttribute("aria-labelledby", `${controlId}-label`);
      }
    }
    return root;
  }
};
const SearchBar = {
  name: "SearchBar",
  description: "Pre-styled search input with a leading magnifying-glass icon, optional trailing submit button, and optional keyboard-shortcut hint. Pass a `$variable` as `value` for two-way binding. Use anywhere a user needs to filter content — toolbars, command bars, lists, headers.",
  props: [
    { name: "id", type: "string" },
    { name: "placeholder", type: "string", optional: true },
    { name: "value", type: "string", optional: true, description: "Bound value (typically $variable)" },
    { name: "shortcut", type: "string", optional: true, description: 'Keyboard hint chip on the right (e.g. "/")' },
    { name: "onSubmit", type: "callable", optional: true, aliases: ["action", "onClick"], description: "Optional submit callable; clicking the trailing button or pressing Enter invokes it" },
    { name: "submitLabel", type: "string", optional: true, description: 'Label for the trailing submit button (default "Search"). Omitted when no action is provided.' },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"], description: "Called with the current query on every keystroke" },
    { name: "clearable", type: "boolean", optional: true, description: "Show a clear (×) button while the query is non-empty (default true)" },
    { name: "onClear", type: "callable", optional: true, description: "Called after the query is cleared — the bound `$variable` is emptied for you" },
    { name: "disabled", type: "boolean", optional: true, description: "Lock the field (e.g. while results are loading or the dataset is unavailable)" },
    { name: "loading", type: "boolean", optional: true, description: "Swap the magnifying glass for a spinner while a search is in flight" },
    { name: "ariaLabel", type: "string", optional: true, description: 'Accessible name for the field (defaults to the placeholder, then "Search")' }
  ],
  render: (node, props, helpers) => {
    const disabled = asBoolean(props.disabled);
    const loading = asBoolean(props.loading);
    const value = asString(props.value);
    const placeholder = asString(props.placeholder, "Search…");
    const root = el("form", {
      class: "rui-search-bar",
      role: "search",
      "data-disabled": disabled ? "true" : null,
      "data-loading": loading ? "true" : null
    });
    root.onsubmit = (event) => {
      event.preventDefault();
      if (disabled) return;
      helpers.invoke(props.onSubmit);
    };
    const iconWrap = loading ? el("span", { class: "rui-search-bar-icon rui-spinner", "aria-hidden": "true" }, [
      el("span", { class: "rui-spinner-ring" })
    ]) : renderIcon("magnifying-glass", { className: "rui-search-bar-icon" }) ?? el("span", { class: "rui-search-bar-icon", "aria-hidden": "true" });
    root.append(iconWrap);
    const input = el("input", {
      class: "rui-search-bar-input",
      id: asString(props.id),
      name: asString(props.id),
      type: "search",
      placeholder,
      value: valueAttr(props.value),
      autocomplete: "off",
      disabled: disabled ? "" : null,
      // A placeholder is not a label: it is not exposed as a name by all AT and
      // it disappears the moment the user types.
      "aria-label": asString(props.ariaLabel) || placeholder || "Search"
    });
    bindToStateAtArg(input, node, 2, helpers);
    bindChangeHandler(input, props, helpers, {
      event: "input",
      getValue: (n) => n.value
    });
    root.append(input);
    if (asBoolean(props.clearable, true) && value !== "" && !disabled) {
      const clearBtn = el("button", {
        type: "button",
        class: "rui-search-bar-clear",
        "aria-label": "Clear search"
      }, ["×"]);
      clearBtn.onclick = (event) => {
        const origin = event.currentTarget ?? event.target;
        const live = origin.closest(".rui-search-bar")?.querySelector(".rui-search-bar-input");
        if (live) {
          live.value = "";
          live.dispatchEvent(new Event("input", { bubbles: true }));
          live.focus();
        }
        helpers.invoke(props.onClear);
      };
      root.append(clearBtn);
    }
    const shortcut = asString(props.shortcut);
    if (shortcut) root.append(el("span", { class: "rui-search-bar-shortcut" }, [shortcut]));
    if (props.onSubmit != null) {
      const btn = el("button", {
        type: "submit",
        class: "rui-search-bar-submit",
        disabled: disabled ? "" : null
      }, [asString(props.submitLabel, "Search")]);
      root.append(btn);
    }
    return root;
  }
};
const Form = {
  name: "Form",
  description: 'Form container. Children FormControls render in order; buttons render at the bottom. Provide `onSubmit` to handle form submission (invoked when the user presses Enter on a focused input or clicks a `type="submit"` Button inside the form).',
  props: [
    { name: "id", type: "string" },
    { name: "buttons", type: "Buttons | Button" },
    { name: "fields", type: "FormControl[]" },
    { name: "onSubmit", type: "callable", optional: true, aliases: ["onsubmit"], description: "Called when the form is submitted (Enter key or submit button)" },
    { name: "error", type: "string", optional: true, description: 'Form-level error rendered above the actions ("Invalid credentials", "That email is already registered")' },
    { name: "loading", type: "boolean", optional: true, aliases: ["submitting"], description: "A submit is in flight — marks the form `aria-busy`, disables its submit buttons and blocks re-entry" }
  ],
  render: (_node, props, helpers) => {
    const loading = asBoolean(props.loading);
    const error = asString(props.error);
    const form = el("form", {
      class: "rui-form",
      id: asString(props.id),
      "data-loading": loading ? "true" : null,
      "data-invalid": error ? "true" : null,
      "aria-busy": loading ? "true" : null
    });
    form.onsubmit = (event) => {
      event.preventDefault();
      if (loading) return;
      helpers.invoke(props.onSubmit);
    };
    for (const field of asArray(props.fields)) form.append(helpers.renderNode(field));
    if (error) {
      form.append(el("div", { class: "rui-form-error rui-field-error", role: "alert" }, [error]));
    }
    if (props.buttons) {
      const actions = el("div", { class: "rui-form-actions" });
      actions.append(helpers.renderNode(props.buttons));
      form.append(actions);
      if (loading) {
        actions.querySelectorAll('button[type="submit"]').forEach((btn) => {
          btn.setAttribute("disabled", "");
          btn.setAttribute("aria-busy", "true");
        });
      }
    }
    return form;
  }
};
const Slider = {
  name: "Slider",
  description: 'Range slider for selecting a single numeric value between `min` and `max`. Pass a `$variable` as `value` for two-way binding. Useful for filters, settings (volume, brightness), and parameter tuning. Use `suffix` ("%", " ms") or `format` ("${value}") so the displayed value carries its unit, and `marks` for a ticked or named scale (`[0, 50, 100]`, or `[{value: 1, label: "Low"}, …]` — a mark\'s label replaces the numeric readout when the slider sits on it). Single-thumb only: there is no two-handle range. For a `$50 – $400` filter use two Sliders that bound each other — `Slider("lo", value: $lo, max: $hi)` above `Slider("hi", value: $hi, min: $lo)` — which the browser then keeps ordered, since `min`/`max` accept `$variables`.',
  props: [
    { name: "id", type: "string" },
    { name: "min", type: "number", optional: true, description: "Default 0" },
    { name: "max", type: "number", optional: true, description: "Default 100" },
    { name: "step", type: "number", optional: true, description: "Default 1" },
    { name: "value", type: "number", optional: true, description: "Bound value (typically $variable)" },
    { name: "label", type: "string", optional: true },
    { name: "showValue", type: "boolean", optional: true, description: "Render the current numeric value beside the slider" },
    { name: "disabled", type: "boolean", optional: true },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"], description: "Called with the new number as the user drags" },
    { name: "suffix", type: "string", optional: true, description: 'Unit appended to the displayed value (e.g. "%", " ms")' },
    { name: "format", type: "string", optional: true, description: 'Display template — `{value}` is replaced by the number (e.g. "${value}"). Without a placeholder it behaves like `suffix`.' },
    { name: "hint", type: "string", optional: true, description: "Helper text rendered below the slider" },
    { name: "error", type: "string", optional: true, description: "Validation error rendered below the slider (marks it invalid)" },
    { name: "marks", type: "any[]", optional: true, description: "Tick scale: numbers (`[0, 50, 100]`) or `{value, label}` objects. A mark's label becomes the readout and the announced value when the slider is on it." },
    ...fieldShellExtraProps()
  ],
  render: (node, props, helpers) => {
    const label = asString(props.label);
    const explicitId = asString(props.id);
    const id = explicitId || fallbackFieldId("rui-slider", label);
    const min = asNumber(props.min, 0);
    const max = asNumber(props.max, 100);
    const step = asNumber(props.step, 1);
    const value = asNumber(props.value, min);
    const suffix = asString(props.suffix);
    const format = asString(props.format);
    const marks = extractSliderMarks(props.marks, min, max);
    const markLabel = (raw) => marks.find((mark) => mark.label && mark.value === Number(raw))?.label ?? "";
    const display = (raw) => {
      const text = String(raw);
      const named = markLabel(raw);
      if (named) return named;
      if (format.includes("{value}")) return format.replace("{value}", text);
      if (format.includes("{}")) return format.replace("{}", text);
      if (format) return `${text}${format}`;
      return suffix ? `${text}${suffix}` : text;
    };
    const needsValueText = (raw) => Boolean(format || suffix || markLabel(raw));
    const root = el("div", { class: "rui-slider", "data-disabled": asBoolean(props.disabled) ? "true" : "false" });
    const showValue = asBoolean(props.showValue);
    if (label || showValue) {
      const head = el("div", { class: "rui-slider-head" });
      if (label) head.append(el("label", { class: "rui-slider-label", for: id || null }, [label]));
      if (showValue) head.append(el("span", { class: "rui-slider-value" }, [display(value)]));
      root.append(head);
    }
    const input = el("input", {
      type: "range",
      class: "rui-slider-input",
      id: id || null,
      name: id || null,
      min: String(min),
      max: String(max),
      step: String(step),
      value: String(value),
      disabled: asBoolean(props.disabled) ? "" : null,
      // Last resort when no id could be derived, and harmless when one was.
      "aria-label": !explicitId && label ? label : null,
      // Screen readers announce the raw number otherwise ("50" for "50 ms",
      // or "2" for a step whose mark says "Medium").
      "aria-valuetext": needsValueText(value) ? display(value) : null
    });
    const stateName = node.argMeta?.[4]?.stateRef;
    if (stateName) {
      helpers.bindState(input, stateName, {
        event: "input",
        getValue: (n) => Number(n.value)
      });
    }
    composeHandler(input, "oninput", (event) => {
      const target = event.currentTarget;
      if (!target) return;
      const text = display(target.value);
      const sliderRoot = target.closest(".rui-slider");
      const valueEl = sliderRoot?.querySelector(".rui-slider-value");
      if (valueEl) valueEl.textContent = text;
      if (needsValueText(target.value)) target.setAttribute("aria-valuetext", text);
      else target.removeAttribute("aria-valuetext");
    });
    bindChangeHandler(input, props, helpers, {
      event: "input",
      getValue: (n) => Number(n.value)
    });
    root.append(input);
    if (marks.length > 0) {
      const span = max - min || 1;
      const row = el("div", { class: "rui-slider-marks", "aria-hidden": "true" });
      for (const mark of marks) {
        row.append(el("span", {
          class: "rui-slider-mark",
          "data-value": String(mark.value),
          // Only the render knows where a tick sits, so the offset is inline;
          // `.rui-slider-mark` supplies the absolute positioning it offsets from.
          style: `left:${((mark.value - min) / span * 100).toFixed(2)}%`
        }, [mark.label || String(mark.value)]));
      }
      root.append(row);
    }
    const shell = withFieldShell(root, { ...props, label: null }, { idKey: "id" });
    relocateControlAria(root, input);
    return shell;
  }
};
const NumberInput = {
  name: "NumberInput",
  description: 'Numeric input with paired increment/decrement buttons. Use for quantity steppers, integer settings, and any field where a `<input type="number">` plus +/- controls is friendlier than the native spinner. Pass a `$variable` as `value` for two-way binding. `prefix` / `suffix` render an inline unit ("€", "GB", "%"), and `precision` fixes the number of decimals (currency fields).',
  props: [
    { name: "id", type: "string" },
    { name: "value", type: "number", optional: true, description: "Bound value (typically $variable)" },
    { name: "min", type: "number", optional: true },
    { name: "max", type: "number", optional: true },
    { name: "step", type: "number", optional: true, description: "Default 1" },
    { name: "placeholder", type: "string", optional: true },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"], description: "Called with the new number (or null when blank)" },
    // `disabled` comes from FIELD_SHELL_PROPS — declaring it here as well added a
    // second, unreachable slot: it inflated the positional arity the validator
    // reports, listed the name twice in the generated prompt, and let a mixed
    // positional+named call fill the first slot and clobber it back to undefined
    // from the second.
    ...FIELD_SHELL_PROPS,
    { name: "prefix", type: "string", optional: true, description: 'Inline text before the number (e.g. "€")' },
    { name: "suffix", type: "string", optional: true, description: 'Inline unit after the number (e.g. "GB", "%", "ms")' },
    { name: "precision", type: "number", optional: true, description: "Number of decimals the field keeps (rounds the value it reports)" }
  ],
  render: (node, props, helpers) => {
    const id = asString(props.id);
    const step = asNumber(props.step, 1);
    const hasMin = props.min !== void 0 && props.min !== null;
    const hasMax = props.max !== void 0 && props.max !== null;
    const min = hasMin ? asNumber(props.min, 0) : Number.NEGATIVE_INFINITY;
    const max = hasMax ? asNumber(props.max, 0) : Number.POSITIVE_INFINITY;
    const disabled = asBoolean(props.disabled);
    const precision = props.precision === void 0 || props.precision === null ? null : Math.max(0, Math.floor(asNumber(props.precision, 0)));
    const stepDecimals = (String(step).split(".")[1] ?? "").length;
    const round = (n) => {
      if (precision !== null) return Number(n.toFixed(precision));
      return stepDecimals > 0 ? Number(n.toFixed(stepDecimals)) : n;
    };
    const root = el("div", { class: "rui-number-input", "data-disabled": disabled ? "true" : "false" });
    const decBtn = el("button", {
      type: "button",
      class: "rui-number-input-button",
      "data-direction": "down",
      "aria-label": "Decrement",
      disabled: disabled ? "" : null
    }, ["−"]);
    const input = el("input", {
      type: "number",
      class: "rui-number-input-field",
      id,
      name: id,
      value: valueAttr(props.value),
      placeholder: asString(props.placeholder),
      min: hasMin ? String(min) : null,
      max: hasMax ? String(max) : null,
      step: String(step),
      disabled: disabled ? "" : null
    });
    const incBtn = el("button", {
      type: "button",
      class: "rui-number-input-button",
      "data-direction": "up",
      "aria-label": "Increment",
      disabled: disabled ? "" : null
    }, ["+"]);
    const stateName = node.argMeta?.[1]?.stateRef;
    const readNumberValue = (n) => {
      const raw = n.value;
      if (raw === "") return null;
      const num2 = Number(raw);
      if (!Number.isFinite(num2)) return null;
      return round(clampNumber(num2, min, max));
    };
    if (stateName) {
      helpers.bindState(input, stateName, { event: "input", getValue: readNumberValue });
    }
    bindChangeHandler(input, props, helpers, {
      event: "input",
      getValue: readNumberValue
    });
    const adjust = (origin, delta) => {
      const liveRoot = origin.closest(".rui-number-input");
      const live = liveRoot?.querySelector(".rui-number-input-field");
      if (!live) return;
      const current = Number(live.value);
      const base = Number.isFinite(current) ? current : 0;
      const next = round(clampNumber(base + delta, min, max));
      live.value = String(next);
      live.dispatchEvent(new Event("input", { bubbles: true }));
    };
    decBtn.onclick = (event) => adjust(event.currentTarget ?? event.target, -step);
    incBtn.onclick = (event) => adjust(event.currentTarget ?? event.target, step);
    attachFocusHandlers(input, props, helpers, readNumberValue);
    composeHandler(input, "onblur", (event) => {
      const live = event.currentTarget ?? event.target;
      if (!live || live.value === "") return;
      const corrected = readNumberValue(live);
      if (corrected !== null && String(corrected) !== live.value) {
        live.value = String(corrected);
        live.dispatchEvent(new Event("input", { bubbles: true }));
      }
    });
    const prefix = asString(props.prefix);
    const suffix = asString(props.suffix);
    root.append(decBtn);
    if (prefix) root.append(el("span", { class: "rui-number-input-prefix", "aria-hidden": "true" }, [prefix]));
    root.append(input);
    if (suffix) root.append(el("span", { class: "rui-number-input-suffix", "aria-hidden": "true" }, [suffix]));
    root.append(incBtn);
    const shell = withFieldShell(root, props, { idKey: "id" });
    relocateControlAria(root, input);
    if (suffix || prefix) {
      const spoken = [prefix, suffix].filter(Boolean).join(" ");
      const existing = asString(props.label);
      if (existing) input.setAttribute("aria-label", `${existing} (${spoken})`);
    }
    return shell;
  }
};
const DatePicker = {
  name: "DatePicker",
  description: "Date picker that wraps the native `<input type=\"date\">` with consistent styling. Pass a `$variable` as `value` for two-way binding. Use `min`/`max` to bound the selectable range, and `error`/`required` to make it part of the form's validation flow. Set `locale` (`de-DE`, `en-GB`) to echo the chosen date in that locale's order under the field — the native widget's own boxes always follow the viewer's browser, which is not the customer's when the app serves one market.",
  props: [
    { name: "id", type: "string" },
    { name: "value", type: "string", optional: true, description: "ISO date (YYYY-MM-DD); typically $variable" },
    { name: "label", type: "string", optional: true },
    { name: "min", type: "string", optional: true, description: "Earliest ISO date" },
    { name: "max", type: "string", optional: true, description: "Latest ISO date" },
    { name: "placeholder", type: "string", optional: true, description: "Accessible name fallback when no `label` is given — date inputs render their own locale format hint, so this is never displayed" },
    { name: "disabled", type: "boolean", optional: true },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"], description: "Called with the new ISO date string when the picker changes" },
    { name: "hint", type: "string", optional: true, description: "Helper text rendered below the control" },
    { name: "error", type: "string", optional: true, description: "Validation error rendered below the control (marks it invalid)" },
    { name: "required", type: "boolean", optional: true, description: "Mark the field required" },
    { name: "onBlur", type: "callable", optional: true, aliases: ["onblur"], description: "Called with the current value when focus leaves the control (validate-on-blur)" },
    { name: "onFocus", type: "callable", optional: true, aliases: ["onfocus"], description: "Called when the control gains focus" },
    { name: "locale", type: "string", optional: true, description: "BCP-47 tag (`de-DE`, `en-GB`, `fr-CH`) the selected date is echoed in beneath the field, and the language the value is announced in. Same `locale` channel as `Table`/`Col`, so a filter and the report it filters read alike. Bound values stay ISO `YYYY-MM-DD`." },
    ...fieldShellExtraProps()
  ],
  render: (node, props, helpers) => {
    const label = asString(props.label);
    const placeholder = asString(props.placeholder);
    const explicitId = asString(props.id);
    const id = explicitId || fallbackFieldId("rui-date", label || placeholder);
    const locale = localeTag$1(props.locale);
    const root = el("div", { class: "rui-date-picker" });
    if (label) root.append(el("label", { class: "rui-date-picker-label", for: id || null }, [label]));
    const input = el("input", {
      type: "date",
      class: "rui-date-picker-input",
      id: id || null,
      name: id || null,
      value: valueAttr(props.value),
      min: asString(props.min) || null,
      max: asString(props.max) || null,
      disabled: asBoolean(props.disabled) ? "" : null,
      // Browsers ignore `placeholder` on `<input type="date">` — it renders its
      // own mm/dd/yyyy hint — so the supplied text is surfaced as the
      // accessible name instead of being silently dropped. Also the fallback
      // when there is no id to hang the visible label's `for` on.
      "aria-label": id && label ? null : label || placeholder || null,
      // The date the field holds is written in `locale`, so say so: `lang` is
      // what tells a screen reader which language to pronounce it in, and the UAs
      // that consult it for the widget's own segment order pick it up too. It is
      // NOT a promise about the boxes — that is the browser's call, which is
      // exactly why the readout below exists.
      lang: locale ?? null
    });
    bindToStateAtArg(input, node, 1, helpers);
    bindChangeHandler(input, props, helpers, {
      event: "change",
      getValue: (n) => n.value
    });
    attachFocusHandlers(input, props, helpers);
    root.append(input);
    if (locale) {
      root.append(el("span", {
        class: "rui-date-picker-readout",
        "aria-hidden": "true"
      }, [formatDateInLocale(props.value, locale)]));
      wireDateReadout(input, ".rui-date-picker", ".rui-date-picker-readout", (liveRoot) => formatDateInLocale(liveRoot.querySelector(".rui-date-picker-input")?.value, locale));
    }
    const shell = withFieldShell(root, { ...props, label: null }, { idKey: "id" });
    relocateControlAria(root, input);
    return shell;
  }
};
const FileUpload = {
  name: "FileUpload",
  description: "Styled file picker. Renders a click/drop area with a leading icon, label, and helper text. Files cannot round-trip through `$variables` (they are not serialisable), so pass a callable as `action` to handle the picked files. Read one with `$util.readFile(files)` — pass the whole pick and it resolves the first file's text (or a data URL), resolving an empty string rather than rejecting on any failure. Set `maxSize` (in bytes) to reject oversized files before the upload starts, `error` to show why one was refused, and `progress` (0–100) while it transfers.",
  props: [
    { name: "id", type: "string" },
    { name: "label", type: "string", optional: true, description: 'Primary label (default "Choose a file")' },
    { name: "hint", type: "string", optional: true, description: "Secondary helper text" },
    { name: "accept", type: "string", optional: true, description: "Comma-separated MIME types or extensions" },
    { name: "multiple", type: "boolean", optional: true },
    { name: "onSelect", type: "callable", optional: true, aliases: ["action", "onChange"], description: "Callable fired with the accepted files when files are picked" },
    { name: "icon", type: "string", optional: true, description: 'Font Awesome icon (default "cloud-arrow-up")' },
    { name: "disabled", type: "boolean", optional: true },
    { name: "maxSize", type: "number", optional: true, description: "Maximum accepted file size in bytes — larger files are rejected client-side and never reach `onSelect`" },
    { name: "error", type: "string", optional: true, description: 'Error rendered below the drop zone ("File too large", "PDF only", "Upload failed")' },
    { name: "progress", type: "number", optional: true, description: "Upload progress 0–100; renders a progress bar under the drop zone" },
    { name: "onRemove", type: "callable", optional: true, description: "Called with the removed File when the user deselects one from the preview" }
  ],
  render: (_node, props, helpers) => {
    const id = asString(props.id);
    const disabled = asBoolean(props.disabled);
    const error = asString(props.error);
    const maxSize = Math.max(0, asNumber(props.maxSize, 0));
    const root = el("div", {
      class: "rui-file-upload",
      "data-disabled": disabled ? "true" : "false",
      "data-invalid": error ? "true" : null
    });
    const dropZone = el("label", {
      class: "rui-file-upload-dropzone",
      for: id
    });
    const iconNode = renderIcon(asString(props.icon, "cloud-arrow-up"), { className: "rui-file-upload-icon" });
    if (iconNode) dropZone.append(iconNode);
    const text = el("div", { class: "rui-file-upload-text" });
    text.append(el("div", { class: "rui-file-upload-label" }, [asString(props.label, "Choose a file")]));
    const hint = asString(props.hint);
    if (hint) text.append(el("div", { class: "rui-file-upload-hint" }, [hint]));
    dropZone.append(text);
    const errorId = error && id ? `${id}-error` : null;
    const input = el("input", {
      type: "file",
      id,
      name: id,
      class: "rui-file-upload-input",
      accept: asString(props.accept) || null,
      multiple: asBoolean(props.multiple) ? "" : null,
      disabled: disabled ? "" : null,
      "aria-invalid": error ? "true" : null,
      "aria-describedby": errorId
    });
    dropZone.append(input);
    root.append(dropZone);
    if (error) {
      root.append(el("div", {
        class: "rui-file-upload-error rui-field-error",
        id: errorId,
        role: "alert"
      }, [error]));
    }
    if (props.progress !== void 0 && props.progress !== null) {
      const pct = Math.min(100, Math.max(0, asNumber(props.progress, 0)));
      const track = el("div", {
        class: "rui-file-upload-progress",
        role: "progressbar",
        "aria-valuemin": "0",
        "aria-valuemax": "100",
        "aria-valuenow": String(Math.round(pct)),
        // Inline so the bar is visible without waiting on a theme rule; a
        // multi-megabyte upload with no feedback is the whole complaint.
        style: "height:4px;border-radius:999px;overflow:hidden;background:var(--rui-color-border-subtle,#e5e7eb)"
      });
      track.append(el("div", {
        class: "rui-file-upload-progress-bar",
        style: `height:100%;width:${pct}%;background:var(--rui-color-primary,#2563eb)`
      }));
      root.append(track);
    }
    const isImageFile = (file, accept) => accept.includes("image") || file.type.startsWith("image/") || /\.(png|jpe?g|gif|webp|svg|avif|bmp)$/i.test(file.name);
    const objectUrls = [];
    const revokeObjectUrls = () => {
      while (objectUrls.length > 0) URL.revokeObjectURL(objectUrls.pop());
    };
    helpers.registerDisposer(revokeObjectUrls, "file-upload-object-urls");
    const partitionBySize = (files) => {
      const list = files ? Array.from(files) : [];
      if (maxSize <= 0) return { accepted: list, rejected: [] };
      return {
        accepted: list.filter((file) => file.size <= maxSize),
        rejected: list.filter((file) => file.size > maxSize)
      };
    };
    const writeFiles = (target, files) => {
      if (!target) return;
      try {
        const dt = new DataTransfer();
        for (const file of files) dt.items.add(file);
        target.files = dt.files;
      } catch {
        if (files.length === 0) target.value = "";
      }
    };
    const showPreview = (uploadRoot, files, rejected = []) => {
      const existing = uploadRoot.querySelector(".rui-file-upload-preview");
      if (existing) existing.remove();
      const list = files ? Array.from(files) : [];
      if (list.length === 0 && rejected.length === 0) return;
      const accept = asString(props.accept);
      const preview = el("div", { class: "rui-file-upload-preview", role: "status" });
      list.forEach((file) => {
        const row = el("div", { class: "rui-file-upload-preview-item" });
        if (isImageFile(file, accept)) {
          const objectUrl = URL.createObjectURL(file);
          objectUrls.push(objectUrl);
          const img = el("img", { src: objectUrl, alt: file.name, class: "rui-file-upload-thumbnail" });
          const release = () => {
            URL.revokeObjectURL(objectUrl);
            const at = objectUrls.indexOf(objectUrl);
            if (at >= 0) objectUrls.splice(at, 1);
          };
          img.onload = release;
          img.onerror = release;
          row.append(img);
        }
        row.append(el("span", { class: "rui-file-upload-filename" }, [file.name]));
        const size2 = formatFileSize(file.size);
        if (size2) row.append(el("span", { class: "rui-file-upload-filesize" }, [size2]));
        const removeBtn = el("button", {
          type: "button",
          class: "rui-file-upload-remove",
          "aria-label": `Remove ${file.name}`
        }, ["×"]);
        removeBtn.onclick = (event) => {
          event.preventDefault();
          event.stopPropagation();
          const origin = event.currentTarget ?? event.target;
          const liveRoot = origin.closest(".rui-file-upload");
          const liveInput = liveRoot?.querySelector(".rui-file-upload-input") ?? null;
          const remaining = Array.from(liveInput?.files ?? list).filter((f) => f !== file);
          writeFiles(liveInput, remaining);
          helpers.invoke(props.onRemove, file);
          helpers.invoke(props.onSelect, liveInput?.files ?? remaining);
          if (liveRoot) showPreview(liveRoot, remaining);
        };
        row.append(removeBtn);
        preview.append(row);
      });
      for (const file of rejected) {
        preview.append(el("div", { class: "rui-file-upload-preview-item", "data-rejected": "true" }, [
          el("span", { class: "rui-file-upload-filename" }, [file.name]),
          el("span", { class: "rui-field-error" }, [`Larger than ${formatFileSize(maxSize)}`])
        ]));
      }
      uploadRoot.append(preview);
    };
    const handleFiles = (origin, files) => {
      const { accepted, rejected } = partitionBySize(files);
      const uploadRoot = origin.closest(".rui-file-upload");
      const liveInput = uploadRoot?.querySelector(".rui-file-upload-input") ?? null;
      if (rejected.length > 0) writeFiles(liveInput, accepted);
      helpers.invoke(props.onSelect, rejected.length > 0 ? accepted : files);
      if (uploadRoot) showPreview(uploadRoot, accepted, rejected);
    };
    input.onchange = (event) => {
      const fileInput = event.currentTarget ?? event.target;
      handleFiles(fileInput, fileInput.files);
    };
    const stop = (e) => {
      e.preventDefault();
      e.stopPropagation();
    };
    const setDragOver = (event, on) => {
      const live = event.currentTarget ?? event.target;
      const zone = live?.closest(".rui-file-upload-dropzone");
      if (zone) zone.classList.toggle("is-dragover", on);
      return zone;
    };
    dropZone.ondragenter = (event) => {
      if (disabled) return;
      stop(event);
      setDragOver(event, true);
    };
    dropZone.ondragover = (event) => {
      if (disabled) return;
      stop(event);
      setDragOver(event, true);
    };
    dropZone.ondragleave = (event) => {
      if (disabled) return;
      stop(event);
      setDragOver(event, false);
    };
    dropZone.ondrop = (event) => {
      if (disabled) return;
      stop(event);
      const zone = setDragOver(event, false);
      const dt = event.dataTransfer;
      if (!dt || dt.files.length === 0) return;
      const liveInput = zone?.querySelector(".rui-file-upload-input") ?? null;
      try {
        if (liveInput) liveInput.files = dt.files;
      } catch {
      }
      handleFiles(zone ?? dropZone, dt.files);
    };
    return root;
  }
};
const COMBOBOX_PANEL = ".rui-combobox-panel";
const COMBOBOX_TRIGGER = ".rui-combobox-trigger";
const MULTISELECT_PANEL = ".rui-multiselect-panel";
const MULTISELECT_TRIGGER = ".rui-multiselect-trigger";
const PICKER_FLOATING = {
  matchAnchorWidth: true,
  minWidth: 180,
  layer: "dropdown"
};
const positionPickerOnMount = (root, panelSelector, triggerSelector) => {
  deferToPaint(() => {
    if (!root.isConnected) return;
    if (root.getAttribute("data-open") !== "true") return;
    syncFloatingPanel(root, true, panelSelector, triggerSelector, PICKER_FLOATING);
  });
};
const SEARCH_DEBOUNCE_MS = 200;
function makeSearchDispatcher(props, helpers, disposerKey) {
  const timer = helpers.useInstanceState("searchTimer", null);
  const cancel = () => {
    const pending = timer.get();
    if (pending === null) return;
    clearTimeout(pending);
    timer.set(null);
  };
  const schedule = (query) => {
    if (props.onSearch == null) return;
    cancel();
    helpers.registerDisposer(cancel, disposerKey);
    const handle = setTimeout(() => {
      timer.set(null);
      helpers.invoke(props.onSearch, query);
    }, SEARCH_DEBOUNCE_MS);
    timer.set(handle);
  };
  return { schedule, cancel };
}
const Combobox = {
  name: "Combobox",
  description: "Searchable single-select dropdown — type to filter, click an option to choose. Use instead of `Select` when the list is long enough that scanning is faster than scrolling (countries, currencies, repos, users). Pass a `$variable` as `value` for two-way binding; the selected option's `value` is written to state on pick. Arrow keys / Home / End / Enter / Escape operate the list. Pass `onSearch` for a server-side type-ahead (called with the query ~200ms after typing stops; local filtering is then skipped — supply the matches as `items`), `loading` while those matches are in flight, `creatable` to accept a value that is not in the list, and `label`/`hint`/`error`/`required` for a labelled field.",
  props: [
    { name: "id", type: "string" },
    { name: "items", type: "SelectItem[]", description: "Options; SelectItem(value, label) or {value, label}" },
    { name: "value", type: "string", optional: true, description: "Bound selected value (typically $variable)" },
    { name: "placeholder", type: "string", optional: true },
    { name: "emptyLabel", type: "string", optional: true, description: 'Text shown when no items match the filter (default "No matches")' },
    { name: "disabled", type: "boolean", optional: true },
    { name: "open", type: "boolean", optional: true, description: "Initial open state — use to demo or pre-open the dropdown" },
    { name: "onOpenChange", type: "callable", optional: true, aliases: ["onopenchange"], description: "Called with the new boolean open state whenever the component opens or closes." },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"], description: "Called with the newly-selected value" },
    { name: "label", type: "string", optional: true, description: "Field label rendered above the control" },
    { name: "hint", type: "string", optional: true, description: "Helper text rendered below the control" },
    { name: "error", type: "string", optional: true, description: "Validation error rendered below the control (marks it invalid)" },
    { name: "required", type: "boolean", optional: true, description: "Mark the field required (adds a `*` and the `required` attribute)" },
    { name: "loading", type: "boolean", optional: true, description: 'Matches are being fetched — shows "Loading…" instead of the (lying) empty label' },
    { name: "onSearch", type: "callable", optional: true, description: "Called with the query on every keystroke for server-side search; disables local filtering" },
    { name: "clearable", type: "boolean", optional: true, description: "Show a clear (×) control so the selection can be reset to the placeholder" },
    { name: "onBlur", type: "callable", optional: true, aliases: ["onblur"], description: "Called with the selected value when focus leaves the control (validate-on-blur)" },
    { name: "onFocus", type: "callable", optional: true, aliases: ["onfocus"], description: "Called when the control gains focus" },
    { name: "creatable", type: "boolean", optional: true, description: 'Offer the typed text itself as an option when it matches nothing ("Create «acme-corp»") so a value outside `items` can be selected' },
    { name: "labelHidden", type: "boolean", optional: true, description: "Keep the label in the accessibility tree but hide it visually — for a field whose purpose is already clear from context (a picker under a section heading that names it, a control in a table cell whose column header is the label)" },
    ...fieldShellExtraProps()
  ],
  render: (node, props, helpers) => {
    const id = asString(props.id);
    const items = extractComboboxItems(props.items);
    const currentValue = asString(props.value);
    const currentLabel = items.find((item) => item.value === currentValue)?.label ?? currentValue;
    const placeholder = asString(props.placeholder, "Select…");
    const emptyLabel = asString(props.emptyLabel, "No matches");
    const disabled = asBoolean(props.disabled);
    const loading = asBoolean(props.loading);
    const remoteSearch = props.onSearch != null;
    const creatable = asBoolean(props.creatable);
    const search = makeSearchDispatcher(props, helpers, "combobox-search");
    const initialOpen = asBoolean(props.open);
    const openSlot = helpers.useInstanceState("open", initialOpen);
    const filterSlot = helpers.useInstanceState("filter", "");
    const activeSlot = helpers.useInstanceState("active", -1);
    const isOpen = openSlot.get();
    const panelId2 = id ? `${id}-listbox` : null;
    const optionDomId = (index) => id ? `${id}-option-${index}` : null;
    const root = el("div", {
      class: "rui-combobox",
      "data-open": isOpen ? "true" : "false",
      "data-disabled": disabled ? "true" : "false",
      "data-loading": loading ? "true" : null
    });
    const triggerBtn = el("button", {
      type: "button",
      class: "rui-combobox-trigger",
      id,
      // Without `role="combobox"` + `aria-controls` the composite is announced
      // as a button followed by a pile of loose buttons.
      role: "combobox",
      "aria-haspopup": "listbox",
      "aria-expanded": isOpen ? "true" : "false",
      "aria-controls": panelId2,
      disabled: disabled ? "" : null
    });
    triggerBtn.append(el("span", {
      class: "rui-combobox-value",
      "data-placeholder": currentLabel ? "false" : "true"
    }, [currentLabel || placeholder]));
    const chevron = renderIcon("chevron-down", { className: "rui-combobox-chevron" });
    if (chevron) triggerBtn.append(chevron);
    attachFocusHandlers(triggerBtn, props, helpers, () => currentValue);
    root.append(triggerBtn);
    const panel = el("div", {
      class: "rui-combobox-panel",
      id: panelId2,
      role: "listbox",
      "aria-busy": loading ? "true" : null
    });
    const filterInput = el("input", {
      type: "text",
      class: "rui-combobox-filter",
      // An id is what makes focus and caret position recoverable: element.ts
      // snapshots the active element by id before a render and restores it after
      // (captureFocus/restoreFocus), and it bails out on an unnamed node. Without
      // one, a validate-on-change `error` appearing mid-word swaps the field
      // shell in around the panel and the user's typing simply stops (D0337).
      id: id ? `${id}-filter` : null,
      placeholder: "Filter…",
      autocomplete: "off",
      value: filterSlot.get(),
      "aria-label": "Filter options",
      "aria-controls": panelId2,
      "aria-autocomplete": "list"
    });
    panel.append(filterInput);
    const list = el("div", { class: "rui-combobox-list" });
    panel.append(list);
    const renderList = (target, filter) => {
      target.replaceChildren();
      if (loading) {
        target.append(el("div", { class: "rui-combobox-loading" }, ["Loading…"]));
        return;
      }
      const query = filter.trim();
      const lower = query.toLowerCase();
      const matches = lower === "" || remoteSearch ? items : items.filter(
        (item) => item.label.toLowerCase().includes(lower) || item.value.toLowerCase().includes(lower)
      );
      const canCreate = creatable && query !== "" && currentValue.toLowerCase() !== lower && !items.some((item) => item.value.toLowerCase() === lower || item.label.toLowerCase() === lower);
      if (matches.length === 0 && !canCreate) {
        target.append(el("div", { class: "rui-combobox-empty" }, [emptyLabel]));
        return;
      }
      const active = activeSlot.get();
      let lastGroup;
      matches.forEach((item, index) => {
        if (item.group && item.group !== lastGroup) {
          target.append(el("div", {
            class: "rui-combobox-group",
            role: "presentation"
          }, [item.group]));
          lastGroup = item.group;
        }
        const option = el("button", {
          type: "button",
          class: "rui-combobox-option",
          role: "option",
          id: optionDomId(index),
          // Options are driven by `aria-activedescendant`, so they must not each
          // be a tab stop — reaching "Zimbabwe" was ~200 Tab presses.
          tabindex: "-1",
          "data-value": item.value,
          "data-active": index === active ? "true" : null,
          "data-disabled": item.disabled ? "true" : null,
          // `aria-disabled` rather than the `disabled` attribute: a listbox
          // option must stay discoverable by arrow keys and announced as
          // unavailable, which `disabled` would prevent by removing it entirely.
          "aria-disabled": item.disabled ? "true" : null,
          "aria-selected": item.value === currentValue ? "true" : "false"
        }, [item.label]);
        if (!item.disabled) {
          option.onclick = (event) => {
            event.stopPropagation();
            selectComboboxValue(event.currentTarget, item.value);
          };
        }
        target.append(option);
      });
      if (canCreate) {
        const create = el("button", {
          type: "button",
          class: "rui-combobox-option rui-combobox-create",
          role: "option",
          id: optionDomId(matches.length),
          tabindex: "-1",
          "data-value": query,
          "data-create": "true",
          "data-active": matches.length === active ? "true" : null,
          "aria-selected": "false"
        }, [`Create “${query}”`]);
        create.onclick = (event) => {
          event.stopPropagation();
          selectComboboxValue(event.currentTarget, query);
        };
        target.append(create);
      }
    };
    const closeCombobox = (origin) => {
      openSlot.set(false);
      helpers.invoke(props.onOpenChange, false);
      search.cancel();
      filterSlot.set("");
      activeSlot.set(-1);
      const live = origin.closest(".rui-combobox");
      if (!live) return;
      live.setAttribute("data-open", "false");
      const trigger = live.querySelector(".rui-combobox-trigger");
      trigger?.setAttribute("aria-expanded", "false");
      closeFloating(live.querySelector(COMBOBOX_PANEL));
      disposeDismissListeners(live);
    };
    const selectComboboxValue = (origin, value) => {
      const stateName = node.argMeta?.[2]?.stateRef;
      if (stateName) {
        helpers.setState(stateName, value);
      }
      helpers.invoke(props.onChange, value);
      closeCombobox(origin);
    };
    renderList(list, filterSlot.get());
    const optionsIn = (scope) => Array.from(scope.querySelectorAll(".rui-combobox-option[data-value]"));
    const setActiveOption = (panelEl, index, filterEl) => {
      const options = optionsIn(panelEl);
      if (options.length === 0) return;
      const clamped = (index % options.length + options.length) % options.length;
      activeSlot.set(clamped);
      options.forEach((option, i) => {
        if (i !== clamped) {
          option.removeAttribute("data-active");
          return;
        }
        option.setAttribute("data-active", "true");
        if (option.id) filterEl?.setAttribute("aria-activedescendant", option.id);
        if (typeof option.scrollIntoView === "function") option.scrollIntoView({ block: "nearest" });
      });
    };
    filterInput.oninput = (event) => {
      const target = event.currentTarget;
      filterSlot.set(target.value);
      activeSlot.set(-1);
      target.removeAttribute("aria-activedescendant");
      search.schedule(target.value);
      const liveList = target.closest(".rui-combobox-panel")?.querySelector(".rui-combobox-list");
      if (liveList) renderList(liveList, target.value);
    };
    filterInput.onkeydown = (event) => {
      const e = event;
      const target = e.currentTarget;
      const panelEl = target.closest(".rui-combobox-panel");
      if (!panelEl) return;
      const active = activeSlot.get();
      if (e.key === "ArrowDown") {
        e.preventDefault();
        setActiveOption(panelEl, active + 1, target);
        return;
      }
      if (e.key === "ArrowUp") {
        e.preventDefault();
        setActiveOption(panelEl, active <= 0 ? optionsIn(panelEl).length - 1 : active - 1, target);
        return;
      }
      if (e.key === "Home") {
        e.preventDefault();
        setActiveOption(panelEl, 0, target);
        return;
      }
      if (e.key === "End") {
        e.preventDefault();
        setActiveOption(panelEl, optionsIn(panelEl).length - 1, target);
        return;
      }
      if (e.key === "Enter") {
        e.preventDefault();
        const options = optionsIn(panelEl);
        const chosen = (active >= 0 ? options[active] : options[0]) ?? null;
        const value = chosen?.getAttribute("data-value");
        if (chosen && value !== null && value !== void 0) selectComboboxValue(chosen, value);
        return;
      }
      if (e.key === "Escape") {
        e.preventDefault();
        closeCombobox(target);
        return;
      }
    };
    triggerBtn.onclick = (event) => {
      if (disabled) return;
      event.stopPropagation();
      const next = !openSlot.get();
      openSlot.set(next);
      helpers.invoke(props.onOpenChange, next);
      const live = event.currentTarget.closest(".rui-combobox");
      live?.setAttribute("data-open", next ? "true" : "false");
      live?.querySelector(".rui-combobox-trigger")?.setAttribute("aria-expanded", next ? "true" : "false");
      if (!live) return;
      syncFloatingPanel(live, next, COMBOBOX_PANEL, COMBOBOX_TRIGGER, PICKER_FLOATING);
      if (!next) {
        search.cancel();
        disposeDismissListeners(live);
        return;
      }
      activeSlot.set(-1);
      const cancelFocus = deferToPaint(() => {
        const target = live.querySelector(".rui-combobox-filter") ?? (filterInput.isConnected ? filterInput : null);
        target?.focus();
      });
      helpers.registerDisposer(cancelFocus, "combobox-focus");
      installDismissListeners({
        liveRoot: live,
        onDismiss: () => {
          openSlot.set(false);
          helpers.invoke(props.onOpenChange, false);
          search.cancel();
          filterSlot.set("");
          activeSlot.set(-1);
          live.setAttribute("data-open", "false");
          live.querySelector(".rui-combobox-trigger")?.setAttribute("aria-expanded", "false");
          closeFloating(live.querySelector(COMBOBOX_PANEL));
        }
      });
    };
    if (asBoolean(props.clearable) && currentValue !== "" && !disabled) {
      const clearBtn = el("button", {
        type: "button",
        class: "rui-combobox-clear",
        "aria-label": "Clear selection"
      }, ["×"]);
      clearBtn.onclick = (event) => {
        event.stopPropagation();
        const stateName = node.argMeta?.[2]?.stateRef;
        if (stateName) helpers.setState(stateName, "");
        helpers.invoke(props.onChange, "");
      };
      root.append(clearBtn);
    }
    root.append(panel);
    if (isOpen) positionPickerOnMount(root, COMBOBOX_PANEL, COMBOBOX_TRIGGER);
    const shell = withFieldShell(root, props, { idKey: "id" });
    relocateControlAria(root, triggerBtn);
    return shell;
  }
};
const MultiSelect = {
  name: "MultiSelect",
  description: "Multi-option searchable dropdown. Type to filter, click an option to add/remove it from the bound array. Renders the selected options as removable chips inside the trigger. Pass a `$variable` (array of values) as `value` for two-way binding. Arrow keys / Home / End move through the list and Enter or Space toggles the highlighted option. `min`/`max` bound the selection size; `onSearch` turns filtering over to the server (called with the query ~200ms after typing stops — supply the matches as `items`, with `loading` while they are in flight), and `creatable` accepts a value that is not in the list.",
  props: [
    { name: "id", type: "string" },
    { name: "items", type: "SelectItem[]", description: "Options; SelectItem(value, label) or {value, label}" },
    { name: "value", type: "any[]", optional: true, description: "Bound array of selected values (typically $variable)" },
    { name: "placeholder", type: "string", optional: true },
    { name: "emptyLabel", type: "string", optional: true, description: "Text shown when no items match the filter" },
    { name: "max", type: "number", optional: true, description: "Maximum number of selections" },
    { name: "disabled", type: "boolean", optional: true },
    { name: "open", type: "boolean", optional: true, description: "Initial open state — use to demo or pre-open the dropdown" },
    { name: "onOpenChange", type: "callable", optional: true, aliases: ["onopenchange"], description: "Called with the new boolean open state whenever the component opens or closes." },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"], description: "Called with the updated array of selected values" },
    { name: "label", type: "string", optional: true, description: "Field label rendered above the control" },
    { name: "hint", type: "string", optional: true, description: "Helper text rendered below the control" },
    { name: "error", type: "string", optional: true, description: "Validation error rendered below the control (marks it invalid)" },
    { name: "required", type: "boolean", optional: true, description: "Mark the field required (adds a `*` and the `required` attribute)" },
    { name: "min", type: "number", optional: true, description: 'Minimum number of selections — the last ones cannot be removed ("pick at least one")' },
    { name: "onSearch", type: "callable", optional: true, description: "Called with the query ~200ms after typing stops, for server-side search; disables local filtering" },
    { name: "loading", type: "boolean", optional: true, description: 'Matches are being fetched — shows "Loading…" instead of the (lying) empty label' },
    { name: "creatable", type: "boolean", optional: true, description: 'Offer the typed text itself as an option when it matches nothing ("Create «backend»") so a tag outside `items` can be added' },
    { name: "labelHidden", type: "boolean", optional: true, description: "Keep the label in the accessibility tree but hide it visually — for a field whose purpose is already clear from context (a picker under a section heading that names it, a control in a table cell whose column header is the label)" },
    ...fieldShellExtraProps()
  ],
  render: (node, props, helpers) => {
    const id = asString(props.id);
    const items = extractComboboxItems(props.items);
    const placeholder = asString(props.placeholder, "Select…");
    const emptyLabel = asString(props.emptyLabel, "No matches");
    const disabled = asBoolean(props.disabled);
    const rawMax = asNumber(props.max, 0);
    const max = Number.isFinite(rawMax) ? Math.max(0, Math.floor(rawMax)) : 0;
    if (props.max !== void 0 && props.max !== null && !Number.isFinite(Number(props.max))) {
      console.warn(`[aktion] MultiSelect max="${asString(props.max)}" is not a number — the selection cap is ignored.`);
    }
    const rawMin = asNumber(props.min, 0);
    const min = Number.isFinite(rawMin) ? Math.max(0, Math.floor(rawMin)) : 0;
    const loading = asBoolean(props.loading);
    const remoteSearch = props.onSearch != null;
    const creatable = asBoolean(props.creatable);
    const search = makeSearchDispatcher(props, helpers, "multiselect-search");
    const selected = Array.isArray(props.value) ? props.value.map((v) => asString(v)).filter(Boolean) : [];
    const selectedSet = new Set(selected);
    const stateName = node.argMeta?.[2]?.stateRef;
    const initialOpen = asBoolean(props.open);
    const openSlot = helpers.useInstanceState("open", initialOpen);
    const filterSlot = helpers.useInstanceState("filter", "");
    const activeSlot = helpers.useInstanceState("active", -1);
    const isOpen = openSlot.get();
    const panelId2 = id ? `${id}-listbox` : null;
    const optionDomId = (index) => id ? `${id}-option-${index}` : null;
    const root = el("div", {
      class: "rui-multiselect",
      "data-open": isOpen ? "true" : "false",
      "data-disabled": disabled ? "true" : "false",
      "data-loading": loading ? "true" : null
    });
    const selectionSummary = selected.length === 0 ? "No options selected" : `${selected.length} selected${max > 0 ? ` of ${max}` : ""}`;
    const fieldLabel = asString(props.label);
    const triggerBtn = el("div", {
      class: "rui-multiselect-trigger",
      id,
      role: "combobox",
      "aria-haspopup": "listbox",
      "aria-expanded": isOpen ? "true" : "false",
      "aria-controls": panelId2,
      tabindex: disabled ? null : "0",
      "aria-disabled": disabled ? "true" : null,
      "aria-label": fieldLabel ? `${fieldLabel}, ${selectionSummary}` : selectionSummary
    });
    const chipRow = el("span", { class: "rui-multiselect-chips" });
    const writeSelection = (origin, next) => {
      if (min > 0 && next.length < min && next.length < selected.length) return;
      const live = origin.closest(".rui-multiselect");
      closeFloating(
        origin.closest(MULTISELECT_PANEL) ?? live?.querySelector(MULTISELECT_PANEL)
      );
      if (stateName) helpers.setState(stateName, next);
      helpers.invoke(props.onChange, next);
      if (!live) return;
      deferToPaint(() => {
        if (!live.isConnected) return;
        if (live.getAttribute("data-open") !== "true") return;
        syncFloatingPanel(live, true, MULTISELECT_PANEL, MULTISELECT_TRIGGER, PICKER_FLOATING);
      });
    };
    if (selected.length === 0) {
      chipRow.append(el("span", { class: "rui-multiselect-placeholder" }, [placeholder]));
    } else {
      selected.forEach((value, chipIndex) => {
        const label = items.find((item) => item.value === value)?.label ?? value;
        const chip = el("span", { class: "rui-multiselect-chip", "data-value": value });
        chip.append(el("span", { class: "rui-multiselect-chip-label" }, [label]));
        const atMin = min > 0 && selected.length <= min;
        const removeBtn = el("button", {
          type: "button",
          class: "rui-multiselect-chip-remove",
          // Positional id, so element.ts's focus snapshot can put the keyboard
          // user back on the same slot in the row after the render a removal
          // triggers — without one, removing a chip dumps focus on <body>.
          id: id ? `${id}-chip-${chipIndex}` : null,
          "aria-label": `Remove ${label}`,
          // At the lower bound there is nothing to remove — say so rather than
          // silently swallowing the click.
          disabled: atMin ? "" : null
        }, ["×"]);
        removeBtn.onclick = (event) => {
          event.stopPropagation();
          const next = selected.filter((v) => v !== value);
          writeSelection(event.currentTarget, next);
        };
        chip.append(removeBtn);
        chipRow.append(chip);
      });
    }
    triggerBtn.append(chipRow);
    const chevron = renderIcon("chevron-down", { className: "rui-multiselect-chevron" });
    if (chevron) triggerBtn.append(chevron);
    root.append(triggerBtn);
    root.append(el("span", {
      class: "rui-multiselect-status",
      role: "status",
      style: "position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0 0 0 0);white-space:nowrap"
    }, [selectionSummary]));
    const panel = el("div", {
      class: "rui-multiselect-panel",
      id: panelId2,
      role: "listbox",
      "aria-multiselectable": "true",
      "aria-busy": loading ? "true" : null
    });
    const filterInput = el("input", {
      type: "text",
      class: "rui-multiselect-filter",
      // See the Combobox filter: an id is what lets element.ts recover focus and
      // caret when a render replaces the field around the panel (D0337).
      id: id ? `${id}-filter` : null,
      placeholder: "Filter…",
      autocomplete: "off",
      value: filterSlot.get(),
      "aria-label": "Filter options",
      "aria-controls": panelId2,
      "aria-autocomplete": "list"
    });
    panel.append(filterInput);
    const list = el("div", { class: "rui-multiselect-list" });
    panel.append(list);
    const renderList = (target, filter) => {
      target.replaceChildren();
      if (loading) {
        target.append(el("div", { class: "rui-multiselect-loading" }, ["Loading…"]));
        return;
      }
      const query = filter.trim();
      const lower = query.toLowerCase();
      const matches = lower === "" || remoteSearch ? items : items.filter(
        (item) => item.label.toLowerCase().includes(lower) || item.value.toLowerCase().includes(lower)
      );
      const atCapAll = max > 0 && selected.length >= max;
      const canCreate = creatable && query !== "" && !atCapAll && !selectedSet.has(query) && !items.some((item) => item.value.toLowerCase() === lower || item.label.toLowerCase() === lower);
      if (matches.length === 0 && !canCreate) {
        target.append(el("div", { class: "rui-multiselect-empty" }, [emptyLabel]));
        return;
      }
      const active = activeSlot.get();
      let lastGroup;
      matches.forEach((item, index) => {
        if (item.group && item.group !== lastGroup) {
          target.append(el("div", {
            class: "rui-multiselect-group",
            role: "presentation"
          }, [item.group]));
          lastGroup = item.group;
        }
        const isSelected = selectedSet.has(item.value);
        const atCap = !isSelected && atCapAll;
        const option = el("button", {
          type: "button",
          class: "rui-multiselect-option",
          role: "option",
          id: optionDomId(index),
          // Driven by `aria-activedescendant`, so not individually tabbable.
          tabindex: "-1",
          "data-value": item.value,
          "data-selected": isSelected ? "true" : "false",
          "data-active": index === active ? "true" : null,
          "data-disabled": item.disabled ? "true" : null,
          // `aria-disabled` rather than the `disabled` attribute for an option
          // the author marked unavailable: a listbox option must stay
          // arrow-reachable and be announced as unavailable, which `disabled`
          // prevents. The selection cap keeps `disabled` — that row is not
          // unavailable in itself, it is the "no room left" state of a
          // still-selectable option.
          "aria-disabled": item.disabled ? "true" : null,
          "aria-selected": isSelected ? "true" : "false",
          disabled: atCap ? "" : null
        });
        const checkbox = el("span", { class: "rui-multiselect-option-check" });
        const checkIcon = renderIcon(isSelected ? "check" : "", { className: "rui-multiselect-option-check-icon" });
        if (checkIcon) checkbox.append(checkIcon);
        option.append(checkbox);
        option.append(el("span", { class: "rui-multiselect-option-label" }, [item.label]));
        if (!item.disabled) {
          option.onclick = (event) => {
            event.stopPropagation();
            if (atCap) return;
            const next = isSelected ? selected.filter((v) => v !== item.value) : [...selected, item.value];
            writeSelection(event.currentTarget, next);
          };
        }
        target.append(option);
      });
      if (canCreate) {
        const create = el("button", {
          type: "button",
          class: "rui-multiselect-option rui-multiselect-create",
          role: "option",
          id: optionDomId(matches.length),
          tabindex: "-1",
          "data-value": query,
          "data-create": "true",
          "data-active": matches.length === active ? "true" : null,
          "aria-selected": "false"
        }, [`Create “${query}”`]);
        create.onclick = (event) => {
          event.stopPropagation();
          writeSelection(event.currentTarget, [...selected, query]);
        };
        target.append(create);
      }
    };
    renderList(list, filterSlot.get());
    const optionsIn = (scope) => Array.from(scope.querySelectorAll(".rui-multiselect-option[data-value]"));
    const setActiveOption = (panelEl, index, filterEl) => {
      const options = optionsIn(panelEl);
      if (options.length === 0) return;
      const clamped = (index % options.length + options.length) % options.length;
      activeSlot.set(clamped);
      options.forEach((option, i) => {
        if (i !== clamped) {
          option.removeAttribute("data-active");
          return;
        }
        option.setAttribute("data-active", "true");
        if (option.id) filterEl?.setAttribute("aria-activedescendant", option.id);
        if (typeof option.scrollIntoView === "function") option.scrollIntoView({ block: "nearest" });
      });
    };
    filterInput.oninput = (event) => {
      const target = event.currentTarget;
      filterSlot.set(target.value);
      activeSlot.set(-1);
      target.removeAttribute("aria-activedescendant");
      search.schedule(target.value);
      const liveList = target.closest(".rui-multiselect-panel")?.querySelector(".rui-multiselect-list");
      if (liveList) renderList(liveList, target.value);
    };
    filterInput.onkeydown = (event) => {
      const e = event;
      const target = e.currentTarget;
      const panelEl = target.closest(".rui-multiselect-panel");
      if (!panelEl) return;
      const active = activeSlot.get();
      if (e.key === "ArrowDown") {
        e.preventDefault();
        setActiveOption(panelEl, active + 1, target);
        return;
      }
      if (e.key === "ArrowUp") {
        e.preventDefault();
        setActiveOption(panelEl, active <= 0 ? optionsIn(panelEl).length - 1 : active - 1, target);
        return;
      }
      if (e.key === "Home") {
        e.preventDefault();
        setActiveOption(panelEl, 0, target);
        return;
      }
      if (e.key === "End") {
        e.preventDefault();
        setActiveOption(panelEl, optionsIn(panelEl).length - 1, target);
        return;
      }
      if (e.key === "Enter" || e.key === " ") {
        const chosen = active >= 0 ? optionsIn(panelEl)[active] : null;
        if (!chosen) return;
        e.preventDefault();
        chosen.click();
        return;
      }
      if (e.key === "Escape") {
        e.preventDefault();
        const live = target.closest(".rui-multiselect");
        openSlot.set(false);
        helpers.invoke(props.onOpenChange, false);
        search.cancel();
        filterSlot.set("");
        activeSlot.set(-1);
        live?.querySelector(".rui-multiselect-trigger")?.focus();
        if (!live) return;
        live.setAttribute("data-open", "false");
        live.querySelector(".rui-multiselect-trigger")?.setAttribute("aria-expanded", "false");
        closeFloating(live.querySelector(MULTISELECT_PANEL));
        disposeDismissListeners(live);
      }
    };
    triggerBtn.onclick = (event) => {
      if (disabled) return;
      event.stopPropagation();
      const next = !openSlot.get();
      openSlot.set(next);
      helpers.invoke(props.onOpenChange, next);
      const live = event.currentTarget.closest(".rui-multiselect");
      live?.setAttribute("data-open", next ? "true" : "false");
      live?.querySelector(".rui-multiselect-trigger")?.setAttribute("aria-expanded", next ? "true" : "false");
      if (!live) return;
      syncFloatingPanel(live, next, MULTISELECT_PANEL, MULTISELECT_TRIGGER, PICKER_FLOATING);
      if (!next) {
        search.cancel();
        disposeDismissListeners(live);
        return;
      }
      activeSlot.set(-1);
      const cancelFocus = deferToPaint(() => {
        const target = live.querySelector(".rui-multiselect-filter") ?? (filterInput.isConnected ? filterInput : null);
        target?.focus();
      });
      helpers.registerDisposer(cancelFocus, "multiselect-focus");
      installDismissListeners({
        liveRoot: live,
        onDismiss: () => {
          openSlot.set(false);
          helpers.invoke(props.onOpenChange, false);
          search.cancel();
          filterSlot.set("");
          activeSlot.set(-1);
          live.setAttribute("data-open", "false");
          live.querySelector(".rui-multiselect-trigger")?.setAttribute("aria-expanded", "false");
          closeFloating(live.querySelector(MULTISELECT_PANEL));
        }
      });
    };
    triggerBtn.onkeydown = (event) => {
      const e = event;
      if (disabled) return;
      if (e.target !== e.currentTarget) return;
      const trigger = e.currentTarget;
      const open = trigger.getAttribute("aria-expanded") === "true";
      if (e.key === "Enter" || e.key === " ") {
        e.preventDefault();
        trigger.click();
        return;
      }
      if (e.key === "ArrowDown" && !open) {
        e.preventDefault();
        trigger.click();
        return;
      }
      if (e.key === "Escape" && open) {
        e.preventDefault();
        trigger.click();
      }
    };
    root.append(panel);
    if (isOpen) positionPickerOnMount(root, MULTISELECT_PANEL, MULTISELECT_TRIGGER);
    const shell = withFieldShell(root, props, { idKey: "id" });
    relocateControlAria(root, triggerBtn);
    return shell;
  }
};
const DateRangePicker = {
  name: "DateRangePicker",
  description: "Paired date inputs with a single label, sharing the same min/max range. Pass `$variable` references for both `from` and `to` to two-way-bind a date range (ISO `YYYY-MM-DD` strings). The endpoints bound each other, so the range cannot be inverted. Pass `error`/`required` for a mandatory reporting period, and `locale` (`de-DE`, `en-GB`) to echo the chosen period in that locale's date order.",
  props: [
    { name: "id", type: "string" },
    { name: "from", type: "string", optional: true, description: "ISO date start; typically $variable" },
    { name: "to", type: "string", optional: true, description: "ISO date end; typically $variable" },
    { name: "label", type: "string", optional: true },
    { name: "min", type: "string", optional: true, description: "Earliest selectable ISO date" },
    { name: "max", type: "string", optional: true, description: "Latest selectable ISO date" },
    { name: "disabled", type: "boolean", optional: true },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"], description: "Called with `{from, to}` whenever either endpoint changes" },
    { name: "hint", type: "string", optional: true, description: "Helper text rendered below the pair" },
    { name: "error", type: "string", optional: true, description: "Validation error rendered below the pair (marks it invalid)" },
    { name: "required", type: "boolean", optional: true, description: "Mark both endpoints required" },
    { name: "onBlur", type: "callable", optional: true, aliases: ["onblur"], description: "Called with the current value when focus leaves an endpoint (validate-on-blur)" },
    { name: "onFocus", type: "callable", optional: true, aliases: ["onfocus"], description: "Called when an endpoint gains focus" },
    { name: "locale", type: "string", optional: true, description: "BCP-47 tag (`de-DE`, `en-GB`) the chosen period is echoed in beneath the pair, and the language both endpoints are announced in. Same `locale` channel as `Table`/`Col`. Bound values stay ISO `YYYY-MM-DD`." },
    ...fieldShellExtraProps()
  ],
  render: (node, props, helpers) => {
    const id = asString(props.id);
    const fromId = `${id}-from`;
    const toId = `${id}-to`;
    const locale = localeTag$1(props.locale);
    const min = asString(props.min);
    const max = asString(props.max);
    const from = asString(props.from);
    const to = asString(props.to);
    const disabled = asBoolean(props.disabled);
    const label = asString(props.label);
    const root = el("div", {
      class: "rui-date-range-picker",
      "data-disabled": disabled ? "true" : "false",
      // The pair is one control; naming the group is what tells a screen-reader
      // user which range the two anonymous date fields belong to.
      role: "group",
      "aria-label": label || null
    });
    if (label) {
      root.append(el("label", {
        class: "rui-date-range-picker-label",
        id: `${id}-label`,
        for: fromId
      }, [label]));
      root.setAttribute("aria-labelledby", `${id}-label`);
      root.removeAttribute("aria-label");
    }
    const row = el("div", { class: "rui-date-range-picker-row" });
    const fromInput = el("input", {
      type: "date",
      class: "rui-date-range-picker-input",
      id: fromId,
      name: fromId,
      value: valueAttr(props.from),
      min: min || null,
      // Bound by the chosen end date: identical min/max on both inputs let the
      // user pick to < from and reported that inverted pair through onChange.
      max: to || max || null,
      disabled: disabled ? "" : null,
      "data-role": "from",
      // The single label points at `from` only, and the visual "–" separator is
      // aria-hidden, so without these the `to` field is announced as an
      // anonymous "date" with nothing to say it is the end of a range.
      "aria-label": label ? `${label} start` : "Start date",
      // Which language the endpoint's date is written in — see DatePicker.
      lang: locale ?? null
    });
    row.append(fromInput);
    row.append(el("span", { class: "rui-date-range-picker-separator", "aria-hidden": "true" }, ["–"]));
    const toInput = el("input", {
      type: "date",
      class: "rui-date-range-picker-input",
      id: toId,
      name: toId,
      value: valueAttr(props.to),
      min: from || min || null,
      max: max || null,
      disabled: disabled ? "" : null,
      "data-role": "to",
      "aria-label": label ? `${label} end` : "End date",
      lang: locale ?? null
    });
    row.append(toInput);
    root.append(row);
    const fromState = node.argMeta?.[1]?.stateRef;
    const toState = node.argMeta?.[2]?.stateRef;
    if (fromState) helpers.bindState(fromInput, fromState);
    if (toState) helpers.bindState(toInput, toState);
    const readRange = (target) => {
      const wrapper = target.closest(".rui-date-range-picker");
      const fromValue = wrapper?.querySelector('.rui-date-range-picker-input[data-role="from"]')?.value ?? "";
      const toValue = wrapper?.querySelector('.rui-date-range-picker-input[data-role="to"]')?.value ?? "";
      return { from: fromValue, to: toValue };
    };
    const onEndpointChange = (event) => {
      const target = event.currentTarget ?? event.target;
      if (!target) return;
      const wrapper = target.closest(".rui-date-range-picker");
      const liveFrom = wrapper?.querySelector('.rui-date-range-picker-input[data-role="from"]');
      const liveTo = wrapper?.querySelector('.rui-date-range-picker-input[data-role="to"]');
      if (liveFrom && liveTo) {
        liveTo.min = liveFrom.value || min;
        liveFrom.max = liveTo.value || max;
      }
      helpers.invoke(props.onChange, readRange(target));
    };
    composeHandler(fromInput, "onchange", onEndpointChange);
    composeHandler(toInput, "onchange", onEndpointChange);
    if (locale) {
      const periodText = (f, t) => {
        const start = formatDateInLocale(f, locale);
        const end = formatDateInLocale(t, locale);
        return start && end ? `${start} – ${end}` : start || end;
      };
      root.append(el("span", {
        class: "rui-date-range-picker-readout",
        // Same values both endpoints already announce — see DatePicker.
        "aria-hidden": "true"
      }, [periodText(from, to)]));
      const syncReadout = (liveRoot) => periodText(
        // By `data-role`, not by document order: the separator and any future
        // chrome between the two fields must not be able to reverse the period.
        liveRoot.querySelector('.rui-date-range-picker-input[data-role="from"]')?.value ?? "",
        liveRoot.querySelector('.rui-date-range-picker-input[data-role="to"]')?.value ?? ""
      );
      wireDateReadout(fromInput, ".rui-date-range-picker", ".rui-date-range-picker-readout", syncReadout);
      wireDateReadout(toInput, ".rui-date-range-picker", ".rui-date-range-picker-readout", syncReadout);
    }
    attachFocusHandlers(fromInput, props, helpers);
    attachFocusHandlers(toInput, props, helpers);
    const shell = withFieldShell(root, { ...props, label: null }, { idKey: "id" });
    relocateControlAria(root, fromInput);
    for (const attr of ["required", "aria-invalid", "aria-describedby"]) {
      const value = fromInput.getAttribute(attr);
      if (value !== null) toInput.setAttribute(attr, value);
    }
    return shell;
  }
};
function clampNumber(value, min, max) {
  if (Number.isNaN(value)) return min === Number.NEGATIVE_INFINITY ? 0 : min;
  return Math.min(Math.max(value, min), max);
}
function renderSearchableSelect(node, props, helpers) {
  const meta = node.argMeta ? [...node.argMeta] : [];
  while (meta.length < 5) meta.push({});
  if (meta[4]?.stateRef) meta[2] = meta[4];
  const rendered = Combobox.render(
    { ...node, argMeta: meta },
    {
      id: props.id,
      items: props.items,
      value: props.value,
      placeholder: props.placeholder,
      emptyLabel: asString(props.emptyLabel) || "No matches",
      // Every prop the author declared has to reach the Combobox or it is
      // silently dropped: `onChange` never fired (so a dependent Select stayed
      // empty), and the label / required marker / error text vanished the moment
      // `searchable: true` was added. Toggling the flag off made all five
      // reappear, which sent authors hunting for a data problem.
      disabled: props.disabled,
      onChange: props.onChange,
      label: props.label,
      hint: props.hint,
      error: props.error,
      required: props.required,
      loading: props.loading,
      // Without this a `searchable` Select could only ever filter the options it
      // was handed, so a picker over anything larger than one state cell (users,
      // SKUs, repos) had to be a Combobox instead.
      onSearch: props.onSearch,
      // The non-searchable branch wires these through `attachFocusHandlers`, so
      // dropping them here made validate-on-blur work on a Select right up until
      // someone added `searchable: true`.
      onBlur: props.onBlur,
      onFocus: props.onFocus,
      // Same class of bug as the five above: a label the author had hidden
      // reappeared the moment the Select became searchable.
      labelHidden: props.labelHidden
    },
    helpers
  );
  const comboRoot = rendered.classList.contains("rui-combobox") ? rendered : rendered.querySelector(".rui-combobox") ?? rendered;
  comboRoot.classList.add("rui-select-searchable");
  return rendered;
}
function bindToStateAtArg(element, node, argIndex, helpers) {
  const stateName = node.argMeta?.[argIndex]?.stateRef;
  if (!stateName) return;
  if (element instanceof HTMLInputElement && element.type === "checkbox") {
    helpers.bindState(element, stateName, {
      event: "change",
      getValue: (n) => n.checked
    });
    return;
  }
  helpers.bindState(element, stateName);
}
function applyValidations(input, validations, hasExplicitType = false) {
  if (!validations) return;
  const list = Array.isArray(validations) ? validations.map((v) => String(v)) : typeof validations === "object" ? Object.entries(validations).map(([k, v]) => v ? `${k}:${v}` : k) : [];
  const positiveInt = (raw, hint) => {
    const n = Number(raw);
    if (!Number.isFinite(n) || n <= 0) {
      console.warn(`[aktion] Input validations: "${hint}" is not a positive number — ignored.`);
      return null;
    }
    return Math.floor(n);
  };
  for (const v of list) {
    if (v === "required") {
      input.required = true;
    } else if (v.startsWith("minLength:")) {
      const n = positiveInt(v.slice("minLength:".length), v);
      if (n !== null) input.minLength = n;
    } else if (v.startsWith("maxLength:")) {
      const n = positiveInt(v.slice("maxLength:".length), v);
      if (n !== null) input.maxLength = n;
    } else if (v.startsWith("pattern:")) {
      const pattern = v.slice("pattern:".length);
      if (pattern) input.pattern = pattern;
    } else if (v.startsWith("min:")) {
      input.min = v.slice("min:".length);
    } else if (v.startsWith("max:")) {
      input.max = v.slice("max:".length);
    } else if (v === "email") {
      if (!hasExplicitType) input.type = "email";
    } else {
      console.warn(`[aktion] Input validations: unrecognised hint "${v}" — ignored.`);
    }
  }
}
const SURFACE_TONES$1 = ["default", "primary", "success", "warning", "danger", "info"];
const FOCUSABLE_SELECTOR = "a[href], button:not([disabled]), input:not([disabled]), select:not([disabled]), textarea:not([disabled]), [tabindex]:not([tabindex='-1'])";
const renderActionsRow = (raw, helpers) => {
  const items = asArray(raw);
  if (items.length === 0) return null;
  const row = el("div", { class: "rui-pattern-actions" });
  for (const item of items) row.append(helpers.renderNode(item));
  return row;
};
const closestFrom = (target, selector) => {
  const node = target instanceof Element ? target : null;
  return node?.closest(selector) ?? null;
};
const routeHref = (path) => "#" + (path.startsWith("/") ? path : "/" + path);
const asRecord$1 = (value) => value !== null && typeof value === "object" && !Array.isArray(value) && !isComponentNode(value) ? value : null;
const activateOnKey = (invoke) => (event) => {
  if (event.key !== "Enter" && event.key !== " ") return;
  event.preventDefault();
  invoke();
};
const HERO_EYEBROW_RULES = [
  { match: /\bbeta\b/i, label: "Beta" },
  { match: /\b(early\s?access|preview)\b/i, label: "Preview" },
  { match: /\b(introduc(?:ing|e)|launch(?:ed|ing)?|announcing)\b/i, label: "Introducing" },
  { match: /\b(welcome\b|get(?:ting)?\s?started)/i, label: "Welcome" },
  { match: /\b(new\b|whats\s?new|now\b)/i, label: "What's new" },
  { match: /\bfree\b/i, label: "Free trial" },
  { match: /\bupgrade\b/i, label: "Upgrade" },
  { match: /\b(sale|discount|deal)\b/i, label: "Limited time" }
];
function deriveHeroEyebrow(title, subtitle) {
  const haystack = `${title} ${subtitle}`.trim();
  if (!haystack) return "";
  for (const rule of HERO_EYEBROW_RULES) {
    if (rule.match.test(haystack)) return rule.label;
  }
  return "";
}
const COVER_OVERLAY_DEFAULT = 0.62;
function resolveOverlay(raw) {
  if (raw === void 0 || raw === null || raw === "") return COVER_OVERLAY_DEFAULT;
  const value = asNumber(raw, COVER_OVERLAY_DEFAULT);
  if (!Number.isFinite(value)) return COVER_OVERLAY_DEFAULT;
  return Math.max(0, Math.min(1, value > 1 ? value / 100 : value));
}
function renderHeroHighlights(raw, layout) {
  const highlights = asArray(raw);
  if (highlights.length === 0) return null;
  const isCover = layout === "cover";
  const tags = el("div", {
    class: isCover ? "rui-hero-highlights rui-cover-highlights" : "rui-hero-highlights"
  });
  let rendered = 0;
  for (const h of highlights) {
    const label = asString(h);
    if (!label) continue;
    rendered += 1;
    tags.append(el("span", {
      class: isCover ? "rui-hero-highlight rui-cover-highlight" : "rui-hero-highlight"
    }, [label]));
  }
  return rendered > 0 ? tags : null;
}
const Hero = {
  name: "Hero",
  description: 'Eye-catching landing/marketing header with eyebrow tag, title, subtitle, optional bullet highlights, and primary/secondary CTA buttons. Use `layout="cover"` with `imageSrc` for an image-backed hero band (pass `height`, optional `caption`, and `overlay` to tune the scrim). Default layout shows an optional side illustration. `align="center"` centers the text block.',
  props: [
    { name: "title", type: "string" },
    { name: "subtitle", type: "string", optional: true },
    { name: "primary", type: "Button", optional: true, description: "Primary CTA — pass a Button(...)" },
    { name: "secondary", type: "Button", optional: true, description: "Secondary CTA — pass a Button(...)" },
    { name: "eyebrow", type: "string", optional: true, description: "Short uppercase tag above the title" },
    { name: "highlights", type: "string[]", optional: true, description: "Bullet items rendered as tag pills" },
    { name: "imageSrc", type: "string", optional: true, description: "Illustration or cover background when layout=cover" },
    { name: "caption", type: "string", optional: true, description: "Small caption above CTAs (cover layout)" },
    { name: "height", type: "string", optional: true, description: "Min-height for cover layout (default 280px)" },
    { name: "actions", type: "Node[]", optional: true, description: "CTA row (cover layout; alternative to primary/secondary)" },
    { name: "layout", type: "string", optional: true, enum: ["default", "cover"], description: "default = text-first; cover = image-backed band" },
    { name: "tone", aliases: ["variant"], type: "string", optional: true, enum: SURFACE_TONES$1, description: "Accent tone" },
    { name: "overlay", type: "string | number", optional: true, description: "Cover scrim strength — 0–1 alpha or 0–100 percent (default 0.62). Use a low value over a light photo, `0` for none." },
    { name: "align", type: "string", optional: true, enum: ["start", "center"], description: "Text alignment inside the band (default start)" }
  ],
  render: (_node, props, helpers) => {
    const layout = asString(props.layout, "default");
    const heroTitle = asString(props.title);
    const heroSubtitle = asString(props.subtitle);
    const explicitEyebrow = asString(props.eyebrow);
    const eyebrow = explicitEyebrow || deriveHeroEyebrow(heroTitle, heroSubtitle);
    const tone = asString(props.tone, "primary");
    const align = asString(props.align, "start");
    if (layout === "cover") {
      const safeImageSrc = sanitiseCssUrl(asString(props.imageSrc));
      const safeHeight = sanitiseCssLength(asString(props.height), "280px");
      const overlay = resolveOverlay(props.overlay);
      const layers = [];
      if (overlay > 0) {
        const top = (overlay * 0.08).toFixed(3);
        layers.push(`linear-gradient(180deg, rgba(15, 23, 42, ${top}) 0%, rgba(15, 23, 42, ${overlay.toFixed(3)}) 100%)`);
      }
      if (safeImageSrc) layers.push(`url("${safeImageSrc}")`);
      const root2 = el("section", {
        class: "rui-cover",
        "data-tone": tone,
        "data-align": align,
        style: `${layers.length > 0 ? `background-image:${layers.join(", ")};` : ""}min-height:${safeHeight};`
      });
      const body2 = el("div", { class: "rui-cover-body" });
      if (eyebrow) body2.append(el("span", { class: "rui-cover-eyebrow" }, [eyebrow]));
      body2.append(el("h1", { class: "rui-cover-title" }, [heroTitle]));
      if (heroSubtitle) body2.append(el("p", { class: "rui-cover-subtitle" }, [heroSubtitle]));
      const coverHighlights = renderHeroHighlights(props.highlights, "cover");
      if (coverHighlights) body2.append(coverHighlights);
      const caption = asString(props.caption);
      if (caption) body2.append(el("p", { class: "rui-cover-caption" }, [caption]));
      const actions = renderActionsRow(props.actions, helpers);
      if (actions) {
        actions.classList.add("rui-cover-actions");
        body2.append(actions);
      } else {
        const ctaItems2 = [props.primary, props.secondary].filter(Boolean);
        if (ctaItems2.length > 0) {
          const ctas = el("div", { class: "rui-cover-actions rui-pattern-actions" });
          for (const cta of ctaItems2) ctas.append(helpers.renderNode(cta));
          body2.append(ctas);
        }
      }
      root2.append(body2);
      return root2;
    }
    const heroImageSrc = sanitiseImageSrc(props.imageSrc);
    const root = el("section", {
      class: "rui-hero",
      "data-tone": tone,
      "data-align": align,
      "data-has-image": heroImageSrc ? "true" : "false"
    });
    const body = el("div", { class: "rui-hero-body" });
    if (eyebrow) body.append(el("span", { class: "rui-hero-eyebrow" }, [eyebrow]));
    body.append(el("h1", { class: "rui-hero-title" }, [heroTitle]));
    if (heroSubtitle) body.append(el("p", { class: "rui-hero-subtitle" }, [heroSubtitle]));
    const highlights = renderHeroHighlights(props.highlights, "default");
    if (highlights) body.append(highlights);
    const ctaItems = [props.primary, props.secondary].filter(Boolean);
    if (ctaItems.length > 0) {
      const ctas = el("div", { class: "rui-hero-ctas" });
      for (const cta of ctaItems) ctas.append(helpers.renderNode(cta));
      body.append(ctas);
    }
    root.append(body);
    if (heroImageSrc) {
      const media = el("div", { class: "rui-hero-media" });
      media.append(el("img", { src: heroImageSrc, alt: "", loading: "lazy" }));
      root.append(media);
    }
    return root;
  }
};
function renderCrumb$1(raw, index, isLast, onCrumbClick, helpers) {
  const record = asRecord$1(raw);
  const label = asString(record ? record.label ?? record.title : raw);
  const to = asString(record ? record.to ?? record.href ?? record.path : "");
  const interactive = !isLast && (to !== "" || typeof onCrumbClick === "function");
  if (!interactive) {
    return el("span", {
      class: "rui-page-header-crumb",
      "aria-current": isLast ? "page" : null
    }, [label]);
  }
  const path = to ? to.startsWith("/") ? to : "/" + to : "";
  const anchor = el("a", {
    class: "rui-page-header-crumb",
    href: path ? routeHref(path) : "#"
  }, [label]);
  anchor.onclick = (event) => {
    if (event.defaultPrevented || event.button !== 0) return;
    if (event.metaKey || event.ctrlKey || event.shiftKey || event.altKey) return;
    event.preventDefault();
    if (path) helpers.router.navigate(path);
    helpers.invoke(onCrumbClick, label, index);
  };
  return anchor;
}
const PageHeader = {
  name: "PageHeader",
  description: 'Page-level header with breadcrumbs, title, subtitle, status tag, and a right-aligned actions row. The canonical first child for any dashboard, settings, or detail page — replaces ad-hoc Stack+Header+Buttons stitching. If `breadcrumbs` is omitted the component auto-derives `["Home", title]` so the page never lacks a trail. Pass `breadcrumbs=false` to opt out.',
  props: [
    { name: "title", type: "string" },
    { name: "subtitle", type: "string", optional: true },
    { name: "breadcrumbs", type: "string[] | {label, to}[] | Breadcrumb | false", optional: true, description: "Array of strings or `{label, to}` objects (a `to` path renders a router link), a Breadcrumb(...) node, or `false` to suppress the auto-derived trail" },
    { name: "actions", type: "Node[]", optional: true, description: "Buttons / NavLinks shown on the right" },
    { name: "status", type: "Badge", optional: true, aliases: ["badge"], description: "Optional Badge(...) rendered next to the title" },
    { name: "onCrumbClick", type: "callable", optional: true, description: "Called with (label, index) when a breadcrumb is clicked — makes string crumbs interactive" }
  ],
  render: (_node, props, helpers) => {
    const root = el("header", { class: "rui-page-header" });
    const title = asString(props.title);
    let crumbs = props.breadcrumbs;
    if (crumbs === void 0 || crumbs === null) {
      if (title) crumbs = ["Home", title];
    } else if (crumbs === false || crumbs === "false") {
      crumbs = null;
    }
    if (crumbs) {
      const crumbWrap = el("nav", { class: "rui-page-header-breadcrumbs", "aria-label": "Breadcrumb" });
      if (Array.isArray(crumbs)) {
        const lastIndex = crumbs.length - 1;
        crumbs.forEach((c, i) => {
          if (i > 0) {
            crumbWrap.append(el("span", { class: "rui-page-header-crumb-sep", "aria-hidden": "true" }, ["/"]));
          }
          if (isComponentNode(c)) {
            crumbWrap.append(helpers.renderNode(c));
            return;
          }
          crumbWrap.append(renderCrumb$1(c, i, i === lastIndex, props.onCrumbClick, helpers));
        });
      } else {
        crumbWrap.append(helpers.renderNode(crumbs));
      }
      root.append(crumbWrap);
    }
    const titleRow = el("div", { class: "rui-page-header-title-row" });
    const titleBlock = el("div", { class: "rui-page-header-title-block" });
    const titleLine = el("div", { class: "rui-page-header-title-line" });
    titleLine.append(el("h1", { class: "rui-page-header-title" }, [title]));
    if (props.status) titleLine.append(helpers.renderNode(props.status));
    titleBlock.append(titleLine);
    const subtitle = asString(props.subtitle);
    if (subtitle) titleBlock.append(el("p", { class: "rui-page-header-subtitle" }, [subtitle]));
    titleRow.append(titleBlock);
    const actions = renderActionsRow(props.actions, helpers);
    if (actions) {
      actions.classList.add("rui-page-header-actions");
      titleRow.append(actions);
    }
    root.append(titleRow);
    return root;
  }
};
const EmptyState = {
  name: "EmptyState",
  description: "Zero-state placeholder for empty lists, searches, dashboards. Renders a centered icon (or illustration), title, description, and either a single `action` Button or an `actions` row (primary + secondary). Always preferable to an empty Card with raw text.",
  props: [
    { name: "title", type: "string" },
    { name: "description", type: "string", optional: true },
    { name: "icon", type: "string", optional: true, description: 'Font Awesome icon name (defaults to "inbox")' },
    { name: "illustration", type: "string", optional: true, description: "Image URL — takes precedence over `icon` when provided" },
    { name: "action", type: "Button", optional: true, description: "Single CTA (legacy slot)" },
    { name: "actions", type: "Node[]", optional: true, description: "Row of CTA Buttons / Links — preferred over `action` for primary + secondary affordances" }
  ],
  render: (_node, props, helpers) => {
    const root = el("div", { class: "rui-empty-state" });
    const illustration = sanitiseImageSrc(props.illustration);
    if (illustration) {
      root.append(el("img", {
        class: "rui-empty-state-illustration",
        src: illustration,
        alt: "",
        loading: "lazy"
      }));
    } else {
      const title = asString(props.title);
      const description = asString(props.description);
      const iconName = asString(props.icon) || pickIconForLabel(`${title} ${description}`) || "inbox";
      const iconNode = renderIcon(iconName, { className: "rui-empty-state-icon" });
      if (iconNode) root.append(iconNode);
    }
    root.append(el("h3", { class: "rui-empty-state-title" }, [asString(props.title)]));
    const desc = asString(props.description);
    if (desc) root.append(el("p", { class: "rui-empty-state-description" }, [desc]));
    const actions = asArray(props.actions);
    if (actions.length > 0) {
      const row = el("div", { class: "rui-empty-state-actions" });
      for (const item of actions) row.append(helpers.renderNode(item));
      root.append(row);
    } else if (props.action) {
      const wrap = el("div", { class: "rui-empty-state-action" });
      wrap.append(helpers.renderNode(props.action));
      root.append(wrap);
    }
    return root;
  }
};
const isIsoLike = (value) => /^\d{4}-\d{2}-\d{2}([T ]|$)/.test(value);
const TimelineItem = {
  name: "TimelineItem",
  description: "Single event on a Timeline. Pass `content` for rich children (Badge, Link, Button) beside the plain-text `description`, and `href`/`onClick` to make the row navigable.",
  props: [
    { name: "title", type: "string" },
    { name: "time", type: "string", optional: true, description: "Display label (ISO, relative, etc.)" },
    { name: "description", type: "string", optional: true },
    { name: "icon", type: "string", optional: true, description: "Font Awesome icon name rendered inside the marker" },
    { name: "tone", aliases: ["variant"], type: "string", optional: true, enum: SURFACE_TONES$1 },
    { name: "content", type: "Node[]", optional: true, description: "Rich children below the description (Badge, Link, Button, …)" },
    { name: "href", type: "string", optional: true, description: "Turns the title into a link (changelog entry → PR, commit, release)" },
    { name: "onClick", type: "callable", optional: true, aliases: ["action", "onclick"], description: "Called when the row is clicked" }
  ],
  render: (_node, props, helpers) => {
    const href = asString(props.href) ? sanitiseHref(props.href) : "";
    const rowClickable = !href && typeof props.onClick === "function";
    const li = el("li", {
      class: "rui-timeline-item",
      "data-tone": asString(props.tone, "default"),
      "data-interactive": href || rowClickable ? "true" : null
    });
    const marker = el("span", { class: "rui-timeline-marker" });
    const iconNode = renderIcon(props.icon);
    if (iconNode) marker.append(iconNode);
    li.append(marker);
    const body = el("div", {
      class: "rui-timeline-body",
      role: rowClickable ? "button" : null,
      tabindex: rowClickable ? "0" : null
    });
    const head = el("div", { class: "rui-timeline-head" });
    const title = asString(props.title);
    if (href) {
      const link = el("a", { class: "rui-timeline-title", href }, [title]);
      if (typeof props.onClick === "function") {
        link.onclick = () => helpers.invoke(props.onClick);
      }
      head.append(link);
    } else {
      head.append(el("span", { class: "rui-timeline-title" }, [title]));
    }
    const time = asString(props.time);
    if (time) {
      head.append(el("time", {
        class: "rui-timeline-time",
        datetime: isIsoLike(time) ? time : null
      }, [time]));
    }
    body.append(head);
    const desc = asString(props.description);
    if (desc) body.append(el("div", { class: "rui-timeline-description" }, [desc]));
    const content = asArray(props.content);
    if (content.length > 0) {
      const extra = el("div", { class: "rui-timeline-content" });
      for (const child of content) extra.append(helpers.renderNode(child));
      body.append(extra);
    }
    if (rowClickable) {
      const fire = () => helpers.invoke(props.onClick);
      body.onclick = fire;
      body.onkeydown = activateOnKey(fire);
    }
    li.append(body);
    return li;
  }
};
const Timeline = {
  name: "Timeline",
  description: "Vertical event timeline. Children must be TimelineItem entries. Ideal for activity feeds, changelogs, and process flows.",
  props: [{ name: "items", type: "TimelineItem[]" }],
  render: (_node, props, helpers) => {
    const root = el("ol", { class: "rui-timeline" });
    for (const item of asArray(props.items)) root.append(helpers.renderNode(item));
    return root;
  }
};
const FeatureItem = {
  name: "FeatureItem",
  description: "Single tile on a FeatureGrid. Pass `href` or `onClick` to make the whole tile a link/button — a category or capability tile is normally the way into that section.",
  props: [
    { name: "title", type: "string" },
    { name: "description", type: "string", optional: true },
    { name: "icon", type: "string", optional: true, description: "Font Awesome icon name shown in a colored disc" },
    { name: "tone", aliases: ["variant"], type: "string", optional: true, enum: SURFACE_TONES$1 },
    { name: "href", type: "string", optional: true, description: "Render the tile as a link to this URL" },
    { name: "onClick", type: "callable", optional: true, aliases: ["action", "onclick"], description: "Called when the tile is clicked" }
  ],
  render: (_node, props, helpers) => {
    const href = asString(props.href) ? sanitiseHref(props.href) : "";
    const clickable = typeof props.onClick === "function";
    const root = el(href ? "a" : "div", {
      class: "rui-feature-item",
      "data-tone": asString(props.tone, "primary"),
      href: href || null,
      "data-interactive": href || clickable ? "true" : null,
      role: !href && clickable ? "button" : null,
      tabindex: !href && clickable ? "0" : null
    });
    if (clickable) {
      const fire = () => helpers.invoke(props.onClick);
      root.onclick = fire;
      if (!href) root.onkeydown = activateOnKey(fire);
    }
    const iconName = asString(props.icon, "sparkles");
    const iconNode = renderIcon(iconName, { className: "rui-feature-icon" });
    if (iconNode) root.append(iconNode);
    root.append(el("h3", { class: "rui-feature-title" }, [asString(props.title)]));
    const desc = asString(props.description);
    if (desc) root.append(el("p", { class: "rui-feature-description" }, [desc]));
    return root;
  }
};
const FeatureGrid = {
  name: "FeatureGrid",
  description: "Responsive grid of FeatureItem tiles (typically 2–3 columns). Use to highlight product capabilities or page categories.",
  props: [
    { name: "items", type: "FeatureItem[]" },
    { name: "columns", type: "number", optional: true, description: "Preferred column count (default auto)" }
  ],
  render: (_node, props, helpers) => {
    const requested = props.columns == null ? 0 : Math.floor(asNumber(props.columns, 0));
    const columns = requested > 0 ? Math.max(1, Math.min(4, requested)) : 0;
    const root = el("div", {
      class: "rui-feature-grid",
      "data-columns": columns > 0 ? String(columns) : null
    });
    for (const item of asArray(props.items)) root.append(helpers.renderNode(item));
    return root;
  }
};
const Testimonial = {
  name: "Testimonial",
  description: "Quote card with author, role, and optional avatar.",
  props: [
    { name: "quote", type: "string" },
    { name: "author", type: "string", aliases: ["name"] },
    { name: "role", type: "string", optional: true },
    { name: "avatarSrc", type: "string", optional: true, aliases: ["src"] },
    { name: "rating", type: "number", optional: true, description: "0–5 stars" }
  ],
  render: (_node, props) => {
    const root = el("figure", { class: "rui-testimonial" });
    const rating = Math.max(0, Math.min(5, Math.round(asNumber(props.rating, 0))));
    if (rating > 0) {
      const stars = el("div", {
        class: "rui-testimonial-rating",
        role: "img",
        "aria-label": `Rated ${rating} out of 5`
      });
      for (let i = 0; i < 5; i += 1) {
        const filled = i < rating;
        const icon = renderIcon(filled ? "star" : "regular:star", { className: "rui-testimonial-rating-star" });
        if (icon) stars.append(icon);
      }
      root.append(stars);
    }
    root.append(el("blockquote", { class: "rui-testimonial-quote" }, [
      asString(props.quote)
    ]));
    const footer = el("figcaption", { class: "rui-testimonial-author" });
    const avatarSrc = sanitiseImageSrc(props.avatarSrc);
    if (avatarSrc) {
      footer.append(el("img", { class: "rui-testimonial-avatar", src: avatarSrc, alt: "" }));
    }
    const meta = el("div", { class: "rui-testimonial-meta" });
    meta.append(el("div", { class: "rui-testimonial-name" }, [asString(props.author)]));
    const role = asString(props.role);
    if (role) meta.append(el("div", { class: "rui-testimonial-role" }, [role]));
    footer.append(meta);
    root.append(footer);
    return root;
  }
};
const ProfileCard = {
  name: "ProfileCard",
  description: "Compact profile/user card with avatar, name, role, optional bio, social tags, and a row of action buttons. Use for team rosters, contributor lists, and contact panels.",
  props: [
    { name: "name", type: "string" },
    { name: "role", type: "string", optional: true },
    { name: "avatarSrc", type: "string", optional: true, aliases: ["src"], description: "Avatar image src; falls back to initials" },
    { name: "bio", type: "string", optional: true },
    { name: "tags", type: "string[]", optional: true },
    { name: "actions", type: "Node[]", optional: true, description: "Buttons to render at the bottom" }
  ],
  render: (_node, props, helpers) => {
    const root = el("div", { class: "rui-profile-card" });
    const header = el("div", { class: "rui-profile-card-header" });
    header.append(renderAvatar(asString(props.avatarSrc), asString(props.name), "lg", helpers));
    const meta = el("div", { class: "rui-profile-card-meta" });
    meta.append(el("h3", { class: "rui-profile-card-name" }, [asString(props.name)]));
    const role = asString(props.role);
    if (role) meta.append(el("p", { class: "rui-profile-card-role" }, [role]));
    header.append(meta);
    root.append(header);
    const bio = asString(props.bio);
    if (bio) root.append(el("p", { class: "rui-profile-card-bio" }, [bio]));
    const tags = asArray(props.tags);
    if (tags.length > 0) {
      const tagRow = el("div", { class: "rui-profile-card-tags" });
      for (const t of tags) {
        const label = asString(t);
        if (label) tagRow.append(el("span", { class: "rui-tag", "data-size": "sm" }, [
          el("span", { class: "rui-tag-label" }, [label])
        ]));
      }
      root.append(tagRow);
    }
    const actions = renderActionsRow(props.actions, helpers);
    if (actions) {
      actions.classList.add("rui-profile-card-actions");
      root.append(actions);
    }
    return root;
  }
};
const Comment = {
  name: "Comment",
  description: "Single comment / message bubble. Renders avatar, author, timestamp, body, and an optional row of toolbar buttons (reply, like, …).",
  props: [
    { name: "author", type: "string" },
    { name: "body", type: "Node[] | Node | string", aliases: ["text", "message"], description: "Comment text, or nodes — Markdown(...), Link(...), Code(...), a Stack of them" },
    { name: "time", type: "string", optional: true, description: "Relative or absolute timestamp" },
    { name: "avatarSrc", type: "string", optional: true, aliases: ["src"] },
    { name: "actions", type: "Node[]", optional: true }
  ],
  render: (_node, props, helpers) => {
    const root = el("article", { class: "rui-comment" });
    root.append(renderAvatar(asString(props.avatarSrc), asString(props.author), "md", helpers));
    const body = el("div", { class: "rui-comment-body" });
    const head = el("header", { class: "rui-comment-header" });
    head.append(el("span", { class: "rui-comment-author" }, [asString(props.author)]));
    const time = asString(props.time);
    if (time) head.append(el("span", { class: "rui-comment-time" }, [time]));
    body.append(head);
    const content = el("div", { class: "rui-comment-content" });
    if (isComponentNode(props.body) || Array.isArray(props.body)) {
      content.append(helpers.renderNode(props.body));
    } else {
      content.append(document.createTextNode(asString(props.body)));
    }
    body.append(content);
    const actions = renderActionsRow(props.actions, helpers);
    if (actions) {
      actions.classList.add("rui-comment-actions");
      body.append(actions);
    }
    root.append(body);
    return root;
  }
};
const Banner = {
  name: "Banner",
  description: "Full-width announcement banner. Use at the top of a page for promos, release notes, or downtime notices. Pass `dismissible: true` for a close button, or `href`/`onClick` to make the whole band a click target. For inline notices prefer Callout or Alert.",
  props: [
    { name: "title", type: "string" },
    { name: "message", type: "string", optional: true, aliases: ["description"] },
    { name: "action", type: "Button", optional: true },
    { name: "icon", type: "string", optional: true, description: "Font Awesome icon name" },
    { name: "tone", type: "string", optional: true, aliases: ["variant"], enum: SURFACE_TONES$1 },
    { name: "dismissible", type: "boolean", optional: true, aliases: ["closable"], description: "Show a close button that hides the banner" },
    { name: "onDismiss", type: "callable", optional: true, aliases: ["onClose"], description: "Called when the banner is dismissed (implies `dismissible`)" },
    { name: "href", type: "string", optional: true, description: "Make the whole banner a link (release notes → changelog)" },
    { name: "onClick", type: "callable", optional: true, aliases: ["onclick"], description: "Called when the banner itself is clicked" }
  ],
  render: (_node, props, helpers) => {
    const tone = asString(props.tone, "primary");
    const isAlert = tone === "danger" || tone === "warning";
    const href = asString(props.href) ? sanitiseHref(props.href) : "";
    const clickable = !href && typeof props.onClick === "function";
    const dismissible = asBoolean(props.dismissible) || props.onDismiss != null;
    const dismissed = helpers.useInstanceState("dismissed", false);
    const tag = href ? "a" : "aside";
    if (dismissible && dismissed.get()) {
      return el(tag, {
        class: "rui-banner",
        "data-tone": tone,
        "data-dismissed": "true",
        hidden: true,
        style: "display:none"
      });
    }
    const root = el(tag, {
      class: "rui-banner",
      "data-tone": tone,
      href: href || null,
      // `aria-live` rather than only `role`, so the announcement survives the
      // link/button root the interactive variants need.
      role: href ? null : clickable ? "button" : isAlert ? "alert" : "status",
      "aria-live": isAlert ? "assertive" : "polite",
      tabindex: clickable ? "0" : null
    });
    if (clickable) {
      const fire = () => helpers.invoke(props.onClick);
      root.onclick = fire;
      root.onkeydown = activateOnKey(fire);
    }
    const iconName = asString(props.icon) || pickIconForTone(tone) || "";
    const iconNode = renderIcon(iconName, { className: "rui-banner-icon" });
    if (iconNode) root.append(iconNode);
    const body = el("div", { class: "rui-banner-body" });
    body.append(el("strong", { class: "rui-banner-title" }, [asString(props.title)]));
    const msg = asString(props.message);
    if (msg) body.append(el("span", { class: "rui-banner-message" }, [msg]));
    root.append(body);
    if (props.action) {
      const wrap = el("div", { class: "rui-banner-action" });
      wrap.append(helpers.renderNode(props.action));
      if (href || clickable) wrap.onclick = (event) => event.stopPropagation();
      root.append(wrap);
    }
    if (dismissible) {
      const close = el("button", {
        class: "rui-banner-dismiss",
        type: "button",
        "aria-label": "Dismiss"
      });
      close.append(renderIcon("xmark", { className: "rui-banner-dismiss-icon" }) ?? document.createTextNode("×"));
      close.onclick = (event) => {
        event.stopPropagation();
        dismissed.set(true);
        const live = closestFrom(event.currentTarget ?? event.target, ".rui-banner");
        if (live) {
          live.setAttribute("data-dismissed", "true");
          live.hidden = true;
          live.style.display = "none";
        }
        helpers.invoke(props.onDismiss);
      };
      root.append(close);
    }
    return root;
  }
};
const KanbanCard = {
  name: "KanbanCard",
  description: "Single card on a Kanban board.",
  props: [
    { name: "title", type: "string" },
    { name: "description", type: "string", optional: true },
    { name: "tags", type: "string[]", optional: true },
    { name: "assignee", type: "string", optional: true, description: "Name shown next to avatar initials" },
    { name: "tone", type: "string", optional: true, aliases: ["variant"], enum: SURFACE_TONES$1 },
    { name: "icon", type: "string", optional: true, description: "Optional Font Awesome icon name shown beside the title" },
    { name: "onClick", type: "callable", optional: true, aliases: ["action", "onclick"], description: "Optional callable fired when the card is clicked" }
  ],
  render: (_node, props, helpers) => {
    const root = el("div", {
      class: "rui-kanban-card",
      "data-tone": asString(props.tone, "default")
    });
    if (typeof props.onClick === "function") {
      root.setAttribute("role", "button");
      root.setAttribute("tabindex", "0");
      root.onclick = () => helpers.invoke(props.onClick);
      root.onkeydown = (event) => {
        const e = event;
        if (e.key === "Enter" || e.key === " ") {
          e.preventDefault();
          helpers.invoke(props.onClick);
        }
      };
    }
    const titleEl = el("div", { class: "rui-kanban-card-title" });
    const iconNode = renderIcon(props.icon, { className: "rui-kanban-card-icon" });
    if (iconNode) titleEl.append(iconNode);
    titleEl.append(document.createTextNode(asString(props.title)));
    root.append(titleEl);
    const desc = asString(props.description);
    if (desc) root.append(el("p", { class: "rui-kanban-card-description" }, [desc]));
    const tags = asArray(props.tags);
    if (tags.length > 0) {
      const tagRow = el("div", { class: "rui-kanban-card-tags" });
      for (const t of tags) {
        const label = asString(t);
        if (label) tagRow.append(el("span", { class: "rui-tag", "data-size": "sm" }, [
          el("span", { class: "rui-tag-label" }, [label])
        ]));
      }
      root.append(tagRow);
    }
    const assignee = asString(props.assignee);
    if (assignee) {
      const footer = el("footer", { class: "rui-kanban-card-footer" });
      footer.append(renderAvatar("", assignee, "sm", helpers));
      footer.append(el("span", { class: "rui-kanban-card-assignee" }, [assignee]));
      root.append(footer);
    }
    return root;
  }
};
const KanbanColumn = {
  name: "KanbanColumn",
  description: 'Single column inside a KanbanBoard. Children must be KanbanCard entries. Pass `actions` for the header\'s "+ Add card" / overflow menu and `limit` for a WIP limit (the count chip renders "3 / 5" and flags an overflow).',
  props: [
    { name: "title", type: "string" },
    { name: "items", type: "KanbanCard[]", aliases: ["cards"] },
    { name: "tone", aliases: ["variant"], type: "string", optional: true, enum: SURFACE_TONES$1, description: "Header accent tone" },
    { name: "actions", type: "Node[]", optional: true, description: "Header buttons — add card, column menu" },
    { name: "limit", type: "number", optional: true, description: 'WIP limit; the count chip becomes "items / limit"' }
  ],
  render: (_node, props, helpers) => {
    const items = asArray(props.items);
    const requestedLimit = props.limit == null ? 0 : Math.floor(asNumber(props.limit, 0));
    const limit = requestedLimit > 0 ? requestedLimit : 0;
    const overLimit = limit > 0 && items.length > limit;
    const root = el("section", {
      class: "rui-kanban-column",
      "data-tone": asString(props.tone, "default"),
      "data-over-limit": overLimit ? "true" : null
    });
    const header = el("header", { class: "rui-kanban-column-header" });
    header.append(el("span", { class: "rui-kanban-column-title" }, [asString(props.title)]));
    header.append(el("span", {
      class: "rui-kanban-column-count",
      "data-over-limit": overLimit ? "true" : null
    }, [limit > 0 ? `${items.length} / ${limit}` : String(items.length)]));
    const headerActions = renderActionsRow(props.actions, helpers);
    if (headerActions) {
      headerActions.classList.add("rui-kanban-column-actions");
      header.append(headerActions);
    }
    root.append(header);
    const body = el("div", { class: "rui-kanban-column-body" });
    for (const item of items) body.append(helpers.renderNode(item));
    if (items.length === 0) {
      body.append(el("div", { class: "rui-kanban-column-empty" }, ["No items"]));
    }
    root.append(body);
    return root;
  }
};
function wireCardDragAndDrop(root, onCardMove, helpers) {
  const titleOf = (element, selector) => asString(element?.querySelector(selector)?.textContent).trim();
  const cardTitle = (card) => titleOf(card, ".rui-kanban-card-title");
  const columnTitle = (column) => titleOf(column, ".rui-kanban-column-title");
  const clearMarkers = (board) => {
    for (const marked of board.querySelectorAll("[data-drop-target]")) {
      marked.removeAttribute("data-drop-target");
    }
    for (const dragging of board.querySelectorAll("[data-dragging]")) {
      dragging.removeAttribute("data-dragging");
    }
  };
  root.dataset.draggableCards = "true";
  for (const card of root.querySelectorAll(".rui-kanban-card")) {
    card.setAttribute("draggable", "true");
  }
  root.ondragstart = (event) => {
    const card = closestFrom(event.target, ".rui-kanban-card");
    if (!card) return;
    card.dataset.dragging = "true";
    event.dataTransfer?.setData("text/plain", cardTitle(card));
    if (event.dataTransfer) event.dataTransfer.effectAllowed = "move";
  };
  root.ondragover = (event) => {
    const column = closestFrom(event.target, ".rui-kanban-column");
    if (!column) return;
    event.preventDefault();
    if (event.dataTransfer) event.dataTransfer.dropEffect = "move";
    column.dataset.dropTarget = "true";
  };
  root.ondragleave = (event) => {
    closestFrom(event.target, ".rui-kanban-column")?.removeAttribute("data-drop-target");
  };
  root.ondrop = (event) => {
    const board = closestFrom(event.currentTarget ?? event.target, ".rui-kanban-board") ?? root;
    const column = closestFrom(event.target, ".rui-kanban-column");
    const card = board.querySelector(".rui-kanban-card[data-dragging='true']");
    const from = columnTitle(card?.closest(".rui-kanban-column"));
    clearMarkers(board);
    if (!column || !card) return;
    event.preventDefault();
    const to = columnTitle(column);
    if (!to || to === from) return;
    helpers.invoke(onCardMove, cardTitle(card), to, from);
  };
  root.ondragend = (event) => {
    clearMarkers(closestFrom(event.currentTarget ?? event.target, ".rui-kanban-board") ?? root);
  };
}
const KanbanBoard = {
  name: "KanbanBoard",
  description: "Horizontal Kanban board. Children must be KanbanColumn entries. The board scrolls horizontally on narrow viewports so columns stay readable. Pass `onCardMove` to enable drag & drop — it is called with (cardTitle, toColumn, fromColumn) so the author moves the data.",
  props: [
    { name: "columns", type: "KanbanColumn[]" },
    { name: "onCardMove", type: "callable", optional: true, description: "Called with (cardTitle, toColumn, fromColumn) after a card is dropped on another column" },
    { name: "draggable", type: "boolean", optional: true, description: "Make cards draggable without an onCardMove handler (implied by onCardMove)" }
  ],
  render: (_node, props, helpers) => {
    const root = el("div", { class: "rui-kanban-board" });
    for (const column of asArray(props.columns)) root.append(helpers.renderNode(column));
    if (typeof props.onCardMove === "function" || asBoolean(props.draggable)) {
      wireCardDragAndDrop(root, props.onCardMove, helpers);
    }
    return root;
  }
};
const SectionHeader = {
  name: "SectionHeader",
  description: "Compact section header for the top of a Card or panel. Renders a small eyebrow, a title, an optional subtitle, an optional status Tag/Badge, and a right-aligned actions row. Use this inside a Card to introduce a section instead of a bare `CardHeader`.",
  props: [
    { name: "title", type: "string" },
    { name: "subtitle", type: "string", optional: true, aliases: ["description"] },
    { name: "eyebrow", type: "string", optional: true, description: "Short uppercase label above the title" },
    { name: "status", type: "Badge | Tag", optional: true, aliases: ["badge"] },
    { name: "actions", type: "Node[]", optional: true, description: "Buttons / Links shown on the right" }
  ],
  render: (_node, props, helpers) => {
    const root = el("header", { class: "rui-section-header" });
    const left = el("div", { class: "rui-section-header-left" });
    const eyebrow = asString(props.eyebrow);
    if (eyebrow) left.append(el("span", { class: "rui-section-header-eyebrow" }, [eyebrow]));
    const titleLine = el("div", { class: "rui-section-header-title-line" });
    titleLine.append(el("h3", { class: "rui-section-header-title" }, [asString(props.title)]));
    if (props.status) titleLine.append(helpers.renderNode(props.status));
    left.append(titleLine);
    const subtitle = asString(props.subtitle);
    if (subtitle) left.append(el("p", { class: "rui-section-header-subtitle" }, [subtitle]));
    root.append(left);
    const actions = renderActionsRow(props.actions, helpers);
    if (actions) {
      actions.classList.add("rui-section-header-actions");
      root.append(actions);
    }
    return root;
  }
};
let toolbarSearchSeq = 0;
function renderToolbarSearch(node, props, helpers) {
  const idSlot = helpers.useInstanceState("searchId", "");
  let searchId = asString(props.searchId);
  if (!searchId) {
    searchId = idSlot.get();
    if (!searchId) {
      toolbarSearchSeq += 1;
      searchId = `toolbar-search-${toolbarSearchSeq}`;
      idSlot.set(searchId);
    }
  }
  const meta = node.argMeta ? [...node.argMeta] : [];
  while (meta.length <= SEARCHBAR_VALUE_SLOT) meta.push({});
  const bound = TOOLBAR_SEARCH_VALUE_SLOT >= 0 ? node.argMeta?.[TOOLBAR_SEARCH_VALUE_SLOT] : void 0;
  if (bound?.stateRef) meta[SEARCHBAR_VALUE_SLOT] = bound;
  return SearchBar.render(
    { ...node, name: "SearchBar", args: [], argMeta: meta },
    {
      id: searchId,
      placeholder: asString(props.searchPlaceholder, "Search…"),
      value: props.searchValue,
      onChange: props.onSearch
    },
    helpers
  );
}
const Toolbar = {
  name: "Toolbar",
  description: "Horizontal toolbar for filters, search, view modes, and primary actions. Left/center/right slots wrap onto separate rows on narrow viewports so the bar never overflows. Pass `searchable: true` to auto-mount a SearchBar in the left slot (bind `searchValue` to a `$variable`, or handle `onSearch`). Use ABOVE a Table, List, Grid, or Kanban view — never replace `PageHeader` with it.",
  props: [
    { name: "left", type: "Node[]", optional: true, description: "Filters / search inputs / chips" },
    { name: "right", type: "Node[]", optional: true, description: "Primary action buttons" },
    { name: "center", type: "Node[]", optional: true, description: "Centered controls (e.g. SegmentedControl, search bar)" },
    { name: "searchable", type: "boolean", optional: true, description: "Auto-mount a SearchBar at the start of the left slot" },
    { name: "searchPlaceholder", type: "string", optional: true, description: "Placeholder for the auto-mounted SearchBar" },
    { name: "searchValue", type: "string", optional: true, description: "$variable bound to the auto-mounted SearchBar" },
    { name: "onSearch", type: "callable", optional: true, aliases: ["onSearchChange", "onChange"], description: "Called with the query on every keystroke of the auto-mounted SearchBar" },
    { name: "searchId", type: "string", optional: true, description: "Explicit id/name for the auto-mounted SearchBar (defaults to a per-instance unique id)" }
  ],
  render: (node, props, helpers) => {
    const center = asArray(props.center);
    const root = el("div", {
      class: "rui-toolbar",
      "data-has-center": center.length > 0 ? "true" : "false"
    });
    const left = el("div", { class: "rui-toolbar-side rui-toolbar-left" });
    if (asBoolean(props.searchable)) {
      left.append(renderToolbarSearch(node, props, helpers));
    }
    for (const child of asArray(props.left)) left.append(helpers.renderNode(child));
    root.append(left);
    if (center.length > 0) {
      const centerWrap = el("div", { class: "rui-toolbar-side rui-toolbar-center" });
      for (const child of center) centerWrap.append(helpers.renderNode(child));
      root.append(centerWrap);
    }
    const right = el("div", { class: "rui-toolbar-side rui-toolbar-right" });
    for (const child of asArray(props.right)) right.append(helpers.renderNode(child));
    root.append(right);
    return root;
  }
};
const TOOLBAR_SEARCH_VALUE_SLOT = Toolbar.props.findIndex((p) => p.name === "searchValue");
const SEARCHBAR_VALUE_SLOT = SearchBar.props.findIndex((p) => p.name === "value");
const SidebarItem = {
  name: "SidebarItem",
  description: "Single navigation item inside a Sidebar. Pass `active=true` to mark as the current page, a `to` path to navigate via the runtime router on click, an `onClick` callable for arbitrary click handling, or an optional `badge` (string/number) for a trailing chip. `to` and `onClick` can coexist — `onClick` is invoked AFTER the router navigates so authors can do extra work (analytics, side-effects). Items with `to`/`href` render as real links, so Cmd/middle-click opens a new tab; `disabled: true` greys a gated item out.",
  props: [
    { name: "label", type: "string" },
    { name: "icon", type: "string", optional: true, description: "Font Awesome icon name rendered before the label" },
    { name: "active", type: "boolean", optional: true, description: "Mark this item as the current page. When `to` is provided and `active` is omitted, the item auto-detects the active state from the current router path." },
    { name: "badge", type: "string", optional: true, description: "Trailing chip (count or status)" },
    { name: "to", type: "string", optional: true, description: 'Router path to navigate to on click (e.g. "/", "/orders"). Uses the runtime router — no full page reload.' },
    { name: "onClick", type: "callable", optional: true, aliases: ["action", "onclick"], description: "Callable invoked on click. Runs in addition to `to`-based navigation." },
    { name: "href", type: "string", optional: true, description: "External/absolute URL — rendered as a plain link (no router)" },
    { name: "disabled", type: "boolean", optional: true, description: "Render a non-interactive, greyed item (gated features)" }
  ],
  render: (_node, props, helpers) => {
    const to = asString(props.to);
    const explicitHref = asString(props.href);
    const disabled = asBoolean(props.disabled);
    const explicitActive = props.active !== void 0 && props.active !== null;
    const currentPath = to ? helpers.router.getPath() : "";
    const autoActive = (() => {
      if (!to) return false;
      if (to === "/") return currentPath === "/";
      if (currentPath === to) return true;
      return currentPath.startsWith(to + "/");
    })();
    const isActive = explicitActive ? asBoolean(props.active) : autoActive;
    const href = explicitHref ? sanitiseHref(explicitHref) : to ? routeHref(to) : "";
    const tag = href ? "a" : "button";
    const root = el(tag, {
      type: tag === "button" ? "button" : null,
      class: "rui-sidebar-item",
      "data-active": isActive ? "true" : "false",
      // Colour alone never told a screen-reader user which item is current.
      "aria-current": isActive ? "page" : null,
      "data-to": to || null,
      href: tag === "a" ? href : null,
      disabled: tag === "button" && disabled ? true : null,
      // The tag stays stable when `disabled` flips (a tagName change makes the
      // morph replace the node and drop focus), so an anchor expresses
      // disablement with aria + removal from the tab order instead.
      "aria-disabled": disabled ? "true" : null,
      "data-disabled": disabled ? "true" : null,
      tabindex: tag === "a" && disabled ? "-1" : null
    });
    const iconNode = renderIcon(props.icon, { className: "rui-sidebar-item-icon" });
    if (iconNode) root.append(iconNode);
    root.append(el("span", { class: "rui-sidebar-item-label" }, [asString(props.label)]));
    const badge = asString(props.badge);
    if (badge) root.append(el("span", { class: "rui-sidebar-item-badge" }, [badge]));
    if (disabled) {
      root.onclick = (event) => {
        event.preventDefault();
      };
    } else if (to || props.onClick != null) {
      root.onclick = (event) => {
        const e = event;
        const modified = e.metaKey || e.ctrlKey || e.shiftKey || e.altKey || e.button !== 0;
        if (to && !modified) {
          e.preventDefault();
          helpers.router.navigate(to);
        }
        if (props.onClick != null) helpers.invoke(props.onClick);
      };
    }
    return root;
  }
};
const SidebarSection = {
  name: "SidebarSection",
  description: "Grouping inside a Sidebar — small uppercase label followed by SidebarItem entries. Use this to chunk a long sidebar into sections.",
  props: [
    { name: "label", type: "string" },
    { name: "items", type: "SidebarItem[]" }
  ],
  render: (_node, props, helpers) => {
    const root = el("div", { class: "rui-sidebar-section" });
    const label = asString(props.label);
    if (label) root.append(el("div", { class: "rui-sidebar-section-label" }, [label]));
    for (const item of asArray(props.items)) root.append(helpers.renderNode(item));
    return root;
  }
};
const Sidebar = {
  name: "Sidebar",
  description: "Vertical app navigation panel. Supports a brand header, navigation items (`SidebarItem` or `SidebarSection`), an optional footer, and a `collapsed` mode that hides labels to leave just an icon rail. Use inside `AppShell` for SaaS-style left navigation.",
  props: [
    { name: "items", type: "(SidebarItem | SidebarSection)[]" },
    { name: "brand", type: "string", optional: true, description: "Product name / workspace label at the top" },
    { name: "tagline", type: "string", optional: true },
    { name: "footer", type: "Node[]", optional: true, description: "Footer block (Avatar + name, upgrade CTA, …)" },
    { name: "collapsed", type: "boolean", optional: true, description: "Render as an icon-only rail (hides labels/badges; brand shrinks to its initial)" },
    { name: "fullHeight", type: "boolean", optional: true, description: "Pin the rail to the full viewport height so the main content scrolls beneath it (default true). Set false for an auto-height sidebar that scrolls with the page." },
    { name: "label", type: "string", optional: true, description: 'Accessible name for the nav landmark (defaults to `brand`, then "Main navigation")' }
  ],
  render: (_node, props, helpers) => {
    const collapsed = asBoolean(props.collapsed);
    const fullHeight = props.fullHeight === void 0 ? true : asBoolean(props.fullHeight);
    const root = el("aside", { class: "rui-sidebar" });
    if (collapsed) root.dataset.collapsed = "true";
    if (fullHeight) root.dataset.fullHeight = "true";
    const brand = asString(props.brand);
    const tagline = asString(props.tagline);
    if (brand || tagline) {
      const header = el("div", { class: "rui-sidebar-header" });
      if (brand) {
        const brandText = collapsed ? brand.trim().charAt(0).toUpperCase() || brand : brand;
        const condensed = brandText !== brand;
        header.append(el("div", {
          class: "rui-sidebar-brand",
          role: "heading",
          "aria-level": "2",
          // A single letter is meaningless to a screen reader or on hover, so
          // the collapsed rail still carries the full product name.
          "aria-label": condensed ? brand : null,
          title: condensed ? brand : null
        }, [brandText]));
      }
      if (tagline) header.append(el("div", { class: "rui-sidebar-tagline" }, [tagline]));
      root.append(header);
    }
    const body = el("nav", {
      class: "rui-sidebar-body",
      "aria-label": asString(props.label) || brand || "Main navigation"
    });
    for (const item of asArray(props.items)) body.append(helpers.renderNode(item));
    root.append(body);
    const footerItems = asArray(props.footer);
    if (footerItems.length > 0) {
      const footer = el("div", { class: "rui-sidebar-footer" });
      for (const item of footerItems) footer.append(helpers.renderNode(item));
      root.append(footer);
    }
    return root;
  }
};
const AppShell = {
  name: "AppShell",
  description: "Canonical SaaS application shell: optional top bar, fixed left Sidebar, and scrollable main content. Reach for this whenever a response represents a full product surface (dashboard with nav, settings + sections, admin panels). Pass `collapsible=true` to render a hamburger that turns the sidebar into a slide-over drawer on narrow viewports; bind `sidebarOpen` to a `$variable` (or handle `onSidebarOpenChange`) to drive/observe that drawer — e.g. to close it after a nav click. Do NOT declare a Modal/Sheet inside `sidebar`: the drawer is a transformed box, so an overlay inside it is positioned against the drawer instead of the viewport.",
  props: [
    { name: "sidebar", type: "Sidebar", description: "Pass a Sidebar(...) node" },
    { name: "content", type: "Node[]", description: "Main content (typically starts with a PageHeader)" },
    { name: "topbar", type: "Node[]", optional: true, description: "Optional thin top bar above the content" },
    { name: "collapsible", type: "boolean", optional: true, description: "Show a hamburger that toggles the sidebar drawer on mobile (default true)" },
    { name: "sidebarOpen", type: "boolean", optional: true, description: "$variable controlling whether the mobile drawer is open (two-way: the hamburger, scrim and Escape write back to it)" },
    { name: "onSidebarOpenChange", type: "callable", optional: true, description: "Called with the new open state whenever the drawer is toggled" }
  ],
  render: (node, props, helpers) => {
    const collapsible = props.collapsible === void 0 ? true : asBoolean(props.collapsible);
    const propOpen = asBoolean(props.sidebarOpen);
    const openSlot = helpers.useInstanceState("sidebarOpen", propOpen);
    const stateRef = APP_SHELL_OPEN_SLOT >= 0 ? node.argMeta?.[APP_SHELL_OPEN_SLOT]?.stateRef : void 0;
    if (stateRef) openSlot.set(propOpen);
    const isOpen = stateRef ? propOpen : openSlot.get();
    const idSlot = helpers.useInstanceState("sidebarId", "");
    let sidebarId = idSlot.get();
    if (!sidebarId) {
      appShellSeq += 1;
      sidebarId = `rui-app-shell-sidebar-${appShellSeq}`;
      idSlot.set(sidebarId);
    }
    const setOpen = (origin, next) => {
      openSlot.set(next);
      const liveRoot = closestFrom(origin, ".rui-app-shell");
      if (liveRoot) {
        if (next) liveRoot.dataset.sidebarOpen = "true";
        else delete liveRoot.dataset.sidebarOpen;
        const toggle = liveRoot.querySelector(".rui-app-shell-toggle");
        toggle?.setAttribute("aria-expanded", next ? "true" : "false");
        const panel = liveRoot.querySelector(".rui-app-shell-sidebar");
        deferToPaint(() => {
          if (next) (panel?.querySelector(FOCUSABLE_SELECTOR) ?? panel)?.focus?.();
          else toggle?.focus?.();
        });
      }
      if (stateRef) helpers.setState(stateRef, next);
      helpers.invoke(props.onSidebarOpenChange, next);
    };
    const root = el("div", { class: "rui-app-shell" });
    if (collapsible) root.dataset.collapsible = "true";
    if (isOpen) root.dataset.sidebarOpen = "true";
    const sidebarHost = el("div", {
      class: "rui-app-shell-sidebar",
      id: sidebarId,
      tabindex: "-1"
    });
    sidebarHost.append(helpers.renderNode(props.sidebar));
    root.append(sidebarHost);
    if (collapsible) {
      const scrim = el("div", { class: "rui-app-shell-scrim", "aria-hidden": "true" });
      scrim.onclick = (event) => setOpen(event.currentTarget ?? event.target, false);
      root.append(scrim);
      const trap = dialogKeydownHandler(".rui-app-shell-sidebar", (origin) => setOpen(origin, false));
      root.onkeydown = (event) => {
        const live = closestFrom(event.currentTarget ?? event.target, ".rui-app-shell");
        if (!live || live.dataset.sidebarOpen !== "true") return;
        trap(event);
      };
    }
    const main = el("div", { class: "rui-app-shell-main" });
    const topbar = asArray(props.topbar);
    const hasTopbarContent = topbar.length > 0;
    if (hasTopbarContent || collapsible) {
      const bar = el("div", { class: "rui-app-shell-topbar" });
      if (!hasTopbarContent) bar.dataset.burgerOnly = "true";
      if (collapsible) {
        const toggle = el("button", {
          class: "rui-app-shell-toggle",
          type: "button",
          "aria-label": "Toggle navigation",
          "aria-expanded": isOpen ? "true" : "false",
          "aria-controls": sidebarId
        });
        toggle.append(renderIcon("bars", { size: "md" }) ?? document.createTextNode("≡"));
        toggle.onclick = (event) => {
          setOpen(event.currentTarget ?? event.target, !openSlot.get());
        };
        bar.append(toggle);
      }
      for (const item of topbar) bar.append(helpers.renderNode(item));
      main.append(bar);
    }
    const content = el("div", { class: "rui-app-shell-content" });
    for (const child of asArray(props.content)) content.append(helpers.renderNode(child));
    main.append(content);
    root.append(main);
    return root;
  }
};
const APP_SHELL_OPEN_SLOT = AppShell.props.findIndex((p) => p.name === "sidebarOpen");
let appShellSeq = 0;
const SplitView = {
  name: "SplitView",
  description: "Two-pane master/detail layout — a narrow primary pane on the left, wider detail pane on the right. Collapses to a single column on narrow viewports. Use for inboxes, file browsers, contact lists. On a phone pass `showDetail` (typically a `$variable`) so only the list or only the selected item is shown.",
  props: [
    { name: "primary", type: "Node[]", description: "Master pane content (list, filters)" },
    { name: "detail", type: "Node[]", aliases: ["secondary"], description: "Detail pane content (selected item, empty state)" },
    { name: "primaryWidth", type: "string", optional: true, aliases: ["splitAt"], description: "CSS width for the primary pane (default 320px)" },
    { name: "showDetail", type: "boolean", optional: true, description: "Which pane wins on narrow viewports: true = detail, false = list. Omit to keep both stacked." }
  ],
  render: (_node, props, helpers) => {
    const width = sanitiseCssLength(asString(props.primaryWidth), "320px");
    const mobilePane = props.showDetail === void 0 || props.showDetail === null ? null : asBoolean(props.showDetail) ? "detail" : "primary";
    const root = el("div", {
      class: "rui-split-view",
      style: `--rui-split-primary:${width}`,
      "data-mobile-pane": mobilePane
    });
    const primary = el("div", { class: "rui-split-view-primary" });
    for (const child of asArray(props.primary)) primary.append(helpers.renderNode(child));
    const detail = el("div", { class: "rui-split-view-detail" });
    for (const child of asArray(props.detail)) detail.append(helpers.renderNode(child));
    root.append(primary, detail);
    return root;
  }
};
const DescriptionItem = {
  name: "DescriptionItem",
  description: "Single row inside a DescriptionList. Renders a small uppercase label on the left and a value (string or arbitrary Node) on the right.",
  props: [
    { name: "label", type: "string" },
    { name: "value", type: "Node | string" },
    { name: "icon", type: "string", optional: true }
  ],
  render: (_node, props, helpers) => {
    const root = el("div", { class: "rui-description-item" });
    const labelWrap = el("dt", { class: "rui-description-label" });
    const iconNode = renderIcon(props.icon, { className: "rui-description-icon" });
    if (iconNode) labelWrap.append(iconNode);
    labelWrap.append(document.createTextNode(asString(props.label)));
    const value = el("dd", { class: "rui-description-value" });
    if (isComponentNode(props.value) || Array.isArray(props.value)) {
      value.append(helpers.renderNode(props.value));
    } else {
      value.append(document.createTextNode(asString(props.value)));
    }
    root.append(labelWrap, value);
    return root;
  }
};
const ActionStripe = {
  name: "ActionStripe",
  description: "Full-width clickable navigation row — leading icon, label + optional description, an optional trailing `value` string or `trailing` node (Switch, Badge, Avatar), and a chevron affordance. Stack them for settings screens, product menus, and drill-down lists. Use `ListItem` instead for a non-interactive content row.",
  props: [
    { name: "label", type: "string", positional: true },
    { name: "description", type: "string", optional: true, aliases: ["subtitle", "meta"] },
    { name: "icon", type: "string", optional: true, description: "Leading Font Awesome icon name" },
    { name: "value", type: "string", optional: true, description: "Trailing value / status text shown before the chevron" },
    { name: "href", type: "string", optional: true, description: "Render as a link instead of a button" },
    { name: "disabled", type: "boolean", optional: true },
    { name: "onClick", type: "callable", optional: true, aliases: ["action", "onclick"] },
    { name: "trailing", type: "Node", optional: true, description: "Trailing node before the chevron — Switch, Badge, Avatar (alternative to the string `value`)" },
    { name: "target", type: "string", optional: true, enum: ["_self", "_blank", "_parent", "_top"], description: 'Link target; `_blank` also sets rel="noopener noreferrer"' }
  ],
  render: (_node, props, helpers) => {
    const href = asString(props.href);
    const disabled = asBoolean(props.disabled);
    const safeHref = href ? sanitiseHref(href) : "";
    const tag = safeHref ? "a" : "button";
    const target = asString(props.target);
    const root = el(tag, {
      class: "rui-action-stripe",
      type: tag === "button" ? "button" : null,
      href: tag === "a" ? safeHref : null,
      target: tag === "a" && target ? target : null,
      // A new-tab link without `noopener` hands the opener window to the target.
      rel: tag === "a" && target === "_blank" ? "noopener noreferrer" : null,
      disabled: tag === "button" && disabled ? true : null,
      "aria-disabled": disabled ? "true" : null,
      tabindex: tag === "a" && disabled ? "-1" : null,
      "data-disabled": disabled ? "true" : null
    });
    const iconNode = renderIcon(props.icon, { className: "rui-action-stripe-icon" });
    if (iconNode) root.append(iconNode);
    const body = el("span", { class: "rui-action-stripe-body" });
    body.append(el("span", { class: "rui-action-stripe-label" }, [asString(props.label)]));
    const description = asString(props.description);
    if (description) {
      body.append(el("span", { class: "rui-action-stripe-description" }, [description]));
    }
    root.append(body);
    const value = asString(props.value);
    if (value) root.append(el("span", { class: "rui-action-stripe-value" }, [value]));
    if (props.trailing != null) {
      const trailing = el("span", { class: "rui-action-stripe-trailing" });
      trailing.append(helpers.renderNode(props.trailing));
      trailing.onclick = (event) => event.stopPropagation();
      root.append(trailing);
    }
    root.append(el("span", { class: "rui-action-stripe-chevron", "aria-hidden": "true" }));
    if (disabled) {
      root.onclick = (event) => {
        event.preventDefault();
      };
    } else if (typeof props.onClick === "function") {
      root.onclick = () => helpers.invoke(props.onClick);
    }
    return root;
  }
};
const DescriptionList = {
  name: "DescriptionList",
  description: "Compact key/value summary for detail pages — replaces a row of `Text`s with a properly aligned `<dl>`. Children must be DescriptionItem entries. Two columns by default on wide viewports.",
  props: [
    { name: "items", type: "DescriptionItem[]" },
    { name: "columns", type: "number", optional: true, description: "1 or 2 (default 2)" }
  ],
  render: (_node, props, helpers) => {
    const requested = Math.floor(asNumber(props.columns, 2));
    const columns = Number.isFinite(requested) ? Math.max(1, Math.min(2, requested)) : 2;
    const root = el("dl", {
      class: "rui-description-list",
      "data-columns": String(columns)
    });
    for (const item of asArray(props.items)) root.append(helpers.renderNode(item));
    return root;
  }
};
const StatusDot = {
  name: "StatusDot",
  description: "Inline status pip + label. Use for compact health/state indicators in toolbars, sidebars, lists, and table cells.",
  props: [
    { name: "label", type: "string" },
    { name: "tone", type: "string", optional: true, aliases: ["variant"], enum: ["default", "primary", "success", "warning", "danger", "info"] },
    { name: "pulse", type: "boolean", optional: true, description: "Animate the dot for 'live' state" }
  ],
  render: (_node, props) => {
    const root = el("span", {
      class: "rui-status-dot",
      // Default to the neutral tone like every sibling pattern (and like the
      // stylesheet's own base marker): `StatusDot("Offline")` used to render a
      // bright green pip next to the word "Offline".
      "data-tone": asString(props.tone, "default"),
      "data-pulse": asBoolean(props.pulse) ? "true" : "false"
    });
    root.append(el("span", { class: "rui-status-dot-marker" }));
    root.append(el("span", { class: "rui-status-dot-label" }, [asString(props.label)]));
    return root;
  }
};
const PricingCard = {
  name: "PricingCard",
  description: 'Single pricing tier card with plan name, price, billing period, description, bullet features, and a CTA button. Mark one tier as `featured=true` to highlight it (raises the card and shows a ribbon — "Most popular" unless `ribbon`/`badge` says otherwise). `features` entries may be plain strings or `{label, included: false}` objects so a cheaper tier can show what it does NOT include.',
  props: [
    { name: "plan", type: "string", description: "Tier name (e.g. 'Pro')" },
    { name: "price", type: "string", description: "Display price (e.g. '$29')" },
    { name: "period", type: "string", optional: true, description: "Billing period (e.g. '/mo')" },
    { name: "description", type: "string", optional: true },
    { name: "features", type: "string[] | {label, included}[]", optional: true, description: "Bullet list — strings are included features; `{label, included: false}` renders a struck-through/excluded bullet" },
    { name: "action", type: "Button", optional: true, aliases: ["cta"], description: "Primary CTA — pass a Button(...)" },
    { name: "badge", type: "string", optional: true, description: "Eyebrow / badge above the plan name" },
    { name: "featured", type: "boolean", optional: true, aliases: ["highlighted"] },
    { name: "ribbon", type: "string", optional: true, aliases: ["badgeLabel"], description: 'Ribbon text for a featured tier (default "Most popular")' }
  ],
  render: (_node, props, helpers) => {
    const featured = asBoolean(props.featured);
    const root = el("article", {
      class: "rui-pricing-card",
      "data-featured": featured ? "true" : "false"
    });
    const badge = asString(props.ribbon) || asString(props.badge) || (featured ? "Most popular" : "");
    if (badge) root.append(el("div", { class: "rui-pricing-card-badge" }, [badge]));
    root.append(el("h3", { class: "rui-pricing-card-plan" }, [asString(props.plan)]));
    const description = asString(props.description);
    if (description) root.append(el("p", { class: "rui-pricing-card-description" }, [description]));
    const priceRow = el("div", { class: "rui-pricing-card-price-row" });
    priceRow.append(el("span", { class: "rui-pricing-card-price" }, [asString(props.price)]));
    const period = asString(props.period);
    if (period) priceRow.append(el("span", { class: "rui-pricing-card-period" }, [period]));
    root.append(priceRow);
    const features = asArray(props.features);
    if (features.length > 0) {
      const list = el("ul", { class: "rui-pricing-card-features" });
      for (const f of features) {
        const record = asRecord$1(f);
        const label = asString(record ? record.label ?? record.text ?? record.name : f);
        if (!label) continue;
        const included = record && record.included !== void 0 ? asBoolean(record.included) : true;
        const check = renderIcon(included ? "circle-check" : "circle-xmark", { className: "rui-pricing-card-check" }) ?? el("span", { class: "rui-pricing-card-check" });
        list.append(el("li", {
          class: "rui-pricing-card-feature",
          "data-included": included ? "true" : "false"
        }, [
          check,
          document.createTextNode(label)
        ]));
      }
      root.append(list);
    }
    if (props.action) {
      const wrap = el("div", { class: "rui-pricing-card-action" });
      wrap.append(helpers.renderNode(props.action));
      root.append(wrap);
    }
    return root;
  }
};
const PricingTable = {
  name: "PricingTable",
  description: "Responsive grid of PricingCard tiers. Items size uniformly across a row and wrap onto multiple rows on narrow viewports. Use as the centerpiece of any pricing or upgrade page.",
  props: [
    { name: "tiers", type: "PricingCard[]" },
    { name: "columns", type: "number", optional: true, description: "Preferred column count (default auto)" }
  ],
  render: (_node, props, helpers) => {
    const requested = props.columns == null ? 0 : Math.floor(asNumber(props.columns, 0));
    const columns = requested > 0 ? Math.max(1, Math.min(4, requested)) : 0;
    const root = el("div", {
      class: "rui-pricing-table",
      "data-columns": columns > 0 ? String(columns) : null
    });
    for (const tier of asArray(props.tiers)) root.append(helpers.renderNode(tier));
    return root;
  }
};
const MediaCard = {
  name: "MediaCard",
  description: 'Card with a media (image) header followed by title, body, optional tags, footer meta, and an actions row. Use for article previews, product cards, project highlights, gallery items — anywhere a Card needs a leading image. Orient with `orientation="horizontal"` for side-by-side media + content on wide viewports. Pass `href` (or `onClick`) to make the whole card the click target, which is what the hover lift implies.',
  props: [
    { name: "title", type: "string" },
    { name: "imageSrc", type: "string", optional: true, aliases: ["src", "image"], description: "Image URL (omit to render a neutral placeholder)" },
    { name: "description", type: "string", optional: true },
    { name: "tags", type: "string[]", optional: true, description: "Tag pill labels" },
    { name: "meta", type: "string", optional: true, description: "Footer meta line (author · date · category)" },
    { name: "actions", type: "Node[]", optional: true, description: "Buttons / Links rendered at the bottom" },
    { name: "badge", type: "string | Badge", optional: true, description: "Eyebrow string or Badge node shown over the image" },
    { name: "orientation", type: "string", optional: true, enum: ["vertical", "horizontal"] },
    { name: "ratio", type: "string", optional: true, description: "Media aspect ratio (default 16:9 vertical, 4:3 horizontal)" },
    { name: "href", type: "string", optional: true, description: "Make the whole card a link (article preview, product card, gallery item)" },
    { name: "onClick", type: "callable", optional: true, aliases: ["action", "onclick"], description: "Called when the card is clicked" }
  ],
  render: (_node, props, helpers) => {
    const orientation = asString(props.orientation, "vertical");
    const href = asString(props.href) ? sanitiseHref(props.href) : "";
    const clickable = typeof props.onClick === "function";
    const root = el(href ? "a" : "article", {
      class: "rui-media-card",
      "data-orientation": orientation,
      href: href || null,
      "data-interactive": href || clickable ? "true" : null,
      role: !href && clickable ? "button" : null,
      tabindex: !href && clickable ? "0" : null
    });
    if (clickable) {
      const fire = () => helpers.invoke(props.onClick);
      root.onclick = fire;
      if (!href) root.onkeydown = activateOnKey(fire);
    }
    const ratio = parseMediaRatio(asString(props.ratio, orientation === "horizontal" ? "4:3" : "16:9"));
    const media = el("div", {
      class: "rui-media-card-media",
      style: `aspect-ratio:${ratio};`
    });
    const mediaPlaceholder = () => renderIcon("image", { className: "rui-media-card-placeholder" }) ?? el("span", { class: "rui-media-card-placeholder" });
    const rawSrc = sanitiseImageSrc(props.imageSrc);
    const mediaFailed = rawSrc ? helpers.useInstanceState(`media-error:${rawSrc}`, false) : null;
    const imageSrc = mediaFailed?.get() ? "" : rawSrc;
    if (imageSrc) {
      const img = el("img", { src: imageSrc, alt: asString(props.title), loading: "lazy" });
      img.onerror = (event) => {
        mediaFailed?.set(true);
        const ev = event;
        const live = ev.currentTarget ?? ev.target;
        const band = live.parentElement;
        live.replaceWith(mediaPlaceholder());
        band?.classList.add("rui-media-card-media-empty");
      };
      media.append(img);
    } else {
      media.classList.add("rui-media-card-media-empty");
      media.append(mediaPlaceholder());
    }
    if (props.badge) {
      const badgeWrap = el("span", { class: "rui-media-card-badge" });
      if (typeof props.badge === "string") {
        badgeWrap.append(document.createTextNode(asString(props.badge)));
      } else {
        badgeWrap.append(helpers.renderNode(props.badge));
      }
      media.append(badgeWrap);
    }
    root.append(media);
    const body = el("div", { class: "rui-media-card-body" });
    body.append(el("h3", { class: "rui-media-card-title" }, [asString(props.title)]));
    const description = asString(props.description);
    if (description) body.append(el("p", { class: "rui-media-card-description" }, [description]));
    const tags = asArray(props.tags);
    if (tags.length > 0) {
      const row = el("div", { class: "rui-media-card-tags" });
      for (const t of tags) {
        const label = asString(t);
        if (label) row.append(el("span", { class: "rui-tag", "data-size": "sm" }, [
          el("span", { class: "rui-tag-label" }, [label])
        ]));
      }
      body.append(row);
    }
    const meta = asString(props.meta);
    if (meta) body.append(el("p", { class: "rui-media-card-meta" }, [meta]));
    const actions = renderActionsRow(props.actions, helpers);
    if (actions) {
      actions.classList.add("rui-media-card-actions");
      if (href || clickable) actions.onclick = (event) => event.stopPropagation();
      body.append(actions);
    }
    root.append(body);
    return root;
  }
};
function parseMediaRatio(input) {
  if (input.includes(":")) {
    const [w, h] = input.split(":");
    const num2 = Number(w);
    const den = Number(h);
    if (Number.isFinite(num2) && num2 > 0 && Number.isFinite(den) && den > 0) return `${num2} / ${den}`;
  }
  const n = Number(input);
  return Number.isFinite(n) && n > 0 ? `${n} / 1` : "16 / 9";
}
function renderInlineSparkline(values, tone = "primary") {
  const width = 80;
  const height = 24;
  const svgNS = "http://www.w3.org/2000/svg";
  const svg = document.createElementNS(svgNS, "svg");
  svg.setAttribute("class", "rui-sparkline");
  svg.setAttribute("data-tone", tone);
  svg.setAttribute("viewBox", `0 0 ${width} ${height}`);
  svg.setAttribute("width", String(width));
  svg.setAttribute("height", String(height));
  svg.setAttribute("aria-hidden", "true");
  if (values.length < 2) return svg;
  const min = Math.min(...values);
  const max = Math.max(...values);
  const range = max - min || 1;
  const step = width / (values.length - 1);
  const points = values.map((value, i) => {
    const x = i * step;
    const y = 2 + (max - value) / range * (height - 4);
    return [x, y];
  });
  const linePath = points.map(([x, y], i) => `${i === 0 ? "M" : "L"}${x.toFixed(1)},${y.toFixed(1)}`).join(" ");
  const areaPath = `${linePath} L${width},${height} L0,${height} Z`;
  const area = document.createElementNS(svgNS, "path");
  area.setAttribute("d", areaPath);
  area.setAttribute("class", "rui-sparkline-area");
  svg.appendChild(area);
  const line = document.createElementNS(svgNS, "path");
  line.setAttribute("d", linePath);
  line.setAttribute("class", "rui-sparkline-line");
  line.setAttribute("fill", "none");
  svg.appendChild(line);
  return svg;
}
const Stats = {
  name: "Stats",
  description: 'KPI strip or grid. Pass `items` as `{label, value, hint?, tone?, spark?}` objects for strip layout, or as `StatCard(...)` nodes when `layout="grid"`.',
  props: [
    { name: "items", type: "object[] | StatCard[]", description: "Stat objects or StatCard nodes when layout=grid" },
    { name: "layout", type: "string", optional: true, enum: ["strip", "grid"], description: "strip = horizontal row; grid = responsive Grid" },
    { name: "columns", type: "number", optional: true, description: "Preferred column count for grid layout (1–6)" },
    { name: "align", type: "string", optional: true, enum: ["start", "center", "end"], description: "Strip alignment (layout=strip only)" }
  ],
  render: (_node, props, helpers) => {
    const items = asArray(props.items);
    const hasComponentItems = items.some((item) => isComponentNode(item));
    const layout = hasComponentItems ? "grid" : asString(props.layout, "strip");
    if (layout === "grid") {
      const columns = props.columns ? Math.max(1, Math.min(6, Math.floor(asNumber(props.columns)))) : 0;
      const gridNode = Grid.render(
        { __kind: "Component", name: "Grid", args: [], argMeta: [] },
        {
          children: items,
          columns: columns > 0 ? columns : "auto",
          gap: "m"
        },
        helpers
      );
      gridNode.classList.add("rui-metric-grid");
      return gridNode;
    }
    const align = asString(props.align, "start");
    const root = el("div", { class: "rui-stats", "data-align": align });
    for (const raw of items) {
      const item = raw ?? {};
      const tone = asString(item.tone, "default");
      const block = el("div", { class: "rui-stats-item", "data-tone": tone });
      block.append(el("div", { class: "rui-stats-label" }, [asString(item.label)]));
      const valueRow = el("div", { class: "rui-stats-value-row" });
      valueRow.append(el("div", { class: "rui-stats-value" }, [asString(item.value)]));
      const sparkValues = asArray(item.spark).map((v) => Number(v)).filter((n) => Number.isFinite(n));
      if (sparkValues.length > 1) {
        valueRow.append(renderInlineSparkline(sparkValues, tone));
      }
      block.append(valueRow);
      const hint = asString(item.hint);
      if (hint) block.append(el("div", { class: "rui-stats-hint" }, [hint]));
      root.append(block);
    }
    return root;
  }
};
const Tile = {
  name: "Tile",
  description: 'Compact icon + label + optional value tile. Smaller and denser than `StatCard`, ideal for menu grids, quick-action panels, category directories, and category filters. Pair with `Grid` for uniform rows. Pass `href` for a directory tile that links to a route, and `selected` for the on-state of a filter tile. `iconPosition: "end"` moves the mark to the trailing edge — the shape a choice card wants, where the copy reads first and the illustration sits opposite it.',
  props: [
    { name: "label", type: "string" },
    { name: "icon", type: "string", optional: true, description: "Font Awesome icon name shown in a colored disc" },
    { name: "value", type: "string", optional: true, description: "Secondary value rendered next to/under the label" },
    { name: "description", type: "string", optional: true },
    { name: "tone", type: "string", optional: true, aliases: ["variant"], enum: SURFACE_TONES$1 },
    { name: "onClick", type: "callable", optional: true, aliases: ["action", "onclick"] },
    { name: "href", type: "string", optional: true, description: "Render the tile as a link to this URL" },
    { name: "selected", type: "boolean", optional: true, aliases: ["active"], description: "Mark the tile as currently applied/current (filter grids, category directories)" },
    // Appended, never inserted: a positional call fills slots in declaration order.
    { name: "iconPosition", type: "string", optional: true, enum: ["start", "end"], description: "Which edge the icon sits on (default `start`)" }
  ],
  render: (_node, props, helpers) => {
    const isClickable = typeof props.onClick === "function";
    const href = asString(props.href) ? sanitiseHref(props.href) : "";
    const tag = href ? "a" : isClickable ? "button" : "div";
    const declaresSelected = props.selected !== void 0 && props.selected !== null;
    const selected = asBoolean(props.selected);
    const root = el(tag, {
      type: tag === "button" ? "button" : null,
      href: href || null,
      class: "rui-tile",
      "data-tone": asString(props.tone, "default"),
      // A filter tile needs an on-state that is more than a CSS hook: pressed
      // for a toggle, current-page for a link.
      "data-selected": selected ? "true" : null,
      "aria-pressed": tag === "button" && declaresSelected ? selected ? "true" : "false" : null,
      "aria-current": tag === "a" && selected ? "page" : null
    });
    const iconNode = renderIcon(props.icon, { className: "rui-tile-icon" });
    const iconAtEnd = asString(props.iconPosition, "start") === "end";
    if (iconNode && !iconAtEnd) root.append(iconNode);
    const body = el("div", { class: "rui-tile-body" });
    body.append(el("div", { class: "rui-tile-label" }, [asString(props.label)]));
    const value = asString(props.value);
    if (value) body.append(el("div", { class: "rui-tile-value" }, [value]));
    const description = asString(props.description);
    if (description) body.append(el("div", { class: "rui-tile-description" }, [description]));
    root.append(body);
    if (iconNode && iconAtEnd) {
      root.setAttribute("data-icon-position", "end");
      root.append(iconNode);
    }
    if (isClickable) {
      root.onclick = () => helpers.invoke(props.onClick);
    }
    return root;
  }
};
const Notification = {
  name: "Notification",
  description: "Inline notification card with title, message, time, optional avatar, and dismiss/action buttons. Use inside notification panels, inboxes, or activity drawers — for top-of-page announcements prefer `Banner`. Pass `onClick` to make the whole row open the underlying item, `dismissible`/`onDismiss` for a per-item close button, and `author` when `avatarSrc` is set so the avatar carries the actor's name.",
  props: [
    { name: "title", type: "string" },
    { name: "message", type: "string", optional: true, aliases: ["description"] },
    { name: "time", type: "string", optional: true, description: "Relative or absolute timestamp" },
    { name: "icon", type: "string", optional: true, description: "Font Awesome icon name shown in a colored disc" },
    { name: "avatarSrc", type: "string", optional: true, aliases: ["src"], description: "Avatar URL (alternative to `icon`)" },
    { name: "tone", type: "string", optional: true, aliases: ["variant"], enum: SURFACE_TONES$1 },
    { name: "unread", type: "boolean", optional: true, description: "Highlights the card with an accent" },
    { name: "actions", type: "Node[]", optional: true },
    // New props are appended, never inserted: a positional call fills slots in
    // declaration order, so a mid-list insert would silently re-route args.
    { name: "author", type: "string", optional: true, aliases: ["actor"], description: "Person the notification is about — supplies the avatar's alt text and initials fallback" },
    { name: "onClick", type: "callable", optional: true, aliases: ["action", "onclick"], description: "Called when the row is clicked — open the item / mark it read" },
    { name: "dismissible", type: "boolean", optional: true, aliases: ["closable"], description: "Show a close button that removes the notification" },
    { name: "onDismiss", type: "callable", optional: true, aliases: ["onClose"], description: "Called when the notification is dismissed (implies `dismissible`)" }
  ],
  render: (_node, props, helpers) => {
    const isUnread = asBoolean(props.unread);
    const clickable = typeof props.onClick === "function";
    const dismissible = asBoolean(props.dismissible) || props.onDismiss != null;
    const dismissed = helpers.useInstanceState("dismissed", false);
    if (dismissible && dismissed.get()) {
      return el("article", {
        class: "rui-notification",
        "data-tone": asString(props.tone, "default"),
        "data-dismissed": "true",
        hidden: true,
        style: "display:none"
      });
    }
    const root = el("article", {
      class: "rui-notification",
      "data-tone": asString(props.tone, "default"),
      "data-unread": isUnread ? "true" : "false",
      "data-clickable": clickable ? "true" : null,
      role: clickable ? "button" : null,
      tabindex: clickable ? "0" : null
    });
    if (clickable) {
      const fire = () => helpers.invoke(props.onClick);
      root.onclick = fire;
      root.onkeydown = activateOnKey(fire);
    }
    const avatarSrc = asString(props.avatarSrc);
    const visual = el("div", { class: "rui-notification-visual" });
    if (avatarSrc) {
      visual.append(renderAvatar(avatarSrc, asString(props.author), "md", helpers));
    } else {
      const iconNode = renderIcon(asString(props.icon, "bell"), { className: "rui-notification-icon" });
      if (iconNode) visual.append(iconNode);
    }
    root.append(visual);
    const body = el("div", { class: "rui-notification-body" });
    const head = el("header", { class: "rui-notification-head" });
    const titleWrap = el("span", { class: "rui-notification-title-wrap" });
    if (isUnread) {
      titleWrap.append(el("span", { class: "rui-notification-unread-dot", "aria-hidden": "true" }));
      titleWrap.append(el("span", { class: "rui-visually-hidden" }, ["Unread"]));
    }
    titleWrap.append(el("span", { class: "rui-notification-title" }, [asString(props.title)]));
    head.append(titleWrap);
    const time = asString(props.time);
    if (time) head.append(el("span", { class: "rui-notification-time" }, [time]));
    body.append(head);
    const message = asString(props.message);
    if (message) body.append(el("p", { class: "rui-notification-message" }, [message]));
    const actions = renderActionsRow(props.actions, helpers);
    if (actions) {
      actions.classList.add("rui-notification-actions");
      if (clickable) actions.onclick = (event) => event.stopPropagation();
      body.append(actions);
    }
    root.append(body);
    if (dismissible) {
      const close = el("button", {
        class: "rui-notification-dismiss",
        type: "button",
        "aria-label": "Dismiss"
      });
      close.append(renderIcon("xmark", { className: "rui-notification-dismiss-icon" }) ?? document.createTextNode("×"));
      close.onclick = (event) => {
        event.stopPropagation();
        dismissed.set(true);
        const live = closestFrom(event.currentTarget ?? event.target, ".rui-notification");
        if (live) {
          live.setAttribute("data-dismissed", "true");
          live.hidden = true;
          live.style.display = "none";
        }
        helpers.invoke(props.onDismiss);
      };
      root.append(close);
    }
    return root;
  }
};
const PersonChip = {
  name: "PersonChip",
  description: 'Inline avatar + name + optional role/meta pill. Use anywhere a person needs to be referenced compactly: table cells, list rows, comments, kanban cards, sidebar footers. Pair multiple chips with `Stack(direction="row", wrap=true)` for assignee lists.',
  props: [
    { name: "name", type: "string" },
    { name: "role", type: "string", optional: true, description: "Sub-line below the name (role, email, handle, …)" },
    { name: "avatarSrc", type: "string", optional: true, aliases: ["src"] },
    { name: "size", type: "string", optional: true, enum: ["sm", "md", "lg"] },
    { name: "status", type: "string", optional: true, enum: ["online", "offline", "busy", "away"] },
    { name: "onClick", type: "callable", optional: true, aliases: ["action", "onclick"] }
  ],
  render: (_node, props, helpers) => {
    const isClickable = typeof props.onClick === "function";
    const tag = isClickable ? "button" : "div";
    const size2 = asString(props.size, "md");
    const avatarSize = size2 === "lg" ? "lg" : size2 === "sm" ? "sm" : "md";
    const root = el(tag, {
      type: isClickable ? "button" : null,
      class: "rui-person-chip",
      "data-size": size2
    });
    const avatarWrap = el("span", { class: "rui-person-chip-avatar" });
    avatarWrap.append(renderAvatar(asString(props.avatarSrc), asString(props.name), avatarSize, helpers));
    const status = asString(props.status);
    if (status) {
      avatarWrap.append(el("span", {
        class: "rui-person-chip-status",
        "data-status": status,
        role: "img",
        "aria-label": status,
        title: status
      }));
    }
    root.append(avatarWrap);
    const meta = el("div", { class: "rui-person-chip-meta" });
    meta.append(el("span", { class: "rui-person-chip-name" }, [asString(props.name)]));
    const role = asString(props.role);
    if (role) meta.append(el("span", { class: "rui-person-chip-role" }, [role]));
    root.append(meta);
    if (isClickable) {
      root.onclick = () => helpers.invoke(props.onClick);
    }
    return root;
  }
};
const COL_ALIGN$1 = ["left", "center", "right"];
const SURFACE_TONES = ["default", "primary", "success", "warning", "danger", "info"];
const Col = {
  name: "Col",
  description: 'Single column inside a Table or DataGrid. Use `align` for per-column text alignment, `format` for cell rendering (`text|number|currency|date`), `currency` for the money code used by `format: "currency"`, `locale` for the BCP-47 tag those formats are rendered in, and `width`/`wrap` to stop one long column from forcing the whole table into horizontal scroll. `values` may be plain values OR an array of component nodes — e.g. `Col("Status", rows.map(r => Badge(r.status)))` or `Col("Actions", rows.map(r => Button("Edit")))` — each component renders directly in its cell. Pass `render: (value, index, row) => …` for the same effect when you prefer to keep `values` as the raw row data (return a component, string, or array). `row` is the whole row (header-keyed) and stays correct even when DataGrid sorts — prefer `row.otherColumn` over indexing a sibling array. Pass `onClick: (value, index, row) => …` to make the whole cell clickable (pointer + keyboard). `sortable` and `filterable` only take effect inside `DataGrid` (Table ignores them).',
  props: [
    { name: "header", type: "string" },
    { name: "values", type: "any[]", description: "Column values. Plain values are formatted as text; component nodes (e.g. `rows.map(r => Badge(r.status))`) render directly. You can also pass the full row array and map each cell with `render`." },
    { name: "format", type: "string", optional: true, enum: ["text", "number", "currency", "date"] },
    { name: "align", type: "string", optional: true, enum: COL_ALIGN$1, description: "Per-column horizontal alignment" },
    { name: "sortable", type: "boolean", optional: true, description: "DataGrid: enable click-to-sort on this column" },
    { name: "filterable", type: "boolean", optional: true, description: "DataGrid: enable a per-column filter chip" },
    { name: "render", type: "callable", optional: true, aliases: ["cell"], description: "`(value, index, row) => Component | string | array` — map each cell to arbitrary content (buttons, badges, links). `row` is the whole row (header-keyed), so it stays correct even when DataGrid sorts internally — prefer `row.otherColumn` over an `index` lookup into a sibling array." },
    { name: "onClick", type: "callable", optional: true, aliases: ["onclick", "cellClick"], description: "`(value, index, row) => void` — fired when a cell in this column is clicked or activated via keyboard." },
    // New slots are appended, never inserted: DataGrid reads Col slots 0-7 positionally.
    { name: "currency", type: "string", optional: true, description: 'ISO 4217 code used by `format: "currency"` — `EUR`, `GBP`, `CHF`, … (default `USD`).' },
    { name: "width", type: "string", optional: true, description: "CSS width for the column (`240px`, `30%`). Caps the column instead of letting one long value stretch the table." },
    { name: "wrap", type: "boolean", optional: true, description: "Let long cell text wrap onto several lines instead of pushing the table into horizontal scroll." },
    { name: "headerTooltip", type: "string", optional: true, aliases: ["hint"], description: "Explanation shown on hover/focus of the header cell — e.g. `MRR = monthly recurring revenue`." },
    { name: "locale", type: "string", optional: true, description: 'BCP-47 tag (`de-DE`, `en-GB`, `fr-CH`) used by `format: "number"|"currency"|"date"` for separators, digit grouping and date order. Defaults to the Table\'s `locale`, then the viewer\'s browser.' },
    // Slots 13–17: DataGrid advanced features. Table ignores these.
    { name: "initiallyHidden", type: "boolean", optional: true, aliases: ["colHidden"], description: "DataGrid: initially hide this column. The user can reveal it from the column settings panel." },
    { name: "pinned", type: "string", optional: true, enum: ["left", "right"], description: "DataGrid: pin this column to the left (or right) edge so it stays visible during horizontal scrolling." },
    { name: "resizable", type: "boolean", optional: true, description: "DataGrid: per-column override for resizing. Takes precedence over the grid-level `resizable` prop." },
    { name: "minWidth", type: "string", optional: true, description: "DataGrid: minimum width when the column is resized (`80px`, `5rem`)." },
    { name: "maxWidth", type: "string", optional: true, description: "DataGrid: maximum width when the column is resized (`400px`, `50%`)." }
  ],
  // Cols are read positionally inside Table.render — this render is a fallback.
  render: (_node, props) => {
    const wrapper = el("div", { class: "rui-col" });
    wrapper.append(el("strong", {}, [asString(props.header)]));
    return wrapper;
  }
};
const TABLE_DENSITY = ["comfortable", "compact"];
const Table = {
  name: "Table",
  description: 'Tabular data view. Children must be Col components. `density="compact"` tightens row padding for dense data, `striped=true` zebra-stripes the rows, `sticky=true` pins the header row when the table scrolls, and `maxHeight` sizes that scroll box. Set `loading=true` while a query is in flight so the table shows skeleton rows instead of its empty state, `onRowClick` for row-level navigation, and `allowOverflow=true` when a cell renders an overlay that must escape the scroll box. The empty-state row uses `emptyLabel` when set.',
  props: [
    { name: "columns", type: "Col[]" },
    { name: "caption", type: "string", optional: true, aliases: ["title"] },
    { name: "density", type: "string", optional: true, enum: TABLE_DENSITY, description: "Row padding (default `comfortable`)" },
    { name: "striped", type: "boolean", optional: true, description: "Zebra-stripe alternating rows" },
    { name: "sticky", type: "boolean", optional: true, description: "Pin the header row when the table scrolls" },
    { name: "emptyLabel", type: "string", optional: true, description: "Text shown when the table has no rows (default `No data`)" },
    { name: "loading", type: "boolean", optional: true, description: "Show shimmering skeleton rows while data loads, instead of the empty state" },
    { name: "loadingRows", type: "number", optional: true, description: "Skeleton rows drawn while `loading` (default 3)" },
    { name: "maxHeight", type: "string", optional: true, description: "CSS max-height for the scroll box (`320px`, `50vh`) — overrides the default cap `sticky` applies" },
    { name: "onRowClick", type: "callable", optional: true, aliases: ["rowAction"], description: "`(rowIndex, row) => void` — fired when a row is clicked or activated via keyboard. `row` is header-keyed." },
    { name: "allowOverflow", type: "boolean", optional: true, aliases: ["overflow"], description: "Drop the wrapper's scroll clipping so an overlay rendered inside a cell is not cut off (the table then widens the page instead of scrolling internally)." },
    { name: "ariaLabel", type: "string", optional: true, description: "Accessible name for the table when there is no `caption`" },
    { name: "locale", type: "string", optional: true, description: "BCP-47 tag (`de-DE`, `en-GB`) applied to every `number`/`currency`/`date` column that does not set its own `locale` — an invoice table formats in the customer's locale, not the browser's." }
  ],
  render: (_node, props, helpers) => {
    const cols = asColumnNodes(props.columns);
    const density = asString(props.density, "comfortable");
    const striped = asBoolean(props.striped);
    const loading = asBoolean(props.loading);
    const maxHeight = sanitiseCssLength(props.maxHeight, "");
    const allowOverflow = asBoolean(props.allowOverflow);
    const sticky = asBoolean(props.sticky) && !allowOverflow;
    const wrapperStyle2 = allowOverflow ? "overflow:visible;" : maxHeight ? `max-height:${maxHeight};overflow:auto;` : "";
    const wrapper = el("div", {
      class: "rui-table-wrapper",
      "data-density": density,
      "data-striped": striped ? "true" : "false",
      "data-sticky": sticky ? "true" : "false",
      // Kept as a styling hook: the table now widens the page instead of
      // scrolling inside itself, which the theme may want to react to.
      "data-overflow": allowOverflow ? "visible" : null,
      style: wrapperStyle2 || null
    });
    const caption = asString(props.caption);
    const ariaLabel = asString(props.ariaLabel);
    const table = el("table", {
      class: "rui-table",
      // A `<caption>` already names the table, and an aria-label would shadow
      // it — only name the table explicitly when there is no caption.
      "aria-label": !caption && ariaLabel ? ariaLabel : null,
      "aria-busy": loading ? "true" : null
    });
    if (caption) table.append(el("caption", { class: "rui-table-caption" }, [caption]));
    const aligns = cols.map((col) => {
      const align = asString(col.args?.[3], "");
      return COL_ALIGN$1.includes(align) ? align : "";
    });
    const widths = cols.map((col) => sanitiseCssLength(col.args?.[9], ""));
    const cellStyles = widths.map((width) => width ? `width:${width};max-width:${width};` : null);
    const wrapAttrs = cols.map((col) => {
      const raw = col.args?.[10];
      if (raw === void 0 || raw === null) return null;
      return asBoolean(raw) ? "true" : "false";
    });
    if (wrapAttrs.some((w) => w === "true")) table.style.minWidth = "0";
    const thead = el("thead");
    const headRow = el("tr");
    for (let c = 0; c < cols.length; c += 1) {
      const col = cols[c];
      const headerTooltip = asString(col.args?.[11]);
      const th = el("th", {
        // Explicit association: the implicit-header heuristic fails as soon as
        // the table gains a caption row or a merged layout.
        scope: "col",
        "data-align": aligns[c] || null,
        "data-wrap": wrapAttrs[c] ?? null,
        title: headerTooltip || null,
        style: cellStyles[c] ?? null
      }, [asString(col.args?.[0])]);
      headRow.append(th);
    }
    thead.append(headRow);
    table.append(thead);
    const tbody = el("tbody");
    const columnValues = cols.map((col) => asArray(col.args?.[1]));
    const formats = cols.map((col) => asString(col.args?.[2], "text"));
    const renders = cols.map((col) => col.args?.[6]);
    const clicks = cols.map((col) => col.args?.[7]);
    const tableLocale = localeTag(props.locale);
    const formatters = cols.map((col) => colFormatter(col.args, tableLocale));
    const rowCount = Math.max(0, ...columnValues.map((c) => c.length));
    const onRowClick = props.onRowClick;
    const headers = cols.map((col, c) => asString(col.args?.[0]) || `col-${c}`);
    for (let r = 0; r < rowCount; r += 1) {
      const tr = el("tr");
      const rowObj = {};
      columnValues.forEach((values, c) => {
        rowObj[headers[c]] = values[r];
      });
      columnValues.forEach((values, c) => {
        const format = formats[c] ?? "text";
        const align = aligns[c];
        const td = el("td", {
          "data-format": format,
          "data-align": align || null,
          "data-wrap": wrapAttrs[c] ?? null,
          style: cellStyles[c] ?? null
        });
        fillTableCell(
          td,
          { format, render: renders[c], onClick: clicks[c] },
          values[r],
          r,
          helpers,
          formatters[c] ?? formatCell,
          rowObj
        );
        tr.append(td);
      });
      if (typeof onRowClick === "function") {
        tr.setAttribute("data-clickable", "true");
        tr.tabIndex = 0;
        const fire = (event) => {
          const target = event.target;
          if (target?.closest("input,button,a,label,select,textarea")) return false;
          helpers.invoke(onRowClick, r, rowObj);
          return true;
        };
        tr.onclick = (event) => {
          fire(event);
        };
        tr.onkeydown = (event) => {
          if (event.key !== "Enter" && event.key !== " ") return;
          if (fire(event)) event.preventDefault();
        };
      }
      tbody.append(tr);
    }
    if (rowCount === 0 && loading) {
      const skeletonRows = Math.max(1, Math.min(20, Math.round(asNumber(props.loadingRows, 3))));
      const skeletonCols = Math.max(1, cols.length);
      for (let r = 0; r < skeletonRows; r += 1) {
        const tr = el("tr", { class: "rui-table-loading-row", "aria-hidden": "true" });
        for (let c = 0; c < skeletonCols; c += 1) {
          const td = el("td");
          td.append(el("div", { class: "rui-skeleton-line", style: "height:12px;" }));
          tr.append(td);
        }
        tbody.append(tr);
      }
    } else if (rowCount === 0) {
      const emptyRow = el("tr");
      const emptyLabel = asString(props.emptyLabel, "No data");
      emptyRow.append(el("td", {
        colspan: String(cols.length || 1),
        class: "rui-table-empty"
      }, [emptyLabel]));
      tbody.append(emptyRow);
    }
    table.append(tbody);
    wrapper.append(table);
    return wrapper;
  }
};
const ListItem = {
  name: "ListItem",
  description: "Single list row: title, optional description, optional leading icon, and an optional `trailing` slot for a badge / switch / chevron. Pass `onClick` (or `href`) to make the row activatable, `active=true` to mark it as the current selection, and `tone` for per-row status emphasis.",
  props: [
    { name: "title", type: "string" },
    { name: "description", type: "string", optional: true, aliases: ["meta"] },
    { name: "icon", type: "string", optional: true, description: "Font Awesome icon name" },
    { name: "onClick", type: "callable", optional: true, aliases: ["onclick", "action"], description: "Callable fired when the row is activated (click or Enter/Space)" },
    { name: "href", type: "string", optional: true, description: "Render the row as a real link so middle-click / open-in-new-tab work" },
    { name: "trailing", type: "Node[]", optional: true, aliases: ["actions", "accessory"], description: "Components pinned to the right of the row (Badge, Switch, icon button)" },
    { name: "active", type: "boolean", optional: true, aliases: ["selected"], description: "Marks the row as the current selection (list-as-navigation, list-as-picker)" },
    { name: "tone", aliases: ["variant"], type: "string", optional: true, enum: SURFACE_TONES, description: "Status emphasis for the row (failed / queued / done lists)" }
  ],
  render: (_node, props, helpers) => {
    const hasAction = typeof props.onClick === "function";
    return buildListRow({
      title: asString(props.title),
      description: asString(props.description),
      icon: props.icon,
      tone: asString(props.tone, "default"),
      active: asBoolean(props.active),
      href: sanitiseHref(props.href, ""),
      onActivate: hasAction ? () => helpers.invoke(props.onClick) : null,
      trailing: asArray(props.trailing).map((item) => helpers.renderNode(item))
    });
  }
};
const List = {
  name: "List",
  description: "Vertical list. `items` are normally `ListItem(...)` nodes, but plain strings and `{title, description, icon}` objects are accepted and coerced into rows. `divided=true` renders a flush list with hairline separators instead of a bordered card per row, `gap` spaces the rows, and `emptyLabel` is shown when `items` is empty.",
  props: [
    { name: "items", type: "ListItem[]" },
    { name: "ordered", type: "boolean", optional: true },
    { name: "emptyLabel", type: "string", optional: true, description: "Text shown when the list has no items (default `No items`)" },
    { name: "divided", type: "boolean", optional: true, description: "Flush list separated by hairlines instead of one bordered card per row" },
    { name: "gap", type: "string", optional: true, enum: SPACING_TOKENS, description: "Space between rows" }
  ],
  render: (_node, props, helpers) => {
    const tag = asBoolean(props.ordered) ? "ol" : "ul";
    const gap = spacingCssValue(props.gap);
    const root = el(tag, {
      class: "rui-list",
      "data-divided": asBoolean(props.divided) ? "true" : null,
      style: gap ? `gap:${gap};` : null
    });
    const items = asArray(props.items);
    for (const item of items) root.append(renderListEntry(item, helpers));
    if (items.length === 0) {
      root.append(el("li", { class: "rui-list-empty" }, [asString(props.emptyLabel, "No items")]));
    }
    return root;
  }
};
const StatCard = {
  name: "StatCard",
  description: 'Single KPI card with label, value, optional delta, optional icon, optional `hint` ("vs. last quarter"), and optional inline sparkline (`spark=[…numbers]`). Pass `onClick` to make the whole tile a drill-down target. Use inside `Stats` for a uniform KPI strip.',
  props: [
    { name: "label", type: "string" },
    { name: "value", type: "string" },
    { name: "trend", type: "string", optional: true, enum: ["up", "down", "flat"] },
    { name: "delta", type: "string", optional: true, description: "Change vs previous period" },
    { name: "icon", type: "string", optional: true, description: 'Font Awesome icon name shown in a chip beside the label. Omit it and one is guessed from the label; pass `"none"` for no icon at all.' },
    { name: "spark", type: "number[]", optional: true, description: "Optional inline sparkline values" },
    { name: "tone", aliases: ["variant"], type: "string", optional: true, enum: SURFACE_TONES },
    { name: "hint", type: "string", optional: true, aliases: ["description"], description: "Small qualifier under the value (`vs. last quarter`, `excl. tax`)" },
    { name: "onClick", type: "callable", optional: true, aliases: ["onclick", "action"], description: "Callable fired when the card is clicked — the standard KPI drill-down" }
  ],
  render: (_node, props, helpers) => {
    const tone = asString(props.tone, "default");
    const clickable = typeof props.onClick === "function";
    const root = el(clickable ? "button" : "div", {
      class: "rui-stat-card",
      type: clickable ? "button" : null,
      "data-tone": tone,
      "data-clickable": clickable ? "true" : null
    });
    const labelRow = el("div", { class: "rui-stat-label-row" });
    const label = asString(props.label);
    const iconName = iconOverride(props.icon) ?? pickIconForLabel(label) ?? "";
    const iconNode = renderIcon(iconName, { className: "rui-stat-icon" });
    if (iconNode) labelRow.append(iconNode);
    labelRow.append(el("div", { class: "rui-stat-label" }, [label]));
    root.append(labelRow);
    root.append(el("div", { class: "rui-stat-value" }, [asString(props.value)]));
    const delta = asString(props.delta);
    const trend = asString(props.trend);
    if (delta || trend) {
      const arrow = trend ? trendArrow(trend) : "";
      root.append(el("div", {
        class: "rui-stat-trend",
        "data-trend": trend || "flat",
        "aria-label": trend ? [delta, `trending ${trend}`].filter(Boolean).join(", ") : null
      }, [[arrow, delta].filter(Boolean).join(" ")]));
    }
    const hint = asString(props.hint);
    if (hint) root.append(el("div", { class: "rui-stat-hint rui-stats-hint" }, [hint]));
    const spark = finiteNumbers(props.spark);
    if (spark.length > 1) {
      const sparkWrap = el("div", { class: "rui-stat-spark" });
      sparkWrap.append(renderInlineSparkline(spark, tone === "default" ? "primary" : tone));
      root.append(sparkWrap);
    }
    if (clickable) root.onclick = () => helpers.invoke(props.onClick);
    return root;
  }
};
const Sparkline = {
  name: "Sparkline",
  description: 'Tiny inline trend chart for KPIs, table cells, and dashboards. Renders an SVG line with a soft fill — use anywhere you would otherwise reach for `LineChart` but a single value series should stay inline with surrounding text. `width`/`height` size it (default 80x24), `min`/`max` fix the scale so a strip of sparklines shares one baseline, and `label` gives screen readers the trend ("7-day trend, up 12%").',
  props: [
    { name: "values", type: "number[]" },
    { name: "tone", aliases: ["variant"], type: "string", optional: true, enum: ["primary", "success", "warning", "danger", "info"] },
    { name: "width", type: "number|string", optional: true, description: "Chart width — a px number or any CSS length (`100%`, `12rem`). Default 80." },
    { name: "height", type: "number|string", optional: true, description: "Chart height — a px number or any CSS length. Default 24." },
    { name: "min", type: "number", optional: true, description: "Fixed scale minimum. Set `min`/`max` on every sparkline in a strip so their slopes are comparable." },
    { name: "max", type: "number", optional: true, description: "Fixed scale maximum" },
    { name: "label", type: "string", optional: true, aliases: ["ariaLabel"], description: "Screen-reader description of the trend. Without it the chart stays decorative (`aria-hidden`)." }
  ],
  render: (_node, props) => {
    const tone = asString(props.tone, "primary");
    const values = finiteNumbers(props.values);
    const wrap = el("span", { class: "rui-sparkline-wrap" });
    if (values.length === 0) {
      wrap.append(el("span", { class: "rui-sparkline-empty" }, ["—"]));
      return wrap;
    }
    const svg = renderSparklineSvg(values, tone, {
      width: props.width,
      height: props.height,
      min: props.min === void 0 || props.min === null ? null : asNumber(props.min),
      max: props.max === void 0 || props.max === null ? null : asNumber(props.max),
      label: asString(props.label)
    });
    wrap.append(svg);
    const widthAttr = svg.getAttribute("width") ?? "";
    if (pxValue(widthAttr) === null) wrap.style.width = widthAttr;
    return wrap;
  }
};
const TreeNode = {
  name: "TreeNode",
  description: "Single node in a Tree view. When `children` is provided the node renders as an expandable branch with a chevron; otherwise it renders as a leaf. `onClick` fires on click, `href` makes the row a real link, `onToggle` fires with the new open state, and `disabled=true` makes the row inert. Set `hasChildren=true` (with no `children` yet) plus `onToggle` to lazy-load a subtree the first time the branch is opened. `expanded` is the INITIAL open state — a branch the user collapsed stays collapsed across re-renders. Use `active=true` to highlight the current selection, or drive selection from `Tree(selectedId:)` instead. In a checkable Tree it is the node's `nodeId` (or label) that appears in `Tree(checkedIds:)`.",
  props: [
    { name: "label", type: "string" },
    { name: "children", aliases: ["child"], type: "TreeNode[]", optional: true },
    { name: "icon", type: "string", optional: true, description: "Font Awesome icon shown before the label" },
    { name: "expanded", type: "boolean", optional: true, description: "Whether the branch starts open (initial value — the user's own collapse wins afterwards)" },
    { name: "active", type: "boolean", optional: true, aliases: ["selected"], description: "Highlights the row as the current selection" },
    { name: "badge", type: "string", optional: true, description: "Trailing chip (count or status)" },
    { name: "onClick", type: "callable", optional: true, aliases: ["action", "onclick"], description: "Callable fired when the row is clicked" },
    { name: "href", type: "string", optional: true, description: "Render the row as a real link (docs sidebars, file paths) so middle-click works" },
    { name: "disabled", type: "boolean", optional: true, description: "Renders the row inert — permission-gated or unavailable nodes" },
    { name: "onToggle", type: "callable", optional: true, description: "`(open) => void` — fired when the branch expands or collapses. Use it to lazy-load children." },
    { name: "nodeId", type: "string", optional: true, aliases: ["value"], description: "Stable id for `Tree(selectedId:)` / `Tree(expandedIds:)` (defaults to the label)" },
    { name: "hasChildren", type: "boolean", optional: true, aliases: ["lazy"], description: "Render an expandable branch even though `children` is still empty — the lazy-loading half of `onToggle`: open the branch, fetch, re-render with real children." }
  ],
  render: (_node, props, helpers) => {
    const children = asArray(props.children);
    const hasChildren = children.length > 0 || asBoolean(props.hasChildren);
    const active = asBoolean(props.active);
    const disabled = asBoolean(props.disabled);
    const label = asString(props.label);
    const nodeId = asString(props.nodeId) || label;
    const href = disabled ? "" : sanitiseHref(props.href, "");
    const hasAction = !disabled && typeof props.onClick === "function";
    const expandedProp = props.expanded === void 0 || props.expanded === null ? null : asBoolean(props.expanded);
    const openSlot = helpers.useInstanceState("open", expandedProp ?? false);
    const lastExpandedProp = helpers.useInstanceState("expandedProp", expandedProp);
    if (expandedProp !== null && expandedProp !== lastExpandedProp.get()) {
      lastExpandedProp.set(expandedProp);
      openSlot.set(expandedProp);
    }
    const open = hasChildren && openSlot.get();
    const rowTag = href ? "a" : hasAction ? "button" : "div";
    const row = el(rowTag, {
      type: rowTag === "button" ? "button" : null,
      href: href || null,
      class: "rui-tree-node-row",
      "data-tree-id": nodeId,
      "data-active": active ? "true" : "false",
      "data-disabled": disabled ? "true" : null,
      "aria-disabled": disabled ? "true" : null,
      disabled: rowTag === "button" && disabled ? "" : null,
      // A branch's treeitem is the `<details>` below, because that is the
      // element which also *contains* the `role="group"` child. Keeping the
      // role on the label row (as this used to) left `role="tree"` owning zero
      // treeitems, so the whole nesting/level relationship was lost.
      role: hasChildren ? null : "treeitem",
      "aria-level": hasChildren ? null : "1",
      "aria-selected": hasChildren ? null : active ? "true" : "false"
    });
    if (!hasChildren) {
      row.append(el("span", { class: "rui-tree-node-chevron-spacer", "aria-hidden": "true" }));
    }
    const iconNode = renderIcon(props.icon, { className: "rui-tree-node-icon" });
    if (iconNode) row.append(iconNode);
    row.append(el("span", { class: "rui-tree-node-label" }, [label]));
    const badge = asString(props.badge);
    if (badge) row.append(el("span", { class: "rui-tree-node-badge" }, [badge]));
    if (hasAction) {
      row.onclick = () => helpers.invoke(props.onClick);
    }
    if (!hasChildren) return row;
    const details = el("details", {
      class: "rui-tree-node",
      role: "treeitem",
      "data-tree-id": nodeId,
      // The `<details>` is the treeitem, so it is the element whose state AT
      // reads — and the element a checkable Tree guards on. Carrying `disabled`
      // only on the inner row left a disabled folder checkable.
      "aria-disabled": disabled ? "true" : null,
      "data-active": active ? "true" : "false",
      "aria-expanded": open ? "true" : "false",
      "aria-level": "1",
      "aria-selected": active ? "true" : "false"
    });
    if (open) details.setAttribute("open", "");
    details.ontoggle = (event) => {
      const live = event.currentTarget ?? event.target;
      openSlot.set(live.open);
      live.setAttribute("aria-expanded", live.open ? "true" : "false");
      live.querySelector(".rui-tree-node-chevron-button")?.setAttribute("aria-expanded", live.open ? "true" : "false");
      helpers.invoke(props.onToggle, live.open);
    };
    const summary = el("summary", {
      class: "rui-tree-node-summary",
      // Flatten the disclosure wrapper: the <details> is the treeitem, and an
      // extra button between `tree` and `treeitem` breaks the ARIA structure.
      role: "none",
      style: TREE_SUMMARY_RESET
    });
    const toggle = el("button", {
      type: "button",
      class: "rui-tree-node-chevron-button",
      "aria-label": `Toggle ${label}`,
      "aria-expanded": open ? "true" : "false",
      style: TREE_CHEVRON_RESET
    });
    const chevron = renderIcon("chevron-right", { className: "rui-tree-node-chevron" });
    if (chevron) toggle.append(chevron);
    toggle.onclick = (event) => {
      event.preventDefault();
      event.stopPropagation();
      const live = event.currentTarget.closest("details.rui-tree-node");
      if (!(live instanceof HTMLDetailsElement)) return;
      live.open = !live.open;
    };
    summary.append(toggle);
    summary.append(row);
    if (hasAction || href) {
      summary.onclick = (event) => {
        const target = event.target;
        if (target?.closest(".rui-tree-node-chevron-button, .rui-tree-node-checkbox")) return;
        event.preventDefault();
      };
    }
    details.append(summary);
    const childList = el("div", { class: "rui-tree-node-children", role: "group" });
    for (const child of children) childList.append(helpers.renderNode(child));
    if (children.length === 0) {
      childList.append(el("div", {
        class: "rui-tree-node-row rui-tree-node-pending",
        role: "treeitem",
        "aria-level": "1",
        "aria-disabled": "true",
        style: TREE_PENDING_RESET
      }, ["Loading…"]));
    }
    for (const descendant of childList.querySelectorAll('[role="treeitem"]')) {
      descendant.setAttribute(
        "aria-level",
        String(asNumber(descendant.getAttribute("aria-level"), 1) + 1)
      );
    }
    details.append(childList);
    return details;
  }
};
const Tree = {
  name: "Tree",
  description: "Hierarchical tree view. Children must be TreeNode entries. Use for file browsers, nested navigation, category pickers, and any parent/child structure with arbitrary depth. Bind `selectedId` to a `$variable` (and/or pass `onSelect`) for container-level single selection instead of wiring `active` + `onClick` on every node; `expandedIds` opens the named branches (reveal a search hit, expand all). `checkable=true` puts a checkbox on every node and binds `checkedIds` — the standard shape for a folder-sync / permission picker; checking a branch checks its whole subtree and a partly-checked branch renders mixed. The tree implements the standard keyboard model: Up/Down move, Right/Left expand/collapse, Home/End jump, Enter activates, Space checks.",
  props: [
    { name: "items", type: "TreeNode[]" },
    { name: "selectedId", type: "string", optional: true, aliases: ["selected", "value"], description: "Id (or label) of the selected node — bind a `$variable` for two-way selection" },
    { name: "onSelect", type: "callable", optional: true, description: "`(nodeId) => void` — fired when a row is activated" },
    { name: "expandedIds", type: "any[]", optional: true, description: "Ids (or labels) of branches to open. Additive: it opens branches, it never forces one closed." },
    { name: "ariaLabel", type: "string", optional: true, description: "Accessible name for the tree (`Files`, `Categories`) — needed as soon as a page shows more than one" },
    { name: "emptyLabel", type: "string", optional: true, description: "Text shown when there are no items (default `No items`)" },
    // Appended, never inserted: `checkedIds` is read from slot 7 for binding.
    { name: "checkable", type: "boolean", optional: true, description: 'Show a checkbox on every node for multi-selection ("pick the folders to sync", permission trees)' },
    { name: "checkedIds", type: "any[]", optional: true, aliases: ["checkedKeys"], description: "Ids (or labels) of the checked nodes — bind a `$variable` for two-way multi-selection. Checking a branch checks its subtree; a partly-checked branch is reported mixed and is NOT listed here." },
    { name: "onCheck", type: "callable", optional: true, description: "`(checkedIds) => void` — fired with the whole checked-id array whenever a checkbox changes" }
  ],
  render: (node, props, helpers) => {
    const items = asArray(props.items);
    const checkedStateName = node.argMeta?.[7]?.stateRef;
    const checkable = asBoolean(props.checkable) || checkedStateName !== void 0;
    const root = el("div", {
      class: "rui-tree",
      role: "tree",
      "aria-label": asString(props.ariaLabel) || null,
      // Checkboxes are exactly what `aria-multiselectable` describes; without it
      // a screen reader announces a single-select tree with stray checked states.
      "aria-multiselectable": checkable ? "true" : null
    });
    for (const item of items) root.append(helpers.renderNode(item));
    if (items.length === 0) {
      root.append(el("div", { class: "rui-tree-empty" }, [asString(props.emptyLabel, "No items")]));
      return root;
    }
    const checkedSlot = helpers.useInstanceState("checked", null);
    const readChecked = () => {
      const src = checkedStateName ? props.checkedIds : checkedSlot.get() ?? props.checkedIds;
      return asArray(src).map((v) => asString(v));
    };
    const expandedIds = new Set(asArray(props.expandedIds).map((v) => asString(v)));
    if (expandedIds.size > 0) {
      for (const branch of root.querySelectorAll("details.rui-tree-node")) {
        if (!expandedIds.has(branch.dataset.treeId ?? "")) continue;
        branch.setAttribute("open", "");
        branch.setAttribute("aria-expanded", "true");
      }
    }
    if (checkable) {
      installTreeCheckboxes(root);
      applyTreeChecks(root, new Set(readChecked()));
    }
    const selectedId = asString(props.selectedId);
    if (selectedId) applyTreeSelection(root, selectedId);
    applyRovingTabindex(root);
    const selectStateName = node.argMeta?.[1]?.stateRef;
    const wantsSelect = typeof props.onSelect === "function" || selectStateName !== void 0;
    const toggleCheck = (item, liveRoot) => {
      if (item.getAttribute("aria-disabled") === "true") return;
      const on = item.getAttribute("aria-checked") !== "true";
      const next = new Set(readChecked());
      for (const target of [item, ...subtreeTreeItems(item)]) {
        if (target.getAttribute("aria-disabled") === "true") continue;
        const id = target.dataset.treeId ?? "";
        if (!id) continue;
        if (on) next.add(id);
        else next.delete(id);
      }
      normaliseTreeChecks(liveRoot, next);
      const ids = Array.from(next);
      if (checkedStateName) helpers.setState(checkedStateName, ids);
      else checkedSlot.set(ids);
      helpers.invoke(props.onCheck, ids);
      applyTreeChecks(liveRoot, new Set(ids));
    };
    if (wantsSelect || checkable) {
      root.onclick = (event) => {
        const target = event.target;
        if (!target) return;
        const liveRoot = event.currentTarget ?? event.target;
        if (checkable && target.classList.contains("rui-tree-node-checkbox")) {
          const item = treeItemForCheckbox(target);
          if (item) toggleCheck(item, liveRoot);
          return;
        }
        if (!wantsSelect || target.closest(".rui-tree-node-chevron-button")) return;
        const row = target.closest(".rui-tree-node-row");
        if (!(row instanceof HTMLElement) || row.getAttribute("aria-disabled") === "true") return;
        const id = row.dataset.treeId ?? "";
        applyTreeSelection(liveRoot, id);
        applyRovingTabindex(liveRoot);
        if (selectStateName) helpers.setState(selectStateName, id);
        helpers.invoke(props.onSelect, id);
      };
    }
    root.onkeydown = (event) => {
      if (!TREE_KEYS.has(event.key)) return;
      const liveRoot = event.currentTarget ?? event.target;
      const visible = visibleTreeItems(liveRoot);
      if (visible.length === 0) return;
      const origin = event.target?.closest('[role="treeitem"]');
      const current = origin instanceof HTMLElement ? origin : null;
      const index = current ? visible.indexOf(current) : -1;
      const focusAt = (next) => {
        const target = visible[Math.max(0, Math.min(visible.length - 1, next))];
        if (!target) return;
        for (const item of treeItems(liveRoot)) item.tabIndex = item === target ? 0 : -1;
        target.focus();
      };
      switch (event.key) {
        case "ArrowDown":
          event.preventDefault();
          focusAt(index + 1);
          return;
        case "ArrowUp":
          event.preventDefault();
          focusAt(index < 0 ? 0 : index - 1);
          return;
        case "Home":
          event.preventDefault();
          focusAt(0);
          return;
        case "End":
          event.preventDefault();
          focusAt(visible.length - 1);
          return;
      }
      if (!current) return;
      const branch = current instanceof HTMLDetailsElement ? current : null;
      if (event.key === "ArrowRight") {
        event.preventDefault();
        if (!branch) return;
        if (!branch.open) branch.open = true;
        else focusAt(index + 1);
        return;
      }
      if (event.key === "ArrowLeft") {
        event.preventDefault();
        if (branch?.open) {
          branch.open = false;
          return;
        }
        const parent = current.parentElement?.closest('[role="treeitem"]');
        if (parent instanceof HTMLElement) focusAt(visible.indexOf(parent));
        return;
      }
      if (checkable && event.key === " ") {
        event.preventDefault();
        toggleCheck(current, liveRoot);
        return;
      }
      const rowNode = current.classList.contains("rui-tree-node-row") ? current : current.querySelector(".rui-tree-node-summary > .rui-tree-node-row");
      event.preventDefault();
      if (rowNode instanceof HTMLElement && rowNode.tagName !== "DIV") rowNode.click();
      else if (branch) branch.open = !branch.open;
    };
    return root;
  }
};
const CONTROL_RESET = "appearance:none;background:none;border:0;padding:0;font:inherit;color:inherit;text-decoration:none;cursor:pointer";
const LIST_ACTION_RESET = `${CONTROL_RESET};display:flex;flex-direction:column;gap:2px;min-width:0;flex:1 1 auto;text-align:left`;
const TRAILING_SLOT = "margin-left:auto;display:flex;align-items:center;gap:var(--rui-spacing-xs);flex:0 0 auto";
const TREE_SUMMARY_RESET = "display:flex;align-items:center;gap:2px;list-style:none";
const TREE_CHEVRON_RESET = `${CONTROL_RESET};display:inline-flex;align-items:center;justify-content:center;flex:0 0 auto`;
const TREE_PENDING_RESET = "color:var(--rui-color-text-muted);cursor:default";
const TREE_CHECKBOX_RESET = "flex:0 0 auto;width:14px;height:14px;margin:0;cursor:pointer;accent-color:var(--rui-color-primary)";
const TREE_CHECK_ROW_RESET = "display:flex;align-items:center;gap:2px";
function trendArrow(trend) {
  if (trend === "up") return "▲";
  if (trend === "down") return "▼";
  return "—";
}
function iconOverride(value) {
  if (value === void 0 || value === null) return null;
  if (value === false) return "";
  const name = asString(value).trim();
  return name === "" || name.toLowerCase() === "none" ? "" : name;
}
function finiteNumbers(value) {
  const out = [];
  for (const entry of asArray(value)) {
    if (entry === null || entry === void 0 || entry === "") continue;
    const n = typeof entry === "number" ? entry : Number(asString(entry));
    if (Number.isFinite(n)) out.push(n);
  }
  return out;
}
const SPARK_WIDTH = 80;
const SPARK_HEIGHT = 24;
const SVG_NS$1 = "http://www.w3.org/2000/svg";
function renderSparklineSvg(values, tone, opts) {
  const widthAttr = cssSize(opts.width, String(SPARK_WIDTH));
  const heightAttr = cssSize(opts.height, String(SPARK_HEIGHT));
  const vbW = pxValue(widthAttr) ?? SPARK_WIDTH;
  const vbH = pxValue(heightAttr) ?? SPARK_HEIGHT;
  const svg = document.createElementNS(SVG_NS$1, "svg");
  svg.setAttribute("class", "rui-sparkline");
  svg.setAttribute("data-tone", tone);
  svg.setAttribute("viewBox", `0 0 ${vbW} ${vbH}`);
  svg.setAttribute("width", widthAttr);
  svg.setAttribute("height", heightAttr);
  svg.setAttribute("preserveAspectRatio", "none");
  if (opts.label) {
    svg.setAttribute("role", "img");
    svg.setAttribute("aria-label", opts.label);
  } else {
    svg.setAttribute("aria-hidden", "true");
  }
  const lo = opts.min ?? Math.min(...values);
  const hi = opts.max ?? Math.max(...values);
  const min = Math.min(lo, hi);
  const max = Math.max(lo, hi);
  const range = max - min || 1;
  const yFor = (value) => 2 + (max - Math.min(max, Math.max(min, value))) / range * (vbH - 4);
  if (values.length === 1) {
    const mid = (vbH / 2).toFixed(1);
    const base = document.createElementNS(SVG_NS$1, "path");
    base.setAttribute("class", "rui-sparkline-baseline");
    base.setAttribute("d", `M0,${mid} L${vbW},${mid}`);
    base.setAttribute("stroke", "currentColor");
    base.setAttribute("stroke-width", "1.5");
    base.setAttribute("stroke-opacity", "0.4");
    base.setAttribute("fill", "none");
    base.setAttribute("vector-effect", "non-scaling-stroke");
    svg.appendChild(base);
    const dot = document.createElementNS(SVG_NS$1, "circle");
    dot.setAttribute("class", "rui-sparkline-dot");
    dot.setAttribute("cx", (vbW / 2).toFixed(1));
    dot.setAttribute("cy", mid);
    dot.setAttribute("r", "2");
    dot.setAttribute("fill", "currentColor");
    svg.appendChild(dot);
    return svg;
  }
  const step = vbW / (values.length - 1);
  const linePath = values.map((value, i) => `${i === 0 ? "M" : "L"}${(i * step).toFixed(1)},${yFor(value).toFixed(1)}`).join(" ");
  const area = document.createElementNS(SVG_NS$1, "path");
  area.setAttribute("d", `${linePath} L${vbW},${vbH} L0,${vbH} Z`);
  area.setAttribute("class", "rui-sparkline-area");
  svg.appendChild(area);
  const line = document.createElementNS(SVG_NS$1, "path");
  line.setAttribute("d", linePath);
  line.setAttribute("class", "rui-sparkline-line");
  line.setAttribute("fill", "none");
  line.setAttribute("vector-effect", "non-scaling-stroke");
  svg.appendChild(line);
  return svg;
}
function cssSize(value, fallback) {
  if (typeof value === "number") return Number.isFinite(value) && value > 0 ? String(value) : fallback;
  const raw = asString(value).trim();
  if (!raw) return fallback;
  return /^\d+(\.\d+)?$/.test(raw) ? raw : sanitiseCssLength(raw, fallback);
}
function pxValue(attr) {
  const match = /^(\d+(?:\.\d+)?)(?:px)?$/.exec(attr);
  return match?.[1] ? Number(match[1]) : null;
}
function buildListRow(opts) {
  const href = opts.href ?? "";
  const activate = opts.onActivate ?? null;
  const interactive = !!href || !!activate;
  const li = el("li", {
    class: "rui-list-item",
    "data-tone": opts.tone || "default",
    "data-active": opts.active ? "true" : "false",
    "data-clickable": interactive ? "true" : null,
    "aria-current": opts.active ? "true" : null
  });
  const iconNode = renderIcon(opts.icon, { className: "rui-list-icon" });
  if (iconNode) li.append(iconNode);
  const tag = href ? "a" : activate ? "button" : "div";
  const text = el(tag, {
    class: interactive ? "rui-list-text rui-list-action" : "rui-list-text",
    type: tag === "button" ? "button" : null,
    href: href || null,
    style: interactive ? LIST_ACTION_RESET : null
  });
  text.append(el("div", { class: "rui-list-title" }, [opts.title]));
  if (opts.description) text.append(el("div", { class: "rui-list-description" }, [opts.description]));
  if (activate) text.onclick = () => activate();
  li.append(text);
  const trailing = opts.trailing ?? [];
  if (trailing.length > 0) {
    const slot = el("div", { class: "rui-list-trailing", style: TRAILING_SLOT });
    for (const item of trailing) slot.append(item);
    li.append(slot);
  }
  return li;
}
function renderListEntry(item, helpers) {
  if (isComponentNode(item)) return helpers.renderNode(item);
  if (item === null || item === void 0) return document.createTextNode("");
  if (typeof item === "string" || typeof item === "number" || typeof item === "boolean") {
    return buildListRow({ title: String(item) });
  }
  if (typeof item === "object") {
    const rec = item;
    const title = rec.title ?? rec.label ?? rec.name ?? rec.text;
    if (title !== null && title !== void 0) {
      return buildListRow({
        title: asString(title),
        description: asString(rec.description ?? rec.meta ?? rec.subtitle ?? rec.hint),
        icon: rec.icon,
        tone: asString(rec.tone, "default"),
        active: asBoolean(rec.active ?? rec.selected)
      });
    }
  }
  return el("li", { class: "rui-list-item rui-unknown-component" }, [
    `Unsupported list item (${Array.isArray(item) ? "array" : typeof item})`
  ]);
}
const TREE_KEYS = /* @__PURE__ */ new Set(["ArrowDown", "ArrowUp", "ArrowLeft", "ArrowRight", "Home", "End", "Enter", " "]);
function treeItems(root) {
  return Array.from(root.querySelectorAll('[role="treeitem"]'));
}
function visibleTreeItems(root) {
  return treeItems(root).filter((item) => {
    let parent = item.parentElement;
    while (parent && parent !== root) {
      if (parent instanceof HTMLDetailsElement && !parent.open) return false;
      parent = parent.parentElement;
    }
    return true;
  });
}
function applyTreeSelection(root, id) {
  for (const target of root.querySelectorAll("[data-tree-id]")) {
    const match = (target.dataset.treeId ?? "") === id;
    if (target.classList.contains("rui-tree-node-row")) {
      target.setAttribute("data-active", match ? "true" : "false");
    }
    if (target.getAttribute("role") === "treeitem") {
      target.setAttribute("aria-selected", match ? "true" : "false");
    }
  }
}
function directTreeChild(parent, cls) {
  if (!parent) return null;
  for (const child of Array.from(parent.children)) {
    if (child.classList.contains(cls)) return child;
  }
  return null;
}
function installTreeCheckboxes(root) {
  for (const item of treeItems(root)) {
    if (item.classList.contains("rui-tree-node-pending")) continue;
    const box = el("input", {
      type: "checkbox",
      class: "rui-tree-node-checkbox",
      tabindex: "-1",
      "aria-hidden": "true",
      style: TREE_CHECKBOX_RESET
    });
    const summary = directTreeChild(item, "rui-tree-node-summary");
    if (summary) {
      const row = directTreeChild(summary, "rui-tree-node-row");
      if (row) summary.insertBefore(box, row);
      else summary.append(box);
      continue;
    }
    const holder = el("div", { class: "rui-tree-node-check-row", style: TREE_CHECK_ROW_RESET });
    item.replaceWith(holder);
    holder.append(box);
    holder.append(item);
  }
}
function treeItemForCheckbox(box) {
  const holder = box.parentElement;
  if (!holder) return null;
  if (holder.classList.contains("rui-tree-node-summary")) {
    return holder.parentElement instanceof HTMLElement ? holder.parentElement : null;
  }
  return directTreeChild(holder, "rui-tree-node-row");
}
function treeCheckbox(item) {
  const holder = directTreeChild(item, "rui-tree-node-summary") ?? item.parentElement;
  const box = directTreeChild(holder, "rui-tree-node-checkbox");
  return box instanceof HTMLInputElement ? box : null;
}
function childTreeItems(item) {
  const group = directTreeChild(item, "rui-tree-node-children");
  if (!group) return [];
  const out = [];
  for (const child of Array.from(group.children)) {
    if (child.getAttribute("role") === "treeitem") {
      out.push(child);
      continue;
    }
    const inner = directTreeChild(child, "rui-tree-node-row");
    if (inner?.getAttribute("role") === "treeitem") out.push(inner);
  }
  return out;
}
function subtreeTreeItems(item) {
  return Array.from(item.querySelectorAll('[role="treeitem"]')).filter((n) => !n.classList.contains("rui-tree-node-pending"));
}
function enabledChildTreeItems(item) {
  return childTreeItems(item).filter((kid) => kid.getAttribute("aria-disabled") !== "true" && !kid.classList.contains("rui-tree-node-pending"));
}
function normaliseTreeChecks(root, checked) {
  for (const item of treeItems(root).reverse()) {
    const kids = enabledChildTreeItems(item);
    if (kids.length === 0) continue;
    const id = item.dataset.treeId ?? "";
    if (!id) continue;
    if (kids.every((kid) => checked.has(kid.dataset.treeId ?? ""))) checked.add(id);
    else checked.delete(id);
  }
}
function applyTreeChecks(root, checked) {
  for (const item of treeItems(root).reverse()) {
    if (item.classList.contains("rui-tree-node-pending")) continue;
    const id = item.dataset.treeId ?? "";
    const kids = enabledChildTreeItems(item);
    let state;
    if (kids.length === 0) {
      state = checked.has(id) ? "true" : "false";
    } else if (kids.every((kid) => kid.getAttribute("aria-checked") === "true")) {
      state = "true";
    } else if (kids.some((kid) => kid.getAttribute("aria-checked") !== "false")) {
      state = "mixed";
    } else {
      state = "false";
    }
    item.setAttribute("aria-checked", state);
    const box = treeCheckbox(item);
    if (!box) continue;
    box.checked = state === "true";
    if (state === "true") box.setAttribute("checked", "");
    else box.removeAttribute("checked");
    box.indeterminate = state === "mixed";
    if (state === "mixed") box.setAttribute("data-indeterminate", "true");
    else box.removeAttribute("data-indeterminate");
  }
}
function applyRovingTabindex(root) {
  for (const inner of root.querySelectorAll(
    ".rui-tree-node-summary, .rui-tree-node-summary .rui-tree-node-row, .rui-tree-node-chevron-button"
  )) {
    inner.tabIndex = -1;
  }
  const items = treeItems(root);
  const focusTarget = items.find((item) => item.getAttribute("aria-selected") === "true") ?? items[0];
  for (const item of items) item.tabIndex = item === focusTarget ? 0 : -1;
}
function currencyCode(value) {
  const code = asString(value).trim().toUpperCase();
  return /^[A-Z]{3}$/.test(code) ? code : "USD";
}
function localeTag(value) {
  const tag = asString(value).trim();
  if (!tag) return void 0;
  try {
    return Intl.getCanonicalLocales(tag)[0];
  } catch {
    return void 0;
  }
}
function colFormatter(args, fallbackLocale) {
  const currency = currencyCode(args?.[8]);
  const locale = localeTag(args?.[12]) ?? fallbackLocale;
  return (value, format) => formatCell(value, format, currency, locale);
}
function formatCell(value, format, currency = "USD", locale) {
  if (value === null || value === void 0) return "";
  switch (format) {
    case "number":
      return typeof value === "number" ? value.toLocaleString(locale) : asString(value);
    case "currency":
      return typeof value === "number" ? value.toLocaleString(locale, { style: "currency", currency }) : asString(value);
    case "date":
      try {
        const d = new Date(asString(value));
        return Number.isNaN(d.getTime()) ? asString(value) : d.toLocaleDateString(locale);
      } catch {
        return asString(value);
      }
    default:
      return asString(value);
  }
}
const PALETTE = [
  "var(--rui-chart-1, #6366f1)",
  "var(--rui-chart-2, #10b981)",
  "var(--rui-chart-3, #f59e0b)",
  "var(--rui-chart-4, #ef4444)",
  "var(--rui-chart-5, #06b6d4)",
  "var(--rui-chart-6, #8b5cf6)"
];
const colorAt = (index) => PALETTE[index % PALETTE.length] ?? PALETTE[0];
const CHART_TONES = ["primary", "success", "warning", "danger", "info"];
function resolveTone(raw, fallback = "primary") {
  const token = asString(raw).trim().toLowerCase();
  return CHART_TONES.includes(token) ? token : fallback;
}
const EMPTY_TEXT = "No data";
const LOADING_TEXT = "Loading…";
const CHART_A11Y_PROPS = [
  {
    name: "ariaLabel",
    type: "string",
    optional: true,
    aliases: ["alt"],
    description: "Accessible name for the graphic, overriding the generated one — the way to name a chart that has no visible `title`"
  },
  {
    name: "decorative",
    type: "boolean",
    optional: true,
    description: "Take the graphic out of the accessibility tree. Only for a chart that repeats information already present in the surrounding text"
  }
];
const Series = {
  name: "Series",
  description: "Named data series for charts. Used inside BarChart, LineChart, PieChart, RadarChart and ScatterChart — the chart reads the series definition itself, so a Series is never placed on its own. Pass `values` for numeric charts, `points` for ScatterChart, and `color` to override this series' palette slot.",
  props: [
    { name: "name", type: "string" },
    {
      name: "values",
      type: "number[]",
      optional: true,
      description: "One value per x-axis label (ScatterChart also accepts its {x, y} points here)"
    },
    {
      name: "color",
      type: "string",
      optional: true,
      description: "Override the palette colour for this series (any CSS colour or var())"
    },
    {
      name: "points",
      type: "{x: number, y: number, label?: string}[]",
      optional: true,
      description: "XY points for ScatterChart — the explicit alternative to passing them as `values`"
    }
  ],
  render: (_node, props) => {
    const name = asString(props.name) || "Series";
    return el("span", { class: "rui-chart-empty rui-series", "data-name": name }, [
      `Series "${name}" is not inside a chart — pass it to a chart's \`series\` prop.`
    ]);
  }
};
const readSeries = (raw) => {
  return raw.map((s, i) => {
    const node = s;
    const name = asString(node.args?.[0], `Series ${i + 1}`);
    const values = asArray(node.args?.[1]).map((v) => asNumber(v));
    const color2 = asString(node.args?.[2]).trim();
    return color2 ? { name, values, color: color2 } : { name, values };
  });
};
const seriesColor = (series, index) => series.color || colorAt(index);
const BarChart = {
  name: "BarChart",
  description: "Bar chart. `labels` define the category axis, `series` define the bars. Pass `stacked: true` to stack the series instead of grouping them, `horizontal: true` when the category names are too long for a vertical axis, and `onBarClick(label, value, seriesName)` for drill-down.",
  props: [
    { name: "labels", type: "string[]" },
    { name: "series", type: "Series[]" },
    { name: "title", type: "string", optional: true },
    { name: "stacked", type: "boolean", optional: true, description: "Stack the series on top of each other instead of grouping them side by side" },
    { name: "horizontal", type: "boolean", optional: true, description: "Draw the bars left-to-right — the readable layout for long category names" },
    { name: "xAxisLabel", type: "string", optional: true, description: "Caption for the category axis" },
    { name: "yAxisLabel", type: "string", optional: true, description: 'Caption for the value axis (e.g. "Revenue (EUR)")' },
    { name: "showLegend", type: "boolean", optional: true, description: "Render the series legend (default true)" },
    { name: "height", type: "number", optional: true, description: "Plot height in px (default 240)" },
    { name: "loading", type: "boolean", optional: true, description: "Render a loading placeholder instead of the plot" },
    { name: "emptyText", type: "string", optional: true, description: 'Message shown when there is no data (default "No data")' },
    { name: "onBarClick", type: "callable", optional: true, description: "(label, value, seriesName) => void, fired when a bar is activated" },
    ...CHART_A11Y_PROPS
  ],
  render: (_node, props, helpers) => {
    const labels = asArray(props.labels).map((l) => asString(l));
    const series = readSeries(asArray(props.series));
    const title = asString(props.title);
    const a11y = readChartA11y(props, typeof props.onBarClick === "function");
    const stacked = asBoolean(props.stacked);
    const horizontal = asBoolean(props.horizontal);
    const showLegend = props.showLegend == null ? true : asBoolean(props.showLegend);
    const onBarClick = props.onBarClick;
    const root = el("div", { class: "rui-chart rui-bar-chart" });
    if (title) root.append(el("div", { class: "rui-chart-title" }, [title]));
    if (asBoolean(props.loading)) return chartPlaceholder(root, LOADING_TEXT, true);
    if (!series.some((s) => s.values.length > 0)) {
      return chartPlaceholder(root, asString(props.emptyText) || EMPTY_TEXT, false);
    }
    const groupCount = Math.max(labels.length, ...series.map((s) => s.values.length));
    const categories = Array.from({ length: groupCount }, (_, i) => labels[i] ?? `#${i + 1}`);
    const stackTotals = categories.map((_, gIdx) => series.reduce((acc, s) => acc + Math.max(0, s.values[gIdx] ?? 0), 0));
    const max = stacked ? Math.max(1, ...stackTotals) : Math.max(1, ...series.flatMap((s) => s.values));
    const xAxisLabel = asString(props.xAxisLabel);
    const yAxisLabel = asString(props.yAxisLabel);
    const width = 640;
    const height = readChartHeight(props.height, 240);
    const valueGutter = tickGutter(0, max);
    const svg = horizontal ? renderHorizontalBars({
      width,
      height,
      categories,
      series,
      stacked,
      max,
      categoryGutter: Math.max(48, Math.min(180, longestLabelPx(categories, 18) + 12)),
      valueGutter,
      xAxisLabel,
      yAxisLabel,
      helpers,
      onBarClick
    }) : renderVerticalBars({
      width,
      height,
      categories,
      series,
      stacked,
      max,
      valueGutter,
      xAxisLabel,
      yAxisLabel,
      helpers,
      onBarClick
    });
    nameChart(svg, a11y, chartLabel(
      horizontal ? "Horizontal bar chart" : "Bar chart",
      title,
      `${series.length} series across ${categories.length} categories.`
    ));
    if (height !== 240) svg.setAttribute("style", `max-height:${height}px`);
    root.append(svg);
    if (!a11y.decorative) {
      root.append(chartTable(
        title || "Bar chart data",
        categories,
        series.map((s) => ({
          label: s.name,
          cells: categories.map((_, i) => cellText(s.values[i]))
        }))
      ));
    }
    if (showLegend && series.length > 0) root.append(legend(series));
    return root;
  }
};
function renderVerticalBars(layout) {
  const {
    width,
    height,
    categories,
    series,
    stacked,
    max,
    valueGutter,
    xAxisLabel,
    yAxisLabel,
    helpers,
    onBarClick
  } = layout;
  const padding = {
    left: valueGutter + (yAxisLabel ? AXIS_CAPTION_PX : 0),
    right: 12,
    top: 12,
    bottom: 0
  };
  const innerWidth = width - padding.left - padding.right;
  const labelPlan = planLabels(categories, innerWidth);
  padding.bottom = labelPlan.bottomPadding + (xAxisLabel ? AXIS_CAPTION_PX : 0);
  const innerHeight = height - padding.top - padding.bottom;
  const svg = createSvg(width, height);
  drawAxes(svg, padding, innerWidth, innerHeight, max);
  const groupWidth = innerWidth / Math.max(categories.length, 1);
  const barWidth = stacked ? groupWidth * 0.7 : groupWidth * 0.7 / Math.max(series.length, 1);
  const stackTop = categories.map(() => 0);
  series.forEach((s, sIdx) => {
    categories.forEach((label, gIdx) => {
      const raw = s.values[gIdx];
      if (raw === void 0) return;
      const value = stacked ? Math.max(0, raw) : raw;
      const barHeight = Math.max(0, value / max * innerHeight);
      const x = stacked ? padding.left + gIdx * groupWidth + groupWidth * 0.15 : padding.left + gIdx * groupWidth + groupWidth * 0.15 + sIdx * barWidth;
      const base = padding.top + innerHeight - stackTop[gIdx];
      const y = base - barHeight;
      if (stacked) stackTop[gIdx] = stackTop[gIdx] + barHeight;
      const rect = svgEl$1("rect", {
        x: x.toFixed(1),
        y: y.toFixed(1),
        width: Math.max(barWidth - 2, 1).toFixed(1),
        height: barHeight.toFixed(1),
        fill: seriesColor(s, sIdx),
        rx: "2"
      });
      rect.append(svgEl$1("title", {}, [`${s.name} — ${label}: ${raw}`]));
      if (typeof onBarClick === "function") {
        makeActivatable(rect, `${s.name} — ${label}: ${raw}`, () => {
          helpers.invoke(onBarClick, label, raw, s.name);
        });
      }
      svg.append(rect);
    });
  });
  drawXAxisLabels(
    svg,
    categories,
    padding,
    innerHeight,
    labelPlan,
    (i) => padding.left + (i + 0.5) * groupWidth
  );
  drawAxisCaptions(svg, padding, innerWidth, innerHeight, height, xAxisLabel, yAxisLabel);
  return svg;
}
function renderHorizontalBars(layout) {
  const {
    width,
    height,
    categories,
    series,
    stacked,
    max,
    valueGutter,
    categoryGutter = 60,
    xAxisLabel,
    yAxisLabel,
    helpers,
    onBarClick
  } = layout;
  const padding = {
    left: categoryGutter + (xAxisLabel ? AXIS_CAPTION_PX : 0),
    right: Math.max(16, valueGutter / 2),
    top: 12,
    bottom: 28 + (yAxisLabel ? AXIS_CAPTION_PX : 0)
  };
  const innerWidth = width - padding.left - padding.right;
  const innerHeight = height - padding.top - padding.bottom;
  const svg = createSvg(width, height);
  drawValueTicks(svg, padding, innerWidth, innerHeight, max);
  const rowHeight = innerHeight / Math.max(categories.length, 1);
  const barHeight = stacked ? rowHeight * 0.7 : rowHeight * 0.7 / Math.max(series.length, 1);
  const stackLeft = categories.map(() => 0);
  const categoryChars = Math.max(4, Math.floor((categoryGutter - 10) / APPROX_CHAR_PX));
  categories.forEach((label, gIdx) => {
    const rowTop = padding.top + gIdx * rowHeight;
    const display = truncateLabel(label, categoryChars);
    const text = svgEl$1("text", {
      x: (padding.left - 8).toFixed(1),
      y: (rowTop + rowHeight / 2 + 3).toFixed(1),
      "text-anchor": "end",
      class: "rui-chart-label"
    }, [display]);
    if (display !== label) text.append(svgEl$1("title", {}, [label]));
    svg.append(text);
  });
  series.forEach((s, sIdx) => {
    categories.forEach((label, gIdx) => {
      const raw = s.values[gIdx];
      if (raw === void 0) return;
      const value = stacked ? Math.max(0, raw) : raw;
      const barWidth = Math.max(0, value / max * innerWidth);
      const rowTop = padding.top + gIdx * rowHeight;
      const y = stacked ? rowTop + rowHeight * 0.15 : rowTop + rowHeight * 0.15 + sIdx * barHeight;
      const x = padding.left + stackLeft[gIdx];
      if (stacked) stackLeft[gIdx] = stackLeft[gIdx] + barWidth;
      const rect = svgEl$1("rect", {
        x: x.toFixed(1),
        y: y.toFixed(1),
        width: barWidth.toFixed(1),
        height: Math.max(barHeight - 2, 1).toFixed(1),
        fill: seriesColor(s, sIdx),
        rx: "2"
      });
      rect.append(svgEl$1("title", {}, [`${s.name} — ${label}: ${raw}`]));
      if (typeof onBarClick === "function") {
        makeActivatable(rect, `${s.name} — ${label}: ${raw}`, () => {
          helpers.invoke(onBarClick, label, raw, s.name);
        });
      }
      svg.append(rect);
    });
  });
  drawAxisCaptions(svg, padding, innerWidth, innerHeight, height, yAxisLabel, xAxisLabel);
  return svg;
}
const LineChart = {
  name: "LineChart",
  description: 'Line chart. `labels` define the x-axis, each Series is a line. As a shortcut you can pass `data=[{x: "Jan", revenue: 12, signups: 4}, …]` and the labels + series will be derived automatically (one line per non-`x` numeric key; a row that omits a key leaves a gap in that line). Use `data` when the dataset is already row-shaped; use `series` when you have explicit Series objects.',
  props: [
    { name: "labels", type: "string[]", optional: true },
    { name: "series", type: "Series[]", optional: true },
    { name: "data", type: "{x: string, [key: string]: number}[]", optional: true, description: "Row-shaped data — labels and series are auto-derived" },
    { name: "title", type: "string", optional: true },
    { name: "filled", type: "boolean", optional: true, description: "Fill the area beneath each line (area-chart style)" },
    { name: "stacked", type: "boolean", optional: true, description: "Stack series when filled=true" },
    { name: "yMin", type: "number", optional: true, description: "Pin the bottom of the value axis (default 0 or the data minimum)" },
    { name: "yMax", type: "number", optional: true, description: "Pin the top of the value axis (default the data maximum)" },
    { name: "xAxisLabel", type: "string", optional: true, description: "Caption for the x axis" },
    { name: "yAxisLabel", type: "string", optional: true, description: 'Caption for the value axis (e.g. "requests/sec")' },
    { name: "showLegend", type: "boolean", optional: true, description: "Render the series legend (default true)" },
    { name: "height", type: "number", optional: true, description: "Plot height in px (default 240)" },
    { name: "loading", type: "boolean", optional: true, description: "Render a loading placeholder instead of the plot" },
    { name: "emptyText", type: "string", optional: true, description: 'Message shown when there is no data (default "No data")' },
    { name: "onPointClick", type: "callable", optional: true, description: "(label, value, seriesName) => void, fired when a data point is activated" },
    ...CHART_A11Y_PROPS
  ],
  render: (_node, props, helpers) => {
    let labels = asArray(props.labels).map((l) => asString(l));
    let series = readSeries(asArray(props.series));
    const rows = asArray(props.data);
    if (rows.length > 0 && (labels.length === 0 || series.length === 0)) {
      const derived = deriveRowSeries(rows);
      if (labels.length === 0) labels = derived.labels;
      if (series.length === 0) series = derived.series;
    }
    const title = asString(props.title);
    const a11y = readChartA11y(props, typeof props.onPointClick === "function");
    const filled = asBoolean(props.filled);
    const stacked = asBoolean(props.stacked);
    const showLegend = props.showLegend == null ? true : asBoolean(props.showLegend);
    const onPointClick = props.onPointClick;
    const root = el("div", { class: "rui-chart rui-line-chart" });
    if (title) root.append(el("div", { class: "rui-chart-title" }, [title]));
    if (asBoolean(props.loading)) return chartPlaceholder(root, LOADING_TEXT, true);
    if (!series.some((s) => s.values.some((v) => v !== null && v !== void 0))) {
      return chartPlaceholder(root, asString(props.emptyText) || EMPTY_TEXT, false);
    }
    const pointCount = Math.max(labels.length, ...series.map((s) => s.values.length), 1);
    const stackedValues = series.map(() => Array(pointCount).fill(0));
    if (filled && stacked) {
      for (let i = 0; i < pointCount; i += 1) {
        let acc = 0;
        series.forEach((s, sIdx) => {
          acc += s.values[i] ?? 0;
          stackedValues[sIdx][i] = acc;
        });
      }
    }
    const all = filled && stacked ? stackedValues.flat() : series.flatMap((s) => s.values).filter((v) => v !== null && v !== void 0);
    const yMin = props.yMin == null ? null : asNumber(props.yMin);
    const yMax = props.yMax == null ? null : asNumber(props.yMax);
    const min = yMin ?? (filled && stacked ? 0 : Math.min(0, ...all));
    let max = yMax ?? Math.max(1, ...all);
    if (max <= min) max = min + 1;
    const xAxisLabel = asString(props.xAxisLabel);
    const yAxisLabel = asString(props.yAxisLabel);
    const width = 640;
    const height = readChartHeight(props.height, 240);
    const padding = {
      left: tickGutter(min, max) + (yAxisLabel ? AXIS_CAPTION_PX : 0),
      right: 12,
      top: 12,
      bottom: 0
    };
    const innerWidth = width - padding.left - padding.right;
    const labelPlan = planLabels(labels, innerWidth, pointCount);
    padding.bottom = labelPlan.bottomPadding + (xAxisLabel ? AXIS_CAPTION_PX : 0);
    const innerHeight = height - padding.top - padding.bottom;
    const svg = createSvg(width, height);
    nameChart(svg, a11y, chartLabel(
      filled ? "Area chart" : "Line chart",
      title,
      `${series.length} series across ${pointCount} points.`
    ));
    if (height !== 240) svg.setAttribute("style", `max-height:${height}px`);
    drawAxes(svg, padding, innerWidth, innerHeight, max, min);
    const stepX = innerWidth / Math.max(pointCount - 1, 1);
    const xForPoint = (i) => padding.left + i * stepX;
    const yFor = (value) => padding.top + innerHeight - (value - min) / (max - min) * innerHeight;
    series.forEach((s, sIdx) => {
      const stroke = seriesColor(s, sIdx);
      const values = filled && stacked ? stackedValues[sIdx] : s.values;
      const baseline = filled && stacked && sIdx > 0 ? stackedValues[sIdx - 1] : null;
      const points = values.map((value, i) => value === null || value === void 0 ? null : [xForPoint(i), yFor(value), i]);
      const runs = splitRuns(points);
      if (filled) {
        for (const run of runs) {
          if (run.length === 0) continue;
          let area = run.map(([x, y], i) => `${i === 0 ? "M" : "L"}${x.toFixed(1)},${y.toFixed(1)}`).join(" ");
          if (baseline) {
            area += " " + run.slice().reverse().map(([x, , i]) => `L${x.toFixed(1)},${yFor(baseline[i] ?? 0).toFixed(1)}`).join(" ") + " Z";
          } else {
            const floor = (padding.top + innerHeight).toFixed(1);
            const first = run[0];
            const last = run[run.length - 1];
            area += ` L${last[0].toFixed(1)},${floor} L${first[0].toFixed(1)},${floor} Z`;
          }
          svg.append(svgEl$1("path", { d: area, fill: stroke, "fill-opacity": "0.2", stroke: "none" }));
        }
      }
      for (const run of runs) {
        if (run.length === 0) continue;
        const d = run.map(([x, y], i) => `${i === 0 ? "M" : "L"}${x.toFixed(1)},${y.toFixed(1)}`).join(" ");
        svg.append(svgEl$1("path", {
          d,
          fill: "none",
          stroke,
          "stroke-width": "2",
          "stroke-linejoin": "round",
          "stroke-linecap": "round"
        }));
      }
      const clickable = typeof onPointClick === "function";
      if (s.values.length > 30 && !clickable) return;
      for (const point of points) {
        if (!point) continue;
        const [x, y, i] = point;
        const raw = s.values[i];
        const dot = svgEl$1("circle", {
          cx: x.toFixed(1),
          cy: y.toFixed(1),
          r: clickable ? "4" : "3",
          fill: stroke
        });
        const description = `${s.name} — ${labels[i] ?? `#${i + 1}`}: ${raw}`;
        dot.append(svgEl$1("title", {}, [description]));
        if (clickable) {
          makeActivatable(dot, description, () => {
            helpers.invoke(onPointClick, labels[i] ?? `#${i + 1}`, raw, s.name);
          });
        }
        svg.append(dot);
      }
    });
    drawXAxisLabels(svg, labels, padding, innerHeight, labelPlan, xForPoint);
    drawAxisCaptions(svg, padding, innerWidth, innerHeight, height, xAxisLabel, yAxisLabel);
    root.append(svg);
    if (!a11y.decorative) {
      root.append(chartTable(
        title || "Line chart data",
        Array.from({ length: pointCount }, (_, i) => labels[i] ?? `#${i + 1}`),
        series.map((s) => ({
          label: s.name,
          cells: Array.from({ length: pointCount }, (_, i) => cellText(s.values[i]))
        }))
      ));
    }
    if (showLegend && series.length > 0) root.append(legend(series));
    return root;
  }
};
function deriveRowSeries(rows) {
  const labels = [];
  const byKey = /* @__PURE__ */ new Map();
  rows.forEach((raw) => {
    const row = raw;
    if (!row || typeof row !== "object") return;
    const rowIndex = labels.length;
    labels.push(asString(row.x ?? row.label ?? ""));
    for (const [k, v] of Object.entries(row)) {
      if (k === "x" || k === "label") continue;
      const num2 = asNumber(v, NaN);
      if (!Number.isFinite(num2)) continue;
      let values = byKey.get(k);
      if (!values) {
        values = [];
        byKey.set(k, values);
      }
      while (values.length < rowIndex) values.push(null);
      values[rowIndex] = num2;
    }
  });
  const series = [...byKey.entries()].map(([name, values]) => {
    while (values.length < labels.length) values.push(null);
    return { name, values };
  });
  return { labels, series };
}
function splitRuns(points) {
  const runs = [];
  let current = [];
  for (const point of points) {
    if (point === null) {
      if (current.length > 0) runs.push(current);
      current = [];
      continue;
    }
    current.push(point);
  }
  if (current.length > 0) runs.push(current);
  return runs;
}
const PieChart = {
  name: "PieChart",
  description: 'Pie / Donut chart. Each segment maps to a label/value pair. Pass `donut: true` (or an `innerRadius` fraction) for a donut. Numeric labels are rendered on every segment by default — set `showValues: false` to hide them, or `valueFormat: "percent"` to show the share instead of the raw value.',
  props: [
    { name: "labels", type: "string[]" },
    { name: "values", type: "number[]" },
    { name: "title", type: "string", optional: true },
    { name: "showValues", type: "boolean", optional: true, description: "Render numeric labels on each slice (default true)" },
    { name: "valueFormat", type: "string", optional: true, enum: ["value", "percent", "both"], description: 'How to format the on-segment label (default "value")' },
    { name: "donut", type: "boolean", optional: true, description: "Cut a hole in the middle (default innerRadius 0.6)" },
    { name: "innerRadius", type: "number", optional: true, description: "Hole size as a fraction of the radius, 0–0.9 (implies donut)" },
    { name: "size", type: "number", optional: true, description: "Diameter in px (default 240)" },
    { name: "showLegend", type: "boolean", optional: true, description: "Render the label legend (default true)" },
    { name: "legendPosition", type: "string", optional: true, enum: ["bottom", "right"], description: 'Where the legend sits relative to the pie (default "bottom")' },
    { name: "loading", type: "boolean", optional: true, description: "Render a loading placeholder instead of the pie" },
    { name: "emptyText", type: "string", optional: true, description: 'Message shown when there is no positive value (default "No data")' },
    { name: "onSliceClick", type: "callable", optional: true, description: "(label, value, share) => void, fired when a slice is activated" },
    ...CHART_A11Y_PROPS
  ],
  render: (_node, props, helpers) => {
    const labels = asArray(props.labels).map((l) => asString(l));
    const values = asArray(props.values).map((v) => asNumber(v));
    const title = asString(props.title);
    const a11y = readChartA11y(props, typeof props.onSliceClick === "function");
    const showValues = props.showValues == null ? true : asBoolean(props.showValues);
    const showLegend = props.showLegend == null ? true : asBoolean(props.showLegend);
    const legendRight = asString(props.legendPosition).toLowerCase() === "right";
    const fmtToken = asString(props.valueFormat, "value").toLowerCase();
    const valueFormat = fmtToken === "percent" ? "percent" : fmtToken === "both" ? "both" : "value";
    const onSliceClick = props.onSliceClick;
    const root = el("div", { class: "rui-chart rui-pie-chart" });
    const titleEl = title ? el("div", { class: "rui-chart-title" }, [title]) : null;
    if (titleEl) root.append(titleEl);
    if (asBoolean(props.loading)) return chartPlaceholder(root, LOADING_TEXT, true);
    const slices = labels.map((name, i) => ({ name, value: values[i] ?? 0 })).filter((s) => s.value > 0);
    if (slices.length === 0) {
      return chartPlaceholder(root, asString(props.emptyText) || EMPTY_TEXT, false);
    }
    const size2 = Math.max(120, Math.min(640, readChartHeight(props.size, 240)));
    const hole = readInnerRadius(props.innerRadius, asBoolean(props.donut));
    const total = slices.reduce((acc, s) => acc + s.value, 0);
    const svg = createSvg(size2, size2);
    nameChart(svg, a11y, chartLabel(
      hole > 0 ? "Donut chart" : "Pie chart",
      title,
      `${slices.length} segments totalling ${formatNumeric(total)}.`
    ));
    svg.setAttribute("style", `max-width:${size2}px;max-height:${size2}px;` + (legendRight ? `flex:0 1 ${size2}px;min-width:0` : "margin-inline:auto"));
    const cx = size2 / 2, cy = size2 / 2, r = size2 / 2 - 30;
    const innerR = r * hole;
    const MIN_LABEL_FRACTION = 0.05;
    let angle = -Math.PI / 2;
    slices.forEach((slice, i) => {
      const fraction = slice.value / total;
      const sweep = fraction * Math.PI * 2;
      const next = angle + sweep;
      const large = sweep > Math.PI ? 1 : 0;
      const segment = svgEl$1("path", {
        d: arcPath(cx, cy, r, innerR, angle, next, large),
        fill: colorAt(i),
        stroke: "var(--rui-color-surface, #fff)",
        "stroke-width": "2"
      });
      const description = `${slice.name}: ${slice.value} (${(fraction * 100).toFixed(1)}%)`;
      segment.append(svgEl$1("title", {}, [description]));
      if (typeof onSliceClick === "function") {
        makeActivatable(segment, description, () => {
          helpers.invoke(onSliceClick, slice.name, slice.value, fraction);
        });
      }
      svg.append(segment);
      if (showValues && fraction >= MIN_LABEL_FRACTION) {
        const mid = angle + sweep / 2;
        const labelR = innerR > 0 ? (innerR + r) / 2 : r * 0.62;
        const lx = cx + labelR * Math.cos(mid);
        const ly = cy + labelR * Math.sin(mid);
        svg.append(svgEl$1("text", {
          x: lx.toFixed(1),
          y: ly.toFixed(1),
          class: "rui-pie-chart-value",
          "text-anchor": "middle",
          "dominant-baseline": "central",
          "paint-order": "stroke",
          stroke: "rgba(15, 23, 42, 0.55)",
          "stroke-width": "3",
          "stroke-linejoin": "round",
          fill: "#fff"
        }, [formatSliceLabel(slice.value, fraction, valueFormat)]));
      }
      angle = next;
    });
    if (legendRight) {
      root.setAttribute("style", "flex-direction:row;align-items:center;flex-wrap:wrap");
      titleEl?.setAttribute("style", "flex:1 0 100%");
    }
    root.append(svg);
    if (!a11y.decorative) {
      root.append(chartTable(
        title || "Pie chart data",
        ["Value", "Share"],
        labels.map((name, i) => {
          const value = values[i] ?? 0;
          return {
            label: name,
            cells: [cellText(values[i]), value > 0 ? `${(value / total * 100).toFixed(1)}%` : "0%"]
          };
        })
      ));
    }
    if (showLegend) {
      const list = legend(slices.map((s) => ({ name: s.name, values: [s.value] })));
      if (legendRight) list.setAttribute("style", "flex-direction:column;flex:1 1 120px");
      root.append(list);
    }
    return root;
  }
};
function arcPath(cx, cy, r, innerR, from, to, large) {
  if (to - from >= Math.PI * 2 - 1e-6) return fullCirclePath(cx, cy, r, innerR);
  const x1 = cx + r * Math.cos(from), y1 = cy + r * Math.sin(from);
  const x2 = cx + r * Math.cos(to), y2 = cy + r * Math.sin(to);
  if (innerR <= 0) {
    return `M${cx.toFixed(1)},${cy.toFixed(1)} L${x1.toFixed(1)},${y1.toFixed(1)} A${r},${r} 0 ${large} 1 ${x2.toFixed(1)},${y2.toFixed(1)} Z`;
  }
  const ix1 = cx + innerR * Math.cos(to), iy1 = cy + innerR * Math.sin(to);
  const ix2 = cx + innerR * Math.cos(from), iy2 = cy + innerR * Math.sin(from);
  return `M${x1.toFixed(1)},${y1.toFixed(1)} A${r},${r} 0 ${large} 1 ${x2.toFixed(1)},${y2.toFixed(1)} L${ix1.toFixed(1)},${iy1.toFixed(1)} A${innerR.toFixed(1)},${innerR.toFixed(1)} 0 ${large} 0 ${ix2.toFixed(1)},${iy2.toFixed(1)} Z`;
}
function fullCirclePath(cx, cy, r, innerR) {
  const ring = (radius, sweep) => {
    const left = (cx - radius).toFixed(1), right = (cx + radius).toFixed(1);
    const y = cy.toFixed(1), rad = radius.toFixed(1);
    return `M${left},${y} A${rad},${rad} 0 1 ${sweep} ${right},${y} A${rad},${rad} 0 1 ${sweep} ${left},${y} Z`;
  };
  return innerR > 0 ? `${ring(r, 1)} ${ring(innerR, 0)}` : ring(r, 1);
}
function readInnerRadius(raw, donut) {
  if (raw == null) return donut ? 0.6 : 0;
  const value = asNumber(raw, 0);
  const fraction = value > 1 ? value / 100 : value;
  if (!Number.isFinite(fraction) || fraction <= 0) return donut ? 0.6 : 0;
  return Math.min(0.9, fraction);
}
function formatSliceLabel(value, fraction, format) {
  const pct = `${Math.round(fraction * 100)}%`;
  const num2 = formatNumeric(value);
  if (format === "percent") return pct;
  if (format === "both") return `${num2} (${pct})`;
  return num2;
}
function formatNumeric(value) {
  if (!Number.isFinite(value)) return String(value);
  const abs = Math.abs(value);
  if (abs >= 1e6) return `${(value / 1e6).toFixed(1).replace(/\.0$/, "")}M`;
  if (abs >= 1e3) return `${(value / 1e3).toFixed(1).replace(/\.0$/, "")}k`;
  if (Number.isInteger(value)) return String(value);
  return value.toFixed(1);
}
function cellText(value) {
  return value === null || value === void 0 ? "—" : String(value);
}
function readChartHeight(raw, fallback) {
  if (raw == null) return fallback;
  const parsed = typeof raw === "string" ? Number.parseFloat(raw) : raw;
  const value = asNumber(parsed, fallback);
  if (!Number.isFinite(value) || value <= 0) return fallback;
  return Math.max(80, Math.min(1200, Math.round(value)));
}
function chartPlaceholder(root, message, busy) {
  if (busy) root.setAttribute("aria-busy", "true");
  root.append(el("div", { class: "rui-chart-empty" }, [message]));
  return root;
}
function chartLabel(kind, title, detail = "") {
  const head = title ? `${kind}: ${title}` : kind;
  return detail ? `${head}. ${detail}` : head;
}
function readChartA11y(props, interactive = false) {
  return {
    name: asString(props.ariaLabel).trim(),
    decorative: asBoolean(props.decorative) && !interactive
  };
}
function nameChart(svg, a11y, generated) {
  if (a11y.decorative) {
    svg.removeAttribute("role");
    svg.setAttribute("aria-hidden", "true");
    return;
  }
  svg.setAttribute("aria-label", a11y.name || generated);
}
function chartTable(caption, columns, rows, rowHeader = "") {
  const wrap = el("div", { class: "rui-visually-hidden" });
  const table = el("table");
  table.append(el("caption", {}, [caption]));
  const head = el("tr");
  head.append(el("th", { scope: "col" }, [rowHeader]));
  for (const column of columns) head.append(el("th", { scope: "col" }, [column]));
  const thead = el("thead");
  thead.append(head);
  table.append(thead);
  const tbody = el("tbody");
  for (const row of rows) {
    const tr = el("tr");
    tr.append(el("th", { scope: "row" }, [row.label]));
    for (const cell of row.cells) tr.append(el("td", {}, [cell]));
    tbody.append(tr);
  }
  table.append(tbody);
  wrap.append(table);
  return wrap;
}
function makeActivatable(node, label, fire) {
  node.setAttribute("role", "button");
  node.setAttribute("tabindex", "0");
  node.setAttribute("aria-label", label);
  if (node.namespaceURI === "http://www.w3.org/2000/svg") node.setAttribute("cursor", "pointer");
  const handlers = node;
  handlers.onclick = () => fire();
  handlers.onkeydown = (event) => {
    if (event.key !== "Enter" && event.key !== " ") return;
    event.preventDefault();
    fire();
  };
}
const APPROX_CHAR_PX = 7;
const TICK_CHAR_PX = 6;
const AXIS_TICKS = 4;
const AXIS_CAPTION_PX = 18;
const MIN_ROTATED_SLOT_PX = 12;
function longestLabelPx(labels, maxChars = 40) {
  const longest = labels.reduce((max, l) => Math.max(max, Math.min(l.length, maxChars)), 0);
  return longest * APPROX_CHAR_PX;
}
function tickGutter(min, max) {
  let chars = 0;
  for (let i = 0; i <= AXIS_TICKS; i += 1) {
    chars = Math.max(chars, formatNumeric(min + (max - min) * (i / AXIS_TICKS)).length);
  }
  return Math.max(32, Math.min(96, Math.ceil(chars * TICK_CHAR_PX) + 10));
}
function planLabels(labels, innerWidth, slotCount = labels.length) {
  if (labels.length === 0) {
    return { step: 1, rotated: false, maxChars: 32, bottomPadding: 32 };
  }
  const slot = innerWidth / Math.max(slotCount, 1);
  const longest = labels.reduce((max, l) => Math.max(max, l.length), 0);
  if (longest * APPROX_CHAR_PX + 4 <= slot) {
    return { step: 1, rotated: false, maxChars: longest, bottomPadding: 32 };
  }
  const step = slot < MIN_ROTATED_SLOT_PX ? Math.max(1, Math.ceil(MIN_ROTATED_SLOT_PX / slot)) : 1;
  const maxChars = Math.min(longest, 14);
  return { step, rotated: true, maxChars, bottomPadding: 60 };
}
function truncateLabel(label, maxChars) {
  if (label.length <= maxChars) return label;
  return label.slice(0, Math.max(maxChars - 1, 1)) + "…";
}
function truncateToWidth(label, availablePx) {
  return truncateLabel(label, Math.max(2, Math.floor(availablePx / APPROX_CHAR_PX)));
}
function drawXAxisLabels(svg, labels, padding, innerHeight, plan, xFor) {
  const baseY = padding.top + innerHeight + (plan.rotated ? 14 : 18);
  labels.forEach((label, i) => {
    if (i % plan.step !== 0) return;
    const x = xFor(i);
    const display = truncateLabel(label, plan.maxChars);
    const attrs = {
      x: String(x),
      y: String(baseY),
      class: "rui-chart-label",
      "text-anchor": plan.rotated ? "end" : "middle"
    };
    if (plan.rotated) {
      attrs.transform = `rotate(-45, ${x}, ${baseY})`;
    }
    const text = svgEl$1("text", attrs, [display]);
    if (display !== label) {
      text.append(svgEl$1("title", {}, [label]));
    }
    svg.append(text);
  });
}
function drawAxisCaptions(svg, padding, innerWidth, innerHeight, height, xAxisLabel, yAxisLabel) {
  if (xAxisLabel) {
    svg.append(svgEl$1("text", {
      x: (padding.left + innerWidth / 2).toFixed(1),
      y: (height - 4).toFixed(1),
      "text-anchor": "middle",
      class: "rui-chart-label",
      "font-weight": "500"
    }, [xAxisLabel]));
  }
  if (yAxisLabel) {
    const x = 12;
    const y = padding.top + innerHeight / 2;
    svg.append(svgEl$1("text", {
      x: String(x),
      y: String(y),
      "text-anchor": "middle",
      transform: `rotate(-90, ${x}, ${y})`,
      class: "rui-chart-label",
      "font-weight": "500"
    }, [yAxisLabel]));
  }
}
function createSvg(width, height) {
  const svg = document.createElementNS("http://www.w3.org/2000/svg", "svg");
  svg.setAttribute("viewBox", `0 0 ${width} ${height}`);
  svg.setAttribute("class", "rui-chart-svg");
  svg.setAttribute("role", "img");
  return svg;
}
function svgEl$1(tag, attrs, children) {
  const node = document.createElementNS("http://www.w3.org/2000/svg", tag);
  for (const [key, value] of Object.entries(attrs)) node.setAttribute(key, value);
  if (children) {
    for (const child of children) {
      node.append(typeof child === "string" ? document.createTextNode(child) : child);
    }
  }
  return node;
}
function drawAxes(svg, padding, innerWidth, innerHeight, max, min = 0) {
  for (let i = 0; i <= AXIS_TICKS; i += 1) {
    const ratio = i / AXIS_TICKS;
    const y = padding.top + innerHeight - ratio * innerHeight;
    svg.append(svgEl$1("line", {
      x1: String(padding.left),
      x2: String(padding.left + innerWidth),
      y1: String(y),
      y2: String(y),
      stroke: "var(--rui-color-border-subtle, rgba(0,0,0,0.08))"
    }));
    svg.append(svgEl$1("text", {
      x: String(padding.left - 6),
      y: String(y + 3),
      "text-anchor": "end",
      class: "rui-chart-tick"
    }, [formatNumeric(min + (max - min) * ratio)]));
  }
}
function drawValueTicks(svg, padding, innerWidth, innerHeight, max, min = 0) {
  const baseline = padding.top + innerHeight;
  for (let i = 0; i <= AXIS_TICKS; i += 1) {
    const ratio = i / AXIS_TICKS;
    const x = padding.left + ratio * innerWidth;
    svg.append(svgEl$1("line", {
      x1: String(x),
      x2: String(x),
      y1: String(padding.top),
      y2: String(baseline),
      stroke: "var(--rui-color-border-subtle, rgba(0,0,0,0.08))"
    }));
    svg.append(svgEl$1("text", {
      x: String(x),
      y: String(baseline + 16),
      "text-anchor": "middle",
      class: "rui-chart-tick"
    }, [formatNumeric(min + (max - min) * ratio)]));
  }
}
function legend(series) {
  const root = el("div", { class: "rui-chart-legend" });
  series.forEach((s, i) => {
    const item = el("span", { class: "rui-chart-legend-item" });
    item.append(el("span", {
      class: "rui-chart-legend-swatch",
      style: `background:${seriesColor(s, i)}`
    }));
    item.append(el("span", {}, [s.name]));
    root.append(item);
  });
  return root;
}
const SectionBlock = {
  name: "SectionBlock",
  description: 'Titled chat block with a description and child content. `level` sets the heading level (2-6, default 3) so nested sections keep a valid outline; `actions` renders trailing controls in the header row (a "View all" link, an overflow menu).',
  props: [
    { name: "title", type: "string" },
    { name: "children", aliases: ["child"], type: "Node[]" },
    { name: "description", type: "string", optional: true },
    { name: "actions", type: "Node[]", optional: true, description: "Header-right controls, e.g. a Link or DropdownMenu" },
    { name: "level", type: "number", optional: true, description: "Heading level 2-6 (default 3); the visual size is unchanged" },
    { name: "icon", type: "string", optional: true, description: "Font Awesome icon shown before the title" }
  ],
  render: (_node, props, helpers) => {
    const root = el("section", { class: "rui-section-block" });
    const level = Math.max(2, Math.min(6, Math.round(asNumber(props.level, 3))));
    const heading = el(`h${level}`, { class: "rui-section-block-title" });
    const iconNode = renderIcon(props.icon, { className: "rui-section-block-icon" });
    if (iconNode) {
      iconNode.style.marginInlineEnd = "0.4em";
      heading.append(iconNode);
    }
    heading.append(document.createTextNode(asString(props.title)));
    const actions = asArray(props.actions).filter((a) => a !== null && a !== void 0);
    if (actions.length > 0) {
      const header = el("div", {
        class: "rui-section-block-header",
        style: "display:flex;align-items:center;justify-content:space-between;gap:var(--rui-spacing-s)"
      });
      header.append(heading);
      const trailing = el("div", { class: "rui-section-block-actions", style: "display:flex;align-items:center;gap:var(--rui-spacing-xs)" });
      for (const action of actions) trailing.append(helpers.renderNode(action));
      header.append(trailing);
      root.append(header);
    } else {
      root.append(heading);
    }
    const desc = asString(props.description);
    if (desc) root.append(el("p", { class: "rui-section-block-description" }, [desc]));
    for (const child of asArray(props.children)) root.append(helpers.renderNode(child));
    return root;
  }
};
const LIST_MARKERS = ["bullet", "none", "check"];
const ListBlock = {
  name: "ListBlock",
  description: "Chat-styled list with bullets, useful for steps or summaries. `items` takes plain strings or component nodes (Text, ActionLink, Badge, …), which render inline. `marker` switches to an unmarked or checklist list; `start` continues an `ordered` list that was split across blocks.",
  props: [
    { name: "items", type: "any[]", description: "Strings, or component nodes rendered inside the list item" },
    { name: "ordered", type: "boolean", optional: true },
    { name: "marker", type: "string", optional: true, enum: LIST_MARKERS, description: "Bullet style (default `bullet`); `none` drops the marker, `check` shows a checkmark" },
    { name: "start", type: "number", optional: true, description: "First number of an `ordered` list (default 1)" }
  ],
  render: (_node, props, helpers) => {
    const ordered = asBoolean(props.ordered);
    const markerRaw = asString(props.marker, "bullet");
    const marker = LIST_MARKERS.includes(markerRaw) ? markerRaw : "bullet";
    const start = Math.round(asNumber(props.start, 1));
    const tag = ordered ? "ol" : "ul";
    const root = el(tag, {
      class: "rui-list-block",
      "data-marker": marker,
      // `start` is only meaningful on <ol>; emitting it otherwise is invalid.
      start: ordered && start !== 1 ? String(start) : null,
      // Suppressing the native marker is the whole point of these variants, so
      // it is inline rather than dependent on a theme rule. `check` keeps the
      // indent for the glyph the stylesheet draws in `::before`.
      style: marker === "none" ? "list-style:none;padding-left:0" : marker === "check" ? "list-style:none" : null
    });
    for (const item of asArray(props.items)) {
      const li = el("li", {});
      if (isComponentNode(item)) li.append(helpers.renderNode(item));
      else li.append(document.createTextNode(asString(item)));
      root.append(li);
    }
    return root;
  }
};
let followUpIdSeq = 0;
const FollowUpBlock = {
  name: "FollowUpBlock",
  description: 'Suggested follow-up prompts shown as buttons. Each item dispatches its label as an assistant message (equivalent to `emit "assistant-message" { message }`) unless `onSelect` is supplied, which receives `(message, label)` instead. `disabled` makes the row inert while the assistant is responding; `layout: "stack"` puts one suggestion per line.',
  props: [
    { name: "items", type: "FollowUpItem[]", description: "Array of FollowUpItem(label, message?), {label, message, disabled?} objects, or plain strings" },
    { name: "title", type: "string", optional: true },
    { name: "onSelect", type: "callable", optional: true, description: "(message, label) => … — replaces the default assistant-message dispatch" },
    { name: "disabled", type: "boolean", optional: true, description: "Render every chip inert (e.g. once one has been picked)" },
    { name: "layout", type: "string", optional: true, aliases: ["columns"], enum: ["wrap", "stack"], description: "`wrap` (default) flows chips in a row; `stack` gives each a full line — better for long suggestion sentences" }
  ],
  render: (_node, props, helpers) => {
    const root = el("div", { class: "rui-follow-up" });
    const title = asString(props.title, "You can also ask");
    const layout = asString(props.layout, "wrap") === "stack" ? "stack" : "wrap";
    const disabled = asBoolean(props.disabled);
    const titleIdSlot = helpers.useInstanceState("rui-follow-up-title-id", "");
    if (!titleIdSlot.get()) titleIdSlot.set(`rui-follow-up-title-${followUpIdSeq += 1}`);
    const titleId = titleIdSlot.get();
    if (title) root.append(el("div", { class: "rui-follow-up-title", id: titleId }, [title]));
    const list = el("div", {
      class: "rui-follow-up-list",
      // Grouped so a screen-reader user knows these are suggestions that send a
      // message, not buttons that mutate application state.
      role: "group",
      "aria-labelledby": title ? titleId : null,
      "aria-label": title ? null : "Suggested follow-ups",
      "data-layout": layout,
      style: layout === "stack" ? "flex-direction:column;align-items:stretch" : null
    });
    for (const item of asArray(props.items)) {
      const button = buildFollowUpButton(item, helpers, disabled, props.onSelect);
      if (button) list.append(button);
    }
    root.append(list);
    return root;
  }
};
const buildFollowUpButton = (item, helpers, blockDisabled, onSelect) => {
  const { label, message, disabled } = extractFollowUp(item);
  if (!label) return null;
  const inert = blockDisabled || disabled;
  const button = el("button", { class: "rui-follow-up-button", type: "button", disabled: inert }, [label]);
  if (inert) return button;
  button.onclick = (event) => {
    const clicked = event.currentTarget ?? event.target;
    if (clicked) clicked.disabled = true;
    if (onSelect === null || onSelect === void 0) helpers.sendToAssistant(message);
    else helpers.invoke(onSelect, message, label);
  };
  return button;
};
const extractFollowUp = (item) => {
  if (typeof item === "string") return { label: item, message: item, disabled: false };
  if (item && typeof item === "object") {
    const node = item;
    if (node.__kind === "Component" && node.name === "FollowUpItem" && Array.isArray(node.args)) {
      const label2 = asString(node.args[0]);
      return { label: label2, message: asString(node.args[1], label2), disabled: false };
    }
    const label = asString(node.label);
    return { label, message: asString(node.message, label), disabled: asBoolean(node.disabled) };
  }
  const fallback = asString(item);
  return { label: fallback, message: fallback, disabled: false };
};
const FollowUpItem = {
  name: "FollowUpItem",
  description: "Single follow-up item.",
  props: [
    { name: "label", type: "string" },
    { name: "message", type: "string", optional: true, description: "Defaults to label" }
  ],
  render: (_node, props, helpers) => {
    const label = asString(props.label);
    const message = asString(props.message, label);
    const button = el("button", { class: "rui-follow-up-button", type: "button" }, [label]);
    button.onclick = () => {
      helpers.sendToAssistant(message);
    };
    return button;
  }
};
const ActionLink = {
  name: "ActionLink",
  description: 'Inline link that runs an action when clicked instead of navigating. `disabled` makes it inert while the work is in flight; `icon` adds a glyph before the label (or after it with `iconPosition: "end"`).',
  props: [
    { name: "label", type: "string" },
    { name: "onClick", type: "callable", aliases: ["action", "onclick"] },
    { name: "disabled", type: "boolean", optional: true, description: "Make the action inert, e.g. while a Retry is already running" },
    { name: "icon", type: "string", optional: true, description: "Font Awesome icon shown with the label" },
    { name: "iconPosition", type: "string", optional: true, enum: ["start", "end"], description: "Which side the icon sits on (default `start`)" },
    { name: "ariaLabel", type: "string", optional: true, description: 'Accessible name, when the visible label alone does not identify the target — e.g. one "Rebuild" link per table row, where every link would otherwise be announced identically' }
  ],
  render: (_node, props, helpers) => {
    const disabled = asBoolean(props.disabled);
    const ariaLabel = asString(props.ariaLabel);
    const iconAtEnd = asString(props.iconPosition, "start") === "end";
    const button = el("button", {
      type: "button",
      class: "rui-action-link" + (props.icon ? " has-icon" : ""),
      disabled,
      "aria-label": ariaLabel || null,
      // `data-icon-position` rather than a second class, matching `Button`.
      "data-icon-position": props.icon ? iconAtEnd ? "end" : "start" : null,
      // The four longhands, NOT the `font` shorthand. `font: inherit` also resets
      // `line-height` — and being inline it beat every stylesheet, so a theme that
      // set a line-height on this control (vision does: 20px) had a rule that
      // could never fire. These four inherit what the shorthand did without
      // touching the fifth property nobody meant to set.
      style: "background:none;border:0;padding:0;text-align:inherit;font-family:inherit;font-size:inherit;font-weight:inherit;font-style:inherit"
    });
    const iconNode = renderIcon(props.icon, { className: "rui-action-link-icon" });
    if (iconNode && !iconAtEnd) button.append(iconNode);
    button.append(document.createTextNode(asString(props.label)));
    if (iconNode && iconAtEnd) button.append(iconNode);
    if (!disabled) {
      button.onclick = () => {
        helpers.invoke(props.onClick);
      };
    }
    return button;
  }
};
const FOCUSABLE_TRIGGER_SELECTOR = 'a[href], button, input, select, textarea, [tabindex], [contenteditable="true"]';
function triggerIsFocusable(node) {
  if (!(node instanceof HTMLElement)) return false;
  return node.matches(FOCUSABLE_TRIGGER_SELECTOR) || node.querySelector(FOCUSABLE_TRIGGER_SELECTOR) !== null;
}
function describeTrigger(node, id) {
  if (!(node instanceof HTMLElement)) return;
  const existing = node.getAttribute("aria-describedby");
  if (!existing) {
    node.setAttribute("aria-describedby", id);
    return;
  }
  if (existing.split(/\s+/).includes(id)) return;
  node.setAttribute("aria-describedby", `${existing} ${id}`);
}
let PANEL_ID_SEQ = 0;
function panelId(slot, prefix) {
  const existing = slot.get();
  if (existing) return existing;
  PANEL_ID_SEQ += 1;
  const next = `${prefix}-${PANEL_ID_SEQ}`;
  slot.set(next);
  return next;
}
const AVATAR_SIZES = ["xs", "sm", "md", "lg", "xl"];
const AVATAR_FALLBACKS = ["initials", "dicebear", "gradient"];
function gradientAvatarStyle(seed) {
  let h = 0;
  for (let i = 0; i < seed.length; i += 1) h = h * 31 + seed.charCodeAt(i) >>> 0;
  const hue = h % 360;
  const hue2 = (hue + 40) % 360;
  return `background:linear-gradient(135deg, hsl(${hue} 65% 55%), hsl(${hue2} 65% 45%));color:#fff;`;
}
const Avatar = {
  name: "Avatar",
  description: 'User avatar. Shows the image at `src`. When `src` is missing, falls back to a deterministic DiceBear illustration seeded by `name` (pass `fallback="initials"` to render two-letter initials instead, or `"gradient"` for an offline seeded swatch). If the image errors at runtime the avatar gracefully degrades to initials. `status` is announced as part of the accessible name ("Ada Lovelace, busy").',
  props: [
    { name: "name", type: "string", description: "Accessible name + initials fallback" },
    { name: "src", type: "string", optional: true, description: "Image URL" },
    { name: "size", type: "string", optional: true, enum: AVATAR_SIZES },
    { name: "status", type: "string", optional: true, enum: ["online", "offline", "busy", "away"] },
    {
      name: "fallback",
      type: "string",
      optional: true,
      enum: AVATAR_FALLBACKS,
      description: "How to render when `src` is missing (default: dicebear illustration; pass `initials` for the two-letter pill)"
    }
  ],
  render: (_node, props, helpers) => {
    const size2 = asString(props.size, "md");
    const name = asString(props.name);
    const status = asString(props.status);
    const label = status ? `${name}, ${status}` : name;
    const root = el("span", {
      class: "rui-avatar",
      "data-size": size2,
      role: name ? "img" : "presentation",
      "aria-label": name ? label : null
    });
    const fallback = asString(props.fallback, "dicebear");
    const explicitSrc = sanitiseImageSrc(props.src);
    const generated = !explicitSrc && fallback === "dicebear" && name ? sanitiseImageSrc(dicebearUrlFor(name)) : "";
    const src = explicitSrc || generated;
    const fallbackNode = () => fallback === "gradient" && name ? el("span", {
      class: "rui-avatar-fallback rui-avatar-gradient",
      style: gradientAvatarStyle(name)
    }, [initialsFor(name)]) : el("span", { class: "rui-avatar-fallback" }, [initialsFor(name)]);
    const errorSlot = src ? helpers.useInstanceState(`img-error:${src}`, false) : null;
    if (src && !errorSlot?.get()) {
      const img = el("img", { src, alt: "", loading: "lazy" });
      img.onerror = (event) => {
        errorSlot?.set(true);
        const ev = event;
        const live = ev.currentTarget ?? ev.target;
        live.replaceWith(fallbackNode());
      };
      root.append(img);
    } else {
      root.append(fallbackNode());
    }
    if (status) {
      root.append(el("span", {
        class: "rui-avatar-status",
        "data-status": status,
        // Sighted users get the presence as a tooltip; AT gets it from the
        // root's aria-label (this node is inside a pruned `role="img"`).
        title: status
      }));
    }
    return root;
  }
};
const AvatarGroup = {
  name: "AvatarGroup",
  description: 'Stack of overlapping avatars with a `+N` chip when the list overflows. Pass either Avatar(...) nodes or plain {name, src, status?, fallback?} objects. Set `total` when the list is only a page of a larger set (5 avatars out of 200 members renders `+195`), and `fallback: "initials"` to keep the whole pile offline (the default DiceBear illustration is a network request per member).',
  props: [
    { name: "items", type: "Avatar[]", description: "Avatar(...) nodes or {name, src, status?, fallback?} objects" },
    { name: "max", type: "number", optional: true, description: "Maximum avatars to show (default 4)" },
    { name: "size", type: "string", optional: true, enum: AVATAR_SIZES },
    { name: "total", type: "number", optional: true, description: "Total member count when `items` is only a page of it — drives the `+N` chip" },
    {
      name: "fallback",
      type: "string",
      optional: true,
      enum: AVATAR_FALLBACKS,
      description: "Default fallback for `{name}` items that have no `src` (per-item `fallback` wins)"
    }
  ],
  render: (_node, props, helpers) => {
    const items = asArray(props.items);
    const max = Math.max(1, Math.floor(asNumber(props.max, 4)));
    const size2 = asString(props.size, "md");
    const groupFallback = asString(props.fallback);
    const visible = items.slice(0, max);
    const total = props.total != null ? Math.max(0, Math.floor(asNumber(props.total, 0))) : null;
    const overflow = Math.max(0, (total ?? items.length) - visible.length);
    const root = el("div", { class: "rui-avatar-group", "data-size": size2 });
    for (const item of visible) {
      if (isComponentNode(item)) {
        const child = helpers.renderNode(item);
        const declaredSize = item.args?.[2];
        if (declaredSize == null && child instanceof Element && child.classList.contains("rui-avatar")) {
          child.setAttribute("data-size", size2);
        }
        root.append(child);
        continue;
      }
      const data = item;
      const plain = typeof data === "string";
      const name = plain ? data : asString((data ?? {}).name);
      const src = plain ? "" : asString((data ?? {}).src);
      const status = plain ? "" : asString((data ?? {}).status);
      const itemFallback = (plain ? "" : asString((data ?? {}).fallback)) || groupFallback;
      root.append(Avatar.render(
        { __kind: "Component", name: "Avatar", args: [], argMeta: [] },
        // `undefined` (not "") so Avatar's own default still applies.
        { name, src, size: size2, status, fallback: itemFallback || void 0 },
        helpers
      ));
    }
    if (overflow > 0) {
      root.append(el("span", {
        class: "rui-avatar rui-avatar-overflow",
        "data-size": size2
      }, [el("span", { class: "rui-avatar-fallback" }, [`+${overflow}`])]));
    }
    return root;
  }
};
const PROGRESS_FALLBACK_NAME = "Progress";
const Progress = {
  name: "Progress",
  description: "Linear progress bar. `value` is clamped between 0 and `max` (default 100). `indeterminate=true` renders a looping animation when the total is unknown. Provide `segments` to render a segmented progress strip (steps in an onboarding flow), or `buffered` for a secondary buffer indicator (downloads, video buffering) — a segmented bar shows the buffer as pre-lit segments. `label` also becomes the bar's accessible name.",
  props: [
    { name: "value", type: "number", optional: true, description: "Current progress; ignored when indeterminate" },
    { name: "max", type: "number", optional: true, description: "Upper bound (default 100)" },
    { name: "label", type: "string", optional: true, description: "Shown above the bar" },
    { name: "tone", aliases: ["variant"], type: "string", optional: true, enum: ["primary", "success", "warning", "danger", "info"] },
    { name: "indeterminate", type: "boolean", optional: true },
    { name: "showValue", type: "boolean", optional: true, description: "Show the numeric value on the right" },
    { name: "segments", type: "number", optional: true, description: "Render N equal segments (filled by current step)" },
    { name: "buffered", type: "number", optional: true, description: "Secondary value (0..max) drawn behind the bar" }
  ],
  render: (_node, props) => {
    const max = Math.max(1, asNumber(props.max, 100));
    const indeterminate = asBoolean(props.indeterminate);
    const value = Math.max(0, Math.min(max, asNumber(props.value, 0)));
    const percent = Math.round(value / max * 100);
    const segments = Math.min(200, Math.max(0, Math.floor(asNumber(props.segments, 0))));
    const buffered = props.buffered != null ? Math.max(0, Math.min(max, asNumber(props.buffered, 0))) : null;
    const tone = asString(props.tone, "primary");
    const root = el("div", { class: "rui-progress", "data-tone": tone });
    const label = asString(props.label);
    const showValue = asBoolean(props.showValue);
    if (label || showValue) {
      const head = el("div", { class: "rui-progress-head" });
      head.append(el("span", { class: "rui-progress-label" }, [label]));
      if (showValue && !indeterminate) {
        const display = segments > 0 ? `${Math.min(segments, Math.round(value / max * segments))} / ${segments}` : `${percent}%`;
        head.append(el("span", { class: "rui-progress-value" }, [display]));
      }
      root.append(head);
    }
    if (segments > 0 && !indeterminate) {
      const filled = Math.min(segments, Math.round(value / max * segments));
      const trackRoot = el("div", {
        class: "rui-progress-segments",
        role: "progressbar",
        // Without a name three bars on one page all announce as an unnamed
        // "progress bar, 62 percent" with nothing to tell them apart.
        "aria-label": label || PROGRESS_FALLBACK_NAME,
        "aria-valuemin": "0",
        "aria-valuemax": String(segments),
        "aria-valuenow": String(filled),
        // A step strip is counted, not measured: the derived percentage AT would
        // otherwise read ("60%") contradicts the "3 / 5" printed next to it.
        "aria-valuetext": `${filled} of ${segments}`
      });
      const bufferedSegments = buffered !== null ? Math.min(segments, Math.round(buffered / max * segments)) : 0;
      for (let i = 0; i < segments; i += 1) {
        trackRoot.append(el("span", {
          class: "rui-progress-segment",
          // Mirror the tone onto every segment: theme overrides target
          // `.rui-progress-segment[data-tone=…]` directly, and tone only lived
          // on the root, so those rules never matched.
          "data-tone": tone,
          "data-filled": i < filled ? "true" : "false",
          "data-buffered": i >= filled && i < bufferedSegments ? "true" : null
        }));
      }
      root.append(trackRoot);
      return root;
    }
    const track = el("div", {
      class: "rui-progress-track",
      role: "progressbar",
      "aria-label": label || (indeterminate ? "Loading" : PROGRESS_FALLBACK_NAME),
      "aria-valuemin": "0",
      "aria-valuemax": String(max),
      "aria-valuenow": indeterminate ? null : String(value),
      "aria-busy": indeterminate ? "true" : null,
      "data-indeterminate": indeterminate ? "true" : "false"
    });
    if (buffered !== null) {
      const bufferedPercent = Math.round(buffered / max * 100);
      track.append(el("div", {
        class: "rui-progress-buffer",
        style: `width:${bufferedPercent}%`,
        "aria-hidden": "true"
      }));
    }
    track.append(el("div", {
      class: "rui-progress-bar",
      style: indeterminate ? "" : `width:${percent}%`
    }));
    root.append(track);
    return root;
  }
};
const Switch = {
  name: "Switch",
  description: "Compact on/off toggle. Pass a `$variable` as `value` for two-way binding — prefer Switch over Checkbox when the control represents a setting. `onChange(checked)` fires with the new boolean.",
  props: [
    { name: "id", type: "string" },
    { name: "label", type: "string", optional: true },
    { name: "value", type: "boolean", optional: true, aliases: ["checked"], description: "Bound value (typically $variable)" },
    { name: "description", type: "string", optional: true },
    { name: "disabled", type: "boolean", optional: true },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"], description: "Called with the new boolean value" },
    { name: "labelHidden", type: "boolean", optional: true, description: "Keep the label in the accessibility tree but hide it visually — for a switch in a table cell whose column header already carries the name" }
  ],
  render: (node, props, helpers) => {
    const id = asString(props.id);
    const root = el("label", {
      class: "rui-switch",
      for: id,
      "data-disabled": asBoolean(props.disabled) ? "true" : "false"
    });
    const stateName = node.argMeta?.[2]?.stateRef;
    const controlled = stateName != null || props.value != null;
    const localSlot = controlled ? null : helpers.useInstanceState("checked", false);
    const isChecked = localSlot ? localSlot.get() : asBoolean(props.value);
    const input = el("input", {
      type: "checkbox",
      id,
      name: id,
      class: "rui-switch-input",
      role: "switch",
      // Only assert `checked` when this render really holds a checked state:
      // morph reads a present attribute as a deliberate assertion, and an
      // absent one as "leave the user's toggle alone".
      checked: isChecked ? "" : null,
      // `role="switch"` overrides the checkbox's native mapping, so the state
      // has to be published explicitly (the change handler keeps it in sync).
      "aria-checked": isChecked ? "true" : "false",
      disabled: asBoolean(props.disabled) ? "" : null
    });
    input.checked = isChecked;
    const track = el("span", { class: "rui-switch-track" }, [
      el("span", { class: "rui-switch-thumb" })
    ]);
    if (stateName) {
      helpers.bindState(input, stateName, {
        event: "change",
        getValue: (n) => n.checked
      });
    }
    const boundChange = input.onchange;
    input.onchange = function(event) {
      const live = event.currentTarget ?? event.target;
      boundChange?.call(this, event);
      if (localSlot) localSlot.set(live.checked);
      live.setAttribute("aria-checked", live.checked ? "true" : "false");
      helpers.invoke(props.onChange, live.checked);
    };
    const label = asString(props.label);
    const description = asString(props.description);
    root.append(input, track);
    if (label || description) {
      const labelClass = asBoolean(props.labelHidden) ? "rui-switch-label rui-visually-hidden" : "rui-switch-label";
      const meta = el("span", { class: "rui-switch-meta" });
      if (label) meta.append(el("span", { class: labelClass }, [label]));
      if (description) meta.append(el("span", { class: "rui-switch-description" }, [description]));
      root.append(meta);
    }
    return root;
  }
};
const TOGGLE_VARIANTS = ["default", "outline", "ghost"];
const ToggleGroup = {
  name: "ToggleGroup",
  description: "Group of Toggle-style buttons. Items are `[value, label]` arrays, `{value, label, icon?, disabled?}` objects, or plain strings (used for both value and label). Single-select by default — pass `multiple: true` for an independently toggleable set (a bold/italic/underline toolbar), which reads and writes an ARRAY of values. Pass a `$variable` as `value` for two-way binding; item values keep their original type, so numeric items write numbers back. `onChange(value)` fires with the new selection. Left/Right arrows move between items; give the group a `label` so screen readers can tell two groups apart.",
  props: [
    { name: "id", type: "string" },
    { name: "items", type: "any[]" },
    { name: "value", type: "any", optional: true },
    { name: "variant", aliases: ["tone"], type: "string", optional: true, enum: TOGGLE_VARIANTS },
    { name: "size", type: "string", optional: true, enum: ["sm", "md", "lg"] },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"], description: "Called with the newly-selected value (an array when `multiple`)" },
    // Appended below `onChange` on purpose: `value` is read from its declared
    // slot index (2), so the existing slots must not move.
    { name: "multiple", type: "boolean", optional: true, description: "Let any number of items be on at once; reads/writes an array" },
    { name: "type", type: "string", optional: true, enum: ["single", "multiple"], description: "Selection mode — `single` (default) or `multiple`" },
    { name: "disabled", type: "boolean", optional: true, description: "Disable every item in the group" },
    { name: "label", type: "string", optional: true, aliases: ["ariaLabel"], description: "Accessible name for the group (required by the radiogroup role)" }
  ],
  render: (node, props, helpers) => {
    const idHoldsItems = Array.isArray(props.id) && props.items == null;
    const id = idHoldsItems ? "" : asString(props.id);
    const multiple = asBoolean(props.multiple) || asString(props.type) === "multiple";
    const variant = asString(props.variant, "outline");
    const size2 = asString(props.size, "md");
    const groupDisabled = asBoolean(props.disabled);
    const groupLabel = asString(props.label);
    const stateName = node.argMeta?.[2]?.stateRef;
    const localSlot = stateName ? null : helpers.useInstanceState("toggle-value", props.value);
    const current = localSlot ? localSlot.get() : props.value;
    const items = asArray(idHoldsItems ? props.id : props.items).map(extractToggleItem);
    const selected = selectedToggleKeys(current, multiple);
    const root = el("div", {
      class: "rui-toggle-group",
      // The declared prop used to swallow the universal `id` channel without
      // applying it, so `ToggleGroup(id: "view")` produced an element with no id.
      id: id || null,
      // A multi-select group is a set of independent toggle buttons, not a set
      // of radios: `aria-pressed` is the model AT announces for that.
      role: multiple ? "group" : "radiogroup",
      "aria-label": groupLabel || null,
      "aria-disabled": groupDisabled ? "true" : null,
      "data-variant": variant,
      "data-size": size2,
      "data-multiple": multiple ? "true" : null,
      "data-disabled": groupDisabled ? "true" : null
    });
    const commit = (origin, item) => {
      const source = localSlot ? localSlot.get() : props.value;
      let next;
      if (multiple) {
        const list = asArray(source);
        next = list.some((v) => asString(v) === item.key) ? list.filter((v) => asString(v) !== item.key) : [...list, item.value];
      } else {
        next = item.value;
      }
      if (stateName) helpers.setState(stateName, next);
      if (localSlot) localSlot.set(next);
      paintToggleGroup(origin.closest(".rui-toggle-group"), next, multiple);
      helpers.invoke(props.onChange, next);
    };
    const activate = (origin) => {
      const key = origin.getAttribute("data-value") ?? "";
      const item = items.find((it) => it.key === key);
      if (item) commit(origin, item);
    };
    const selectedIndex = items.findIndex((it) => selected.has(it.key) && !it.disabled);
    const firstEnabled = items.findIndex((it) => !it.disabled);
    const rovingIndex = selectedIndex >= 0 ? selectedIndex : Math.max(0, firstEnabled);
    items.forEach((item, index) => {
      const isOn = selected.has(item.key);
      const disabled = groupDisabled || item.disabled;
      const btn = el("button", {
        type: "button",
        class: "rui-toggle",
        role: multiple ? null : "radio",
        "aria-checked": multiple ? null : isOn ? "true" : "false",
        "aria-pressed": multiple ? isOn ? "true" : "false" : null,
        "data-variant": variant,
        "data-size": size2,
        "data-state": isOn ? "on" : "off",
        "data-value": item.key,
        disabled: disabled ? "" : null,
        tabindex: multiple || disabled ? null : index === rovingIndex ? "0" : "-1"
      });
      const itemIconNode = renderIcon(item.icon, { className: "rui-toggle-icon" });
      if (itemIconNode) btn.append(itemIconNode);
      btn.append(el("span", { class: "rui-toggle-label" }, [item.label]));
      if (!disabled) {
        btn.onclick = (event) => {
          activate(event.currentTarget ?? event.target);
        };
        if (!multiple) {
          btn.onkeydown = (event) => moveToggleFocus(event, activate);
        }
      }
      root.append(btn);
    });
    return root;
  }
};
function extractToggleItem(raw) {
  if (typeof raw === "string") return { value: raw, key: raw, label: raw, icon: "", disabled: false };
  if (typeof raw === "number" || typeof raw === "boolean") {
    const key = String(raw);
    return { value: raw, key, label: key, icon: "", disabled: false };
  }
  if (Array.isArray(raw)) {
    const value = raw[0];
    return {
      value,
      key: asString(value),
      label: asString(raw[1], asString(value)),
      icon: asString(raw[2]),
      disabled: asBoolean(raw[3])
    };
  }
  if (raw && typeof raw === "object") {
    const r = raw;
    const value = r.value;
    return {
      value,
      key: asString(value),
      label: asString(r.label, asString(value)),
      icon: asString(r.icon),
      disabled: asBoolean(r.disabled)
    };
  }
  return { value: "", key: "", label: "", icon: "", disabled: false };
}
function selectedToggleKeys(value, multiple) {
  if (multiple) return new Set(asArray(value).map((v) => asString(v)));
  return value == null ? /* @__PURE__ */ new Set() : /* @__PURE__ */ new Set([asString(value)]);
}
function paintToggleGroup(root, value, multiple) {
  if (!root) return;
  const keys = selectedToggleKeys(value, multiple);
  const buttons = [...root.querySelectorAll(".rui-toggle")];
  for (const btn of buttons) {
    const on = keys.has(btn.getAttribute("data-value") ?? "");
    btn.setAttribute("data-state", on ? "on" : "off");
    if (multiple) btn.setAttribute("aria-pressed", on ? "true" : "false");
    else btn.setAttribute("aria-checked", on ? "true" : "false");
  }
  if (multiple) return;
  const enabled = buttons.filter((btn) => !btn.hasAttribute("disabled"));
  const checked = enabled.find((btn) => keys.has(btn.getAttribute("data-value") ?? "")) ?? enabled[0];
  for (const btn of enabled) btn.setAttribute("tabindex", btn === checked ? "0" : "-1");
}
function moveToggleFocus(event, activate) {
  const forward = event.key === "ArrowRight" || event.key === "ArrowDown";
  const back = event.key === "ArrowLeft" || event.key === "ArrowUp";
  if (!forward && !back && event.key !== "Home" && event.key !== "End") return;
  const origin = event.currentTarget ?? event.target;
  const group = origin?.closest(".rui-toggle-group");
  if (!origin || !group) return;
  const buttons = [...group.querySelectorAll(".rui-toggle:not([disabled])")];
  const at = buttons.indexOf(origin);
  if (at < 0) return;
  event.preventDefault();
  const target = event.key === "Home" ? buttons[0] : event.key === "End" ? buttons[buttons.length - 1] : buttons[(at + (forward ? 1 : -1) + buttons.length) % buttons.length];
  if (!target) return;
  for (const btn of buttons) btn.setAttribute("tabindex", btn === target ? "0" : "-1");
  target.focus();
  activate(target);
}
const POPOVER_SIDES = ["bottom", "top", "left", "right"];
const POPOVER_ALIGNS = ["start", "center", "end"];
const hoverPanelController = (rootSelector, panelSelector, defaultSide, floating) => ({
  resolve: (event) => {
    const origin = event.currentTarget ?? event.target;
    return origin?.closest(rootSelector) ?? null;
  },
  open: (liveRoot) => {
    const panel = liveRoot.querySelector(panelSelector);
    if (!panel) return;
    liveRoot.setAttribute("data-open", "true");
    panel.removeAttribute("aria-hidden");
    panel.removeAttribute("inert");
    openFloating(panel, {
      ...floating,
      anchor: liveRoot,
      side: liveRoot.getAttribute("data-side") ?? defaultSide,
      // `align` is authored on the root, so read it live rather than freezing
      // the controller's default — the panel is positioned in JS.
      align: liveRoot.getAttribute("data-align") ?? floating.align
    });
  },
  close: (liveRoot) => {
    liveRoot.setAttribute("data-open", "false");
    const panel = liveRoot.querySelector(panelSelector);
    hidePanelFromAT(panel);
    closeFloating(panel);
  }
});
function hidePanelFromAT(panel) {
  if (!panel) return;
  panel.setAttribute("aria-hidden", "true");
  panel.setAttribute("inert", "");
}
const wireFocusReveal = (root, triggerNode, reveal, hide) => {
  root.onfocus = reveal;
  root.onblur = hide;
  if (!(triggerNode instanceof HTMLElement)) return;
  const priorFocus = triggerNode.onfocus;
  const priorBlur = triggerNode.onblur;
  triggerNode.onfocus = function(event) {
    priorFocus?.call(this, event);
    reveal(event);
  };
  triggerNode.onblur = function(event) {
    priorBlur?.call(this, event);
    hide(event);
  };
};
const positionOnMount = (root, open) => {
  deferToPaint(() => {
    if (!root.isConnected) return;
    if (root.getAttribute("data-open") !== "true") return;
    open(root);
  });
};
const TOOLTIP_PANEL = hoverPanelController(
  ".rui-tooltip",
  ".rui-tooltip-content",
  "top",
  // Hints centre on the trigger edge (what `--rui-tooltip-x: -50%` used to do)
  // and must never grow an internal scrollbar — a scrolling tooltip reads as
  // broken, and a hint is short enough that it never needs one.
  { align: "center", layer: "tooltip", maxHeight: "none" }
);
const TOOLTIP_PRESSED = /* @__PURE__ */ new WeakSet();
const armDelay = (slot, helpers, key, ms, run) => {
  const pending = slot.get();
  if (pending !== null) clearTimeout(pending);
  const handle = setTimeout(() => {
    slot.set(null);
    run();
  }, ms);
  slot.set(handle);
  helpers.registerDisposer(() => clearTimeout(handle), key);
};
const Tooltip = {
  name: "Tooltip",
  description: "Wraps a trigger node and shows `label` when the user hovers or focuses it — text, or a node when the hint needs a `Kbd` chip. The tooltip hides on click/touch (so it does not stay stuck on touch devices) and on Escape, and `side` + `align` place it on any of the 12 edge positions — it is never clipped by a scrolling or `overflow: hidden` container. Pass `delay` (ms) so sweeping across a toolbar does not pop every hint, or `open: true` to force one open for a product tour. `onOpenChange(open)` fires on every transition, so a tour step can advance when the hint is dismissed. Use for short hints (≤6 words); reach for HoverCard when you need rich content.",
  props: [
    { name: "label", type: "string | Node", description: "Hint text, or a node when the hint needs a Kbd chip or emphasis" },
    { name: "trigger", type: "Node", aliases: ["children"] },
    { name: "side", type: "string", optional: true, enum: ["top", "bottom", "left", "right"], aliases: ["placement"] },
    { name: "delay", type: "number", optional: true, aliases: ["delayDuration", "enterDelay"], description: "Milliseconds to wait before showing on hover/focus (default 0)" },
    { name: "open", type: "boolean", optional: true, description: "Force the hint open (coach-marks, screenshots)" },
    // Appended after `open` rather than filed next to `side`: positional calls
    // bind slots in declaration order, so inserting mid-list would silently
    // rebind `Tooltip("hint", trigger, "bottom", 300)`'s fourth argument.
    { name: "align", type: "string", optional: true, enum: POPOVER_ALIGNS, description: 'Alignment along the trigger edge (default "center") — `end` keeps a hint on a right-edge button inside the viewport' },
    { name: "onOpenChange", type: "callable", optional: true, description: "Called with the new boolean whenever the hint is revealed or hidden (hover, focus, Escape, tap)" }
  ],
  render: (_node, props, helpers) => {
    const side = asString(props.side, "top");
    const align = asString(props.align, "center");
    const delay = Math.max(0, asNumber(props.delay, 0));
    const forcedOpen = asBoolean(props.open);
    const openSlot = helpers.useInstanceState("open", forcedOpen);
    const isOpen = forcedOpen || openSlot.get();
    const setOpen = (next) => {
      if (openSlot.get() === next) return;
      openSlot.set(next);
      helpers.invoke(props.onOpenChange, next);
    };
    const delaySlot = helpers.useInstanceState("delay-timer", null);
    const idSlot = helpers.useInstanceState("content-id", "");
    const contentId = panelId(idSlot, "rui-tooltip");
    const triggerNode = helpers.renderNode(props.trigger);
    const root = el("span", {
      class: "rui-tooltip",
      "data-side": side,
      // Read live off the root by the floating controller, so this is what gives
      // the hint the 12 placements the reference libraries expose.
      "data-align": align,
      // Visibility is now driven by this attribute rather than by `:hover` /
      // `:focus-within`, because a panel promoted into the top layer has to be
      // positioned from JS and the two must agree on when it is showing.
      "data-open": isOpen ? "true" : "false",
      // Only when the trigger cannot take focus itself: an IconButton inside a
      // wrapper with its own tabindex costs the keyboard user two Tab presses,
      // and static text gains a tab stop that leads nowhere.
      tabindex: triggerIsFocusable(triggerNode) ? null : "0"
    });
    root.append(el("span", { class: "rui-tooltip-trigger" }, [triggerNode]));
    const content = el("span", {
      class: "rui-tooltip-content",
      role: "tooltip",
      // Referenced by the trigger's `aria-describedby` — a `role="tooltip"` that
      // nothing points at is never announced, leaving icon-only buttons bare.
      id: contentId,
      // Mirrored onto the panel so the arrow can be placed for the alignment
      // actually in force: the panel is promoted into the top layer, where a
      // `.rui-tooltip[data-align] .rui-tooltip-content` descendant rule still
      // matches, but the reparenting fallback's does not.
      "data-align": align,
      "aria-hidden": isOpen ? null : "true",
      inert: isOpen ? null : ""
    }, [
      // A hint that needs a Kbd chip or two emphasised lines could not have one
      // while the label was stringified. Strings keep the text-node fast path.
      isComponentNode(props.label) ? helpers.renderNode(props.label) : asString(props.label)
    ]);
    describeTrigger(triggerNode, contentId);
    content.append(el("span", { class: "rui-tooltip-arrow", "aria-hidden": "true" }));
    root.append(content);
    const show = (live) => {
      setOpen(true);
      TOOLTIP_PANEL.open(live);
    };
    const reveal = (event) => {
      const live = TOOLTIP_PANEL.resolve(event);
      if (!live) return;
      if (delay > 0) {
        armDelay(delaySlot, helpers, "tooltip-delay", delay, () => {
          if (live.isConnected) show(live);
        });
        return;
      }
      show(live);
    };
    const hide = (event) => {
      const live = TOOLTIP_PANEL.resolve(event);
      if (!live) return;
      const pending = delaySlot.get();
      if (pending !== null) {
        clearTimeout(pending);
        delaySlot.set(null);
      }
      if (forcedOpen) return;
      TOOLTIP_PRESSED.delete(live);
      setOpen(false);
      TOOLTIP_PANEL.close(live);
    };
    root.onpointerenter = (event) => {
      if (event.pointerType === "touch") return;
      reveal(event);
    };
    root.onpointerleave = hide;
    root.onpointerdown = (event) => {
      const live = TOOLTIP_PANEL.resolve(event);
      if (!live) return;
      TOOLTIP_PRESSED.add(live);
      if (forcedOpen) return;
      const pending = delaySlot.get();
      if (pending !== null) {
        clearTimeout(pending);
        delaySlot.set(null);
      }
      setOpen(false);
      TOOLTIP_PANEL.close(live);
    };
    root.onpointerup = (event) => {
      const live = TOOLTIP_PANEL.resolve(event);
      if (live) TOOLTIP_PRESSED.delete(live);
    };
    root.onkeydown = (event) => {
      if (event.key !== "Escape") return;
      if (forcedOpen) return;
      const live = TOOLTIP_PANEL.resolve(event);
      if (!live || live.getAttribute("data-open") !== "true") return;
      const pending = delaySlot.get();
      if (pending !== null) {
        clearTimeout(pending);
        delaySlot.set(null);
      }
      setOpen(false);
      TOOLTIP_PANEL.close(live);
    };
    wireFocusReveal(root, triggerNode, (event) => {
      const live = TOOLTIP_PANEL.resolve(event);
      if (!live) return;
      if (TOOLTIP_PRESSED.delete(live)) return;
      reveal(event);
    }, hide);
    if (isOpen) positionOnMount(root, TOOLTIP_PANEL.open);
    return root;
  }
};
const HOVER_CARD_PANEL = hoverPanelController(
  ".rui-hover-card",
  ".rui-hover-card-content",
  "bottom",
  // A rich card can be tall, so the floating layer's default height cap (fit
  // the chosen side, scroll internally) is the behaviour we want here.
  { align: "start", layer: "popover" }
);
const HoverCard = {
  name: "HoverCard",
  description: "Wraps a trigger node and reveals a card with rich content on hover/focus (tap toggles it on touch devices, Escape closes it). Use for previewing a referenced item (profile, link target, definition). `openDelay` / `closeDelay` keep a paragraph full of mentions from flashing cards and give the user time to move onto the card; `width` and `align` size and place it. `onOpenChange(open)` fires on every transition, so the preview can be fetched on open and cancelled on close. Give it a `label` when the card is interactive so the popup has an accessible name.",
  props: [
    { name: "trigger", type: "Node" },
    { name: "content", type: "Node[]", aliases: ["children"] },
    { name: "side", type: "string", optional: true, enum: ["top", "bottom", "left", "right"], aliases: ["placement"] },
    { name: "open", type: "boolean", optional: true, description: "Force the card open (otherwise reveal on hover/focus)" },
    { name: "align", type: "string", optional: true, enum: POPOVER_ALIGNS, description: 'Alignment along the trigger edge (default "start")' },
    { name: "width", type: "string", optional: true, description: "CSS width for the card (default 240–320px)" },
    { name: "openDelay", type: "number", optional: true, description: "Milliseconds to wait before revealing (default 0; 700 is a good hover-card value)" },
    { name: "closeDelay", type: "number", optional: true, description: "Milliseconds to keep the card after the pointer leaves, so the user can move onto it" },
    { name: "label", type: "string", optional: true, aliases: ["ariaLabel"], description: "Accessible name for the card" },
    { name: "onOpenChange", type: "callable", optional: true, description: "Called with the new boolean whenever the card is revealed or hidden (hover, focus, tap, Escape) — use it to lazy-load the preview" }
  ],
  render: (_node, props, helpers) => {
    const forcedOpen = asBoolean(props.open);
    const openSlot = helpers.useInstanceState("open", forcedOpen);
    const isOpen = forcedOpen || openSlot.get();
    const setOpen = (next) => {
      if (openSlot.get() === next) return;
      openSlot.set(next);
      helpers.invoke(props.onOpenChange, next);
    };
    const openDelay = Math.max(0, asNumber(props.openDelay, 0));
    const closeDelay = Math.max(0, asNumber(props.closeDelay, 0));
    const timerSlot = helpers.useInstanceState("hover-timer", null);
    const width = sanitiseCssLength(props.width, "");
    const label = asString(props.label);
    const triggerNode = helpers.renderNode(props.trigger);
    const root = el("span", {
      class: "rui-hover-card",
      "data-side": asString(props.side, "bottom"),
      "data-align": asString(props.align, "start"),
      // Always written (rather than omitted while closed) so the JS reveal and
      // the morph reconciler both see a stable attribute to flip.
      "data-open": isOpen ? "true" : "false",
      tabindex: triggerIsFocusable(triggerNode) ? null : "0"
    });
    root.append(el("span", { class: "rui-hover-card-trigger" }, [triggerNode]));
    const card = el("span", {
      class: "rui-hover-card-content",
      // A dialog role with no accessible name announces as an anonymous dialog,
      // so only claim it when the author named the card.
      role: label ? "dialog" : null,
      "aria-label": label || null,
      // `max-width` in the sheet always beats `width`, inline or not — override
      // it here or a wider link-preview card is silently clamped to 320px.
      style: width ? `width: ${width}; max-width: none;` : null,
      // A card hidden by opacity alone left its buttons in the tab order and its
      // text in the reading order of the surrounding prose.
      "aria-hidden": isOpen ? null : "true",
      inert: isOpen ? null : ""
    });
    for (const child of asArray(props.content)) card.append(helpers.renderNode(child));
    root.append(card);
    const show = (live) => {
      setOpen(true);
      HOVER_CARD_PANEL.open(live);
    };
    const dismiss = (live) => {
      setOpen(false);
      HOVER_CARD_PANEL.close(live);
    };
    const clearPending = () => {
      const pending = timerSlot.get();
      if (pending !== null) {
        clearTimeout(pending);
        timerSlot.set(null);
      }
    };
    const reveal = (event) => {
      const live = HOVER_CARD_PANEL.resolve(event);
      if (!live) return;
      clearPending();
      if (openDelay > 0) {
        armDelay(timerSlot, helpers, "hover-card-delay", openDelay, () => {
          if (live.isConnected) show(live);
        });
        return;
      }
      show(live);
    };
    const hide = (event) => {
      if (forcedOpen) return;
      const live = HOVER_CARD_PANEL.resolve(event);
      if (!live) return;
      clearPending();
      if (closeDelay > 0) {
        armDelay(timerSlot, helpers, "hover-card-delay", closeDelay, () => {
          if (live.isConnected) dismiss(live);
        });
        return;
      }
      dismiss(live);
    };
    root.onpointerenter = (event) => {
      if (event.pointerType === "touch") return;
      reveal(event);
    };
    root.onpointerleave = (event) => {
      if (event.pointerType === "touch") return;
      hide(event);
    };
    root.onpointerdown = (event) => {
      if (event.pointerType !== "touch") return;
      const live = HOVER_CARD_PANEL.resolve(event);
      if (!live) return;
      clearPending();
      if (live.getAttribute("data-open") === "true") dismiss(live);
      else show(live);
    };
    root.onkeydown = (event) => {
      if (event.key !== "Escape") return;
      const live = HOVER_CARD_PANEL.resolve(event);
      if (!live || live.getAttribute("data-open") !== "true") return;
      clearPending();
      dismiss(live);
    };
    wireFocusReveal(root, triggerNode, reveal, hide);
    if (isOpen) positionOnMount(root, HOVER_CARD_PANEL.open);
    return root;
  }
};
const RATING_ICONS = {
  star: { full: "star", half: "star-half-stroke", empty: "regular:star" },
  heart: { full: "heart", half: "heart", empty: "regular:heart" },
  thumb: { full: "thumbs-up", half: "thumbs-up", empty: "regular:thumbs-up" },
  fire: { full: "fire", half: "fire", empty: "regular:fire" },
  bolt: { full: "bolt", half: "bolt", empty: "regular:bolt" }
};
const Rating = {
  name: "Rating",
  description: "Compact 0–5 star rating with optional numeric badge and review count. Use in product cards, testimonials, reviews, and KPI rows. Pass `interactive: true` (or an `onChange` handler) to let users pick a rating; bind `value: $rating` for two-way binding, or read the new value from `onChange: (v) => …`. With `halfStep: true` clicking the left half of a star sets a fractional value; keyboard users get whole steps with Enter/Space and half steps with the arrow keys. `allowClear: true` lets clicking the current rating remove it. `icon` swaps the glyph family — `star` (default), `heart`, `thumb`, `fire`, `bolt`, or any custom Font Awesome name — and `tone` recolours it (amber by default).",
  props: [
    { name: "value", type: "number", description: "0–max; can be a $variable when interactive" },
    { name: "max", type: "number", optional: true, description: "Maximum number of stars (default 5)" },
    { name: "label", type: "string", optional: true, description: 'Inline text shown after the stars (e.g. "4.2 of 5"); also names the group when interactive' },
    { name: "count", type: "number", optional: true, description: 'Review/voter count rendered in parentheses (0 renders "(0)")' },
    { name: "size", type: "string", optional: true, enum: ["sm", "md", "lg"] },
    { name: "interactive", type: "boolean", optional: true, description: "Allow clicking a star to set the value" },
    { name: "readonly", type: "boolean", optional: true, description: "Force read-only (overrides `interactive`)" },
    { name: "halfStep", type: "boolean", optional: true, description: "Allow half-star resolution when interactive" },
    { name: "icon", type: "string", optional: true, description: "Icon family — `star` (default), `heart`, `thumb`, `fire`, `bolt`, or any FA name" },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"], description: "Called with the new rating when the user picks a star (interactive mode)" },
    { name: "allowClear", type: "boolean", optional: true, description: 'Clicking the current rating again clears it to 0 ("remove my rating")' },
    { name: "tone", type: "string", optional: true, aliases: ["variant", "color"], enum: ["primary", "success", "warning", "danger", "info"], description: "Colour of the filled icons (default warning/amber)" }
  ],
  render: (node, props, helpers) => {
    const max = Math.max(1, Math.floor(asNumber(props.max, 5)));
    const size2 = asString(props.size, "md");
    const halfStep = asBoolean(props.halfStep);
    const allowClear = asBoolean(props.allowClear);
    const tone = asString(props.tone);
    const stateName = node.argMeta?.[0]?.stateRef;
    const interactive = (asBoolean(props.interactive) || props.onChange != null) && !asBoolean(props.readonly);
    const uncontrolled = interactive && !stateName;
    const localSlot = uncontrolled ? helpers.useInstanceState("rating-value", asNumber(props.value, 0)) : null;
    const current = localSlot ? asNumber(localSlot.get(), 0) : asNumber(props.value, 0);
    const raw = Math.max(0, Math.min(max, current));
    const iconChoice = resolveRatingIcons(asString(props.icon));
    const label = asString(props.label);
    const root = el("div", {
      class: "rui-rating",
      "data-size": size2,
      "data-tone": tone || null,
      "data-interactive": interactive ? "true" : "false",
      "data-half-step": interactive && halfStep ? "true" : "false"
    });
    const stars = el("span", {
      class: "rui-rating-stars",
      role: interactive ? "radiogroup" : "img",
      // A `role="img"` IS its own content, so "3.5 of 5" is exactly the right
      // name for the display form. A radiogroup is a control and needs a name
      // that does not change under the user: its value is carried by the radios
      // and by the live status node below.
      "aria-label": interactive ? label || "Rating" : `${raw} of ${max}`
    });
    const rovingValue = raw > 0 ? Math.min(max, Math.max(1, Math.ceil(raw))) : 1;
    const readCurrent = () => localSlot ? Math.max(0, Math.min(max, asNumber(localSlot.get(), 0))) : raw;
    const commit = (origin, next) => {
      const clamped = Math.max(0, Math.min(max, next));
      if (stateName) helpers.setState(stateName, clamped);
      if (localSlot) localSlot.set(clamped);
      paintRating(origin.closest(".rui-rating"), clamped, max, iconChoice);
      helpers.invoke(props.onChange, clamped);
    };
    for (let i = 1; i <= max; i += 1) {
      const fill = Math.max(0, Math.min(1, raw - (i - 1)));
      const iconName = fill >= 1 ? iconChoice.full : fill > 0 ? iconChoice.half : iconChoice.empty;
      const iconClasses = resolveIconClasses(iconName).join(" ");
      const star = el(interactive ? "button" : "span", {
        class: `rui-rating-star ${iconClasses}`.trim(),
        type: interactive ? "button" : null,
        "data-fill": fill >= 1 ? "full" : fill > 0 ? "half" : "empty",
        // Families without a dedicated half glyph (heart, fire, custom names)
        // reuse the full one, so a half is indistinguishable from the next
        // whole value unless CSS clips it — this flags which ones need that.
        "data-half-glyph": iconChoice.half === iconChoice.full ? "synthetic" : null,
        role: interactive ? "radio" : null,
        "aria-checked": interactive ? i === rovingValue && raw > 0 ? "true" : "false" : null,
        tabindex: interactive ? i === rovingValue ? "0" : "-1" : null,
        "aria-label": interactive ? `Rate ${i}` : null,
        "aria-hidden": interactive ? null : "true"
      });
      if (interactive) {
        const fullValue = i;
        const halfValue = i - 0.5;
        star.onclick = (event) => {
          const evt = event;
          const origin = evt.currentTarget ?? evt.target;
          const fromKeyboard = evt.detail === 0 && evt.clientX === 0 && evt.clientY === 0;
          let next = fullValue;
          if (halfStep && !fromKeyboard) {
            const rect = origin.getBoundingClientRect();
            if (rect.width > 0 && evt.clientX - rect.left < rect.width / 2) {
              next = halfValue;
            }
          }
          if (allowClear && next === readCurrent()) next = 0;
          commit(origin, next);
        };
        star.onkeydown = (event) => {
          const evt = event;
          const step = halfStep ? 0.5 : 1;
          const origin = evt.currentTarget ?? evt.target;
          if (evt.key === "ArrowRight" || evt.key === "ArrowUp") {
            evt.preventDefault();
            commit(origin, readCurrent() + step);
          } else if (evt.key === "ArrowLeft" || evt.key === "ArrowDown") {
            evt.preventDefault();
            commit(origin, readCurrent() - step);
          } else if (evt.key === "Home") {
            evt.preventDefault();
            commit(origin, allowClear ? 0 : step);
          } else if (evt.key === "End") {
            evt.preventDefault();
            commit(origin, max);
          }
        };
      }
      stars.append(star);
    }
    root.append(stars);
    if (interactive) {
      root.append(el("span", {
        class: "rui-rating-status rui-visually-hidden",
        role: "status",
        "aria-live": "polite"
      }, [`${raw} of ${max}`]));
    }
    if (label) root.append(el("span", { class: "rui-rating-label" }, [label]));
    const count = props.count != null ? asNumber(props.count, 0) : null;
    if (count !== null && count >= 0) {
      root.append(el("span", { class: "rui-rating-count" }, [`(${count.toLocaleString()})`]));
    }
    return root;
  }
};
function paintRating(root, value, max, icons) {
  if (!root) return;
  const clamped = Math.max(0, Math.min(max, value));
  const interactive = root.getAttribute("data-interactive") === "true";
  const checkedAt = clamped > 0 ? Math.min(max, Math.max(1, Math.ceil(clamped))) : 0;
  const stars = root.querySelectorAll(".rui-rating-star");
  stars.forEach((star, idx) => {
    const fill = Math.max(0, Math.min(1, value - idx));
    const iconName = fill >= 1 ? icons.full : fill > 0 ? icons.half : icons.empty;
    star.setAttribute("data-fill", fill >= 1 ? "full" : fill > 0 ? "half" : "empty");
    star.className = `rui-rating-star ${resolveIconClasses(iconName).join(" ")}`.trim();
    if (icons.half === icons.full) star.setAttribute("data-half-glyph", "synthetic");
    if (!interactive) return;
    star.setAttribute("aria-checked", idx + 1 === checkedAt ? "true" : "false");
    star.setAttribute("tabindex", idx + 1 === (checkedAt || 1) ? "0" : "-1");
  });
  if (interactive) {
    const status = root.querySelector(".rui-rating-status");
    if (status) status.textContent = `${clamped} of ${max}`;
  } else {
    root.querySelector(".rui-rating-stars")?.setAttribute("aria-label", `${clamped} of ${max}`);
  }
}
function resolveRatingIcons(icon) {
  const key = icon.trim().toLowerCase();
  if (!key) return RATING_ICONS.star;
  if (RATING_ICONS[key]) return RATING_ICONS[key];
  return { full: key, half: key, empty: `regular:${key}` };
}
const ProgressRing = {
  name: "ProgressRing",
  description: "Circular progress indicator. Use for KPIs, quotas, completion rings, and any metric better shown as a circle than a bar. Renders the value (or a custom `label`) inside the ring; pass `icon` for a glyph instead (completion checkmarks). `size` takes `sm|md|lg` or a plain number of px (`size: 40`), so the ring fits a table cell or fills a hero card.",
  props: [
    { name: "value", type: "number", optional: true, description: "Current value (ignored when indeterminate)" },
    { name: "max", type: "number", optional: true, description: "Upper bound (default 100)" },
    { name: "label", type: "string", optional: true, description: 'Text shown inside the ring (default "{percent}%")' },
    { name: "caption", type: "string", optional: true, aliases: ["description"], description: "Small caption rendered under the ring" },
    { name: "tone", aliases: ["variant"], type: "string", optional: true, enum: ["primary", "success", "warning", "danger", "info"] },
    // The enum stays so a mistyped token is still caught, and it only gates
    // string literals — a numeric `size: 40` passes straight through to the
    // diameter path below.
    { name: "size", type: "string|number", optional: true, enum: ["sm", "md", "lg"], description: "`sm|md|lg`, or a diameter in px as a number (`size: 40`)" },
    { name: "indeterminate", type: "boolean", optional: true },
    { name: "icon", type: "string", optional: true, description: "Font Awesome icon rendered inside the ring instead of the label" }
  ],
  render: (_node, props) => {
    const max = Math.max(1, asNumber(props.max, 100));
    const indeterminate = asBoolean(props.indeterminate);
    const value = Math.max(0, Math.min(max, asNumber(props.value, 0)));
    const percent = Math.round(value / max * 100);
    const sizeRaw = asString(props.size, "md");
    const explicitPx = /^\d+(\.\d+)?(px)?$/.test(sizeRaw.trim()) ? Math.min(480, Math.max(16, parseFloat(sizeRaw))) : null;
    const size2 = explicitPx !== null ? explicitPx < 84 ? "sm" : explicitPx < 108 ? "md" : "lg" : sizeRaw;
    const px = explicitPx ?? (size2 === "lg" ? 120 : size2 === "sm" ? 72 : 96);
    const stroke = explicitPx !== null ? Math.max(2, Math.round(px / 12)) : size2 === "lg" ? 10 : size2 === "sm" ? 6 : 8;
    const r = (px - stroke) / 2;
    const circumference = 2 * Math.PI * r;
    const offset = indeterminate ? circumference * 0.65 : circumference * (1 - percent / 100);
    const caption = asString(props.caption);
    const root = el("div", {
      class: "rui-progress-ring",
      "data-tone": asString(props.tone, "primary"),
      "data-size": size2,
      "data-indeterminate": indeterminate ? "true" : "false",
      // Same control semantics as the sibling Progress: without these the ring
      // announced as loose text with no min/max and no busy state.
      role: "progressbar",
      "aria-label": caption || (indeterminate ? "Loading" : PROGRESS_FALLBACK_NAME),
      "aria-valuemin": "0",
      "aria-valuemax": indeterminate ? null : String(max),
      "aria-valuenow": indeterminate ? null : String(value),
      "aria-busy": indeterminate ? "true" : null
    });
    const wrap = el("div", { class: "rui-progress-ring-wrap" });
    const svgNS = "http://www.w3.org/2000/svg";
    const svg = document.createElementNS(svgNS, "svg");
    svg.setAttribute("width", String(px));
    svg.setAttribute("height", String(px));
    svg.setAttribute("viewBox", `0 0 ${px} ${px}`);
    svg.setAttribute("class", "rui-progress-ring-svg");
    const track = document.createElementNS(svgNS, "circle");
    track.setAttribute("class", "rui-progress-ring-track");
    track.setAttribute("cx", String(px / 2));
    track.setAttribute("cy", String(px / 2));
    track.setAttribute("r", String(r));
    track.setAttribute("stroke-width", String(stroke));
    track.setAttribute("fill", "none");
    svg.appendChild(track);
    const bar = document.createElementNS(svgNS, "circle");
    bar.setAttribute("class", "rui-progress-ring-bar");
    bar.setAttribute("cx", String(px / 2));
    bar.setAttribute("cy", String(px / 2));
    bar.setAttribute("r", String(r));
    bar.setAttribute("stroke-width", String(stroke));
    bar.setAttribute("fill", "none");
    bar.setAttribute("stroke-linecap", "round");
    bar.setAttribute("stroke-dasharray", String(circumference));
    bar.setAttribute("stroke-dashoffset", String(offset));
    svg.appendChild(bar);
    wrap.append(svg);
    const rawLabel = asString(props.label, indeterminate ? "…" : `${percent}%`);
    const center = el("span", { class: "rui-progress-ring-value" });
    const iconProp = asString(props.icon);
    const iconSource = iconProp || (looksLikeIconName(rawLabel) ? rawLabel : "");
    const labelIcon = iconSource ? renderIcon(iconSource, { className: "rui-progress-ring-icon" }) : null;
    if (labelIcon && resolveIconClasses(iconSource).length > 0) {
      center.append(labelIcon);
      if (!indeterminate) root.setAttribute("aria-valuetext", `${percent}%`);
    } else {
      center.append(document.createTextNode(rawLabel));
      if (!indeterminate && asString(props.label)) root.setAttribute("aria-valuetext", rawLabel);
    }
    wrap.append(center);
    root.append(wrap);
    if (caption) root.append(el("span", { class: "rui-progress-ring-caption" }, [caption]));
    return root;
  }
};
function looksLikeIconName(value) {
  const token = value.trim();
  if (!token) return false;
  if (getCustomIcon(token)) return true;
  if (/^(solid|regular|brands):[a-z0-9-]+$/.test(token)) return true;
  return /^[a-z0-9]+(-[a-z0-9]+)+$/.test(token);
}
const ChatBubble = {
  name: "ChatBubble",
  description: 'Single chat-style message bubble with author, time, and body. Use for conversation threads, agent transcripts, support chats, and any message-style UI. Set `from="me"` for the active speaker — the bubble aligns to the right with a primary tint. `from="agent"` (default) renders as the canonical incoming bubble on the left, and `from="system"` as an avatar-less transcript annotation. Pass `content: [...]` for rich bodies (CodeBlock, Image, Link, Table) and `onRetry` alongside `status: "error"` so a failed send is recoverable.',
  props: [
    { name: "author", type: "string" },
    { name: "body", type: "string", aliases: ["text", "message"] },
    { name: "time", type: "string", optional: true },
    { name: "avatarSrc", type: "string", optional: true, aliases: ["src"] },
    { name: "from", type: "string", optional: true, enum: ["agent", "me", "system"], aliases: ["role"], description: "Lane (default agent)" },
    { name: "status", type: "string", optional: true, enum: ["sending", "sent", "delivered", "read", "error"] },
    { name: "content", type: "Node[]", optional: true, aliases: ["children"], description: "Rich body nodes rendered under `body` (CodeBlock, Image, Link, Table, …)" },
    { name: "onRetry", type: "callable", optional: true, description: 'Shown as a Retry action next to `status: "error"`' }
  ],
  render: (_node, props, helpers) => {
    const from = asString(props.from, "agent");
    const author = asString(props.author);
    const root = el("div", {
      class: "rui-chat-bubble",
      "data-from": from
    });
    const showAvatar = from !== "system";
    if (showAvatar && from !== "me") {
      root.append(renderAvatarFallback(asString(props.avatarSrc), author));
    }
    const bubble = el("div", { class: "rui-chat-bubble-bubble" });
    const head = el("header", { class: "rui-chat-bubble-head" });
    head.append(el("span", { class: "rui-chat-bubble-author" }, [author]));
    const time = asString(props.time);
    if (time) head.append(el("span", { class: "rui-chat-bubble-time" }, [time]));
    bubble.append(head);
    const body = asString(props.body);
    if (body) bubble.append(el("p", { class: "rui-chat-bubble-body" }, [body]));
    const content = asArray(props.content);
    if (content.length > 0) {
      const rich = el("div", { class: "rui-chat-bubble-content" });
      for (const child of content) rich.append(helpers.renderNode(child));
      bubble.append(rich);
    }
    const status = asString(props.status);
    if (status) {
      const footer = el("span", { class: "rui-chat-bubble-status", "data-status": status }, [status]);
      if (status === "error" && props.onRetry != null) {
        const retry = el("button", {
          type: "button",
          class: "rui-chat-bubble-retry"
        }, ["Retry"]);
        retry.onclick = (event) => {
          event.stopPropagation();
          helpers.invoke(props.onRetry);
        };
        footer.append(retry);
      }
      bubble.append(footer);
    }
    root.append(bubble);
    if (showAvatar && from === "me") {
      root.append(renderAvatarFallback(asString(props.avatarSrc), author));
    }
    return root;
  }
};
function renderAvatarFallback(src, name) {
  const wrap = el("span", { class: "rui-chat-bubble-avatar" });
  const safeSrc = sanitiseImageSrc(src);
  if (safeSrc) {
    wrap.append(el("img", { src: safeSrc, alt: name, loading: "lazy" }));
  } else {
    wrap.append(el("span", { class: "rui-chat-bubble-fallback" }, [initialsFor(name)]));
  }
  return wrap;
}
const Kbd = {
  name: "Kbd",
  description: "Renders a keyboard shortcut chip (e.g. `Cmd+K`). Pass a single label, or multiple labels as an array to render a `key + key + …` combo.",
  props: [
    { name: "keys", type: "string | string[]" },
    { name: "size", type: "string", optional: true, enum: ["sm", "md"] }
  ],
  render: (_node, props) => {
    const size2 = asString(props.size, "md");
    const root = el("span", { class: "rui-kbd-group", "data-size": size2 });
    const keys = Array.isArray(props.keys) ? props.keys : [props.keys];
    keys.forEach((key, i) => {
      const label = asString(key);
      if (!label) return;
      if (i > 0) root.append(el("span", { class: "rui-kbd-sep" }, ["+"]));
      root.append(el("kbd", { class: "rui-kbd" }, [label]));
    });
    return root;
  }
};
const Popover = {
  name: "Popover",
  description: "Click-triggered popup with arbitrary rich content. Use when HoverCard's hover trigger is too eager and Modal/Sheet is too heavy — perfect for filter panels, color pickers, share menus, and small settings flyouts. The trigger stays visible while the popover is open — clicking it again, clicking outside, pressing Escape, or clicking the built-in × button all close it (pass `showClose: false` to drop the × and its header row). Focus moves into the panel while it is open and returns to the trigger on close. `onOpenChange(open)` fires on every transition, so a filter flyout can apply its draft on close.",
  props: [
    { name: "trigger", type: "Node", description: "Clickable trigger element (Button, Avatar, IconButton, …). The trigger remains visible while the popover is open." },
    { name: "content", type: "Node[]", aliases: ["children"], description: "Body rendered inside the popover" },
    { name: "title", type: "string", optional: true, description: "Optional bold heading rendered above the content (also names the dialog)" },
    { name: "side", type: "string", optional: true, enum: POPOVER_SIDES, aliases: ["placement"], description: 'Where the popover opens relative to the trigger (default "bottom")' },
    { name: "align", type: "string", optional: true, enum: POPOVER_ALIGNS, description: 'Alignment along the trigger edge (default "start")' },
    { name: "width", type: "string", optional: true, description: 'CSS width for the popover panel (default "280px")' },
    { name: "open", type: "boolean", optional: true, description: "Initial open state — use to demo or pre-open the popover" },
    { name: "onOpenChange", type: "callable", optional: true, description: "Called with the new boolean whenever the popover opens or closes" },
    { name: "showClose", type: "boolean", optional: true, description: "Render the × button and its header row (default true)" },
    { name: "disabled", type: "boolean", optional: true, description: "Make the trigger inert (e.g. while a request is in flight)" }
  ],
  render: (_node, props, helpers) => {
    const initialOpen = asBoolean(props.open);
    const openSlot = helpers.useInstanceState("open", initialOpen);
    const isOpen = openSlot.get();
    const width = sanitiseCssLength(props.width, "");
    const disabled = asBoolean(props.disabled);
    const showClose = props.showClose == null || asBoolean(props.showClose);
    const titleText = asString(props.title);
    const idSlot = helpers.useInstanceState("title-id", "");
    const titleId = titleText ? panelId(idSlot, "rui-popover-title") : "";
    const onOpenChange = props.onOpenChange;
    const notify = (next) => {
      helpers.invoke(onOpenChange, next);
    };
    const root = el("div", {
      class: "rui-popover",
      "data-open": isOpen ? "true" : "false",
      "data-side": asString(props.side, "bottom"),
      "data-align": asString(props.align, "start"),
      "data-disabled": disabled ? "true" : null
    });
    const triggerNode = helpers.renderNode(props.trigger);
    const focusableTrigger = triggerIsFocusable(triggerNode);
    const triggerWrap = el("span", {
      class: "rui-popover-trigger",
      "data-state": isOpen ? "open" : "closed",
      "aria-haspopup": "dialog",
      "aria-expanded": isOpen ? "true" : "false",
      "aria-disabled": disabled ? "true" : null,
      // The prop description sanctions non-button triggers (Avatar, Text), and
      // the wrapper carried the keyboard handler while nothing could focus it —
      // so those popovers were mouse-only. Only add the role/tab stop when the
      // wrapped trigger does not bring its own.
      role: focusableTrigger ? null : "button",
      tabindex: focusableTrigger || disabled ? null : "0"
    });
    triggerWrap.append(triggerNode);
    root.append(triggerWrap);
    const body = el("div", {
      class: "rui-popover-content",
      role: "dialog",
      // A dialog with no name announces as "dialog"; the title is the name when
      // there is one, so link it rather than leaving it decorative.
      "aria-labelledby": titleId || null,
      "aria-label": titleId ? null : "Popover",
      // The sheet's `max-width` always beats `width`, inline or not, so a
      // 480px date-range picker silently rendered at 360px.
      style: width ? `width: ${width}; max-width: none;` : null
    });
    const closeBtn = el("button", {
      type: "button",
      class: "rui-popover-close",
      "aria-label": "Close popover"
    }, ["×"]);
    closeBtn.onclick = (event) => {
      event.stopPropagation();
      setPopoverOpen(event.currentTarget ?? event.target, false, openSlot);
      notify(false);
    };
    if (showClose || titleText) {
      const header = el("div", { class: "rui-popover-header" });
      header.append(
        titleText ? el("div", { class: "rui-popover-title", id: titleId || null }, [titleText]) : el("span", { class: "rui-popover-title-spacer" })
      );
      if (showClose) header.append(closeBtn);
      body.append(header);
    }
    for (const child of asArray(props.content)) {
      body.append(helpers.renderNode(child));
    }
    root.append(body);
    const toggle = (origin) => {
      const next = !openSlot.get();
      const liveRoot = setPopoverOpen(origin, next, openSlot);
      if (next && liveRoot) installPopoverDismiss(liveRoot, openSlot, notify);
      notify(next);
    };
    if (!disabled) {
      triggerWrap.onclick = (event) => {
        event.stopPropagation();
        toggle(event.currentTarget ?? event.target);
      };
      triggerWrap.onkeydown = (event) => {
        const e = event;
        const origin = e.currentTarget ?? e.target;
        if (e.key === "Enter" || e.key === " ") {
          e.preventDefault();
          toggle(origin);
        } else if (e.key === "Escape" && openSlot.get()) {
          e.preventDefault();
          setPopoverOpen(origin, false, openSlot);
          notify(false);
        }
      };
    }
    const dialogKeys = dialogKeydownHandler(".rui-popover-content", (origin) => {
      setPopoverOpen(origin, false, openSlot);
      notify(false);
    });
    root.onkeydown = (event) => {
      const origin = event.currentTarget ?? event.target;
      if (origin?.getAttribute("data-open") !== "true") return;
      dialogKeys(event);
    };
    wireDialogFocus(root, ".rui-popover-content", helpers);
    if (isOpen) {
      positionOnMount(root, (liveRoot) => {
        syncFloatingPanel(
          liveRoot,
          true,
          ".rui-popover-content",
          ".rui-popover-trigger",
          { layer: "popover" }
        );
        installPopoverDismiss(liveRoot, openSlot, notify);
      });
    }
    return root;
  }
};
const setPopoverOpen = (origin, next, openSlot) => {
  openSlot.set(next);
  const liveRoot = origin.closest(".rui-popover");
  if (!liveRoot) return null;
  liveRoot.setAttribute("data-open", next ? "true" : "false");
  const trigger = liveRoot.querySelector(".rui-popover-trigger");
  trigger?.setAttribute("aria-expanded", next ? "true" : "false");
  trigger?.setAttribute("data-state", next ? "open" : "closed");
  syncFloatingPanel(
    liveRoot,
    next,
    ".rui-popover-content",
    ".rui-popover-trigger",
    { layer: "popover" }
  );
  if (!next) disposeDismissListeners(liveRoot);
  return liveRoot;
};
const installPopoverDismiss = (liveRoot, openSlot, notify) => {
  installDismissListeners({
    liveRoot,
    onDismiss: () => {
      openSlot.set(false);
      liveRoot.setAttribute("data-open", "false");
      const trigger = liveRoot.querySelector(".rui-popover-trigger");
      trigger?.setAttribute("aria-expanded", "false");
      trigger?.setAttribute("data-state", "closed");
      closeFloating(liveRoot.querySelector(".rui-popover-content"));
      notify?.(false);
    }
  });
};
const TOAST_TONES = ["default", "primary", "success", "warning", "danger", "info"];
const TOASTS_POSITIONS = [
  "top-right",
  "top-left",
  "top-center",
  "bottom-right",
  "bottom-left",
  "bottom-center"
];
const promoteToast = (live) => {
  if (isShowingPopover(live)) return true;
  const ready = typeof live.showPopover === "function" && getComputedStyle(live).getPropertyValue("--rui-toast-top-layer").trim() === "1";
  if (ready) {
    try {
      live.setAttribute("popover", "manual");
      live.showPopover();
      return true;
    } catch {
    }
  }
  live.removeAttribute("popover");
  return false;
};
function isShowingPopover(live) {
  try {
    return live.matches(":popover-open");
  } catch {
    return false;
  }
}
const Toast = {
  name: "Toast",
  description: "Single transient notification card. Always shows a close (×) button that removes the toast from the DOM (and fires `onClose` if set). Pass `duration` (ms) to auto-dismiss — with `pauseOnHover: true` the countdown stops while the pointer is over the card — or `position` for a standalone one-off toast (the renderer will pin it to the viewport corner so you do not have to wrap a single notification in `Stack(...)`). Use `Toasts` for grouped stacks; prefer `Banner` for top-of-page announcements and `Notification` for permanent inbox entries.",
  props: [
    { name: "title", type: "string" },
    { name: "message", type: "string", optional: true, aliases: ["description"] },
    { name: "tone", type: "string", optional: true, aliases: ["variant"], enum: TOAST_TONES, description: 'Visual accent (default "default")' },
    { name: "icon", type: "string", optional: true, description: "Font Awesome icon name (default picked from tone)" },
    { name: "duration", type: "number", optional: true, description: "Auto-dismiss after N milliseconds (e.g. 4000). Omit to keep the toast until the user closes it." },
    { name: "action", type: "Button", optional: true, description: "Optional inline `Button` action shown above the message" },
    { name: "onClose", type: "callable", optional: true, description: "Callable invoked when the toast is dismissed (× button, auto-dismiss, or programmatic)" },
    { name: "position", type: "string", optional: true, enum: TOASTS_POSITIONS, description: "Pin a standalone Toast to a viewport corner without wrapping it in `Stack(...)`" },
    { name: "pauseOnHover", type: "boolean", optional: true, description: "Pause the auto-dismiss countdown while the pointer is over the toast" }
  ],
  render: (node, props, helpers) => {
    const tone = asString(props.tone, "default");
    const position = asString(props.position);
    const title = asString(props.title);
    const message = asString(props.message);
    const identity = `${title}\0${message}\0${tone}`;
    const root = el("div", {
      class: position ? "rui-toast rui-toast-standalone" : "rui-toast",
      // Danger toasts are interruptive — `alert` + `assertive` so screen
      // readers announce them immediately; others are polite `status`.
      role: tone === "danger" ? "alert" : "status",
      "aria-live": tone === "danger" ? "assertive" : "polite",
      "data-tone": tone,
      "data-position": position || null,
      // Reconciliation of unkeyed siblings is positional, so dismissing the top
      // toast shifted every survivor onto its neighbour's DOM node — replaying
      // the entry animation and dropping focus mid-interaction. A content-derived
      // key lets `keyFor` match them across the shift. An author `key:` (and the
      // runtime's own `$toast` id key) always wins.
      "data-rui-key": node.explicitKey == null ? toastAutoKey(identity) : null
    });
    const iconName = asString(props.icon) || defaultToastIcon(tone);
    const iconNode = renderIcon(iconName, { className: "rui-toast-icon" });
    if (iconNode) root.append(iconNode);
    const body = el("div", { class: "rui-toast-body" });
    body.append(el("div", { class: "rui-toast-title" }, [title]));
    if (message) body.append(el("div", { class: "rui-toast-message" }, [message]));
    if (props.action) {
      const actionWrap = el("div", { class: "rui-toast-action" });
      actionWrap.append(helpers.renderNode(props.action));
      body.append(actionWrap);
    }
    root.append(body);
    const dismissedSlot = helpers.useInstanceState("dismissed-id", null);
    const isDismissed = () => dismissedSlot.get() === identity;
    const timerSlot = helpers.useInstanceState("timer", null);
    const removalTimerSlot = helpers.useInstanceState("removal-timer", null);
    const deadlineSlot = helpers.useInstanceState("deadline", 0);
    const remainingSlot = helpers.useInstanceState("remaining", 0);
    const liveNodeSlot = helpers.useInstanceState("live-node", null);
    if (liveNodeSlot.get()?.isConnected !== true) {
      deferToPaint(() => {
        if (root.isConnected) liveNodeSlot.set(root);
      });
    }
    const promotedSlot = helpers.useInstanceState("promoted", false);
    if (position) {
      if (promotedSlot.get()) root.setAttribute("popover", "manual");
      deferToPaint(() => {
        if (!root.isConnected) return;
        promotedSlot.set(promoteToast(root));
      });
    }
    const liveToast = (origin) => {
      if (origin) {
        const live = origin.closest(".rui-toast");
        if (live) return live;
      }
      if (root.isConnected) return root;
      const tracked = liveNodeSlot.get();
      return tracked && tracked.isConnected ? tracked : null;
    };
    const cancelTimer = () => {
      const handle = timerSlot.get();
      if (handle !== null) {
        clearTimeout(handle);
        timerSlot.set(null);
      }
    };
    const dismiss = (origin) => {
      if (isDismissed()) return;
      dismissedSlot.set(identity);
      cancelTimer();
      const target = liveToast(origin);
      if (target) {
        target.classList.add("is-dismissed");
        const handle = setTimeout(() => {
          removalTimerSlot.set(null);
          target.remove();
        }, 180);
        removalTimerSlot.set(handle);
        helpers.registerDisposer(() => clearTimeout(handle), "exit-animation-timer");
      }
      helpers.invoke(props.onClose);
    };
    const duration = asNumber(props.duration, 0);
    const armAutoDismiss = (ms) => {
      if (ms <= 0 || isDismissed()) return;
      const handle = setTimeout(() => {
        timerSlot.set(null);
        dismiss();
      }, ms);
      timerSlot.set(handle);
      deadlineSlot.set(Date.now() + ms);
      helpers.registerDisposer(() => clearTimeout(handle), "auto-dismiss-timer");
    };
    const closeBtn = el("button", {
      type: "button",
      class: "rui-toast-close",
      "aria-label": "Dismiss notification"
    }, ["×"]);
    closeBtn.onclick = (event) => {
      event.stopPropagation();
      dismiss(event.currentTarget ?? event.target);
    };
    root.append(closeBtn);
    if (asBoolean(props.pauseOnHover) && duration > 0) {
      root.onpointerenter = () => {
        const deadline = deadlineSlot.get();
        if (timerSlot.get() === null) return;
        remainingSlot.set(Math.max(0, deadline - Date.now()));
        cancelTimer();
      };
      root.onpointerleave = () => {
        if (isDismissed() || timerSlot.get() !== null) return;
        armAutoDismiss(remainingSlot.get() || duration);
      };
    }
    if (duration > 0 && timerSlot.get() === null && !isDismissed()) {
      armAutoDismiss(duration);
    }
    if (isDismissed()) {
      const placeholder = el("div", { class: "rui-toast-placeholder", hidden: "" });
      return placeholder;
    }
    return root;
  }
};
function toastAutoKey(identity) {
  let h = 0;
  for (let i = 0; i < identity.length; i += 1) h = h * 31 + identity.charCodeAt(i) >>> 0;
  return `toast-${h.toString(36)}`;
}
const Toasts = {
  name: "Toasts",
  description: "Stacked container for transient `Toast` notifications, pinned to a viewport corner. Usually unnecessary: `$toast.success(...)` (and `.show/.error/.info/.warning`) auto-render their own stack. Reach for `Toasts` only for custom placement — render the reactive `$toast.items` list into it and the auto-layer steps aside: `Toasts($toast.items.map(t => Toast({ title: t.message, tone: t.tone, onClose: () => $toast.dismiss(t.id) })))` — add `key: t.id` there when the list can reorder. `max` caps how many are shown at once (the rest are summarised as `+N more`). Use a standalone `Toast` with `position` for a single one-off notice.",
  props: [
    { name: "children", aliases: ["child"], type: "Node[]", description: "Toast components to stack" },
    { name: "position", type: "string", optional: true, enum: TOASTS_POSITIONS, description: 'Viewport corner the stack pins to (default "top-right")' },
    { name: "max", type: "number", optional: true, aliases: ["limit"], description: "Maximum toasts shown at once; the overflow is summarised as `+N more`" }
  ],
  render: (_node, props, helpers) => {
    const root = el("div", {
      class: "rui-toasts",
      "data-position": asString(props.position, "top-right"),
      // The stack itself is chrome, not content — individual toasts carry
      // their own `status`/`alert` roles for screen readers.
      "aria-live": "off"
    });
    const children = asArray(props.children);
    const max = props.max != null ? Math.max(1, Math.floor(asNumber(props.max, 0))) : null;
    const shown = max !== null && children.length > max ? children.slice(children.length - max) : children;
    const hidden = children.length - shown.length;
    for (const child of shown) root.append(helpers.renderNode(child));
    if (hidden > 0) {
      root.append(el("div", { class: "rui-toasts-overflow", role: "status" }, [`+${hidden} more`]));
    }
    return root;
  }
};
function defaultToastIcon(tone) {
  switch (tone) {
    case "success":
      return "circle-check";
    case "warning":
      return "triangle-exclamation";
    case "danger":
      return "circle-xmark";
    case "primary":
      return "bell";
    case "info":
      return "circle-info";
    default:
      return "circle-info";
  }
}
function routeHash(to) {
  return `#${to.startsWith("/") ? to : `/${to}`}`;
}
const DEFAULT_HOME_ICON = "house";
function slugify(label) {
  return label.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "");
}
function derivedPath(labels, index) {
  const segments = [];
  for (let i = 0; i <= index; i += 1) {
    const slug = slugify(labels[i] ?? "");
    if (!slug) return "";
    segments.push(slug);
  }
  return `/${segments.join("/")}`;
}
function asCrumbRecord(value) {
  if (!value || typeof value !== "object" || Array.isArray(value)) return null;
  if (value.__kind === "Component") return null;
  return value;
}
function prependCrumbIcon(crumbEl, icon) {
  if (crumbEl.querySelector(".rui-breadcrumb-icon")) return;
  const host = crumbEl.querySelector(
    ".rui-breadcrumb-link, .rui-breadcrumb-current, .rui-breadcrumb-text"
  );
  const iconNode = renderIcon(icon, { className: "rui-breadcrumb-icon" });
  if (!host || !iconNode) return;
  host.insertBefore(iconNode, host.firstChild);
}
function isPlainClick(event) {
  const evt = event;
  if (typeof evt.button === "number" && evt.button !== 0) return false;
  return !(evt.metaKey || evt.ctrlKey || evt.shiftKey || evt.altKey);
}
function renderCrumb(opts, helpers) {
  const root = el("li", { class: "rui-breadcrumb-item" });
  const inner = [];
  const iconNode = renderIcon(opts.icon, { className: "rui-breadcrumb-icon" });
  if (iconNode) inner.push(iconNode);
  inner.push(el("span", { class: "rui-breadcrumb-label" }, [opts.label]));
  if (opts.current) {
    root.setAttribute("aria-current", "page");
    root.append(el("span", { class: "rui-breadcrumb-current" }, inner));
    return root;
  }
  const to = opts.to ?? "";
  const href = opts.href ?? "";
  const onClick = typeof opts.onClick === "function" ? opts.onClick : null;
  if (to || href) {
    const anchor = el("a", {
      class: "rui-breadcrumb-link",
      href: to ? routeHash(to) : href
    }, inner);
    anchor.onclick = (event) => {
      if (event.defaultPrevented) return;
      if (!isPlainClick(event)) return;
      if (onClick) helpers.invoke(onClick);
      if (to) {
        event.preventDefault();
        helpers.router.navigate(to);
      } else if (href === "#") {
        event.preventDefault();
      }
    };
    root.append(anchor);
    return root;
  }
  if (onClick) {
    const btn = el("button", {
      type: "button",
      class: "rui-breadcrumb-link rui-breadcrumb-button"
    }, inner);
    btn.onclick = () => helpers.invoke(onClick);
    root.append(btn);
    return root;
  }
  root.append(el("span", { class: "rui-breadcrumb-text" }, inner));
  return root;
}
const BreadcrumbItem = {
  name: "BreadcrumbItem",
  description: "Single item inside a Breadcrumb trail. Pass `to` for an in-app route (navigated through the built-in router), `href` for an external URL, or `onClick` to handle the click yourself. Omit all three for the current/leaf page (rendered with emphasis).",
  props: [
    { name: "label", type: "string" },
    { name: "href", type: "string", optional: true, description: "External URL — use `to` for in-app routes" },
    { name: "icon", type: "string", optional: true, description: "Optional Font Awesome icon name" },
    { name: "to", type: "string", optional: true, description: 'In-app route, e.g. "/projects" — navigated through the router without reloading the app' },
    { name: "onClick", type: "callable", optional: true, aliases: ["onclick"], description: "Callable fired on click — use instead of `to`/`href` to pop a step without changing the URL" }
  ],
  render: (_node, props, helpers) => {
    const rawHref = asString(props.href);
    const to = asString(props.to);
    return renderCrumb({
      label: asString(props.label),
      icon: props.icon,
      to,
      href: rawHref ? sanitiseHref(rawHref) : "",
      onClick: props.onClick,
      // No target of any kind = the leaf page the user is on.
      current: !rawHref && !to && typeof props.onClick !== "function"
    }, helpers);
  }
};
const Breadcrumb = {
  name: "Breadcrumb",
  description: 'Trail showing the user\'s location. Every crumb except the last one is a link that navigates. Items may be BreadcrumbItem(label, { to?, href? }) nodes, `{ label, to }` / `{ label, href }` objects, or plain strings — a string trail derives its own cumulative route from the labels (`["Workspace", "Reports", "Q3"]` → `/workspace`, `/workspace/reports`), which `autoLink: false` turns off. The first crumb carries a home icon unless `homeIcon: false`; pass an icon name to change it. `maxItems` collapses the middle of a long trail behind an ellipsis.',
  props: [
    { name: "items", type: "BreadcrumbItem[] | string[] | {label, to}[]" },
    { name: "separator", type: "string", optional: true, description: "Default `/`" },
    { name: "maxItems", type: "number", optional: true, description: "Collapse the middle of the trail to an ellipsis once there are more items than this (keeps the first crumb and the tail)" },
    { name: "onItemClick", type: "callable", optional: true, description: "Called with (index, label) when a crumb is clicked — fires alongside any navigation" },
    { name: "homeIcon", type: "boolean | string", optional: true, description: "Leading icon on the FIRST crumb — `true` (default) uses `house`, `false` removes it, a string picks another Font Awesome name" },
    { name: "autoLink", type: "boolean", optional: true, description: "Derive a cumulative route from plain-string labels so they navigate (default `true`). Set `false` for a trail that is pure text unless an item names its own `to`/`href`" }
  ],
  render: (_node, props, helpers) => {
    const items = asArray(props.items);
    const separator = asString(props.separator, "/");
    const maxItems = Math.max(0, Math.floor(asNumber(props.maxItems, 0)));
    const onItemClick = typeof props.onItemClick === "function" ? props.onItemClick : null;
    const autoLink = asBoolean(props.autoLink, true);
    const homeIconProp = props.homeIcon;
    const homeIconName = homeIconProp === false || homeIconProp === "false" ? "" : typeof homeIconProp === "string" && homeIconProp.trim() !== "" ? homeIconProp.trim() : DEFAULT_HOME_ICON;
    const labels = items.map((item) => {
      const record = asCrumbRecord(item);
      if (record) return asString(record.label ?? record.title);
      return asString(item);
    });
    const root = el("nav", { class: "rui-breadcrumb", "aria-label": "Breadcrumb" });
    const list = el("ol", { class: "rui-breadcrumb-list" });
    const shown = [];
    if (maxItems > 0 && items.length > maxItems) {
      const lead = maxItems > 1 ? 1 : 0;
      for (let i = 0; i < lead; i += 1) shown.push(i);
      shown.push(null);
      for (let i = items.length - (maxItems - lead); i < items.length; i += 1) shown.push(i);
    } else {
      items.forEach((_item, i) => shown.push(i));
    }
    shown.forEach((index, position) => {
      if (position > 0) {
        list.append(el("li", { class: "rui-breadcrumb-separator", "aria-hidden": "true" }, [separator]));
      }
      if (index === null) {
        list.append(el("li", { class: "rui-breadcrumb-item rui-breadcrumb-ellipsis" }, [
          el("span", { "aria-hidden": "true" }, ["…"]),
          el("span", { class: "rui-visually-hidden" }, ["collapsed items"])
        ]));
        return;
      }
      const item = items[index];
      const isLast = index === items.length - 1;
      const isFirst = index === 0;
      if (item && typeof item === "object" && item.__kind === "Component") {
        const rendered = helpers.renderNode(item);
        if (isFirst && homeIconName && rendered instanceof HTMLElement) {
          prependCrumbIcon(rendered, homeIconName);
        }
        list.append(rendered);
        return;
      }
      const record = asCrumbRecord(item);
      const label = labels[index] ?? "";
      const rawHref = record ? asString(record.href) : "";
      const explicitTo = record ? asString(record.to ?? record.path) : "";
      const to = explicitTo || (!rawHref && autoLink && !isLast ? derivedPath(labels, index) : "");
      const icon = record && record.icon != null ? record.icon : isFirst && homeIconName ? homeIconName : void 0;
      list.append(renderCrumb({
        label,
        icon,
        current: isLast,
        to: isLast ? "" : to,
        // `sanitiseHref` neutralises a hostile `javascript:` URL coming from an
        // LLM-authored item; `to` never needs it (it is always a `#/route`).
        href: !isLast && rawHref ? sanitiseHref(rawHref) : "",
        onClick: !isLast && onItemClick ? () => helpers.invoke(onItemClick, index, label) : void 0
      }, helpers));
    });
    root.append(list);
    return root;
  }
};
const PER_PAGE_OPTIONS = [10, 20, 50, 100];
const Pagination = {
  name: "Pagination",
  description: 'Page navigator with Prev/Next, page numbers, and ellipses. Pass a `$variable` as `page` for two-way binding — clicking a page button sets that state to the new (1-indexed) value. Add `total` to render a "Showing N–M of T" summary, pass `$variable` as `perPage` to expose a per-page selector, or set `compact: true` to drop the page-number row for tight toolbars. For server-side paging pass `onChange` — it receives the new page number.',
  props: [
    { name: "page", type: "number", description: "Current page (1-indexed); typically a $variable" },
    { name: "totalPages", type: "number", aliases: ["pages"] },
    { name: "siblings", type: "number", optional: true, description: "Number of page links shown around the current page (default 1, max 6)" },
    { name: "total", type: "number", optional: true, description: 'Total record count — enables the "Showing N–M of T" summary' },
    { name: "perPage", type: "number", optional: true, description: "Bind a `$variable` to expose a per-page selector" },
    { name: "perPageOptions", type: "number[]", optional: true, description: "Override the per-page choices (default 10/20/50/100)" },
    { name: "compact", type: "boolean", optional: true, description: "Hide page numbers — keep Prev / Next only" },
    { name: "onChange", type: "callable", optional: true, aliases: ["onPageChange"], description: "Called with the new (1-indexed) page — required when `page` is not a bare `$variable`" },
    { name: "disabled", type: "boolean", optional: true, description: "Disable the whole pager (e.g. while a page fetch is in flight)" },
    { name: "onPerPageChange", type: "callable", optional: true, description: "Called with the new page size when the per-page selector changes" }
  ],
  render: (node, props, helpers) => {
    const total = Math.max(1, Math.floor(asNumber(props.totalPages, 1)));
    const current = Math.max(1, Math.min(total, Math.floor(asNumber(props.page, 1))));
    const siblings = Math.min(6, Math.max(0, Math.floor(asNumber(props.siblings, 1))));
    const compact = asBoolean(props.compact);
    const stateName = node.argMeta?.[0]?.stateRef;
    const onChange = typeof props.onChange === "function" ? props.onChange : null;
    const inert = !stateName && !onChange;
    const controlsDisabled = asBoolean(props.disabled);
    const allDisabled = controlsDisabled || inert;
    const root = el("nav", {
      class: "rui-pagination",
      "aria-label": "Pagination",
      "data-compact": compact ? "true" : "false",
      "data-disabled": allDisabled ? "true" : null
    });
    const setPage = (next) => {
      const clamped = Math.max(1, Math.min(total, next));
      if (clamped === current) return;
      if (stateName) helpers.setState(stateName, clamped);
      helpers.invoke(onChange, clamped);
    };
    const totalRecords = props.total != null ? Math.max(0, Math.floor(asNumber(props.total, 0))) : null;
    const perPageValue = props.perPage != null ? Math.max(1, Math.floor(asNumber(props.perPage, 0))) : null;
    if (totalRecords !== null && perPageValue && perPageValue > 0) {
      const start = totalRecords === 0 ? 0 : (current - 1) * perPageValue + 1;
      const end = Math.min(totalRecords, current * perPageValue);
      root.append(el("span", { class: "rui-pagination-summary" }, [
        totalRecords === 0 ? "No results" : `Showing ${start.toLocaleString()}–${end.toLocaleString()} of ${totalRecords.toLocaleString()}`
      ]));
    } else if (totalRecords !== null) {
      root.append(el("span", { class: "rui-pagination-summary" }, [
        `${totalRecords.toLocaleString()} result${totalRecords === 1 ? "" : "s"}`
      ]));
    }
    const buttonsWrap = el("div", { class: "rui-pagination-buttons" });
    const button = (label, target, opts = {}) => {
      if (opts.ellipsis) {
        return el("span", { class: "rui-pagination-ellipsis", "aria-hidden": "true" }, [label]);
      }
      const isDisabled = allDisabled || opts.disabled === true;
      const btn = el("button", {
        type: "button",
        class: "rui-pagination-button",
        "data-active": opts.active ? "true" : "false",
        "aria-current": opts.active ? "page" : null,
        "aria-label": opts.ariaLabel ?? null,
        disabled: isDisabled ? "" : null
      }, [label]);
      if (!isDisabled && !opts.active) {
        btn.onclick = () => setPage(target);
      }
      return btn;
    };
    buttonsWrap.append(button("‹", current - 1, { disabled: current <= 1, ariaLabel: "Previous page" }));
    if (!compact) {
      const pageNumbers = computePageNumbers(current, total, siblings);
      for (const entry of pageNumbers) {
        if (entry === "…") {
          buttonsWrap.append(button("…", 0, { ellipsis: true }));
        } else {
          buttonsWrap.append(button(String(entry), entry, { active: entry === current }));
        }
      }
    } else {
      buttonsWrap.append(el("span", { class: "rui-pagination-current" }, [`${current} / ${total}`]));
    }
    buttonsWrap.append(button("›", current + 1, { disabled: current >= total, ariaLabel: "Next page" }));
    root.append(buttonsWrap);
    const perPageState = node.argMeta?.[4]?.stateRef;
    const onPerPageChange = typeof props.onPerPageChange === "function" ? props.onPerPageChange : null;
    if (perPageValue && (perPageState || onPerPageChange || asArray(props.perPageOptions).length > 0)) {
      const perPageWrap = el("label", { class: "rui-pagination-per-page" }, [
        document.createTextNode("Show ")
      ]);
      if (perPageState || onPerPageChange) {
        const options = asArray(props.perPageOptions).length > 0 ? asArray(props.perPageOptions).map((v) => Math.max(1, Math.floor(Number(v) || 0))).filter((n) => n > 0) : Array.from(PER_PAGE_OPTIONS);
        const select = el("select", {
          class: "rui-pagination-per-page-select",
          disabled: controlsDisabled ? "" : null
        });
        for (const opt of options) {
          const optEl = el("option", {
            value: String(opt),
            selected: opt === perPageValue ? "" : null
          }, [String(opt)]);
          select.append(optEl);
        }
        if (perPageState) {
          helpers.bindState(select, perPageState, {
            event: "change",
            getValue: (n) => Number(n.value)
          });
        }
        if (onPerPageChange) {
          const bound = select.onchange;
          select.onchange = (event) => {
            bound?.call(select, event);
            const live = event.currentTarget ?? event.target;
            const size2 = Number(live?.value);
            if (Number.isFinite(size2) && size2 > 0) helpers.invoke(onPerPageChange, size2);
          };
        }
        perPageWrap.append(select);
      } else {
        perPageWrap.append(el("span", { class: "rui-pagination-per-page-value" }, [String(perPageValue)]));
      }
      perPageWrap.append(document.createTextNode(" per page"));
      root.append(perPageWrap);
    }
    return root;
  }
};
function computePageNumbers(current, total, siblings) {
  const pages = [];
  const range = (from, to) => {
    for (let i = from; i <= to; i += 1) pages.push(i);
  };
  const totalNumbers = siblings * 2 + 5;
  if (total <= totalNumbers) {
    range(1, total);
    return pages;
  }
  const leftSibling = Math.max(2, current - siblings);
  const rightSibling = Math.min(total - 1, current + siblings);
  pages.push(1);
  if (leftSibling > 2) pages.push("…");
  range(leftSibling, rightSibling);
  if (rightSibling < total - 1) pages.push("…");
  pages.push(total);
  return pages;
}
const NavbarItem = {
  name: "NavbarItem",
  description: "Single link inside a Navbar's main item slot. Renders as an inline anchor / button — pass `to` for a router-aware link, `href` for an external link, or `onClick` for a click handler (`onClick` also runs alongside `to`/`href`, e.g. to log the navigation). `active=true` highlights the current page; `disabled=true` greys it out and makes it unclickable.",
  props: [
    { name: "label", type: "string" },
    { name: "to", type: "string", optional: true, description: "Internal route (consumes the built-in router)" },
    { name: "href", type: "string", optional: true, description: "External href; opens in a new tab when set with `external=true`" },
    { name: "icon", type: "string", optional: true },
    { name: "active", type: "boolean", optional: true },
    { name: "onClick", type: "callable", optional: true, aliases: ["action", "onclick"], description: "Callable fired on click — runs alongside `to`/`href` navigation, not instead of it" },
    { name: "external", type: "boolean", optional: true },
    { name: "disabled", type: "boolean", optional: true, description: "Grey out and de-activate the entry (gated feature / missing permission)" }
  ],
  render: (_node, props, helpers) => {
    const label = asString(props.label);
    const icon = props.icon;
    const active = asBoolean(props.active);
    const to = asString(props.to);
    const safeHref = sanitiseHref(props.href, "");
    const external = asBoolean(props.external);
    const disabled = asBoolean(props.disabled);
    const onClick = typeof props.onClick === "function" ? props.onClick : null;
    const tagName = to || safeHref ? "a" : "button";
    const root = el(tagName, {
      class: "rui-navbar-item",
      type: tagName === "button" ? "button" : null,
      // A disabled entry keeps its shape but loses the href entirely, so it is
      // neither clickable nor keyboard-focusable — a greyed-out link the user
      // can still Tab into and open is worse than no gating at all.
      href: disabled ? null : safeHref || (to ? routeHash(to) : null),
      target: !disabled && external && safeHref ? "_blank" : null,
      rel: !disabled && external && safeHref ? "noopener noreferrer" : null,
      disabled: disabled && tagName === "button" ? "" : null,
      "aria-disabled": disabled ? "true" : null,
      "data-disabled": disabled ? "true" : null,
      "data-active": active ? "true" : "false"
    });
    const iconNode = renderIcon(icon, { className: "rui-navbar-item-icon" });
    if (iconNode) root.append(iconNode);
    root.append(el("span", { class: "rui-navbar-item-label" }, [label]));
    if (!disabled && (onClick || to && !safeHref)) {
      root.onclick = (event) => {
        if (event.defaultPrevented) return;
        if ((safeHref || to) && !isPlainClick(event)) return;
        if (onClick) helpers.invoke(onClick);
        if (to && !safeHref) {
          event.preventDefault();
          helpers.router.navigate(to);
        } else if (!safeHref) {
          event.preventDefault();
        }
      };
    }
    return root;
  }
};
let navbarIdSeq = 0;
function setNavbarMenuOpen(origin, next, openSlot) {
  openSlot.set(next);
  const live = origin.closest(".rui-navbar");
  if (!live) return;
  live.setAttribute("data-menu-open", next ? "true" : "false");
  live.querySelector(".rui-navbar-burger")?.setAttribute("aria-expanded", next ? "true" : "false");
}
const Navbar = {
  name: "Navbar",
  description: "Top navigation bar with a brand on the left, primary nav items in the middle, and a right-aligned actions slot (user avatar, DropdownMenu, CTA buttons, …). Use `sticky=true` to pin it to the top of the page and `collapsible=true` to fold the item row behind a burger toggle on narrow viewports. The canonical companion of `Sidebar` for product surfaces; prefer Navbar for marketing/docs pages without a sidebar.",
  props: [
    { name: "brand", type: "string | Node", optional: true, description: "Workspace/product name (string) or a node (e.g. logo Image)" },
    // `links` is accepted too — see the matching note on marketing NavBar.
    { name: "items", type: "NavbarItem[]", optional: true, aliases: ["links"], description: "Center navigation items" },
    { name: "actions", type: "Node[]", optional: true, description: "Right-side controls (Buttons, Avatar, DropdownMenu, …)" },
    { name: "sticky", type: "boolean", optional: true, description: "Pin the bar to the top of the viewport" },
    { name: "variant", aliases: ["tone"], type: "string", optional: true, enum: ["default", "transparent"], description: "Visual variant" },
    { name: "collapsible", type: "boolean", optional: true, description: "Collapse the item row behind a burger toggle below 640px (recommended with more than ~4 items, and with `sticky`)" }
  ],
  render: (_node, props, helpers) => {
    const collapsible = asBoolean(props.collapsible);
    const openSlot = helpers.useInstanceState("rui-navbar-menu-open", false);
    const menuOpen = collapsible && openSlot.get();
    const root = el("nav", {
      class: "rui-navbar",
      "data-sticky": asBoolean(props.sticky) ? "true" : "false",
      "data-variant": asString(props.variant, "default"),
      "data-collapsible": collapsible ? "true" : null,
      "data-menu-open": collapsible ? menuOpen ? "true" : "false" : null,
      "aria-label": "Primary"
    });
    const brand = props.brand;
    if (brand !== void 0 && brand !== null && brand !== "") {
      const brandWrap = el("div", { class: "rui-navbar-brand" });
      if (typeof brand === "string") {
        brandWrap.append(document.createTextNode(brand));
      } else {
        brandWrap.append(helpers.renderNode(brand));
      }
      root.append(brandWrap);
    }
    let itemsId = "";
    if (collapsible) {
      const idSlot = helpers.useInstanceState("rui-navbar-items-id", "");
      if (!idSlot.get()) idSlot.set(`rui-navbar-items-${navbarIdSeq += 1}`);
      itemsId = idSlot.get();
    }
    const items = asArray(props.items);
    if (items.length > 0) {
      const list = el("div", { class: "rui-navbar-items", id: itemsId || null });
      for (const item of items) list.append(helpers.renderNode(item));
      if (collapsible) {
        list.onclick = (event) => {
          const origin = event.target?.closest(".rui-navbar-item");
          if (origin) setNavbarMenuOpen(origin, false, openSlot);
        };
      }
      root.append(list);
    }
    const actions = asArray(props.actions);
    if (actions.length > 0) {
      const right = el("div", { class: "rui-navbar-actions" });
      for (const item of actions) right.append(helpers.renderNode(item));
      root.append(right);
    }
    if (collapsible && items.length > 0) {
      const burger = el("button", {
        type: "button",
        class: "rui-navbar-burger",
        "aria-label": "Toggle navigation",
        "aria-expanded": menuOpen ? "true" : "false",
        "aria-controls": itemsId || null
      });
      const burgerIcon = renderIcon("bars", { className: "rui-navbar-burger-icon" });
      if (burgerIcon) burger.append(burgerIcon);
      else burger.textContent = "≡";
      burger.onclick = (event) => {
        const origin = event.currentTarget ?? event.target;
        if (origin) setNavbarMenuOpen(origin, !openSlot.get(), openSlot);
      };
      root.append(burger);
    }
    return root;
  }
};
const MENU_SIDES = ["bottom", "top", "left", "right"];
const MENU_ALIGNS = ["start", "center", "end"];
const MENU_VARIANTS = ["default", "danger"];
const MENU_ITEM_ROLES = ["menuitem", "menuitemcheckbox", "menuitemradio"];
const TRIGGER_FOCUSABLE = "button:not([disabled]), a[href], input:not([disabled]), select:not([disabled]), textarea:not([disabled]), [tabindex]:not([tabindex='-1'])";
function applyOpenAttrs(liveRoot, open) {
  liveRoot.setAttribute("data-open", open ? "true" : "false");
  const trigger = liveRoot.querySelector(".rui-dropdown-menu-trigger");
  if (!trigger) return;
  trigger.setAttribute("data-state", open ? "open" : "closed");
  trigger.setAttribute("aria-expanded", open ? "true" : "false");
  trigger.querySelector('[aria-haspopup="menu"]')?.setAttribute("aria-expanded", open ? "true" : "false");
}
function dropdownRootOf(origin) {
  const direct = origin.closest(".rui-dropdown-menu");
  if (direct) return direct;
  const owner = origin.closest(".rui-dropdown-menu-content")?.getAttribute("data-menu-root");
  if (!owner) return null;
  const scope = origin.getRootNode();
  return scope.querySelector(`.rui-dropdown-menu[data-menu-id="${owner}"]`);
}
function menuPanelOf(liveRoot) {
  const inside = liveRoot.querySelector(".rui-dropdown-menu-content");
  if (inside) return inside;
  const id = liveRoot.getAttribute("data-menu-id");
  if (!id) return null;
  const scope = liveRoot.getRootNode();
  return scope.querySelector(`.rui-dropdown-menu-content[data-menu-root="${id}"]`);
}
function menuItemsOf(liveRoot) {
  return Array.from(menuPanelOf(liveRoot)?.querySelectorAll(".rui-menu-item") ?? []);
}
const itemDisabled = (item) => item.getAttribute("aria-disabled") === "true";
function triggerTabStop(liveRoot) {
  const wrap = liveRoot.querySelector(".rui-dropdown-menu-trigger");
  if (!wrap) return null;
  return wrap.querySelector(TRIGGER_FOCUSABLE) ?? wrap;
}
const PAGE_DISMISS = /* @__PURE__ */ new WeakMap();
function installPageDismiss(liveRoot, close) {
  PAGE_DISMISS.get(liveRoot)?.();
  const doc = liveRoot.ownerDocument;
  const panel = menuPanelOf(liveRoot);
  function onOutside(event) {
    const path = typeof event.composedPath === "function" ? event.composedPath() : [];
    if (path.includes(liveRoot)) return;
    if (panel && path.includes(panel)) return;
    dispose();
    close();
  }
  function dispose() {
    doc.removeEventListener("click", onOutside, true);
    if (PAGE_DISMISS.get(liveRoot) === dispose) PAGE_DISMISS.delete(liveRoot);
  }
  PAGE_DISMISS.set(liveRoot, dispose);
  setTimeout(() => {
    if (PAGE_DISMISS.get(liveRoot) !== dispose) return;
    doc.addEventListener("click", onOutside, true);
  }, 0);
}
function releaseDismiss(liveRoot) {
  disposeDismissListeners(liveRoot);
  PAGE_DISMISS.get(liveRoot)?.();
}
function installDismiss(liveRoot, ctl) {
  const close = () => {
    ctl.set(false);
    applyOpenAttrs(liveRoot, false);
    closeFloating(menuPanelOf(liveRoot));
    releaseDismiss(liveRoot);
  };
  installDismissListeners({ liveRoot, onDismiss: close });
  installPageDismiss(liveRoot, close);
}
const setDropdownOpen = (origin, next, ctl) => {
  ctl.set(next);
  const liveRoot = dropdownRootOf(origin);
  if (!liveRoot) return null;
  applyOpenAttrs(liveRoot, next);
  syncFloatingLayer(liveRoot, next);
  if (!next) releaseDismiss(liveRoot);
  return liveRoot;
};
function syncFloatingLayer(liveRoot, open) {
  if (!open) {
    closeFloating(menuPanelOf(liveRoot));
    return;
  }
  syncFloatingPanel(
    liveRoot,
    true,
    ".rui-dropdown-menu-content",
    ".rui-dropdown-menu-trigger",
    { layer: "dropdown" }
  );
}
const isMenuChild = (item, name) => {
  if (!item || typeof item !== "object") return false;
  const node = item;
  return node.__kind === "Component" && node.name === name;
};
function menuItemView(source) {
  const role = asString(source.role, "menuitem");
  return {
    label: asString(source.label),
    icon: source.icon,
    shortcut: asString(source.shortcut),
    variant: asString(source.variant, "default"),
    disabled: asBoolean(source.disabled),
    checked: source.checked === void 0 || source.checked === null ? null : asBoolean(source.checked),
    role: MENU_ITEM_ROLES.includes(role) ? role : "menuitem",
    keepOpen: asBoolean(source.keepOpen)
  };
}
function menuItemButton(view) {
  const role = view.role !== "menuitem" ? view.role : view.checked !== null ? "menuitemcheckbox" : "menuitem";
  const checkable = role !== "menuitem";
  const btn = el("button", {
    type: "button",
    class: "rui-menu-item",
    role,
    "data-variant": view.variant,
    // `aria-disabled` rather than the native attribute, which a WAI-ARIA menu
    // must not use: `disabled` takes the row out of the tab order and refuses
    // focus, so a keyboard / screen-reader user never learns that "Delete —
    // requires admin" exists at all. `aria-disabled` keeps it reachable and
    // announces it as present-but-unavailable; every activation path below
    // checks the flag, so the row still does nothing when clicked or Entered.
    "aria-disabled": view.disabled ? "true" : null,
    "data-disabled": view.disabled ? "true" : null,
    "aria-checked": checkable ? view.checked === true ? "true" : "false" : null,
    "data-checked": view.checked === true ? "true" : null,
    // Read back by DropdownMenu: a toggle row must survive its own activation so
    // the user can flip several settings in a row.
    "data-keep-open": view.keepOpen ? "true" : null
  });
  if (checkable) {
    btn.append(el("span", {
      class: "rui-menu-item-check",
      "aria-hidden": "true",
      // Sized inline (mirroring `.rui-menu-item-icon`) so the labels of a mixed
      // checked/unchecked group line up without depending on new CSS.
      style: "width:14px;display:inline-flex;justify-content:center"
    }, [view.checked === true ? "✓" : ""]));
  }
  const iconNode = renderIcon(view.icon, { className: "rui-menu-item-icon" });
  if (iconNode) btn.append(iconNode);
  btn.append(el("span", { class: "rui-menu-item-label" }, [view.label]));
  if (view.shortcut) btn.append(el("span", { class: "rui-menu-item-shortcut" }, [view.shortcut]));
  return btn;
}
function objectMenuItem(raw) {
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) return null;
  const r = raw;
  if (r.__kind !== void 0) return null;
  if (r.separator === true || asString(r.type) === "separator") return "separator";
  const label = asString(r.label ?? r.title ?? r.text);
  if (!label) return null;
  return {
    view: menuItemView({ ...r, label }),
    action: r.onClick ?? r.action
  };
}
function wireMenuItem(rendered, ctl) {
  if (!(rendered instanceof HTMLElement)) return;
  if (!rendered.classList.contains("rui-menu-item")) return;
  const inner = rendered.onclick;
  const keepOpen = rendered.getAttribute("data-keep-open") === "true";
  rendered.onclick = (event) => {
    const origin = event.currentTarget ?? event.target;
    if (!origin) return;
    const item = origin.closest(".rui-menu-item");
    if (!item || item.getAttribute("aria-disabled") === "true") return;
    if (!keepOpen) setDropdownOpen(item, false, ctl);
    inner?.call(item, event);
  };
}
function flattenItems(raw, depth = 4) {
  const out = [];
  for (const entry of asArray(raw)) {
    if (Array.isArray(entry) && depth > 0) out.push(...flattenItems(entry, depth - 1));
    else out.push(entry);
  }
  return out;
}
let menuIdSeq = 0;
const DropdownMenu = {
  name: "DropdownMenu",
  description: "Click-triggered dropdown menu. Click the trigger to toggle, click a MenuItem to run its action and close, click outside or press Escape to close without acting; ArrowUp/ArrowDown/Home/End and typeahead move between items. Items may be MenuItem / MenuSeparator / MenuLabel nodes, nested arrays of them, or plain `{label, onClick, icon?, shortcut?, disabled?, checked?, separator?}` objects. Bind a `$variable` to `open` for two-way control, or watch `onOpenChange(isOpen)`.",
  props: [
    { name: "trigger", type: "Node", description: "Clickable trigger element (typically a Button or Avatar)" },
    { name: "items", type: "(MenuItem | MenuSeparator | MenuLabel)[]" },
    { name: "side", type: "string", optional: true, enum: MENU_SIDES, description: 'Where the menu opens relative to the trigger (default "bottom")' },
    { name: "align", type: "string", optional: true, enum: MENU_ALIGNS, description: 'How the menu aligns along the trigger edge (default "start")' },
    { name: "label", type: "string", optional: true, description: "Optional ARIA label for the menu" },
    { name: "open", type: "boolean", optional: true, description: "Open state — bind a `$variable` for two-way control (an action can then close the menu); a plain `true` pre-opens it" },
    { name: "onOpenChange", type: "callable", optional: true, description: "(isOpen) => … fired whenever the menu opens or closes (lazy-load contents, keep a row highlighted, log usage)" },
    { name: "disabled", type: "boolean", optional: true, description: "Disable the trigger entirely (read-only or in-flight record)" }
  ],
  render: (node, props, helpers) => {
    const disabled = asBoolean(props.disabled);
    const openRef = node.argMeta?.[5]?.stateRef;
    const propOpen = asBoolean(props.open);
    const openSlot = helpers.useInstanceState("rui-dropdown-open", null);
    const stored = openSlot.get();
    const followProp = !stored || stored.prop !== propOpen;
    if (followProp) openSlot.set({ prop: propOpen, value: propOpen });
    const isOpen = !disabled && (openRef || followProp ? propOpen : stored.value);
    const ctl = {
      get: () => openSlot.get()?.value ?? propOpen,
      set: (next) => {
        const current = openSlot.get();
        if (current && current.value === next) return;
        openSlot.set({ prop: propOpen, value: next });
        if (openRef) helpers.setState(openRef, next);
        helpers.invoke(props.onOpenChange, next);
      }
    };
    const idSlot = helpers.useInstanceState("rui-dropdown-id", "");
    if (!idSlot.get()) idSlot.set(`rui-menu-${menuIdSeq += 1}`);
    const menuId = idSlot.get();
    const root = el("div", {
      class: "rui-dropdown-menu",
      "data-menu-id": menuId,
      "data-open": isOpen ? "true" : "false",
      "data-side": asString(props.side, "bottom"),
      "data-align": asString(props.align, "start"),
      "data-disabled": disabled ? "true" : null
    });
    const triggerWrap = el("span", {
      class: "rui-dropdown-menu-trigger",
      "data-state": isOpen ? "open" : "closed",
      "aria-expanded": isOpen ? "true" : "false",
      "aria-disabled": disabled ? "true" : null
    });
    triggerWrap.append(helpers.renderNode(props.trigger));
    const innerControl = triggerWrap.querySelector(TRIGGER_FOCUSABLE);
    if (innerControl) {
      innerControl.setAttribute("aria-haspopup", "menu");
      innerControl.setAttribute("aria-expanded", isOpen ? "true" : "false");
      if (disabled) innerControl.setAttribute("aria-disabled", "true");
    } else {
      triggerWrap.setAttribute("role", "button");
      triggerWrap.setAttribute("aria-haspopup", "menu");
      if (!disabled) triggerWrap.setAttribute("tabindex", "0");
    }
    const toggleFrom = (origin, next, moveFocus) => {
      const liveRoot = setDropdownOpen(origin, next, ctl);
      if (!liveRoot) return;
      if (!next) return;
      installDismiss(liveRoot, ctl);
      if (!moveFocus) return;
      const items = menuItemsOf(liveRoot);
      (items.find((item) => !itemDisabled(item)) ?? items[0])?.focus();
    };
    if (!disabled) {
      triggerWrap.onclick = (event) => {
        event.stopPropagation();
        const origin = event.currentTarget ?? event.target;
        toggleFrom(origin, !ctl.get(), false);
      };
      triggerWrap.onkeydown = (event) => {
        const e = event;
        const origin = e.currentTarget ?? e.target;
        if (e.key === "Enter" || e.key === " ") {
          e.preventDefault();
          toggleFrom(origin, !ctl.get(), true);
          return;
        }
        if (e.key === "ArrowDown" && !ctl.get()) {
          e.preventDefault();
          toggleFrom(origin, true, true);
          return;
        }
        if (e.key === "Escape" && ctl.get()) {
          e.preventDefault();
          setDropdownOpen(origin, false, ctl);
        }
      };
    }
    root.append(triggerWrap);
    const content = el("div", {
      class: "rui-dropdown-menu-content",
      role: "menu",
      "aria-label": asString(props.label) || null,
      "data-menu-root": menuId
    });
    let group = null;
    const target = () => group ?? content;
    const separator = () => el("div", { class: "rui-menu-separator", role: "separator" });
    flattenItems(props.items).forEach((raw, index) => {
      if (isMenuChild(raw, "MenuItem")) {
        const itemProps = mapPositionalArgs(MenuItem, raw.args ?? []);
        const rendered = MenuItem.render(raw, itemProps, helpers);
        if (raw.universal) applyUniversal(rendered, raw.universal);
        if (raw.explicitKey != null && rendered instanceof Element && !rendered.hasAttribute("data-rui-key")) {
          rendered.setAttribute("data-rui-key", String(raw.explicitKey));
        }
        wireMenuItem(rendered, ctl);
        target().append(rendered);
        return;
      }
      if (isMenuChild(raw, "MenuSeparator")) {
        group = null;
        content.append(separator());
        return;
      }
      if (isMenuChild(raw, "MenuLabel")) {
        const labelId = `${menuId}-label-${index}`;
        group = el("div", {
          class: "rui-menu-group",
          role: "group",
          "aria-labelledby": labelId,
          style: "display:contents"
        });
        group.append(el("div", {
          class: "rui-menu-label",
          id: labelId,
          role: "presentation"
        }, [asString((raw.args ?? [])[0])]));
        content.append(group);
        return;
      }
      const plain = objectMenuItem(raw);
      if (plain === "separator") {
        group = null;
        content.append(separator());
        return;
      }
      if (plain) {
        const btn = menuItemButton(plain.view);
        if (!plain.view.disabled) {
          btn.onclick = () => {
            helpers.invoke(plain.action);
          };
        }
        wireMenuItem(btn, ctl);
        target().append(btn);
        return;
      }
      const custom = el("div", { class: "rui-menu-custom", role: "none", style: "display:contents" });
      custom.append(helpers.renderNode(raw));
      target().append(custom);
    });
    content.onkeydown = (event) => {
      const e = event;
      const origin = e.currentTarget ?? e.target;
      const live = origin ? dropdownRootOf(origin) : null;
      if (!live) return;
      if (e.key === "Escape") {
        e.preventDefault();
        e.stopPropagation();
        setDropdownOpen(live, false, ctl);
        triggerTabStop(live)?.focus?.();
        return;
      }
      if (e.key === "Tab") {
        setDropdownOpen(live, false, ctl);
        return;
      }
      const items = menuItemsOf(live);
      if (items.length === 0) return;
      const doc = live.getRootNode();
      const active = doc.activeElement;
      const idx = items.indexOf(active?.closest(".rui-menu-item") ?? null);
      if (e.key === "ArrowDown") {
        e.preventDefault();
        items[(idx + 1) % items.length]?.focus();
        return;
      }
      if (e.key === "ArrowUp") {
        e.preventDefault();
        items[(idx <= 0 ? items.length : idx) - 1]?.focus();
        return;
      }
      if (e.key === "Home") {
        e.preventDefault();
        items[0]?.focus();
        return;
      }
      if (e.key === "End") {
        e.preventDefault();
        items[items.length - 1]?.focus();
        return;
      }
      if (e.key.length !== 1 || e.ctrlKey || e.metaKey || e.altKey) return;
      const from = e.target;
      if (from?.closest(".rui-menu-custom") || from?.isContentEditable) return;
      const ch = e.key.toLowerCase();
      for (let i = 1; i <= items.length; i += 1) {
        const candidate = items[(Math.max(idx, 0) + i) % items.length];
        const text = candidate.querySelector(".rui-menu-item-label")?.textContent ?? "";
        if (!text.trim().toLowerCase().startsWith(ch)) continue;
        e.preventDefault();
        candidate.focus();
        return;
      }
    };
    root.append(content);
    const liveSlot = helpers.useInstanceState("rui-dropdown-live", null);
    deferToPaint(() => {
      const live = root.isConnected ? root : liveSlot.get();
      if (!live?.isConnected) return;
      liveSlot.set(live);
      applyOpenAttrs(live, isOpen);
      syncFloatingLayer(live, isOpen);
      if (isOpen) installDismiss(live, ctl);
      else releaseDismiss(live);
    });
    return root;
  }
};
const MenuItem = {
  name: "MenuItem",
  description: 'Single item inside a DropdownMenu. Renders a button-style row with an optional leading icon and trailing keyboard-shortcut hint. `onClick` (legacy: `action`) runs when clicked and the menu closes afterwards — set `keepOpen=true` for a toggle the user flips several times. Pass `checked` for a checkbox item (or `role="menuitemradio"` with `checked` for a radio group, e.g. a sort direction or theme picker).',
  props: [
    { name: "label", type: "string" },
    { name: "onClick", type: "callable", optional: true, aliases: ["action", "onclick"], description: "Callable to execute on click" },
    { name: "icon", type: "string", optional: true, description: "Font Awesome icon name shown before the label" },
    { name: "shortcut", type: "string", optional: true, description: 'Trailing keyboard-shortcut hint (e.g. "⌘ K")' },
    { name: "variant", aliases: ["tone"], type: "string", optional: true, enum: MENU_VARIANTS, description: 'Use "danger" for destructive actions' },
    { name: "disabled", type: "boolean", optional: true, description: "Unavailable action — the row stays visible and reachable by keyboard, is announced as disabled, and does nothing when activated" },
    { name: "checked", type: "boolean", optional: true, description: 'Render as a checkable item showing this state (implies `role="menuitemcheckbox"`)' },
    { name: "role", type: "string", optional: true, enum: MENU_ITEM_ROLES, description: 'ARIA role — use "menuitemradio" with `checked` for a one-of-many group' },
    { name: "keepOpen", type: "boolean", optional: true, description: "Leave the menu open after this item is activated (toggles, multi-select)" }
  ],
  // Standalone render (when used outside a DropdownMenu): an inert button so
  // the structure still appears, but without the parent's open/close wiring.
  render: (_node, props, helpers) => {
    const view = menuItemView(props);
    const btn = menuItemButton(view);
    if (!view.disabled) {
      btn.onclick = () => {
        helpers.invoke(props.onClick);
      };
    }
    return btn;
  }
};
const MenuSeparator = {
  name: "MenuSeparator",
  description: "Thin horizontal rule used inside a DropdownMenu to group items.",
  props: [],
  render: () => el("div", { class: "rui-menu-separator", role: "separator" })
};
const MenuLabel = {
  name: "MenuLabel",
  description: 'Small uppercase section header inside a DropdownMenu. Use to group related MenuItems (e.g. "Account", "Workspace", "Danger zone").',
  props: [{ name: "label", type: "string" }],
  render: (_node, props) => el("div", { class: "rui-menu-label" }, [asString(props.label)])
};
const COL_ALIGN = ["left", "center", "right"];
const SR_ONLY = "position:absolute;width:1px;height:1px;margin:-1px;padding:0;overflow:hidden;clip-path:inset(50%);white-space:nowrap;border:0";
function readDataGridCols(raw) {
  return asColumnNodes(raw).map((node, idx) => {
    const args = node.args ?? [];
    const header = asString(args[0]);
    const currency = currencyCode(args[8]);
    const rawWrap = args[10];
    const rawResizable = args[15];
    return {
      header,
      values: asArray(args[1]),
      format: asString(args[2], "text"),
      align: COL_ALIGN.includes(asString(args[3])) ? asString(args[3]) : "",
      sortable: asBoolean(args[4]),
      filterable: asBoolean(args[5]),
      render: args[6],
      onClick: args[7],
      key: header || `col-${idx}`,
      width: sanitiseCssLength(args[9], ""),
      wrap: rawWrap === void 0 || rawWrap === null ? null : asBoolean(rawWrap) ? "true" : "false",
      headerTooltip: asString(args[11]),
      // Filtering, sorting and the CSV export all read through this, so a money
      // column exports and sorts on exactly the string the user can see.
      fmt: (value, format) => formatCell(value, format, currency),
      initiallyHidden: asBoolean(args[13]),
      pinned: asString(args[14]) === "left" ? "left" : "",
      resizable: rawResizable === void 0 || rawResizable === null ? void 0 : asBoolean(rawResizable),
      minWidth: sanitiseCssLength(args[16], ""),
      maxWidth: sanitiseCssLength(args[17], "")
    };
  });
}
function compareCells(a, b, format) {
  if (a === null || a === void 0) return b === null || b === void 0 ? 0 : 1;
  if (b === null || b === void 0) return -1;
  if (format === "number" || format === "currency") {
    return asNumber(a, 0) - asNumber(b, 0);
  }
  if (format === "date") {
    const ta = new Date(asString(a)).getTime();
    const tb = new Date(asString(b)).getTime();
    if (Number.isFinite(ta) && Number.isFinite(tb)) return ta - tb;
  }
  return asString(a).localeCompare(asString(b));
}
function csvField(value) {
  const guarded = /^[=+\-@\t\r]/.test(value) ? `'${value}` : value;
  if (/[",\n\r]/.test(guarded)) return `"${guarded.replace(/"/g, '""')}"`;
  return guarded;
}
function buildDataGridCsv(cols, indices) {
  const header = cols.map((c) => csvField(c.header)).join(",");
  const rows = indices.map(
    (r) => cols.map((c) => csvField(c.fmt(c.values[r], c.format))).join(",")
  );
  return [header, ...rows].join("\r\n");
}
function csvFilename(raw) {
  const base = raw.replace(/[\u0000-\u001f\u007f]/g, "").split(/[/\\]/).pop() ?? "";
  const cleaned = base.replace(/[^A-Za-z0-9._-]/g, "_").replace(/^\.+/, "");
  if (!cleaned) return "data.csv";
  return /\.csv$/i.test(cleaned) ? cleaned : `${cleaned}.csv`;
}
function downloadCsv(csv, filename) {
  try {
    if (typeof Blob === "undefined" || typeof URL === "undefined" || typeof document === "undefined") return;
    const url = URL.createObjectURL(new Blob([csv], { type: "text/csv;charset=utf-8" }));
    const a = document.createElement("a");
    a.href = url;
    a.download = csvFilename(filename);
    document.body.appendChild(a);
    a.click();
    a.remove();
    setTimeout(() => URL.revokeObjectURL(url), 0);
  } catch {
  }
}
const STORAGE_VERSION = 1;
function readPersistedConfig(key) {
  try {
    const raw = localStorage.getItem(`aktion-datagrid-${key}`);
    if (!raw) return null;
    const parsed = JSON.parse(raw);
    if (!parsed || typeof parsed !== "object" || parsed.v !== STORAGE_VERSION) return null;
    return parsed;
  } catch {
    return null;
  }
}
function persistConfig(key, config) {
  try {
    localStorage.setItem(`aktion-datagrid-${key}`, JSON.stringify(config));
  } catch {
  }
}
function initColumnConfig(cols, persisted) {
  const defaultOrder = cols.map((c) => c.key);
  const config = {
    order: defaultOrder,
    hidden: new Set(cols.filter((c) => c.initiallyHidden).map((c) => c.key)),
    pinned: new Set(cols.filter((c) => c.pinned === "left").map((c) => c.key)),
    widths: {}
  };
  for (const c of cols) {
    if (c.width) config.widths[c.key] = c.width;
  }
  config.order = normalizeOrder(config.order, config.pinned);
  if (!persisted) return config;
  if (persisted.order) {
    const colKeys = new Set(defaultOrder);
    const validOrder = persisted.order.filter((k) => colKeys.has(k));
    const missing = defaultOrder.filter((k) => !validOrder.includes(k));
    config.order = [...validOrder, ...missing];
  }
  if (persisted.hidden) config.hidden = new Set(persisted.hidden.filter((k) => cols.some((c) => c.key === k)));
  if (persisted.pinned) config.pinned = new Set(persisted.pinned.filter((k) => cols.some((c) => c.key === k)));
  if (persisted.widths) {
    for (const [k, v] of Object.entries(persisted.widths)) {
      const sanitised = sanitiseCssLength(v, "");
      if (sanitised && cols.some((c) => c.key === k)) config.widths[k] = sanitised;
    }
  }
  config.order = normalizeOrder(config.order, config.pinned);
  return config;
}
function normalizeOrder(order, pinned) {
  const head = [];
  const tail = [];
  for (const key of order) (pinned.has(key) ? head : tail).push(key);
  return [...head, ...tail];
}
function reconcileColumnConfig(cols, config) {
  const keys = cols.map((c) => c.key);
  const known = new Set(keys);
  const kept = config.order.filter((k) => known.has(k));
  const added = keys.filter((k) => !kept.includes(k));
  if (added.length === 0 && kept.length === config.order.length) return config;
  const prune = (set) => new Set([...set].filter((k) => known.has(k)));
  const widths = {};
  for (const [k, v] of Object.entries(config.widths)) {
    if (known.has(k)) widths[k] = v;
  }
  for (const c of cols) {
    if (!(c.key in widths) && c.width) widths[c.key] = c.width;
  }
  const nextPinned = prune(config.pinned);
  return {
    order: normalizeOrder([...kept, ...added], nextPinned),
    hidden: prune(config.hidden),
    pinned: nextPinned,
    widths
  };
}
function effectiveCols(cols, config) {
  const byKey = new Map(cols.map((c) => [c.key, c]));
  return config.order.filter((k) => !config.hidden.has(k) && byKey.has(k)).map((k) => byKey.get(k));
}
function configToStorage(config) {
  return {
    v: STORAGE_VERSION,
    order: config.order,
    hidden: Array.from(config.hidden),
    pinned: Array.from(config.pinned),
    widths: config.widths
  };
}
const LEAD_SELECT_WIDTH = 36;
const LEAD_ROWNUM_WIDTH = 42;
const LEAD_SELECT_KEY = "@@select";
const LEAD_ROWNUM_KEY = "@@rownum";
function attrSelectorValue(raw) {
  const escaper = globalThis.CSS?.escape;
  if (typeof escaper === "function") return escaper(raw);
  return raw.replace(/["\\]/g, "\\$&");
}
const INITIAL_HINT = { overflows: false, atStart: true, atEnd: true, headHeight: 0 };
const DataGrid = {
  name: "DataGrid",
  description: "Advanced data table with sortable headers, per-column filter chips, row selection (checkboxes), sticky header / first column, optional pagination, an optional bulk-action toolbar slot, and click-to-act rows. Columns are Col(header, values, format?, align?, sortable?, filterable?) entries. Sorting, selection and pagination work on their own; bind `$sort` (`{key, direction}` object), `$selectedIds` (string[]) and `$page` (number) when the host needs to read or drive them, or use `onSort` / `onSelectionChange` for server-side work. Use `loading` / `error` for query states. Set `columnMenu=true` to let the user hide, reorder, and pin columns — the button is pinned to the top-right of the header and stays put while the grid scrolls sideways, without taking a column of its own. Pinning MOVES a column to the front of the table and above a divider in the panel; drag and arrow-key reordering both stay inside their own group, so the only way across that divider is the pin. The last visible column cannot be hidden. To drive the panel from your own toolbar, bind `columnMenuOpen` (with `onColumnMenuOpenChange`), point `columnMenuAnchor` at your button and set `columnMenuButton: false` — the panel keeps working, it just loses the in-header icon. `columnMenuTitle` / `columnMenuDescription` / `columnMenuResetLabel` take translated strings. Set `resizable=true` to let the user drag column borders to resize; the first drag pins the columns at the widths they are already rendered at and switches to a fixed layout, so a narrowed column truncates instead of pushing its neighbours around. `scrollArrows=false` turns off the small chevrons that appear in the header band when there are columns to scroll to. Pass `persistKey` to save the user's column layout to localStorage — give each grid its own key, and prefix it per app when several apps share an origin; the key is mirrored onto the grid as `data-persist-key`. Set `wrapCells=false` for single-line cells with ellipsis + hover tooltip. Use INSTEAD of `Table` when you need any of those interactions.",
  props: [
    { name: "columns", type: "Col[]", description: "Columns; pass sortable=true / filterable=true on each Col." },
    { name: "rowIds", type: "any[]", optional: true, description: "Stable id per row (used by `selectedIds` and as the row's morph key); defaults to row index." },
    { name: "caption", type: "string", optional: true },
    { name: "sort", type: "object", optional: true, description: "`{key, direction}` — pass a $variable for two-way binding" },
    { name: "selectedIds", type: "any[]", optional: true, description: "Array of selected row ids — bind a $variable" },
    { name: "selectable", type: "boolean", optional: true, description: "Render leading selection checkboxes" },
    { name: "page", type: "number", optional: true, description: "1-indexed current page — bind a $variable" },
    { name: "perPage", type: "number", optional: true, description: "Page size (default 20)" },
    { name: "emptyLabel", type: "string", optional: true, description: "Text shown when no rows match (default `No results`)" },
    { name: "onRowClick", type: "callable", optional: true, aliases: ["rowAction"], description: "Callable fired when a row is clicked. Receives (rowIndex, row, rowId) where `row` is header-keyed." },
    { name: "toolbar", type: "Node[]", optional: true, description: "Bulk-action toolbar shown above the table when any rows are selected" },
    { name: "density", type: "string", optional: true, enum: ["comfortable", "compact"] },
    { name: "striped", type: "boolean", optional: true },
    { name: "stickyHeader", type: "boolean", optional: true, description: "Pin the header row (default true). Constrains the grid's height to `maxHeight` so there is something to scroll." },
    { name: "stickyFirstColumn", type: "boolean", optional: true, description: "Pin the first column horizontally" },
    { name: "exportable", type: "boolean", optional: true, description: "Show an Export CSV button that downloads the filtered rows" },
    { name: "exportFilename", type: "string", optional: true, description: "CSV download filename (default `data.csv`)" },
    { name: "loading", type: "boolean", optional: true, description: "Show a loading row instead of the empty message while rows are in flight" },
    { name: "error", type: "string", optional: true, description: "Failure message shown instead of the rows (takes precedence over `loading`)" },
    { name: "loadingLabel", type: "string", optional: true, description: "Label for the loading row (default `Loading…`)" },
    { name: "maxHeight", type: "string", optional: true, description: "Scroll-area height cap, e.g. `70vh` / `480px`. Applied when `stickyHeader` is on (default `70vh`) — without it the header has no scrollport to stick to." },
    { name: "allowOverflow", type: "boolean", optional: true, description: "Let cell content (menus, popovers) escape the scroll box instead of clipping it. Disables the sticky header." },
    { name: "onSort", type: "callable", optional: true, description: "Callable fired with (columnKey, direction) when a header is activated — use for server-side sorting" },
    { name: "onSelectionChange", type: "callable", optional: true, description: "Callable fired with the array of selected row ids" },
    { name: "perPageOptions", type: "number[]", optional: true, description: "Page sizes offered in a footer dropdown, e.g. [20, 50, 100]" },
    { name: "onPerPageChange", type: "callable", optional: true, description: "Callable fired with the newly chosen page size" },
    // Slots 26–34: advanced column management features. All optional.
    // APPEND-ONLY: positional args are resolved by index, so a new prop goes at
    // the END of this list — inserting one silently re-points every prop after it.
    { name: "persistKey", type: "string", optional: true, description: "localStorage key — when set, column widths, order, visibility, and pinning survive page refreshes. Each table needs its own key, prefixed per app where several apps share an origin: two grids on one key fight over a single saved layout. Mirrored onto the grid element as `data-persist-key` so the slot is visible in DevTools." },
    { name: "resizable", type: "boolean", optional: true, description: "Let the user drag column header borders to resize columns. Double-click a handle (or press Home on it) to auto-fit. The first drag switches the table to a fixed layout so resized cells truncate instead of overflowing." },
    { name: "columnMenu", type: "boolean", optional: true, description: "Show a column settings button for hiding, reordering, and pinning columns. Overlays the top-right of the header instead of occupying a column, so cell widths are unaffected." },
    { name: "globalSearch", type: "string", optional: true, description: "Global search term that filters across all columns — bind a $variable for two-way control." },
    { name: "onGlobalSearch", type: "callable", optional: true, description: "Callable fired with the search term when the global search input changes." },
    { name: "wrapCells", type: "boolean", optional: true, description: "Default cell content wrapping. `false` = single line with ellipsis and hover tooltip; `true` = allow wrapping. Per-column `Col(wrap:)` overrides." },
    { name: "rowNumbers", type: "boolean", optional: true, description: "Show a leading row-number column." },
    { name: "highlightOnHover", type: "boolean", optional: true, description: "Highlight rows on mouse hover (default true)." },
    { name: "scrollArrows", type: "boolean", optional: true, description: "Show small chevron buttons in the header band while there are columns hidden to the left / right (default true). They sit in the header, never over a data cell." },
    { name: "columnMenuOpen", type: "boolean", optional: true, description: "Open state of the column-settings panel — bind a `$variable` for two-way control, so an external button can open it. Leave unset to let the built-in trigger own the state." },
    { name: "onColumnMenuOpenChange", type: "callable", optional: true, aliases: ["oncolumnmenuopenchange"], description: "Called with the new boolean whenever the column-settings panel opens or closes — including via Escape, the × button, or an outside click." },
    { name: "columnMenuButton", type: "boolean", optional: true, description: "Render the built-in column-settings trigger in the header (default true). Set `false` when an external control opens the panel, which keeps the panel and its configuration without the in-header icon." },
    { name: "columnMenuAnchor", type: "string", optional: true, description: 'CSS selector for the element the panel should hang off, e.g. `"#table-settings"`. Defaults to the built-in trigger; required when `columnMenuButton` is `false`, or the panel has nothing to anchor to.' },
    { name: "columnMenuTitle", type: "string", optional: true, description: 'Heading of the column-settings panel (default "Table settings"). Pass a translated string in a localised app.' },
    { name: "columnMenuDescription", type: "string", optional: true, description: 'Sub-heading under the panel title (default "Manage column visibility and order"). Pass `""` to drop the line.' },
    { name: "columnMenuResetLabel", type: "string", optional: true, description: `Label of the panel's reset action (default "Reset to default").` }
  ],
  render: (node, props, helpers) => {
    const allCols = readDataGridCols(props.columns);
    const rowCount = Math.max(0, ...allCols.map((c) => c.values.length));
    const rowIds = asArray(props.rowIds);
    const idFor = (rowIdx) => asString(rowIds[rowIdx] ?? rowIdx);
    const sortStateName = node.argMeta?.[3]?.stateRef;
    const selectedStateName = node.argMeta?.[4]?.stateRef;
    const pageStateName = node.argMeta?.[6]?.stateRef;
    const perPageStateName = node.argMeta?.[7]?.stateRef;
    const globalSearchStateName = node.argMeta?.[29]?.stateRef;
    const selectable = asBoolean(props.selectable) || selectedStateName !== void 0;
    const allowOverflow = asBoolean(props.allowOverflow);
    const stickyHeader = allowOverflow ? false : props.stickyHeader === void 0 ? true : asBoolean(props.stickyHeader);
    const stickyFirst = asBoolean(props.stickyFirstColumn);
    const density = asString(props.density, "comfortable");
    const striped = asBoolean(props.striped);
    const loading = asBoolean(props.loading);
    const errorText = asString(props.error);
    const emptyLabel = asString(props.emptyLabel, "No results");
    const loadingLabel = asString(props.loadingLabel, "Loading…");
    const perPageOptions = asArray(props.perPageOptions).map((v) => Math.floor(asNumber(v, 0))).filter((n) => n > 0);
    const persistKey = asString(props.persistKey);
    const gridResizable = asBoolean(props.resizable);
    const columnMenuEnabled = asBoolean(props.columnMenu);
    const showRowNumbers = asBoolean(props.rowNumbers);
    const highlightOnHover = props.highlightOnHover === void 0 ? true : asBoolean(props.highlightOnHover);
    const scrollArrows = props.scrollArrows === void 0 ? true : asBoolean(props.scrollArrows);
    const wrapCellsProp = props.wrapCells;
    const wrapCellsDefault = wrapCellsProp === void 0 || wrapCellsProp === null ? null : asBoolean(wrapCellsProp) ? "true" : "false";
    const filterSlot = helpers.useInstanceState("filters", {});
    const sortSlot = helpers.useInstanceState("sort", null);
    const selectedSlot = helpers.useInstanceState("selected", null);
    const pageSlot = helpers.useInstanceState("page", null);
    const perPageSlot = helpers.useInstanceState("perPage", null);
    const globalSearchSlot = helpers.useInstanceState("globalSearch", "");
    const persisted = persistKey ? readPersistedConfig(persistKey) : null;
    const colConfigSlot = helpers.useInstanceState(
      "colConfig",
      initColumnConfig(allCols, persisted)
    );
    const colConfigPanelOpen = helpers.useInstanceState("colConfigOpen", false);
    const livePanelSlot = helpers.useInstanceState("livePanel", null);
    const autoWidthSlot = helpers.useInstanceState("autoWidths", {});
    const hintSlot = helpers.useInstanceState("scrollHint", INITIAL_HINT);
    const stickyLeftSlot = helpers.useInstanceState("stickyLeft", {});
    const liveViewSlot = helpers.useInstanceState("liveViewport", null);
    const observerSlot = helpers.useInstanceState("resizeObserver", null);
    const getColConfig = () => {
      const stored = colConfigSlot.get();
      const merged = reconcileColumnConfig(allCols, stored);
      if (merged !== stored) colConfigSlot.set(merged);
      return merged;
    };
    const updateColConfig = (patch) => {
      const current = getColConfig();
      const next = { ...current, ...patch };
      next.order = normalizeOrder(next.order, next.pinned);
      colConfigSlot.set(next);
      if (persistKey) persistConfig(persistKey, configToStorage(next));
    };
    const cols = effectiveCols(allCols, getColConfig());
    const isColResizable = (col) => col.resizable === void 0 ? gridResizable : col.resizable;
    const fixedLayout = allCols.some(isColResizable);
    const widthFor = (col, config2) => config2.widths[col.key] || autoWidthSlot.get()[col.key] || "";
    const layoutMeasured = fixedLayout && cols.length > 0 && cols.every((c) => widthFor(c, getColConfig()) !== "");
    const readSort = () => {
      const fromProps = props.sort && typeof props.sort === "object" ? props.sort : null;
      const src = sortStateName ? fromProps : sortSlot.get() ?? fromProps;
      return {
        key: asString(src?.key),
        dir: asString(src?.direction, "asc") === "desc" ? "desc" : "asc"
      };
    };
    const readSelected = () => {
      const src = selectedStateName ? props.selectedIds : selectedSlot.get() ?? props.selectedIds;
      return asArray(src).map((v) => asString(v));
    };
    const readPerPage = () => {
      const local = perPageStateName ? null : perPageSlot.get();
      return Math.max(1, Math.floor(asNumber(local ?? props.perPage, 20)));
    };
    const readGlobalSearch = () => {
      if (globalSearchStateName) return asString(props.globalSearch).trim().toLowerCase();
      return (globalSearchSlot.get() || asString(props.globalSearch)).trim().toLowerCase();
    };
    const readModel = () => {
      const filters = filterSlot.get();
      const { key: sortKey, dir: sortDir } = readSort();
      const gSearch = readGlobalSearch();
      const indices = [];
      for (let r = 0; r < rowCount; r += 1) {
        let keep = true;
        for (const c of allCols) {
          if (!c.filterable) continue;
          const term = (filters[c.key] ?? "").trim().toLowerCase();
          if (!term) continue;
          if (!c.fmt(c.values[r], c.format).toLowerCase().includes(term)) {
            keep = false;
            break;
          }
        }
        if (keep && gSearch) {
          let gMatch = false;
          for (const c of allCols) {
            if (c.fmt(c.values[r], c.format).toLowerCase().includes(gSearch)) {
              gMatch = true;
              break;
            }
          }
          if (!gMatch) keep = false;
        }
        if (keep) indices.push(r);
      }
      if (sortKey) {
        const sortCol = allCols.find((c) => c.key === sortKey);
        if (sortCol && sortCol.sortable) {
          indices.sort((a, b) => {
            const cmp = compareCells(sortCol.values[a], sortCol.values[b], sortCol.format);
            return sortDir === "desc" ? -cmp : cmp;
          });
        }
      }
      const total = indices.length;
      const perPage = readPerPage();
      const totalPages = Math.max(1, Math.ceil(total / perPage));
      const rawPageSrc = pageStateName ? props.page : pageSlot.get() ?? props.page;
      const rawPage = Math.max(1, Math.floor(asNumber(rawPageSrc, 1)));
      const page = Math.min(rawPage, totalPages);
      return {
        indices,
        total,
        perPage,
        totalPages,
        page,
        visible: indices.slice((page - 1) * perPage, page * perPage),
        sortKey,
        sortDir,
        selected: new Set(readSelected())
      };
    };
    const wrapper = el("div", {
      class: "rui-data-grid",
      "data-density": density,
      "data-striped": striped ? "true" : "false",
      "data-sticky-header": stickyHeader ? "true" : "false",
      "data-sticky-first": stickyFirst ? "true" : "false",
      "data-allow-overflow": allowOverflow ? "true" : null,
      "data-highlight-hover": highlightOnHover ? "true" : "false",
      "data-nowrap": wrapCellsDefault === "false" ? "true" : null,
      "data-resizable": fixedLayout ? "true" : null,
      // Emitted from state, never only imperatively: morph strips attributes the
      // fresh tree omits (see ScrollHintState).
      "data-fixed-layout": layoutMeasured ? "true" : null,
      "data-col-menu": columnMenuEnabled ? "true" : null,
      // Which localStorage slot this grid's column layout is saved under, mirrored
      // so it is inspectable: two grids that share a key silently fight over one
      // layout, and without this the key is invisible in the DOM and in DevTools.
      "data-persist-key": persistKey || null
    });
    const liveRootSlot = helpers.useInstanceState("liveRoot", null);
    const liveScope = (origin) => {
      const found = origin?.closest(".rui-data-grid") ?? liveViewSlot.get()?.closest(".rui-data-grid");
      if (found?.isConnected) {
        liveRootSlot.set(found);
        return found;
      }
      const remembered = liveRootSlot.get();
      return remembered?.isConnected ? remembered : wrapper;
    };
    const bodyOf = (scope) => scope.querySelector(".rui-data-grid-table > tbody");
    const stickyStyle = (key, pinned) => {
      if (!pinned) return null;
      const left = stickyLeftSlot.get()[key];
      return left === void 0 ? "position:sticky" : `position:sticky;left:${Math.round(left)}px`;
    };
    const liveColEl = (scope, key) => scope.querySelector(
      `.rui-data-grid-table > colgroup > col[data-col-key="${attrSelectorValue(key)}"]`
    );
    const buildColGroup = (visCols, config2) => {
      const group = el("colgroup");
      if (selectable) {
        group.append(el("col", { class: "rui-data-grid-col-lead", style: `width:${LEAD_SELECT_WIDTH}px` }));
      }
      if (showRowNumbers) {
        group.append(el("col", { class: "rui-data-grid-col-lead", style: `width:${LEAD_ROWNUM_WIDTH}px` }));
      }
      for (const col of visCols) {
        const w = widthFor(col, config2);
        group.append(el("col", { "data-col-key": col.key, style: w ? `width:${w}` : null }));
      }
      if (fixedLayout) group.append(el("col", { class: "rui-data-grid-col-filler" }));
      return group;
    };
    const fillerCell = (tag) => el(tag, { class: "rui-data-grid-filler", "aria-hidden": "true", role: "presentation" });
    const buildScrollArrow = (side) => {
      const btn = el("button", {
        type: "button",
        class: `rui-data-grid-scroll-arrow rui-data-grid-scroll-arrow-${side}`,
        "aria-label": side === "left" ? "Scroll columns left" : "Scroll columns right",
        // Redundant with the (focusable, arrow-key scrollable) scroll port, so
        // it stays out of the tab order and out of the accessibility tree.
        "aria-hidden": "true",
        tabindex: "-1"
      });
      const icon = renderIcon(side === "left" ? "chevron-left" : "chevron-right", {
        className: "rui-data-grid-scroll-arrow-icon"
      });
      if (icon) btn.append(icon);
      else btn.textContent = side === "left" ? "‹" : "›";
      btn.onclick = (event) => {
        event.stopPropagation();
        const view = event.currentTarget.closest(".rui-data-grid-viewport");
        const scroller = view?.querySelector(".rui-data-grid-scroll");
        if (!scroller) return;
        const step = Math.max(120, Math.round(scroller.clientWidth * 0.75));
        scroller.scrollBy({ left: side === "left" ? -step : step, behavior: "smooth" });
      };
      return btn;
    };
    const commitWidth = (key, px) => {
      const cfg2 = getColConfig();
      updateColConfig({ widths: { ...cfg2.widths, [key]: px } });
    };
    const buildColTh = (col, colIdx, config2, isLast) => {
      const isPinned = config2.pinned.has(col.key);
      const th = el("th", {
        scope: "col",
        "data-col-key": col.key,
        "data-align": col.align || null,
        "data-sortable": col.sortable ? "true" : null,
        "data-first": colIdx === 0 ? "true" : null,
        "data-last": isLast ? "true" : null,
        "data-wrap": col.wrap,
        "data-pinned": isPinned ? "true" : null,
        title: col.headerTooltip || null,
        style: stickyStyle(col.key, isPinned)
      });
      if (col.sortable) {
        const btn = el("button", { type: "button", class: "rui-data-grid-sort" });
        btn.append(el("span", {}, [col.header]));
        btn.onclick = (event) => writeSort(col.key, event.currentTarget);
        th.append(btn);
      } else {
        th.append(document.createTextNode(col.header));
      }
      if (isColResizable(col)) {
        const colKey = col.key;
        const minW = parsePx(col.minWidth) || 50;
        const maxW = parsePx(col.maxWidth) || 2e3;
        const handle = el("div", {
          class: "rui-data-grid-resize-handle",
          "data-resize-col": colKey,
          role: "separator",
          "aria-orientation": "vertical",
          "aria-label": `Resize ${col.header}`,
          tabindex: "0"
        });
        const currentWidth = (handleEl) => {
          freezeColumnWidths(liveScope(handleEl));
          const liveTh = handleEl.parentElement;
          return liveTh ? liveTh.getBoundingClientRect().width : minW;
        };
        const setWidth = (handleEl, next) => {
          const px = `${Math.round(Math.max(minW, Math.min(maxW, next)))}px`;
          const colEl = liveColEl(liveScope(handleEl), colKey);
          if (colEl) colEl.style.width = px;
          return px;
        };
        const autoFitWidth = (handleEl) => {
          const scope = liveScope(handleEl);
          freezeColumnWidths(scope);
          const liveTh = handleEl.parentElement;
          let widest = liveTh ? liveTh.scrollWidth : 0;
          scope.querySelectorAll(
            `.rui-data-grid-table > tbody > tr > td[data-col-key="${attrSelectorValue(colKey)}"]`
          ).forEach((td) => {
            widest = Math.max(widest, td.scrollWidth);
          });
          return widest + 2;
        };
        handle.onpointerdown = (event) => {
          if (event.pointerType === "mouse" && event.button !== 0) return;
          event.preventDefault();
          event.stopPropagation();
          const liveHandle = event.currentTarget;
          const startX = event.clientX;
          const startW = currentWidth(liveHandle);
          const pointerId = event.pointerId;
          liveHandle.classList.add("rui-data-grid-resize-active");
          try {
            liveHandle.setPointerCapture(pointerId);
          } catch {
          }
          let last = `${Math.round(startW)}px`;
          const onMove = (ev) => {
            last = setWidth(liveHandle, startW + (ev.clientX - startX));
          };
          const onUp = () => {
            liveHandle.classList.remove("rui-data-grid-resize-active");
            document.removeEventListener("pointermove", onMove);
            document.removeEventListener("pointerup", onUp);
            document.removeEventListener("pointercancel", onUp);
            try {
              liveHandle.releasePointerCapture(pointerId);
            } catch {
            }
            commitWidth(colKey, last);
          };
          document.addEventListener("pointermove", onMove);
          document.addEventListener("pointerup", onUp);
          document.addEventListener("pointercancel", onUp);
        };
        handle.ondblclick = (event) => {
          event.preventDefault();
          event.stopPropagation();
          const liveHandle = event.currentTarget;
          commitWidth(colKey, setWidth(liveHandle, autoFitWidth(liveHandle)));
        };
        handle.onkeydown = (event) => {
          const liveHandle = event.currentTarget;
          const step = event.shiftKey ? 48 : 12;
          if (event.key === "ArrowLeft") {
            commitWidth(colKey, setWidth(liveHandle, currentWidth(liveHandle) - step));
          } else if (event.key === "ArrowRight") {
            commitWidth(colKey, setWidth(liveHandle, currentWidth(liveHandle) + step));
          } else if (event.key === "Home" || event.key === "Enter") {
            commitWidth(colKey, setWidth(liveHandle, autoFitWidth(liveHandle)));
          } else {
            return;
          }
          event.preventDefault();
        };
        th.append(handle);
      }
      return th;
    };
    const buildFilterTd = (col, filters, isPinned) => {
      const td = el("td", {
        "data-col-key": col.key,
        "data-pinned": isPinned ? "true" : null,
        style: stickyStyle(col.key, isPinned)
      });
      if (col.filterable) {
        const input = el("input", {
          type: "search",
          class: "rui-data-grid-filter",
          placeholder: `Filter ${col.header}`,
          "aria-label": `Filter ${col.header}`,
          value: valueAttr(filters[col.key])
        });
        input.oninput = (event) => {
          const target = event.currentTarget;
          filterSlot.set({ ...filterSlot.get(), [col.key]: target.value });
          writePage(1);
          repaint(target);
        };
        td.append(input);
      }
      return td;
    };
    const columnSignature = (config2, visCols) => [
      config2.order.join(""),
      [...config2.hidden].sort().join(""),
      [...config2.pinned].sort().join(""),
      visCols.map((c) => `${c.key}=${widthFor(c, config2)}`).join("")
    ].join("");
    const syncColumns = (scope) => {
      const config2 = getColConfig();
      const visCols = effectiveCols(allCols, config2);
      const signature = columnSignature(config2, visCols);
      if (scope.getAttribute("data-col-sig") === signature) return;
      scope.setAttribute("data-col-sig", signature);
      const table2 = scope.querySelector(".rui-data-grid-table");
      const group = table2?.querySelector(":scope > colgroup");
      if (table2 && group) group.replaceWith(buildColGroup(visCols, config2));
      const headRow2 = scope.querySelector(".rui-data-grid-table > thead > tr:first-child");
      if (headRow2) {
        headRow2.querySelectorAll("th[data-col-key]").forEach((n) => n.remove());
        const filler = headRow2.querySelector(".rui-data-grid-filler");
        visCols.forEach((col, c) => {
          headRow2.insertBefore(buildColTh(col, c, config2, c === visCols.length - 1), filler);
        });
      }
      const filterRow = scope.querySelector(".rui-data-grid-filter-row");
      if (filterRow) {
        filterRow.querySelectorAll("td[data-col-key]").forEach((n) => n.remove());
        const filler = filterRow.querySelector(".rui-data-grid-filler");
        const filters = filterSlot.get();
        for (const col of visCols) {
          filterRow.insertBefore(buildFilterTd(col, filters, config2.pinned.has(col.key)), filler);
        }
      }
    };
    const syncStickyOffsets = (scope) => {
      const config2 = getColConfig();
      if (config2.pinned.size === 0) return;
      const headRow2 = scope.querySelector(".rui-data-grid-table > thead > tr:first-child");
      if (!headRow2) return;
      const leadCount = (selectable ? 1 : 0) + (showRowNumbers ? 1 : 0);
      const next = {};
      const plan = [];
      let offset = 0;
      Array.from(headRow2.children).forEach((cell, index) => {
        const isLead = index < leadCount;
        const key = isLead ? index === 0 && selectable ? LEAD_SELECT_KEY : LEAD_ROWNUM_KEY : cell.getAttribute("data-col-key");
        if (!key || !(isLead || cell.getAttribute("data-pinned") === "true")) {
          plan.push(null);
          return;
        }
        next[key] = offset;
        plan.push(offset);
        offset += cell.getBoundingClientRect().width;
      });
      stickyLeftSlot.set(next);
      const apply = (row) => {
        const rowCells = Array.from(row.children);
        plan.forEach((left, index) => {
          const cell = rowCells[index];
          if (!cell) return;
          if (left === null) {
            cell.style.removeProperty("left");
            return;
          }
          cell.style.position = "sticky";
          cell.style.left = `${Math.round(left)}px`;
        });
      };
      scope.querySelectorAll(".rui-data-grid-table > thead > tr").forEach(apply);
      scope.querySelectorAll(".rui-data-grid-table > tbody > tr").forEach(apply);
    };
    const buildRows = (target, m) => {
      target.replaceChildren();
      const config2 = getColConfig();
      const visCols = effectiveCols(allCols, config2);
      const leadCols = (selectable ? 1 : 0) + (showRowNumbers ? 1 : 0);
      const span = leadCols + Math.max(visCols.length, 1) + (fixedLayout ? 1 : 0);
      const placeholder = (extraClass, children) => {
        const tr = el("tr");
        tr.append(el("td", {
          colspan: String(span),
          class: `rui-data-grid-empty${extraClass ? ` ${extraClass}` : ""}`
        }, children));
        target.append(tr);
      };
      if (errorText) {
        placeholder("rui-data-grid-error", [errorText]);
        return;
      }
      if (m.visible.length === 0) {
        if (loading) {
          const spin = renderIcon("spinner", { className: "rui-data-grid-spin" });
          placeholder("rui-data-grid-loading", [spin, loadingLabel].filter(Boolean));
        } else {
          placeholder("", [emptyLabel]);
        }
        return;
      }
      for (let vi = 0; vi < m.visible.length; vi += 1) {
        const r = m.visible[vi];
        const id = idFor(r);
        const isSelected = m.selected.has(id);
        const tr = el("tr", {
          "data-rui-key": id,
          "data-row-id": id,
          "data-selected": isSelected ? "true" : null
        });
        const leadPinned = config2.pinned.size > 0;
        if (selectable) {
          const cellTd = el("td", {
            class: "rui-data-grid-cell-select",
            "data-pinned": leadPinned ? "true" : null,
            style: stickyStyle(LEAD_SELECT_KEY, leadPinned)
          });
          const cb = el("input", {
            type: "checkbox",
            class: "rui-data-grid-checkbox",
            "aria-label": "Select row",
            checked: isSelected ? "" : null
          });
          cb.onclick = (event) => {
            event.stopPropagation();
            const target2 = event.currentTarget;
            const rowEl = target2.closest("tr");
            const rowId = rowEl?.getAttribute("data-row-id") ?? id;
            const next = new Set(readSelected());
            if (target2.checked) next.add(rowId);
            else next.delete(rowId);
            writeSelection(Array.from(next), target2);
          };
          cellTd.append(cb);
          tr.append(cellTd);
        }
        if (showRowNumbers) {
          const numTd = el("td", {
            class: "rui-data-grid-cell-rownum",
            "data-pinned": leadPinned ? "true" : null,
            style: stickyStyle(LEAD_ROWNUM_KEY, leadPinned)
          });
          numTd.textContent = String((m.page - 1) * m.perPage + vi + 1);
          tr.append(numTd);
        }
        const rowObj = {};
        for (const cc of allCols) rowObj[cc.key] = cc.values[r];
        visCols.forEach((col, c) => {
          const isPinned = config2.pinned.has(col.key);
          const cellWrap = col.wrap ?? wrapCellsDefault;
          const td = el("td", {
            "data-format": col.format,
            "data-align": col.align || null,
            "data-first": c === 0 ? "true" : null,
            "data-last": c === visCols.length - 1 ? "true" : null,
            "data-wrap": cellWrap,
            "data-pinned": isPinned ? "true" : null,
            style: stickyStyle(col.key, isPinned)
          });
          td.setAttribute("data-col-key", col.key);
          fillTableCell(td, col, col.values[r], r, helpers, col.fmt, rowObj);
          if (cellWrap === "false" && !col.render && !isComponentNode(col.values[r])) {
            const text = td.textContent ?? "";
            if (text) td.title = text;
          }
          tr.append(td);
        });
        if (fixedLayout) tr.append(fillerCell("td"));
        if (typeof props.onRowClick === "function") {
          tr.setAttribute("data-clickable", "true");
          tr.tabIndex = 0;
          const fire = (event) => {
            const target2 = event.target;
            if (target2?.closest("input,button,a,label,select,textarea")) return false;
            helpers.invoke(props.onRowClick, r, rowObj, id);
            return true;
          };
          tr.onclick = (event) => {
            fire(event);
          };
          tr.onkeydown = (event) => {
            if (event.key !== "Enter" && event.key !== " ") return;
            if (fire(event)) event.preventDefault();
          };
        }
        target.append(tr);
      }
    };
    const syncHeader = (scope, m) => {
      scope.querySelectorAll(".rui-data-grid-table thead th[data-col-key]").forEach((th) => {
        const key = th.getAttribute("data-col-key") ?? "";
        const col = allCols.find((c) => c.key === key);
        if (!col?.sortable) return;
        const active = key === m.sortKey;
        if (active) th.setAttribute("data-active", "true");
        else th.removeAttribute("data-active");
        th.setAttribute("aria-sort", active ? m.sortDir === "asc" ? "ascending" : "descending" : "none");
        const btn = th.querySelector(".rui-data-grid-sort");
        if (!btn) return;
        const icon = renderIcon(
          active ? m.sortDir === "asc" ? "arrow-up-short-wide" : "arrow-down-wide-short" : "sort",
          { className: "rui-data-grid-sort-icon" }
        );
        if (!icon) return;
        const prev = btn.querySelector(".rui-data-grid-sort-icon");
        if (prev) prev.replaceWith(icon);
        else btn.append(icon);
      });
    };
    const applySelection = (scope, selected) => {
      let onPage = 0;
      let selectedOnPage = 0;
      scope.querySelectorAll(".rui-data-grid-table > tbody > tr[data-row-id]").forEach((tr) => {
        const on = selected.has(tr.getAttribute("data-row-id") ?? "");
        onPage += 1;
        if (on) selectedOnPage += 1;
        if (on) tr.setAttribute("data-selected", "true");
        else tr.removeAttribute("data-selected");
        const cb = tr.querySelector("input.rui-data-grid-checkbox");
        if (!cb) return;
        cb.checked = on;
        if (on) cb.setAttribute("checked", "");
        else cb.removeAttribute("checked");
      });
      const all = scope.querySelector("thead .rui-data-grid-checkbox");
      if (all) {
        const every = onPage > 0 && selectedOnPage === onPage;
        const some = selectedOnPage > 0 && !every;
        all.checked = every;
        if (every) all.setAttribute("checked", "");
        else all.removeAttribute("checked");
        all.indeterminate = some;
        all.setAttribute("aria-checked", some ? "mixed" : every ? "true" : "false");
        if (some) all.setAttribute("data-indeterminate", "true");
        else all.removeAttribute("data-indeterminate");
      }
      const bulk = scope.querySelector(".rui-data-grid-bulk");
      if (bulk) {
        const count = selected.size;
        const label = bulk.querySelector(".rui-data-grid-bulk-count");
        if (label) label.textContent = `${count} selected`;
        bulk.hidden = count === 0;
        bulk.style.display = count === 0 ? "none" : "";
      }
    };
    const syncFooter = (scope, m) => {
      const footer = scope.querySelector(".rui-data-grid-footer");
      if (!footer) return;
      const summary = footer.querySelector(".rui-data-grid-footer-summary");
      if (summary) {
        const start = m.total === 0 ? 0 : (m.page - 1) * m.perPage + 1;
        const end = Math.min(m.total, m.page * m.perPage);
        summary.textContent = m.total === 0 ? emptyLabel : `Showing ${start}–${end} of ${m.total}`;
      }
      const buttons = scope.querySelector(".rui-data-grid-footer-buttons");
      if (buttons) {
        const paged = m.totalPages > 1;
        buttons.hidden = !paged;
        buttons.style.display = paged ? "" : "none";
        const [prev, next] = Array.from(
          buttons.querySelectorAll(".rui-data-grid-page-button")
        );
        const setDisabled = (btn, off) => {
          if (!btn) return;
          btn.disabled = off;
          if (off) btn.setAttribute("disabled", "");
          else btn.removeAttribute("disabled");
        };
        setDisabled(prev, m.page <= 1);
        setDisabled(next, m.page >= m.totalPages);
        const current = buttons.querySelector(".rui-data-grid-page-current");
        if (current) current.textContent = `${m.page} / ${m.totalPages}`;
      }
      const select = footer.querySelector(".rui-data-grid-per-page");
      if (select && select.value !== String(m.perPage)) select.value = String(m.perPage);
    };
    const syncStatus = (scope, m) => {
      const region = scope.querySelector(".rui-data-grid-status");
      if (!region) return;
      const next = errorText ? errorText : loading ? loadingLabel : `${m.total} ${m.total === 1 ? "result" : "results"}`;
      if (region.textContent !== next) region.textContent = next;
    };
    const syncScrollHint = (view) => {
      const scroller = view.querySelector(".rui-data-grid-scroll");
      if (!scroller) return;
      const headRow2 = view.querySelector(".rui-data-grid-table > thead > tr:first-child");
      const headHeight = headRow2 ? Math.round(headRow2.getBoundingClientRect().height) : 0;
      const slack = scroller.scrollWidth - scroller.clientWidth;
      const overflows = slack > 1;
      const left = scroller.scrollLeft;
      const next = {
        overflows,
        atStart: !overflows || left <= 1,
        atEnd: !overflows || left >= slack - 1,
        headHeight: headHeight || hintSlot.get().headHeight
      };
      const prev = hintSlot.get();
      hintSlot.set(next);
      if (prev.overflows === next.overflows && prev.atStart === next.atStart && prev.atEnd === next.atEnd && prev.headHeight === next.headHeight && view.hasAttribute("data-measured")) return;
      applyHint(view, next);
    };
    const applyHint = (view, hint2) => {
      view.setAttribute("data-measured", "true");
      const flag = (name, on) => {
        if (on) view.setAttribute(name, "true");
        else view.removeAttribute(name);
      };
      flag("data-overflow-x", hint2.overflows);
      flag("data-at-start", hint2.atStart);
      flag("data-at-end", hint2.atEnd);
      if (hint2.headHeight > 0) view.style.setProperty("--rui-dg-head-h", `${hint2.headHeight}px`);
      const scroller = view.querySelector(".rui-data-grid-scroll");
      if (!scroller) return;
      if (hint2.overflows) scroller.setAttribute("tabindex", "0");
      else scroller.removeAttribute("tabindex");
    };
    const freezeColumnWidths = (scope) => {
      if (!fixedLayout) return;
      const config2 = getColConfig();
      const auto = { ...autoWidthSlot.get() };
      let changed = false;
      scope.querySelectorAll(
        ".rui-data-grid-table > thead > tr:first-child > th[data-col-key]"
      ).forEach((th) => {
        const key = th.getAttribute("data-col-key") ?? "";
        if (!key || config2.widths[key] || auto[key]) return;
        const width = Math.ceil(th.getBoundingClientRect().width);
        if (width <= 0) return;
        auto[key] = `${width}px`;
        changed = true;
        const colEl = liveColEl(scope, key);
        if (colEl) colEl.style.width = `${width}px`;
      });
      if (!changed) return;
      autoWidthSlot.set(auto);
      const grid = scope.closest(".rui-data-grid") ?? scope;
      grid.setAttribute("data-fixed-layout", "true");
      grid.setAttribute(
        "data-col-sig",
        columnSignature(getColConfig(), effectiveCols(allCols, getColConfig()))
      );
    };
    const paint = (scope, m) => {
      syncColumns(scope);
      const body = bodyOf(scope);
      if (body) buildRows(body, m);
      syncHeader(scope, m);
      applySelection(scope, m.selected);
      syncFooter(scope, m);
      syncStatus(scope, m);
      const view = scope.querySelector(".rui-data-grid-viewport");
      if (view?.isConnected) {
        syncStickyOffsets(scope);
        syncScrollHint(view);
      }
    };
    const repaint = (origin, m = readModel()) => paint(liveScope(origin), m);
    let restoreColMenu = null;
    const writePage = (next) => {
      if (pageStateName) helpers.setState(pageStateName, next);
      else pageSlot.set(next);
    };
    const writeSort = (key, origin) => {
      const cur = readSort();
      const direction = cur.key === key && cur.dir === "asc" ? "desc" : "asc";
      if (sortStateName) helpers.setState(sortStateName, { key, direction });
      else sortSlot.set({ key, direction });
      helpers.invoke(props.onSort, key, direction);
      repaint(origin);
    };
    const writeSelection = (next, origin) => {
      if (selectedStateName) helpers.setState(selectedStateName, next);
      else selectedSlot.set(next);
      helpers.invoke(props.onSelectionChange, next);
      applySelection(liveScope(origin), new Set(next));
    };
    const toolbarChildren = asArray(props.toolbar);
    if (toolbarChildren.length > 0) {
      const bar = el("div", { class: "rui-data-grid-bulk" });
      bar.append(el("span", { class: "rui-data-grid-bulk-count" }, ["0 selected"]));
      const tools = el("div", { class: "rui-data-grid-bulk-tools" });
      for (const child of toolbarChildren) tools.append(helpers.renderNode(child));
      bar.append(tools);
      wrapper.append(bar);
    }
    const hasToolbar = asBoolean(props.exportable) || typeof props.globalSearch === "string" || globalSearchStateName;
    if (hasToolbar) {
      const toolbarRow = el("div", { class: "rui-data-grid-toolbar" });
      if (typeof props.globalSearch === "string" || globalSearchStateName) {
        const searchWrap = el("div", { class: "rui-data-grid-global-search" });
        const searchIcon = renderIcon("magnifying-glass", { className: "rui-data-grid-search-icon" });
        if (searchIcon) searchWrap.append(searchIcon);
        const searchInput = el("input", {
          type: "search",
          class: "rui-data-grid-search-input",
          placeholder: "Search all columns…",
          "aria-label": "Search all columns",
          value: valueAttr(readGlobalSearch())
        });
        searchInput.oninput = (event) => {
          const target = event.currentTarget;
          const term = target.value;
          if (globalSearchStateName) helpers.setState(globalSearchStateName, term);
          else globalSearchSlot.set(term);
          helpers.invoke(props.onGlobalSearch, term);
          writePage(1);
          repaint(target);
        };
        searchWrap.append(searchInput);
        toolbarRow.append(searchWrap);
      }
      const toolbarActions = el("div", { class: "rui-data-grid-toolbar-actions" });
      if (asBoolean(props.exportable)) {
        const btn = el("button", { type: "button", class: "rui-data-grid-export" });
        const icon = renderIcon("download", { className: "rui-data-grid-export-icon" });
        if (icon) btn.append(icon);
        btn.append(el("span", {}, ["Export CSV"]));
        btn.onclick = () => {
          const csv = buildDataGridCsv(cols, readModel().indices);
          downloadCsv(csv, asString(props.exportFilename, "data.csv") || "data.csv");
        };
        toolbarActions.append(btn);
      }
      toolbarRow.append(toolbarActions);
      wrapper.append(toolbarRow);
    }
    wrapper.append(el("span", {
      class: "rui-data-grid-status",
      role: "status",
      "aria-live": "polite",
      style: SR_ONLY
    }));
    const hint = hintSlot.get();
    const viewport = el("div", {
      class: "rui-data-grid-viewport",
      "data-overflow-x": hint.overflows ? "true" : null,
      "data-at-start": hint.atStart ? "true" : null,
      "data-at-end": hint.atEnd ? "true" : null,
      style: hint.headHeight > 0 ? `--rui-dg-head-h:${hint.headHeight}px` : null
    });
    const tableWrap = el("div", {
      class: "rui-data-grid-scroll",
      tabindex: hint.overflows ? "0" : null
    });
    if (allowOverflow) {
      tableWrap.style.overflow = "visible";
      wrapper.style.overflowX = "auto";
    } else {
      const cap = sanitiseCssLength(props.maxHeight, stickyHeader ? "70vh" : "");
      if (cap) tableWrap.style.maxHeight = cap;
    }
    const table = el("table", { class: "rui-data-grid-table" });
    if (loading) table.setAttribute("aria-busy", "true");
    const caption = asString(props.caption);
    if (caption) table.append(el("caption", { class: "rui-data-grid-caption" }, [caption]));
    table.append(buildColGroup(cols, getColConfig()));
    const thead = el("thead");
    const headRow = el("tr");
    if (selectable) {
      const leadPinned = getColConfig().pinned.size > 0;
      const th = el("th", {
        class: "rui-data-grid-cell-select",
        scope: "col",
        "data-pinned": leadPinned ? "true" : null,
        style: stickyStyle(LEAD_SELECT_KEY, leadPinned)
      });
      const cb = el("input", {
        type: "checkbox",
        class: "rui-data-grid-checkbox",
        "aria-label": "Select all rows on this page"
      });
      cb.onclick = (event) => {
        const target = event.currentTarget;
        const scope = liveScope(target);
        const next = new Set(readSelected());
        scope.querySelectorAll(".rui-data-grid-table > tbody > tr[data-row-id]").forEach((tr) => {
          const id = tr.getAttribute("data-row-id") ?? "";
          if (target.checked) next.add(id);
          else next.delete(id);
        });
        writeSelection(Array.from(next), target);
      };
      th.append(cb);
      headRow.append(th);
    }
    if (showRowNumbers) {
      const leadPinned = getColConfig().pinned.size > 0;
      headRow.append(el("th", {
        class: "rui-data-grid-cell-rownum",
        scope: "col",
        "data-pinned": leadPinned ? "true" : null,
        style: stickyStyle(LEAD_ROWNUM_KEY, leadPinned)
      }, ["#"]));
    }
    const config = getColConfig();
    cols.forEach((col, c) => {
      headRow.append(buildColTh(col, c, config, c === cols.length - 1));
    });
    if (fixedLayout) headRow.append(fillerCell("th"));
    if (columnMenuEnabled) {
      const reorderHintId = `${autoId(helpers, "rui-data-grid-col")}-reorder-hint`;
      const columnMenuTitle = asString(props.columnMenuTitle) || "Table settings";
      const columnMenuSubtitle = props.columnMenuDescription === void 0 ? "Manage column visibility and order" : asString(props.columnMenuDescription);
      const columnMenuResetLabel = asString(props.columnMenuResetLabel) || "Reset to default";
      const openSlotIndex = DataGrid.props.findIndex((pr) => pr.name === "columnMenuOpen");
      const openStateName = openSlotIndex >= 0 ? node.argMeta?.[openSlotIndex]?.stateRef : void 0;
      const openControlled = props.columnMenuOpen !== void 0;
      const columnMenuAnchor = asString(props.columnMenuAnchor);
      const showMenuButton = props.columnMenuButton === void 0 ? true : asBoolean(props.columnMenuButton);
      if (openControlled) colConfigPanelOpen.set(asBoolean(props.columnMenuOpen));
      const reportOpen = (next) => {
        if (colConfigPanelOpen.get() === next) return;
        colConfigPanelOpen.set(next);
        if (openStateName) helpers.setState(openStateName, next);
        helpers.invoke(props.onColumnMenuOpenChange, next);
      };
      const menuWrap = el("div", { class: "rui-data-grid-col-menu" });
      const menuBtn = el("button", {
        type: "button",
        class: "rui-data-grid-col-menu-btn",
        "aria-label": "Column settings",
        "aria-haspopup": "dialog",
        "aria-expanded": colConfigPanelOpen.get() ? "true" : "false"
      });
      const gearIcon = renderIcon("sliders", { className: "rui-data-grid-col-menu-icon" });
      if (gearIcon) menuBtn.append(gearIcon);
      else menuBtn.textContent = "☰";
      if (!showMenuButton) menuWrap.setAttribute("data-hidden", "true");
      const panel = el("div", {
        class: "rui-data-grid-col-panel",
        role: "dialog",
        "aria-label": "Column settings",
        "data-open": colConfigPanelOpen.get() ? "true" : "false"
      });
      const findLive = (origin) => {
        const menu = liveScope(origin).querySelector(".rui-data-grid-col-menu");
        const inPlace = menu?.querySelector(".rui-data-grid-col-panel") ?? null;
        const remembered = livePanelSlot.get();
        const live = inPlace ?? (remembered?.isConnected ? remembered : null);
        if (live) livePanelSlot.set(live);
        return {
          btn: menu?.querySelector(".rui-data-grid-col-menu-btn") ?? null,
          panel: live
        };
      };
      const closePanel = (origin) => {
        reportOpen(false);
        colConfigPanelOpen.set(false);
        const live = findLive(origin ?? null);
        if (live.panel) {
          closeFloating(live.panel);
          live.panel.setAttribute("data-open", "false");
        }
        live.btn?.setAttribute("aria-expanded", "false");
      };
      const rebuildColPanel = (panelEl) => {
        panelEl.replaceChildren();
        const cfg0 = getColConfig();
        const panelHead = el("div", { class: "rui-data-grid-col-panel-head" });
        const heading = el("span", { class: "rui-data-grid-col-panel-heading" });
        heading.append(el("span", { class: "rui-data-grid-col-panel-title" }, [columnMenuTitle]));
        if (columnMenuSubtitle) {
          heading.append(el("span", { class: "rui-data-grid-col-panel-subtitle" }, [columnMenuSubtitle]));
        }
        panelHead.append(heading);
        const resetBtn = el("button", {
          type: "button",
          class: "rui-data-grid-col-panel-reset"
        });
        const resetIcon = renderIcon("rotate-left", { className: "rui-data-grid-col-panel-reset-icon" });
        if (resetIcon) resetBtn.append(resetIcon);
        resetBtn.append(el("span", {}, [columnMenuResetLabel]));
        resetBtn.onclick = (ev) => {
          ev.stopPropagation();
          colConfigSlot.set(initColumnConfig(allCols, null));
          autoWidthSlot.set({});
          if (persistKey) {
            try {
              localStorage.removeItem(`aktion-datagrid-${persistKey}`);
            } catch {
            }
          }
          rebuildColPanel(panelEl);
          repaint(ev.currentTarget);
        };
        const closeBtn = el("button", {
          type: "button",
          class: "rui-data-grid-col-panel-close",
          "aria-label": "Close column settings"
        }, ["×"]);
        closeBtn.onclick = (ev) => {
          ev.stopPropagation();
          closePanel(ev.currentTarget);
        };
        panelHead.append(closeBtn);
        panelEl.append(panelHead);
        const list = el("div", { class: "rui-data-grid-col-panel-list" });
        let colDrag = null;
        const DRAG_THRESHOLD = 4;
        const dragTargetIndex = (d, dy) => {
          const raw = d.fromIndex + Math.round(dy / d.rowH);
          return Math.max(d.groupStart, Math.min(d.groupEnd, raw));
        };
        const applyDragOffsets = (d, dy) => {
          d.node.style.transform = `translateY(${dy}px)`;
          for (const [i, r] of d.rows.entries()) {
            if (i === d.fromIndex) continue;
            let shift = 0;
            if (d.fromIndex < d.toIndex && i > d.fromIndex && i <= d.toIndex) shift = -d.rowH;
            else if (d.fromIndex > d.toIndex && i >= d.toIndex && i < d.fromIndex) shift = d.rowH;
            r.style.transform = shift === 0 ? "" : `translateY(${shift}px)`;
          }
        };
        const clearDragStyles = (d) => {
          for (const r of d.rows) {
            r.style.transform = "";
            r.classList.remove("rui-data-grid-col-dragging", "rui-data-grid-col-shifting");
          }
          list.classList.remove("rui-data-grid-col-reordering");
        };
        const AUTO_SCROLL_ZONE = 28;
        const AUTO_SCROLL_STEP = 12;
        let autoScrollRaf = 0;
        let autoScrollDir = 0;
        const stopAutoScroll = () => {
          if (autoScrollRaf !== 0) cancelAnimationFrame(autoScrollRaf);
          autoScrollRaf = 0;
          autoScrollDir = 0;
        };
        const stepAutoScroll = () => {
          autoScrollRaf = 0;
          const d = colDrag;
          if (!d || !d.active || autoScrollDir === 0 || !panelEl.isConnected) {
            stopAutoScroll();
            return;
          }
          const before = panelEl.scrollTop;
          panelEl.scrollTop = before + autoScrollDir * AUTO_SCROLL_STEP;
          if (panelEl.scrollTop !== before) {
            const dy = d.lastY - d.startY + (panelEl.scrollTop - d.startScroll);
            d.toIndex = dragTargetIndex(d, dy);
            applyDragOffsets(d, dy);
          }
          autoScrollRaf = requestAnimationFrame(stepAutoScroll);
        };
        const updateAutoScroll = (clientY) => {
          const box = panelEl.getBoundingClientRect();
          const dir = clientY < box.top + AUTO_SCROLL_ZONE ? -1 : clientY > box.bottom - AUTO_SCROLL_ZONE ? 1 : 0;
          autoScrollDir = dir;
          if (dir !== 0 && autoScrollRaf === 0 && typeof requestAnimationFrame === "function") {
            autoScrollRaf = requestAnimationFrame(stepAutoScroll);
          }
        };
        const endColDrag = (commit, origin) => {
          const d = colDrag;
          colDrag = null;
          stopAutoScroll();
          if (!d) return;
          try {
            d.node.releasePointerCapture(d.pointerId);
          } catch {
          }
          if (!d.active) return;
          clearDragStyles(d);
          if (!commit || d.toIndex === d.fromIndex) return;
          const cfg2 = getColConfig();
          const order = [...cfg2.order];
          const srcIdx = order.indexOf(d.key);
          if (srcIdx < 0) return;
          order.splice(srcIdx, 1);
          order.splice(d.toIndex, 0, d.key);
          updateColConfig({ order });
          rebuildColPanel(panelEl);
          repaint(origin);
        };
        const groupBounds = (index) => {
          const pinnedCount = cfg0.order.filter((k) => cfg0.pinned.has(k)).length;
          return index < pinnedCount ? { start: 0, end: pinnedCount - 1 } : { start: pinnedCount, end: cfg0.order.length - 1 };
        };
        const refocusRow = (key, childSelector) => {
          for (const r of panelEl.querySelectorAll(".rui-data-grid-col-panel-row")) {
            if (r.getAttribute("data-col-key") !== key) continue;
            r.querySelector(childSelector)?.focus();
            return;
          }
        };
        const nudgeColumn = (key, delta, origin) => {
          const cfg2 = getColConfig();
          const order = [...cfg2.order];
          const from = order.indexOf(key);
          const to = from + delta;
          if (from < 0) return;
          const bounds = groupBounds(from);
          if (to < bounds.start || to > bounds.end) return;
          order.splice(from, 1);
          order.splice(to, 0, key);
          updateColConfig({ order });
          rebuildColPanel(panelEl);
          repaint(origin);
          refocusRow(key, ".rui-data-grid-col-panel-handle");
        };
        const visibleCount = cfg0.order.filter((k) => !cfg0.hidden.has(k)).length;
        let dividerDrawn = false;
        for (const key of cfg0.order) {
          const colDef = allCols.find((cd) => cd.key === key);
          if (!colDef) continue;
          const isHidden = cfg0.hidden.has(key);
          const isPinnedCol = cfg0.pinned.has(key);
          const isLastVisible = !isHidden && visibleCount <= 1;
          if (!isPinnedCol && !dividerDrawn && cfg0.pinned.size > 0) {
            dividerDrawn = true;
            list.append(el("div", {
              class: "rui-data-grid-col-panel-divider",
              role: "separator",
              "aria-label": "Pinned columns above"
            }));
          }
          const row = el("div", {
            class: "rui-data-grid-col-panel-row",
            "data-col-key": key,
            "data-pinned": isPinnedCol ? "true" : "false"
          });
          const handle = el("button", {
            type: "button",
            class: "rui-data-grid-col-panel-handle",
            "aria-label": `Reorder ${colDef.header}`,
            "aria-describedby": reorderHintId
          }, ["⠿"]);
          handle.onkeydown = (ev) => {
            const delta = ev.key === "ArrowUp" ? -1 : ev.key === "ArrowDown" ? 1 : 0;
            if (delta === 0) return;
            ev.preventDefault();
            ev.stopPropagation();
            nudgeColumn(key, delta, ev.currentTarget);
          };
          row.append(handle);
          row.append(el("span", {
            class: "rui-data-grid-col-panel-label"
          }, [colDef.header]));
          const pinBtn = el("button", {
            type: "button",
            class: "rui-data-grid-col-panel-pin",
            "aria-label": isPinnedCol ? `Unpin ${colDef.header}` : `Pin ${colDef.header}`,
            "data-active": isPinnedCol ? "true" : null
          });
          const pinIcon = renderIcon("thumbtack", { className: "rui-data-grid-col-panel-pin-icon" });
          if (pinIcon) pinBtn.append(pinIcon);
          pinBtn.onclick = (ev) => {
            ev.stopPropagation();
            const cfg2 = getColConfig();
            const nextPinned = new Set(cfg2.pinned);
            if (isPinnedCol) nextPinned.delete(key);
            else nextPinned.add(key);
            updateColConfig({ pinned: nextPinned });
            rebuildColPanel(panelEl);
            repaint(ev.currentTarget);
            refocusRow(key, ".rui-data-grid-col-panel-pin");
          };
          row.append(pinBtn);
          const cb = el("input", {
            type: "checkbox",
            class: "rui-data-grid-col-panel-cb",
            checked: isHidden ? null : "",
            // Disabled rather than silently ignored: a checkbox that does nothing
            // when clicked reads as broken, whereas a greyed one says why it cannot
            // move. The title carries the reason for a pointer user.
            disabled: isLastVisible ? "" : null,
            title: isLastVisible ? "At least one column must stay visible" : null,
            "aria-label": `Show ${colDef.header}`
          });
          cb.onclick = (ev) => {
            ev.stopPropagation();
            const target2 = ev.currentTarget;
            const cfg2 = getColConfig();
            const nextHidden = new Set(cfg2.hidden);
            if (target2.checked) {
              nextHidden.delete(key);
            } else if (cfg2.order.filter((k) => !cfg2.hidden.has(k)).length <= 1) {
              target2.checked = true;
              return;
            } else {
              nextHidden.add(key);
            }
            updateColConfig({ hidden: nextHidden });
            rebuildColPanel(panelEl);
            repaint(target2);
            refocusRow(key, ".rui-data-grid-col-panel-cb");
          };
          row.append(cb);
          row.onpointerdown = (ev) => {
            if (ev.button !== 0) return;
            const from = ev.target;
            if (from?.closest?.("input, button:not(.rui-data-grid-col-panel-handle)")) return;
            if (ev.pointerType === "touch" && !from?.closest?.(".rui-data-grid-col-panel-handle")) return;
            const node2 = ev.currentTarget;
            const rows = [...list.querySelectorAll(".rui-data-grid-col-panel-row")];
            const fromIndex = rows.indexOf(node2);
            if (fromIndex < 0) return;
            colDrag = {
              key,
              rows,
              fromIndex,
              toIndex: fromIndex,
              rowH: node2.getBoundingClientRect().height,
              startY: ev.clientY,
              startScroll: panelEl.scrollTop,
              pointerId: ev.pointerId,
              node: node2,
              active: false,
              lastY: ev.clientY,
              groupStart: groupBounds(fromIndex).start,
              groupEnd: groupBounds(fromIndex).end
            };
            try {
              node2.setPointerCapture(ev.pointerId);
            } catch {
            }
          };
          row.onpointermove = (ev) => {
            const d = colDrag;
            if (!d || d.pointerId !== ev.pointerId) return;
            const dy = ev.clientY - d.startY + (panelEl.scrollTop - d.startScroll);
            if (!d.active) {
              if (Math.abs(dy) < DRAG_THRESHOLD) return;
              d.active = true;
              list.classList.add("rui-data-grid-col-reordering");
              d.node.classList.add("rui-data-grid-col-dragging");
              for (const r of d.rows) if (r !== d.node) r.classList.add("rui-data-grid-col-shifting");
            }
            ev.preventDefault();
            d.lastY = ev.clientY;
            d.toIndex = dragTargetIndex(d, dy);
            applyDragOffsets(d, dy);
            updateAutoScroll(ev.clientY);
          };
          row.onpointerup = (ev) => {
            if (!colDrag || colDrag.pointerId !== ev.pointerId) return;
            endColDrag(true, ev.currentTarget);
          };
          row.onpointercancel = (ev) => {
            if (!colDrag || colDrag.pointerId !== ev.pointerId) return;
            endColDrag(false, ev.currentTarget);
          };
          row.onlostpointercapture = (ev) => {
            if (!colDrag || colDrag.pointerId !== ev.pointerId) return;
            endColDrag(false, ev.currentTarget);
          };
          row.onkeydown = (ev) => {
            if (ev.key !== "Escape" || !colDrag) return;
            ev.stopPropagation();
            endColDrag(false, ev.currentTarget);
          };
          list.append(row);
        }
        panelEl.append(list);
        const footer = el("div", { class: "rui-data-grid-col-panel-footer" });
        footer.append(resetBtn);
        panelEl.append(footer);
        panelEl.append(el("span", {
          id: reorderHintId,
          class: "rui-data-grid-col-panel-hint"
        }, ["Press the up and down arrow keys to move this column within its group."]));
      };
      const resolveMenuAnchor = (fallback) => {
        const base = showMenuButton ? fallback : viewport ?? fallback;
        if (!columnMenuAnchor) return base;
        const scope = liveScope(fallback);
        const root = scope.getRootNode?.();
        for (const where of [root, scope, typeof document === "undefined" ? null : document]) {
          if (!where) continue;
          try {
            const found = where.querySelector?.(columnMenuAnchor);
            if (found instanceof HTMLElement) return found;
          } catch {
          }
        }
        return base;
      };
      const openPanel = (origin) => {
        const live = findLive(origin);
        if (!live.panel || !live.btn) return;
        reportOpen(true);
        colConfigPanelOpen.set(true);
        live.panel.setAttribute("data-open", "true");
        live.btn.setAttribute("aria-expanded", "true");
        rebuildColPanel(live.panel);
        openFloating(live.panel, {
          anchor: resolveMenuAnchor(live.btn),
          side: "bottom",
          align: "end",
          offset: 4,
          layer: "dropdown",
          maxHeight: "viewport"
        });
      };
      restoreColMenu = () => {
        const live = findLive(null);
        if (!colConfigPanelOpen.get()) {
          if (live.panel && isFloating(live.panel)) {
            closeFloating(live.panel);
            live.panel.setAttribute("data-open", "false");
            live.btn?.setAttribute("aria-expanded", "false");
          }
          return;
        }
        if (!live.panel || !live.btn) return;
        if (isFloating(live.panel)) updateFloating(live.panel);
        else openPanel(live.btn);
      };
      menuBtn.onclick = (event) => {
        event.stopPropagation();
        const origin = event.currentTarget;
        if (colConfigPanelOpen.get()) closePanel(origin);
        else openPanel(origin);
      };
      menuBtn.onkeydown = (event) => {
        if (event.key !== "Escape" || !colConfigPanelOpen.get()) return;
        event.stopPropagation();
        closePanel(event.currentTarget);
      };
      menuWrap.append(menuBtn);
      menuWrap.onmousedown = (ev) => ev.stopPropagation();
      panel.onclick = (ev) => ev.stopPropagation();
      panel.onkeydown = (event) => {
        if (event.key !== "Escape") return;
        event.stopPropagation();
        const live = findLive(event.currentTarget);
        closePanel(event.currentTarget);
        live.btn?.focus();
      };
      const closeOnOutside = (event) => {
        if (!colConfigPanelOpen.get()) return;
        const path = event.composedPath?.() ?? [];
        for (const node2 of path) {
          if (!(node2 instanceof Element)) continue;
          if (node2.classList?.contains("rui-data-grid-col-menu") || node2.classList?.contains("rui-data-grid-col-panel")) return;
        }
        closePanel();
      };
      if (typeof document !== "undefined") {
        document.addEventListener("mousedown", closeOnOutside);
        helpers.registerDisposer(() => {
          document.removeEventListener("mousedown", closeOnOutside);
          closeFloating(livePanelSlot.get());
        }, "col-menu-outside");
      }
      rebuildColPanel(panel);
      menuWrap.append(panel);
      viewport.append(menuWrap);
    }
    thead.append(headRow);
    if (cols.some((c) => c.filterable)) {
      const filters = filterSlot.get();
      const leadPinned = config.pinned.size > 0;
      const filterRow = el("tr", { class: "rui-data-grid-filter-row" });
      if (selectable) {
        filterRow.append(el("td", {
          class: "rui-data-grid-cell-select",
          "data-pinned": leadPinned ? "true" : null,
          style: stickyStyle(LEAD_SELECT_KEY, leadPinned)
        }));
      }
      if (showRowNumbers) {
        filterRow.append(el("td", {
          class: "rui-data-grid-cell-rownum",
          "data-pinned": leadPinned ? "true" : null,
          style: stickyStyle(LEAD_ROWNUM_KEY, leadPinned)
        }));
      }
      for (const col of cols) {
        filterRow.append(buildFilterTd(col, filters, config.pinned.has(col.key)));
      }
      if (fixedLayout) filterRow.append(fillerCell("td"));
      thead.append(filterRow);
    }
    table.append(thead);
    const tbody = el("tbody");
    table.append(tbody);
    tableWrap.append(table);
    viewport.append(tableWrap);
    if (scrollArrows && !allowOverflow) {
      viewport.append(buildScrollArrow("left"), buildScrollArrow("right"));
    }
    wrapper.append(viewport);
    const model = readModel();
    if (perPageOptions.length > 0 || rowCount > model.perPage) {
      const footer = el("div", { class: "rui-data-grid-footer" });
      footer.append(el("span", { class: "rui-data-grid-footer-summary" }));
      if (perPageOptions.length > 0) {
        const select = el("select", {
          class: "rui-data-grid-per-page",
          "aria-label": "Rows per page"
        });
        for (const size2 of perPageOptions) {
          select.append(el("option", { value: String(size2) }, [`${size2} / page`]));
        }
        select.onchange = (event) => {
          const target = event.currentTarget;
          const size2 = Math.max(1, Math.floor(asNumber(target.value, model.perPage)));
          if (perPageStateName) helpers.setState(perPageStateName, size2);
          else perPageSlot.set(size2);
          helpers.invoke(props.onPerPageChange, size2);
          writePage(1);
          repaint(target);
        };
        footer.append(select);
      }
      const buttons = el("div", { class: "rui-data-grid-footer-buttons" });
      const prev = el("button", { type: "button", class: "rui-data-grid-page-button" }, ["‹ Prev"]);
      const next = el("button", { type: "button", class: "rui-data-grid-page-button" }, ["Next ›"]);
      const step = (delta) => (event) => {
        const target = event.currentTarget;
        const m = readModel();
        const to = m.page + delta;
        if (to < 1 || to > m.totalPages) return;
        writePage(to);
        repaint(target);
      };
      prev.onclick = step(-1);
      next.onclick = step(1);
      buttons.append(prev);
      buttons.append(el("span", { class: "rui-data-grid-page-current" }));
      buttons.append(next);
      footer.append(buttons);
      wrapper.append(footer);
    }
    paint(wrapper, model);
    deferToPaint(() => {
      const live = viewport.isConnected ? viewport : liveViewSlot.get();
      if (!live?.isConnected) return;
      liveViewSlot.set(live);
      syncStickyOffsets(live.closest(".rui-data-grid") ?? live);
      syncScrollHint(live);
      restoreColMenu?.();
      const scroller = live.querySelector(".rui-data-grid-scroll");
      if (!scroller) return;
      scroller.onscroll = () => syncScrollHint(live);
      const existing = observerSlot.get();
      if (existing && existing.node !== live) {
        existing.ro.disconnect();
        observerSlot.set(null);
      }
      if (observerSlot.get() || typeof ResizeObserver === "undefined") return;
      const ro = new ResizeObserver(() => syncScrollHint(live));
      try {
        ro.observe(scroller);
        const table2 = live.querySelector(".rui-data-grid-table");
        if (table2) ro.observe(table2);
      } catch {
      }
      observerSlot.set({ ro, node: live });
      helpers.registerDisposer(() => {
        ro.disconnect();
        observerSlot.set(null);
      }, "rui-data-grid-observer");
    });
    return wrapper;
  }
};
function parsePx(value) {
  if (!value) return 0;
  const match = /^(\d+(?:\.\d+)?)px$/i.exec(value.trim());
  return match ? Number(match[1]) : 0;
}
function readCalendarEvents(raw) {
  const out = [];
  asArray(raw).forEach((entry, idx) => {
    if (!entry || typeof entry !== "object") return;
    const e = entry;
    const date = asString(e.date);
    if (!date) return;
    out.push({
      // Events need an identity for `onEventClick` to be actionable; fall back
      // to a positional id so an author who omits `id` still gets a handle.
      id: asString(e.id) || `${date}#${idx}`,
      date,
      title: asString(e.title),
      tone: asString(e.tone, "primary"),
      time: asString(e.time)
    });
  });
  return out;
}
const ISO_MONTH_OR_DAY = /^(\d{4})-(\d{2})(?:-(\d{2}))?$/;
function parseLocalIso(raw) {
  const m = ISO_MONTH_OR_DAY.exec(raw.trim());
  if (!m) return null;
  return new Date(Number(m[1]), Number(m[2]) - 1, m[3] ? Number(m[3]) : 1);
}
function startOfWeek(date, weekStartsOn) {
  const out = new Date(date.getFullYear(), date.getMonth(), date.getDate());
  const day = out.getDay();
  const diff = (day - weekStartsOn + 7) % 7;
  out.setDate(out.getDate() - diff);
  return out;
}
function addDays(date, days) {
  return new Date(date.getFullYear(), date.getMonth(), date.getDate() + days);
}
function formatIsoDate(date) {
  const y = date.getFullYear();
  const m = String(date.getMonth() + 1).padStart(2, "0");
  const d = String(date.getDate()).padStart(2, "0");
  return `${y}-${m}-${d}`;
}
function rangeLabel(first, last) {
  const sameMonth = first.getMonth() === last.getMonth() && first.getFullYear() === last.getFullYear();
  const head = first.toLocaleDateString(void 0, sameMonth ? { day: "numeric" } : { day: "numeric", month: "short" });
  const tail = last.toLocaleDateString(void 0, { day: "numeric", month: "short", year: "numeric" });
  return `${head} – ${tail}`;
}
const CAL_EVENT_BUTTON = "border:0;font-family:inherit;cursor:pointer;width:100%";
const CAL_MORE_BUTTON = "border:0;background:none;font:inherit;font-size:11px;cursor:pointer;color:inherit;text-align:left";
const CAL_DISABLED_DAY = "opacity:0.45;cursor:default";
const CAL_AVATAR = "width:100%;height:100%;border-radius:50%;object-fit:cover;display:block";
const CalendarView = {
  name: "CalendarView",
  description: "Full-month or week calendar grid for scheduling apps — distinct from the form-input `DatePicker`. Pass events as an array of `{date: 'YYYY-MM-DD', title, tone?, time?, id?}` objects. Bind `value` to a `$variable` for the selected date (ISO string) and `month` for the visible month; the built-in ‹ / › / Today controls write both. Use `view=\"week\"` for a single-week strip. `firstDay=1` (Monday) matches most business apps. `onEventClick` makes the event chips clickable, `min` / `max` / `disabledDates` block days.",
  props: [
    { name: "value", type: "string", optional: true, description: "Selected ISO date (YYYY-MM-DD); bind a $variable" },
    { name: "month", type: "string", optional: true, description: "Visible month — ISO date or YYYY-MM. Bind a $variable to follow the prev/next controls (defaults to `value`, else today)" },
    { name: "events", type: "object[]", optional: true, description: "Array of {date, title, tone?, time?, id?} objects" },
    { name: "view", type: "string", optional: true, enum: ["month", "week"] },
    { name: "firstDay", type: "number", optional: true, description: "0=Sunday, 1=Monday (default 1)" },
    { name: "onSelect", type: "callable", optional: true, description: "Callable fired when a day is clicked; receives the ISO date string" },
    { name: "onMonthChange", type: "callable", optional: true, aliases: ["onNavigate"], description: "Callable fired when the prev/next/today controls move the grid; receives the new anchor ISO date" },
    { name: "onEventClick", type: "callable", optional: true, description: "Callable fired with (eventId, event) when an event chip is clicked; makes the chips real buttons" },
    { name: "maxEventsPerDay", type: "number", optional: true, description: "Event chips per day before a `+N more` toggle (default 3)" },
    { name: "min", type: "string", optional: true, description: "Earliest selectable ISO date" },
    { name: "max", type: "string", optional: true, description: "Latest selectable ISO date" },
    { name: "disabledDates", type: "string[]", optional: true, description: "ISO dates that cannot be selected (blackout days)" },
    { name: "hideNav", type: "boolean", optional: true, description: "Hide the built-in prev / next / today controls" }
  ],
  render: (node, props, helpers) => {
    const view = asString(props.view, "month");
    const events = readCalendarEvents(props.events);
    const today = /* @__PURE__ */ new Date();
    const todayIso = formatIsoDate(today);
    const weekStartsOn = (asNumber(props.firstDay, 1) % 7 + 7) % 7;
    const maxEvents = Math.max(1, Math.floor(asNumber(props.maxEventsPerDay, 3)));
    const valueStateName = node.argMeta?.[0]?.stateRef;
    const monthStateName = node.argMeta?.[1]?.stateRef;
    const selectionSlot = helpers.useInstanceState("selected", "");
    const uncontrolled = !valueStateName && !asString(props.value);
    const valueRaw = uncontrolled ? selectionSlot.get() : asString(props.value);
    const valueIso = /^\d{4}-\d{2}-\d{2}$/.test(valueRaw) ? valueRaw : "";
    const navSlot = helpers.useInstanceState("anchor", "");
    const localNav = monthStateName ? "" : navSlot.get();
    const anchorRaw = localNav || asString(props.month) || asString(props.value);
    const anchorDate = parseLocalIso(anchorRaw) ?? (anchorRaw ? new Date(anchorRaw) : today);
    const refDate = Number.isNaN(anchorDate.getTime()) ? today : anchorDate;
    const minIso = asString(props.min).slice(0, 10);
    const maxIso = asString(props.max).slice(0, 10);
    const blackout = new Set(asArray(props.disabledDates).map((d) => asString(d).slice(0, 10)));
    const isDisabled = (iso) => minIso !== "" && iso < minIso || maxIso !== "" && iso > maxIso || blackout.has(iso);
    const eventsByDate = /* @__PURE__ */ new Map();
    for (const evt of events) {
      const key = evt.date.slice(0, 10);
      const list = eventsByDate.get(key) ?? [];
      list.push(evt);
      eventsByDate.set(key, list);
    }
    const focusSlot = helpers.useInstanceState("focus", "");
    const expandedSlot = helpers.useInstanceState("expanded", "");
    const eventClickable = typeof props.onEventClick === "function";
    const root = el("div", { class: "rui-calendar", "data-view": view });
    const liveScope = (origin) => origin?.closest(".rui-calendar") ?? root;
    const cellsFor = (ref) => {
      const out = [];
      if (view === "week") {
        const start = startOfWeek(ref, weekStartsOn);
        for (let i = 0; i < 7; i += 1) {
          const d = addDays(start, i);
          out.push({ date: d, inMonth: d.getMonth() === ref.getMonth() });
        }
        return out;
      }
      const firstOfMonth = new Date(ref.getFullYear(), ref.getMonth(), 1);
      const lastOfMonth = new Date(ref.getFullYear(), ref.getMonth() + 1, 0);
      const gridStart = startOfWeek(firstOfMonth, weekStartsOn);
      const totalDays = Math.ceil((lastOfMonth.getDate() + (firstOfMonth.getDay() - weekStartsOn + 7) % 7) / 7) * 7;
      for (let i = 0; i < totalDays; i += 1) {
        const d = addDays(gridStart, i);
        out.push({ date: d, inMonth: d.getMonth() === ref.getMonth() });
      }
      return out;
    };
    const navigateTo = (iso, origin) => {
      if (monthStateName) helpers.setState(monthStateName, iso);
      else navSlot.set(iso);
      helpers.invoke(props.onMonthChange, iso);
      const ref = parseLocalIso(iso);
      if (ref) applyView(liveScope(origin), ref);
    };
    const shiftAnchor = (delta) => {
      if (view === "week") return formatIsoDate(addDays(startOfWeek(refDate, weekStartsOn), delta * 7));
      return formatIsoDate(new Date(refDate.getFullYear(), refDate.getMonth() + delta, 1));
    };
    const applySelected = (scope, iso) => {
      scope.querySelectorAll(".rui-calendar-day").forEach((cell) => {
        const on = cell.getAttribute("data-date") === iso;
        cell.setAttribute("data-selected", on ? "true" : "false");
        cell.setAttribute("aria-selected", on ? "true" : "false");
      });
    };
    const selectDay = (iso, origin) => {
      if (isDisabled(iso)) return;
      if (valueStateName) helpers.setState(valueStateName, iso);
      else if (uncontrolled) selectionSlot.set(iso);
      helpers.invoke(props.onSelect, iso);
      if (valueStateName || uncontrolled) applySelected(liveScope(origin), iso);
    };
    const setRoving = (scope, iso) => {
      focusSlot.set(iso);
      scope.querySelectorAll(".rui-calendar-day").forEach((cell) => {
        cell.tabIndex = cell.getAttribute("data-date") === iso ? 0 : -1;
      });
    };
    const moveFocus = (origin, target) => {
      const iso = formatIsoDate(target);
      const scope = liveScope(origin);
      const focusIn = () => {
        const cell = scope.querySelector(`.rui-calendar-day[data-date="${iso}"]`);
        if (!cell) return false;
        setRoving(scope, iso);
        cell.focus();
        return true;
      };
      if (focusIn()) return;
      navigateTo(iso, origin);
      focusIn();
    };
    const fillDayEvents = (container, dayEvents, iso, expanded) => {
      container.replaceChildren();
      const shown = expanded ? dayEvents : dayEvents.slice(0, maxEvents);
      for (const evt of shown) {
        const attrs = {
          class: "rui-calendar-event",
          "data-tone": evt.tone ?? "primary",
          "data-event-id": evt.id,
          title: evt.time ? `${evt.time} — ${evt.title}` : evt.title
        };
        if (!eventClickable) {
          container.append(el("span", attrs, [evt.title]));
          continue;
        }
        const chip = el("button", { ...attrs, type: "button", style: CAL_EVENT_BUTTON }, [evt.title]);
        chip.onclick = (event) => {
          event.stopPropagation();
          helpers.invoke(props.onEventClick, evt.id, {
            id: evt.id,
            date: evt.date,
            title: evt.title,
            time: evt.time,
            tone: evt.tone
          });
        };
        container.append(chip);
      }
      if (!expanded && dayEvents.length > maxEvents) {
        const more = el("button", {
          type: "button",
          class: "rui-calendar-event-more",
          style: CAL_MORE_BUTTON
        }, [`+${dayEvents.length - maxEvents} more`]);
        more.onclick = (event) => {
          event.stopPropagation();
          expandedSlot.set(iso);
          const live = event.currentTarget.closest(".rui-calendar-day-events");
          if (live) fillDayEvents(live, dayEvents, iso, true);
        };
        container.append(more);
      }
    };
    const buildGrid = (grid, ref) => {
      grid.replaceChildren();
      const cells = cellsFor(ref);
      const expanded = expandedSlot.get();
      const isoList = cells.map((c) => formatIsoDate(c.date));
      const enabled = (iso) => !isDisabled(iso);
      const rovingIso = [focusSlot.get(), valueIso, todayIso].find((iso) => iso && isoList.includes(iso) && enabled(iso)) ?? isoList.find((iso, i) => cells[i].inMonth && enabled(iso)) ?? isoList[0] ?? "";
      let row = null;
      cells.forEach((cell, i) => {
        if (i % 7 === 0) {
          row = el("div", { class: "rui-calendar-row", role: "row", style: "display:contents" });
          grid.append(row);
        }
        const iso = isoList[i];
        const disabled = isDisabled(iso);
        const dayCell = el("div", {
          class: "rui-calendar-day",
          role: "gridcell",
          "data-date": iso,
          "data-in-month": cell.inMonth ? "true" : "false",
          "data-today": iso === todayIso ? "true" : "false",
          "data-selected": iso === valueIso ? "true" : "false",
          "data-disabled": disabled ? "true" : null,
          "aria-selected": iso === valueIso ? "true" : "false",
          "aria-current": iso === todayIso ? "date" : null,
          "aria-disabled": disabled ? "true" : null,
          "aria-label": cell.date.toLocaleDateString(void 0, {
            weekday: "long",
            year: "numeric",
            month: "long",
            day: "numeric"
          }),
          tabindex: iso === rovingIso ? "0" : "-1",
          style: disabled ? CAL_DISABLED_DAY : null
        });
        dayCell.append(el("span", { class: "rui-calendar-daynumber" }, [String(cell.date.getDate())]));
        const dayEvents = eventsByDate.get(iso) ?? [];
        if (dayEvents.length > 0) {
          const evts = el("div", { class: "rui-calendar-day-events" });
          fillDayEvents(evts, dayEvents, iso, expanded === iso);
          dayCell.append(evts);
        }
        if (!disabled) {
          dayCell.onclick = (event) => {
            const origin = event.currentTarget;
            setRoving(liveScope(origin), iso);
            selectDay(iso, origin);
          };
        }
        dayCell.onkeydown = (event) => {
          const origin = event.currentTarget;
          const from = parseLocalIso(origin.getAttribute("data-date") ?? "") ?? cell.date;
          let target = null;
          switch (event.key) {
            case "ArrowLeft":
              target = addDays(from, -1);
              break;
            case "ArrowRight":
              target = addDays(from, 1);
              break;
            case "ArrowUp":
              target = addDays(from, -7);
              break;
            case "ArrowDown":
              target = addDays(from, 7);
              break;
            case "Home":
              target = startOfWeek(from, weekStartsOn);
              break;
            case "End":
              target = addDays(startOfWeek(from, weekStartsOn), 6);
              break;
            case "PageUp":
              target = new Date(from.getFullYear(), from.getMonth() - 1, from.getDate());
              break;
            case "PageDown":
              target = new Date(from.getFullYear(), from.getMonth() + 1, from.getDate());
              break;
            case "Enter":
            case " ":
              event.preventDefault();
              if (!disabled) selectDay(formatIsoDate(from), origin);
              return;
            default:
              return;
          }
          event.preventDefault();
          moveFocus(origin, target);
        };
        (row ?? grid).append(dayCell);
      });
    };
    const applyView = (scope, ref) => {
      const cells = cellsFor(ref);
      const title = scope.querySelector(".rui-calendar-title");
      if (title) {
        title.textContent = view === "week" && cells.length > 0 ? rangeLabel(cells[0].date, cells[cells.length - 1].date) : ref.toLocaleDateString(void 0, { month: "long", year: "numeric" });
      }
      const grid = scope.querySelector(".rui-calendar-grid");
      if (grid) {
        buildGrid(grid, ref);
        const label = title?.textContent ?? "";
        if (label) grid.setAttribute("aria-label", label);
      }
    };
    const header = el("div", { class: "rui-calendar-header" });
    header.append(el("div", { class: "rui-calendar-title" }));
    if (!asBoolean(props.hideNav)) {
      const nav = el("div", { class: "rui-calendar-nav" });
      const navButton = (label, aria, onActivate) => {
        const btn = el("button", {
          type: "button",
          class: "rui-data-grid-page-button rui-calendar-nav-button",
          "aria-label": aria
        }, [label]);
        btn.onclick = (event) => onActivate(event.currentTarget);
        return btn;
      };
      nav.append(navButton(
        "‹",
        view === "week" ? "Previous week" : "Previous month",
        (origin) => navigateTo(shiftAnchor(-1), origin)
      ));
      nav.append(navButton("Today", "Today", (origin) => navigateTo(todayIso, origin)));
      nav.append(navButton(
        "›",
        view === "week" ? "Next week" : "Next month",
        (origin) => navigateTo(shiftAnchor(1), origin)
      ));
      header.append(nav);
    }
    root.append(header);
    const weekRow = el("div", { class: "rui-calendar-weekrow", "aria-hidden": "true" });
    const dayLabels = ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"];
    for (let i = 0; i < 7; i += 1) {
      const label = dayLabels[(weekStartsOn + i) % 7] ?? "";
      weekRow.append(el("div", { class: "rui-calendar-weekday" }, [label]));
    }
    root.append(weekRow);
    root.append(el("div", { class: "rui-calendar-grid", "data-view": view, role: "grid" }));
    applyView(root, refDate);
    return root;
  }
};
function readFeedEntries(raw) {
  const out = [];
  for (const entry of asArray(raw)) {
    if (!entry || typeof entry !== "object") continue;
    const e = entry;
    out.push({
      title: asString(e.title),
      description: asString(e.description),
      actor: asString(e.actor),
      avatarSrc: asString(e.avatarSrc),
      href: asString(e.href),
      time: asString(e.time),
      icon: asString(e.icon),
      tone: asString(e.tone, "default"),
      meta: asString(e.meta)
    });
  }
  return out;
}
const FEED_TITLE_BUTTON = "border:0;background:none;padding:0;font:inherit;color:inherit;cursor:pointer;text-align:left";
function renderFeed(klass, items, opts) {
  const root = el("ol", { class: klass, "data-variant": opts.variant });
  const clickable = typeof opts.onItemClick === "function";
  items.forEach((entry, idx) => {
    const li = el("li", {
      class: `${klass}-item`,
      "data-tone": entry.tone,
      "data-clickable": clickable || entry.href ? "true" : null
    });
    const marker = el("span", { class: `${klass}-marker` });
    const avatar = sanitiseImageSrc(entry.avatarSrc);
    if (avatar) {
      marker.append(el("img", { class: `${klass}-avatar`, src: avatar, alt: "", style: CAL_AVATAR }));
    } else {
      const iconNode = renderIcon(entry.icon, { className: `${klass}-icon` });
      if (iconNode) marker.append(iconNode);
    }
    li.append(marker);
    const body = el("div", { class: `${klass}-body` });
    const head = el("div", { class: `${klass}-head` });
    if (entry.actor) head.append(el("span", { class: `${klass}-actor` }, [entry.actor]));
    if (entry.href) {
      head.append(el("a", {
        class: `${klass}-title`,
        href: sanitiseHref(entry.href)
      }, [entry.title]));
    } else if (clickable) {
      const btn = el("button", {
        type: "button",
        class: `${klass}-title`,
        style: FEED_TITLE_BUTTON
      }, [entry.title]);
      btn.onclick = () => opts.helpers.invoke(opts.onItemClick, idx, entry);
      head.append(btn);
    } else {
      head.append(el("span", { class: `${klass}-title` }, [entry.title]));
    }
    if (entry.time) head.append(el("span", { class: `${klass}-time` }, [entry.time]));
    body.append(head);
    if (entry.description) {
      body.append(el("p", { class: `${klass}-description` }, [entry.description]));
    }
    if (entry.meta) {
      body.append(el("span", { class: `${klass}-meta` }, [entry.meta]));
    }
    li.append(body);
    root.append(li);
  });
  if (items.length === 0 && !opts.loading) {
    root.append(el("li", { class: `${klass}-empty` }, [opts.emptyLabel]));
  }
  if (opts.loading) {
    const li = el("li", { class: `${klass}-loading`, role: "status", "aria-live": "polite" });
    const spin = renderIcon("spinner", { className: `${klass}-spin` });
    if (spin) li.append(spin);
    li.append(el("span", {}, [opts.loaderLabel]));
    root.append(li);
  }
  return root;
}
const ActivityLog = {
  name: "ActivityLog",
  description: 'Purpose-built feed of user/system activity. Each entry has `actor`, `title`, `description?`, `time?`, `icon?`, `avatarSrc?`, `tone?`, `href?`, and optional `meta` (IP, browser, request id). An entry\'s `href` renders its title as a link; `onItemClick` makes every title a button. Use `variant="audit"` to render `meta` in monospace for security/admin trails. Pass items as `{actor, title, description, time, icon, tone, avatarSrc, href, meta}` objects.',
  props: [
    { name: "items", type: "object[]" },
    // Kept to what the stylesheet actually does: it used to promise a whole
    // "monospace voice with meta column" and delivered a monospace meta chip.
    { name: "variant", aliases: ["tone"], type: "string", optional: true, enum: ["default", "audit"], description: "audit = monospace `meta` styling for security/admin trails" },
    { name: "emptyLabel", type: "string", optional: true, description: "Message shown when `items` is empty (default `No activity yet`)" },
    { name: "onItemClick", type: "callable", optional: true, description: "Callable fired with (index, item) when an entry title is activated" },
    { name: "loading", type: "boolean", optional: true, description: "Append a loading row while older activity is being fetched" },
    { name: "loaderLabel", type: "string", optional: true, description: "Label for the loading row (default `Loading activity…`)" }
  ],
  render: (_node, props, helpers) => {
    const variant = asString(props.variant, "default");
    const klass = variant === "audit" ? "rui-audit-trail" : "rui-activity-log";
    return renderFeed(klass, readFeedEntries(props.items), {
      variant,
      emptyLabel: asString(props.emptyLabel, "No activity yet"),
      loading: asBoolean(props.loading),
      loaderLabel: asString(props.loaderLabel, "Loading activity…"),
      onItemClick: props.onItemClick,
      helpers
    });
  }
};
const CMP_STICKY_CELL = "position:sticky;left:0;z-index:1;background:var(--rui-color-surface, var(--rui-color-bg))";
const ComparisonTable = {
  name: "ComparisonTable",
  description: "Feature/spec comparison table — generic counterpart of `PricingTable`. Pass `columns` (e.g. plan/product names) and `rows` of `{label, values}` where `values` aligns 1-to-1 with `columns`. Each value can be a boolean (✓/—), a string, or a node. Rows sharing a `group` are kept together under one group header.",
  props: [
    { name: "columns", type: "string[]", description: "Column headers" },
    { name: "rows", type: "object[]", description: "Array of {label, values, hint?, group?} entries" },
    { name: "highlightColumn", type: "number", optional: true, description: "0-indexed column to visually emphasise" },
    { name: "featureLabel", type: "string", optional: true, description: "Header of the first (row-label) column — default `Feature`" },
    { name: "caption", type: "string", optional: true, aliases: ["ariaLabel"], description: "Table caption; also its accessible name" },
    { name: "stickyFirstColumn", type: "boolean", optional: true, description: "Keep the feature labels visible while the plan columns scroll horizontally" }
  ],
  render: (_node, props, helpers) => {
    const columns = asArray(props.columns).map((c) => asString(c));
    const rows = asArray(props.rows).map((entry) => {
      const r = entry ?? {};
      return {
        label: asString(r.label),
        values: asArray(r.values),
        hint: asString(r.hint),
        group: asString(r.group)
      };
    });
    const highlightIdx = Math.floor(asNumber(props.highlightColumn, -1));
    const stickyFirst = asBoolean(props.stickyFirstColumn);
    const root = el("div", {
      class: "rui-comparison-table",
      "data-sticky-first": stickyFirst ? "true" : null
    });
    root.style.overflowX = "auto";
    const table = el("table");
    const caption = asString(props.caption);
    if (caption) table.append(el("caption", { class: "rui-comparison-table-caption" }, [caption]));
    const thead = el("thead");
    const headRow = el("tr");
    const featureTh = el("th", {
      scope: "col",
      class: "rui-comparison-table-feature"
    }, [asString(props.featureLabel, "Feature")]);
    if (stickyFirst) featureTh.setAttribute("style", `${CMP_STICKY_CELL};z-index:2`);
    headRow.append(featureTh);
    columns.forEach((col, c) => {
      headRow.append(el("th", {
        scope: "col",
        "data-highlight": c === highlightIdx ? "true" : null
      }, [col]));
    });
    thead.append(headRow);
    table.append(thead);
    const buckets = /* @__PURE__ */ new Map();
    for (const row of rows) {
      const bucket = buckets.get(row.group);
      if (bucket) bucket.push(row);
      else buckets.set(row.group, [row]);
    }
    const ordered = Array.from(buckets.values()).flat();
    const tbody = el("tbody");
    let currentGroup = "";
    for (const row of ordered) {
      if (row.group && row.group !== currentGroup) {
        currentGroup = row.group;
        const groupRow = el("tr", { class: "rui-comparison-table-group" });
        groupRow.append(el("td", { colspan: String(columns.length + 1) }, [row.group]));
        tbody.append(groupRow);
      }
      const tr = el("tr");
      const labelCell = el("td", { class: "rui-comparison-table-feature" });
      if (stickyFirst) labelCell.setAttribute("style", CMP_STICKY_CELL);
      labelCell.append(el("div", { class: "rui-comparison-table-feature-label" }, [row.label]));
      if (row.hint) labelCell.append(el("div", { class: "rui-comparison-table-feature-hint" }, [row.hint]));
      tr.append(labelCell);
      for (let c = 0; c < columns.length; c += 1) {
        const value = row.values[c];
        const td = el("td", { "data-highlight": c === highlightIdx ? "true" : null });
        if (value === true) {
          td.append(el("span", { class: "rui-comparison-yes" }, ["✓"]));
        } else if (value === false || value === null || value === void 0) {
          td.append(el("span", { class: "rui-comparison-no" }, ["—"]));
        } else if (isComponentNode(value)) {
          td.append(helpers.renderNode(value));
        } else {
          td.textContent = asString(value);
        }
        tr.append(td);
      }
      tbody.append(tr);
    }
    table.append(tbody);
    root.append(table);
    return root;
  }
};
const InfiniteList = {
  name: "InfiniteList",
  description: "Vertical list that fires `onLoadMore` when the user scrolls near the bottom. Pass already-rendered child nodes as `items`; wire `onLoadMore` to an `action` that awaits a `$mutation` or `$query` (e.g. `await loadMore.invoke()`) and appends to the bound state. Use `loading=true` to show the spinner row, `hasMore=false` to suppress further loads, and `error` + `onRetry` when a page fails.",
  props: [
    { name: "items", type: "Node[]", description: "Already-rendered child nodes" },
    { name: "onLoadMore", type: "callable", optional: true, description: "Callable fired when the sentinel scrolls into view" },
    { name: "loading", type: "boolean", optional: true },
    { name: "hasMore", type: "boolean", optional: true, description: "Default true — set false to hide the sentinel" },
    { name: "loaderLabel", type: "string", optional: true, description: "Label rendered while loading (default `Loading…`)" },
    { name: "error", type: "string", optional: true, description: "Failure message shown instead of the loader, with a Retry button" },
    { name: "onRetry", type: "callable", optional: true, description: "Callable fired by the Retry button (defaults to `onLoadMore`)" },
    { name: "emptyLabel", type: "string", optional: true, description: "Message shown when there are no items (default `No results`)" },
    { name: "rootMargin", type: "string", optional: true, description: "Prefetch distance for the scroll sentinel (default `200px`)" },
    { name: "threshold", type: "number", optional: true, description: "Fraction of the sentinel that must be visible, 0–1 (default 0)" },
    { name: "retryLabel", type: "string", optional: true, description: "Label for the retry button (default `Retry`)" }
  ],
  render: (_node, props, helpers) => {
    const root = el("div", { class: "rui-infinite-list" });
    const list = el("div", { class: "rui-infinite-list-body" });
    const items = asArray(props.items);
    for (const child of items) list.append(helpers.renderNode(child));
    root.append(list);
    const hasMore = props.hasMore === void 0 ? true : asBoolean(props.hasMore);
    const loading = asBoolean(props.loading);
    const errorText = asString(props.error);
    const loaderLabel = asString(props.loaderLabel, "Loading…");
    const canLoad = typeof props.onLoadMore === "function";
    if (items.length === 0 && !loading && !errorText) {
      root.append(el("div", { class: "rui-infinite-list-empty" }, [asString(props.emptyLabel, "No results")]));
    }
    const rootMargin = sanitiseCssLength(props.rootMargin, "200px");
    const threshold = Math.min(1, Math.max(0, asNumber(props.threshold, 0)));
    const watchSlot = helpers.useInstanceState("sentinel-watch", {
      node: null,
      observer: null,
      key: ""
    });
    if (hasMore || loading || errorText) {
      const sentinel = el("div", {
        class: "rui-infinite-list-sentinel",
        role: "status",
        "aria-live": "polite",
        "aria-busy": loading ? "true" : null
      });
      if (errorText) {
        sentinel.append(el("span", { class: "rui-infinite-list-error" }, [errorText]));
        if (typeof props.onRetry === "function" || canLoad) {
          const retry = el("button", {
            type: "button",
            class: "rui-infinite-list-load-more"
          }, [asString(props.retryLabel, "Retry")]);
          retry.onclick = () => helpers.invoke(props.onRetry ?? props.onLoadMore);
          sentinel.append(retry);
        }
      } else if (canLoad) {
        const btn = el("button", {
          type: "button",
          class: "rui-infinite-list-load-more",
          disabled: loading ? "" : null,
          "aria-disabled": loading ? "true" : null
        });
        if (loading) {
          const spin = renderIcon("spinner", { className: "rui-infinite-list-spin" });
          if (spin) btn.append(spin);
        }
        btn.append(el("span", {}, [loading ? loaderLabel : "Load more"]));
        btn.onclick = () => {
          if (!loading) helpers.invoke(props.onLoadMore);
        };
        sentinel.append(btn);
      } else if (loading) {
        const spin = renderIcon("spinner", { className: "rui-infinite-list-spin" });
        if (spin) sentinel.append(spin);
        sentinel.append(el("span", {}, [loaderLabel]));
      }
      root.append(sentinel);
      const wantObserver = hasMore && !loading && !errorText && canLoad && typeof IntersectionObserver !== "undefined";
      const callback = props.onLoadMore;
      const key = `${rootMargin}|${threshold}`;
      deferToPaint(() => {
        const watch = watchSlot.get();
        const live = sentinel.isConnected ? sentinel : watch.node?.isConnected ? watch.node : null;
        if (!live) {
          if (watch.observer) {
            watch.observer.disconnect();
            watchSlot.set({ node: null, observer: null, key: "" });
          }
          return;
        }
        if (!wantObserver) {
          watch.observer?.disconnect();
          watchSlot.set({ node: live, observer: null, key: "" });
          return;
        }
        if (watch.observer && watch.node === live && watch.key === key) return;
        watch.observer?.disconnect();
        const observer = new IntersectionObserver((entries) => {
          for (const entry of entries) {
            if (entry.isIntersecting) {
              helpers.invoke(callback);
              break;
            }
          }
        }, { rootMargin, threshold });
        observer.observe(live);
        watchSlot.set({ node: live, observer, key });
        helpers.registerDisposer(() => observer.disconnect(), "infinite-observer");
      });
    }
    return root;
  }
};
const SAFE_MEDIA_SCHEMES = /^(https?:|blob:|data:(audio|video)\/)/i;
const SAFE_TRACK_SCHEMES = /^(https?:|blob:|data:text\/vtt)/i;
function sanitiseSubresource(raw, schemes) {
  const value = asString(raw).trim();
  if (!value) return "";
  if (value.startsWith("//")) return "";
  if (value.startsWith("/") || value.startsWith(".")) return value;
  return schemes.test(value) ? value : "";
}
const sanitiseMediaSrc = (raw) => sanitiseSubresource(raw, SAFE_MEDIA_SCHEMES);
const sanitiseTrackSrc = (raw) => sanitiseSubresource(raw, SAFE_TRACK_SCHEMES);
const TRACK_KINDS = ["subtitles", "captions", "descriptions", "chapters", "metadata"];
const MEDIA_OVERLAY_STYLE = "position:absolute;inset:0;display:flex;align-items:center;justify-content:center;gap:8px;padding:12px;text-align:center;color:#fff;font-size:13px;";
const MEDIA_PLAY_BUTTON_STYLE = "position:absolute;left:50%;top:50%;transform:translate(-50%,-50%);width:56px;height:56px;border:0;border-radius:50%;background:rgba(0,0,0,0.55);color:#fff;font-size:20px;cursor:pointer;display:inline-flex;align-items:center;justify-content:center;";
function toggleMediaPlayback(event, frameSelector) {
  const origin = event.currentTarget ?? event.target;
  const live = origin?.closest(frameSelector)?.querySelector("video, audio");
  if (!live) return;
  if (!live.paused) {
    live.pause();
    return;
  }
  if (typeof live.play !== "function") return;
  const started = live.play();
  if (started && typeof started.catch === "function") started.catch(() => {
  });
}
function wireMediaState(media, selectors, playing, endedRef, helpers) {
  const reflect = (event) => {
    const live = event.currentTarget ?? event.target;
    const isPlaying = !live.paused && !live.ended;
    playing.set(isPlaying);
    const btn = live.closest(selectors.root)?.querySelector(selectors.toggle);
    if (!btn) return;
    btn.setAttribute("data-playing", isPlaying ? "true" : "false");
    btn.setAttribute("aria-label", isPlaying ? "Pause" : "Play");
    const swap = renderIcon(isPlaying ? "pause" : "play");
    if (!swap) return;
    const previous = btn.querySelector(".rui-icon");
    if (previous) previous.replaceWith(swap);
    else btn.append(swap);
  };
  media.onplay = reflect;
  media.onpause = reflect;
  media.onended = (event) => {
    reflect(event);
    helpers.invoke(endedRef.get());
  };
}
function renderPlayToggle(className, playing, style) {
  const btn = el("button", {
    type: "button",
    class: className,
    "data-playing": playing ? "true" : "false",
    "aria-label": playing ? "Pause" : "Play",
    style
  });
  const icon = renderIcon(playing ? "pause" : "play");
  if (icon) btn.append(icon);
  return btn;
}
const VideoPlayer = {
  name: "VideoPlayer",
  description: "Themed native `<video>` wrapper. Pass a `src` URL (or `sources` array for multi-codec fallback) and optional `poster`. Standard controls are visible by default; `controls=false` swaps them for a single play/pause button (click the video too). `autoplay` only works alongside `muted` — browsers block unmuted autoplay. Add `tracks` for captions/subtitles (required for prerecorded video, WCAG 1.2.2). `onEnded` fires on completion; a missing, unsafe, or failing source shows `fallback` and calls `onError`. Use for product demos, tutorials, and any inline video.",
  props: [
    { name: "src", type: "string", optional: true, description: "Video URL (mp4 / webm / etc.)" },
    { name: "sources", type: "object[]", optional: true, description: "Array of {src, type} entries for multi-codec fallback" },
    { name: "poster", type: "string", optional: true, description: "Thumbnail image URL shown before playback" },
    { name: "caption", type: "string", optional: true },
    { name: "controls", type: "boolean", optional: true, description: "Show native controls (default true); false renders a play/pause button instead" },
    { name: "autoplay", type: "boolean", optional: true, description: "Requires `muted: true` — browsers block unmuted autoplay" },
    { name: "loop", type: "boolean", optional: true },
    { name: "muted", type: "boolean", optional: true },
    { name: "ratio", type: "string", optional: true, description: "Aspect ratio (default 16:9)" },
    { name: "tracks", type: "object[]", optional: true, description: "Caption/subtitle tracks: {src, label?, srclang?, kind?, default?} (kind: subtitles|captions|descriptions|chapters|metadata)" },
    { name: "onEnded", type: "callable", optional: true, aliases: ["onended"], description: "Callable invoked when playback reaches the end" },
    { name: "fallback", type: "string", optional: true, description: "Message shown when the source is missing/unsafe or fails to load" },
    { name: "onError", type: "callable", optional: true, aliases: ["onerror"], description: "Callable invoked when the video fails to load" }
  ],
  render: (_node, props, helpers) => {
    const root = el("figure", { class: "rui-video-player" });
    const ratio = parseRatio(asString(props.ratio, "16:9"));
    const playerWrap = el("div", {
      class: "rui-video-player-frame",
      style: `aspect-ratio:${ratio};`
    });
    const showControls = props.controls === void 0 ? true : asBoolean(props.controls);
    const video = el("video", {
      class: "rui-video-player-video",
      controls: showControls ? "" : null,
      autoplay: asBoolean(props.autoplay) ? "" : null,
      loop: asBoolean(props.loop) ? "" : null,
      muted: asBoolean(props.muted) ? "" : null,
      poster: sanitiseImageSrc(props.poster) || null,
      playsinline: "",
      preload: "metadata"
    });
    if (asBoolean(props.muted)) {
      video.muted = true;
      video.defaultMuted = true;
    }
    const sources = asArray(props.sources);
    if (sources.length > 0) {
      for (const raw of sources) {
        if (!raw || typeof raw !== "object") continue;
        const s = raw;
        const safeSrc = sanitiseMediaSrc(s.src);
        if (!safeSrc) continue;
        video.append(el("source", { src: safeSrc, type: asString(s.type) || null }));
      }
    } else {
      const safeSrc = sanitiseMediaSrc(props.src);
      if (safeSrc) video.setAttribute("src", safeSrc);
    }
    for (const raw of asArray(props.tracks)) {
      if (!raw || typeof raw !== "object") continue;
      const t = raw;
      const safeSrc = sanitiseTrackSrc(t.src);
      if (!safeSrc) continue;
      const kind = asString(t.kind, "subtitles");
      video.append(el("track", {
        src: safeSrc,
        kind: TRACK_KINDS.includes(kind) ? kind : "subtitles",
        label: asString(t.label) || null,
        srclang: asString(t.srclang) || null,
        default: asBoolean(t.default) ? "" : null
      }));
    }
    playerWrap.append(video);
    const playing = helpers.useInstanceState("playing", false);
    const endedRef = helpers.useInstanceState("onEnded", void 0);
    endedRef.set(props.onEnded);
    wireMediaState(
      video,
      { root: ".rui-video-player", toggle: ".rui-video-player-play" },
      playing,
      endedRef,
      helpers
    );
    const fallbackText = asString(props.fallback);
    const hasSource = video.hasAttribute("src") || video.querySelector("source") !== null;
    if (!hasSource) {
      playerWrap.append(renderMediaFallback(fallbackText || "No video source — check `src`."));
    }
    video.onerror = (event) => {
      const live = event.currentTarget ?? event.target;
      const frame = live?.closest(".rui-video-player-frame");
      if (frame && !frame.querySelector(".rui-video-player-empty")) {
        frame.append(renderMediaFallback(fallbackText || "This video could not be loaded."));
      }
      helpers.invoke(props.onError);
    };
    if (!showControls) {
      const playBtn = renderPlayToggle("rui-video-player-play", playing.get(), MEDIA_PLAY_BUTTON_STYLE);
      playBtn.onclick = (event) => toggleMediaPlayback(event, ".rui-video-player-frame");
      video.onclick = (event) => toggleMediaPlayback(event, ".rui-video-player-frame");
      playerWrap.append(playBtn);
    }
    root.append(playerWrap);
    const caption = asString(props.caption);
    if (caption) root.append(el("figcaption", { class: "rui-video-player-caption" }, [caption]));
    return root;
  }
};
function renderMediaFallback(label) {
  const box = el("div", { class: "rui-video-player-empty", style: MEDIA_OVERLAY_STYLE });
  const icon = renderIcon("circle-exclamation", { className: "rui-video-player-empty-icon" });
  if (icon) box.append(icon);
  box.append(el("span", { class: "rui-video-player-empty-text" }, [label]));
  return box;
}
let audioIdSeq = 0;
const AudioPlayer = {
  name: "AudioPlayer",
  description: "Themed native `<audio>` wrapper with a title, optional artist, and standard transport controls. Pass `src` (or `sources`) plus a `title` — the title names the player for assistive tech. `controls=false` replaces the native bar with a single play/pause button so the row still works. `autoplay` needs `muted: true` to be allowed by the browser. `onEnded` fires on completion (advance a playlist, mark an episode listened). Use for podcasts, voice notes, and demo audio.",
  props: [
    { name: "src", type: "string", optional: true, description: "Audio URL (mp3 / ogg / wav / etc.)" },
    { name: "sources", type: "object[]", optional: true, description: "Array of {src, type} entries for multi-codec fallback" },
    { name: "title", type: "string", optional: true },
    { name: "artist", type: "string", optional: true },
    { name: "controls", type: "boolean", optional: true, description: "Show native controls (default true); false renders a play/pause button instead" },
    { name: "autoplay", type: "boolean", optional: true, description: "Requires `muted: true` — browsers block unmuted autoplay" },
    { name: "loop", type: "boolean", optional: true },
    { name: "icon", type: "string", optional: true, description: "Leading icon (default `music`)" },
    { name: "muted", type: "boolean", optional: true },
    { name: "onEnded", type: "callable", optional: true, aliases: ["onended"], description: "Callable invoked when playback reaches the end" }
  ],
  render: (_node, props, helpers) => {
    const root = el("div", { class: "rui-audio-player" });
    const meta = el("div", { class: "rui-audio-player-meta" });
    const iconNode = renderIcon(asString(props.icon, "music"), { className: "rui-audio-player-icon" });
    if (iconNode) meta.append(iconNode);
    const text = el("div", { class: "rui-audio-player-text" });
    const idSlot = helpers.useInstanceState("labelId", "");
    if (!idSlot.get()) idSlot.set(`rui-audio-player-${audioIdSeq += 1}`);
    const baseId = idSlot.get();
    const title = asString(props.title);
    if (title) text.append(el("div", { class: "rui-audio-player-title", id: `${baseId}-title` }, [title]));
    const artist = asString(props.artist);
    if (artist) text.append(el("div", { class: "rui-audio-player-artist", id: `${baseId}-artist` }, [artist]));
    meta.append(text);
    root.append(meta);
    const labelledBy = [title ? `${baseId}-title` : "", artist ? `${baseId}-artist` : ""].filter(Boolean).join(" ");
    const showControls = props.controls === void 0 ? true : asBoolean(props.controls);
    const audio = el("audio", {
      class: "rui-audio-player-audio",
      controls: showControls ? "" : null,
      autoplay: asBoolean(props.autoplay) ? "" : null,
      loop: asBoolean(props.loop) ? "" : null,
      muted: asBoolean(props.muted) ? "" : null,
      preload: "metadata",
      "aria-labelledby": labelledBy || null,
      "aria-label": labelledBy ? null : "Audio player"
    });
    if (asBoolean(props.muted)) {
      audio.muted = true;
      audio.defaultMuted = true;
    }
    const sources = asArray(props.sources);
    if (sources.length > 0) {
      for (const raw of sources) {
        if (!raw || typeof raw !== "object") continue;
        const s = raw;
        const safeSrc = sanitiseMediaSrc(s.src);
        if (!safeSrc) continue;
        audio.append(el("source", { src: safeSrc, type: asString(s.type) || null }));
      }
    } else {
      const safeSrc = sanitiseMediaSrc(props.src);
      if (safeSrc) audio.setAttribute("src", safeSrc);
    }
    const playing = helpers.useInstanceState("playing", false);
    const endedRef = helpers.useInstanceState("onEnded", void 0);
    endedRef.set(props.onEnded);
    wireMediaState(
      audio,
      { root: ".rui-audio-player", toggle: ".rui-audio-player-toggle" },
      playing,
      endedRef,
      helpers
    );
    if (!showControls) {
      const toggle = renderPlayToggle("rui-audio-player-toggle", playing.get(), null);
      toggle.onclick = (event) => toggleMediaPlayback(event, ".rui-audio-player");
      root.append(toggle);
    }
    root.append(audio);
    return root;
  }
};
function renderCarouselSlide(item, helpers) {
  if (item === null || item === void 0) return null;
  if (isComponentNode(item)) {
    return helpers.renderNode(item);
  }
  if (typeof item === "string") {
    const safeSrc = sanitiseImageSrc(item);
    if (!safeSrc) return null;
    return el("img", { src: safeSrc, alt: "", loading: "lazy", class: "rui-carousel-image" });
  }
  if (typeof item === "object") {
    const obj = item;
    const safeSrc = sanitiseImageSrc(obj.src);
    if (safeSrc) {
      const wrap = el("figure", { class: "rui-carousel-figure" });
      wrap.append(el("img", {
        src: safeSrc,
        alt: asString(obj.alt ?? obj.caption ?? obj.title),
        loading: "lazy",
        class: "rui-carousel-image"
      }));
      const captionText = asString(obj.caption ?? obj.title);
      if (captionText) {
        wrap.append(el("figcaption", { class: "rui-carousel-caption" }, [captionText]));
      }
      return wrap;
    }
  }
  return helpers.renderNode(item);
}
const SWIPE_THRESHOLD = 40;
function markCarouselSlide(slide, isActive) {
  if (isActive) {
    slide.removeAttribute("aria-hidden");
    slide.removeAttribute("inert");
  } else {
    slide.setAttribute("aria-hidden", "true");
    slide.setAttribute("inert", "");
  }
}
function setTrackOffset(track, index) {
  if (index <= 0) track.style.removeProperty("transform");
  else track.style.transform = `translateX(${index * -100}%)`;
}
const Carousel = {
  name: "Carousel",
  description: "Horizontal slider with prev/next buttons, dot navigation, arrow-key and swipe support. Each child slide takes full width. Slides may be Component nodes (Image, Card, MediaCard, …), URL strings, or plain `{src, alt, caption?}` image objects — bare image objects are auto-wrapped into a captioned figure. Bind a `$variable` to `activeIndex` to drive or observe the active slide, or use `onChange`. `autoplay` rotates every `interval` ms (paused while hovered or focused) for hero banners. Give it a `label` so screen readers can tell two carousels apart, and `empty`/`emptyText` for the no-slides case. The active slide is preserved across re-renders via instance state.",
  props: [
    { name: "items", type: "Node[]", description: "Slide nodes, image URLs, or {src, alt, caption?} objects" },
    { name: "activeIndex", type: "number", optional: true, description: "0-indexed active slide (default 0); bind a $variable to control it" },
    { name: "ratio", type: "string", optional: true, description: "Aspect ratio of the frame (default `16:9`)" },
    { name: "showDots", type: "boolean", optional: true, description: "Show indicator dots (default true)" },
    { name: "showArrows", type: "boolean", optional: true, description: "Show prev/next arrows (default true)" },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"], description: "Callable invoked with the new slide index whenever it changes" },
    { name: "autoplay", type: "boolean", optional: true, description: "Rotate slides automatically (pauses on hover / focus)" },
    { name: "interval", type: "number", optional: true, description: "Autoplay delay in ms (default 5000, minimum 1500)" },
    { name: "label", type: "string", optional: true, description: "Accessible name for the carousel region" },
    { name: "empty", type: "Node", optional: true, description: "Node rendered when `items` is empty" },
    { name: "emptyText", type: "string", optional: true, description: "Message shown when `items` is empty (default `No slides to show.`)" }
  ],
  render: (node, props, helpers) => {
    const items = asArray(props.items);
    const count = items.length;
    const label = asString(props.label);
    const root = el("div", {
      class: "rui-carousel",
      role: "group",
      "aria-roledescription": "carousel",
      "aria-label": label || null
    });
    if (count === 0) {
      const custom = props.empty === void 0 ? null : helpers.renderNode(props.empty);
      root.append(custom ?? el("div", { class: "rui-carousel-empty" }, [
        asString(props.emptyText, "No slides to show.")
      ]));
      return root;
    }
    const indexStateName = node.argMeta?.[1]?.stateRef;
    const slot = helpers.useInstanceState("active", 0);
    const seed = helpers.useInstanceState("activeSeed", null);
    if (props.activeIndex !== void 0) {
      const wanted = Math.floor(asNumber(props.activeIndex, 0));
      if (seed.get() !== wanted) {
        seed.set(wanted);
        slot.set(wanted);
      }
    }
    let active = slot.get();
    if (!Number.isInteger(active) || active < 0 || active >= count) {
      active = 0;
      slot.set(active);
    }
    const showDots = props.showDots === void 0 ? true : asBoolean(props.showDots);
    const showArrows = props.showArrows === void 0 ? true : asBoolean(props.showArrows);
    const ratio = parseRatio(asString(props.ratio, "16:9"));
    const frame = el("div", {
      class: "rui-carousel-frame",
      style: `aspect-ratio:${ratio};`,
      // Only claim a tab stop when there is no other way in — with arrows or
      // dots on screen, focusing one of those is enough to reach the keys.
      tabindex: count > 1 && !showArrows && !showDots ? "0" : null
    });
    const track = el("div", { class: "rui-carousel-track" });
    setTrackOffset(track, active);
    items.forEach((item, i) => {
      const slide = el("div", {
        class: "rui-carousel-slide",
        role: "group",
        "aria-roledescription": "slide",
        "aria-label": `${i + 1} of ${count}`,
        "aria-hidden": i === active ? null : "true",
        inert: i === active ? null : ""
      });
      const rendered = renderCarouselSlide(item, helpers);
      if (rendered) slide.append(rendered);
      track.append(slide);
    });
    frame.append(track);
    const applySlide = (liveRoot, index) => {
      const liveTrack = liveRoot.querySelector(".rui-carousel-track");
      if (liveTrack) setTrackOffset(liveTrack, index);
      const liveFrame = liveRoot.querySelector(".rui-carousel-frame");
      if (liveFrame && liveFrame.scrollLeft !== 0) liveFrame.scrollLeft = 0;
      liveRoot.querySelectorAll(".rui-carousel-slide").forEach((slide, i) => {
        markCarouselSlide(slide, i === index);
      });
      liveRoot.querySelectorAll(".rui-carousel-dot").forEach((dot, i) => {
        dot.setAttribute("data-active", i === index ? "true" : "false");
        dot.setAttribute("aria-current", i === index ? "true" : "false");
      });
    };
    const move = (origin, next) => {
      const clamped = (next % count + count) % count;
      const previous = slot.get();
      slot.set(clamped);
      if (indexStateName) helpers.setState(indexStateName, clamped);
      const liveRoot = origin.closest(".rui-carousel");
      if (liveRoot) applySlide(liveRoot, clamped);
      if (previous !== clamped) helpers.invoke(props.onChange, clamped);
    };
    if (showArrows && count > 1) {
      const prev = el("button", {
        type: "button",
        class: "rui-carousel-arrow",
        "data-direction": "prev",
        "aria-label": "Previous slide"
      });
      const prevIcon = renderIcon("chevron-left");
      if (prevIcon) prev.append(prevIcon);
      const next = el("button", {
        type: "button",
        class: "rui-carousel-arrow",
        "data-direction": "next",
        "aria-label": "Next slide"
      });
      const nextIcon = renderIcon("chevron-right");
      if (nextIcon) next.append(nextIcon);
      prev.onclick = (event) => move(event.currentTarget, slot.get() - 1);
      next.onclick = (event) => move(event.currentTarget, slot.get() + 1);
      frame.append(prev, next);
    }
    if (count > 1) {
      const dragFrom = helpers.useInstanceState("dragX", null);
      frame.onpointerdown = (event) => {
        if (event.pointerType === "mouse" && event.button !== 0) return;
        dragFrom.set(event.clientX);
      };
      frame.onpointerup = (event) => {
        const startX = dragFrom.get();
        dragFrom.set(null);
        if (startX === null) return;
        const dx = event.clientX - startX;
        if (Math.abs(dx) < SWIPE_THRESHOLD) return;
        move(event.currentTarget, slot.get() + (dx < 0 ? 1 : -1));
      };
      frame.onpointercancel = () => dragFrom.set(null);
      root.onkeydown = (event) => {
        const target = event.target;
        if (target?.closest("input,textarea,select,[contenteditable='true']")) return;
        const origin = event.currentTarget ?? event.target;
        if (event.key === "ArrowLeft") move(origin, slot.get() - 1);
        else if (event.key === "ArrowRight") move(origin, slot.get() + 1);
        else if (event.key === "Home") move(origin, 0);
        else if (event.key === "End") move(origin, count - 1);
        else return;
        event.preventDefault();
      };
    }
    root.append(frame);
    if (showDots && count > 1) {
      const dots = el("div", { class: "rui-carousel-dots" });
      items.forEach((_item, i) => {
        const dot = el("button", {
          type: "button",
          class: "rui-carousel-dot",
          "data-active": i === active ? "true" : "false",
          "aria-current": i === active ? "true" : "false",
          "aria-label": `Go to slide ${i + 1}`
        });
        dot.onclick = (event) => move(event.currentTarget, i);
        dots.append(dot);
      });
      root.append(dots);
    }
    if (asBoolean(props.autoplay) && count > 1) {
      const interval = Math.max(1500, Math.floor(asNumber(props.interval, 5e3)));
      deferToPaint(() => {
        if (!root.isConnected) return;
        const timer = setInterval(() => {
          if (!root.isConnected) {
            clearInterval(timer);
            return;
          }
          const doc = root.getRootNode();
          if (root.contains(doc.activeElement)) return;
          try {
            if (root.matches(":hover")) return;
          } catch {
          }
          move(root, slot.get() + 1);
        }, interval);
        helpers.registerDisposer(() => clearInterval(timer), "rui-carousel-autoplay");
      });
    }
    return root;
  }
};
const GALLERY_FIT = ["cover", "contain"];
const Gallery = {
  name: "Gallery",
  description: 'Responsive image grid. Pass items as plain URL strings, `{src, alt, caption?}` objects, or `Image(...)` nodes (rendered in full, so their own `fit`/`fallback`/`srcset` apply). `fit: "contain"` stops logos and screenshots being centre-cropped. When `onSelect` is provided each tile becomes a button; bind it through an Action that opens a `Lightbox`. `empty`/`emptyText` cover the no-results case.',
  props: [
    { name: "items", type: "any[]" },
    { name: "columns", type: "number", optional: true, description: "Preferred column count 1–6 (default auto)" },
    { name: "ratio", type: "string", optional: true, description: "Per-tile aspect ratio (default `1:1`)" },
    { name: "onSelect", type: "callable", optional: true, description: "Callable fired when a tile is clicked" },
    { name: "fit", type: "string", optional: true, enum: GALLERY_FIT, description: "How each image fills its tile (default `cover`)" },
    { name: "label", type: "string", optional: true, description: "Accessible name for the grid" },
    { name: "empty", type: "Node", optional: true, description: "Node rendered when `items` is empty" },
    { name: "emptyText", type: "string", optional: true, description: "Message shown when `items` is empty (default `No images to show.`)" }
  ],
  render: (_node, props, helpers) => {
    const items = asArray(props.items);
    const rawColumns = props.columns === void 0 || props.columns === null ? NaN : asNumber(props.columns, NaN);
    const columns = Number.isFinite(rawColumns) ? Math.max(1, Math.min(6, Math.floor(rawColumns))) : null;
    const fit = asString(props.fit, "cover") === "contain" ? "contain" : "cover";
    const ratio = parseRatio(asString(props.ratio, "1:1"));
    const label = asString(props.label);
    const root = el("div", {
      class: "rui-gallery",
      "data-columns": columns === null ? null : String(columns),
      "data-fit": fit,
      role: label ? "group" : null,
      "aria-label": label || null
    });
    if (items.length === 0) {
      const custom = props.empty === void 0 ? null : helpers.renderNode(props.empty);
      root.append(custom ?? el("div", { class: "rui-gallery-empty" }, [
        asString(props.emptyText, "No images to show.")
      ]));
      return root;
    }
    const clickable = typeof props.onSelect === "function";
    items.forEach((raw, i) => {
      const tile = el(clickable ? "button" : "figure", {
        type: clickable ? "button" : null,
        class: "rui-gallery-tile",
        "data-clickable": clickable ? "true" : null,
        style: `aspect-ratio:${ratio};`,
        "data-index": String(i)
      });
      if (isComponentNode(raw)) {
        tile.append(helpers.renderNode(raw));
      } else {
        const { src, alt, caption } = extractGalleryItem(raw);
        const safeSrc = sanitiseImageSrc(src);
        if (safeSrc) {
          tile.append(el("img", { src: safeSrc, alt, loading: "lazy", style: `object-fit:${fit};` }));
        } else {
          const placeholder = renderIcon("image", { className: "rui-gallery-placeholder" });
          if (placeholder) tile.append(placeholder);
        }
        if (caption) {
          tile.append(el("span", { class: "rui-gallery-caption" }, [caption]));
        }
        if (clickable && !alt && !caption) tile.setAttribute("aria-label", `Image ${i + 1}`);
      }
      if (clickable) {
        tile.onclick = () => helpers.invoke(props.onSelect, i, raw);
      }
      root.append(tile);
    });
    return root;
  }
};
function extractGalleryItem(raw) {
  if (typeof raw === "string") return { src: raw, alt: "", caption: "" };
  if (raw && typeof raw === "object") {
    const r = raw;
    if (r.__kind === "Component" && Array.isArray(r.args)) {
      return {
        src: asString(r.args[0]),
        alt: asString(r.args[1]),
        caption: asString(r.args[2])
      };
    }
    return {
      src: asString(r.src),
      alt: asString(r.alt),
      caption: asString(r.caption)
    };
  }
  return { src: "", alt: "", caption: "" };
}
const LIGHTBOX_POPOVER_RESET = "width:auto;height:auto;max-width:none;max-height:none;border:0;overflow:hidden;";
function lightboxLabel(entry, index, total) {
  return entry.alt || entry.caption || `Image ${index + 1} of ${total}`;
}
const Lightbox = {
  name: "Lightbox",
  description: "Image overlay. Pass `items` (string URLs or `{src, alt, caption?}` objects). `open` and `index` work as literals or as `$variable` bindings; without a bound `open` the component manages itself and renders a clickable thumbnail of the current image (suppress it with `showThumbnail: false` when something else does the opening). Clicking the backdrop or ×, or pressing Escape, closes and fires `onClose`; the arrows and ArrowLeft/ArrowRight step through the array. Focus moves into the viewer on open, is trapped while it is open, and is restored on close.",
  props: [
    { name: "items", type: "any[]" },
    { name: "open", type: "boolean", optional: true, description: "Open/closed; bind a $variable to control externally" },
    { name: "index", type: "number", optional: true, description: "0-indexed current image; typically a $variable" },
    { name: "onClose", type: "callable", optional: true, aliases: ["onclose"], description: "Callable invoked whenever the viewer closes" },
    { name: "showThumbnail", type: "boolean", optional: true, description: "Render the clickable thumbnail (default: only when `open` is not bound)" }
  ],
  render: (node, props, helpers) => {
    const items = asArray(props.items).map((raw) => extractGalleryItem(raw)).filter((entry) => sanitiseImageSrc(entry.src) !== "");
    const total = items.length;
    const indexStateName = node.argMeta?.[2]?.stateRef;
    const openStateName = node.argMeta?.[1]?.stateRef;
    const openGiven = props.open !== void 0;
    const indexGiven = props.index !== void 0;
    const internalOpen = helpers.useInstanceState("open", openGiven ? asBoolean(props.open) : false);
    const internalIndex = helpers.useInstanceState(
      "index",
      indexGiven ? Math.floor(asNumber(props.index, 0)) : 0
    );
    const openSeed = helpers.useInstanceState("openSeed", null);
    if (!openStateName && openGiven) {
      const wanted = asBoolean(props.open);
      if (openSeed.get() !== wanted) {
        openSeed.set(wanted);
        internalOpen.set(wanted);
      }
    }
    const indexSeed = helpers.useInstanceState("indexSeed", null);
    if (!indexStateName && indexGiven) {
      const wanted = Math.floor(asNumber(props.index, 0));
      if (indexSeed.get() !== wanted) {
        indexSeed.set(wanted);
        internalIndex.set(wanted);
      }
    }
    const isOpen = openStateName ? asBoolean(props.open) : internalOpen.get();
    const normalise = (raw) => total === 0 ? 0 : (raw % total + total) % total;
    const currentIndex = () => normalise(
      indexStateName ? Math.floor(asNumber(props.index, 0)) : internalIndex.get()
    );
    const idx = currentIndex();
    const wasOpen = helpers.useInstanceState("wasOpen", false);
    const focusPrev = helpers.useInstanceState("focusPrev", null);
    const scrollLock = helpers.useInstanceState("scrollLock", null);
    const liveRootRef = helpers.useInstanceState("liveRoot", null);
    const lockScroll = () => {
      const body = typeof document === "undefined" ? null : document.body;
      if (!body || scrollLock.get() !== null) return;
      scrollLock.set(body.style.overflow);
      body.style.overflow = "hidden";
    };
    const unlockScroll = () => {
      const body = typeof document === "undefined" ? null : document.body;
      const previous = scrollLock.get();
      if (!body || previous === null) return;
      body.style.overflow = previous;
      scrollLock.set(null);
    };
    const lockDisposer = helpers.useInstanceState("scrollDisposer", false);
    if (!lockDisposer.get()) {
      lockDisposer.set(true);
      helpers.registerDisposer(() => unlockScroll(), "rui-lightbox-scroll");
    }
    const promote = (overlayEl, next) => {
      const api = overlayEl;
      if (typeof api.showPopover !== "function") return;
      try {
        if (next) api.showPopover();
        else api.hidePopover?.();
      } catch {
      }
    };
    const applyIndex = (liveRoot, next) => {
      const entry = items[next];
      if (!entry) return;
      const safeSrc2 = sanitiseImageSrc(entry.src);
      const dialog2 = liveRoot.querySelector(".rui-lightbox");
      if (dialog2) {
        dialog2.setAttribute("aria-label", lightboxLabel(entry, next, total));
        const img = dialog2.querySelector(".rui-lightbox-image-wrap img");
        if (img && safeSrc2) {
          img.setAttribute("src", safeSrc2);
          img.setAttribute("alt", entry.alt);
        }
        const caption = dialog2.querySelector(".rui-lightbox-caption");
        if (caption) {
          caption.textContent = entry.caption;
          caption.toggleAttribute("hidden", !entry.caption);
        }
        const counter = dialog2.querySelector(".rui-lightbox-counter");
        if (counter) counter.textContent = `${next + 1} / ${total}`;
      }
      const thumb = liveRoot.querySelector(".rui-lightbox-thumb");
      if (thumb) {
        thumb.setAttribute("aria-label", entry.alt || "Open image");
        const thumbImg = thumb.querySelector("img");
        if (thumbImg && safeSrc2) {
          thumbImg.setAttribute("src", safeSrc2);
          thumbImg.setAttribute("alt", entry.alt);
        }
      }
    };
    const applyOpen = (liveRoot, next) => {
      const overlayEl = liveRoot.querySelector(".rui-lightbox-overlay");
      if (!overlayEl) return;
      overlayEl.setAttribute("data-open", next ? "true" : "false");
      promote(overlayEl, next);
      const doc = liveRoot.getRootNode();
      if (next) {
        focusPrev.set(doc.activeElement ?? null);
        lockScroll();
        const dialog2 = overlayEl.querySelector(".rui-lightbox") ?? overlayEl;
        deferToPaint(() => {
          if (dialog2.isConnected) dialog2.focus?.();
        });
      } else {
        unlockScroll();
        const restore = focusPrev.get();
        focusPrev.set(null);
        deferToPaint(() => {
          if (restore?.isConnected) restore.focus?.();
        });
      }
    };
    const liveRootFrom = (event) => {
      const origin = event.currentTarget ?? event.target;
      return origin?.closest(".rui-lightbox-root") ?? liveRootRef.get();
    };
    const setOpen = (next, liveRoot) => {
      wasOpen.set(next);
      if (openStateName) helpers.setState(openStateName, next);
      else internalOpen.set(next);
      if (liveRoot) applyOpen(liveRoot, next);
      if (!next) helpers.invoke(props.onClose);
    };
    const setIndex = (next, liveRoot) => {
      if (total === 0) return;
      const clamped = normalise(next);
      if (indexStateName) helpers.setState(indexStateName, clamped);
      else internalIndex.set(clamped);
      if (liveRoot) applyIndex(liveRoot, clamped);
    };
    const root = el("div", { class: "rui-lightbox-root" });
    if (!liveRootRef.get()?.isConnected) {
      deferToPaint(() => {
        if (root.isConnected) liveRootRef.set(root);
      });
    }
    if (isOpen !== wasOpen.get()) {
      wasOpen.set(isOpen);
      deferToPaint(() => {
        const live = root.isConnected ? root : liveRootRef.get();
        if (live?.isConnected) applyOpen(live, isOpen);
      });
    }
    const showThumb = props.showThumbnail === void 0 ? !openStateName : asBoolean(props.showThumbnail);
    if (showThumb && total > 0) {
      const current2 = items[idx];
      const thumb = el("button", {
        type: "button",
        class: "rui-lightbox-thumb",
        "aria-label": current2.alt || "Open image"
      });
      const safeThumb = sanitiseImageSrc(current2.src);
      if (safeThumb) thumb.append(el("img", { src: safeThumb, alt: current2.alt, loading: "lazy" }));
      thumb.onclick = (event) => {
        event.stopPropagation();
        setOpen(true, liveRootFrom(event));
      };
      root.append(thumb);
    }
    const overlay = el("div", {
      class: "rui-lightbox-overlay",
      "data-open": isOpen ? "true" : "false",
      // `manual` only: dismissal stays entirely ours, so native light-dismiss
      // cannot fight the Escape handler below.
      popover: "manual",
      style: LIGHTBOX_POPOVER_RESET
    });
    root.append(overlay);
    if (total === 0) return root;
    const current = items[idx];
    const safeSrc = sanitiseImageSrc(current.src);
    overlay.onclick = (event) => {
      if (event.target !== event.currentTarget) return;
      setOpen(false, liveRootFrom(event));
    };
    const closeFromKeys = dialogKeydownHandler(".rui-lightbox", (origin) => {
      setOpen(false, origin.closest(".rui-lightbox-root") ?? liveRootRef.get());
    });
    overlay.onkeydown = (event) => {
      if (total > 1 && (event.key === "ArrowLeft" || event.key === "ArrowRight")) {
        event.preventDefault();
        setIndex(currentIndex() + (event.key === "ArrowLeft" ? -1 : 1), liveRootFrom(event));
        return;
      }
      closeFromKeys(event);
    };
    const dialog = el("div", {
      class: "rui-lightbox",
      role: "dialog",
      "aria-modal": "true",
      "aria-label": lightboxLabel(current, idx, total),
      // Focusable so focus can move in on open and be trapped while open.
      tabindex: "-1"
    });
    const closeBtn = el("button", {
      type: "button",
      class: "rui-lightbox-close",
      "aria-label": "Close lightbox"
    }, ["×"]);
    closeBtn.onclick = (event) => {
      event.stopPropagation();
      setOpen(false, liveRootFrom(event));
    };
    dialog.append(closeBtn);
    if (total > 1) {
      const prev = el("button", {
        type: "button",
        class: "rui-lightbox-arrow",
        "data-direction": "prev",
        "aria-label": "Previous image"
      });
      const prevIcon = renderIcon("chevron-left");
      if (prevIcon) prev.append(prevIcon);
      prev.onclick = (event) => {
        event.stopPropagation();
        setIndex(currentIndex() - 1, liveRootFrom(event));
      };
      const next = el("button", {
        type: "button",
        class: "rui-lightbox-arrow",
        "data-direction": "next",
        "aria-label": "Next image"
      });
      const nextIcon = renderIcon("chevron-right");
      if (nextIcon) next.append(nextIcon);
      next.onclick = (event) => {
        event.stopPropagation();
        setIndex(currentIndex() + 1, liveRootFrom(event));
      };
      dialog.append(prev, next);
    }
    const imageWrap = el("div", { class: "rui-lightbox-image-wrap" });
    if (safeSrc) {
      imageWrap.append(el("img", { src: safeSrc, alt: current.alt }));
    }
    dialog.append(imageWrap);
    dialog.append(el("div", {
      class: "rui-lightbox-caption",
      hidden: current.caption ? null : ""
    }, [current.caption]));
    if (total > 1) {
      dialog.append(el("div", { class: "rui-lightbox-counter" }, [`${idx + 1} / ${total}`]));
    }
    overlay.append(dialog);
    return root;
  }
};
const MAX_LAT = 85;
const MAX_LNG = 180;
const clampLat = (value) => Math.max(-MAX_LAT, Math.min(MAX_LAT, value));
const clampLng = (value) => Math.max(-MAX_LNG, Math.min(MAX_LNG, value));
function parseLatLng(raw) {
  if (!raw) return null;
  if (typeof raw === "object" && !Array.isArray(raw)) {
    const r = raw;
    const lat = asNumber(r.lat ?? r.latitude, NaN);
    const lng = asNumber(r.lng ?? r.longitude, NaN);
    if (isLatLng(lat, lng)) return { lat, lng };
  }
  if (Array.isArray(raw) && raw.length >= 2) {
    const lat = asNumber(raw[0], NaN);
    const lng = asNumber(raw[1], NaN);
    if (isLatLng(lat, lng)) return { lat, lng };
  }
  if (typeof raw === "string") {
    const parts = raw.split(",").map((s) => Number(s.trim()));
    if (parts.length >= 2 && isLatLng(parts[0], parts[1])) {
      return { lat: parts[0], lng: parts[1] };
    }
  }
  return null;
}
function isLatLng(lat, lng) {
  return Number.isFinite(lat) && Number.isFinite(lng) && Math.abs(lat) <= 90 && Math.abs(lng) <= MAX_LNG;
}
const Map$1 = {
  name: "Map",
  description: "Static map view centered on a lat/lng coordinate. Renders an OpenStreetMap embed inside a sandboxed `<iframe>` (no external JS, no API key). Pass `lat` (-90…90) and `lng` (-180…180) as bare numbers; `zoom` controls the level (1–18, default 13). Optional `markers` are projected onto the map as pins and listed as labelled chips beneath it. Use for store locators, address cards, itinerary previews.",
  props: [
    { name: "lat", type: "number", description: "Latitude of the map center (-90…90)" },
    { name: "lng", type: "number", description: "Longitude of the map center (-180…180)" },
    { name: "zoom", type: "number", optional: true, description: "1–18 (default 13)" },
    { name: "markers", type: "object[]", optional: true, aliases: ["locations"], description: "Array of {lat, lng, label?} — drawn as pins over the map and listed beneath it" },
    { name: "height", type: "string", optional: true, description: "CSS height (default 320px)" },
    { name: "caption", type: "string", optional: true },
    { name: "title", type: "string", optional: true, description: "Accessible name for the map frame (defaults to the caption)" }
  ],
  render: (_node, props) => {
    const root = el("figure", { class: "rui-map" });
    const height = sanitiseCssLength(props.height, "320px");
    const frameWrap = el("div", { class: "rui-map-frame", style: `height:${height};` });
    const caption = asString(props.caption);
    const lat = asNumber(props.lat, NaN);
    const lng = asNumber(props.lng, NaN);
    const center = isLatLng(lat, lng) ? { lat, lng } : null;
    if (!center) {
      const finite = Number.isFinite(lat) && Number.isFinite(lng);
      frameWrap.append(el("div", { class: "rui-map-empty" }, [
        finite ? "Out of range — lat must be -90…90 and lng -180…180." : "Pass lat & lng (numbers) to render the map."
      ]));
      root.append(frameWrap);
      if (caption) root.append(el("figcaption", { class: "rui-map-caption" }, [caption]));
      return root;
    }
    const zoom = Math.max(1, Math.min(18, Math.floor(asNumber(props.zoom, 13))));
    const span = 1 / Math.pow(2, zoom - 8);
    const west = clampLng(center.lng - span);
    const east = clampLng(center.lng + span);
    const south = clampLat(center.lat - span / 2);
    const north = clampLat(center.lat + span / 2);
    const bbox = [west, south, east, north].join(",");
    const url = `https://www.openstreetmap.org/export/embed.html?bbox=${bbox}&layer=mapnik&marker=${center.lat},${center.lng}`;
    const iframe = el("iframe", {
      class: "rui-map-iframe",
      src: url,
      loading: "lazy",
      title: asString(props.title) || caption || "Map view",
      referrerpolicy: "no-referrer",
      // Tiles and pan/zoom need scripts; nothing else. Without a sandbox the
      // third-party frame keeps the ability to navigate the host page's
      // top-level browsing context on user activation.
      sandbox: "allow-scripts"
    });
    frameWrap.append(iframe);
    const markers = asArray(props.markers).map((raw) => {
      const pin = parseLatLng(raw);
      const label = raw && typeof raw === "object" ? asString(raw.label) : "";
      return pin ? { ...pin, label } : null;
    }).filter((p) => p !== null);
    if (markers.length > 0) {
      const pins = el("div", {
        class: "rui-map-pins",
        style: "position:absolute;inset:0;pointer-events:none;",
        "aria-hidden": "true"
      });
      for (const m of markers) {
        const x = (m.lng - west) / (east - west) * 100;
        const y = (north - m.lat) / (north - south) * 100;
        if (!Number.isFinite(x) || !Number.isFinite(y)) continue;
        if (x < 0 || x > 100 || y < 0 || y > 100) continue;
        const pin = el("span", {
          class: "rui-map-pin",
          style: `position:absolute;left:${x.toFixed(3)}%;top:${y.toFixed(3)}%;transform:translate(-50%,-100%);color:var(--rui-color-primary);font-size:18px;text-shadow:0 1px 2px rgba(0,0,0,0.45);`
        });
        const pinIcon = renderIcon("location-dot");
        if (pinIcon) pin.append(pinIcon);
        pins.append(pin);
      }
      if (pins.childElementCount > 0) frameWrap.append(pins);
    }
    root.append(frameWrap);
    if (markers.length > 0) {
      const list = el("ul", { class: "rui-map-markers" });
      for (const m of markers) {
        const li = el("li", { class: "rui-map-marker" });
        const pinIcon = renderIcon("location-dot", { className: "rui-map-marker-icon" });
        if (pinIcon) li.append(pinIcon);
        li.append(el("span", { class: "rui-map-marker-label" }, [
          m.label || `${m.lat.toFixed(4)}, ${m.lng.toFixed(4)}`
        ]));
        list.append(li);
      }
      root.append(list);
    }
    if (caption) root.append(el("figcaption", { class: "rui-map-caption" }, [caption]));
    return root;
  }
};
function parseRatio(input) {
  if (input.includes(":")) {
    const [w, h] = input.split(":");
    const num2 = Number(w);
    const den = Number(h);
    if (Number.isFinite(num2) && Number.isFinite(den) && num2 > 0 && den > 0) return `${num2} / ${den}`;
  }
  const n = Number(input);
  return Number.isFinite(n) && n > 0 ? `${n} / 1` : "16 / 9";
}
function renderChildAsNode(helpers, child) {
  if (child == null) return document.createTextNode("");
  if (Array.isArray(child)) {
    const wrap = el("span", { class: "rui-wrapper-fragment", style: "display: contents;" });
    for (const c of child) {
      if (c == null) continue;
      wrap.append(typeof c === "string" ? document.createTextNode(c) : helpers.renderNode(c));
    }
    return wrap;
  }
  if (typeof child === "string") return document.createTextNode(child);
  return helpers.renderNode(child);
}
const BOXED_UNIVERSALS = ["sx", "style", "animate", "className", "class"];
function wrapperStyle(universal, transparent) {
  if (!universal) return transparent;
  if (universal.hidden === true) return "display: none;";
  const needsBox = BOXED_UNIVERSALS.some((key) => universal[key] != null);
  return needsBox ? "display: block;" : transparent;
}
function transparentWrapper(className, universal, extraAttrs) {
  return el("span", {
    class: `rui-wrapper ${className}`,
    style: wrapperStyle(universal, "display: contents;"),
    ...extraAttrs ?? {}
  });
}
function useLiveBox(helpers, key, initial) {
  const box = helpers.useInstanceState(key, initial).get();
  box.props = initial.props;
  return box;
}
function useLiveWrapper(helpers, wrapper, key) {
  const slot = helpers.useInstanceState(key, wrapper);
  const remembered = slot.get();
  if (remembered === wrapper || remembered.isConnected) return remembered;
  slot.set(wrapper);
  return wrapper;
}
function installOnce(helpers, key, target, install) {
  const slot = helpers.useInstanceState(key, null);
  if (slot.get() === target) return;
  slot.set(target);
  install(target);
}
const SELF_ACTING_CHILD = [
  "a",
  "button",
  "input",
  "select",
  "textarea",
  "label",
  "summary",
  '[contenteditable="true"]',
  '[role="button"]',
  '[role="link"]',
  '[role="checkbox"]',
  '[role="switch"]',
  '[role="menuitem"]',
  '[role="option"]',
  '[role="tab"]',
  '[role="textbox"]'
].join(",");
function fromSelfActingChild(wrapper, event) {
  const target = event.target;
  if (!target || target === wrapper || typeof target.closest !== "function") return false;
  const owner = target.closest(SELF_ACTING_CHILD);
  return owner != null && owner !== wrapper && wrapper.contains(owner);
}
function sanitiseInlineStyle(input) {
  const raw = asString(input).trim();
  if (!raw) return "";
  if (/[<>]/.test(raw)) return "";
  if (/\bexpression\s*\(|\bjavascript\s*:|\bbehavior\s*:|@import\b/i.test(raw)) return "";
  return raw;
}
const CLASS_TOKEN_RE = /^[A-Za-z_][A-Za-z0-9_\-:/]*$/;
function sanitiseClassList(input) {
  const tokens = Array.isArray(input) ? input.map((value) => asString(value)) : asString(input).split(/\s+/);
  return tokens.map((token) => token.trim()).filter((token) => token.length > 0 && token.length <= 64 && CLASS_TOKEN_RE.test(token));
}
const OnClick = {
  name: "OnClick",
  description: "Make any component clickable. Wraps the child in a transparent span and dispatches `onClick(event)` when the user clicks or taps it. Clicks that land on a self-acting child (a Button, Link, or form control) are left to that child, so nesting is safe. Enter / Space activate it too unless `keyboard: false`. Use to attach click behaviour to components that do not expose an `action` / `onClick` prop (cards, list rows, media tiles, custom layouts).",
  props: [
    { name: "child", type: "Node", positional: true, required: true, aliases: ["children"], description: "Component (or array of components) to wrap" },
    { name: "onClick", type: "callable", required: true, aliases: ["action", "onclick"], description: "Callable invoked on click / tap. Receives the native MouseEvent." },
    { name: "disabled", type: "boolean", optional: true, description: "Skip firing the handler while truthy (also sets aria-disabled and leaves the tab order)" },
    { name: "stopPropagation", type: "boolean", optional: true, description: "Call event.stopPropagation() after invoking the handler (default false)" },
    { name: "role", type: "string", optional: true, description: 'ARIA role for the wrapper (default "button"). Pass "none" when the wrapped element is a list row / table cell whose container owns the semantics.' },
    { name: "keyboard", type: "boolean", optional: true, description: "Activate on Enter / Space and expose a tab stop (default true). Pass false when the child is already focusable — e.g. a card containing inputs." }
  ],
  render: (node, props, helpers) => {
    const disabled = asBoolean(props.disabled);
    const keyboard = props.keyboard === void 0 ? true : asBoolean(props.keyboard);
    const role = asString(props.role, keyboard ? "button" : "");
    const wrapper = transparentWrapper("rui-on-click", node.universal, {
      role: role || null,
      // Only claim a tab stop while we actually handle keys. A disabled wrapper
      // leaves the tab order but stays announced as a disabled control.
      tabindex: keyboard ? disabled ? "-1" : "0" : null,
      "aria-disabled": disabled ? "true" : null,
      "data-disabled": disabled ? "true" : null
    });
    wrapper.append(renderChildAsNode(helpers, props.child));
    if (!disabled) wrapper.style.cursor = "pointer";
    const stopProp = asBoolean(props.stopPropagation);
    const fire = (event) => {
      if (disabled) return false;
      const self = event.currentTarget ?? event.target;
      if (fromSelfActingChild(self, event)) return false;
      if (stopProp) event.stopPropagation();
      helpers.invoke(props.onClick, event);
      return true;
    };
    wrapper.onclick = (event) => {
      fire(event);
    };
    wrapper.onkeydown = keyboard ? (event) => {
      if (event.key !== "Enter" && event.key !== " ") return;
      if (fire(event)) event.preventDefault();
    } : null;
    return wrapper;
  }
};
const MOUSE_EVENT_MAP = [
  { prop: "enter", handler: "onmouseenter" },
  { prop: "leave", handler: "onmouseleave" },
  { prop: "hover", handler: "onmouseover" },
  { prop: "move", handler: "onmousemove" },
  { prop: "down", handler: "onmousedown" },
  { prop: "up", handler: "onmouseup" },
  { prop: "click", handler: "onclick" },
  { prop: "doubleClick", handler: "ondblclick" },
  { prop: "contextMenu", handler: "oncontextmenu" },
  { prop: "wheel", handler: "onwheel" },
  { prop: "pointerDown", handler: "onpointerdown" },
  { prop: "pointerMove", handler: "onpointermove" },
  { prop: "pointerUp", handler: "onpointerup" },
  { prop: "drag", handler: "ondrag" },
  { prop: "drop", handler: "ondrop" },
  { prop: "dragStart", handler: "ondragstart" },
  { prop: "dragEnd", handler: "ondragend" },
  { prop: "dragEnter", handler: "ondragenter" },
  { prop: "dragLeave", handler: "ondragleave" },
  { prop: "dragOver", handler: "ondragover" }
];
const OnMouse = {
  name: "OnMouse",
  description: "Attach any combination of mouse / pointer / drag listeners to a component. Pass only the props you need — unused events install no handler so the wrapper is essentially free. Each handler receives the native MouseEvent / PointerEvent / DragEvent / WheelEvent. Use for hover tracking, custom drag-and-drop, context menus, scroll-aware UIs.",
  props: [
    { name: "child", type: "Node", positional: true, required: true, aliases: ["children"], description: "Component to wrap" },
    { name: "enter", type: "callable", optional: true, description: "Fired when the pointer enters the element (mouseenter)" },
    { name: "leave", type: "callable", optional: true, description: "Fired when the pointer leaves the element (mouseleave)" },
    { name: "hover", type: "callable", optional: true, description: "Fired on every mouseover inside the element" },
    { name: "move", type: "callable", optional: true, description: "Fired on every mousemove inside the element" },
    { name: "down", type: "callable", optional: true, description: "Fired on mousedown" },
    { name: "up", type: "callable", optional: true, description: "Fired on mouseup" },
    { name: "click", type: "callable", optional: true, description: "Fired on click / tap" },
    { name: "doubleClick", type: "callable", optional: true, description: "Fired on double-click" },
    { name: "contextMenu", type: "callable", optional: true, description: "Fired on right-click (contextmenu)" },
    { name: "scroll", type: "callable", optional: true, description: "Fired when an inner element scrolls (observed on the capture phase, since scroll does not bubble)" },
    { name: "wheel", type: "callable", optional: true, description: "Fired on mouse-wheel / trackpad scroll. Not passive, so `event.preventDefault()` works (custom zoom)." },
    { name: "pointerDown", type: "callable", optional: true, description: "Fired on pointerdown — use instead of `down` for touch / pen drags (gives `pointerId`, pressure, setPointerCapture)" },
    { name: "pointerMove", type: "callable", optional: true, description: "Fired on pointermove" },
    { name: "pointerUp", type: "callable", optional: true, description: "Fired on pointerup" },
    { name: "drag", type: "callable", optional: true, description: "Fired while the element is being dragged" },
    { name: "drop", type: "callable", optional: true, description: "Fired when something is dropped onto the element" },
    { name: "dragStart", type: "callable", optional: true, description: "Fired when a drag begins" },
    { name: "dragEnd", type: "callable", optional: true, description: "Fired when a drag ends" },
    { name: "dragEnter", type: "callable", optional: true, description: "Fired when a drag enters the element" },
    { name: "dragLeave", type: "callable", optional: true, description: "Fired when a drag leaves the element" },
    { name: "dragOver", type: "callable", optional: true, description: "Fired while something is dragged over the element. Call `event.preventDefault()` to make the element a valid drop target." },
    { name: "draggable", type: "boolean", optional: true, description: 'Make the wrapper itself draggable (sets `draggable="true"`)' },
    { name: "passiveScroll", type: "boolean", optional: true, description: "Register the `scroll` listener as passive (default true, for scroll performance). Read on the first render." }
  ],
  render: (node, props, helpers) => {
    const wrapper = transparentWrapper("rui-on-mouse", node.universal, {
      draggable: asBoolean(props.draggable) ? "true" : null
    });
    wrapper.append(renderChildAsNode(helpers, props.child));
    for (const { prop: prop2, handler } of MOUSE_EVENT_MAP) {
      const callback = props[prop2];
      if (callback == null) continue;
      wrapper[handler] = (event) => {
        helpers.invoke(callback, event);
      };
    }
    const box = useLiveBox(helpers, "rui-on-mouse", { props, wantsScroll: false });
    if (props.scroll != null) box.wantsScroll = true;
    if (box.wantsScroll) {
      const passive = props.passiveScroll === void 0 ? true : asBoolean(props.passiveScroll);
      const live = useLiveWrapper(helpers, wrapper, "rui-on-mouse-node");
      installOnce(helpers, "rui-on-mouse-scroll", live, (target) => {
        target.addEventListener("scroll", (event) => {
          helpers.invoke(box.props.scroll, event);
        }, { capture: true, passive });
      });
    }
    return wrapper;
  }
};
const KEYBOARD_EVENT_MAP = [
  { prop: "onKeyDown", event: "keydown", handler: "onkeydown" },
  { prop: "onKeyUp", event: "keyup", handler: "onkeyup" },
  { prop: "onKeyPress", event: "keypress", handler: "onkeypress" }
];
const OnKeyboard = {
  name: "OnKeyboard",
  description: 'Attach keyboard listeners to a component. Pass any combination of `onKeyDown`, `onKeyUp`, and `onKeyPress`; each handler receives the native KeyboardEvent. Use for navigation and custom focusable widgets. The wrapper is focusable by default (tabindex="0") so it can be reached via Tab; pass `focusable=false` when the child is already focusable. For an app-wide shortcut (Cmd+K, `?`) pass `global: true`, which listens on the window instead so the keys fire wherever focus is.',
  props: [
    { name: "child", type: "Node", positional: true, required: true, aliases: ["children"] },
    { name: "onKeyDown", type: "callable", optional: true, aliases: ["onkeydown"], description: "Fired on keydown" },
    { name: "onKeyUp", type: "callable", optional: true, aliases: ["onkeyup"], description: "Fired on keyup" },
    { name: "onKeyPress", type: "callable", optional: true, aliases: ["onkeypress"], description: "Fired on keypress" },
    { name: "focusable", type: "boolean", optional: true, description: "Make the wrapper focusable via Tab (default true, or false when `global`)" },
    { name: "global", type: "boolean", optional: true, description: "Listen on the window rather than the wrapper, so the shortcut fires regardless of focus (command palettes, help overlays). Removed when the component leaves the tree." }
  ],
  render: (node, props, helpers) => {
    const isGlobal = asBoolean(props.global);
    const focusable = props.focusable === void 0 ? !isGlobal : asBoolean(props.focusable);
    const wrapper = transparentWrapper(
      "rui-on-keyboard",
      node.universal,
      focusable ? { tabindex: "0" } : {}
    );
    wrapper.append(renderChildAsNode(helpers, props.child));
    const box = useLiveBox(helpers, "rui-on-keyboard", {
      props,
      detachGlobal: null,
      disposerInstalled: false
    });
    if (!isGlobal) {
      for (const { prop: prop2, handler } of KEYBOARD_EVENT_MAP) {
        const callback = props[prop2];
        if (callback == null) continue;
        wrapper[handler] = (event) => {
          helpers.invoke(callback, event);
        };
      }
    }
    if (isGlobal && box.detachGlobal == null && typeof window !== "undefined") {
      const bound = KEYBOARD_EVENT_MAP.map(({ prop: prop2, event }) => {
        const listener = (ev) => {
          helpers.invoke(box.props[prop2], ev);
        };
        window.addEventListener(event, listener);
        return { event, listener };
      });
      box.detachGlobal = () => {
        for (const { event, listener } of bound) window.removeEventListener(event, listener);
      };
      if (!box.disposerInstalled) {
        box.disposerInstalled = true;
        helpers.registerDisposer(() => {
          box.detachGlobal?.();
          box.detachGlobal = null;
        }, "rui-on-keyboard-global");
      }
    } else if (!isGlobal && box.detachGlobal != null) {
      box.detachGlobal();
      box.detachGlobal = null;
    }
    return wrapper;
  }
};
const OnFocus = {
  name: "OnFocus",
  description: "Attach focus / blur listeners to a component. Use to track input focus rings, custom focus indicators, or autosave-on-blur flows. Listens for the bubbling `focusin` / `focusout` events, so focus entering or leaving any descendant is observed.",
  props: [
    { name: "child", type: "Node", positional: true, required: true, aliases: ["children"] },
    { name: "onFocus", type: "callable", optional: true, aliases: ["onfocus"], description: "Fired when focus enters the element or any descendant" },
    { name: "onBlur", type: "callable", optional: true, aliases: ["onblur"], description: "Fired when focus leaves the element and all descendants" }
  ],
  render: (node, props, helpers) => {
    const wrapper = transparentWrapper("rui-on-focus", node.universal);
    wrapper.append(renderChildAsNode(helpers, props.child));
    const box = useLiveBox(helpers, "rui-on-focus", { props });
    const live = useLiveWrapper(helpers, wrapper, "rui-on-focus-node");
    installOnce(helpers, "rui-on-focus-listeners", live, (target) => {
      target.addEventListener("focusin", (event) => {
        helpers.invoke(box.props.onFocus, event);
      });
      target.addEventListener("focusout", (event) => {
        helpers.invoke(box.props.onBlur, event);
      });
    });
    return wrapper;
  }
};
const ROOT_MARGIN_TOKEN = /^[+-]?(?:\d+|\d*\.\d+)(?:px|%)$|^[+-]?0$/;
function sanitiseRootMargin(raw) {
  const value = asString(raw).trim();
  if (!value) return "";
  const tokens = value.split(/\s+/);
  if (tokens.length > 4) return "";
  return tokens.every((token) => ROOT_MARGIN_TOKEN.test(token)) ? value : "";
}
function resolveObserverRoot(target, selector) {
  try {
    const ancestor = target.closest(selector);
    if (ancestor && ancestor !== target) return ancestor;
    const root = target.getRootNode();
    if (root instanceof Document || root instanceof ShadowRoot) {
      return root.querySelector(selector);
    }
  } catch {
  }
  return null;
}
const OnIntersect = {
  name: "OnIntersect",
  description: "Observe whether a component is visible in the viewport (or a scroll container passed as `root`) using IntersectionObserver. Fires `onEnter` the first time the wrapped element becomes visible, `onLeave` when it leaves, and `onChange({visible, ratio})` for every transition. Use for lazy-load sentinels, infinite-scroll triggers, impression analytics, and reveal-on-scroll animations. Set `disabled: true` once there is nothing left to load.",
  props: [
    { name: "child", type: "Node", positional: true, required: true, aliases: ["children"] },
    { name: "onEnter", type: "callable", optional: true, description: "Fired when the element enters the viewport" },
    { name: "onLeave", type: "callable", optional: true, description: "Fired when the element leaves the viewport" },
    { name: "onChange", type: "callable", optional: true, description: "Fired with `{visible, ratio}` on every transition" },
    { name: "threshold", type: "number", optional: true, description: "Visible-ratio threshold 0–1 (default 0.05)" },
    { name: "rootMargin", type: "string", optional: true, description: 'Root margin as 1–4 px / % lengths (e.g. "0px 0px -64px 0px"). Unit-less values are ignored.' },
    { name: "root", type: "string", optional: true, description: 'CSS selector for the scroll container to observe inside (e.g. ".rui-scroll-area"). Defaults to the viewport.' },
    { name: "once", type: "boolean", optional: true, description: "Disconnect after the first entry (default false)" },
    { name: "disabled", type: "boolean", optional: true, description: "Stop firing the callbacks while truthy — use it when the last page of an infinite list has loaded" }
  ],
  render: (node, props, helpers) => {
    const wrapper = el("span", {
      class: "rui-wrapper rui-on-intersect",
      style: wrapperStyle(node.universal, "display: block;")
    });
    wrapper.append(renderChildAsNode(helpers, props.child));
    const ObserverCtor = typeof IntersectionObserver !== "undefined" ? IntersectionObserver : void 0;
    if (!ObserverCtor) return wrapper;
    const box = useLiveBox(helpers, "rui-on-intersect", {
      props,
      observer: null,
      node: null,
      optionsKey: "",
      lastVisible: null
    });
    const rawThreshold = asNumber(props.threshold, 0.05);
    const threshold = Number.isFinite(rawThreshold) ? Math.max(0, Math.min(1, rawThreshold)) : 0.05;
    const rootMargin = sanitiseRootMargin(props.rootMargin);
    const rootSelector = asString(props.root).trim();
    const optionsKey = `${threshold}|${rootMargin}|${rootSelector}`;
    const install = (target) => {
      if (box.observer && box.node === target && box.optionsKey === optionsKey) return;
      box.observer?.disconnect();
      box.observer = null;
      const root = rootSelector ? resolveObserverRoot(target, rootSelector) : null;
      let observer;
      try {
        observer = new ObserverCtor((entries) => {
          const current = box.props;
          if (asBoolean(current.disabled)) return;
          for (const entry of entries) {
            const visible = entry.isIntersecting;
            if (visible !== box.lastVisible) {
              if (visible && current.onEnter != null) helpers.invoke(current.onEnter, entry);
              if (!visible && box.lastVisible === true && current.onLeave != null) {
                helpers.invoke(current.onLeave, entry);
              }
              box.lastVisible = visible;
            }
            if (current.onChange != null) {
              helpers.invoke(current.onChange, { visible, ratio: entry.intersectionRatio });
            }
            if (asBoolean(current.once) && visible) {
              box.observer?.disconnect();
              return;
            }
          }
        }, {
          threshold,
          ...rootMargin ? { rootMargin } : {},
          ...root ? { root } : {}
        });
      } catch {
        return;
      }
      observer.observe(target);
      box.observer = observer;
      box.node = target;
      box.optionsKey = optionsKey;
      helpers.registerDisposer(() => observer.disconnect(), "rui-onintersect-io");
    };
    const live = useLiveWrapper(helpers, wrapper, "rui-on-intersect-node");
    if (rootSelector && !live.isConnected) {
      deferToPaint(() => {
        if (live.isConnected) install(live);
      });
    } else {
      install(live);
    }
    return wrapper;
  }
};
function serialiseDeps(deps) {
  if (deps == null) return null;
  const list = Array.isArray(deps) ? deps : [deps];
  return list.map((value) => {
    if (value == null) return String(value);
    const kind = typeof value;
    if (kind === "object" || kind === "function") {
      try {
        return JSON.stringify(value) ?? "[unserialisable]";
      } catch {
        return "[unserialisable]";
      }
    }
    return `${kind}:${String(value)}`;
  }).join("|");
}
const runSoon = (fn) => {
  if (typeof queueMicrotask === "function") queueMicrotask(fn);
  else void Promise.resolve().then(fn);
};
function runUnmount(box, helpers) {
  if (!box.mounted) return;
  box.mounted = false;
  const target = box.live;
  const callback = box.props.onUnmount;
  if (target == null || callback == null) return;
  runSoon(() => helpers.invoke(callback, target));
}
const OnMount = {
  name: "OnMount",
  description: "Run imperative code against the wrapped component's rendered DOM node. `onMount(node)` fires once, on a microtask after the child is attached to the DOM — the Aktion way to get a DOM ref (measure an element, focus it, or hand it to an imperative library such as a chart / map / editor). `onUnmount(node)` fires when the component leaves the tree. Pass `deps` to tear down and re-run the pair when the values in it change. Pair with `$ref(...)` to stash the node across renders.",
  props: [
    { name: "child", type: "Node", positional: true, required: true, aliases: ["children"] },
    { name: "onMount", type: "callable", optional: true, description: "`(node) => void` — fired once after the wrapped element is attached." },
    { name: "onUnmount", type: "callable", optional: true, description: "`(node) => void` — fired when the wrapped element leaves the tree." },
    { name: "deps", type: "any[]", optional: true, description: "Re-run `onUnmount` + `onMount` whenever a value in this list changes (e.g. `deps: [$docId]` to rebind an editor to another document)." }
  ],
  render: (node, props, helpers) => {
    const wrapper = el("span", {
      class: "rui-wrapper rui-on-mount",
      style: wrapperStyle(node.universal, "display: contents;")
    });
    wrapper.append(renderChildAsNode(helpers, props.child));
    const box = useLiveBox(helpers, "rui-on-mount", {
      props,
      live: null,
      mounted: false,
      depsKey: null,
      disposer: null
    });
    const depsKey = serialiseDeps(props.deps);
    if (box.mounted && depsKey !== box.depsKey) runUnmount(box, helpers);
    if (!box.mounted) {
      box.depsKey = depsKey;
      const fire = () => {
        if (box.mounted) return;
        const fresh = wrapper.firstElementChild ?? wrapper;
        const target = fresh.isConnected ? fresh : box.live?.isConnected ? box.live : null;
        if (!target) return;
        box.mounted = true;
        box.live = target;
        helpers.invoke(box.props.onMount, target);
      };
      runSoon(fire);
    }
    if (!box.disposer) box.disposer = () => {
      runUnmount(box, helpers);
    };
    helpers.registerDisposer(box.disposer, "rui-on-mount-unmount");
    return wrapper;
  }
};
function renderLinkChildren(helpers, child) {
  if (child == null) return [];
  if (typeof child === "string") return [document.createTextNode(child)];
  if (Array.isArray(child)) {
    return child.filter((c) => c != null).map((c) => typeof c === "string" ? document.createTextNode(c) : helpers.renderNode(c));
  }
  return [helpers.renderNode(child)];
}
const ABSOLUTE_URL_RE = /^[a-zA-Z][a-zA-Z0-9+.-]*:|^\/\//;
const DOWNLOAD_NAME_RE = /^[A-Za-z0-9._ ()-]{1,128}$/;
function routerHref(helpers, to) {
  if (to.startsWith("#")) return to;
  const path = to.startsWith("/") ? to : `/${to}`;
  const mode = typeof helpers.router?.getMode === "function" ? helpers.router.getMode() : "hash";
  return mode === "history" ? path : `#${path}`;
}
const Link = {
  name: "Link",
  description: 'Anchor link. Accepts either a plain string label or a wrapped component as its positional child. Pass `to` for client-side router navigation (no page reload) or `href` for a regular anchor. `external: true` opens the link in a new tab with `rel="noopener noreferrer"`. `disabled: true` renders a non-navigating link (pagination edges, gated steps). Use to make any component clickable as a link — cards, icons, badges, list rows.',
  props: [
    { name: "label", type: "string | Node", positional: true, required: true, aliases: ["child", "children"], description: "Visible text OR a wrapped component" },
    { name: "to", type: "string", optional: true, description: "Router path (preferred). Navigates via the runtime router on click." },
    { name: "href", type: "string", optional: true, description: "Standard anchor href. Used when `to` is omitted." },
    { name: "external", type: "boolean", optional: true, description: 'Open in a new tab (`target="_blank"`, `rel="noopener noreferrer"`)' },
    { name: "variant", aliases: ["tone"], type: "string", optional: true, enum: ["default", "subtle"] },
    { name: "disabled", type: "boolean", optional: true, description: "Render as a non-navigating link: no href, out of the tab order, announced as disabled" },
    { name: "onClick", type: "callable", optional: true, aliases: ["onclick"], description: "Ran before navigation (analytics, closing a drawer). Call `event.preventDefault()` inside it to cancel the navigation." },
    { name: "download", type: "boolean | string", optional: true, description: "Download the target instead of navigating. Pass a string to suggest a filename." }
  ],
  render: (_node, props, helpers) => {
    const to = asString(props.to).trim();
    const external = asBoolean(props.external);
    const disabled = asBoolean(props.disabled);
    const isRoute = to !== "" && !ABSOLUTE_URL_RE.test(to);
    const safeHref = disabled ? null : isRoute ? routerHref(helpers, to) : sanitiseHref(to || props.href, "#");
    const download = props.download;
    const downloadName = typeof download === "string" ? download.trim() : "";
    const downloadAttr = download === true || downloadName === "true" ? "" : downloadName !== "" && downloadName !== "false" && DOWNLOAD_NAME_RE.test(downloadName) ? downloadName : null;
    const variant = asString(props.variant, "default");
    const anchor = el("a", {
      class: "rui-link",
      "data-variant": variant,
      href: safeHref,
      // A disabled link keeps its place in the layout but is not activatable:
      // no href (so Enter does nothing), no tab stop, announced as disabled.
      role: disabled ? "link" : null,
      "aria-disabled": disabled ? "true" : null,
      "data-disabled": disabled ? "true" : null,
      tabindex: disabled ? "-1" : null,
      target: external && !disabled ? "_blank" : null,
      rel: external && !disabled ? "noopener noreferrer" : null,
      download: downloadAttr
    });
    for (const node of renderLinkChildren(helpers, props.label)) anchor.append(node);
    if (external && !disabled) {
      anchor.append(el("span", { class: "rui-visually-hidden" }, [" (opens in new tab)"]));
    }
    anchor.onclick = (event) => {
      if (disabled) {
        event.preventDefault();
        return;
      }
      helpers.invoke(props.onClick, event);
      if (!isRoute || external || downloadAttr != null) return;
      if (event.defaultPrevented) return;
      if (event.button !== 0) return;
      if (event.metaKey || event.ctrlKey || event.shiftKey || event.altKey) return;
      event.preventDefault();
      helpers.router.navigate(to);
    };
    return anchor;
  }
};
function applyCss(target, classes, style) {
  for (const token of classes) target.classList.add(token);
  if (!style) return;
  const existing = target.getAttribute("style");
  target.setAttribute("style", existing ? `${existing};${style}` : style);
}
const Css = {
  name: "Css",
  description: "Apply raw CSS class names and / or an inline style string to a wrapped component. The styling is merged onto the rendered child's DOM element — no extra wrapper element is added when the child renders as one element (including an `Svg` root). Several children are each styled individually; a plain-text child gets a wrapping span to carry the styling. Reach for `Css` only when the component's own props cannot express the styling (use `Box`/`Stack`/`Grid` props for layout, `Theme` for tokens, `Styles` + selector classes for sweeping changes).",
  props: [
    { name: "child", type: "Node", positional: true, required: true, aliases: ["children"] },
    { name: "style", type: "string", optional: true, description: 'Inline CSS declarations (e.g. "padding: 16px; background: #eef;")' },
    { name: "class", type: "string | string[]", optional: true, aliases: ["className", "classes"], description: "Class name (space-separated string or array). Tokens must match `[A-Za-z_][A-Za-z0-9_-:/]*`." }
  ],
  render: (_node, props, helpers) => {
    const safeClasses = sanitiseClassList(props.class);
    const safeStyle = sanitiseInlineStyle(props.style);
    if (Array.isArray(props.child)) {
      const fragment = renderChildAsNode(helpers, props.child);
      for (const child of Array.from(fragment.children)) applyCss(child, safeClasses, safeStyle);
      return fragment;
    }
    const rendered = renderChildAsNode(helpers, props.child);
    if (rendered instanceof Element) {
      applyCss(rendered, safeClasses, safeStyle);
      return rendered;
    }
    const span = el("span", { class: "rui-css" });
    span.append(rendered);
    applyCss(span, safeClasses, safeStyle);
    return span;
  }
};
function attachOnChange(element, callback, helpers, options) {
  if (callback === null || callback === void 0) return;
  const eventName = options.event ?? "change";
  element.addEventListener(eventName, () => {
    helpers.invoke(callback, options.getValue(element));
  });
}
const DEFAULT_RTE_TOOLS = [
  { key: "bold", command: "bold", icon: "bold", label: "Bold" },
  { key: "italic", command: "italic", icon: "italic", label: "Italic" },
  { key: "underline", command: "underline", icon: "underline", label: "Underline" },
  { key: "strikethrough", command: "strikeThrough", icon: "strikethrough", label: "Strikethrough" },
  { key: "heading", command: "formatBlock", icon: "heading", label: "Heading", block: "h2" },
  { key: "quote", command: "formatBlock", icon: "quote-left", label: "Quote", block: "blockquote" },
  { key: "bulletList", command: "insertUnorderedList", icon: "list-ul", label: "Bullet list" },
  { key: "numberedList", command: "insertOrderedList", icon: "list-ol", label: "Numbered list" },
  { key: "link", command: "createLink", icon: "link", label: "Link" }
];
const RTE_TOOL_KEYS = DEFAULT_RTE_TOOLS.map((t) => t.key);
function selectRteTools(raw) {
  const requested = asArray(raw).map((t) => asString(t).trim().toLowerCase()).filter(Boolean);
  if (requested.length === 0) return DEFAULT_RTE_TOOLS;
  const out = [];
  for (const token of requested) {
    const tool = DEFAULT_RTE_TOOLS.find(
      (t) => t.key.toLowerCase() === token || t.command.toLowerCase() === token || t.block === token
    );
    if (tool && !out.includes(tool)) out.push(tool);
  }
  return out;
}
const syncRteToolStates = (origin) => {
  if (!origin) return;
  const live = origin.closest(".rui-rich-text");
  if (!live) return;
  let block = "";
  try {
    block = String(document.queryCommandValue?.("formatBlock") ?? "").toLowerCase();
  } catch {
  }
  for (const btn of live.querySelectorAll(".rui-rich-text-tool")) {
    const command = btn.getAttribute("data-command") ?? "";
    const wanted = btn.getAttribute("data-block");
    let active = false;
    if (command === "formatBlock") {
      active = !!wanted && block === wanted;
    } else if (command !== "createLink") {
      try {
        active = document.queryCommandState?.(command) ?? false;
      } catch {
        active = false;
      }
    }
    btn.setAttribute("aria-pressed", active ? "true" : "false");
  }
};
const RichTextEditor = {
  name: "RichTextEditor",
  description: "Rich-text WYSIWYG editor for CMS, email, and comment surfaces. Renders a small toolbar (bold / italic / underline / strikethrough / headings / lists / quote / link) above a `contenteditable` region; pass `tools` to subset it. Pass `$variable` as `value` for two-way binding — the HTML body is written back to state on every edit. Provide `placeholder` for the empty-state prompt, `maxHeight` to make a long document scroll, `readonly` for review flows, and `label`/`hint`/`error`/`required` for a labelled field shell.",
  props: [
    { name: "id", type: "string" },
    { name: "value", type: "string", optional: true, description: "Bound HTML body (typically $variable)" },
    { name: "placeholder", type: "string", optional: true, description: "Empty-state prompt" },
    { name: "minHeight", type: "string", optional: true, description: "CSS min-height for the editor area (default 160px)" },
    { name: "disabled", type: "boolean", optional: true },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"], description: "Called with the current HTML on every keystroke" },
    { name: "tools", type: "string[]", optional: true, enum: RTE_TOOL_KEYS, description: "Subset (and order) of toolbar tools — defaults to all of them" },
    { name: "maxHeight", type: "string", optional: true, description: "CSS max-height for the editor area; the body scrolls past it" },
    { name: "readonly", type: "boolean", optional: true, description: "Content stays readable and selectable at full contrast but cannot be edited" },
    { name: "name", type: "string", optional: true, description: "Form field name — the HTML body is mirrored into a hidden input for native submission" },
    { name: "label", type: "string", optional: true, description: "Field label rendered above the editor" },
    { name: "hint", type: "string", optional: true, aliases: ["helperText"], description: "Helper text rendered below the editor" },
    { name: "error", type: "string", optional: true, description: "Validation error rendered below the editor (marks it invalid)" },
    { name: "required", type: "boolean", optional: true, description: "Mark the field required (adds a `*`)" },
    ...fieldShellExtraProps()
  ],
  render: (node, props, helpers) => {
    const id = asString(props.id);
    const placeholder = asString(props.placeholder, "Start writing…");
    const disabled = asBoolean(props.disabled);
    const readonly = asBoolean(props.readonly);
    const inert = disabled || readonly;
    const root = el("div", {
      class: "rui-rich-text",
      "data-disabled": disabled ? "true" : "false",
      "data-readonly": readonly ? "true" : "false"
    });
    const tools = selectRteTools(props.tools);
    const toolbar = el("div", { class: "rui-rich-text-toolbar", role: "toolbar", "aria-label": "Formatting" });
    let firstEnabled = true;
    for (const tool of tools) {
      const btn = el("button", {
        type: "button",
        class: "rui-rich-text-tool",
        "data-command": tool.command,
        "data-block": tool.block ?? null,
        "aria-label": tool.label,
        "aria-pressed": "false",
        title: tool.label,
        disabled: inert ? "" : null,
        // A toolbar is a single tab stop: the first enabled tool holds it and
        // the arrow keys move a roving focus between the rest.
        tabindex: !inert && firstEnabled ? "0" : "-1"
      });
      if (!inert && firstEnabled) firstEnabled = false;
      const iconNode = renderIcon(tool.icon);
      if (iconNode) btn.append(iconNode);
      btn.onmousedown = (event) => event.preventDefault();
      btn.onclick = (event) => {
        const origin = event.currentTarget ?? event.target;
        const live = origin.closest(".rui-rich-text");
        const editor2 = live?.querySelector(".rui-rich-text-content");
        if (!editor2 || editor2.getAttribute("contenteditable") === "false") return;
        editor2.focus();
        try {
          if (tool.command === "createLink") {
            const safe = sanitiseHref(window.prompt("URL"), "");
            if (safe) document.execCommand("createLink", false, safe);
          } else if (tool.command === "formatBlock" && tool.block) {
            document.execCommand("formatBlock", false, tool.block);
          } else {
            document.execCommand(tool.command, false);
          }
        } catch {
        }
        editor2.dispatchEvent(new Event("input", { bubbles: true }));
        syncRteToolStates(origin);
      };
      toolbar.append(btn);
    }
    toolbar.onkeydown = (event) => {
      const e = event;
      if (e.key !== "ArrowLeft" && e.key !== "ArrowRight" && e.key !== "Home" && e.key !== "End") return;
      const bar = e.currentTarget ?? e.target;
      const items = Array.from(bar.querySelectorAll(".rui-rich-text-tool:not([disabled])"));
      if (items.length === 0) return;
      e.preventDefault();
      const active = bar.getRootNode().activeElement;
      const found = items.findIndex((t) => t === active);
      const current = found < 0 ? 0 : found;
      const next = e.key === "Home" ? 0 : e.key === "End" ? items.length - 1 : e.key === "ArrowRight" ? (current + 1) % items.length : (current - 1 + items.length) % items.length;
      for (const t of items) t.setAttribute("tabindex", "-1");
      const target = items[next];
      target.setAttribute("tabindex", "0");
      target.focus();
    };
    root.append(toolbar);
    const initial = asString(props.value) || "";
    const isEmpty = (html) => {
      const text = html.replace(/<br\s*\/?>(?=\s*$)/gi, "").replace(/<[^>]+>/g, "").replace(/&nbsp;/g, " ").trim();
      return text.length === 0;
    };
    const asserts = props.value !== void 0 && props.value !== null;
    const emptySlot = helpers.useInstanceState("empty", null);
    const liveSlot = helpers.useInstanceState("live", null);
    const assertedSlot = helpers.useInstanceState("asserted", null);
    const emptyNow = asserts ? isEmpty(initial) : emptySlot.get() ?? isEmpty(initial);
    const heightStyle = `min-height:${asString(props.minHeight, "160px")};` + (asString(props.maxHeight) ? `max-height:${asString(props.maxHeight)};` : "");
    const editor = el("div", {
      class: "rui-rich-text-content",
      id,
      contenteditable: inert ? "false" : "true",
      role: "textbox",
      "aria-multiline": "true",
      "aria-readonly": readonly ? "true" : null,
      "aria-placeholder": placeholder,
      "aria-label": asString(props.label) || null,
      "data-placeholder": placeholder,
      "data-empty": emptyNow ? "true" : "false",
      // The user's caret, selection and typed markup live in these children.
      // Without this the reconciler diffs them against the render-time `value`
      // and deletes the draft on any unrelated re-render — and there is no
      // caret-preserving path for contenteditable the way there is for inputs.
      "data-rui-preserve": "",
      style: heightStyle
    });
    setSanitisedHtml(editor, initial);
    const hiddenName = asString(props.name);
    if (hiddenName) {
      root.append(el("input", {
        type: "hidden",
        class: "rui-rich-text-mirror",
        name: hiddenName,
        value: valueAttr(props.value)
      }));
    }
    const refreshLive = (target) => {
      const empty = isEmpty(target.innerHTML);
      emptySlot.set(empty);
      liveSlot.set(target);
      target.setAttribute("data-empty", empty ? "true" : "false");
      const mirror = target.closest(".rui-rich-text")?.querySelector(".rui-rich-text-mirror");
      if (mirror) mirror.value = readSanitisedHtml(target);
    };
    if (asserts && assertedSlot.get() !== initial) {
      const previous = assertedSlot.get();
      assertedSlot.set(initial);
      const live = liveSlot.get();
      if (previous !== null && live?.isConnected && readSanitisedHtml(live) !== initial) {
        setSanitisedHtml(live, initial);
        refreshLive(live);
      }
    }
    if (liveSlot.get() === null) {
      deferToPaint(() => {
        if (editor.isConnected) liveSlot.set(editor);
      });
    }
    const stateName = node.argMeta?.[1]?.stateRef;
    if (stateName && !inert) {
      helpers.bindState(editor, stateName, {
        event: "input",
        // Sync the placeholder state on every keystroke, alongside the
        // state write — keeps both behaviours in the single property-based
        // `oninput` slot so the morph reconciler transfers them as a unit.
        getValue: (n) => {
          const target = n;
          refreshLive(target);
          return readSanitisedHtml(target);
        }
      });
    } else {
      editor.oninput = (event) => {
        refreshLive(event.currentTarget ?? event.target);
      };
    }
    editor.onblur = (event) => {
      refreshLive(event.currentTarget ?? event.target);
    };
    editor.onkeyup = (event) => syncRteToolStates(event.currentTarget ?? event.target);
    editor.onmouseup = (event) => syncRteToolStates(event.currentTarget ?? event.target);
    attachOnChange(editor, props.onChange, helpers, {
      event: "input",
      getValue: (n) => readSanitisedHtml(n)
    });
    root.append(editor);
    return withFieldShell(root, props, { idKey: "id" });
  }
};
const CODE_LANGUAGES = ["text", "javascript", "typescript", "json", "html", "css", "bash", "python", "sql", "markdown"];
const CODE_LINE_HEIGHT = 20;
const fillGutter = (gutter, text) => {
  gutter.replaceChildren();
  const total = Math.max(1, text.split(/\r?\n/).length);
  for (let i = 1; i <= total; i += 1) {
    gutter.append(el("span", { class: "rui-code-editor-line" }, [String(i)]));
  }
};
const insertText = (target, text) => {
  if (target.getRootNode().activeElement !== target) return false;
  try {
    return document.execCommand?.("insertText", false, text) ?? false;
  } catch {
    return false;
  }
};
const reindent = (target, outdent, indent) => {
  const value = target.value;
  const start = target.selectionStart ?? 0;
  const end = target.selectionEnd ?? start;
  if (start === end && !outdent) {
    if (!insertText(target, indent)) {
      if (typeof target.setRangeText === "function") target.setRangeText(indent, start, end, "end");
      else target.value = value.slice(0, start) + indent + value.slice(end);
      target.selectionStart = target.selectionEnd = start + indent.length;
      target.dispatchEvent(new Event("input", { bubbles: true }));
    }
    return;
  }
  const lineStart = value.lastIndexOf("\n", Math.max(0, start - 1)) + 1;
  let lineEnd = value.indexOf("\n", end);
  if (lineEnd === -1) lineEnd = value.length;
  const outdentRe = new RegExp(`^(?:\\t| {1,${indent.length}})`);
  let firstDelta = 0;
  let totalDelta = 0;
  const lines = value.slice(lineStart, lineEnd).split("\n").map((line, i) => {
    const updated = outdent ? line.replace(outdentRe, "") : indent + line;
    const delta = updated.length - line.length;
    if (i === 0) firstDelta = delta;
    totalDelta += delta;
    return updated;
  });
  if (totalDelta === 0) return;
  const replacement = lines.join("\n");
  target.setSelectionRange(lineStart, lineEnd);
  if (!insertText(target, replacement)) {
    if (typeof target.setRangeText === "function") target.setRangeText(replacement, lineStart, lineEnd, "end");
    else target.value = value.slice(0, lineStart) + replacement + value.slice(lineEnd);
    target.dispatchEvent(new Event("input", { bubbles: true }));
  }
  const nextStart = Math.max(lineStart, start + firstDelta);
  target.setSelectionRange(nextStart, Math.max(nextStart, end + totalDelta));
};
const CodeEditor = {
  name: "CodeEditor",
  description: "Lightweight, dependency-free code editor. Pairs a styled textarea with a synchronised line-number gutter — no syntax highlighting, but the editor stays a single rendered node so it works inside Shadow DOM. Tab / Shift+Tab indent and unindent the selected lines, and Ctrl/Cmd+S calls `onSave`. Use for dev tooling, snippet editing, prompt playgrounds. Pass a `$variable` as `value` for two-way binding, `maxHeight` for a fixed-height scrolling pane, and `label`/`hint`/`error` to surface a parse error under the field. For read-only rendering with highlights prefer `CodeBlock`.",
  props: [
    { name: "id", type: "string" },
    { name: "value", type: "string", optional: true, description: "Bound source text (typically $variable)" },
    { name: "language", type: "string", optional: true, enum: CODE_LANGUAGES, description: "Language label shown on the header (default `text`)" },
    { name: "placeholder", type: "string", optional: true },
    { name: "minHeight", type: "string", optional: true, description: "CSS min-height for the editor area (default 200px)" },
    { name: "tabSize", type: "number", optional: true, description: "Spaces per Tab (default 2)" },
    { name: "showGutter", type: "boolean", optional: true, description: "Show line-number gutter (default true)" },
    { name: "readonly", type: "boolean", optional: true, description: "Content stays focusable and submittable but cannot be edited" },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"], description: "Called with the current source on every keystroke" },
    { name: "maxHeight", type: "string", optional: true, description: "CSS max-height for the editor area; the body scrolls past it" },
    { name: "filename", type: "string", optional: true, description: "Filename shown on the left of the header" },
    { name: "copyable", type: "boolean", optional: true, description: "Show a copy-to-clipboard button on the right of the header" },
    { name: "onSave", type: "callable", optional: true, description: "Called with the current source on Ctrl/Cmd+S" },
    { name: "name", type: "string", optional: true, description: "Form field name (defaults to `id`)" },
    { name: "disabled", type: "boolean", optional: true, description: "Disable the control (non-editable, skipped by tab order, not submitted)" },
    { name: "label", type: "string", optional: true, description: "Field label rendered above the editor" },
    { name: "hint", type: "string", optional: true, aliases: ["helperText"], description: "Helper text rendered below the editor" },
    { name: "error", type: "string", optional: true, description: "Validation error rendered below the editor (marks it invalid)" },
    { name: "required", type: "boolean", optional: true, description: "Mark the field required (adds a `*`)" },
    ...fieldShellExtraProps()
  ],
  render: (node, props, helpers) => {
    const id = asString(props.id);
    const language = asString(props.language, "text");
    const showGutter = props.showGutter === void 0 ? true : asBoolean(props.showGutter);
    const tabSize = Math.max(1, Math.min(8, Math.floor(asNumber(props.tabSize, 2))));
    const readonly = asBoolean(props.readonly);
    const disabled = asBoolean(props.disabled);
    const inert = readonly || disabled;
    const minHeight = asString(props.minHeight, "200px");
    const maxHeight = asString(props.maxHeight);
    const sourceSlot = helpers.useInstanceState("source", null);
    const asserts = props.value !== void 0 && props.value !== null;
    const source = asserts ? asString(props.value) : sourceSlot.get() ?? "";
    const root = el("div", {
      class: "rui-code-editor",
      "data-language": language,
      "data-gutter": showGutter ? "true" : "false",
      "data-disabled": disabled ? "true" : "false"
    });
    const head = el("div", { class: "rui-code-editor-head" });
    const meta = el("div", {
      class: "rui-code-editor-head-meta",
      style: "display:flex;align-items:center;gap:8px;min-width:0;"
    });
    const filename = asString(props.filename);
    if (filename) {
      meta.append(el("span", {
        class: "rui-code-editor-filename",
        style: "font-size:12px;font-weight:600;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;"
      }, [filename]));
    }
    meta.append(el("span", { class: "rui-code-editor-language" }, [language]));
    head.append(meta);
    if (asBoolean(props.copyable)) {
      const copyBtn = el("button", {
        type: "button",
        class: "rui-code-block-copy rui-code-editor-copy",
        "aria-label": "Copy code"
      }, ["Copy"]);
      copyBtn.onclick = (event) => {
        const origin = event.currentTarget ?? event.target;
        const area = origin.closest(".rui-code-editor")?.querySelector(".rui-code-editor-textarea");
        if (!area) return;
        navigator.clipboard?.writeText(area.value)?.catch(() => {
        });
        origin.setAttribute("data-copied", "true");
        const timer = setTimeout(() => origin.removeAttribute("data-copied"), 1500);
        helpers.registerDisposer(() => clearTimeout(timer), "rui-code-editor-copy");
      };
      head.append(copyBtn);
    }
    root.append(head);
    const body = el("div", {
      class: "rui-code-editor-body",
      style: `min-height:${minHeight};` + (maxHeight ? `max-height:${maxHeight};overflow:auto;` : "")
    });
    const gutter = el("div", {
      class: "rui-code-editor-gutter",
      "aria-hidden": "true",
      // Identical font metrics to the textarea, or the numbers drift out of
      // register; `overflow: hidden` is what makes the scroll sync below work.
      style: `font-size:13px;line-height:${CODE_LINE_HEIGHT}px;overflow:hidden;`
    });
    if (showGutter) {
      fillGutter(gutter, source);
      body.append(gutter);
    }
    const textarea = el("textarea", {
      class: "rui-code-editor-textarea",
      id,
      name: asString(props.name) || id,
      spellcheck: "false",
      autocorrect: "off",
      autocapitalize: "off",
      placeholder: asString(props.placeholder),
      readonly: readonly ? "" : null,
      disabled: disabled ? "" : null,
      // `min-width: 0` defeats the grid column's automatic minimum size, which
      // otherwise refuses to shrink below the textarea's min-content width and
      // pushes the code out of the `overflow: hidden` root on narrow viewports.
      style: `tab-size:${tabSize};-moz-tab-size:${tabSize};line-height:${CODE_LINE_HEIGHT}px;min-width:0;`
    });
    textarea.value = source;
    const readSource = (n) => {
      const target = n;
      sourceSlot.set(target.value);
      if (showGutter) {
        const liveGutter = target.closest(".rui-code-editor")?.querySelector(".rui-code-editor-gutter");
        if (liveGutter) fillGutter(liveGutter, target.value);
      }
      return target.value;
    };
    const stateName = node.argMeta?.[1]?.stateRef;
    if (stateName && !inert) {
      helpers.bindState(textarea, stateName, { event: "input", getValue: readSource });
    } else {
      textarea.oninput = (event) => {
        readSource(event.currentTarget ?? event.target);
      };
    }
    textarea.onscroll = (event) => {
      const target = event.currentTarget ?? event.target;
      const liveGutter = target.closest(".rui-code-editor")?.querySelector(".rui-code-editor-gutter");
      if (liveGutter) liveGutter.scrollTop = target.scrollTop;
    };
    textarea.onkeydown = (event) => {
      const e = event;
      const target = e.currentTarget ?? e.target;
      if ((e.metaKey || e.ctrlKey) && e.key.toLowerCase() === "s") {
        e.preventDefault();
        helpers.invoke(props.onSave, target.value);
        return;
      }
      if (e.key !== "Tab" || e.altKey || e.ctrlKey || e.metaKey) return;
      if (target.readOnly || target.disabled) return;
      e.preventDefault();
      reindent(target, e.shiftKey, " ".repeat(tabSize));
    };
    if (!inert) {
      attachOnChange(textarea, props.onChange, helpers, {
        event: "input",
        getValue: readSource
      });
    }
    body.append(textarea);
    root.append(body);
    return withFieldShell(root, props, { idKey: "id" });
  }
};
const CONTEXT_TRIGGERS = ["contextmenu", "click", "longpress"];
const CONTEXT_PLACEMENTS = ["bottom", "top", "left", "right"];
const LONG_PRESS_MS = 500;
const LONG_PRESS_SLOP = 10;
function extractContextItem(raw) {
  if (!raw) return null;
  if (typeof raw === "object") {
    const node = raw;
    if (node.__kind === "Component" && node.name === "MenuItem" && Array.isArray(node.args)) {
      const args = node.args;
      return {
        label: asString(args[0]),
        action: args[1],
        icon: asString(args[2]),
        shortcut: asString(args[3]),
        variant: asString(args[4], "default"),
        disabled: asBoolean(args[5]),
        separator: false
      };
    }
    if (node.__kind === "Component" && node.name === "MenuSeparator") {
      return { label: "", action: null, icon: "", shortcut: "", variant: "default", disabled: false, separator: true };
    }
    const r = raw;
    if (r.separator) {
      return { label: "", action: null, icon: "", shortcut: "", variant: "default", disabled: false, separator: true };
    }
    return {
      label: asString(r.label),
      action: r.action,
      icon: asString(r.icon),
      shortcut: asString(r.shortcut),
      variant: asString(r.variant, "default"),
      disabled: asBoolean(r.disabled),
      separator: false
    };
  }
  return null;
}
const POINTER_MENUS = /* @__PURE__ */ new WeakMap();
const LONG_PRESSES = /* @__PURE__ */ new WeakMap();
const cancelLongPress = (target) => {
  if (!target) return;
  const pending = LONG_PRESSES.get(target);
  if (!pending) return;
  clearTimeout(pending.timer);
  LONG_PRESSES.delete(target);
};
const pointerMenuFrom = (origin) => {
  if (!origin) return null;
  const root = origin.closest(".rui-context-menu");
  const byRoot = root ? POINTER_MENUS.get(root) : void 0;
  if (byRoot) return byRoot;
  const panel = origin.closest(".rui-context-menu-pop");
  return (panel ? POINTER_MENUS.get(panel) : null) ?? null;
};
const livePanelFor = (origin) => {
  if (!origin) return null;
  const registered = pointerMenuFrom(origin)?.panel;
  if (registered) return registered;
  return origin.closest(".rui-context-menu-pop") ?? origin.closest(".rui-context-menu")?.querySelector(".rui-context-menu-pop") ?? null;
};
const teardownMenu = (origin) => {
  const state = pointerMenuFrom(origin);
  if (state) {
    state.close();
    return;
  }
  const panel = livePanelFor(origin);
  if (!panel) return;
  panel.setAttribute("data-open", "false");
  origin?.closest(".rui-context-menu")?.setAttribute("data-open", "false");
  closeFloating(panel);
  disposeDismissListeners(panel);
  disposeDismissListeners(origin?.closest(".rui-context-menu"));
};
const pointerAnchor = (clientX, clientY) => {
  const rect = {
    x: clientX,
    y: clientY,
    left: clientX,
    right: clientX,
    top: clientY,
    bottom: clientY,
    width: 0,
    height: 0,
    toJSON: () => ({})
  };
  return { getBoundingClientRect: () => rect };
};
const openMenuAt = (origin, anchor, ctx) => {
  teardownMenu(origin);
  const liveRoot = origin.closest(".rui-context-menu");
  const panel = liveRoot?.querySelector(".rui-context-menu-pop");
  if (!liveRoot || !panel) return;
  const wrap = liveRoot.querySelector(".rui-context-menu-target");
  liveRoot.setAttribute("data-open", "true");
  panel.setAttribute("data-open", "true");
  wrap?.setAttribute("aria-expanded", "true");
  wrap?.setAttribute("data-state", "open");
  wrap?.querySelector(".rui-icon-button")?.setAttribute("data-state", "open");
  openFloating(panel, {
    anchor,
    side: ctx.side,
    align: "start",
    offset: ctx.offset,
    layer: "dropdown"
  });
  const host = panel.getRootNode();
  const dismissHost = ctx.trigger === "click" ? liveRoot : panel;
  const report = () => {
    ctx.openSlot.set(false);
    if (ctx.stateName) ctx.helpers.setState(ctx.stateName, false);
    close();
    ctx.helpers.invoke(ctx.onOpenChange, false);
  };
  const onContextElsewhere = (event) => {
    if (event.composedPath().includes(panel)) return;
    report();
  };
  const release = () => {
    POINTER_MENUS.delete(liveRoot);
    POINTER_MENUS.delete(panel);
    host.removeEventListener("contextmenu", onContextElsewhere, true);
    disposeDismissListeners(dismissHost);
  };
  const close = () => {
    liveRoot.setAttribute("data-open", "false");
    panel.setAttribute("data-open", "false");
    wrap?.setAttribute("aria-expanded", "false");
    wrap?.removeAttribute("data-state");
    wrap?.querySelector(".rui-icon-button")?.removeAttribute("data-state");
    const doc = panel.getRootNode();
    const hadFocus = panel.contains(doc.activeElement);
    closeFloating(panel);
    release();
    if (hadFocus) wrap?.focus();
  };
  ctx.helpers.registerDisposer(release, "rui-context-menu");
  POINTER_MENUS.set(liveRoot, { panel, close });
  POINTER_MENUS.set(panel, { panel, close });
  installDismissListeners({
    liveRoot: dismissHost,
    key: "rui-context-menu",
    onDismiss: () => {
      if (!POINTER_MENUS.has(panel)) return;
      report();
    }
  });
  host.addEventListener("contextmenu", onContextElsewhere, true);
  const rows = panel.querySelectorAll("[role=menuitem]");
  (panel.querySelector("[role=menuitem]:not([aria-disabled='true'])") ?? rows[0])?.focus();
};
const setMenuOpen = (origin, next, anchor, ctx) => {
  ctx.openSlot.set(next);
  if (ctx.stateName) ctx.helpers.setState(ctx.stateName, next);
  if (next) openMenuAt(origin, anchor, ctx);
  else teardownMenu(origin);
  ctx.helpers.invoke(ctx.onOpenChange, next);
};
const ContextMenu = {
  name: "ContextMenu",
  description: 'Right-click (or long-press on touch) menu that attaches to a child node. Wraps `target` and shows the menu at the pointer. Items are `MenuItem(...)` nodes, `MenuSeparator()` entries, or `{label, action, icon?, shortcut?, variant?, disabled?, separator?}` objects. Set `trigger: "click"` for the overflow-menu pattern, `disabled` to suppress it on locked rows, and pass a `$variable` as `open` to observe or drive the open state. Shift+F10 (or the ContextMenu key) opens it from the keyboard, and ArrowUp/ArrowDown/Home/End plus typeahead move between items while Escape closes it and returns focus to the target. Use on table rows, tree nodes, kanban cards, file browser entries.',
  props: [
    { name: "target", type: "Node", description: "Child node the menu is bound to" },
    { name: "items", type: "any[]", description: "MenuItem nodes or {label, action} objects" },
    { name: "label", type: "string", optional: true, description: "ARIA label for the menu" },
    { name: "trigger", type: "string", optional: true, enum: CONTEXT_TRIGGERS, description: "What opens the menu (default `contextmenu`)" },
    { name: "disabled", type: "boolean", optional: true, description: "Never open the menu (locked / read-only rows)" },
    { name: "placement", type: "string", optional: true, enum: CONTEXT_PLACEMENTS, aliases: ["side"], description: "Which side of the anchor the menu opens on (default `bottom`)" },
    { name: "offset", type: "number", optional: true, description: "Gap in px between the anchor and the menu (default 0)" },
    { name: "open", type: "boolean", optional: true, description: "Open state — pass a $variable for two-way binding" },
    { name: "onOpenChange", type: "callable", optional: true, description: "Called with the new boolean open state" }
  ],
  render: (node, props, helpers) => {
    const disabled = asBoolean(props.disabled);
    const trigger = asString(props.trigger, "contextmenu");
    const side = asString(props.placement, "bottom");
    const offset = Math.max(0, asNumber(props.offset, 0));
    const declared = props.open === void 0 ? null : asBoolean(props.open);
    const openSlot = helpers.useInstanceState("open", declared ?? false);
    const declaredSlot = helpers.useInstanceState("declaredOpen", null);
    if (declared !== null && declared !== declaredSlot.get()) {
      declaredSlot.set(declared);
      openSlot.set(declared);
    }
    const isOpen = openSlot.get() && !disabled;
    const ctx = {
      openSlot,
      stateName: node.argMeta?.[7]?.stateRef,
      onOpenChange: props.onOpenChange,
      helpers,
      side,
      offset,
      trigger
    };
    const root = el("div", {
      class: "rui-context-menu",
      "data-open": isOpen ? "true" : "false",
      "data-disabled": disabled ? "true" : "false"
    });
    const wrap = el("div", {
      class: "rui-context-menu-target",
      // Focusable so the Shift+F10 / ContextMenu-key opener is reachable even
      // when the wrapped target holds nothing focusable of its own.
      tabindex: disabled ? null : "0",
      "aria-haspopup": disabled ? null : "menu",
      "aria-expanded": disabled ? null : isOpen ? "true" : "false",
      "data-state": isOpen ? "open" : null,
      // Suppress the iOS callout that would otherwise race the long press.
      style: trigger === "longpress" ? "-webkit-touch-callout:none;touch-action:manipulation;user-select:none;-webkit-user-select:none;" : "-webkit-touch-callout:none;"
    });
    wrap.append(helpers.renderNode(props.target));
    root.append(wrap);
    const menu = el("div", {
      class: "rui-context-menu-pop",
      role: "menu",
      "aria-label": asString(props.label) || null,
      "data-open": isOpen ? "true" : "false"
    });
    const items = asArray(props.items).map(extractContextItem).filter((i) => i !== null);
    for (const item of items) {
      if (item.separator) {
        menu.append(el("div", { class: "rui-menu-separator", role: "separator" }));
        continue;
      }
      const btn = el("button", {
        type: "button",
        class: "rui-menu-item",
        role: "menuitem",
        "data-variant": item.variant,
        // `aria-disabled`, not the native attribute: a `role="menu"` row must
        // stay focusable so a keyboard user learns the action exists but is
        // unavailable. No click handler is attached below, so it is inert.
        "aria-disabled": item.disabled ? "true" : null,
        "data-disabled": item.disabled ? "true" : null,
        // Roving focus: the menu is one tab stop and Up/Down move within it.
        tabindex: "-1"
      });
      const iconNode = renderIcon(item.icon, { className: "rui-menu-item-icon" });
      if (iconNode) btn.append(iconNode);
      btn.append(el("span", { class: "rui-menu-item-label" }, [item.label]));
      if (item.shortcut) btn.append(el("span", { class: "rui-menu-item-shortcut" }, [item.shortcut]));
      if (!item.disabled) {
        btn.onclick = (event) => {
          const origin = event.currentTarget ?? event.target;
          openSlot.set(false);
          if (ctx.stateName) helpers.setState(ctx.stateName, false);
          teardownMenu(origin);
          helpers.invoke(props.onOpenChange, false);
          helpers.invoke(item.action);
        };
      }
      menu.append(btn);
    }
    root.append(menu);
    const openFromPointer = (origin, clientX, clientY) => {
      setMenuOpen(origin, true, pointerAnchor(clientX, clientY), ctx);
    };
    if (!disabled && (trigger === "contextmenu" || trigger === "longpress")) {
      wrap.oncontextmenu = (event) => {
        event.preventDefault();
        openFromPointer(event.currentTarget ?? event.target, event.clientX, event.clientY);
      };
    }
    if (!disabled && trigger === "click") {
      wrap.onclick = (event) => {
        const live = event.currentTarget ?? event.target;
        setMenuOpen(live, !openSlot.get(), live, ctx);
      };
    }
    if (!disabled && trigger !== "click") {
      wrap.onpointerdown = (event) => {
        const e = event;
        if (trigger === "contextmenu" && e.pointerType !== "touch" && e.pointerType !== "pen") return;
        if (e.button !== 0 && e.pointerType === "mouse") return;
        const origin = e.currentTarget ?? e.target;
        cancelLongPress(origin);
        const timer = setTimeout(() => {
          LONG_PRESSES.delete(origin);
          openFromPointer(origin, e.clientX, e.clientY);
        }, LONG_PRESS_MS);
        LONG_PRESSES.set(origin, { timer, x: e.clientX, y: e.clientY });
        helpers.registerDisposer(() => cancelLongPress(origin), "rui-context-longpress");
      };
      wrap.onpointermove = (event) => {
        const e = event;
        const origin = e.currentTarget ?? e.target;
        const pending = LONG_PRESSES.get(origin);
        if (!pending) return;
        if (Math.abs(e.clientX - pending.x) > LONG_PRESS_SLOP || Math.abs(e.clientY - pending.y) > LONG_PRESS_SLOP) cancelLongPress(origin);
      };
      wrap.onpointerup = (event) => cancelLongPress(event.currentTarget ?? event.target);
      wrap.onpointercancel = (event) => cancelLongPress(event.currentTarget ?? event.target);
      wrap.onpointerleave = (event) => cancelLongPress(event.currentTarget ?? event.target);
    }
    const onKeydown = (event) => {
      const e = event;
      const origin = e.currentTarget ?? e.target;
      const from = e.target ?? origin;
      if (origin.classList.contains("rui-context-menu") && from.closest(".rui-context-menu-pop")) return;
      if (e.key === "Escape") {
        if (!openSlot.get()) return;
        e.preventDefault();
        openSlot.set(false);
        if (ctx.stateName) helpers.setState(ctx.stateName, false);
        teardownMenu(from);
        helpers.invoke(props.onOpenChange, false);
        return;
      }
      if (disabled) return;
      if (e.key === "ContextMenu" || e.shiftKey && e.key === "F10") {
        e.preventDefault();
        const liveWrap = origin.closest(".rui-context-menu")?.querySelector(".rui-context-menu-target");
        if (!liveWrap) return;
        setMenuOpen(liveWrap, true, liveWrap, ctx);
        return;
      }
      const navKey = e.key === "ArrowDown" || e.key === "ArrowUp" || e.key === "Home" || e.key === "End";
      const typeahead = !navKey && e.key.length === 1 && !e.ctrlKey && !e.metaKey && !e.altKey;
      if (!navKey && !typeahead) return;
      const panel = livePanelFor(from);
      if (!panel || panel.getAttribute("data-open") !== "true") return;
      if (typeahead && !panel.contains(from)) return;
      const options = Array.from(panel.querySelectorAll("[role=menuitem]"));
      if (options.length === 0) return;
      const active = panel.getRootNode().activeElement;
      const found = options.findIndex((o) => o === active);
      if (typeahead) {
        const ch = e.key.toLowerCase();
        for (let i = 1; i <= options.length; i += 1) {
          const candidate = options[(Math.max(found, 0) + i) % options.length];
          const text = candidate.querySelector(".rui-menu-item-label")?.textContent ?? "";
          if (!text.trim().toLowerCase().startsWith(ch)) continue;
          e.preventDefault();
          candidate.focus();
          return;
        }
        return;
      }
      e.preventDefault();
      const next = e.key === "Home" ? 0 : e.key === "End" ? options.length - 1 : e.key === "ArrowDown" ? (found + 1) % options.length : (found <= 0 ? options.length : found) - 1;
      options[next].focus();
    };
    root.onkeydown = onKeydown;
    menu.onkeydown = onKeydown;
    if (isOpen) {
      deferToPaint(() => {
        if (!root.isConnected) return;
        const liveWrap = root.querySelector(".rui-context-menu-target");
        if (liveWrap && !pointerMenuFrom(root)) openMenuAt(liveWrap, liveWrap, ctx);
      });
    }
    return root;
  }
};
const COLOR_FORMATS = ["hex", "rgb", "hsl"];
const DEFAULT_SWATCHES = [
  "#0f172a",
  "#334155",
  "#64748b",
  "#94a3b8",
  "#ef4444",
  "#f59e0b",
  "#facc15",
  "#10b981",
  "#06b6d4",
  "#3b82f6",
  "#6366f1",
  "#8b5cf6"
];
const clamp255 = (n) => Math.max(0, Math.min(255, Math.round(n)));
const clamp01 = (n) => Math.max(0, Math.min(1, n));
function fromHexDigits(hex) {
  const at = (i) => parseInt(hex.slice(i, i + 2), 16);
  return { r: at(0), g: at(2), b: at(4), a: hex.length === 8 ? at(6) / 255 : 1 };
}
function hslToRgb(h, s, l) {
  const hue = (h % 360 + 360) % 360;
  const c = (1 - Math.abs(2 * l - 1)) * s;
  const x = c * (1 - Math.abs(hue / 60 % 2 - 1));
  const m = l - c / 2;
  const wedges = [
    [c, x, 0],
    [x, c, 0],
    [0, c, x],
    [0, x, c],
    [x, 0, c],
    [c, 0, x]
  ];
  const [r1, g1, b1] = wedges[Math.floor(hue / 60) % 6] ?? [0, 0, 0];
  return { r: clamp255((r1 + m) * 255), g: clamp255((g1 + m) * 255), b: clamp255((b1 + m) * 255) };
}
function rgbToHsl(c) {
  const r = c.r / 255;
  const g = c.g / 255;
  const b = c.b / 255;
  const max = Math.max(r, g, b);
  const min = Math.min(r, g, b);
  const l = (max + min) / 2;
  const d = max - min;
  if (d === 0) return { h: 0, s: 0, l: Math.round(l * 100) };
  const s = d / (1 - Math.abs(2 * l - 1));
  const h = max === r ? 60 * ((g - b) / d % 6) : max === g ? 60 * ((b - r) / d + 2) : 60 * ((r - g) / d + 4);
  return { h: Math.round((h % 360 + 360) % 360), s: Math.round(s * 100), l: Math.round(l * 100) };
}
function parseColor(raw) {
  const value = asString(raw).trim().toLowerCase();
  if (!value) return null;
  if (value.startsWith("#")) {
    const hex = value.slice(1);
    if (!/^[0-9a-f]+$/.test(hex)) return null;
    if (hex.length === 3 || hex.length === 4) {
      return fromHexDigits(hex.split("").map((ch) => ch + ch).join(""));
    }
    if (hex.length === 6 || hex.length === 8) return fromHexDigits(hex);
    return null;
  }
  const fn = /^(rgba?|hsla?)\(([^)]*)\)$/.exec(value);
  if (!fn) return null;
  const parts = fn[2].split(/[,/\s]+/).map((p) => p.trim()).filter(Boolean);
  if (parts.length < 3) return null;
  const num2 = (raw2) => {
    const text = asString(raw2);
    const n = Number(text.endsWith("%") ? text.slice(0, -1) : text);
    return Number.isFinite(n) ? n : NaN;
  };
  const alphaText = parts[3];
  const alpha = alphaText === void 0 ? 1 : clamp01(alphaText.endsWith("%") ? num2(alphaText) / 100 : num2(alphaText));
  if (Number.isNaN(alpha)) return null;
  if (fn[1].startsWith("rgb")) {
    const [r, g, b] = [num2(parts[0]), num2(parts[1]), num2(parts[2])];
    if ([r, g, b].some(Number.isNaN)) return null;
    const pct = (parts[0] ?? "").endsWith("%");
    const scale = (n) => clamp255(pct ? n / 100 * 255 : n);
    return { r: scale(r), g: scale(g), b: scale(b), a: alpha };
  }
  const [h, s, l] = [num2(parts[0]), num2(parts[1]), num2(parts[2])];
  if ([h, s, l].some(Number.isNaN)) return null;
  return { ...hslToRgb(h, clamp01(s / 100), clamp01(l / 100)), a: alpha };
}
const hexByte = (n) => clamp255(n).toString(16).padStart(2, "0");
const toHex6 = (c) => `#${hexByte(c.r)}${hexByte(c.g)}${hexByte(c.b)}`;
function formatColor(c, format, allowAlpha) {
  const a = allowAlpha ? clamp01(c.a) : 1;
  const alphaText = String(Math.round(a * 1e3) / 1e3);
  if (format === "rgb") {
    return a < 1 ? `rgba(${c.r}, ${c.g}, ${c.b}, ${alphaText})` : `rgb(${c.r}, ${c.g}, ${c.b})`;
  }
  if (format === "hsl") {
    const { h, s, l } = rgbToHsl(c);
    return a < 1 ? `hsla(${h}, ${s}%, ${l}%, ${alphaText})` : `hsl(${h}, ${s}%, ${l}%)`;
  }
  return a < 1 ? `${toHex6(c)}${hexByte(a * 255)}` : toHex6(c);
}
function normaliseHex(value, allowAlpha = false) {
  const parsed = parseColor(value);
  return parsed ? formatColor(parsed, "hex", allowAlpha) : "";
}
const ColorPicker = {
  name: "ColorPicker",
  description: 'Hex / RGB / HSL color form control with preset swatches. Pairs a native `<input type="color">` chip with a text input and a row of preset swatches. Pass a `$variable` as `value` (e.g. `"#6366f1"`) for two-way binding; `format` picks the notation written back (`hex` by default) and `allowAlpha` keeps the alpha channel instead of dropping it. Use `showInput` / `showSwatches` to render only the half you need. Use for theme builders, label color pickers, and any "pick a color" surface.',
  props: [
    { name: "id", type: "string" },
    { name: "value", type: "string", optional: true, description: "Bound color value (typically $variable)" },
    { name: "label", type: "string", optional: true },
    { name: "swatches", type: "string[]", optional: true, description: "Preset colors (default to a 12-color palette)" },
    { name: "disabled", type: "boolean", optional: true },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"], description: "Called with the newly-selected color string" },
    { name: "format", type: "string", optional: true, enum: COLOR_FORMATS, description: "Notation written back to state / onChange (default `hex`)" },
    { name: "allowAlpha", type: "boolean", optional: true, description: "Keep the alpha channel (8-digit hex / rgba / hsla) instead of dropping it" },
    { name: "showInput", type: "boolean", optional: true, description: "Show the text input (default true)" },
    { name: "showSwatches", type: "boolean", optional: true, description: "Show the preset swatch row (default true)" },
    { name: "name", type: "string", optional: true, description: "Form field name (defaults to `id`)" },
    { name: "hint", type: "string", optional: true, aliases: ["helperText"], description: "Helper text rendered below the control" },
    { name: "error", type: "string", optional: true, description: "Validation error rendered below the control (marks it invalid)" },
    { name: "required", type: "boolean", optional: true, description: "Mark the field required (adds a `*`)" },
    ...fieldShellExtraProps()
  ],
  render: (node, props, helpers) => {
    const id = asString(props.id);
    const disabled = asBoolean(props.disabled);
    const format = asString(props.format, "hex");
    const allowAlpha = asBoolean(props.allowAlpha);
    const showInput = props.showInput === void 0 ? true : asBoolean(props.showInput);
    const showSwatches = props.showSwatches === void 0 ? true : asBoolean(props.showSwatches);
    const label = asString(props.label);
    const parsed = parseColor(props.value);
    const display = parsed ? formatColor(parsed, format, allowAlpha) : asString(props.value);
    const chipHex = parsed ? toHex6(parsed) : "#000000";
    const root = el("div", { class: "rui-color-picker", "data-disabled": disabled ? "true" : "false" });
    const row = el("div", { class: "rui-color-picker-row" });
    const colorInput = el("input", {
      type: "color",
      class: "rui-color-picker-color",
      id,
      name: asString(props.name) || id,
      // `valueAttr` semantics: only assert a value when the program supplied
      // one, so the reconciler leaves a user-picked colour alone otherwise.
      value: props.value === void 0 || props.value === null ? null : chipHex,
      disabled: disabled ? "" : null
    });
    colorInput.value = chipHex;
    const textInput = el("input", {
      type: "text",
      class: "rui-color-picker-hex",
      id: id ? `${id}-value` : null,
      // The only `<label for>` points at the chip, so the text field needs its
      // own name — `placeholder` stops being exposed once a value is present.
      "aria-label": label ? `${label} color value` : "Color value",
      value: props.value === void 0 || props.value === null ? null : display,
      placeholder: format === "hex" ? "#000000" : "rgb(0, 0, 0)",
      disabled: disabled ? "" : null,
      autocomplete: "off",
      autocapitalize: "off",
      spellcheck: "false",
      inputmode: "text",
      maxlength: format === "hex" ? "9" : "32"
    });
    textInput.value = display;
    const readColor = (n) => {
      const live = n.closest(".rui-color-picker");
      const chip = live?.querySelector(".rui-color-picker-color") ?? n;
      const field = live?.querySelector(".rui-color-picker-hex") ?? null;
      const base = parseColor(chip.value) ?? { r: 0, g: 0, b: 0, a: 1 };
      const alpha = allowAlpha ? parseColor(field?.value)?.a ?? 1 : 1;
      const out = formatColor({ ...base, a: alpha }, format, allowAlpha);
      const doc = n.getRootNode();
      if (field && doc.activeElement !== field) field.value = out;
      if (live) {
        const canonical = normaliseHex(out, allowAlpha);
        for (const swatch of live.querySelectorAll(".rui-color-picker-swatch")) {
          const own = swatch.getAttribute("data-color") ?? "";
          swatch.setAttribute("data-active", own === canonical ? "true" : "false");
        }
      }
      return out;
    };
    const stateName = node.argMeta?.[1]?.stateRef;
    if (stateName && !disabled) {
      helpers.bindState(colorInput, stateName, { event: "input", getValue: readColor });
    } else if (!disabled) {
      colorInput.oninput = (event) => {
        readColor(event.currentTarget ?? event.target);
      };
    }
    if (!disabled) {
      attachOnChange(colorInput, props.onChange, helpers, { event: "input", getValue: readColor });
    }
    if (!disabled) {
      textInput.oninput = (event) => {
        const field = event.currentTarget ?? event.target;
        const next = parseColor(field.value);
        if (!next) {
          field.setAttribute("aria-invalid", "true");
          return;
        }
        field.removeAttribute("aria-invalid");
        const chip = field.closest(".rui-color-picker")?.querySelector(".rui-color-picker-color");
        if (!chip) return;
        chip.value = toHex6(next);
        chip.dispatchEvent(new Event("input", { bubbles: true }));
      };
    }
    if (!showInput) {
      textInput.setAttribute("hidden", "");
      textInput.setAttribute("aria-hidden", "true");
      textInput.setAttribute("tabindex", "-1");
    }
    row.append(colorInput, textInput);
    root.append(row);
    if (showSwatches) {
      const swatchRow = el("div", { class: "rui-color-picker-swatches" });
      const swatches = asArray(props.swatches).map((c) => asString(c)).filter(Boolean);
      const palette = swatches.length > 0 ? swatches : DEFAULT_SWATCHES;
      const activeHex = normaliseHex(display, allowAlpha);
      for (const swatch of palette) {
        const safeColor = normaliseHex(swatch, allowAlpha) || sanitiseCssColor(swatch);
        if (!safeColor) continue;
        const canonical = normaliseHex(safeColor, allowAlpha);
        const btn = el("button", {
          type: "button",
          class: "rui-color-picker-swatch",
          style: `background:${safeColor}`,
          "aria-label": safeColor,
          title: safeColor,
          "data-color": canonical,
          "data-active": canonical !== "" && canonical === activeHex ? "true" : "false",
          disabled: disabled ? "" : null
        });
        btn.onclick = (event) => {
          const origin = event.currentTarget ?? event.target;
          const live = origin.closest(".rui-color-picker");
          const chip = live?.querySelector(".rui-color-picker-color");
          if (!chip) return;
          const picked = parseColor(origin.getAttribute("data-color") ?? safeColor);
          if (!picked) return;
          const field = live?.querySelector(".rui-color-picker-hex");
          if (field) field.value = formatColor(picked, format, allowAlpha);
          chip.value = toHex6(picked);
          chip.dispatchEvent(new Event("input", { bubbles: true }));
        };
        swatchRow.append(btn);
      }
      root.append(swatchRow);
    }
    return withFieldShell(root, props, { idKey: "id" });
  }
};
const Gauge = {
  name: "Gauge",
  description: 'Half-doughnut gauge indicator for a single value between `min` and `max`. The inner value renders as a plain number — pass `unit` ("%", "ms", "pts") or `format: "percent"` for a suffix, or `label` to override the text entirely. Pass `thresholds: [{value, tone}]` to band the track and recolour the arc as the value crosses each band. Use for KPI thresholds (uptime %, score, capacity, NPS, page-speed).',
  props: [
    { name: "value", type: "number" },
    { name: "min", type: "number", optional: true, description: "Lower bound (default 0)" },
    { name: "max", type: "number", optional: true, description: "Upper bound (default 100)" },
    { name: "label", type: "string", optional: true, description: "Inner label override (defaults to the formatted value)" },
    { name: "tone", aliases: ["variant"], type: "string", optional: true, enum: ["primary", "success", "warning", "danger", "info"] },
    { name: "size", type: "string", optional: true, enum: ["sm", "md", "lg"] },
    { name: "caption", type: "string", optional: true, description: "Small caption below the gauge" },
    { name: "unit", type: "string", optional: true, aliases: ["suffix"], description: 'Suffix appended to the value ("%", "ms", "pts")' },
    { name: "format", type: "string", optional: true, enum: ["number", "percent", "compact"], description: 'Number formatting for the inner label (default "number")' },
    { name: "thresholds", type: "{value: number, tone: string}[]", optional: true, description: "Bands drawn on the track; the highest one the value passes colours the arc" },
    { name: "showRange", type: "boolean", optional: true, description: "Print the min / max end labels so the scale is readable" },
    ...CHART_A11Y_PROPS
  ],
  render: (_node, props) => {
    const a11y = readChartA11y(props);
    const min = asNumber(props.min, 0);
    const max = Math.max(min + 1, asNumber(props.max, 100));
    const value = Math.max(min, Math.min(max, asNumber(props.value, min)));
    const pct = (value - min) / (max - min);
    const thresholds = readThresholds(props.thresholds, min, max);
    const crossed = thresholds.filter((t) => value >= t.value);
    const tone = crossed.length > 0 ? crossed[crossed.length - 1].tone : resolveTone(props.tone);
    const size2 = asString(props.size, "md");
    const showRange = asBoolean(props.showRange);
    const px = size2 === "lg" ? 220 : size2 === "sm" ? 140 : 180;
    const half = px / 2;
    const stroke = size2 === "lg" ? 18 : size2 === "sm" ? 10 : 14;
    const r = half - stroke;
    const root = el("div", {
      // `rui-chart` supplies the card frame every sibling chart gets; without
      // it a Gauge next to a BarChart rendered as an unframed transparent block.
      class: "rui-chart rui-gauge",
      "data-tone": tone,
      "data-size": size2
    });
    const svg = createSvg(px, half + stroke + (showRange ? 22 : 4));
    nameChart(svg, { name: "", decorative: true }, "");
    const cx = half;
    const cy = half;
    const startX = cx - r;
    const startY = cy;
    const endX = cx + r;
    const endY = cy;
    const track = `M${startX},${startY} A${r},${r} 0 0 1 ${endX},${endY}`;
    svg.append(svgEl$1("path", {
      d: track,
      fill: "none",
      stroke: "var(--rui-color-border, #e2e8f0)",
      "stroke-width": String(stroke),
      "stroke-linecap": "round"
    }));
    thresholds.forEach((threshold, i) => {
      const from = (threshold.value - min) / (max - min);
      const to = i + 1 < thresholds.length ? (thresholds[i + 1].value - min) / (max - min) : 1;
      if (to <= from) return;
      const [x1, y1] = arcPoint(cx, cy, r, from);
      const [x2, y2] = arcPoint(cx, cy, r, to);
      svg.append(svgEl$1("path", {
        d: `M${x1.toFixed(2)},${y1.toFixed(2)} A${r},${r} 0 0 1 ${x2.toFixed(2)},${y2.toFixed(2)}`,
        fill: "none",
        stroke: `var(--rui-color-${threshold.tone}, ${colorAt(0)})`,
        "stroke-width": String(stroke),
        "stroke-opacity": "0.22",
        "stroke-linecap": "butt"
      }));
    });
    if (pct > 0) {
      const arcLength = Math.PI * r;
      svg.append(svgEl$1("path", {
        d: track,
        fill: "none",
        stroke: `var(--rui-color-${tone}, ${colorAt(0)})`,
        "stroke-width": String(stroke),
        "stroke-linecap": "round",
        "stroke-dasharray": `${(arcLength * pct).toFixed(2)} ${(arcLength + 1).toFixed(2)}`,
        class: "rui-gauge-arc"
      }));
    }
    if (showRange) {
      const baseY = cy + stroke / 2 + 14;
      svg.append(svgEl$1("text", {
        x: String(Math.max(2, startX - stroke / 2)),
        y: baseY.toFixed(1),
        "text-anchor": "start",
        class: "rui-chart-tick"
      }, [formatNumeric(min)]));
      svg.append(svgEl$1("text", {
        x: String(Math.min(px - 2, endX + stroke / 2)),
        y: baseY.toFixed(1),
        "text-anchor": "end",
        class: "rui-chart-tick"
      }, [formatNumeric(max)]));
    }
    root.append(svg);
    const unit = asString(props.unit).trim();
    const format = asString(props.format, "number").toLowerCase();
    const autoLabel = formatGaugeValue(value, format, unit);
    const label = asString(props.label) || autoLabel;
    const caption = asString(props.caption);
    if (!a11y.decorative) {
      root.setAttribute("role", "meter");
      root.setAttribute("aria-valuenow", String(value));
      root.setAttribute("aria-valuemin", String(min));
      root.setAttribute("aria-valuemax", String(max));
      root.setAttribute("aria-valuetext", label);
      root.setAttribute("aria-label", a11y.name || caption || asString(props.label) || "Gauge");
    }
    root.append(el("div", { class: "rui-gauge-value" }, [label]));
    if (caption) root.append(el("div", { class: "rui-gauge-caption" }, [caption]));
    return root;
  }
};
function arcPoint(cx, cy, r, fraction) {
  const angle = Math.PI * Math.max(0, Math.min(1, fraction));
  return [cx - r * Math.cos(angle), cy - r * Math.sin(angle)];
}
function readThresholds(raw, min, max) {
  return asArray(raw).map((entry) => {
    const r = entry ?? {};
    return {
      value: asNumber(r.value ?? r.at, NaN),
      tone: resolveTone(r.tone, "warning")
    };
  }).filter((t) => Number.isFinite(t.value) && t.value > min && t.value <= max).sort((a, b) => a.value - b.value);
}
function formatGaugeValue(value, format, unit) {
  const num2 = format === "compact" ? formatNumeric(value) : plainNumber(value);
  const suffix = unit || (format === "percent" ? "%" : "");
  if (!suffix) return num2;
  return /^[a-z]/i.test(suffix) ? `${num2} ${suffix}` : `${num2}${suffix}`;
}
function plainNumber(value) {
  if (Math.abs(value) >= 1e3) return Math.round(value).toLocaleString();
  if (value % 1 === 0) return String(value);
  return value.toFixed(2).replace(/\.?0+$/, "");
}
const Heatmap = {
  name: "Heatmap",
  description: "Color-intensity matrix grid (calendar-style or correlation-style). Pass `xLabels`, `yLabels`, and a `values` array of arrays (rows × columns). Cell intensity scales across the data range — pin it with `min`/`max` when two heatmaps must be comparable (or for a -1..1 correlation matrix). Set `showValues: false` for a GitHub-style colour-only grid. Use for activity heatmaps, schedule density, correlation matrices.",
  props: [
    { name: "xLabels", type: "string[]" },
    { name: "yLabels", type: "string[]" },
    { name: "values", type: "number[][]", description: "Matrix indexed by row (y), then column (x)" },
    { name: "title", type: "string", optional: true },
    { name: "tone", aliases: ["variant"], type: "string", optional: true, enum: ["primary", "success", "warning", "danger", "info"] },
    { name: "showValues", type: "boolean", optional: true, description: "Print the number inside each cell (default true)" },
    { name: "min", type: "number", optional: true, description: "Value mapped to the lightest cell (default 0 or the data minimum)" },
    { name: "max", type: "number", optional: true, description: "Value mapped to the most saturated cell (default the data maximum)" },
    { name: "valueFormat", type: "string", optional: true, enum: ["value", "compact", "percent"], description: 'How cell numbers are formatted (default "value")' },
    { name: "emptyText", type: "string", optional: true, description: 'Message shown when there is no data (default "No data")' },
    { name: "onCellClick", type: "callable", optional: true, description: "(value, xLabel, yLabel) => void, fired when a cell is activated" },
    ...CHART_A11Y_PROPS
  ],
  render: (_node, props, helpers) => {
    const xLabels = asArray(props.xLabels).map((l) => asString(l));
    const yLabels = asArray(props.yLabels).map((l) => asString(l));
    const valueRows = asArray(props.values).map((row) => asArray(row).map((v) => asNumber(v, 0)));
    const tone = resolveTone(props.tone);
    const showValues = props.showValues == null ? true : asBoolean(props.showValues);
    const valueFormat = asString(props.valueFormat, "value").toLowerCase();
    const onCellClick = props.onCellClick;
    const title = asString(props.title);
    const root = el("div", { class: "rui-chart rui-heatmap", "data-tone": tone });
    if (title) root.append(el("div", { class: "rui-chart-title" }, [title]));
    if (!valueRows.some((row) => row.length > 0)) {
      return chartPlaceholder(root, asString(props.emptyText) || EMPTY_TEXT, false);
    }
    const flat = valueRows.flat();
    const domainMin = props.min == null ? Math.min(0, ...flat) : asNumber(props.min, 0);
    const domainMax = props.max == null ? Math.max(1, ...flat) : asNumber(props.max, 1);
    const span = domainMax - domainMin || 1;
    const cols = Math.max(1, xLabels.length, ...valueRows.map((row) => row.length));
    const clickable = typeof onCellClick === "function";
    const a11y = readChartA11y(props, clickable);
    const tableWrap = el("div", {
      class: "rui-heatmap-table",
      style: `--rui-heatmap-cols:${cols}`
    });
    if (a11y.decorative) {
      tableWrap.setAttribute("aria-hidden", "true");
    } else {
      tableWrap.setAttribute("role", clickable ? "grid" : "table");
      const rows = valueRows.length;
      tableWrap.setAttribute("aria-label", a11y.name || chartLabel(
        "Heatmap",
        title,
        // Spoken aloud, so it is worth being grammatical about a 1×N matrix.
        `${rows} row${rows === 1 ? "" : "s"} by ${cols} column${cols === 1 ? "" : "s"}.`
      ));
    }
    const cellRole = clickable ? "gridcell" : "cell";
    const headerRow = el("div", { class: "rui-heatmap-row rui-heatmap-row-header", role: "row" });
    headerRow.append(el("div", { class: "rui-heatmap-cell rui-heatmap-corner", role: "columnheader" }));
    for (let c = 0; c < cols; c += 1) {
      headerRow.append(el("div", {
        class: "rui-heatmap-cell rui-heatmap-xlabel",
        role: "columnheader"
      }, [xLabels[c] ?? ""]));
    }
    tableWrap.append(headerRow);
    valueRows.forEach((row, rIdx) => {
      const yLabel = yLabels[rIdx] ?? String(rIdx + 1);
      const rowEl = el("div", { class: "rui-heatmap-row", role: "row" });
      rowEl.append(el("div", {
        class: "rui-heatmap-cell rui-heatmap-ylabel",
        role: "rowheader"
      }, [yLabel]));
      for (let cIdx = 0; cIdx < cols; cIdx += 1) {
        const value = row[cIdx];
        const xLabel = xLabels[cIdx] ?? String(cIdx + 1);
        if (value === void 0) {
          rowEl.append(el("div", { class: "rui-heatmap-cell", role: cellRole }));
          continue;
        }
        const intensity = Math.max(0, Math.min(1, (value - domainMin) / span));
        const background = `background:color-mix(in srgb, var(--rui-color-${tone}, ${colorAt(0)}) ${Math.round(intensity * 90 + 5)}%, transparent);`;
        const cell = el("div", {
          class: "rui-heatmap-cell rui-heatmap-value",
          style: clickable ? `${background}cursor:pointer;` : background,
          title: `${xLabel} · ${yLabel}: ${value}`,
          role: cellRole
        });
        if (showValues) cell.append(el("span", {}, [formatCellValue(value, valueFormat)]));
        if (clickable) {
          makeActivatable(cell, `${xLabel} · ${yLabel}: ${value}`, () => {
            helpers.invoke(onCellClick, value, xLabel, yLabel);
          });
          cell.setAttribute("role", cellRole);
        }
        rowEl.append(cell);
      }
      tableWrap.append(rowEl);
    });
    root.append(tableWrap);
    return root;
  }
};
function formatCellValue(value, format) {
  if (format === "compact") return formatNumeric(value);
  if (format === "percent") return `${Math.round(value)}%`;
  return String(value);
}
const RadarChart = {
  name: "RadarChart",
  description: "Polygon chart with one axis per category (at least three). Use for skill maps, scorecards, capability comparisons, and any multi-dimensional snapshot. Each Series renders as a filled polygon — overlapping is expected for comparisons. Set `showValues: true` to print the number at each vertex.",
  props: [
    { name: "axes", type: "string[]", description: "Category labels — one per radial axis (minimum 3)" },
    { name: "series", type: "Series[]" },
    { name: "max", type: "number", optional: true, description: "Outer ring value (default = max across series)" },
    { name: "title", type: "string", optional: true },
    { name: "size", type: "number", optional: true, description: "Diameter in px (default 280) — raise it for long axis names" },
    { name: "showDots", type: "boolean", optional: true, description: "Draw a marker at each vertex (default true)" },
    { name: "showValues", type: "boolean", optional: true, description: "Print each series value next to its vertex" },
    { name: "emptyText", type: "string", optional: true, description: 'Message shown when there is no data (default "No data")' },
    ...CHART_A11Y_PROPS
  ],
  render: (_node, props) => {
    const axes = asArray(props.axes).map((a) => asString(a));
    const series = readSeries(asArray(props.series));
    const title = asString(props.title);
    const a11y = readChartA11y(props);
    const showDots = props.showDots == null ? true : asBoolean(props.showDots);
    const showValues = asBoolean(props.showValues);
    const root = el("div", { class: "rui-chart rui-radar-chart" });
    if (title) root.append(el("div", { class: "rui-chart-title" }, [title]));
    if (axes.length < 3) {
      return chartPlaceholder(root, "A radar chart needs at least 3 axes", false);
    }
    if (!series.some((s) => s.values.length > 0)) {
      return chartPlaceholder(root, asString(props.emptyText) || EMPTY_TEXT, false);
    }
    const n = axes.length;
    const size2 = Math.max(160, Math.min(640, readChartHeight(props.size, 280)));
    const vbWidth = Math.round(size2 * 1.45);
    const cx = vbWidth / 2;
    const cy = size2 / 2;
    const r = size2 / 2 - 26;
    const max = Math.max(1, asNumber(props.max, series.flatMap((s) => s.values).reduce((m, v) => Math.max(m, v), 1)));
    const svg = createSvg(vbWidth, size2);
    nameChart(svg, a11y, chartLabel(
      "Radar chart",
      title,
      `${series.length} series across ${n} axes: ${axes.join(", ")}.`
    ));
    svg.setAttribute("style", `max-width:${vbWidth}px;max-height:${size2}px;margin-inline:auto`);
    const rings = 4;
    for (let i = 1; i <= rings; i += 1) {
      const radius = r / rings * i;
      const points = [];
      for (let j = 0; j < n; j += 1) {
        const angle = Math.PI * 2 * j / n - Math.PI / 2;
        points.push(`${(cx + radius * Math.cos(angle)).toFixed(1)},${(cy + radius * Math.sin(angle)).toFixed(1)}`);
      }
      svg.append(svgEl$1("polygon", {
        points: points.join(" "),
        fill: "none",
        stroke: "var(--rui-color-border-subtle, rgba(0,0,0,0.08))",
        "stroke-width": "1"
      }));
    }
    for (let j = 0; j < n; j += 1) {
      const angle = Math.PI * 2 * j / n - Math.PI / 2;
      const x = cx + r * Math.cos(angle);
      const y = cy + r * Math.sin(angle);
      svg.append(svgEl$1("line", {
        x1: String(cx),
        y1: String(cy),
        x2: x.toFixed(1),
        y2: y.toFixed(1),
        stroke: "var(--rui-color-border-subtle, rgba(0,0,0,0.08))",
        "stroke-width": "1"
      }));
      const labelX = cx + (r + 14) * Math.cos(angle);
      const labelY = cy + (r + 14) * Math.sin(angle);
      const cos = Math.cos(angle);
      const anchor = cos > 0.15 ? "start" : cos < -0.15 ? "end" : "middle";
      const available = anchor === "start" ? vbWidth - labelX - 2 : anchor === "end" ? labelX - 2 : Math.min(labelX, vbWidth - labelX) * 2 - 2;
      const label = axes[j] ?? "";
      const display = truncateToWidth(label, available);
      const text = svgEl$1("text", {
        x: labelX.toFixed(1),
        y: labelY.toFixed(1),
        "text-anchor": anchor,
        "dominant-baseline": "middle",
        class: "rui-chart-label",
        "font-size": "13",
        "font-weight": "500"
      }, [display]);
      if (display !== label) text.append(svgEl$1("title", {}, [label]));
      svg.append(text);
    }
    series.forEach((s, sIdx) => {
      const color2 = seriesColor(s, sIdx);
      const vertices = [];
      for (let j = 0; j < n; j += 1) {
        const value = s.values[j];
        if (value === void 0) continue;
        const ratio = Math.max(0, Math.min(1, value / max));
        const angle = Math.PI * 2 * j / n - Math.PI / 2;
        vertices.push({
          x: cx + r * ratio * Math.cos(angle),
          y: cy + r * ratio * Math.sin(angle),
          value,
          axis: axes[j] ?? ""
        });
      }
      if (vertices.length < 2) return;
      const points = vertices.map((v) => `${v.x.toFixed(1)},${v.y.toFixed(1)}`).join(" ");
      const complete = vertices.length === n;
      const shape = svgEl$1(complete ? "polygon" : "polyline", {
        points,
        fill: complete ? color2 : "none",
        "fill-opacity": "0.2",
        stroke: color2,
        "stroke-width": "2",
        "stroke-linejoin": "round",
        // An open dashed outline signals "this series does not cover every
        // axis" rather than silently describing a different shape.
        ...complete ? {} : { "stroke-dasharray": "5 4" }
      });
      shape.append(svgEl$1("title", {}, [
        complete ? s.name : `${s.name} (${vertices.length} of ${n} axes)`
      ]));
      svg.append(shape);
      if (showDots || showValues) {
        for (const vertex of vertices) {
          if (showDots) {
            const dot = svgEl$1("circle", {
              cx: vertex.x.toFixed(1),
              cy: vertex.y.toFixed(1),
              r: "3.5",
              fill: color2
            });
            dot.append(svgEl$1("title", {}, [`${s.name} — ${vertex.axis}: ${vertex.value}`]));
            svg.append(dot);
          }
          if (showValues) {
            svg.append(svgEl$1("text", {
              x: vertex.x.toFixed(1),
              y: (vertex.y - 7).toFixed(1),
              "text-anchor": "middle",
              class: "rui-chart-tick"
            }, [formatNumeric(vertex.value)]));
          }
        }
      }
    });
    root.append(svg);
    if (!a11y.decorative) {
      root.append(chartTable(
        title || "Radar chart data",
        axes,
        series.map((s) => ({
          label: s.name,
          cells: axes.map((_, j) => cellText(s.values[j]))
        }))
      ));
    }
    if (series.length > 0) root.append(legend(series));
    return root;
  }
};
function readScatterSeries(raw) {
  return raw.map((entry, i) => {
    const node = entry;
    const name = asString(node.args?.[0], `Series ${i + 1}`);
    const color2 = asString(node.args?.[2]).trim();
    const raw2 = node.args?.[3] ?? node.args?.[1];
    const points = asArray(raw2).map((p) => {
      if (Array.isArray(p)) {
        return { x: asNumber(p[0], 0), y: asNumber(p[1], 0), label: asString(p[2]) };
      }
      if (p && typeof p === "object") {
        const r = p;
        return { x: asNumber(r.x, 0), y: asNumber(r.y, 0), label: asString(r.label) };
      }
      return { x: 0, y: 0, label: "" };
    });
    return color2 ? { name, points, color: color2 } : { name, points };
  });
}
const ScatterChart = {
  name: "ScatterChart",
  description: 'XY scatter plot — one dot per data point, optionally grouped by series. Pass each `Series(name, points)` with points as `{x, y, label?}` objects or `[x, y, label?]` tuples. Tune `pointSize` / `pointOpacity` for dense distributions and pin `xMin`/`xMax`/`yMin`/`yMax` when two plots must share a scale. Use for correlations, distributions, and "price vs. rating" style charts.',
  props: [
    { name: "series", type: "Series[]" },
    { name: "xLabel", type: "string", optional: true },
    { name: "yLabel", type: "string", optional: true },
    { name: "title", type: "string", optional: true },
    { name: "pointSize", type: "number", optional: true, description: "Dot radius in px (default 7 — lower it for dense plots)" },
    { name: "pointOpacity", type: "number", optional: true, description: "Dot fill opacity 0–1 (default 0.8)" },
    { name: "xMin", type: "number", optional: true, description: "Pin the left of the x axis (default the data minimum)" },
    { name: "xMax", type: "number", optional: true, description: "Pin the right of the x axis (default the data maximum)" },
    { name: "yMin", type: "number", optional: true, description: "Pin the bottom of the y axis (default the data minimum)" },
    { name: "yMax", type: "number", optional: true, description: "Pin the top of the y axis (default the data maximum)" },
    { name: "height", type: "number", optional: true, description: "Plot height in px (default 280)" },
    { name: "showLegend", type: "boolean", optional: true, description: "Render the series legend (default true)" },
    { name: "emptyText", type: "string", optional: true, description: 'Message shown when there are no points (default "No points")' },
    { name: "onPointClick", type: "callable", optional: true, description: "(x, y, label, seriesName) => void, fired when a point is activated" },
    ...CHART_A11Y_PROPS
  ],
  render: (_node, props, helpers) => {
    const series = readScatterSeries(asArray(props.series));
    const title = asString(props.title);
    const showLegend = props.showLegend == null ? true : asBoolean(props.showLegend);
    const onPointClick = props.onPointClick;
    const a11y = readChartA11y(props, typeof onPointClick === "function");
    const root = el("div", { class: "rui-chart rui-scatter-chart" });
    if (title) root.append(el("div", { class: "rui-chart-title" }, [title]));
    const points = series.flatMap((s) => s.points);
    if (points.length === 0) {
      return chartPlaceholder(root, asString(props.emptyText) || "No points", false);
    }
    const xLabel = asString(props.xLabel);
    const yLabel = asString(props.yLabel);
    const width = 640;
    const height = readChartHeight(props.height, 280);
    const xs = points.map((p) => p.x);
    const ys = points.map((p) => p.y);
    const minX = props.xMin == null ? Math.min(...xs) : asNumber(props.xMin);
    const maxXRaw = props.xMax == null ? Math.max(...xs) : asNumber(props.xMax);
    const minY = props.yMin == null ? Math.min(...ys) : asNumber(props.yMin);
    const maxYRaw = props.yMax == null ? Math.max(...ys) : asNumber(props.yMax);
    const maxX = maxXRaw > minX ? maxXRaw : minX + 1;
    const maxY = maxYRaw > minY ? maxYRaw : minY + 1;
    const xRange = maxX - minX;
    const yRange = maxY - minY;
    const padding = {
      left: tickGutter(minY, maxY) + (yLabel ? AXIS_CAPTION_PX : 0),
      right: 16,
      top: 16,
      bottom: 24 + (xLabel ? AXIS_CAPTION_PX : 0)
    };
    const innerWidth = width - padding.left - padding.right;
    const innerHeight = height - padding.top - padding.bottom;
    const svg = createSvg(width, height);
    nameChart(svg, a11y, chartLabel(
      "Scatter plot",
      title,
      `${points.length} points across ${series.length} series${xLabel || yLabel ? `, ${xLabel || "x"} against ${yLabel || "y"}` : ""}.`
    ));
    if (height !== 280) svg.setAttribute("style", `max-height:${height}px`);
    drawAxes(svg, padding, innerWidth, innerHeight, maxY, minY);
    drawValueTicks(svg, padding, innerWidth, innerHeight, maxX, minX);
    svg.append(svgEl$1("line", {
      x1: String(padding.left),
      y1: String(padding.top + innerHeight),
      x2: String(padding.left + innerWidth),
      y2: String(padding.top + innerHeight),
      stroke: "var(--rui-color-border, #e2e8f0)"
    }));
    const pointSize = Math.max(1, Math.min(24, asNumber(props.pointSize, 7)));
    const pointOpacity = Math.max(0.05, Math.min(1, asNumber(props.pointOpacity, 0.8)));
    series.forEach((s, sIdx) => {
      const color2 = seriesColor(s, sIdx);
      s.points.forEach((pt) => {
        const cx = padding.left + (pt.x - minX) / xRange * innerWidth;
        const cy = padding.top + innerHeight - (pt.y - minY) / yRange * innerHeight;
        const circle = svgEl$1("circle", {
          cx: cx.toFixed(1),
          cy: cy.toFixed(1),
          r: String(pointSize),
          fill: color2,
          "fill-opacity": String(pointOpacity),
          // The dots sit on the chart card, whose background is
          // `--rui-color-surface`; a literal #fff haloed every point in dark mode.
          stroke: "var(--rui-color-surface, #fff)",
          "stroke-width": "1.5"
        });
        const description = pt.label || `${s.name}: ${pt.x}, ${pt.y}`;
        circle.append(svgEl$1("title", {}, [description]));
        if (typeof onPointClick === "function") {
          makeActivatable(circle, description, () => {
            helpers.invoke(onPointClick, pt.x, pt.y, pt.label, s.name);
          });
        }
        svg.append(circle);
      });
    });
    drawAxisCaptions(svg, padding, innerWidth, innerHeight, height, xLabel, yLabel);
    root.append(svg);
    if (!a11y.decorative) {
      root.append(chartTable(
        title || "Scatter plot data",
        [xLabel || "x", yLabel || "y", "Label"],
        series.flatMap((s) => s.points.map((pt, i) => ({
          label: `${s.name} #${i + 1}`,
          cells: [String(pt.x), String(pt.y), pt.label || "—"]
        })))
      ));
    }
    const seriesData = series.map((s) => ({
      name: s.name,
      values: s.points.map((p) => p.y),
      ...s.color ? { color: s.color } : {}
    }));
    if (showLegend && seriesData.length > 0) root.append(legend(seriesData));
    return root;
  }
};
const Histogram = {
  name: "Histogram",
  description: "Frequency distribution from raw numeric values. Pass `values` directly (the component bins them automatically, `binCount` bins) or pre-computed `bins` of `{label, count}` objects. Use for response-time histograms, score distributions, age buckets.",
  props: [
    { name: "values", type: "number[]", optional: true, description: "Raw observations (binned automatically)" },
    // `binCount` precedes `bins` so the documented `Histogram(values, binCount)`
    // call binds where the prompt says it does. It used to land in `bins`,
    // fail the array check, and be discarded without a diagnostic.
    { name: "binCount", type: "number", optional: true, description: "Number of bins when computing from `values` (default 10)" },
    { name: "bins", type: "object[]", optional: true, description: "Pre-computed {label, count} entries (overrides `values`)" },
    { name: "title", type: "string", optional: true },
    { name: "tone", aliases: ["variant"], type: "string", optional: true, enum: ["primary", "success", "warning", "danger", "info"], description: "Bar colour token (default the first palette colour)" },
    { name: "xLabel", type: "string", optional: true, description: 'Caption for the value axis (e.g. "ms")' },
    { name: "yLabel", type: "string", optional: true, description: 'Caption for the frequency axis (e.g. "requests")' },
    { name: "height", type: "number", optional: true, description: "Plot height in px (default 240)" },
    { name: "emptyText", type: "string", optional: true, description: 'Message shown when there is no data (default "No data")' },
    { name: "loading", type: "boolean", optional: true, description: "Render a loading placeholder instead of the plot" },
    { name: "onBinClick", type: "callable", optional: true, description: "(binLabel, count, binIndex) => void, fired when a bar is activated" },
    ...CHART_A11Y_PROPS
  ],
  render: (_node, props, helpers) => {
    let bins = [];
    if (Array.isArray(props.bins) && props.bins.length > 0) {
      bins = props.bins.map((entry) => {
        const r = entry ?? {};
        return { label: asString(r.label), count: asNumber(r.count, 0) };
      });
    } else {
      const values = asArray(props.values).map((v) => asNumber(v, NaN)).filter((n) => Number.isFinite(n));
      if (values.length > 0) {
        const binCount = Math.max(2, Math.min(50, Math.floor(asNumber(props.binCount, 10))));
        const min = Math.min(...values);
        const max2 = Math.max(...values);
        const span = max2 - min || 1;
        const step = span / binCount;
        const counts = new Array(binCount).fill(0);
        for (const v of values) {
          let idx = Math.floor((v - min) / step);
          if (idx >= binCount) idx = binCount - 1;
          if (idx < 0) idx = 0;
          counts[idx] += 1;
        }
        bins = counts.map((count, i) => {
          const a = min + i * step;
          const b = a + step;
          return { label: `${formatBinLabel(a)}–${formatBinLabel(b)}`, count };
        });
      }
    }
    const title = asString(props.title);
    const tone = resolveTone(props.tone, "");
    const onBinClick = props.onBinClick;
    const a11y = readChartA11y(props, typeof onBinClick === "function");
    const root = el("div", { class: "rui-chart rui-histogram" });
    if (title) root.append(el("div", { class: "rui-chart-title" }, [title]));
    if (asBoolean(props.loading)) return chartPlaceholder(root, LOADING_TEXT, true);
    if (bins.length === 0) {
      return chartPlaceholder(root, asString(props.emptyText) || EMPTY_TEXT, false);
    }
    const xLabel = asString(props.xLabel);
    const yLabel = asString(props.yLabel);
    const width = 640;
    const height = readChartHeight(props.height, 240);
    const max = Math.max(1, ...bins.map((b) => b.count));
    const labels = bins.map((b) => b.label);
    const padding = {
      left: 40 + (yLabel ? AXIS_CAPTION_PX : 0),
      right: 12,
      top: 16,
      bottom: 0
    };
    const innerWidth = width - padding.left - padding.right;
    const labelPlan = planLabels(labels, innerWidth);
    padding.bottom = labelPlan.bottomPadding + (xLabel ? AXIS_CAPTION_PX : 0);
    const innerHeight = height - padding.top - padding.bottom;
    const slot = innerWidth / bins.length;
    const fill = tone ? `var(--rui-color-${tone}, ${colorAt(0)})` : colorAt(0);
    const svg = createSvg(width, height);
    nameChart(svg, a11y, chartLabel("Histogram", title, `${bins.length} bins.`));
    if (height !== 240) svg.setAttribute("style", `max-height:${height}px`);
    drawAxes(svg, padding, innerWidth, innerHeight, max);
    bins.forEach((bin, i) => {
      const x = padding.left + i * slot + slot * 0.1;
      const barWidth = slot * 0.8;
      const barHeight = bin.count / max * innerHeight;
      const y = padding.top + innerHeight - barHeight;
      const rect = svgEl$1("rect", {
        x: x.toFixed(1),
        y: y.toFixed(1),
        width: barWidth.toFixed(1),
        height: barHeight.toFixed(1),
        fill,
        rx: "2"
      });
      rect.append(svgEl$1("title", {}, [`${bin.label}: ${bin.count}`]));
      if (typeof onBinClick === "function") {
        makeActivatable(rect, `${bin.label}: ${bin.count}`, () => {
          helpers.invoke(onBinClick, bin.label, bin.count, i);
        });
      }
      svg.append(rect);
    });
    drawXAxisLabels(
      svg,
      labels,
      padding,
      innerHeight,
      labelPlan,
      (i) => padding.left + i * slot + slot / 2
    );
    drawAxisCaptions(svg, padding, innerWidth, innerHeight, height, xLabel, yLabel);
    root.append(svg);
    if (!a11y.decorative) {
      root.append(chartTable(
        title || "Histogram data",
        labels,
        [{ label: yLabel || "Count", cells: bins.map((b) => String(b.count)) }]
      ));
    }
    return root;
  }
};
function formatBinLabel(value) {
  if (!Number.isFinite(value)) return "?";
  if (Math.abs(value) >= 1e3) return Math.round(value).toLocaleString();
  if (Math.abs(value) >= 10) return value.toFixed(0);
  return value.toFixed(1);
}
const PIN_TYPES = ["numeric", "alphanumeric"];
function forwardFieldAria(wrapper, control, options = {}) {
  for (const attr of ["aria-invalid", "aria-describedby"]) {
    const value = wrapper.getAttribute(attr);
    if (value === null) continue;
    wrapper.removeAttribute(attr);
    control.setAttribute(attr, value);
  }
  if (!wrapper.hasAttribute("required")) return;
  wrapper.removeAttribute("required");
  if (options.native === false) control.setAttribute("aria-required", "true");
  else control.setAttribute("required", "");
}
function stepAttr(raw) {
  if (raw === null || raw === void 0) return null;
  if (typeof raw === "string" && raw.trim().toLowerCase() === "any") return "any";
  return String(Math.max(1, Math.floor(asNumber(raw, 60))));
}
function padSlots(chars, length) {
  const out = chars.slice(0, length).map((c) => c.slice(0, 1));
  while (out.length < length) out.push("");
  return out;
}
function renderPin(opts) {
  const { id, length, type, chars, disabled, mask, groupLabel, onSlots, onFocusChange } = opts;
  const alphanumeric = type === "alphanumeric";
  const root = el("div", {
    class: "rui-pin-input",
    "data-disabled": disabled ? "true" : "false",
    // The slots only make sense as a set: without group semantics a screen
    // reader announced "edit text" eight times with no name for the code.
    role: "group",
    "aria-label": groupLabel || (alphanumeric ? `${length}-character code` : `${length}-digit code`)
  });
  const inputs = [];
  for (let i = 0; i < length; i += 1) {
    const input = el("input", {
      class: "rui-pin-input-slot",
      id: i === 0 ? id : null,
      // Deliberately NO `maxlength="1"`: browsers apply it to pastes too, so a
      // pasted SMS code was truncated to its first character — the single
      // most-used interaction on a 2FA screen. The multi-character branch in
      // `oninput` distributes the extra characters across the following slots
      // and trims each slot back to one, so the cap is still enforced.
      autocomplete: "one-time-code",
      inputmode: alphanumeric ? "text" : "numeric",
      type: mask ? "password" : "text",
      "aria-label": alphanumeric ? `Character ${i + 1} of ${length}` : `Digit ${i + 1} of ${length}`,
      // Asserting the value on every render is deliberate here: the slot array
      // (gaps included) is the component's model, and re-asserting it is what
      // restores a cleared middle slot in the right box.
      value: chars[i] ?? "",
      disabled: disabled ? "" : null
    });
    inputs.push(input);
    root.append(input);
  }
  const getLiveSlots = (origin) => {
    const pinRoot = origin.closest(".rui-pin-input");
    if (!pinRoot) return inputs;
    return Array.from(pinRoot.querySelectorAll(".rui-pin-input-slot"));
  };
  const collectLive = (origin) => padSlots(getLiveSlots(origin).map((slot) => slot.value), length);
  inputs.forEach((input, idx) => {
    input.oninput = (event) => {
      const target = event.currentTarget ?? event.target;
      const liveSlots = getLiveSlots(target);
      let v = target.value;
      if (type === "numeric") v = v.replace(/\D/g, "");
      else v = v.replace(/[^A-Za-z0-9]/g, "");
      if (v.length > 1) {
        const chars2 = v.split("");
        chars2.slice(0, length - idx).forEach((c, k) => {
          const next = liveSlots[idx + k];
          if (next) next.value = c;
        });
        const lastFilled = Math.min(idx + chars2.length, length - 1);
        liveSlots[lastFilled]?.focus();
      } else {
        target.value = v;
        if (v && idx < length - 1) liveSlots[idx + 1]?.focus();
      }
      onSlots(collectLive(target));
    };
    input.onkeydown = (event) => {
      const e = event;
      const target = e.currentTarget;
      const liveSlots = getLiveSlots(target);
      if (e.key === "Backspace" && !target.value && idx > 0) {
        e.preventDefault();
        const prev = liveSlots[idx - 1];
        if (prev) {
          prev.value = "";
          prev.focus();
        }
        onSlots(collectLive(target));
      } else if (e.key === "ArrowLeft" && idx > 0) {
        e.preventDefault();
        liveSlots[idx - 1]?.focus();
      } else if (e.key === "ArrowRight" && idx < length - 1) {
        e.preventDefault();
        liveSlots[idx + 1]?.focus();
      }
    };
    if (!onFocusChange) return;
    const leftTheGroup = (e) => {
      const origin = e.currentTarget ?? e.target;
      const group = origin.closest(".rui-pin-input");
      const other = e.relatedTarget;
      return !(other && group?.contains(other));
    };
    input.onblur = (event) => {
      const e = event;
      if (!leftTheGroup(e)) return;
      onFocusChange("blur", collectLive(e.currentTarget ?? e.target));
    };
    input.onfocus = (event) => {
      const e = event;
      if (!leftTheGroup(e)) return;
      onFocusChange("focus", collectLive(e.currentTarget ?? e.target));
    };
  });
  return root;
}
const PinInput = {
  name: "PinInput",
  description: 'Per-digit PIN entry. Auto-advances focus as the user types and supports pasting a whole code into any slot. Pass a `$variable` as `value` for two-way binding (the bound value is the joined string). Use `type="numeric"` for PINs / 2FA codes, `"alphanumeric"` for invite codes. `onComplete` fires once every slot is filled (auto-submit hook); pass `label`/`hint`/`error` for the labelled field shell.',
  props: [
    { name: "id", type: "string" },
    { name: "length", type: "number", optional: true, description: "Number of slots (default 4)" },
    { name: "value", type: "string", optional: true, description: "Bound value (typically $variable)" },
    { name: "type", type: "string", optional: true, enum: PIN_TYPES },
    { name: "mask", type: "boolean", optional: true, description: "Render slots as `<input type=password>`" },
    { name: "autoFocus", type: "boolean", optional: true, aliases: ["autofocus"], description: "Focus the first slot on mount" },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"], description: "Called with the current joined string on every keystroke" },
    { name: "onComplete", type: "callable", optional: true, aliases: ["oncomplete"], description: "Called with the code once every slot is filled" },
    ...FIELD_SHELL_PROPS
  ],
  render: (node, props, helpers) => {
    const id = asString(props.id) || autoId(helpers, "rui-pin-input");
    const length = Math.max(1, Math.min(12, Math.floor(asNumber(props.length, 4))));
    const type = asString(props.type, "numeric");
    const value = asString(props.value);
    const disabled = asBoolean(props.disabled);
    const mask = asBoolean(props.mask);
    const stateName = node.argMeta?.[2]?.stateRef;
    const slotsSlot = helpers.useInstanceState("slots", []);
    const stored = slotsSlot.get();
    const asserted = props.value !== null && props.value !== void 0;
    const inSync = stored.length === length && stored.join("") === value;
    const chars = !asserted || inSync ? padSlots(stored, length) : padSlots(value.split(""), length);
    const root = renderPin({
      id,
      length,
      type,
      chars,
      disabled,
      mask,
      groupLabel: asString(props.label),
      onSlots: (slots) => {
        slotsSlot.set(slots);
        const joined = slots.join("");
        if (stateName) helpers.setState(stateName, joined);
        helpers.invoke(props.onChange, joined);
        if (joined.length === length) helpers.invoke(props.onComplete, joined);
      },
      onFocusChange: props.onBlur != null || props.onFocus != null ? (kind, slots) => {
        helpers.invoke(kind === "blur" ? props.onBlur : props.onFocus, slots.join(""));
      } : void 0
    });
    const autoFocusedSlot = helpers.useInstanceState("autoFocused", false);
    if (asBoolean(props.autoFocus) && !disabled && !autoFocusedSlot.get()) {
      deferToPaint(() => {
        if (!root.isConnected || autoFocusedSlot.get()) return;
        const first = root.querySelector(".rui-pin-input-slot");
        if (!first) return;
        autoFocusedSlot.set(true);
        first.focus();
      });
    }
    const shell = withFieldShell(root, { ...props, id });
    forwardFieldAria(root, root, { native: false });
    return shell;
  }
};
const PASSWORD_AUTOCOMPLETE = ["current-password", "new-password", "off"];
function passwordStrength(value) {
  if (!value) return { score: 0, label: "" };
  let score = 0;
  if (value.length >= 8) score += 1;
  if (value.length >= 12) score += 1;
  if (/[A-Z]/.test(value) && /[a-z]/.test(value)) score += 1;
  if (/[0-9]/.test(value)) score += 1;
  if (/[^A-Za-z0-9]/.test(value)) score += 1;
  score = Math.min(4, score);
  const labels = ["Too short", "Weak", "Fair", "Good", "Strong"];
  return { score, label: labels[score] ?? "" };
}
const PasswordInput = {
  name: "PasswordInput",
  description: 'Password input with a show/hide toggle and an optional strength meter. Pass a `$variable` as `value` for two-way binding. Set `strengthMeter=true` to render a 4-step indicator and label. Use `autocomplete="new-password"` on sign-up / reset forms so the browser offers a generated password. Pass `label`/`hint`/`error`/`required` for the labelled field shell.',
  props: [
    { name: "id", type: "string" },
    { name: "value", type: "string", optional: true, description: "Bound value (typically $variable)" },
    { name: "placeholder", type: "string", optional: true },
    { name: "strengthMeter", type: "boolean", optional: true, aliases: ["showStrength"] },
    { name: "autocomplete", type: "string", optional: true, enum: PASSWORD_AUTOCOMPLETE, description: 'Password-manager hint (default "current-password")' },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"], description: "Called with the current value on every keystroke" },
    ...FIELD_SHELL_PROPS
  ],
  render: (node, props, helpers) => {
    const id = asString(props.id) || autoId(helpers, "rui-password-input");
    const visibleSlot = helpers.useInstanceState("visible", false);
    const visible = visibleSlot.get();
    const disabled = asBoolean(props.disabled);
    const showStrength = asBoolean(props.strengthMeter);
    const root = el("div", { class: "rui-password-input", "data-disabled": disabled ? "true" : "false" });
    const row = el("div", { class: "rui-password-input-row" });
    const input = el("input", {
      type: visible ? "text" : "password",
      class: "rui-password-input-field",
      id,
      name: asString(props.name, id),
      autocomplete: asString(props.autocomplete, "current-password"),
      placeholder: asString(props.placeholder),
      // `valueAttr`, not `asString`: an absent prop must emit no attribute at
      // all, otherwise morph reads `value=""` as a deliberate clear and wipes
      // whatever the user typed on the next re-render from anywhere.
      value: valueAttr(props.value),
      disabled: disabled ? "" : null
    });
    const toggleBtn = el("button", {
      type: "button",
      class: "rui-password-input-toggle",
      "aria-label": visible ? "Hide password" : "Show password",
      "aria-pressed": visible ? "true" : "false",
      disabled: disabled ? "" : null
    });
    const toggleIcon = renderIcon(visible ? "eye-slash" : "eye");
    if (toggleIcon) toggleBtn.append(toggleIcon);
    toggleBtn.onclick = (event) => {
      event.preventDefault();
      const next = !visibleSlot.get();
      visibleSlot.set(next);
      const btn = event.currentTarget ?? event.target;
      const live = btn.closest(".rui-password-input");
      const liveInput = live?.querySelector(".rui-password-input-field");
      if (liveInput) liveInput.type = next ? "text" : "password";
      btn.setAttribute("aria-label", next ? "Hide password" : "Show password");
      btn.setAttribute("aria-pressed", next ? "true" : "false");
      const nextIcon = renderIcon(next ? "eye-slash" : "eye");
      if (nextIcon) {
        const currentIcon = btn.querySelector(".rui-icon");
        if (currentIcon) currentIcon.replaceWith(nextIcon);
        else btn.append(nextIcon);
      }
    };
    const syncStrength = (origin) => {
      const live = origin.closest(".rui-password-input");
      const meterEl = live?.querySelector(".rui-password-input-strength");
      const field = live?.querySelector(".rui-password-input-field");
      if (!meterEl || !field) return;
      const next = passwordStrength(field.value);
      meterEl.setAttribute("data-score", String(next.score));
      meterEl.querySelectorAll(".rui-password-input-strength-bar").forEach((bar, i) => {
        bar.setAttribute("data-filled", i < next.score ? "true" : "false");
      });
      const labelEl = live?.querySelector(".rui-password-input-strength-label");
      if (labelEl) labelEl.textContent = next.label;
    };
    const stateName = node.argMeta?.[1]?.stateRef;
    input.oninput = (event) => {
      const live = event.currentTarget ?? event.target;
      if (showStrength) syncStrength(live);
      if (stateName) helpers.setState(stateName, live.value);
      helpers.invoke(props.onChange, live.value);
    };
    attachFocusHandlers(input, props, helpers);
    row.append(input);
    row.append(toggleBtn);
    root.append(row);
    if (showStrength) {
      const strength = passwordStrength(asString(props.value));
      const meter = el("div", { class: "rui-password-input-strength", "data-score": String(strength.score) });
      for (let i = 0; i < 4; i += 1) {
        meter.append(el("span", {
          class: "rui-password-input-strength-bar",
          "data-filled": i < strength.score ? "true" : "false"
        }));
      }
      const labelRow = el("div", { class: "rui-password-input-strength-row" });
      labelRow.append(meter);
      labelRow.append(el("span", { class: "rui-password-input-strength-label" }, [strength.label]));
      root.append(labelRow);
    }
    const shell = withFieldShell(root, { ...props, id });
    forwardFieldAria(root, input);
    return shell;
  }
};
const TagInput = {
  name: "TagInput",
  description: "Tag/chip input — type a value, press Enter (or comma) to commit, click × on a chip to remove. Tabbing away commits the pending text too. Pass a `$variable` (array of strings) as `value` for two-way binding. Use for keywords, recipients, labels, skills, allowlists. Pass `suggestions` for autocomplete and `label`/`hint`/`error` for the labelled field shell.",
  props: [
    { name: "id", type: "string" },
    { name: "value", type: "string[]", optional: true, description: "Bound array of tag values" },
    { name: "placeholder", type: "string", optional: true },
    { name: "max", type: "number", optional: true, description: "Maximum number of tags" },
    { name: "suggestions", type: "any[]", optional: true, description: "Values offered as autocomplete while typing" },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"], description: "Called with the updated array of tags whenever one is added or removed" },
    ...FIELD_SHELL_PROPS
  ],
  render: (node, props, helpers) => {
    const id = asString(props.id) || autoId(helpers, "rui-tag-input");
    const tags = asArray(props.value).map((v) => asString(v)).filter(Boolean);
    const max = Math.max(0, Math.floor(asNumber(props.max, 0)));
    const disabled = asBoolean(props.disabled);
    const stateName = node.argMeta?.[1]?.stateRef;
    const draftSlot = helpers.useInstanceState("draft", "");
    const setTags = (next) => {
      if (disabled) return;
      if (stateName) helpers.setState(stateName, next);
      helpers.invoke(props.onChange, next);
    };
    const root = el("div", {
      class: "rui-tag-input",
      "data-disabled": disabled ? "true" : "false"
    });
    for (const tag of tags) {
      const chip = el("span", { class: "rui-tag-input-chip" });
      chip.append(el("span", {}, [tag]));
      const remove = el("button", {
        type: "button",
        class: "rui-tag-input-remove",
        "aria-label": `Remove ${tag}`,
        // A greyed-out field that still lets you delete chips is worse than no
        // `disabled` at all — a viewer-role user could destroy data.
        disabled: disabled ? "" : null
      }, ["×"]);
      remove.onclick = () => setTags(tags.filter((t) => t !== tag));
      chip.append(remove);
      root.append(chip);
    }
    const suggestions = asArray(props.suggestions).map((v) => asString(v)).filter((v) => v !== "" && !tags.includes(v));
    const listId = suggestions.length > 0 ? `${id}-suggestions` : null;
    const input = el("input", {
      type: "text",
      class: "rui-tag-input-field",
      id,
      name: id,
      // Keyed so the morph reconciler parks the live field when a new chip is
      // inserted ahead of it. Without a key the chip landed on the field's
      // index, the tag mismatch replaced the node, and focus was lost after
      // every committed tag.
      "data-rui-key": "tag-field",
      placeholder: asString(props.placeholder, tags.length === 0 ? "Add tags…" : ""),
      value: draftSlot.get(),
      list: listId,
      disabled: disabled ? "" : null,
      autocomplete: "off"
    });
    const commitDraft = (liveInput) => {
      const value = liveInput.value.trim();
      if (!value || disabled) return tags;
      if (max > 0 && tags.length >= max) return tags;
      liveInput.value = "";
      draftSlot.set("");
      if (tags.includes(value)) return tags;
      const next = [...tags, value];
      setTags(next);
      return next;
    };
    input.oninput = (event) => {
      const liveInput = event.currentTarget ?? event.target;
      draftSlot.set(liveInput.value);
    };
    input.onkeydown = (event) => {
      const e = event;
      const liveInput = e.currentTarget ?? e.target;
      if (e.key === "Enter" || e.key === ",") {
        e.preventDefault();
        commitDraft(liveInput);
      } else if (e.key === "Backspace" && liveInput.value === "" && tags.length > 0) {
        e.preventDefault();
        setTags(tags.slice(0, -1));
      }
    };
    input.onblur = (event) => {
      const liveInput = event.currentTarget ?? event.target;
      const next = commitDraft(liveInput);
      if (props.onBlur != null) helpers.invoke(props.onBlur, next);
    };
    if (props.onFocus != null) {
      input.onfocus = (event) => {
        const liveInput = event.currentTarget ?? event.target;
        helpers.invoke(props.onFocus, liveInput.value);
      };
    }
    root.append(input);
    if (listId) {
      const list = el("datalist", { id: listId });
      for (const value of suggestions) list.append(el("option", { value }));
      root.append(list);
    }
    const shell = withFieldShell(root, { ...props, id });
    forwardFieldAria(root, input, { native: false });
    return shell;
  }
};
const MENTION_FORMATS = ["handle", "label"];
function readMentionItems(raw) {
  return asArray(raw).map((entry) => {
    if (typeof entry === "string") return { value: entry, label: entry };
    if (entry && typeof entry === "object") {
      const r = entry;
      const display = asString(r.label ?? r.name ?? r.value ?? r.id);
      const handle = asString(r.handle ?? r.username ?? r.value ?? r.name ?? r.label ?? r.id);
      return { value: handle, label: display };
    }
    return { value: "", label: "" };
  }).filter((i) => i.value || i.label);
}
const PROMOTED_PANELS = /* @__PURE__ */ new WeakMap();
const openMentionPanel = (panel, anchor) => {
  if (!panel.isConnected || !anchor.isConnected) return;
  PROMOTED_PANELS.set(anchor, panel);
  openFloating(panel, {
    anchor,
    side: "bottom",
    align: "start",
    // Mirrors `.rui-mention-input-suggestions { min-width: 220px }` so a narrow
    // composer still gets a list wide enough to read a name plus a handle.
    matchAnchorWidth: true,
    minWidth: 220,
    layer: "dropdown"
  });
};
const positionMentionOnMount = (root) => {
  deferToPaint(() => {
    if (!root.isConnected) return;
    const panel = root.querySelector(".rui-mention-input-suggestions");
    const anchor = root.querySelector(".rui-mention-input-field");
    if (!panel || !anchor || panel.getAttribute("data-open") !== "true") return;
    openMentionPanel(panel, anchor);
  });
};
const MentionInput = {
  name: "MentionInput",
  description: 'Multi-line input with inline @-mention suggestions. Typing `@` opens a popover listing the provided `people` (filtered by what follows). Selecting an option inserts `@handle` — the `handle`/`value` key, falling back to the display label; set `mentionFormat: "label"` to insert the display name instead. Pass a `$variable` as `value` for two-way binding. Use `onSearch` + `loading` for a server-side directory. Use for comments, task notes, chat composers.',
  props: [
    { name: "id", type: "string" },
    { name: "people", type: "any[]", description: "Available mentions: strings or {value, label} objects" },
    { name: "value", type: "string", optional: true, description: "Bound text (typically $variable)" },
    { name: "placeholder", type: "string", optional: true },
    { name: "rows", type: "number", optional: true, description: "TextArea rows (default 3)" },
    { name: "maxSuggestions", type: "number", optional: true, description: "Maximum options listed (default 6)" },
    { name: "mentionFormat", type: "string", optional: true, enum: MENTION_FORMATS, description: "Insert `@handle` (default) or `@label`" },
    { name: "loading", type: "boolean", optional: true, description: 'Show "Searching…" while `onSearch` results are in flight' },
    { name: "onSearch", type: "callable", optional: true, description: "Called with the text typed after `@` so the app can fetch `people`" },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"], description: "Called with the current text on every keystroke" },
    ...FIELD_SHELL_PROPS
  ],
  render: (node, props, helpers) => {
    const id = asString(props.id) || autoId(helpers, "rui-mention-input");
    const panelId2 = `${id}-suggestions`;
    const people = readMentionItems(props.people);
    const disabled = asBoolean(props.disabled);
    const loading = asBoolean(props.loading);
    const maxSuggestions = Math.max(1, Math.floor(asNumber(props.maxSuggestions, 6)));
    const insertLabel = asString(props.mentionFormat, "handle") === "label";
    const root = el("div", { class: "rui-mention-input", "data-disabled": disabled ? "true" : "false" });
    const textarea = el("textarea", {
      class: "rui-mention-input-field",
      id,
      name: id,
      rows: String(Math.max(2, Math.floor(asNumber(props.rows, 3)))),
      placeholder: asString(props.placeholder, "Type @ to mention someone"),
      disabled: disabled ? "" : null,
      // Combobox semantics: without these the popover, its options and the
      // active row existed only in `data-*` attributes, i.e. only for sighted
      // users. `aria-multiline` keeps the "multiline" announcement that
      // `role=combobox` would otherwise drop.
      role: "combobox",
      "aria-expanded": "false",
      "aria-autocomplete": "list",
      "aria-multiline": "true",
      "aria-controls": panelId2
    });
    textarea.value = asString(props.value);
    const suggestions = el("div", {
      class: "rui-mention-input-suggestions",
      id: panelId2,
      role: "listbox",
      "data-open": "false"
    });
    const activeSlot = helpers.useInstanceState("activeIndex", 0);
    const matchesSlot = helpers.useInstanceState("matches", []);
    const querySlot = helpers.useInstanceState("query", null);
    const blurTimerSlot = helpers.useInstanceState("blurTimer", null);
    const liveSuggestionsFor = (origin) => {
      const root2 = origin.closest(".rui-mention-input");
      const inPlace = root2?.querySelector(".rui-mention-input-suggestions");
      if (inPlace) return inPlace;
      const field = root2?.querySelector(".rui-mention-input-field");
      return (field ? PROMOTED_PANELS.get(field) : null) ?? null;
    };
    const liveTextareaFor = (origin) => {
      const root2 = origin.closest(".rui-mention-input");
      return root2?.querySelector(".rui-mention-input-field") ?? null;
    };
    const insertMention = (liveTextarea, item) => {
      const text = liveTextarea.value;
      const caret = liveTextarea.selectionStart ?? text.length;
      const before = text.slice(0, caret);
      const triggerIdx = before.lastIndexOf("@");
      if (triggerIdx === -1) return;
      const after = text.slice(caret);
      const token = insertLabel ? item.label || item.value : item.value || item.label;
      const insert = `@${token} `;
      const next = before.slice(0, triggerIdx) + insert + after;
      liveTextarea.value = next;
      const cursor = triggerIdx + insert.length;
      liveTextarea.selectionStart = liveTextarea.selectionEnd = cursor;
      liveTextarea.dispatchEvent(new Event("input", { bubbles: true }));
      liveTextarea.focus();
    };
    const applyActive = (panel, field, next) => {
      const items = panel.querySelectorAll(".rui-mention-input-option");
      items.forEach((b, i) => {
        b.setAttribute("data-active", i === next ? "true" : "false");
        b.setAttribute("aria-selected", i === next ? "true" : "false");
      });
      const activeId = items[next]?.id;
      if (activeId) field.setAttribute("aria-activedescendant", activeId);
      else field.removeAttribute("aria-activedescendant");
    };
    const paintSuggestions = (panel, query, anchor) => {
      const field = anchor ?? textarea;
      panel.replaceChildren();
      if (query === null) {
        panel.setAttribute("data-open", "false");
        field.setAttribute("aria-expanded", "false");
        field.removeAttribute("aria-activedescendant");
        matchesSlot.set([]);
        closeFloating(panel);
        return;
      }
      const q = query.toLowerCase();
      const filtered = people.filter(
        (p) => p.label.toLowerCase().includes(q) || p.value.toLowerCase().includes(q)
      );
      const slice = filtered.slice(0, maxSuggestions);
      matchesSlot.set(loading ? [] : slice);
      panel.setAttribute("data-open", "true");
      field.setAttribute("aria-expanded", "true");
      if (loading || slice.length === 0) {
        panel.append(el("div", {
          class: "rui-mention-input-status",
          role: "option",
          "aria-disabled": "true",
          "aria-selected": "false"
        }, [loading ? "Searching…" : "No people found"]));
        field.removeAttribute("aria-activedescendant");
        if (anchor) openMentionPanel(panel, anchor);
        return;
      }
      const nextActive = Math.min(Math.max(0, activeSlot.get()), slice.length - 1);
      activeSlot.set(nextActive);
      slice.forEach((item, idx) => {
        const btn = el("button", {
          type: "button",
          class: "rui-mention-input-option",
          id: `${panelId2}-option-${idx}`,
          role: "option",
          "aria-selected": idx === nextActive ? "true" : "false",
          "data-value": item.value,
          "data-active": idx === nextActive ? "true" : "false"
        });
        btn.append(el("span", { class: "rui-mention-input-option-label" }, [item.label]));
        if (item.value && item.value !== item.label) {
          btn.append(el("span", { class: "rui-mention-input-option-handle" }, [`@${item.value}`]));
        }
        btn.onmousedown = (event) => event.preventDefault();
        btn.onclick = (event) => {
          const origin = event.currentTarget;
          const ta = anchor ?? liveTextareaFor(origin);
          const live = ta ? liveSuggestionsFor(ta) : liveSuggestionsFor(origin);
          if (ta) insertMention(ta, item);
          querySlot.set(null);
          if (live) paintSuggestions(live, null, ta);
        };
        panel.append(btn);
      });
      applyActive(panel, field, nextActive);
      if (anchor) openMentionPanel(panel, anchor);
    };
    const updateFromCaret = (liveTextarea) => {
      const live = liveSuggestionsFor(liveTextarea);
      if (!live) return;
      const caret = liveTextarea.selectionStart ?? liveTextarea.value.length;
      const before = liveTextarea.value.slice(0, caret);
      const match = /(?:^|\s)@([\w-]*)$/.exec(before);
      const nextQuery = match ? match[1] ?? "" : null;
      const previous = querySlot.get();
      querySlot.set(nextQuery);
      if (nextQuery !== null && nextQuery !== previous) helpers.invoke(props.onSearch, nextQuery);
      paintSuggestions(live, nextQuery, liveTextarea);
    };
    const stateName = node.argMeta?.[2]?.stateRef;
    textarea.oninput = (event) => {
      const ta = event.currentTarget ?? event.target;
      updateFromCaret(ta);
      if (stateName) helpers.setState(stateName, ta.value);
      helpers.invoke(props.onChange, ta.value);
    };
    textarea.onkeydown = (event) => {
      const ta = event.currentTarget;
      const live = liveSuggestionsFor(ta);
      if (!live || live.getAttribute("data-open") !== "true") return;
      const matches = matchesSlot.get();
      if (matches.length === 0) return;
      if (event.key === "ArrowDown") {
        event.preventDefault();
        const next = (activeSlot.get() + 1) % matches.length;
        activeSlot.set(next);
        applyActive(live, ta, next);
      } else if (event.key === "ArrowUp") {
        event.preventDefault();
        const next = (activeSlot.get() - 1 + matches.length) % matches.length;
        activeSlot.set(next);
        applyActive(live, ta, next);
      } else if (event.key === "Enter" || event.key === "Tab") {
        event.preventDefault();
        const chosen = matches[activeSlot.get()];
        if (chosen) {
          insertMention(ta, chosen);
          querySlot.set(null);
          paintSuggestions(live, null, ta);
        }
      } else if (event.key === "Escape") {
        querySlot.set(null);
        paintSuggestions(live, null, ta);
      }
    };
    textarea.onblur = (event) => {
      const ta = event.currentTarget ?? event.target;
      helpers.registerDisposer(() => {
        const pending = blurTimerSlot.get();
        if (pending === null) return;
        clearTimeout(pending);
        blurTimerSlot.set(null);
      }, "mention-blur-dismiss");
      const handle = setTimeout(() => {
        blurTimerSlot.set(null);
        if (!ta.isConnected) return;
        const live = liveSuggestionsFor(ta);
        querySlot.set(null);
        if (live) paintSuggestions(live, null, ta);
      }, 120);
      blurTimerSlot.set(handle);
      if (props.onBlur != null) helpers.invoke(props.onBlur, ta.value);
    };
    if (props.onFocus != null) {
      textarea.onfocus = (event) => {
        const ta = event.currentTarget ?? event.target;
        helpers.invoke(props.onFocus, ta.value);
      };
    }
    root.append(textarea);
    root.append(suggestions);
    paintSuggestions(suggestions, querySlot.get());
    if (suggestions.getAttribute("data-open") === "true") positionMentionOnMount(root);
    const shell = withFieldShell(root, { ...props, id });
    forwardFieldAria(root, textarea);
    return shell;
  }
};
const TimePicker = {
  name: "TimePicker",
  description: 'Time-of-day picker that wraps `<input type="time">`. Pass a `$variable` as `value` for two-way binding (HH:MM 24-hour, written on commit). Set `step` to constrain to specific increments (e.g. 900 for 15-minute buckets, or `"any"` for seconds). Pass `label`/`hint`/`error`/`required` for the labelled field shell.',
  props: [
    { name: "id", type: "string" },
    { name: "value", type: "string", optional: true, description: "HH:MM value; typically $variable" },
    { name: "min", type: "string", optional: true },
    { name: "max", type: "string", optional: true },
    { name: "step", type: "number", optional: true, description: "Seconds between selectable times" },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"], description: "Called with the new HH:MM string when the user picks a time" },
    ...FIELD_SHELL_PROPS
  ],
  render: (node, props, helpers) => {
    const id = asString(props.id) || autoId(helpers, "rui-time-picker");
    const root = el("div", { class: "rui-time-picker" });
    const input = el("input", {
      type: "time",
      class: "rui-time-picker-input",
      id,
      name: asString(props.name, id),
      value: valueAttr(props.value),
      min: asString(props.min) || null,
      max: asString(props.max) || null,
      step: stepAttr(props.step),
      disabled: asBoolean(props.disabled) ? "" : null
    });
    const stateName = node.argMeta?.[1]?.stateRef;
    if (stateName) {
      helpers.bindState(input, stateName, {
        event: "change",
        getValue: (n) => n.value
      });
    }
    attachOnChange(input, props.onChange, helpers, {
      event: "change",
      getValue: (n) => n.value
    });
    attachFocusHandlers(input, props, helpers);
    root.append(input);
    const shell = withFieldShell(root, { ...props, id });
    forwardFieldAria(root, input);
    return shell;
  }
};
const DateTimePicker = {
  name: "DateTimePicker",
  description: 'Combined date + time picker — wraps `<input type="datetime-local">`. Pass a `$variable` as `value` for two-way binding (ISO `YYYY-MM-DDTHH:MM`, written on commit). The field is timezone-naive, so spell the timezone out in `hint`. Pass `label`/`hint`/`error`/`required` for the labelled field shell.',
  props: [
    { name: "id", type: "string" },
    { name: "value", type: "string", optional: true, description: "ISO date-time value; typically $variable" },
    { name: "min", type: "string", optional: true },
    { name: "max", type: "string", optional: true },
    { name: "step", type: "number", optional: true, description: "Seconds between selectable times" },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"], description: "Called with the new ISO `YYYY-MM-DDTHH:MM` string" },
    ...FIELD_SHELL_PROPS
  ],
  render: (node, props, helpers) => {
    const id = asString(props.id) || autoId(helpers, "rui-datetime-picker");
    const root = el("div", { class: "rui-datetime-picker" });
    const input = el("input", {
      type: "datetime-local",
      class: "rui-datetime-picker-input",
      id,
      name: asString(props.name, id),
      value: valueAttr(props.value),
      min: asString(props.min) || null,
      max: asString(props.max) || null,
      step: stepAttr(props.step),
      disabled: asBoolean(props.disabled) ? "" : null
    });
    const stateName = node.argMeta?.[1]?.stateRef;
    if (stateName) {
      helpers.bindState(input, stateName, {
        event: "change",
        getValue: (n) => n.value
      });
    }
    attachOnChange(input, props.onChange, helpers, {
      event: "change",
      getValue: (n) => n.value
    });
    attachFocusHandlers(input, props, helpers);
    root.append(input);
    const shell = withFieldShell(root, { ...props, id });
    forwardFieldAria(root, input);
    return shell;
  }
};
function applyMask$1(value, mask) {
  if (!mask) return value;
  let out = "";
  let i = 0;
  const v = String(value ?? "");
  for (const ch of mask) {
    if (i >= v.length) break;
    if (ch === "9") {
      while (i < v.length && !/\d/.test(v[i] ?? "")) i += 1;
      if (i >= v.length) break;
      out += v[i];
      i += 1;
    } else if (ch === "A") {
      while (i < v.length && !/[a-zA-Z]/.test(v[i] ?? "")) i += 1;
      if (i >= v.length) break;
      out += v[i];
      i += 1;
    } else if (ch === "*") {
      out += v[i];
      i += 1;
    } else {
      out += ch;
      if (v[i] === ch) i += 1;
    }
  }
  return out;
}
function isMaskToken(ch) {
  return ch === "9" || ch === "A" || ch === "*";
}
function unmaskValue(masked, mask) {
  if (!mask) return masked;
  let out = "";
  for (let i = 0; i < masked.length && i < mask.length; i += 1) {
    if (isMaskToken(mask[i])) out += masked[i];
  }
  return out;
}
const MASK_INPUT_MODES = ["text", "numeric", "decimal", "tel", "none"];
function maskInputMode(mask) {
  const tokens = Array.from(mask).filter((c) => isMaskToken(c));
  if (tokens.length === 0 || tokens.some((t) => t !== "9")) return null;
  return "numeric";
}
const MaskedInput = {
  name: "MaskedInput",
  description: 'Text input with an inline mask — `9` matches a digit, `A` matches a letter, `*` matches any character, every other character is a fixed delimiter. Useful for phone numbers, postal codes, credit cards. Pass `mask` (e.g. `"(999) 999-9999"`) and a `$variable` as `value`. Set `unmasked: true` to bind the token characters only (no punctuation in the submitted payload). Pass `label`/`hint`/`error`/`required` for the labelled field shell.',
  props: [
    { name: "id", type: "string" },
    { name: "mask", type: "string", description: "Mask pattern" },
    { name: "value", type: "string", optional: true, description: "Bound value (typically $variable)" },
    { name: "placeholder", type: "string", optional: true },
    { name: "unmasked", type: "boolean", optional: true, aliases: ["rawValue"], description: "Bind/report the token characters only, without the mask's delimiters" },
    { name: "inputMode", type: "string", optional: true, aliases: ["inputmode"], enum: MASK_INPUT_MODES, description: "Mobile keyboard hint (derived from the mask when omitted)" },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"], description: "Called with the current value on every keystroke" },
    ...FIELD_SHELL_PROPS
  ],
  render: (node, props, helpers) => {
    const id = asString(props.id) || autoId(helpers, "rui-masked-input");
    const mask = asString(props.mask);
    const disabled = asBoolean(props.disabled);
    const unmasked = asBoolean(props.unmasked);
    const initial = valueAttr(props.value) === null ? null : applyMask$1(asString(props.value), mask);
    const input = el("input", {
      type: "text",
      class: "rui-masked-input",
      id,
      name: asString(props.name, id),
      value: initial,
      placeholder: asString(props.placeholder, mask),
      inputmode: asString(props.inputMode) || maskInputMode(mask),
      disabled: disabled ? "" : null,
      autocomplete: "off"
    });
    if (initial !== null) input.value = initial;
    const stateName = node.argMeta?.[2]?.stateRef;
    const formatInPlace = (target) => {
      const before = target.value;
      const masked = applyMask$1(before, mask);
      if (masked === before) return masked;
      const caret = target.selectionStart ?? before.length;
      target.value = masked;
      let pos = Math.min(masked.length, applyMask$1(before.slice(0, caret), mask).length);
      while (pos < masked.length && pos < mask.length && !isMaskToken(mask[pos])) pos += 1;
      try {
        target.setSelectionRange(pos, pos);
      } catch {
      }
      return masked;
    };
    input.oninput = (event) => {
      const live = event.currentTarget ?? event.target;
      const masked = formatInPlace(live);
      const reported = unmasked ? unmaskValue(masked, mask) : masked;
      if (stateName) helpers.setState(stateName, reported);
      helpers.invoke(props.onChange, reported);
    };
    attachFocusHandlers(input, props, helpers, (n) => {
      const masked = n.value;
      return unmasked ? unmaskValue(masked, mask) : masked;
    });
    return withFieldShell(input, { ...props, id });
  }
};
const HEADING_TAGS = ["h2", "h3", "h4", "h5", "h6"];
const FormSection = {
  name: "FormSection",
  description: "Semantic grouping for related form fields — renders a small heading (`label`), optional helper paragraph, and stacks the children with consistent spacing. Use INSTEAD of wrapping fields in `Card` + `SectionHeader` by hand. Set `level` (2-6, default 3) so the heading fits the surrounding document outline. Pair with `FieldSet` when the group is a true `<fieldset>` (radio sets, checkbox groups).",
  props: [
    { name: "label", type: "string", aliases: ["title"] },
    { name: "children", type: "Node[]", aliases: ["child", "fields"] },
    { name: "helper", type: "string", optional: true, aliases: ["description"], description: "Description rendered below the label" },
    { name: "level", type: "number", optional: true, description: "Heading level 2-6 (default 3) — keep the page outline correct" }
  ],
  render: (_node, props, helpers) => {
    const root = el("section", { class: "rui-form-section" });
    const label = asString(props.label);
    const helper = asString(props.helper);
    if (label || helper) {
      const header = el("header", { class: "rui-form-section-header" });
      if (label) {
        const level = Math.max(2, Math.min(6, Math.floor(asNumber(props.level, 3))));
        header.append(el(HEADING_TAGS[level - 2] ?? "h3", { class: "rui-form-section-label" }, [label]));
      }
      if (helper) header.append(el("p", { class: "rui-form-section-helper" }, [helper]));
      root.append(header);
    }
    const body = el("div", { class: "rui-form-section-body" });
    for (const child of asArray(props.children)) body.append(helpers.renderNode(child));
    root.append(body);
    return root;
  }
};
const FieldSet = {
  name: "FieldSet",
  description: 'Native `<fieldset>`/`<legend>` wrapper for accessible grouping of related controls. Use when assistive tech should announce the wrapper (radio sets, checkbox groups). `error` renders a GROUP-level validation message ("Select at least one contact method") that belongs to the set rather than to any single control. For purely visual grouping prefer `FormSection`.',
  props: [
    { name: "legend", type: "string", aliases: ["title", "label"] },
    { name: "children", type: "Node[]", aliases: ["child", "fields"] },
    { name: "helper", type: "string", optional: true, aliases: ["hint", "description"] },
    { name: "disabled", type: "boolean", optional: true },
    { name: "error", type: "string", optional: true, description: "Group-level validation error (marks the whole group invalid)" },
    { name: "required", type: "boolean", optional: true, description: "Mark the group required (adds a `*` to the legend)" }
  ],
  render: (_node, props, helpers) => {
    const helper = asString(props.helper);
    const error = asString(props.error);
    const required = asBoolean(props.required);
    const baseId = autoId(helpers, "rui-fieldset");
    const root = el("fieldset", {
      class: "rui-fieldset",
      disabled: asBoolean(props.disabled) ? "" : null,
      "data-invalid": error ? "true" : null,
      "aria-invalid": error ? "true" : null,
      "aria-required": required ? "true" : null
    });
    const legend2 = el("legend", { class: "rui-fieldset-legend" }, [asString(props.legend)]);
    if (required) legend2.append(el("span", { class: "rui-field-required", "aria-hidden": "true" }, ["*"]));
    root.append(legend2);
    const describedBy = [];
    if (helper) {
      const helperId = `${baseId}-helper`;
      describedBy.push(helperId);
      root.append(el("p", { class: "rui-fieldset-helper", id: helperId }, [helper]));
    }
    if (error) {
      const errorId = `${baseId}-error`;
      describedBy.push(errorId);
      root.append(el("p", {
        class: "rui-fieldset-error rui-field-error",
        id: errorId,
        role: "alert"
      }, [error]));
    }
    if (describedBy.length > 0) root.setAttribute("aria-describedby", describedBy.join(" "));
    for (const child of asArray(props.children)) root.append(helpers.renderNode(child));
    return root;
  }
};
const ValidationSummary = {
  name: "ValidationSummary",
  description: 'Aggregate error list rendered at the top of a form. Pass `errors` as `{label?, message, field?}` objects or plain strings; an entry with a `field` renders as a link that focuses that control (or calls `onErrorClick(field)`). Set `count: true` to lead with "There are 3 problems with this form". Pair with individual field hints via `FormControl(hint=...)`.',
  props: [
    { name: "errors", type: "any[]" },
    { name: "title", type: "string", optional: true, description: 'Heading (default "Please fix the following:")' },
    { name: "tone", aliases: ["variant"], type: "string", optional: true, enum: ["danger", "warning"] },
    { name: "count", type: "boolean", optional: true, description: 'Use a counting heading ("There are 3 problems with this form")' },
    { name: "onErrorClick", type: "callable", optional: true, description: "Called with the entry's `field` key when it is clicked" }
  ],
  render: (_node, props, helpers) => {
    const errors = asArray(props.errors).map((entry) => {
      if (!entry) return null;
      if (typeof entry === "string") return { label: "", message: entry, field: "" };
      if (typeof entry === "object") {
        const r = entry;
        return {
          label: asString(r.label),
          message: asString(r.message ?? r.error),
          field: asString(r.field)
        };
      }
      return null;
    }).filter((e) => e !== null && e.message !== "");
    const populatedSlot = helpers.useInstanceState("populated", false);
    if (errors.length === 0) {
      populatedSlot.set(false);
      return el("div", { class: "rui-validation-summary", "data-empty": "true", hidden: "" });
    }
    const appearing = !populatedSlot.get();
    populatedSlot.set(true);
    const tone = asString(props.tone, "danger");
    const root = el("aside", {
      class: "rui-validation-summary",
      "data-tone": tone,
      role: "alert",
      // Focusable so the user is taken to the errors instead of having to
      // shift-tab back up through the whole form from the Submit button.
      tabindex: "-1"
    });
    const titleNode = el("div", { class: "rui-validation-summary-title" });
    const iconNode = renderIcon(tone === "warning" ? "triangle-exclamation" : "circle-xmark", { className: "rui-validation-summary-icon" });
    if (iconNode) titleNode.append(iconNode);
    const counted = errors.length === 1 ? "There is 1 problem with this form" : `There are ${errors.length} problems with this form`;
    const heading = asString(props.title, asBoolean(props.count) ? counted : "Please fix the following:");
    titleNode.append(document.createTextNode(heading));
    root.append(titleNode);
    const focusField = (origin, field) => {
      const scope = origin.getRootNode();
      const host = scope instanceof ShadowRoot || scope instanceof Document ? scope : origin.ownerDocument;
      if (!host) return;
      let target = null;
      try {
        const escaped = typeof CSS !== "undefined" && typeof CSS.escape === "function" ? CSS.escape(field) : field;
        target = host.querySelector(`#${escaped}`);
      } catch {
      }
      if (!target) return;
      try {
        target.scrollIntoView?.({ behavior: "smooth", block: "center" });
      } catch {
      }
      if (!target.matches("input, select, textarea, button, a[href], [tabindex]")) {
        target.setAttribute("tabindex", "-1");
      }
      target.focus?.();
    };
    const list = el("ul", { class: "rui-validation-summary-list" });
    for (const err of errors) {
      const li = el("li", { class: "rui-validation-summary-item" });
      if (err.label) li.append(el("strong", {}, [`${err.label}: `]));
      if (err.field) {
        const link = el("a", {
          class: "rui-validation-summary-link",
          href: sanitiseHref(`#${err.field}`, "#")
        }, [err.message]);
        const field = err.field;
        link.onclick = (event) => {
          event.preventDefault();
          const origin = event.currentTarget ?? event.target;
          if (origin) focusField(origin, field);
          helpers.invoke(props.onErrorClick, field);
        };
        li.append(link);
      } else {
        li.append(document.createTextNode(err.message));
      }
      list.append(li);
    }
    root.append(list);
    if (appearing) {
      deferToPaint(() => {
        if (!root.isConnected) return;
        try {
          root.scrollIntoView?.({ behavior: "smooth", block: "start" });
        } catch {
        }
        root.focus?.();
      });
    }
    return root;
  }
};
function readSteps(raw) {
  return asArray(raw).map((entry) => {
    if (!entry || typeof entry !== "object") {
      return { title: asString(entry), details: "", content: null };
    }
    const r = entry;
    return {
      title: asString(r.title),
      details: asString(r.details),
      content: r.content ?? null
    };
  });
}
const MultiStepForm = {
  name: "MultiStepForm",
  description: 'Multi-step / wizard form composite. Renders a `Steps` indicator, the active step\'s `content`, and Prev/Next buttons that drive a `$variable` for the current 0-indexed step (or call `onStepChange` when `current` is a literal). Use INSTEAD of hand-rolling `Steps` + content + manual prev/next wiring. Set `nextDisabled` to block advancing past an incomplete step and `submitting` while the submit is in flight. The submit button is rendered on the final step (override via `submitLabel`). Step indicator direction defaults to `column` (stacked next to the content); set `stepsLayout: "row"` for a classic horizontal stepper.',
  props: [
    { name: "steps", type: "object[]", description: "Array of {title, details?, content} step objects" },
    { name: "current", type: "number", description: "0-indexed active step — bind a $variable (or pass a literal plus `onStepChange`)" },
    { name: "onSubmit", type: "callable", optional: true, description: "Callable fired when the user clicks Submit on the final step" },
    { name: "prevLabel", type: "string", optional: true, description: 'Default "Back"' },
    { name: "nextLabel", type: "string", optional: true, description: 'Default "Continue"' },
    { name: "submitLabel", type: "string", optional: true, description: 'Default "Submit" (final step)' },
    { name: "stepsLayout", type: "string", optional: true, enum: ["column", "row"], aliases: ["layout", "stepsDirection"], description: 'Direction of the steps indicator (default "column")' },
    { name: "nextDisabled", type: "boolean", optional: true, description: "Block Continue/Submit while the active step is incomplete" },
    { name: "submitting", type: "boolean", optional: true, description: "Disable the footer buttons while the submit is in flight" },
    { name: "onStepChange", type: "callable", optional: true, description: "Called with the new 0-indexed step whenever the step changes" },
    { name: "onStepClick", type: "callable", optional: true, description: "Called with the index when a completed step in the indicator is clicked" },
    { name: "clickableSteps", type: "boolean", optional: true, description: "Let the user jump back to a completed step from the indicator" },
    { name: "showProgress", type: "boolean", optional: true, description: "Show the `n / total` counter (default true)" },
    { name: "hideFooter", type: "boolean", optional: true, description: "Suppress the built-in Back/Continue row (supply your own action row)" },
    { name: "emptyText", type: "string", optional: true, description: "Message shown while `steps` is empty (e.g. before a fetch lands)" }
  ],
  render: (node, props, helpers) => {
    const steps = readSteps(props.steps);
    const total = steps.length;
    const layoutToken = asString(props.stepsLayout, "column").toLowerCase();
    const layout = layoutToken === "row" || layoutToken === "horizontal" ? "row" : "column";
    if (total === 0) {
      const empty = el("div", { class: "rui-multi-step-form", "data-empty": "true" });
      const box = el("div", { class: "rui-empty-state" });
      box.append(el("p", { class: "rui-empty-state-description" }, [
        asString(props.emptyText, "No steps to show yet.")
      ]));
      empty.append(box);
      return empty;
    }
    const current = Math.max(0, Math.min(total - 1, Math.floor(asNumber(props.current, 0))));
    const stateName = node.argMeta?.[1]?.stateRef;
    const submitting = asBoolean(props.submitting);
    const nextBlocked = asBoolean(props.nextDisabled);
    const canNavigate = stateName != null || props.onStepChange != null;
    const goTo = (next) => {
      const target = Math.max(0, Math.min(total - 1, next));
      if (target === current) return;
      if (stateName) helpers.setState(stateName, target);
      helpers.invoke(props.onStepChange, target);
    };
    const root = el("div", {
      class: "rui-multi-step-form",
      "data-layout": layout
    });
    const stepsEl = el("ol", {
      class: "rui-steps rui-multi-step-form-steps",
      "data-layout": layout,
      "aria-label": "Progress"
    });
    const stepsClickable = (asBoolean(props.clickableSteps) || props.onStepClick != null) && !submitting;
    steps.forEach((step, idx) => {
      const complete = idx < current;
      const li = el("li", {
        class: "rui-steps-item",
        "data-active": idx === current ? "true" : "false",
        "data-complete": complete ? "true" : "false",
        // Active/complete used to live only in `data-*`, which no screen reader
        // reads, and the ✓ is a CSS `content` most of them skip.
        "aria-current": idx === current ? "step" : null
      });
      li.append(el("div", { class: "rui-steps-title" }, [step.title || `Step ${idx + 1}`]));
      if (complete) li.append(el("span", { class: "rui-visually-hidden" }, [" (completed)"]));
      if (step.details) li.append(el("div", { class: "rui-steps-details" }, [step.details]));
      if (stepsClickable && complete) {
        li.setAttribute("role", "button");
        li.setAttribute("tabindex", "0");
        li.setAttribute("data-clickable", "true");
        li.setAttribute("data-index", String(idx));
        const jump = (origin) => {
          const target = Number(origin.getAttribute("data-index"));
          if (!Number.isFinite(target)) return;
          goTo(target);
          helpers.invoke(props.onStepClick, target);
        };
        li.onclick = (event) => jump(event.currentTarget ?? event.target);
        li.onkeydown = (event) => {
          const e = event;
          if (e.key !== "Enter" && e.key !== " ") return;
          e.preventDefault();
          jump(e.currentTarget ?? e.target);
        };
      }
      stepsEl.append(li);
    });
    root.append(stepsEl);
    const active = steps[current];
    const body = el("div", {
      class: "rui-multi-step-form-body",
      role: "group",
      "aria-label": `${active?.title || `Step ${current + 1}`} — step ${current + 1} of ${total}`
    });
    if (active && active.content) {
      body.append(helpers.renderNode(active.content));
    }
    root.append(body);
    if (!asBoolean(props.hideFooter)) {
      const footer = el("div", { class: "rui-multi-step-form-footer" });
      const prevBtn = el("button", {
        type: "button",
        class: "rui-button",
        "data-variant": "ghost",
        disabled: current <= 0 || submitting || !canNavigate ? "" : null
      }, [asString(props.prevLabel, "Back")]);
      prevBtn.onclick = () => {
        if (submitting) return;
        goTo(current - 1);
      };
      const isFinal = current >= total - 1;
      const nextLabel = isFinal ? asString(props.submitLabel, "Submit") : asString(props.nextLabel, "Continue");
      const nextBtn = el("button", {
        type: "button",
        class: "rui-button",
        "data-variant": "primary",
        disabled: nextBlocked || submitting || !isFinal && !canNavigate ? "" : null,
        "aria-busy": submitting ? "true" : null
      }, [nextLabel]);
      nextBtn.onclick = () => {
        if (submitting || nextBlocked) return;
        if (isFinal) {
          helpers.invoke(props.onSubmit);
          return;
        }
        goTo(current + 1);
      };
      footer.append(prevBtn);
      if (asBoolean(props.showProgress, true)) {
        footer.append(el("span", { class: "rui-multi-step-form-progress" }, [`${current + 1} / ${total}`]));
      }
      footer.append(nextBtn);
      root.append(footer);
    }
    root.append(el("div", {
      class: "rui-visually-hidden",
      role: "status",
      "aria-live": "polite"
    }, [`Step ${current + 1} of ${total}: ${active?.title || ""}`]));
    return root;
  }
};
function readRequirements(raw) {
  return asArray(raw).map((entry) => {
    if (typeof entry === "string") return entry ? { label: entry, met: null } : null;
    if (entry && typeof entry === "object") {
      const r = entry;
      const label = asString(r.label ?? r.text);
      if (!label) return null;
      const met = r.met === void 0 || r.met === null ? null : r.met === true;
      return { label, met };
    }
    return null;
  }).filter((r) => r !== null);
}
const RequirementList = {
  name: "RequirementList",
  description: 'Checklist of rules a value has to satisfy, each marked met (check), unmet (cross) or not yet checked (dot) — password requirements, naming rules, policy checks. Pass `items` as strings or `{label, met}` objects, where `met` omitted means "not evaluated yet" and renders neutral, so an untouched field does not accuse the reader of breaking rules. Pair it with a field\'s `invalid` and `describedBy` props to keep the border red and the list as the explanation, instead of repeating the rules in an `error` string. Set `announce` to have changes read out politely as the value is edited.',
  props: [
    { name: "items", type: "any[]", positional: true, aliases: ["rules", "requirements"], description: "Rules: strings, or `{label, met}` where `met` is true / false / omitted" },
    { name: "title", type: "string", optional: true, description: 'Heading above the list (e.g. "Your password must:")' },
    { name: "pending", type: "boolean", optional: true, description: "Force every row neutral regardless of its `met` — for a field the user has not touched yet" },
    { name: "announce", type: "boolean", optional: true, description: "Announce progress to assistive tech as rows change (a polite count, not the rule text). Off by default, because a list that talks on every keystroke is worse than one that stays quiet" },
    { name: "announceText", type: "string", optional: true, description: 'What `announce` says; `{met}` and `{total}` are substituted (default "{met} of {total} requirements met")' },
    { name: "metLabel", type: "string", optional: true, description: 'Screen-reader prefix for a met row (default "Met") — colour and glyph alone would not say which is which' },
    { name: "unmetLabel", type: "string", optional: true, description: 'Screen-reader prefix for an unmet row (default "Not met")' }
  ],
  render: (_node, props) => {
    const items = readRequirements(props.items);
    const pending = asBoolean(props.pending);
    const root = el("div", {
      class: "rui-requirement-list",
      "data-pending": pending ? "true" : null
    });
    const title = asString(props.title);
    if (title) root.append(el("div", { class: "rui-requirement-list-title" }, [title]));
    if (items.length === 0) return root;
    const metLabel = asString(props.metLabel) || "Met";
    const unmetLabel = asString(props.unmetLabel) || "Not met";
    const list = el("ul", { class: "rui-requirement-list-items" });
    let metCount = 0;
    for (const item of items) {
      const state = pending || item.met === null ? "pending" : item.met ? "true" : "false";
      if (state === "true") metCount += 1;
      const row = el("li", { class: "rui-requirement", "data-met": state });
      const icon = renderIcon(
        state === "true" ? "circle-check" : state === "false" ? "circle-xmark" : "circle",
        { className: "rui-requirement-icon" }
      );
      if (icon) row.append(icon);
      if (state !== "pending") {
        row.append(el("span", { class: "rui-visually-hidden" }, [`${state === "true" ? metLabel : unmetLabel}: `]));
      }
      row.append(el("span", { class: "rui-requirement-text" }, [item.label]));
      list.append(row);
    }
    root.append(list);
    if (asBoolean(props.announce)) {
      const template = asString(props.announceText, "{met} of {total} requirements met");
      const text = template.split("{met}").join(String(metCount)).split("{total}").join(String(items.length));
      root.append(el("div", {
        class: "rui-requirement-list-status rui-visually-hidden",
        role: "status",
        "aria-live": "polite",
        "aria-atomic": "true"
      }, [text]));
    }
    return root;
  }
};
let labelIdSeq = 0;
function instanceLabelId(helpers, key, prefix) {
  const slot = helpers.useInstanceState(key, "");
  if (!slot.get()) slot.set(`${prefix}-${labelIdSeq += 1}`);
  return slot.get();
}
function readInboxEntries(raw) {
  return asArray(raw).map((entry) => {
    if (!entry || typeof entry !== "object") return null;
    const r = entry;
    return {
      title: asString(r.title),
      message: asString(r.message),
      time: asString(r.time),
      icon: asString(r.icon),
      tone: asString(r.tone, "default"),
      unread: asBoolean(r.unread),
      avatarSrc: asString(r.avatarSrc),
      onClick: r.onClick ?? r.action,
      actions: r.actions
    };
  }).filter((e) => e !== null);
}
const INTERACTIVE_CHILD = "input,button,a,label,select,textarea";
const InboxPanel = {
  name: "InboxPanel",
  description: "Grouped notification list — entries are grouped into Unread/Earlier sections, with a count chip on each group header. Pass `items` as `{title, message, time, icon?, tone?, unread?, avatarSrc?, onClick?, actions?}` objects (`action` is also accepted as an alias for `onClick`; `actions` takes Button(...) entries rendered inside the row). Pair with a `SectionHeader` for the panel title (the component does not render its own title to avoid duplication). Set `loading` while entries are still being fetched so the panel does not claim the inbox is empty. Use for top-bar notification trays, activity drawers, and alert center pages.",
  props: [
    { name: "items", type: "object[]" },
    { name: "emptyLabel", type: "string", optional: true, description: "Text shown when there are no notifications" },
    { name: "onMarkAllRead", type: "callable", optional: true, description: 'Callable fired by the "Mark all as read" button' },
    { name: "loading", type: "boolean", optional: true, description: "Show a busy placeholder instead of the empty state while entries load" },
    { name: "loadingLabel", type: "string", optional: true, description: 'Busy text (default "Loading…")' },
    { name: "markAllLabel", type: "string", optional: true, description: 'Label of the mark-all button (default "Mark all as read")' },
    { name: "unreadLabel", type: "string", optional: true, description: 'Unread group heading (default "Unread")' },
    { name: "earlierLabel", type: "string", optional: true, description: 'Read group heading (default "Earlier")' }
  ],
  render: (_node, props, helpers) => {
    const entries = readInboxEntries(props.items);
    const unread = entries.filter((e) => e.unread);
    const read = entries.filter((e) => !e.unread);
    const loading = asBoolean(props.loading);
    const root = el("div", { class: "rui-inbox-panel", "aria-busy": loading ? "true" : null });
    if (typeof props.onMarkAllRead === "function" && unread.length > 0) {
      const toolbar = el("div", { class: "rui-inbox-panel-toolbar" });
      const btn = el("button", { type: "button", class: "rui-inbox-panel-mark-all" }, [
        asString(props.markAllLabel, "Mark all as read")
      ]);
      btn.onclick = () => helpers.invoke(props.onMarkAllRead);
      toolbar.append(btn);
      root.append(toolbar);
    }
    if (entries.length === 0) {
      if (loading) {
        const busy = el("div", { class: "rui-inbox-panel-empty rui-inbox-panel-loading", role: "status", "aria-live": "polite" });
        const spinner = el("span", { class: "rui-spinner", "data-size": "md", "data-tone": "primary" });
        spinner.append(el("span", { class: "rui-spinner-ring", "aria-hidden": "true" }));
        busy.append(spinner, el("span", {}, [asString(props.loadingLabel, "Loading…")]));
        root.append(busy);
        return root;
      }
      root.append(el("div", { class: "rui-inbox-panel-empty" }, [asString(props.emptyLabel, "You're all caught up.")]));
      return root;
    }
    const renderGroup = (label, items) => {
      if (items.length === 0) return;
      const group = el("section", { class: "rui-inbox-panel-group" });
      const groupHead = el("header", { class: "rui-inbox-panel-group-head" });
      groupHead.append(el("span", { class: "rui-inbox-panel-group-label" }, [label]));
      groupHead.append(el("span", { class: "rui-inbox-panel-group-count" }, [String(items.length)]));
      group.append(groupHead);
      for (const entry of items) {
        const card = Notification.render(
          { __kind: "Component", name: "Notification", args: [], argMeta: [] },
          {
            title: entry.title,
            message: entry.message,
            time: entry.time,
            icon: entry.icon,
            tone: entry.tone,
            avatarSrc: entry.avatarSrc,
            unread: entry.unread,
            actions: entry.actions
          },
          helpers
        );
        if (typeof entry.onClick === "function") {
          card.setAttribute("data-clickable", "true");
          card.setAttribute("role", "button");
          card.tabIndex = 0;
          const fire = (event) => {
            const target = event.target;
            if (target?.closest(INTERACTIVE_CHILD)) return false;
            helpers.invoke(entry.onClick);
            return true;
          };
          card.onclick = (event) => {
            fire(event);
          };
          card.onkeydown = (event) => {
            if (event.key !== "Enter" && event.key !== " ") return;
            if (fire(event)) event.preventDefault();
          };
        }
        group.append(card);
      }
      root.append(group);
    };
    renderGroup(asString(props.unreadLabel, "Unread"), unread);
    renderGroup(asString(props.earlierLabel, "Earlier"), read);
    return root;
  }
};
function readChecklistItems(raw) {
  return asArray(raw).map((entry) => {
    if (!entry || typeof entry !== "object") return null;
    const r = entry;
    return {
      title: asString(r.title),
      description: asString(r.description),
      done: asBoolean(r.done),
      onClick: r.onClick ?? r.action,
      cta: asString(r.cta)
    };
  }).filter((c) => c !== null);
}
const OnboardingChecklist = {
  name: "OnboardingChecklist",
  description: 'Step-by-step product checklist with completion progress at the top. Pass `items` as `{title, description?, done?, onClick?, cta?}` objects (`action` is also accepted as an alias). The progress percentage is computed automatically from `done`; `onComplete` fires once every step is done. An item\'s `onClick` stays available after it is completed (the button label defaults to "Start", or "Review" for a done step). Use on first-run dashboards, empty workspaces, and "complete your profile" surfaces.',
  props: [
    { name: "items", type: "object[]" },
    { name: "title", type: "string", optional: true, description: 'Heading (default "Getting started")' },
    { name: "subtitle", type: "string", optional: true },
    { name: "onDismiss", type: "callable", optional: true, description: "Callable fired by the dismiss (×) button — omit to render no dismiss affordance" },
    { name: "onComplete", type: "callable", optional: true, description: "Callable fired once, when the last remaining step flips to done" }
  ],
  render: (_node, props, helpers) => {
    const items = readChecklistItems(props.items);
    const completed = items.filter((i) => i.done).length;
    const total = Math.max(1, items.length);
    const pct = Math.round(completed / total * 100);
    const root = el("div", { class: "rui-onboarding-checklist" });
    const head = el("header", { class: "rui-onboarding-checklist-header" });
    const headRow = el("div", { class: "rui-onboarding-checklist-headrow" });
    headRow.append(el("h3", { class: "rui-onboarding-checklist-title" }, [
      asString(props.title, "Getting started")
    ]));
    if (typeof props.onDismiss === "function") {
      const dismiss = el("button", {
        type: "button",
        class: "rui-onboarding-checklist-dismiss",
        "aria-label": "Dismiss"
      });
      const icon = renderIcon("xmark");
      if (icon) dismiss.append(icon);
      else dismiss.append(document.createTextNode("×"));
      dismiss.onclick = () => helpers.invoke(props.onDismiss);
      headRow.append(dismiss);
    }
    head.append(headRow);
    const subtitle = asString(props.subtitle);
    if (subtitle) head.append(el("p", { class: "rui-onboarding-checklist-subtitle" }, [subtitle]));
    head.append(el("div", { class: "rui-onboarding-checklist-progress" }, [
      el("div", {
        class: "rui-onboarding-checklist-bar",
        // Without these the completion state is carried only by a CSS width
        // and an aria-hidden icon, i.e. nothing assistive tech can read.
        role: "progressbar",
        "aria-valuenow": String(pct),
        "aria-valuemin": "0",
        "aria-valuemax": "100",
        "aria-valuetext": `${completed} of ${items.length} steps complete`
      }, [
        el("div", {
          class: "rui-onboarding-checklist-fill",
          style: `width:${pct}%`
        })
      ]),
      el("span", { class: "rui-onboarding-checklist-meta" }, [`${completed}/${items.length} complete`])
    ]));
    root.append(head);
    const list = el("ol", { class: "rui-onboarding-checklist-list" });
    for (const item of items) {
      const li = el("li", {
        class: "rui-onboarding-checklist-item",
        "data-done": item.done ? "true" : "false"
      });
      const marker = el("span", { class: "rui-onboarding-checklist-marker" });
      const iconNode = renderIcon(item.done ? "circle-check" : "circle", { className: "rui-onboarding-checklist-marker-icon" });
      if (iconNode) marker.append(iconNode);
      marker.append(el("span", { class: "rui-visually-hidden" }, [item.done ? "Completed" : "Not completed"]));
      li.append(marker);
      const body = el("div", { class: "rui-onboarding-checklist-body" });
      body.append(el("div", { class: "rui-onboarding-checklist-item-title" }, [item.title]));
      if (item.description) body.append(el("p", { class: "rui-onboarding-checklist-item-description" }, [item.description]));
      li.append(body);
      if (typeof item.onClick === "function") {
        const btn = el("button", {
          type: "button",
          class: "rui-button",
          "data-variant": "secondary",
          "data-size": "sm"
        }, [item.cta || (item.done ? "Review" : "Start")]);
        btn.onclick = () => helpers.invoke(item.onClick);
        li.append(btn);
      }
      list.append(li);
    }
    root.append(list);
    const completeSlot = helpers.useInstanceState("completed", false);
    const allDone = items.length > 0 && completed === items.length;
    if (!allDone) {
      completeSlot.set(false);
    } else if (!completeSlot.get()) {
      completeSlot.set(true);
      deferToPaint(() => helpers.invoke(props.onComplete));
    }
    return root;
  }
};
function renderStateCard(opts) {
  const root = el("div", {
    class: `rui-${opts.klass}`,
    role: opts.role ?? null,
    "aria-live": opts.live ?? null
  });
  if (opts.iconName) {
    const icon = renderIcon(opts.iconName, { className: opts.iconClass });
    if (icon) root.append(icon);
  }
  if (opts.title) root.append(el("h3", { class: `rui-${opts.klass}-title` }, [opts.title]));
  if (opts.description) root.append(el("p", { class: `rui-${opts.klass}-description` }, [opts.description]));
  const items = asArray(opts.actions);
  if (items.length > 0) {
    const row = el("div", { class: `rui-${opts.klass}-actions` });
    for (const item of items) row.append(opts.helpers.renderNode(item));
    root.append(row);
  }
  return root;
}
const LoadingState = {
  name: "LoadingState",
  description: "Full-card loading state — large spinner + title + description. Use while a query is in flight or while a long-running tool runs. Pass `actions` for escape hatches (Cancel / Run in background) and `icon` to replace the spinner for a queued/waiting state. For tiny inline loaders prefer `Spinner`; for skeleton placeholders prefer `Skeleton`.",
  props: [
    { name: "title", type: "string", optional: true, description: 'Default "Loading…"' },
    { name: "description", type: "string", optional: true },
    { name: "actions", type: "Node[]", optional: true, description: "Buttons rendered under the text (Cancel, Run in background, …)" },
    { name: "icon", type: "string", optional: true, description: "Font Awesome icon shown INSTEAD of the spinner (e.g. `clock` for a queued state)" }
  ],
  render: (_node, props, helpers) => {
    const root = el("div", {
      class: "rui-loading-state",
      role: "status",
      "aria-live": "polite",
      "aria-busy": "true"
    });
    const iconName = asString(props.icon);
    const icon = iconName ? renderIcon(iconName, { className: "rui-loading-state-icon" }) : null;
    if (icon) {
      root.append(icon);
    } else {
      const spinner = el("span", { class: "rui-spinner", "data-size": "lg", "data-tone": "primary" });
      spinner.append(el("span", { class: "rui-spinner-ring", "aria-hidden": "true" }));
      root.append(spinner);
    }
    root.append(el("h3", { class: "rui-loading-state-title" }, [asString(props.title, "Loading…")]));
    const description = asString(props.description);
    if (description) root.append(el("p", { class: "rui-loading-state-description" }, [description]));
    const actions = asArray(props.actions);
    if (actions.length > 0) {
      const row = el("div", { class: "rui-loading-state-actions" });
      for (const item of actions) row.append(helpers.renderNode(item));
      root.append(row);
    }
    return root;
  }
};
const ErrorState = {
  name: "ErrorState",
  description: "Full-card error placeholder. Pairs a danger icon with title, description, and a row of recovery actions (Retry / Contact support / Go home). Pass `actions` as Button(...) entries.",
  props: [
    { name: "title", type: "string", optional: true, description: 'Default "Something went wrong"' },
    { name: "description", type: "string", optional: true },
    { name: "actions", type: "Node[]", optional: true },
    { name: "icon", type: "string", optional: true, description: "Font Awesome icon (default `circle-exclamation`)" }
  ],
  render: (_node, props, helpers) => renderStateCard({
    klass: "error-state",
    iconName: asString(props.icon, "circle-exclamation"),
    iconClass: "rui-error-state-icon",
    title: asString(props.title, "Something went wrong"),
    description: asString(props.description),
    actions: props.actions,
    // A failure that replaces content must interrupt — a silent swap leaves
    // the user believing the operation succeeded.
    role: "alert",
    helpers
  })
};
const SuccessState = {
  name: "SuccessState",
  description: 'Full-card success placeholder. Use for confirmation screens ("Order placed", "Payment succeeded", "Account verified") at the end of a flow. Pass `actions` for follow-up CTAs.',
  props: [
    { name: "title", type: "string", optional: true, description: 'Default "Success" — an empty title falls back rather than rendering an icon-only card' },
    { name: "description", type: "string", optional: true },
    { name: "actions", type: "Node[]", optional: true },
    { name: "icon", type: "string", optional: true, description: "Default `circle-check`" }
  ],
  render: (_node, props, helpers) => renderStateCard({
    klass: "success-state",
    iconName: asString(props.icon, "circle-check"),
    iconClass: "rui-success-state-icon",
    // `asString(x, fallback)` only covers null/undefined; a title bound to a
    // still-empty $variable would otherwise render a bare green tick.
    title: asString(props.title).trim() || "Success",
    description: asString(props.description),
    actions: props.actions,
    role: "status",
    live: "polite",
    helpers
  })
};
const Tour = {
  name: "Tour",
  description: "Product-tour controller — renders the current step's title, description, and a Prev/Next/Skip row. Bind `current` to a `$variable` (0-indexed) to drive the step from your own state; without a binding the component advances itself. Bind `open` to a `$variable` so Skip/Finish can close it (otherwise it closes itself). Pass `steps` as `{title, description, target?}` objects; the optional `target` is a CSS selector that renders alongside the step for designers to reference.",
  props: [
    { name: "steps", type: "object[]" },
    { name: "current", type: "number", description: "0-indexed active step — bind a $variable" },
    { name: "open", type: "boolean", optional: true, description: "Whether the tour is visible — bind a $variable to control it" },
    { name: "onOpenChange", type: "callable", optional: true, aliases: ["onopenchange"], description: "Called with the new boolean open state whenever the component opens or closes." },
    { name: "onComplete", type: "callable", optional: true, description: "Fired when the user reaches Finish" },
    { name: "onSkip", type: "callable", optional: true, description: "Fired when the user bails out via Skip (falls back to `onComplete` when omitted)" },
    { name: "skipLabel", type: "string", optional: true, description: 'Default "Skip"' },
    { name: "backLabel", type: "string", optional: true, description: 'Default "Back"' },
    { name: "nextLabel", type: "string", optional: true, description: 'Default "Next"' },
    { name: "finishLabel", type: "string", optional: true, description: 'Default "Finish"' }
  ],
  render: (node, props, helpers) => {
    const steps = asArray(props.steps).map((entry) => {
      if (!entry || typeof entry !== "object") return { title: asString(entry), description: "", target: "" };
      const r = entry;
      return { title: asString(r.title), description: asString(r.description), target: asString(r.target) };
    });
    const total = steps.length;
    const clampStep = (n) => Math.max(0, Math.min(total - 1, Math.floor(n)));
    const stepRef = node.argMeta?.[1]?.stateRef;
    const openRef = node.argMeta?.[2]?.stateRef;
    const labelId = instanceLabelId(helpers, "rui-tour-label-id", "rui-tour-label");
    const declared = clampStep(asNumber(props.current, 0));
    const stepSlot = helpers.useInstanceState("step", declared);
    const dismissSlot = helpers.useInstanceState("dismissed", false);
    const openProp = props.open === void 0 ? true : asBoolean(props.open);
    const isOpen = openRef ? openProp : openProp && !dismissSlot.get();
    const current = stepRef ? declared : clampStep(stepSlot.get());
    const overlay = el("div", { class: "rui-tour", "data-open": isOpen ? "true" : "false" });
    if (!isOpen || total === 0) return overlay;
    const step = steps[current];
    if (!step) return overlay;
    const close = (origin) => {
      helpers.invoke(props.onOpenChange, false);
      if (openRef) {
        helpers.setState(openRef, false);
        return;
      }
      dismissSlot.set(true);
      const live = origin?.closest(".rui-tour");
      live?.setAttribute("data-open", "false");
    };
    const goTo = (origin, index) => {
      const target = clampStep(index);
      if (stepRef) {
        helpers.setState(stepRef, target);
        return;
      }
      stepSlot.set(target);
      const live = origin?.closest(".rui-tour");
      const card = live?.querySelector(".rui-tour-card");
      if (!live || !card) return;
      const scope = live.getRootNode();
      const hadFocus = card.contains(scope.activeElement);
      const fresh = buildCard(target);
      card.replaceWith(fresh);
      if (hadFocus) fresh.querySelector(".rui-tour-next")?.focus();
    };
    const originOf = (event) => event.currentTarget ?? event.target;
    const buildCard = (index) => {
      const active = steps[index];
      const isLast = index >= total - 1;
      const card = el("div", {
        class: "rui-tour-card",
        role: "dialog",
        "aria-modal": "true",
        "aria-labelledby": labelId,
        tabindex: "-1"
      });
      card.append(el("div", { class: "rui-tour-step" }, [`Step ${index + 1} of ${total}`]));
      card.append(el("h3", { class: "rui-tour-title", id: labelId }, [active.title]));
      if (active.description) card.append(el("p", { class: "rui-tour-description" }, [active.description]));
      if (active.target) card.append(el("div", { class: "rui-tour-target" }, [`Target: ${active.target}`]));
      const footer = el("div", { class: "rui-tour-footer" });
      const skip = el("button", {
        type: "button",
        class: "rui-button rui-tour-skip",
        "data-variant": "ghost"
      }, [asString(props.skipLabel, "Skip")]);
      skip.onclick = (event) => {
        if (typeof props.onSkip === "function") helpers.invoke(props.onSkip, index);
        else helpers.invoke(props.onComplete);
        close(originOf(event));
      };
      const prev = el("button", {
        type: "button",
        class: "rui-button rui-tour-back",
        "data-variant": "secondary",
        disabled: index <= 0 ? "" : null
      }, [asString(props.backLabel, "Back")]);
      prev.onclick = (event) => {
        if (index > 0) goTo(originOf(event), index - 1);
      };
      const next = el("button", {
        type: "button",
        class: "rui-button rui-tour-next",
        "data-variant": "primary"
      }, [isLast ? asString(props.finishLabel, "Finish") : asString(props.nextLabel, "Next")]);
      next.onclick = (event) => {
        const origin = originOf(event);
        if (isLast) {
          helpers.invoke(props.onComplete);
          close(origin);
          return;
        }
        goTo(origin, index + 1);
      };
      footer.append(skip, prev, next);
      card.append(footer);
      return card;
    };
    overlay.append(buildCard(current));
    overlay.onkeydown = dialogKeydownHandler(".rui-tour-card", (origin) => close(origin));
    wireDialogFocus(overlay, ".rui-tour-card", helpers);
    return overlay;
  }
};
const RING_PAD = 6;
const ringStyle = (r) => `top:${r.top}px;left:${r.left}px;width:${r.width}px;height:${r.height}px;`;
function paintSpotlightRing(live, selector, slot) {
  const ring = live.querySelector(".rui-spotlight-ring");
  if (!ring) return;
  let anchor = null;
  try {
    anchor = live.getRootNode().querySelector(selector);
  } catch {
    anchor = null;
  }
  const rect = anchor?.getBoundingClientRect();
  if (!rect || rect.width === 0 && rect.height === 0) {
    slot.set(null);
    live.setAttribute("data-ring", "false");
    ring.style.cssText = "display:none;";
    return;
  }
  const next = {
    top: rect.top - RING_PAD,
    left: rect.left - RING_PAD,
    width: rect.width + RING_PAD * 2,
    height: rect.height + RING_PAD * 2
  };
  slot.set(next);
  live.setAttribute("data-ring", "true");
  ring.style.cssText = ringStyle(next);
}
function trackSpotlightRing(root, selector, slot, helpers) {
  deferToPaint(() => {
    if (!root.isConnected) return;
    paintSpotlightRing(root, selector, slot);
    const onReflow = () => paintSpotlightRing(root, selector, slot);
    window.addEventListener("resize", onReflow, { passive: true });
    window.addEventListener("scroll", onReflow, { capture: true, passive: true });
    helpers.registerDisposer(() => {
      window.removeEventListener("resize", onReflow);
      window.removeEventListener("scroll", onReflow, { capture: true });
    }, "rui-spotlight-ring");
  });
}
const Spotlight = {
  name: "Spotlight",
  description: 'Single-step product highlight — a dimmed full-page overlay with an explainer card, plus a ring around the element named by `target` (a CSS selector). Omit `target` for a centred callout on a plain dim. Use for one-off feature reveals ("Try the new commands menu"). Bind `open` to a `$variable` to control it; the × button, a backdrop click and Escape all dismiss and fire `onClose`.',
  props: [
    { name: "title", type: "string" },
    { name: "open", type: "boolean", optional: true, description: "Whether the spotlight is visible — typically a $variable (default true)" },
    { name: "description", type: "string", optional: true },
    { name: "actions", type: "Node[]", optional: true, aliases: ["action"] },
    { name: "onClose", type: "callable", optional: true, aliases: ["onclose"], description: 'Callable invoked when the spotlight is dismissed — persist "already seen" here' },
    { name: "target", type: "string", optional: true, description: 'CSS selector of the element to ring, e.g. "#commands-button"' }
  ],
  render: (node, props, helpers) => {
    const openRef = node.argMeta?.[1]?.stateRef;
    const dismissSlot = helpers.useInstanceState("dismissed", false);
    const openProp = props.open === void 0 ? true : asBoolean(props.open);
    const isOpen = openRef ? openProp : openProp && !dismissSlot.get();
    const selector = asString(props.target).trim();
    const labelId = instanceLabelId(helpers, "rui-spotlight-label-id", "rui-spotlight-label");
    const ringSlot = helpers.useInstanceState("ring", null);
    const ringRect = selector ? ringSlot.get() : null;
    const overlay = el("div", {
      class: "rui-spotlight",
      "data-open": isOpen ? "true" : "false",
      "data-ring": ringRect ? "true" : "false"
    });
    if (!isOpen) return overlay;
    const close = (origin) => {
      if (openRef) helpers.setState(openRef, false);
      else {
        dismissSlot.set(true);
        origin?.closest(".rui-spotlight")?.setAttribute("data-open", "false");
      }
      helpers.invoke(props.onClose);
    };
    if (selector) {
      overlay.append(el("div", {
        class: "rui-spotlight-ring",
        "aria-hidden": "true",
        style: ringRect ? ringStyle(ringRect) : "display:none;"
      }));
    }
    const card = el("div", {
      class: "rui-spotlight-card",
      role: "dialog",
      "aria-modal": "true",
      "aria-labelledby": labelId,
      tabindex: "-1"
    });
    const head = el("div", { class: "rui-spotlight-head" });
    head.append(el("h3", { class: "rui-spotlight-title", id: labelId }, [asString(props.title)]));
    const closeBtn = el("button", {
      type: "button",
      class: "rui-spotlight-close",
      "aria-label": "Close"
    });
    const closeIcon = renderIcon("xmark");
    if (closeIcon) closeBtn.append(closeIcon);
    else closeBtn.append(document.createTextNode("×"));
    closeBtn.onclick = (event) => close(event.currentTarget ?? event.target);
    head.append(closeBtn);
    card.append(head);
    const description = asString(props.description);
    if (description) card.append(el("p", { class: "rui-spotlight-description" }, [description]));
    const actions = asArray(props.actions);
    if (actions.length > 0) {
      const row = el("div", { class: "rui-spotlight-actions" });
      for (const item of actions) row.append(helpers.renderNode(item));
      card.append(row);
    }
    overlay.append(card);
    overlay.onclick = (event) => {
      if (event.target !== event.currentTarget) return;
      close(event.currentTarget);
    };
    overlay.onkeydown = dialogKeydownHandler(".rui-spotlight-card", (origin) => close(origin));
    wireDialogFocus(overlay, ".rui-spotlight-card", helpers);
    if (selector) trackSpotlightRing(overlay, selector, ringSlot, helpers);
    return overlay;
  }
};
const Sticky = {
  name: "Sticky",
  description: 'Wraps content in a `position: sticky` container so it pins to the top (or bottom / left / right) of the nearest scrollable ancestor. Use for toolbar action rows above tables, in-page navs, status banners, and pinned first/last columns in a horizontally scrolling row. Sets `data-stuck="true"` on itself once pinned, so a CSS hook (a shadow/border) can flag the pinned state.',
  props: [
    { name: "children", aliases: ["child"], type: "Node[]" },
    { name: "side", type: "string", optional: true, enum: ["top", "bottom", "left", "right"] },
    { name: "offset", type: "string", optional: true, description: "CSS offset (default 0)" },
    { name: "zIndex", type: "number", optional: true, description: "Z-index (default 10)" }
  ],
  render: (_node, props, helpers) => {
    const side = asString(props.side, "top");
    const offset = sanitiseCssLength(props.offset, "0");
    const z = Math.max(0, Math.floor(asNumber(props.zIndex, 10)));
    const styles = `position:sticky;${side}:${offset};z-index:${z};`;
    const stuckSlot = helpers.useInstanceState("stuck", false);
    const root = el("div", {
      class: "rui-sticky",
      style: styles,
      "data-stuck": stuckSlot.get() ? "true" : "false"
    });
    for (const child of asArray(props.children)) root.append(helpers.renderNode(child));
    if (typeof IntersectionObserver !== "undefined") {
      const offsetPx = /^(\d+(?:\.\d+)?)px$/.exec(offset)?.[1] ?? "0";
      const edge = `-${Number(offsetPx) + 1}px`;
      const margin = side === "bottom" ? `0px 0px ${edge} 0px` : side === "left" ? `0px 0px 0px ${edge}` : side === "right" ? `0px ${edge} 0px 0px` : `${edge} 0px 0px 0px`;
      deferToPaint(() => {
        if (!root.isConnected) return;
        const io = new IntersectionObserver(
          ([entry]) => {
            if (!entry) return;
            const stuck = entry.intersectionRatio < 1;
            stuckSlot.set(stuck);
            root.setAttribute("data-stuck", stuck ? "true" : "false");
          },
          { threshold: [1], rootMargin: margin }
        );
        io.observe(root);
        helpers.registerDisposer(() => io.disconnect(), "rui-sticky-io");
      });
    }
    return root;
  }
};
const RESIZE_MIN_PCT = 15;
const RESIZE_MAX_PCT = 85;
const clampResizePct = (n) => Math.max(RESIZE_MIN_PCT, Math.min(RESIZE_MAX_PCT, n));
const percentOf = (raw) => {
  const match = /^(\d+(?:\.\d+)?)%$/.exec(raw.trim());
  return match ? Number(match[1]) : null;
};
const ResizablePanels = {
  name: "ResizablePanels",
  description: "Two-pane horizontal split with a draggable divider. The user can drag the divider (or focus it and use the arrow keys / Home / End) to resize the primary pane; `onResize` reports the new percentage so the split can be persisted. Use for code editors, file browsers, master/detail layouts that need user-controllable proportions.",
  props: [
    { name: "primary", type: "Node[]" },
    { name: "secondary", type: "Node[]" },
    { name: "initialPrimaryWidth", type: "string", optional: true, description: "CSS width for the primary pane (default 40%)" },
    { name: "minPrimaryWidth", type: "string", optional: true, description: "Min width of the PRIMARY pane (default 240px)" },
    { name: "minSecondaryWidth", type: "string", optional: true, description: "Min width of the secondary pane (default 0)" },
    { name: "onResize", type: "callable", optional: true, description: "Called with the primary pane's new width percentage when a drag or key press ends" }
  ],
  render: (_node, props, helpers) => {
    const initial = sanitiseCssLength(props.initialPrimaryWidth, "40%");
    const minWidth = sanitiseCssLength(props.minPrimaryWidth, "240px");
    const minSecondary = sanitiseCssLength(props.minSecondaryWidth, "0");
    const pctSlot = helpers.useInstanceState("primaryPct", null);
    const storedPct = pctSlot.get();
    const primaryWidth = storedPct === null ? initial : `${storedPct}%`;
    const root = el("div", {
      class: "rui-resizable-panels",
      style: `--rui-resizable-primary:${primaryWidth};--rui-resizable-min:${minWidth};--rui-resizable-min-secondary:${minSecondary};`
    });
    const primary = el("div", { class: "rui-resizable-panel rui-resizable-panel-primary" });
    for (const child of asArray(props.primary)) primary.append(helpers.renderNode(child));
    const shownPct = storedPct ?? percentOf(initial);
    const divider = el("div", {
      class: "rui-resizable-divider",
      role: "separator",
      "aria-orientation": "vertical",
      "aria-label": "Resize panes",
      "aria-valuemin": String(RESIZE_MIN_PCT),
      "aria-valuemax": String(RESIZE_MAX_PCT),
      // Omitted (rather than guessed) while the width is still a px/calc value.
      "aria-valuenow": shownPct === null ? null : String(Math.round(shownPct)),
      tabindex: "0"
    });
    const secondary = el("div", { class: "rui-resizable-panel rui-resizable-panel-secondary" });
    for (const child of asArray(props.secondary)) secondary.append(helpers.renderNode(child));
    root.append(primary, divider, secondary);
    const currentPct = (live) => {
      const stored = pctSlot.get() ?? percentOf(initial);
      if (stored !== null) return clampResizePct(stored);
      const width = live.getBoundingClientRect().width;
      const pane = live.querySelector(".rui-resizable-panel-primary");
      if (!pane || width === 0) return 40;
      return clampResizePct(pane.getBoundingClientRect().width / width * 100);
    };
    const applyPct = (live, handle, next) => {
      const clamped = clampResizePct(next);
      live.style.setProperty("--rui-resizable-primary", `${clamped}%`);
      handle.setAttribute("aria-valuenow", String(Math.round(clamped)));
      pctSlot.set(clamped);
      return clamped;
    };
    divider.onpointerdown = (event) => {
      const e = event;
      const handle = e.currentTarget ?? e.target;
      const live = handle.closest(".rui-resizable-panels");
      if (!live) return;
      handle.setPointerCapture(e.pointerId);
      const rect = live.getBoundingClientRect();
      let latest = currentPct(live);
      let ended = false;
      const onMove = (moveEvent) => {
        latest = applyPct(live, handle, (moveEvent.clientX - rect.left) / rect.width * 100);
      };
      const stop = (endEvent) => {
        if (ended) return;
        ended = true;
        try {
          handle.releasePointerCapture(endEvent.pointerId);
        } catch {
        }
        handle.removeEventListener("pointermove", onMove);
        handle.removeEventListener("pointerup", stop);
        handle.removeEventListener("pointercancel", stop);
        handle.removeEventListener("lostpointercapture", stop);
        helpers.invoke(props.onResize, latest);
      };
      handle.addEventListener("pointermove", onMove);
      handle.addEventListener("pointerup", stop);
      handle.addEventListener("pointercancel", stop);
      handle.addEventListener("lostpointercapture", stop);
    };
    divider.onkeydown = (event) => {
      const handle = event.currentTarget ?? event.target;
      const live = handle.closest(".rui-resizable-panels");
      if (!live) return;
      const step = event.shiftKey ? 10 : 2;
      let next;
      switch (event.key) {
        case "ArrowLeft":
          next = currentPct(live) - step;
          break;
        case "ArrowRight":
          next = currentPct(live) + step;
          break;
        case "Home":
          next = RESIZE_MIN_PCT;
          break;
        case "End":
          next = RESIZE_MAX_PCT;
          break;
        default:
          return;
      }
      event.preventDefault();
      helpers.invoke(props.onResize, applyPct(live, handle, next));
    };
    return root;
  }
};
const clampMasonryColumns = (n) => Math.max(1, Math.min(6, Math.floor(n)));
const MasonryGrid = {
  name: "MasonryGrid",
  description: "Pinterest-style column grid. Children flow into columns that reflow on viewport changes. Use for galleries, social-style feeds, and mixed-height card walls. `columns` accepts a responsive map like `{base: 1, md: 2, lg: 4}`. Prefer `Grid` when children should share the same height per row.",
  props: [
    { name: "items", type: "Node[]" },
    { name: "columns", type: "number | object", optional: true, description: "Preferred column count 1–6 (default 3). May be a responsive map like `{base: 1, md: 2, lg: 4}`." },
    { name: "gap", type: "string", optional: true, enum: SPACING_TOKENS }
  ],
  render: (_node, props, helpers) => {
    const columns = readResponsiveProp(props.columns);
    const gap = normalizeSpacingToken(props.gap, "md");
    const attrs = {
      class: "rui-masonry-grid",
      "data-gap": gap
    };
    const styleParts = [];
    if (columns.kind === "single") {
      attrs["data-columns"] = String(clampMasonryColumns(asNumber(columns.value, 3)));
    } else {
      attrs["data-responsive-columns"] = "true";
      for (const bp of RESPONSIVE_BREAKPOINTS) {
        const value = columns.values[bp];
        if (value === void 0) continue;
        styleParts.push(`--rui-masonry-columns-${bp}:${clampMasonryColumns(asNumber(value, 3))}`);
      }
    }
    if (styleParts.length > 0) attrs.style = `${styleParts.join(";")};`;
    const root = el("div", attrs);
    for (const item of asArray(props.items)) {
      const cell = el("div", { class: "rui-masonry-item" });
      cell.append(helpers.renderNode(item));
      root.append(cell);
    }
    return root;
  }
};
const Drawer = {
  name: "Drawer",
  description: "Side drawer overlay shown when `open` is true. Pass a `$variable` as `open` to control it. Choose `side` for slide direction (default right) and `width` for a wide detail drawer. `onClose` fires whenever the drawer is dismissed (× button, Escape, or backdrop click — set `closeOnBackdrop: false` to keep a form safe from stray clicks).",
  props: [
    { name: "title", type: "string" },
    { name: "open", type: "boolean", description: "Open/closed state — usually a $variable" },
    { name: "children", aliases: ["child"], type: "Node[]" },
    { name: "side", type: "string", optional: true, enum: ["right", "left", "top", "bottom"] },
    { name: "footer", type: "Node[]", optional: true, description: "Optional footer actions row" },
    { name: "onClose", type: "callable", optional: true, aliases: ["onclose"], description: "Callable invoked when the drawer is dismissed" },
    { name: "width", type: "string", optional: true, aliases: ["size"], description: 'Panel width for left/right drawers, height for top/bottom (default 420px), e.g. "720px" or "60vw"' },
    { name: "closeOnBackdrop", type: "boolean", optional: true, description: "Whether clicking the backdrop dismisses (default true)" }
  ],
  render: (node, props, helpers) => {
    const isOpen = asBoolean(props.open);
    const side = asString(props.side, "right");
    const overlay = el("div", {
      class: "rui-sheet-overlay",
      "data-open": isOpen ? "true" : "false",
      "data-side": side
    });
    const width = sanitiseCssLength(props.width, "");
    const alongBlockAxis = side === "top" || side === "bottom";
    const panelStyle = width ? alongBlockAxis ? `height:${width};max-height:100vh;` : `width:${width};max-width:100vw;` : null;
    const title = asString(props.title);
    const labelId = instanceLabelId(helpers, "rui-drawer-label-id", "rui-drawer-label");
    const panel = el("aside", {
      // `rui-drawer-*` classes shadow the `rui-sheet-*` ones, whose later
      // (Sheet-era) redefinitions stripped this component's body layout and
      // shrank the close glyph.
      class: "rui-sheet rui-drawer",
      role: "dialog",
      "aria-modal": "true",
      "aria-labelledby": title ? labelId : null,
      "aria-label": title ? null : "Drawer",
      "data-side": side,
      style: panelStyle,
      tabindex: "-1"
    });
    const header = el("header", { class: "rui-sheet-header rui-drawer-header" });
    header.append(el("h3", {
      class: "rui-sheet-title rui-drawer-title",
      id: title ? labelId : null
    }, [title]));
    const closeBtn = el("button", {
      type: "button",
      class: "rui-sheet-close rui-drawer-close",
      "aria-label": "Close"
    }, ["×"]);
    const stateName = node.argMeta?.[1]?.stateRef;
    const closeDrawer = () => {
      if (stateName) helpers.setState(stateName, false);
      helpers.invoke(props.onClose);
    };
    closeBtn.onclick = () => closeDrawer();
    const closeOnBackdrop = props.closeOnBackdrop === void 0 ? true : asBoolean(props.closeOnBackdrop);
    overlay.onclick = (event) => {
      if (!closeOnBackdrop) return;
      if (event.target !== event.currentTarget) return;
      closeDrawer();
    };
    header.append(closeBtn);
    panel.append(header);
    const body = el("div", { class: "rui-sheet-body rui-drawer-body" });
    for (const child of asArray(props.children)) body.append(helpers.renderNode(child));
    panel.append(body);
    const footer = asArray(props.footer);
    if (footer.length > 0) {
      const footerRow = el("footer", { class: "rui-sheet-footer rui-drawer-footer" });
      for (const child of footer) footerRow.append(helpers.renderNode(child));
      panel.append(footerRow);
    }
    overlay.append(panel);
    overlay.onkeydown = dialogKeydownHandler(".rui-sheet", () => closeDrawer());
    wireDialogFocus(overlay, ".rui-sheet", helpers);
    return overlay;
  }
};
const TopBar = {
  name: "TopBar",
  description: 'Compact header strip that pairs a title (or breadcrumb) with search and action slots. Use INSTEAD of hand-rolling a `Stack(direction="row")` above a page. For full SaaS shells use `Navbar` (links) or `AppShell` (sidebar + topbar + content).',
  props: [
    { name: "title", type: "string", optional: true },
    { name: "subtitle", type: "string", optional: true },
    { name: "left", type: "Node[]", optional: true, aliases: ["badges"], description: "Leading slot (breadcrumbs, brand, status, badges)" },
    { name: "center", type: "Node[]", optional: true, aliases: ["search"], description: "Centered slot (search bar, segmented control)" },
    { name: "right", type: "Node[]", optional: true, aliases: ["actions"], description: "Trailing slot (actions, avatar)" },
    { name: "sticky", type: "boolean", optional: true }
  ],
  render: (_node, props, helpers) => {
    const root = el("header", {
      class: "rui-topbar",
      "data-sticky": asBoolean(props.sticky) ? "true" : "false"
    });
    const left = el("div", { class: "rui-topbar-side rui-topbar-left" });
    const title = asString(props.title);
    const subtitle = asString(props.subtitle);
    if (title || subtitle) {
      const titleBlock = el("div", { class: "rui-topbar-title-block" });
      if (title) titleBlock.append(el("h2", { class: "rui-topbar-title" }, [title]));
      if (subtitle) titleBlock.append(el("p", { class: "rui-topbar-subtitle" }, [subtitle]));
      left.append(titleBlock);
    }
    for (const child of asArray(props.left)) left.append(helpers.renderNode(child));
    if (left.childNodes.length > 0) root.append(left);
    const center = asArray(props.center);
    if (center.length > 0) {
      const centerWrap = el("div", { class: "rui-topbar-side rui-topbar-center" });
      for (const child of center) centerWrap.append(helpers.renderNode(child));
      root.append(centerWrap);
    }
    const right = el("div", { class: "rui-topbar-side rui-topbar-right" });
    for (const child of asArray(props.right)) right.append(helpers.renderNode(child));
    if (right.childNodes.length > 0) root.append(right);
    return root;
  }
};
const NavLink = {
  name: "NavLink",
  description: 'Anchor that navigates to a route on click and stays in sync with the URL hash. Reflects `data-active="true"` (and `aria-current="page"` for assistive tech) when the current path matches `to` (set `exact=true` to require exact equality instead of prefix matching).',
  props: [
    { name: "label", type: "string", description: "Visible link text." },
    { name: "to", type: "string", description: 'Target route path, e.g. "/about".' },
    {
      name: "variant",
      aliases: ["tone"],
      type: "string",
      optional: true,
      enum: ["default", "primary", "ghost", "pill"],
      description: "Visual variant."
    },
    {
      name: "exact",
      type: "boolean",
      optional: true,
      description: "Match the current path exactly (default: prefix match)."
    },
    {
      name: "icon",
      type: "string",
      optional: true,
      description: "Optional Font Awesome icon name shown before the label."
    },
    {
      name: "prefetch",
      type: "callable",
      optional: true,
      description: "Called once on first hover/focus (warm a `$query` cache for the target route)."
    },
    {
      name: "disabled",
      type: "boolean",
      optional: true,
      description: "Grey out the link and make it unclickable / unfocusable (a route the user cannot enter yet)."
    }
  ],
  render: (_node, props, helpers) => {
    const label = asString(props.label, "");
    const to = asString(props.to, "/");
    const variant = asString(props.variant, "default");
    const exact = asBoolean(props.exact, false);
    const disabled = asBoolean(props.disabled, false);
    const router = helpers.router;
    const currentPath = router.getPath();
    const isActive = (() => {
      if (!currentPath) return false;
      if (exact) return currentPath === to;
      if (to === "/") return currentPath === "/";
      if (currentPath === to) return true;
      return currentPath.startsWith(to + "/");
    })();
    const anchor = el("a", {
      class: "rui-nav-link",
      "data-variant": variant,
      "data-active": isActive ? "true" : "false",
      // Expose the active route to assistive tech, not just CSS.
      "aria-current": isActive ? "page" : null,
      // A disabled link drops its href so it is neither activatable nor a tab
      // stop; `data-disabled` is the styling hook (and the house convention).
      "data-disabled": disabled ? "true" : null,
      "aria-disabled": disabled ? "true" : null,
      tabindex: disabled ? "-1" : null,
      href: disabled ? null : "#" + (to.startsWith("/") ? to : "/" + to)
    });
    const iconNode = renderIcon(props.icon, { className: "rui-nav-link-icon" });
    if (iconNode) anchor.append(iconNode);
    anchor.append(el("span", { class: "rui-nav-link-label" }, [label]));
    if (!disabled && typeof props.prefetch === "function") {
      const warmedSlot = helpers.useInstanceState("rui-navlink-warmed", false);
      const warm = () => {
        if (warmedSlot.get()) return;
        warmedSlot.set(true);
        helpers.invoke(props.prefetch, to);
      };
      anchor.onpointerenter = warm;
      anchor.onfocus = warm;
    }
    if (!disabled) {
      anchor.onclick = (event) => {
        if (event.defaultPrevented) return;
        if (event.button !== 0) return;
        if (event.metaKey || event.ctrlKey || event.shiftKey || event.altKey) return;
        event.preventDefault();
        router.navigate(to);
      };
    }
    return anchor;
  }
};
let uidSeq = 0;
const nextUid = (prefix) => `${prefix}-${(uidSeq += 1).toString(36)}`;
const TOP_LAYER_RESET = [
  ["inset", "auto"],
  ["margin", "0"],
  ["padding", "0"],
  ["border", "0"],
  ["background", "transparent"],
  ["width", "0"],
  ["height", "0"],
  ["overflow", "visible"],
  ["color", "inherit"]
];
function promoteOverlay(hostEl) {
  const api = hostEl;
  if (typeof api.showPopover !== "function") return;
  if (hostEl.hasAttribute("popover")) return;
  hostEl.setAttribute("popover", "manual");
  try {
    api.showPopover();
  } catch {
    hostEl.removeAttribute("popover");
    return;
  }
  for (const [prop2, value] of TOP_LAYER_RESET) hostEl.style.setProperty(prop2, value, "important");
}
function demoteOverlay(hostEl) {
  if (!hostEl || !hostEl.hasAttribute("popover")) return;
  try {
    hostEl.hidePopover?.();
  } catch {
  }
  hostEl.removeAttribute("popover");
  for (const [prop2] of TOP_LAYER_RESET) hostEl.style.removeProperty(prop2);
}
function readChipList(raw) {
  return asArray(raw).map((entry) => {
    if (entry && typeof entry === "object") {
      const obj = entry;
      const value2 = asString(obj.value ?? obj.label);
      return { value: value2, label: asString(obj.label, value2) };
    }
    const value = asString(entry);
    return { value, label: value };
  }).filter((c) => c.label !== "");
}
function readPlainObjects(raw) {
  return asArray(raw).filter(
    (e) => !!e && typeof e === "object" && !Array.isArray(e)
  );
}
function readCommandItems(raw) {
  return asArray(raw).map((entry) => {
    if (entry && typeof entry === "object") {
      const obj = entry;
      const value2 = asString(obj.value ?? obj.label);
      return {
        value: value2,
        label: asString(obj.label, value2),
        group: asString(obj.group) || void 0,
        shortcut: asString(obj.shortcut) || void 0,
        action: obj.action
      };
    }
    const value = asString(entry);
    return { value, label: value };
  }).filter((i) => i.label !== "");
}
function readFields(raw) {
  return asArray(raw).map((entry) => {
    if (entry && typeof entry === "object") {
      const obj = entry;
      const name2 = asString(obj.name ?? obj.label);
      return {
        name: name2,
        label: asString(obj.label, name2),
        type: (asString(obj.type, "text") || "text").trim().toLowerCase(),
        options: obj.options,
        operators: obj.operators,
        placeholder: asString(obj.placeholder) || void 0
      };
    }
    const name = asString(entry);
    return { name, label: name, type: "text" };
  }).filter((f) => f.name !== "");
}
function readGanttTasks(raw) {
  return readPlainObjects(raw).map((t, i) => ({
    id: asString(t.id, `task-${i}`),
    label: asString(t.label ?? t.name, `Task ${i + 1}`),
    start: asString(t.start),
    end: asString(t.end),
    progress: t.progress != null ? asNumber(t.progress, 0) : void 0,
    tone: asString(t.tone ?? t.status) || void 0
  }));
}
function parseDate(value) {
  const t = Date.parse(value);
  return Number.isNaN(t) ? null : t;
}
const DIFF_CELL_BUDGET = 1e6;
function diffLines(left, right) {
  const a = left.split("\n");
  const b = right.split("\n");
  const out = [];
  const n = a.length;
  const m = b.length;
  if (n * m > DIFF_CELL_BUDGET) {
    const max = Math.max(n, m);
    for (let i2 = 0; i2 < max; i2 += 1) {
      const la = a[i2];
      const lb = b[i2];
      if (la === lb) {
        if (la !== void 0) out.push({ type: "same", text: la, leftNo: i2 + 1, rightNo: i2 + 1 });
      } else {
        if (la !== void 0) out.push({ type: "remove", text: la, leftNo: i2 + 1 });
        if (lb !== void 0) out.push({ type: "add", text: lb, rightNo: i2 + 1 });
      }
    }
    return out;
  }
  const width = m + 1;
  const lcs = new Uint32Array((n + 1) * width);
  for (let i2 = n - 1; i2 >= 0; i2 -= 1) {
    for (let j2 = m - 1; j2 >= 0; j2 -= 1) {
      lcs[i2 * width + j2] = a[i2] === b[j2] ? (lcs[(i2 + 1) * width + (j2 + 1)] ?? 0) + 1 : Math.max(lcs[(i2 + 1) * width + j2] ?? 0, lcs[i2 * width + (j2 + 1)] ?? 0);
    }
  }
  let i = 0;
  let j = 0;
  while (i < n && j < m) {
    if (a[i] === b[j]) {
      out.push({ type: "same", text: a[i], leftNo: i + 1, rightNo: j + 1 });
      i += 1;
      j += 1;
    } else if ((lcs[(i + 1) * width + j] ?? 0) >= (lcs[i * width + (j + 1)] ?? 0)) {
      out.push({ type: "remove", text: a[i], leftNo: i + 1 });
      i += 1;
    } else {
      out.push({ type: "add", text: b[j], rightNo: j + 1 });
      j += 1;
    }
  }
  while (i < n) {
    out.push({ type: "remove", text: a[i], leftNo: i + 1 });
    i += 1;
  }
  while (j < m) {
    out.push({ type: "add", text: b[j], rightNo: j + 1 });
    j += 1;
  }
  return out;
}
function collapseContext(rows, context) {
  if (context < 0) return rows;
  const keep = new Array(rows.length).fill(false);
  rows.forEach((row, idx) => {
    if (row.type === "same") return;
    const from = Math.max(0, idx - context);
    const to = Math.min(rows.length - 1, idx + context);
    for (let k = from; k <= to; k += 1) keep[k] = true;
  });
  const out = [];
  let hidden = 0;
  for (let idx = 0; idx < rows.length; idx += 1) {
    if (keep[idx]) {
      if (hidden > 0) {
        out.push({ type: "gap", text: "", count: hidden });
        hidden = 0;
      }
      out.push(rows[idx]);
    } else {
      hidden += 1;
    }
  }
  if (hidden > 0) out.push({ type: "gap", text: "", count: hidden });
  return out;
}
function jsonPreview(value) {
  try {
    return JSON.stringify(value, null, 2);
  } catch {
    return String(value);
  }
}
const IconButton = {
  name: "IconButton",
  description: "Icon-only button with an accessible label. Use for toolbars, table row actions, and compact controls. `active` makes it a toggle (reflected as `aria-pressed`), `loading` shows a spinner and blocks clicks, and `href` renders it as a link instead of a button.",
  props: [
    { name: "icon", type: "string", description: "Font Awesome icon name" },
    { name: "label", type: "string", required: true, description: "Accessible label — the control's only name, so never omit it" },
    { name: "onClick", type: "callable", optional: true, aliases: ["action", "onclick"] },
    { name: "variant", type: "string", optional: true, aliases: ["tone"], enum: BUTTON_VARIANTS, description: "Same vocabulary as `Button` — `primary`/`default`, `secondary`, `outline`, `ghost` (the default here), `link`, `danger`" },
    { name: "size", type: "string", optional: true, enum: BUTTON_SIZES, description: "Size token `xs|sm|md|lg|xl` (default `md`)" },
    { name: "disabled", type: "boolean", optional: true },
    { name: "active", type: "boolean", optional: true, aliases: ["pressed", "selected"], description: "Toggle state — adds `aria-pressed` for assistive tech" },
    { name: "loading", type: "boolean", optional: true, description: "Show a spinner and ignore clicks" },
    { name: "type", type: "string", optional: true, enum: ["button", "submit", "reset"], description: "HTML button type (default `button`)" },
    { name: "href", type: "string", optional: true, description: "Render as a link (sanitised) so middle-click and ctrl-click work" }
  ],
  render: (_node, props, helpers) => {
    const loading = asBoolean(props.loading);
    const disabled = asBoolean(props.disabled) || loading;
    const name = asString(props.label).trim() || asString(props.icon).trim().replace(/[-_]/g, " ");
    const active = props.active === void 0 ? null : asBoolean(props.active);
    const href = asString(props.href).trim();
    const shared = {
      class: "rui-icon-button",
      "data-variant": asString(props.variant, "ghost"),
      "data-size": normaliseButtonSize(props.size),
      "data-active": active === null ? null : active ? "true" : "false",
      "data-loading": loading ? "true" : null,
      // Deliberately no `title`: several AT/browser pairs announce a title that
      // duplicates the accessible name twice. Use `Tooltip` for a hover hint.
      "aria-label": name || null
    };
    const iconNode = loading ? renderIcon("spinner", { className: "rui-icon-button-spinner" }) : renderIcon(props.icon, { className: "rui-icon-button-icon" });
    if (href) {
      const link = el("a", {
        ...shared,
        href: sanitiseHref(href),
        "aria-disabled": disabled ? "true" : null
      });
      if (iconNode) link.append(iconNode);
      link.onclick = (event) => {
        if (disabled) {
          event.preventDefault();
          return;
        }
        helpers.invoke(props.onClick);
      };
      return link;
    }
    const btn = el("button", {
      ...shared,
      type: asString(props.type, "button"),
      "aria-pressed": active === null ? null : active ? "true" : "false",
      "aria-busy": loading ? "true" : null,
      disabled: disabled ? "" : null
    });
    if (iconNode) btn.append(iconNode);
    btn.onclick = () => {
      if (loading) return;
      helpers.invoke(props.onClick);
    };
    return btn;
  }
};
const PALETTE_OPEN_ARG = 1;
const PALETTE_PANEL_SELECTOR = ".rui-command-palette-panel";
const CommandPalette = {
  name: "CommandPalette",
  description: "Cmd-K style searchable command list. Pass `items` as `{label, value, group?, shortcut?, action?}` objects. Arrow keys move the selection, Enter runs it, Escape closes. Bind `open` to a `$variable` (or pass `onClose`) so dismissing the palette is written back — otherwise the next re-render re-opens it.",
  props: [
    { name: "items", type: "any[]" },
    { name: "open", type: "boolean", optional: true, description: "Whether the palette is visible (default true)" },
    { name: "placeholder", type: "string", optional: true },
    { name: "shortcut", type: "string", optional: true, description: "Hint label, e.g. Cmd+K" },
    { name: "onSelect", type: "callable", optional: true, description: "Receives the selected item's `value`" },
    { name: "onClose", type: "callable", optional: true, aliases: ["onOpenChange"], description: "Called with `false` whenever the palette is dismissed" },
    { name: "loading", type: "boolean", optional: true, description: "Show a pending state instead of the empty row" },
    { name: "emptyLabel", type: "string", optional: true, description: 'Text shown when nothing matches (default "No commands found")' },
    { name: "label", type: "string", optional: true, description: 'Accessible name for the dialog (default "Command palette")' },
    { name: "maxResults", type: "number", optional: true, description: "Rows rendered before the list is truncated (default 50)" }
  ],
  render: (node, props, helpers) => {
    const items = readCommandItems(props.items);
    const loading = asBoolean(props.loading);
    const maxResults = Math.max(1, Math.floor(asNumber(props.maxResults, 50)));
    const propProvided = props.open !== void 0;
    const propOpen = propProvided ? asBoolean(props.open) : true;
    const openSlot = helpers.useInstanceState("open", propOpen);
    if (propProvided && openSlot.get() !== propOpen) openSlot.set(propOpen);
    const filterSlot = helpers.useInstanceState("filter", "");
    const activeSlot = helpers.useInstanceState("active", 0);
    const idSlot = helpers.useInstanceState("id", nextUid("rui-cmd"));
    const isOpen = propProvided ? propOpen : openSlot.get();
    const stateRef = node.argMeta?.[PALETTE_OPEN_ARG]?.stateRef;
    const listId = `${idSlot.get()}-list`;
    const optionId = (index) => `${idSlot.get()}-opt-${index}`;
    const host = el("div", { class: "rui-command-palette", "data-open": isOpen ? "true" : "false" });
    wireDialogFocus(host, PALETTE_PANEL_SELECTOR, helpers);
    const closePalette = (origin) => {
      openSlot.set(false);
      filterSlot.set("");
      activeSlot.set(0);
      const liveHost = origin?.closest(".rui-command-palette") ?? null;
      const liveShell = liveHost?.querySelector(PALETTE_PANEL_SELECTOR) ?? null;
      liveHost?.setAttribute("data-open", "false");
      disposeDismissListeners(liveShell ?? void 0);
      demoteOverlay(liveHost);
      if (stateRef) helpers.setState(stateRef, false);
      helpers.invoke(props.onClose, false);
    };
    if (!isOpen) return host;
    const backdrop = el("div", { class: "rui-command-palette-backdrop" });
    const shell = el("div", {
      class: "rui-command-palette-panel",
      role: "dialog",
      "aria-modal": "true",
      "aria-label": asString(props.label, "Command palette")
    });
    const header = el("div", { class: "rui-command-palette-header" });
    const search = el("input", {
      type: "text",
      class: "rui-command-palette-input",
      placeholder: asString(props.placeholder, "Search commands…"),
      value: filterSlot.get(),
      autocomplete: "off",
      // Combobox semantics: the input keeps focus and names the active row
      // through `aria-activedescendant`, which is what makes arrow-key
      // selection audible to a screen reader.
      role: "combobox",
      "aria-expanded": "true",
      "aria-controls": listId,
      "aria-autocomplete": "list",
      "aria-haspopup": "listbox"
    });
    header.append(search);
    const shortcut = asString(props.shortcut);
    if (shortcut) header.append(el("span", { class: "rui-command-palette-shortcut" }, [shortcut]));
    shell.append(header);
    const list = el("div", {
      class: "rui-command-palette-list",
      role: "listbox",
      id: listId,
      "aria-busy": loading ? "true" : null
    });
    const matchesFor = (filter) => {
      const lower = filter.trim().toLowerCase();
      if (lower === "") return items;
      return items.filter((i) => i.label.toLowerCase().includes(lower) || i.value.toLowerCase().includes(lower) || (i.group ?? "").toLowerCase().includes(lower));
    };
    const runItem = (row, item) => {
      helpers.invoke(item.action);
      helpers.invoke(props.onSelect, item.value);
      closePalette(row);
    };
    const paintList = (target, filter) => {
      target.replaceChildren();
      if (loading) {
        const pending = el("div", { class: "rui-command-palette-empty", role: "status" });
        const spin = renderIcon("spinner", { className: "rui-command-palette-spinner" });
        if (spin) pending.append(spin);
        target.append(pending);
        target.setAttribute("aria-busy", "true");
        return;
      }
      target.removeAttribute("aria-busy");
      const matches = matchesFor(filter);
      const shown = matches.slice(0, maxResults);
      const active = Math.max(0, Math.min(activeSlot.get(), shown.length - 1));
      activeSlot.set(active);
      let lastGroup = "";
      shown.forEach((item, index) => {
        if (item.group && item.group !== lastGroup) {
          lastGroup = item.group;
          target.append(el("div", { class: "rui-command-palette-group" }, [lastGroup]));
        }
        const row = el("div", {
          class: "rui-command-palette-item",
          role: "option",
          id: optionId(index),
          "data-value": item.value,
          "data-index": String(index),
          "data-active": index === active ? "true" : null,
          "aria-selected": index === active ? "true" : "false"
        }, [item.label]);
        if (item.shortcut) row.append(el("span", { class: "rui-command-palette-item-kbd" }, [item.shortcut]));
        row.onclick = (event) => {
          event.stopPropagation();
          runItem(event.currentTarget, item);
        };
        target.append(row);
      });
      if (matches.length === 0) {
        target.append(el("div", { class: "rui-command-palette-empty" }, [
          asString(props.emptyLabel, "No commands found")
        ]));
      } else if (matches.length > shown.length) {
        target.append(el("div", { class: "rui-command-palette-footer" }, [
          `${shown.length} / ${matches.length}`
        ]));
      }
    };
    paintList(list, filterSlot.get());
    search.setAttribute("aria-activedescendant", list.children.length > 0 ? optionId(activeSlot.get()) : "");
    shell.append(list);
    backdrop.onclick = (event) => closePalette(event.currentTarget);
    host.append(backdrop, shell);
    const setActive = (input, rows, index) => {
      if (rows.length === 0) return;
      const next = (index % rows.length + rows.length) % rows.length;
      rows.forEach((row, i) => {
        if (i === next) row.setAttribute("data-active", "true");
        else row.removeAttribute("data-active");
        row.setAttribute("aria-selected", i === next ? "true" : "false");
      });
      const target = rows[next];
      activeSlot.set(Number(target.getAttribute("data-index") ?? next));
      input.setAttribute("aria-activedescendant", target.id);
      target.scrollIntoView?.({ block: "nearest" });
    };
    search.oninput = (event) => {
      const target = event.currentTarget;
      const liveList = target.closest(PALETTE_PANEL_SELECTOR)?.querySelector(".rui-command-palette-list");
      filterSlot.set(target.value);
      activeSlot.set(0);
      if (!liveList) return;
      paintList(liveList, target.value);
      const rows = Array.from(liveList.querySelectorAll(".rui-command-palette-item"));
      if (rows.length > 0) setActive(target, rows, 0);
      else target.setAttribute("aria-activedescendant", "");
    };
    search.onkeydown = (event) => {
      const kev = event;
      const input = kev.currentTarget ?? kev.target;
      const liveList = input.closest(PALETTE_PANEL_SELECTOR)?.querySelector(".rui-command-palette-list");
      const rows = liveList ? Array.from(liveList.querySelectorAll(".rui-command-palette-item")) : [];
      if (rows.length === 0) return;
      const current = Math.max(0, rows.findIndex((r) => r.getAttribute("data-active") === "true"));
      if (kev.key === "ArrowDown") {
        kev.preventDefault();
        setActive(input, rows, current + 1);
        return;
      }
      if (kev.key === "ArrowUp") {
        kev.preventDefault();
        setActive(input, rows, current - 1);
        return;
      }
      if (kev.key === "Home") {
        kev.preventDefault();
        setActive(input, rows, 0);
        return;
      }
      if (kev.key === "End") {
        kev.preventDefault();
        setActive(input, rows, rows.length - 1);
        return;
      }
      if (kev.key === "Enter") {
        kev.preventDefault();
        rows[current]?.click();
      }
    };
    host.onkeydown = dialogKeydownHandler(PALETTE_PANEL_SELECTOR, (origin) => closePalette(origin));
    deferToPaint(() => {
      if (!shell.isConnected) return;
      const liveHost = shell.closest(".rui-command-palette");
      if (!liveHost || liveHost.getAttribute("data-open") !== "true") return;
      promoteOverlay(liveHost);
      const handle = installDismissListeners({
        liveRoot: shell,
        key: "command-palette",
        onDismiss: () => closePalette(shell)
      });
      helpers.registerDisposer(() => {
        handle.dispose();
        demoteOverlay(liveHost);
      }, "rui-command-palette");
    });
    return host;
  }
};
const FilterPill = {
  name: "FilterPill",
  description: "Toggleable filter pill — click to switch a filter on/off. Reflects state through `active` (and `aria-pressed` for assistive tech), with an optional leading `icon` and trailing `count`. Use for filter bars; use `FilterChips` to show already-applied filters that can be removed.",
  props: [
    { name: "label", type: "string", positional: true },
    { name: "active", type: "boolean", optional: true, aliases: ["selected", "pressed"] },
    { name: "count", type: "number", optional: true, description: "Optional trailing match count" },
    { name: "icon", type: "string", optional: true },
    { name: "disabled", type: "boolean", optional: true },
    { name: "onToggle", type: "callable", optional: true, aliases: ["onClick", "action"], description: "Receives the next active state" }
  ],
  render: (_node, props, helpers) => {
    const active = asBoolean(props.active);
    const disabled = asBoolean(props.disabled);
    const root = el("button", {
      type: "button",
      class: "rui-filter-pill",
      "data-active": active ? "true" : "false",
      "aria-pressed": active ? "true" : "false",
      disabled: disabled ? true : null
    });
    const iconNode = renderIcon(props.icon, { className: "rui-filter-pill-icon" });
    if (iconNode) root.append(iconNode);
    root.append(el("span", { class: "rui-filter-pill-label" }, [asString(props.label)]));
    if (props.count !== null && props.count !== void 0 && asString(props.count) !== "") {
      root.append(el("span", { class: "rui-filter-pill-count" }, [asString(asNumber(props.count, 0))]));
    }
    if (!disabled) root.onclick = () => helpers.invoke(props.onToggle, !active);
    return root;
  }
};
const FilterChips = {
  name: "FilterChips",
  description: 'Removable filter chips with an optional clear-all control. Set `max` to collapse the tail into a "+N" chip, and `disabled` to freeze the row while a filtered query is in flight.',
  props: [
    { name: "chips", type: "any[]", description: "Array of strings or {label, value} objects" },
    { name: "onRemove", type: "callable", optional: true, description: "Receives the removed chip value as an argument" },
    { name: "onClear", type: "callable", optional: true },
    { name: "clearLabel", type: "string", optional: true, description: 'Text of the clear-all control (default "Clear all")' },
    { name: "max", type: "number", optional: true, description: 'Chips shown before the rest collapse into a "+N" chip' },
    { name: "disabled", type: "boolean", optional: true, description: "Block removal while a request is in flight" }
  ],
  render: (_node, props, helpers) => {
    const chips = readChipList(props.chips);
    const disabled = asBoolean(props.disabled);
    const max = props.max != null ? Math.max(1, Math.floor(asNumber(props.max, 0))) : null;
    const shown = max != null ? chips.slice(0, max) : chips;
    const overflow = chips.length - shown.length;
    const root = el("div", { class: "rui-filter-chips", "data-disabled": disabled ? "true" : null });
    const row = el("div", { class: "rui-filter-chips-row" });
    const status = el("div", {
      class: "rui-filter-chips-status rui-visually-hidden",
      role: "status",
      "aria-live": "polite"
    });
    for (const chip of shown) {
      const pill = el("span", { class: "rui-filter-chip", "data-value": chip.value });
      pill.append(el("span", { class: "rui-filter-chip-label" }, [chip.label]));
      const remove = el("button", {
        type: "button",
        class: "rui-filter-chip-remove",
        "aria-label": `Remove ${chip.label}`,
        disabled: disabled ? "" : null
      });
      const xIcon = renderIcon("xmark", { className: "rui-filter-chip-remove-icon" });
      if (xIcon) remove.append(xIcon);
      if (!disabled) {
        remove.onclick = (event) => {
          const origin = event.currentTarget ?? event.target;
          const liveRoot = origin.closest(".rui-filter-chips");
          const buttons = liveRoot ? Array.from(liveRoot.querySelectorAll(".rui-filter-chip-remove")) : [];
          const at = buttons.indexOf(origin);
          helpers.invoke(props.onRemove, chip.value);
          deferToPaint(() => {
            if (!liveRoot?.isConnected) return;
            const live = liveRoot.querySelector(".rui-filter-chips-status");
            if (live) live.textContent = `${chip.label} removed`;
            const next = Array.from(liveRoot.querySelectorAll(".rui-filter-chip-remove"));
            if (next.length === 0) {
              liveRoot.querySelector(".rui-filter-chips-clear")?.focus?.();
              return;
            }
            const target = next[Math.min(at < 0 ? 0 : at, next.length - 1)] ?? next[0];
            target.focus?.();
          });
        };
      }
      pill.append(remove);
      row.append(pill);
    }
    if (overflow > 0) {
      row.append(el("span", {
        class: "rui-filter-chip",
        "data-overflow": "true"
      }, [`+${overflow}`]));
    }
    root.append(row, status);
    if (chips.length > 0 && typeof props.onClear === "function") {
      const clear = el("button", {
        type: "button",
        class: "rui-filter-chips-clear",
        disabled: disabled ? "" : null
      }, [asString(props.clearLabel, "Clear all")]);
      if (!disabled) clear.onclick = () => helpers.invoke(props.onClear);
      root.append(clear);
    }
    return root;
  }
};
const REPEATER_INPUT_TYPES = /* @__PURE__ */ new Set([
  "text",
  "number",
  "email",
  "tel",
  "url",
  "password",
  "search",
  "color",
  "date",
  "time",
  "datetime-local",
  "month",
  "week"
]);
const REPEATER_ITEMS_ARG = 0;
const FieldRepeater = {
  name: "FieldRepeater",
  description: "Dynamic list of field groups (invoice lines, recipients, key/value pairs). Pass `items` as row objects and `fields` as `{name, label, type?, options?, placeholder?}` definitions — `type` accepts the native input types plus `checkbox`, `textarea` and `select` (with `options`). Bind `items` to a `$variable` for two-way capture, or read edits from `onChange(index, field, value, rows)`.",
  props: [
    { name: "items", type: "any[]" },
    { name: "fields", type: "any[]" },
    { name: "onAdd", type: "callable", optional: true },
    { name: "onRemove", type: "callable", optional: true, description: "Receives the removed row's 0-indexed position" },
    { name: "addLabel", type: "string", optional: true },
    { name: "onChange", type: "callable", optional: true, description: "Receives `(index, fieldName, value, rows)` on every edit" },
    { name: "removeLabel", type: "string", optional: true, description: 'Text of the per-row remove control (default "Remove")' },
    { name: "min", type: "number", optional: true, description: "Rows that can never be removed (default 0)" },
    { name: "max", type: "number", optional: true, description: 'Maximum rows — "Add row" is disabled at the cap' }
  ],
  render: (node, props, helpers) => {
    const rows = readPlainObjects(props.items);
    const fields = readFields(props.fields);
    const itemsRef = node.argMeta?.[REPEATER_ITEMS_ARG]?.stateRef;
    const minRows = Math.max(0, Math.floor(asNumber(props.min, 0)));
    const maxRows = props.max != null ? Math.max(1, Math.floor(asNumber(props.max, 1))) : Infinity;
    const removeText = asString(props.removeLabel, "Remove");
    const root = el("div", { class: "rui-field-repeater" });
    const commitCell = (origin, fieldName, value) => {
      const card = origin.closest(".rui-field-repeater-row");
      const parsed = Number(card?.getAttribute("data-index"));
      const index = Number.isFinite(parsed) ? parsed : -1;
      if (index < 0) return;
      const current = readPlainObjects(props.items);
      const next = current.map((r, i) => i === index ? { ...r, [fieldName]: value } : r);
      if (itemsRef) helpers.setState(itemsRef, next);
      helpers.invoke(props.onChange, index, fieldName, value, next);
    };
    const buildControl = (field, row, index) => {
      const name = `${field.name}-${index}`;
      const cell = row[field.name];
      if (field.type === "textarea") {
        const area = el("textarea", {
          class: "rui-textarea",
          name,
          rows: "2",
          placeholder: field.placeholder ?? null
        });
        area.value = asString(cell);
        area.oninput = (event) => {
          const live = event.currentTarget ?? event.target;
          commitCell(live, field.name, live.value);
        };
        return area;
      }
      if (field.type === "select") {
        const select = el("select", { class: "rui-select", name });
        for (const opt of readChipList(field.options)) {
          select.append(el("option", { value: opt.value }, [opt.label]));
        }
        select.value = asString(cell);
        select.onchange = (event) => {
          const live = event.currentTarget ?? event.target;
          commitCell(live, field.name, live.value);
        };
        return select;
      }
      if (field.type === "checkbox") {
        const box = el("input", {
          class: "rui-checkbox-input",
          type: "checkbox",
          name,
          // Only assert `checked` when the row actually carries a truthy value —
          // an always-present attribute is what morph reads as a deliberate
          // assertion and would fight the user's own clicks.
          checked: asBoolean(cell) ? "" : null
        });
        box.onchange = (event) => {
          const live = event.currentTarget ?? event.target;
          commitCell(live, field.name, live.checked);
        };
        return box;
      }
      const input = el("input", {
        class: "rui-input",
        type: REPEATER_INPUT_TYPES.has(field.type) ? field.type : "text",
        name,
        placeholder: field.placeholder ?? null,
        // `valueAttr`, not `asString`: a row that simply has no value for this
        // field must not assert `value=""`, or morph wipes what the user typed
        // on the next unrelated re-render.
        value: valueAttr(cell)
      });
      input.oninput = (event) => {
        const live = event.currentTarget ?? event.target;
        commitCell(live, field.name, live.value);
      };
      return input;
    };
    rows.forEach((row, index) => {
      const card = el("div", { class: "rui-field-repeater-row", "data-index": String(index) });
      const grid = el("div", { class: "rui-field-repeater-grid" });
      for (const field of fields) {
        const wrap = el("label", { class: "rui-field-repeater-field" });
        wrap.append(el("span", { class: "rui-field-repeater-label" }, [field.label]));
        wrap.append(buildControl(field, row, index));
        grid.append(wrap);
      }
      card.append(grid);
      if (typeof props.onRemove === "function" && rows.length > minRows) {
        const hint = asString(row[fields[0]?.name ?? ""]).trim();
        const remove = el("button", {
          type: "button",
          class: "rui-field-repeater-remove",
          "aria-label": hint ? `${removeText} ${index + 1}: ${hint}` : `${removeText} ${index + 1}`
        }, [removeText]);
        remove.onclick = (event) => {
          const origin = event.currentTarget ?? event.target;
          const card2 = origin.closest(".rui-field-repeater-row");
          const parsed = Number(card2?.getAttribute("data-index"));
          helpers.invoke(props.onRemove, Number.isFinite(parsed) ? parsed : index);
        };
        card.append(remove);
      }
      root.append(card);
    });
    if (typeof props.onAdd === "function") {
      const atCap = rows.length >= maxRows;
      const add = el("button", {
        type: "button",
        class: "rui-field-repeater-add rui-button",
        "data-variant": "secondary",
        disabled: atCap ? "" : null
      }, [asString(props.addLabel, "Add row")]);
      if (!atCap) add.onclick = () => helpers.invoke(props.onAdd);
      root.append(add);
    }
    return root;
  }
};
function renderPlaceholder(value, className, helpers) {
  const box = el("div", { class: className });
  if (value && typeof value === "object") box.append(helpers.renderNode(value));
  else box.append(document.createTextNode(asString(value)));
  return box;
}
const VirtualList = {
  name: "VirtualList",
  description: "Windowed vertical list for large datasets. Pass pre-rendered nodes as `items` or plain row objects plus a `renderItem` lambda (`row => Node`) invoked per row. Every row is boxed at `itemHeight` so the virtualization maths matches the real layout.",
  props: [
    { name: "items", type: "any[]" },
    { name: "itemHeight", type: "number", optional: true, description: "Fixed row height in px (default 40)" },
    { name: "renderItem", type: "callable", optional: true, description: "Lambda `(row, index) => Node` rendered per data row" },
    { name: "height", type: "number", optional: true, description: "Viewport height in px (default 12 rows)" },
    { name: "onItemClick", type: "callable", optional: true, description: "Receives the clicked row and its index" },
    { name: "empty", type: "any", optional: true, description: "Node or text shown when `items` is empty" },
    { name: "loading", type: "boolean", optional: true, description: "Show a pending state instead of the empty state" }
  ],
  render: (_node, props, helpers) => {
    const itemHeight = Math.max(24, asNumber(props.itemHeight, 40));
    const rawItems = asArray(props.items);
    const total = rawItems.length;
    const loading = asBoolean(props.loading);
    const clickable = typeof props.onItemClick === "function";
    const scrollSlot = helpers.useInstanceState("scrollTop", 0);
    const viewport = el("div", { class: "rui-virtual-list" });
    if (total === 0 && (loading || props.empty !== void 0)) {
      const box = loading ? el("div", { class: "rui-virtual-list-empty", role: "status", "aria-busy": "true" }) : renderPlaceholder(props.empty, "rui-virtual-list-empty", helpers);
      if (loading) {
        const spin = renderIcon("spinner", { className: "rui-virtual-list-spinner" });
        if (spin) box.append(spin);
      }
      viewport.append(box);
      return viewport;
    }
    const scrollEl = el("div", { class: "rui-virtual-list-scroller" });
    const spacer = el("div", { class: "rui-virtual-list-spacer" });
    const windowEl = el("div", { class: "rui-virtual-list-window" });
    spacer.style.height = `${total * itemHeight}px`;
    spacer.style.position = "relative";
    windowEl.style.position = "absolute";
    windowEl.style.top = "0";
    windowEl.style.left = "0";
    windowEl.style.right = "0";
    const viewportHeight = props.height != null ? Math.max(itemHeight, asNumber(props.height, 480)) : Math.min(Math.max(total, 1), 12) * itemHeight;
    scrollEl.style.maxHeight = `${viewportHeight}px`;
    scrollEl.style.overflow = "auto";
    const renderRow = (entry, index) => {
      const row = el("div", {
        class: "rui-virtual-list-item",
        style: `height:${itemHeight}px`,
        "data-index": String(index),
        role: clickable ? "button" : null,
        tabindex: clickable ? "0" : null
      });
      if (typeof props.renderItem === "function") {
        let produced;
        try {
          produced = props.renderItem(entry, index);
        } catch (err) {
          console.error("[aktion] VirtualList renderItem threw", err);
          produced = null;
        }
        if (produced != null) row.append(helpers.renderNode(produced));
      } else if (entry && typeof entry === "object") {
        row.append(helpers.renderNode(entry));
      } else {
        row.append(el("span", {}, [asString(entry)]));
      }
      if (clickable) {
        const fire = (origin) => {
          const at = Number(origin.getAttribute("data-index"));
          const idx = Number.isFinite(at) ? at : index;
          helpers.invoke(props.onItemClick, rawItems[idx], idx);
        };
        row.onclick = (event) => fire(event.currentTarget);
        row.onkeydown = (event) => {
          const kev = event;
          if (kev.key !== "Enter" && kev.key !== " ") return;
          kev.preventDefault();
          fire(kev.currentTarget ?? kev.target);
        };
      }
      return row;
    };
    let lastStart = -1;
    const renderSlice = (winNode, scroller, force = false) => {
      const viewHeight = scroller.clientHeight || viewportHeight;
      const visible = Math.ceil(viewHeight / itemHeight) + 2;
      const scrollTop = Number.isFinite(scroller.scrollTop) ? scroller.scrollTop : 0;
      const start = Math.max(0, Math.floor(scrollTop / itemHeight));
      if (!force && start === lastStart) return;
      lastStart = start;
      const end = Math.min(total, start + visible);
      winNode.replaceChildren();
      winNode.style.transform = `translateY(${start * itemHeight}px)`;
      for (let i = start; i < end; i += 1) winNode.append(renderRow(rawItems[i], i));
    };
    scrollEl.onscroll = (ev) => {
      const liveScroller = ev?.currentTarget ?? ev?.target ?? scrollEl;
      const liveWindow = liveScroller.querySelector(".rui-virtual-list-window");
      scrollSlot.set(Number.isFinite(liveScroller.scrollTop) ? liveScroller.scrollTop : 0);
      if (liveWindow) renderSlice(liveWindow, liveScroller);
    };
    const seeded = scrollSlot.get();
    if (seeded > 0) {
      const visible = Math.ceil(viewportHeight / itemHeight) + 2;
      const start = Math.max(0, Math.floor(seeded / itemHeight));
      lastStart = start;
      windowEl.style.transform = `translateY(${start * itemHeight}px)`;
      for (let i = start; i < Math.min(total, start + visible); i += 1) {
        windowEl.append(renderRow(rawItems[i], i));
      }
    } else {
      renderSlice(windowEl, scrollEl, true);
    }
    scrollEl.append(spacer, windowEl);
    viewport.append(scrollEl);
    deferToPaint(() => {
      if (!scrollEl.isConnected) return;
      if (seeded > 0 && scrollEl.scrollTop === 0) scrollEl.scrollTop = seeded;
      if (typeof ResizeObserver === "undefined") return;
      const ro = new ResizeObserver(() => renderSlice(windowEl, scrollEl, true));
      try {
        ro.observe(scrollEl);
      } catch {
        return;
      }
      helpers.registerDisposer(() => ro.disconnect(), "rui-virtual-list-resize");
    });
    return viewport;
  }
};
const VirtualGrid = {
  name: "VirtualGrid",
  description: "Windowed 2-D grid for very large collections (galleries, tiles, thumbnails). Only the visible rows of cells are mounted. Pass pre-rendered nodes (or plain values) as `items`; set `columns`, `itemHeight`, and `gap`. Prefer `minItemWidth` over a fixed `columns` count so the grid reflows on narrow screens (XI.3).",
  props: [
    { name: "items", type: "any[]" },
    { name: "columns", type: "number", optional: true, description: "Cells per row (default 4)" },
    { name: "itemHeight", type: "number", optional: true, description: "Row height in px (default 120)" },
    { name: "gap", type: "number", optional: true, description: "Gap between cells in px (default 8)" },
    { name: "height", type: "number", optional: true, description: "Viewport height in px (default 480)" },
    { name: "minItemWidth", type: "number", optional: true, description: "Minimum cell width in px — the grid auto-fills and reflows instead of forcing `columns`" },
    { name: "onItemClick", type: "callable", optional: true, description: "Receives the clicked item and its index" },
    { name: "empty", type: "any", optional: true, description: "Node or text shown when `items` is empty" },
    { name: "loading", type: "boolean", optional: true, description: "Show a pending state instead of the empty state" }
  ],
  render: (_node, props, helpers) => {
    const columns = Math.max(1, Math.min(12, Math.floor(asNumber(props.columns, 4))));
    const itemHeight = Math.max(24, asNumber(props.itemHeight, 120));
    const gap = Math.max(0, asNumber(props.gap, 8));
    const height = Math.max(120, asNumber(props.height, 480));
    const minItemWidth = props.minItemWidth != null ? Math.max(40, Math.floor(asNumber(props.minItemWidth, 160))) : null;
    const items = asArray(props.items);
    const total = items.length;
    const rowStride = itemHeight + gap;
    const loading = asBoolean(props.loading);
    const clickable = typeof props.onItemClick === "function";
    const scrollSlot = helpers.useInstanceState("scrollTop", 0);
    const viewport = el("div", { class: "rui-virtual-grid" });
    if (total === 0 && (loading || props.empty !== void 0)) {
      const box = loading ? el("div", { class: "rui-virtual-grid-empty", role: "status", "aria-busy": "true" }) : renderPlaceholder(props.empty, "rui-virtual-grid-empty", helpers);
      if (loading) {
        const spin = renderIcon("spinner", { className: "rui-virtual-grid-spinner" });
        if (spin) box.append(spin);
      }
      viewport.append(box);
      return viewport;
    }
    const scrollEl = el("div", { class: "rui-virtual-grid-scroller", style: `max-height:${height}px;overflow:auto` });
    const spacer = el("div", { class: "rui-virtual-grid-spacer", style: "position:relative" });
    const template = minItemWidth != null ? `repeat(auto-fill,minmax(min(${minItemWidth}px,100%),1fr))` : `repeat(${columns},minmax(0,1fr))`;
    const windowEl = el("div", {
      class: "rui-virtual-grid-window",
      style: `position:absolute;left:0;right:0;top:0;display:grid;grid-template-columns:${template};gap:${gap}px`
    });
    const measureColumns = (winNode) => {
      if (minItemWidth == null || typeof getComputedStyle !== "function") return columns;
      const tracks = getComputedStyle(winNode).gridTemplateColumns;
      if (!tracks || tracks === "none") return columns;
      const count = tracks.trim().split(/\s+/).filter(Boolean).length;
      return count > 0 ? count : columns;
    };
    const renderCell = (entry, index) => {
      const cell = el("div", {
        class: "rui-virtual-grid-cell",
        style: `height:${itemHeight}px`,
        "data-index": String(index),
        role: clickable ? "button" : null,
        tabindex: clickable ? "0" : null
      });
      if (entry && typeof entry === "object") cell.append(helpers.renderNode(entry));
      else cell.append(el("span", {}, [asString(entry)]));
      if (clickable) {
        const fire = (origin) => {
          const at = Number(origin.getAttribute("data-index"));
          const idx = Number.isFinite(at) ? at : index;
          helpers.invoke(props.onItemClick, items[idx], idx);
        };
        cell.onclick = (event) => fire(event.currentTarget);
        cell.onkeydown = (event) => {
          const kev = event;
          if (kev.key !== "Enter" && kev.key !== " ") return;
          kev.preventDefault();
          fire(kev.currentTarget ?? kev.target);
        };
      }
      return cell;
    };
    let lastFirstRow = -1;
    const fillWindow = (winNode, scroller, scrollTop, force = false) => {
      const cols = measureColumns(winNode);
      const rowCount = Math.ceil(total / cols);
      const spacerHeight = Math.max(0, rowCount * rowStride - gap);
      const liveSpacer = scroller?.querySelector(".rui-virtual-grid-spacer") ?? spacer;
      liveSpacer.style.height = `${spacerHeight}px`;
      const offset = Number.isFinite(scrollTop) ? scrollTop : 0;
      const firstRow = Math.max(0, Math.floor(offset / rowStride) - 1);
      if (!force && firstRow === lastFirstRow) return;
      lastFirstRow = firstRow;
      const visibleRows = Math.ceil(height / rowStride) + 2;
      const lastRow = Math.min(rowCount, firstRow + visibleRows);
      winNode.replaceChildren();
      winNode.style.transform = `translateY(${firstRow * rowStride}px)`;
      for (let r = firstRow; r < lastRow; r += 1) {
        for (let c = 0; c < cols; c += 1) {
          const idx = r * cols + c;
          if (idx >= total) break;
          winNode.append(renderCell(items[idx], idx));
        }
      }
    };
    scrollEl.onscroll = (ev) => {
      const liveScroller = ev?.currentTarget ?? ev?.target ?? scrollEl;
      const liveWindow = liveScroller.querySelector(".rui-virtual-grid-window");
      scrollSlot.set(Number.isFinite(liveScroller.scrollTop) ? liveScroller.scrollTop : 0);
      if (liveWindow) fillWindow(liveWindow, liveScroller, liveScroller.scrollTop);
    };
    fillWindow(windowEl, null, scrollSlot.get(), true);
    scrollEl.append(spacer, windowEl);
    viewport.append(scrollEl);
    deferToPaint(() => {
      if (!scrollEl.isConnected) return;
      const seeded = scrollSlot.get();
      if (seeded > 0 && scrollEl.scrollTop === 0) scrollEl.scrollTop = seeded;
      if (typeof ResizeObserver === "undefined") return;
      const ro = new ResizeObserver(() => fillWindow(windowEl, scrollEl, scrollEl.scrollTop, true));
      try {
        ro.observe(scrollEl);
      } catch {
        return;
      }
      helpers.registerDisposer(() => ro.disconnect(), "rui-virtual-grid-resize");
    });
    return viewport;
  }
};
const OPERATOR_LABELS = {
  equals: "equals",
  notEquals: "does not equal",
  contains: "contains",
  notContains: "does not contain",
  startsWith: "starts with",
  endsWith: "ends with",
  gt: "greater than",
  gte: "at least",
  lt: "less than",
  lte: "at most",
  before: "before",
  after: "after",
  between: "between",
  in: "is one of",
  notIn: "is not one of",
  isEmpty: "is empty",
  isNotEmpty: "is not empty",
  isTrue: "is true",
  isFalse: "is false"
};
const OPERATORS_BY_TYPE = {
  text: ["equals", "notEquals", "contains", "notContains", "startsWith", "endsWith", "isEmpty", "isNotEmpty"],
  number: ["equals", "notEquals", "gt", "gte", "lt", "lte", "between"],
  date: ["equals", "before", "after", "between"],
  "datetime-local": ["equals", "before", "after", "between"],
  boolean: ["isTrue", "isFalse"],
  enum: ["in", "notIn", "equals", "notEquals"],
  select: ["in", "notIn", "equals", "notEquals"]
};
function readOperators(raw) {
  return asArray(raw).map((entry) => {
    if (entry && typeof entry === "object") {
      const obj = entry;
      const value2 = asString(obj.value ?? obj.op ?? obj.label);
      return {
        value: value2,
        label: asString(obj.label, OPERATOR_LABELS[value2] ?? value2),
        type: asString(obj.type) || void 0
      };
    }
    const value = asString(entry);
    return { value, label: OPERATOR_LABELS[value] ?? value };
  }).filter((o) => o.value !== "");
}
const QUERY_VALUE_TYPES = /* @__PURE__ */ new Set(["number", "date", "time", "datetime-local", "month", "week"]);
const QUERY_VALUE_ARG = 1;
const QueryBuilder = {
  name: "QueryBuilder",
  description: "Visual AND/OR filter builder. Pass `fields` as `{name, label, type?, operators?}` and bind `value` to a rule array of `{field, op, value, combinator?}`. Every rule after the first carries an AND/OR combinator; operators default to a sensible set per field `type` and can be overridden globally (`operators`) or per field.",
  props: [
    { name: "fields", type: "any[]" },
    { name: "value", type: "any[]", optional: true },
    { name: "onChange", type: "callable", optional: true, description: "Receives the next rule array" },
    { name: "operators", type: "any[]", optional: true, description: "Operator tokens or `{value, label, type?}` objects — `type` scopes them to matching fields" },
    { name: "disabled", type: "boolean", optional: true, description: "Freeze the builder while a query is in flight" },
    { name: "maxRules", type: "number", optional: true, description: "Maximum number of rules (default unlimited)" }
  ],
  render: (node, props, helpers) => {
    const fields = readFields(props.fields);
    const disabled = asBoolean(props.disabled);
    const maxRules = props.maxRules != null ? Math.max(1, Math.floor(asNumber(props.maxRules, 1))) : Infinity;
    const globalOperators = readOperators(props.operators);
    const stateRef = node.argMeta?.[QUERY_VALUE_ARG]?.stateRef;
    const incoming = readPlainObjects(props.value);
    const seedSlot = helpers.useInstanceState("seed", "");
    const rulesSlot = helpers.useInstanceState("rules", incoming);
    const seed = jsonPreview(incoming);
    if (seedSlot.get() !== seed) {
      seedSlot.set(seed);
      rulesSlot.set(incoming);
    }
    const root = el("div", { class: "rui-query-builder", "data-disabled": disabled ? "true" : null });
    const operatorsFor = (field) => {
      const own = readOperators(field?.operators);
      if (own.length > 0) return own;
      if (globalOperators.length > 0) {
        const scoped = globalOperators.filter((o) => !o.type || o.type === field?.type);
        if (scoped.length > 0) return scoped;
      }
      const tokens = OPERATORS_BY_TYPE[field?.type ?? "text"] ?? OPERATORS_BY_TYPE.text;
      return tokens.map((t) => ({ value: t, label: OPERATOR_LABELS[t] ?? t }));
    };
    const commit = (origin, next) => {
      rulesSlot.set(next);
      seedSlot.set(jsonPreview(next));
      if (stateRef) helpers.setState(stateRef, next);
      else helpers.invoke(props.onChange, next);
      const liveRoot = origin.closest(".rui-query-builder");
      if (liveRoot) paint(liveRoot, next);
    };
    const paint = (container, current) => {
      container.replaceChildren();
      current.forEach((rule, index) => {
        const row = el("div", { class: "rui-query-builder-row", "data-index": String(index) });
        const position = index + 1;
        if (index > 0) {
          const combinator = el("select", {
            class: "rui-select rui-query-builder-combinator",
            "aria-label": `Combine rule ${position} with`,
            disabled: disabled ? "" : null
          });
          combinator.append(el("option", { value: "and" }, ["AND"]));
          combinator.append(el("option", { value: "or" }, ["OR"]));
          combinator.value = asString(rule.combinator, "and").toLowerCase() === "or" ? "or" : "and";
          if (!disabled) {
            combinator.onchange = (event) => {
              const live = event.currentTarget ?? event.target;
              const next = current.map((r, i) => i === index ? { ...r, combinator: live.value } : r);
              commit(live, next);
            };
          }
          row.append(combinator);
        }
        const activeField = fields.find((f) => f.name === asString(rule.field)) ?? fields[0];
        const fieldSelect = el("select", {
          class: "rui-select rui-query-builder-field",
          "aria-label": `Rule ${position} field`,
          disabled: disabled ? "" : null
        });
        for (const f of fields) fieldSelect.append(el("option", { value: f.name }, [f.label]));
        fieldSelect.value = asString(rule.field ?? activeField?.name);
        const ops = operatorsFor(activeField);
        const opSelect = el("select", {
          class: "rui-select rui-query-builder-op",
          "aria-label": `Rule ${position} operator`,
          disabled: disabled ? "" : null
        }, []);
        for (const op of ops) opSelect.append(el("option", { value: op.value }, [op.label]));
        const currentOp = asString(rule.op, ops[0]?.value ?? "equals");
        opSelect.value = ops.some((o) => o.value === currentOp) ? currentOp : ops[0]?.value ?? "equals";
        const valueInput = el("input", {
          class: "rui-input rui-query-builder-value",
          type: QUERY_VALUE_TYPES.has(activeField?.type ?? "text") ? activeField.type : "text",
          "aria-label": `Rule ${position} value`,
          disabled: disabled ? "" : null,
          value: valueAttr(rule.value)
        });
        if (!disabled) {
          fieldSelect.onchange = (event) => {
            const live = event.currentTarget ?? event.target;
            const nextField = fields.find((f) => f.name === live.value);
            const allowed = operatorsFor(nextField);
            const next = current.map((r, i) => i === index ? {
              ...r,
              field: live.value,
              op: allowed.some((o) => o.value === asString(r.op)) ? r.op : allowed[0]?.value ?? "equals"
            } : r);
            commit(live, next);
          };
          opSelect.onchange = (event) => {
            const live = event.currentTarget ?? event.target;
            const next = current.map((r, i) => i === index ? { ...r, op: live.value } : r);
            commit(live, next);
          };
          valueInput.oninput = (event) => {
            const live = event.currentTarget ?? event.target;
            const next = current.map((r, i) => i === index ? { ...r, value: live.value } : r);
            commit(live, next);
          };
        }
        row.append(fieldSelect, opSelect, valueInput);
        const remove = el("button", {
          type: "button",
          class: "rui-query-builder-remove",
          "aria-label": `Remove rule ${position}`,
          disabled: disabled ? "" : null
        }, ["×"]);
        if (!disabled) {
          remove.onclick = (event) => {
            const live = event.currentTarget ?? event.target;
            const next = current.filter((_, i) => i !== index).map((r, i) => i === 0 ? { ...r, combinator: void 0 } : r);
            commit(live, next);
          };
        }
        row.append(remove);
        container.append(row);
      });
      const atCap = current.length >= maxRules;
      const add = el("button", {
        type: "button",
        class: "rui-query-builder-add",
        disabled: disabled || atCap ? "" : null
      }, ["Add rule"]);
      if (!disabled && !atCap) {
        add.onclick = (event) => {
          const live = event.currentTarget ?? event.target;
          const first = fields[0];
          const next = [...current, {
            field: first?.name ?? "",
            op: operatorsFor(first)[0]?.value ?? "equals",
            value: "",
            ...current.length > 0 ? { combinator: "and" } : {}
          }];
          commit(live, next);
        };
      }
      container.append(add);
    };
    paint(root, rulesSlot.get());
    return root;
  }
};
const DiffViewer = {
  name: "DiffViewer",
  description: "Side-by-side or unified diff of two text blobs, aligned by a line-level LCS diff so an inserted line does not mark the rest of the file as changed. Set `leftTitle`/`rightTitle` to label the columns, `lineNumbers` to number them, and `contextLines` to collapse unchanged runs.",
  props: [
    { name: "left", type: "string" },
    { name: "right", type: "string" },
    { name: "mode", type: "string", optional: true, enum: ["split", "unified"], description: "Default split" },
    { name: "leftTitle", type: "string", optional: true, description: 'Header for the left column (e.g. "Before")' },
    { name: "rightTitle", type: "string", optional: true, description: 'Header for the right column (e.g. "After")' },
    { name: "lineNumbers", type: "boolean", optional: true, description: "Show original line numbers" },
    { name: "contextLines", type: "number", optional: true, description: "Unchanged lines kept around each change (others collapse)" },
    { name: "maxHeight", type: "number", optional: true, description: "Scroller height in px (default 320)" }
  ],
  render: (_node, props) => {
    const left = asString(props.left);
    const right = asString(props.right);
    const mode = asString(props.mode, "split");
    const lineNumbers = asBoolean(props.lineNumbers);
    const context = props.contextLines != null ? Math.max(0, Math.floor(asNumber(props.contextLines, 3))) : -1;
    const maxHeight = Math.max(40, Math.floor(asNumber(props.maxHeight, 320)));
    const rows = collapseContext(diffLines(left, right), context);
    const root = el("div", { class: "rui-diff-viewer", "data-mode": mode });
    const heightStyle = `max-height:${maxHeight}px;overflow:auto`;
    const numberCell = (value) => lineNumbers ? el("span", { class: "rui-diff-line-num", "aria-hidden": "true" }, [value != null ? String(value) : ""]) : null;
    const gapLabel = (count) => `⋯ ${count ?? 0} ⋯`;
    if (mode === "unified") {
      const body = el("pre", { class: "rui-diff-viewer-unified", style: heightStyle });
      for (const row of rows) {
        if (row.type === "gap") {
          body.append(el("div", { class: "rui-diff-line rui-diff-line-gap" }, [gapLabel(row.count)]));
          continue;
        }
        const line = el("div", { class: `rui-diff-line rui-diff-line-${row.type}` });
        const num2 = numberCell(row.type === "add" ? row.rightNo : row.leftNo);
        if (num2) line.append(num2);
        const marker = row.type === "add" ? "+ " : row.type === "remove" ? "- " : "  ";
        line.append(el("span", { class: "rui-diff-line-text" }, [marker + row.text]));
        body.append(line);
      }
      root.append(body);
      return root;
    }
    const panes = el("div", { class: "rui-diff-viewer-panes", style: heightStyle });
    const leftTitle = asString(props.leftTitle);
    const rightTitle = asString(props.rightTitle);
    if (leftTitle || rightTitle) {
      panes.append(el("div", { class: "rui-diff-viewer-title", "data-side": "left" }, [leftTitle]));
      panes.append(el("div", { class: "rui-diff-viewer-title", "data-side": "right" }, [rightTitle]));
    }
    const cell = (side, row, filled) => {
      const classes = ["rui-diff-viewer-cell", "rui-diff-line"];
      if (!filled) classes.push("rui-diff-viewer-filler");
      else if (side === "left" && row.type === "remove") classes.push("rui-diff-line-remove");
      else if (side === "right" && row.type === "add") classes.push("rui-diff-line-add");
      const box = el("div", { class: classes.join(" "), "data-side": side });
      if (!filled) return box;
      const num2 = numberCell(side === "left" ? row.leftNo : row.rightNo);
      if (num2) box.append(num2);
      box.append(el("span", { class: "rui-diff-line-text" }, [row.text]));
      return box;
    };
    for (const row of rows) {
      if (row.type === "gap") {
        panes.append(el("div", {
          class: "rui-diff-line rui-diff-line-gap",
          // Inline so the collapsed marker spans both grid columns without
          // depending on a stylesheet rule.
          style: "grid-column:1/-1"
        }, [gapLabel(row.count)]));
        continue;
      }
      panes.append(cell("left", row, row.type !== "add"));
      panes.append(cell("right", row, row.type !== "remove"));
    }
    root.append(panes);
    return root;
  }
};
const JSON_TREE_MAX_DEPTH = 16;
const JsonTree = {
  name: "JsonTree",
  description: "Expandable JSON tree viewer for objects and arrays. Arrow keys walk the tree, Enter/Space toggles a branch. Cycles render as `[Circular]`, so a normalised store or a parent back-reference is safe to inspect.",
  props: [
    { name: "data", type: "any" },
    { name: "expanded", type: "boolean", optional: true, description: "Expand all nodes (default: first level only)" },
    { name: "expandedDepth", type: "number", optional: true, description: "Levels open by default (default 1)" },
    { name: "maxHeight", type: "number", optional: true, description: "Scroll the tree beyond this height in px" }
  ],
  render: (_node, props, helpers) => {
    const expandAll = asBoolean(props.expanded);
    const defaultDepth = props.expandedDepth != null ? Math.max(0, Math.floor(asNumber(props.expandedDepth, 1))) : expandAll ? JSON_TREE_MAX_DEPTH : 1;
    const maxHeight = props.maxHeight != null ? Math.max(40, Math.floor(asNumber(props.maxHeight, 320))) : null;
    const openSlot = helpers.useInstanceState("open", {});
    const idSlot = helpers.useInstanceState("id", nextUid("rui-json"));
    const treeId = idSlot.get();
    const root = el("div", {
      class: "rui-json-tree",
      role: "tree",
      "aria-label": "JSON",
      style: maxHeight != null ? `max-height:${maxHeight}px;overflow:auto` : null
    });
    const isOpen = (path, depth) => {
      const stored = openSlot.get()[path];
      return stored === void 0 ? depth < defaultDepth : stored;
    };
    const setOpen = (path, value) => {
      openSlot.set({ ...openSlot.get(), [path]: value });
    };
    const entriesOf = (data) => Array.isArray(data) ? data.map((v, i) => [String(i), v]) : Object.entries(data);
    const summaryOf = (data, count) => Array.isArray(data) ? ` Array(${count})` : ` Object(${count})`;
    const childIds = /* @__PURE__ */ new Map();
    const childrenIdFor = (path) => {
      let id = childIds.get(path);
      if (id === void 0) {
        id = `${treeId}-c${childIds.size}`;
        childIds.set(path, id);
      }
      return id;
    };
    const appendChildren = (container, entries, depth, path, ancestors) => {
      for (const [key, value] of entries) {
        container.append(buildNode(value, key, depth + 1, `${path}/${key}`, ancestors));
      }
    };
    function buildNode(data, key, depth, path, ancestors) {
      const node = el("div", {
        class: "rui-json-tree-node",
        role: "none",
        "data-depth": String(depth),
        "data-path": path
      });
      const keyPrefix = key === null ? "" : `${key}: `;
      const leafRow = (text) => {
        const row2 = el("div", {
          class: "rui-json-tree-row",
          role: "treeitem",
          "aria-level": String(depth + 1),
          "data-path": path,
          tabindex: "-1"
        });
        if (keyPrefix) row2.append(el("span", { class: "rui-json-tree-key" }, [keyPrefix]));
        row2.append(el("span", { class: "rui-json-tree-leaf" }, [text]));
        return row2;
      };
      if (data === null || typeof data !== "object") {
        node.append(leafRow(jsonPreview(data)));
        return node;
      }
      if (ancestors.includes(data)) {
        node.append(leafRow("[Circular]"));
        return node;
      }
      if (depth >= JSON_TREE_MAX_DEPTH) {
        node.append(leafRow("…"));
        return node;
      }
      const entries = entriesOf(data);
      const open = isOpen(path, depth);
      const childrenId = childrenIdFor(path);
      const row = el("div", {
        class: "rui-json-tree-row",
        role: "treeitem",
        "aria-level": String(depth + 1),
        "aria-expanded": open ? "true" : "false",
        "aria-controls": childrenId,
        "data-path": path,
        tabindex: depth === 0 ? "0" : "-1"
      });
      const toggle = el("button", {
        type: "button",
        class: "rui-json-tree-toggle",
        tabindex: "-1",
        // The row owns the tree semantics; the button is the visual affordance.
        "aria-hidden": "true"
      }, [
        el("span", { class: "rui-json-tree-toggle-glyph" }, [open ? "▼" : "▶"])
      ]);
      row.append(toggle);
      if (keyPrefix) row.append(el("span", { class: "rui-json-tree-key" }, [keyPrefix]));
      row.append(el("span", { class: "rui-json-tree-summary" }, [summaryOf(data, entries.length)]));
      const children = el("div", {
        class: "rui-json-tree-children",
        role: "group",
        id: childrenId,
        "data-open": open ? "true" : "false"
      });
      const chain = [...ancestors, data];
      if (open) appendChildren(children, entries, depth, path, chain);
      row.onclick = (event) => {
        toggleRow(event.currentTarget ?? event.target, entries, depth, path, chain);
      };
      node.append(row, children);
      return node;
    }
    const toggleRow = (liveRow, entries, depth, path, chain) => {
      const node = liveRow.parentElement;
      const children = node ? Array.from(node.children).find((c) => c.classList.contains("rui-json-tree-children")) : void 0;
      if (!children) return;
      const next = children.getAttribute("data-open") !== "true";
      setOpen(path, next);
      children.setAttribute("data-open", next ? "true" : "false");
      liveRow.setAttribute("aria-expanded", next ? "true" : "false");
      const glyph = liveRow.querySelector(".rui-json-tree-toggle-glyph");
      if (glyph) glyph.textContent = next ? "▼" : "▶";
      if (next && children.childElementCount === 0) {
        appendChildren(children, entries, depth, path, chain);
      } else if (!next) {
        children.replaceChildren();
      }
    };
    const visibleRows = (treeRoot) => Array.from(treeRoot.querySelectorAll(".rui-json-tree-row")).filter((r) => !r.closest('.rui-json-tree-children[data-open="false"]'));
    const focusRow = (rows, index) => {
      if (rows.length === 0) return;
      const clamped = Math.max(0, Math.min(index, rows.length - 1));
      rows.forEach((r, i) => r.setAttribute("tabindex", i === clamped ? "0" : "-1"));
      rows[clamped].focus?.();
    };
    root.onkeydown = (event) => {
      const kev = event;
      const treeRoot = kev.currentTarget ?? kev.target;
      const target = kev.target;
      const active = target?.closest?.(".rui-json-tree-row");
      const rows = visibleRows(treeRoot);
      const at = active ? rows.indexOf(active) : -1;
      switch (kev.key) {
        case "ArrowDown":
          kev.preventDefault();
          focusRow(rows, at + 1);
          return;
        case "ArrowUp":
          kev.preventDefault();
          focusRow(rows, at < 0 ? 0 : at - 1);
          return;
        case "Home":
          kev.preventDefault();
          focusRow(rows, 0);
          return;
        case "End":
          kev.preventDefault();
          focusRow(rows, rows.length - 1);
          return;
        case "Enter":
        case " ":
          if (!active?.hasAttribute("aria-expanded")) return;
          kev.preventDefault();
          active.click();
          return;
        case "ArrowRight":
          kev.preventDefault();
          if (active?.getAttribute("aria-expanded") === "false") active.click();
          else focusRow(rows, at + 1);
          return;
        case "ArrowLeft": {
          kev.preventDefault();
          if (active?.getAttribute("aria-expanded") === "true") {
            active.click();
            return;
          }
          const parentRow = active?.parentElement?.parentElement?.previousElementSibling;
          if (parentRow?.classList.contains("rui-json-tree-row")) focusRow(rows, rows.indexOf(parentRow));
          else focusRow(rows, at - 1);
          return;
        }
      }
    };
    root.append(buildNode(props.data, null, 0, "$", []));
    return root;
  }
};
const GANTT_WARNED = /* @__PURE__ */ new Set();
function warnGanttDates(id, start, end) {
  const key = `${id}|${start}|${end}`;
  if (GANTT_WARNED.has(key)) return;
  GANTT_WARNED.add(key);
  console.warn(
    `[aktion] Gantt task "${id}" has unparseable dates (start: "${start}", end: "${end}") — expected ISO strings like 2026-03-01. The bar was skipped.`
  );
}
const Gantt = {
  name: "Gantt",
  description: "Simple Gantt chart. Pass `tasks` as `{id, label, start, end, progress?, tone?}` with ISO date strings; `progress` accepts either a 0-1 fraction or a 0-100 percentage, and `tone` colours the bar (`success`, `warning`, `danger`, …). Set `axis` for a date scale and `today` for a now marker.",
  props: [
    { name: "tasks", type: "any[]" },
    { name: "startDate", type: "string", optional: true },
    { name: "endDate", type: "string", optional: true },
    { name: "axis", type: "boolean", optional: true, description: "Render a date axis above the bars" },
    { name: "ticks", type: "number", optional: true, description: "Axis tick count (default 5, implies `axis`)" },
    { name: "today", type: "boolean | string", optional: true, description: "Draw a marker line at now, or at the given ISO date" },
    { name: "onTaskClick", type: "callable", optional: true, description: "Receives the clicked task's id" }
  ],
  render: (_node, props, helpers) => {
    const tasks = readGanttTasks(props.tasks);
    const parsed = tasks.map((task) => {
      const start = parseDate(task.start);
      const end = parseDate(task.end);
      if (start === null || end === null) warnGanttDates(task.id, task.start, task.end);
      const lo = start !== null && end !== null ? Math.min(start, end) : start ?? end;
      const hi = start !== null && end !== null ? Math.max(start, end) : end ?? start;
      return { task, start: lo, end: hi, valid: start !== null && end !== null };
    });
    const starts = parsed.filter((p) => p.start !== null).map((p) => p.start);
    const ends = parsed.filter((p) => p.end !== null).map((p) => p.end);
    let rangeStart = props.startDate ? parseDate(asString(props.startDate)) ?? (starts.length ? Math.min(...starts) : Date.now()) : starts.length ? Math.min(...starts) : Date.now();
    let rangeEnd = props.endDate ? parseDate(asString(props.endDate)) ?? (ends.length ? Math.max(...ends) : rangeStart + 864e5) : ends.length ? Math.max(...ends) : rangeStart + 864e5;
    if (rangeEnd < rangeStart) [rangeStart, rangeEnd] = [rangeEnd, rangeStart];
    if (rangeEnd === rangeStart) rangeEnd = rangeStart + 864e5;
    const span = rangeEnd - rangeStart;
    const pctOf = (time) => (time - rangeStart) / span * 100;
    const root = el("div", { class: "rui-gantt" });
    const tickCount = props.ticks != null ? Math.max(2, Math.min(12, Math.floor(asNumber(props.ticks, 5)))) : 5;
    const wantsAxis = asBoolean(props.axis) || props.ticks != null;
    const formatTick = (time) => {
      try {
        return new Date(time).toLocaleDateString(void 0, { month: "short", day: "numeric" });
      } catch {
        return new Date(time).toISOString().slice(0, 10);
      }
    };
    const todayTime = props.today === void 0 || props.today === false ? null : typeof props.today === "string" ? parseDate(props.today) : Date.now();
    if (wantsAxis) {
      const axis = el("div", { class: "rui-gantt-axis", "aria-hidden": "true" });
      axis.append(el("div", { class: "rui-gantt-label" }));
      const ticks = el("div", { class: "rui-gantt-axis-ticks" });
      for (let i = 0; i < tickCount; i += 1) {
        const at = rangeStart + span * i / (tickCount - 1);
        ticks.append(el("div", {
          class: "rui-gantt-tick",
          style: `left:${i / (tickCount - 1) * 100}%`
        }, [formatTick(at)]));
      }
      axis.append(ticks);
      root.append(axis);
    }
    const track = el("div", { class: "rui-gantt-track", role: "list" });
    const clickable = typeof props.onTaskClick === "function";
    for (const entry of parsed) {
      const { task } = entry;
      const progressPct = task.progress == null ? null : task.progress > 0 && task.progress <= 1 ? task.progress * 100 : task.progress;
      const summary = entry.valid ? `${task.label}: ${task.start} → ${task.end}${progressPct != null ? `, ${Math.round(progressPct)}%` : ""}` : `${task.label}: dates unavailable`;
      const row = el("div", {
        class: "rui-gantt-row",
        role: "listitem",
        "data-task-id": task.id,
        "data-invalid": entry.valid ? null : "true",
        "aria-label": summary
      });
      row.append(el("div", { class: "rui-gantt-label" }, [task.label]));
      const barWrap = el("div", { class: "rui-gantt-bars" });
      if (entry.valid && entry.start !== null && entry.end !== null) {
        const rawStart = pctOf(entry.start);
        const rawEnd = pctOf(entry.end);
        if (rawEnd > 0 && rawStart < 100) {
          const startPct = Math.max(0, Math.min(100, rawStart));
          const widthPct = Math.max(Math.min(100 - startPct, rawEnd - startPct), 1);
          const bar = el("div", {
            class: "rui-gantt-bar",
            style: `left:${startPct}%;width:${widthPct}%`,
            "data-tone": task.tone ?? null,
            "aria-hidden": "true"
          });
          if (progressPct != null) {
            bar.append(el("div", {
              class: "rui-gantt-bar-progress",
              style: `width:${Math.min(100, Math.max(0, progressPct))}%`
            }));
          }
          barWrap.append(bar);
        }
      }
      if (todayTime != null) {
        const at = pctOf(todayTime);
        if (at >= 0 && at <= 100) {
          barWrap.append(el("div", { class: "rui-gantt-today", style: `left:${at}%`, "aria-hidden": "true" }));
        }
      }
      row.append(barWrap);
      if (clickable) {
        row.setAttribute("role", "button");
        row.setAttribute("tabindex", "0");
        const fire = (origin) => {
          helpers.invoke(props.onTaskClick, origin.getAttribute("data-task-id") ?? task.id);
        };
        row.onclick = (event) => fire(event.currentTarget);
        row.onkeydown = (event) => {
          const kev = event;
          if (kev.key !== "Enter" && kev.key !== " ") return;
          kev.preventDefault();
          fire(kev.currentTarget ?? kev.target);
        };
      }
      track.append(row);
    }
    root.append(track);
    return root;
  }
};
const TRUNCATE_EXPANDED_ARG = 4;
const Truncate = {
  name: "Truncate",
  description: 'Clamp long text (or a `child` node) with an expand control. The toggle hides itself when the content already fits, and `expanded` + `onToggle` make the state controllable for "expand all" flows.',
  props: [
    { name: "text", type: "string" },
    { name: "maxLines", type: "number", optional: true, description: "Lines before clamping (default 3)" },
    { name: "expandLabel", type: "string", optional: true },
    { name: "collapseLabel", type: "string", optional: true, description: 'Text of the collapse control (default "Show less")' },
    { name: "expanded", type: "boolean", optional: true, description: "Controlled expansion state — bind to a $variable" },
    { name: "onToggle", type: "callable", optional: true, description: "Receives the next expanded state" },
    { name: "child", aliases: ["children"], type: "Node", optional: true, description: "Formatted content to clamp instead of `text`" }
  ],
  render: (node, props, helpers) => {
    const maxLines = Math.max(1, Math.floor(asNumber(props.maxLines, 3)));
    const controlled = props.expanded !== void 0;
    const propExpanded = controlled ? asBoolean(props.expanded) : false;
    const expandedSlot = helpers.useInstanceState("expanded", propExpanded);
    if (controlled && expandedSlot.get() !== propExpanded) expandedSlot.set(propExpanded);
    const overflowSlot = helpers.useInstanceState("overflow", null);
    const idSlot = helpers.useInstanceState("id", nextUid("rui-truncate"));
    const stateRef = node.argMeta?.[TRUNCATE_EXPANDED_ARG]?.stateRef;
    const expanded = controlled ? propExpanded : expandedSlot.get();
    const expandText = asString(props.expandLabel, "Show more");
    const collapseText = asString(props.collapseLabel, "Show less");
    const bodyId = idSlot.get();
    const root = el("div", {
      class: "rui-truncate",
      "data-expanded": expanded ? "true" : "false",
      "data-overflow": overflowSlot.get() === null ? null : String(overflowSlot.get()),
      style: `--rui-truncate-lines:${maxLines}`
    });
    const body = el("p", { class: "rui-truncate-text", id: bodyId });
    if (props.child !== void 0 && props.child !== null) body.append(helpers.renderNode(props.child));
    else body.append(document.createTextNode(asString(props.text)));
    const toggle = el("button", {
      type: "button",
      class: "rui-truncate-toggle",
      "aria-expanded": expanded ? "true" : "false",
      "aria-controls": bodyId,
      // Hidden only once we have measured that the text fits — never on a
      // guess, or a genuinely clamped paragraph loses its only affordance.
      hidden: overflowSlot.get() === false ? "" : null
    }, [expanded ? collapseText : expandText]);
    toggle.onclick = (event) => {
      const liveToggle = event.currentTarget;
      const liveRoot = liveToggle.closest(".rui-truncate");
      if (!liveRoot) return;
      const next = liveRoot.getAttribute("data-expanded") !== "true";
      expandedSlot.set(next);
      liveRoot.setAttribute("data-expanded", next ? "true" : "false");
      liveToggle.setAttribute("aria-expanded", next ? "true" : "false");
      liveToggle.textContent = next ? collapseText : expandText;
      if (stateRef) helpers.setState(stateRef, next);
      helpers.invoke(props.onToggle, next);
    };
    root.append(body, toggle);
    const clampActive = (target) => {
      if (typeof getComputedStyle !== "function") return false;
      const cs = getComputedStyle(target);
      const clamp2 = cs.getPropertyValue("-webkit-line-clamp") || cs.webkitLineClamp || "";
      return clamp2.trim() !== "" && clamp2.trim() !== "none";
    };
    const applyOverflow = (liveRoot) => {
      const liveBody = liveRoot.querySelector(".rui-truncate-text");
      const liveToggle = liveRoot.querySelector(".rui-truncate-toggle");
      if (!liveBody || !liveToggle) return;
      if (liveRoot.getAttribute("data-expanded") === "true") return;
      if (!clampActive(liveBody)) return;
      const overflows = liveBody.scrollHeight - liveBody.clientHeight > 1;
      overflowSlot.set(overflows);
      liveRoot.setAttribute("data-overflow", overflows ? "true" : "false");
      if (overflows) liveToggle.removeAttribute("hidden");
      else liveToggle.setAttribute("hidden", "");
    };
    deferToPaint(() => {
      if (!root.isConnected) return;
      applyOverflow(root);
      if (typeof ResizeObserver === "undefined") return;
      const ro = new ResizeObserver(() => applyOverflow(root));
      try {
        ro.observe(root);
      } catch {
        return;
      }
      helpers.registerDisposer(() => ro.disconnect(), "rui-truncate-resize");
    });
    return root;
  }
};
const INLINE_EDIT_TYPES = /* @__PURE__ */ new Set([
  "text",
  "number",
  "email",
  "tel",
  "url",
  "password",
  "search",
  "color",
  "date",
  "time",
  "datetime-local",
  "month",
  "week"
]);
const InlineEdit = {
  name: "InlineEdit",
  description: 'Click-to-edit inline field. Enter or blur commits (only when the value actually changed), Escape discards. Pass `type: "textarea"` for multi-line, `placeholder` so an empty field still has an affordance, and `error`/`required` to surface a rejected save.',
  props: [
    { name: "value", type: "string" },
    { name: "label", type: "string", optional: true },
    { name: "onSave", type: "callable", optional: true, description: "Receives the committed draft string" },
    { name: "placeholder", type: "string", optional: true, description: "Shown when the value is empty (also names the trigger)" },
    { name: "type", type: "string", optional: true, description: "Input type, or `textarea` for multi-line" },
    { name: "onCancel", type: "callable", optional: true, description: "Called when the edit is discarded with Escape" },
    // `label` is declared above so `InlineEdit(value, label, onSave)` keeps its
    // documented positional order; the rest of the field-shell contract
    // (disabled / hint / error / required / onBlur / onFocus) comes from the
    // shared list so it cannot drift from the other inputs.
    ...FIELD_SHELL_PROPS.filter((p) => p.name !== "label")
  ],
  render: (node, props, helpers) => {
    const editingSlot = helpers.useInstanceState("editing", false);
    const draftSlot = helpers.useInstanceState("draft", asString(props.value));
    const idSlot = helpers.useInstanceState("id", nextUid("rui-inline-edit"));
    const stateRef = node.argMeta?.[0]?.stateRef;
    const disabled = asBoolean(props.disabled);
    const current = asString(props.value);
    const placeholder = asString(props.placeholder);
    const labelText = asString(props.label);
    const inputId = idSlot.get();
    const editing = editingSlot.get() && !disabled;
    const multiline = asString(props.type).trim().toLowerCase() === "textarea";
    const controlName = labelText || placeholder || "Edit";
    const root = el("div", {
      class: "rui-inline-edit",
      "data-editing": editing ? "true" : "false",
      "data-disabled": disabled ? "true" : null
    });
    const displayText = current || placeholder || "—";
    const display = el("button", {
      type: "button",
      class: "rui-inline-edit-display",
      id: `${inputId}-display`,
      "data-empty": current === "" ? "true" : null,
      disabled: disabled ? "" : null,
      "aria-label": labelText ? `${labelText}: ${displayText}` : null
    }, [displayText]);
    const controlAttrs = {
      class: "rui-inline-edit-input rui-input",
      id: inputId,
      name: inputId,
      placeholder: placeholder || null,
      "aria-label": controlName,
      required: asBoolean(props.required) ? "" : null,
      "aria-invalid": asString(props.error) ? "true" : null,
      disabled: disabled ? "" : null
    };
    let input;
    if (multiline) {
      const area = el("textarea", { ...controlAttrs, rows: "3" });
      area.value = editing ? draftSlot.get() : current;
      input = area;
    } else {
      const type = asString(props.type, "text").trim().toLowerCase();
      input = el("input", {
        ...controlAttrs,
        type: INLINE_EDIT_TYPES.has(type) ? type : "text",
        // `valueAttr` keeps "unset" distinct from "empty" for the morph
        // reconciler; while editing the draft is the asserted value.
        value: editing ? draftSlot.get() : valueAttr(props.value)
      });
    }
    const resolveLive = (origin) => {
      const liveRoot = origin.closest(".rui-inline-edit");
      const liveInput = liveRoot?.querySelector(".rui-inline-edit-input");
      if (!liveRoot || !liveInput) return null;
      return { root: liveRoot, input: liveInput };
    };
    const commit = (origin) => {
      if (!editingSlot.get()) return;
      const live = resolveLive(origin);
      editingSlot.set(false);
      live?.root.setAttribute("data-editing", "false");
      const draft = draftSlot.get();
      if (draft === current) return;
      if (stateRef) helpers.setState(stateRef, draft);
      helpers.invoke(props.onSave, draft);
    };
    const cancel = (origin) => {
      if (!editingSlot.get()) return;
      const live = resolveLive(origin);
      draftSlot.set(current);
      editingSlot.set(false);
      if (live) {
        live.root.setAttribute("data-editing", "false");
        live.input.value = current;
      }
      helpers.invoke(props.onCancel);
    };
    if (!disabled) {
      display.onclick = (event) => {
        const live = resolveLive(event.currentTarget);
        draftSlot.set(current);
        editingSlot.set(true);
        if (live) {
          live.root.setAttribute("data-editing", "true");
          live.input.value = current;
          setTimeout(() => live.input.focus(), 0);
        }
      };
      input.oninput = (event) => {
        const live = event.currentTarget ?? event.target;
        draftSlot.set(live.value);
      };
      input.onkeydown = (event) => {
        const kev = event;
        const origin = kev.currentTarget ?? kev.target;
        if (kev.key === "Enter" && (!multiline || kev.metaKey || kev.ctrlKey)) {
          kev.preventDefault();
          commit(origin);
          return;
        }
        if (kev.key === "Escape") {
          kev.preventDefault();
          cancel(origin);
        }
      };
      input.onblur = (event) => {
        const origin = event.currentTarget ?? event.target;
        helpers.invoke(props.onBlur, origin.value);
        commit(origin);
      };
      input.onfocus = (event) => {
        const origin = event.currentTarget ?? event.target;
        helpers.invoke(props.onFocus, origin.value);
      };
    }
    root.append(display, input);
    return withFieldShell(root, props);
  }
};
const BELL_PANEL_SELECTOR = ".rui-notification-bell-panel";
const BELL_TRIGGER_SELECTOR = ".rui-notification-bell-trigger";
const bellFloatingOpts = (align) => ({
  side: "bottom",
  align: align === "left" ? "start" : "end",
  layer: "dropdown"
});
const NotificationBell = {
  name: "NotificationBell",
  description: "Bell icon with unread count badge and dropdown notification list. Pass `items` as `{title, message?, time?, href?, unread?}` objects; `onItemClick` receives the clicked item and `onMarkAllRead` renders a footer control. Arrow keys walk the list, Escape closes.",
  props: [
    { name: "count", type: "number", optional: true },
    { name: "items", type: "any[]", optional: true, description: "{title, message?, time?, href?, unread?} objects" },
    { name: "onOpen", type: "callable", optional: true },
    { name: "onItemClick", type: "callable", optional: true, description: "Receives the clicked notification object and its index" },
    { name: "onMarkAllRead", type: "callable", optional: true, description: 'Renders a "mark all read" footer control' },
    { name: "align", type: "string", optional: true, enum: ["left", "right"], description: "Which edge the panel aligns to (default right)" },
    { name: "loading", type: "boolean", optional: true, description: "Show a pending state instead of the empty message" },
    { name: "emptyLabel", type: "string", optional: true, description: 'Text shown with no notifications (default "No notifications")' },
    { name: "label", type: "string", optional: true, description: 'Accessible name for the bell (default "Notifications")' },
    { name: "markAllLabel", type: "string", optional: true, description: "Text of the mark-all-read control" },
    { name: "maxCount", type: "number", optional: true, description: 'Badge cap before it renders as "N+" (default 99)' }
  ],
  render: (_node, props, helpers) => {
    const count = Math.max(0, Math.floor(asNumber(props.count, 0)));
    const maxCount = Math.max(1, Math.floor(asNumber(props.maxCount, 99)));
    const items = readPlainObjects(props.items);
    const loading = asBoolean(props.loading);
    const align = asString(props.align, "right");
    const labelText = asString(props.label, "Notifications");
    const openSlot = helpers.useInstanceState("open", false);
    const isOpen = openSlot.get();
    const clickable = typeof props.onItemClick === "function";
    const root = el("div", {
      class: "rui-notification-bell",
      "data-open": isOpen ? "true" : "false",
      "data-align": align === "left" ? "left" : "right"
    });
    const trigger = el("button", {
      type: "button",
      class: "rui-notification-bell-trigger",
      "aria-expanded": isOpen ? "true" : "false",
      // The panel is a menu, so `aria-haspopup` must say so.
      "aria-haspopup": "menu",
      // The icon is `aria-hidden`, so without this the trigger announced as a
      // bare "button" with no hint of what it opens or how many are unread.
      "aria-label": count > 0 ? `${labelText}, ${count} unread` : labelText
    });
    const bell = renderIcon("bell", { className: "rui-notification-bell-icon" });
    if (bell) trigger.append(bell);
    if (count > 0) {
      trigger.append(el("span", {
        class: "rui-notification-bell-badge",
        // The count is already in the trigger's name; announcing the badge too
        // would double it.
        "aria-hidden": "true"
      }, [count > maxCount ? `${maxCount}+` : String(count)]));
    }
    root.append(trigger);
    root.append(el("div", {
      class: "rui-notification-bell-status rui-visually-hidden",
      role: "status",
      "aria-live": "polite"
    }, [count > 0 ? `${labelText}: ${count}` : ""]));
    const panel = el("div", {
      class: "rui-notification-bell-panel",
      role: "menu",
      "aria-label": labelText,
      "aria-busy": loading ? "true" : null
    });
    if (loading) {
      const pending = el("div", { class: "rui-notification-bell-empty", role: "status" });
      const spin = renderIcon("spinner", { className: "rui-notification-bell-spinner" });
      if (spin) pending.append(spin);
      panel.append(pending);
    } else if (items.length === 0) {
      panel.append(el("div", { class: "rui-notification-bell-empty" }, [
        asString(props.emptyLabel, "No notifications")
      ]));
    } else {
      items.forEach((item, index) => {
        const href = asString(item.href).trim();
        const unread = asBoolean(item.unread);
        const shared = {
          class: "rui-notification-bell-item",
          role: "menuitem",
          tabindex: "-1",
          "data-index": String(index),
          "data-unread": unread ? "true" : null
        };
        const row = href ? el("a", { ...shared, href: sanitiseHref(href) }) : el(clickable ? "button" : "div", { ...shared, ...clickable ? { type: "button" } : {} });
        row.append(el("div", { class: "rui-notification-bell-item-title" }, [asString(item.title)]));
        const msg = asString(item.message);
        if (msg) row.append(el("div", { class: "rui-notification-bell-item-message" }, [msg]));
        const time = asString(item.time);
        if (time) row.append(el("div", { class: "rui-notification-bell-item-time" }, [time]));
        if (unread) row.append(el("span", { class: "rui-visually-hidden" }, ["unread"]));
        if (clickable || href) {
          row.onclick = (event) => {
            const origin = event.currentTarget ?? event.target;
            const at = Number(origin.getAttribute("data-index"));
            const idx = Number.isFinite(at) ? at : index;
            helpers.invoke(props.onItemClick, items[idx], idx);
          };
        }
        panel.append(row);
      });
    }
    if (typeof props.onMarkAllRead === "function") {
      const footer = el("div", { class: "rui-notification-bell-footer" });
      const markAll = el("button", {
        type: "button",
        class: "rui-notification-bell-mark",
        role: "menuitem",
        tabindex: "-1"
      }, [asString(props.markAllLabel, "Mark all read")]);
      markAll.onclick = () => helpers.invoke(props.onMarkAllRead);
      footer.append(markAll);
      panel.append(footer);
    }
    const focusItem = (livePanel, index) => {
      const rows = Array.from(livePanel.querySelectorAll('[role="menuitem"]'));
      if (rows.length === 0) return;
      const next = (index % rows.length + rows.length) % rows.length;
      rows.forEach((r, i) => r.setAttribute("tabindex", i === next ? "0" : "-1"));
      rows[next].focus?.();
    };
    panel.onkeydown = (event) => {
      const kev = event;
      const livePanel = kev.currentTarget ?? kev.target;
      const rows = Array.from(livePanel.querySelectorAll('[role="menuitem"]'));
      const at = rows.indexOf(kev.target?.closest?.('[role="menuitem"]'));
      if (kev.key === "ArrowDown") {
        kev.preventDefault();
        focusItem(livePanel, at + 1);
        return;
      }
      if (kev.key === "ArrowUp") {
        kev.preventDefault();
        focusItem(livePanel, at < 0 ? rows.length - 1 : at - 1);
        return;
      }
      if (kev.key === "Home") {
        kev.preventDefault();
        focusItem(livePanel, 0);
        return;
      }
      if (kev.key === "End") {
        kev.preventDefault();
        focusItem(livePanel, rows.length - 1);
        return;
      }
      if (kev.key !== "Escape") return;
      const liveRoot = livePanel.closest(".rui-notification-bell");
      if (!liveRoot) return;
      kev.preventDefault();
      openSlot.set(false);
      liveRoot.setAttribute("data-open", "false");
      liveRoot.querySelector(BELL_TRIGGER_SELECTOR)?.setAttribute("aria-expanded", "false");
      closeFloating(liveRoot.querySelector(BELL_PANEL_SELECTOR));
      disposeDismissListeners(liveRoot);
      liveRoot.querySelector(BELL_TRIGGER_SELECTOR)?.focus?.();
    };
    root.append(panel);
    const openBell = (liveTrigger, next) => {
      const liveRoot = liveTrigger.closest(".rui-notification-bell");
      if (!liveRoot) return;
      openSlot.set(next);
      liveRoot.setAttribute("data-open", next ? "true" : "false");
      liveTrigger.setAttribute("aria-expanded", next ? "true" : "false");
      syncFloatingPanel(
        liveRoot,
        next,
        BELL_PANEL_SELECTOR,
        BELL_TRIGGER_SELECTOR,
        bellFloatingOpts(align)
      );
      if (!next) {
        disposeDismissListeners(liveRoot);
        return;
      }
      helpers.invoke(props.onOpen);
      installDismissListeners({
        liveRoot,
        key: "notification-bell",
        onDismiss: () => {
          openSlot.set(false);
          liveRoot.setAttribute("data-open", "false");
          liveTrigger.setAttribute("aria-expanded", "false");
          closeFloating(liveRoot.querySelector(BELL_PANEL_SELECTOR));
        }
      });
    };
    trigger.onclick = (event) => {
      event.stopPropagation();
      openBell(event.currentTarget, !openSlot.get());
    };
    trigger.onkeydown = (event) => {
      const kev = event;
      if (kev.key !== "ArrowDown" && kev.key !== "ArrowUp") return;
      kev.preventDefault();
      const liveTrigger = kev.currentTarget ?? kev.target;
      if (!openSlot.get()) openBell(liveTrigger, true);
      const livePanel = liveTrigger.closest(".rui-notification-bell")?.querySelector(BELL_PANEL_SELECTOR);
      if (livePanel) focusItem(livePanel, kev.key === "ArrowDown" ? 0 : -1);
    };
    if (isOpen) {
      deferToPaint(() => {
        if (!root.isConnected) return;
        if (root.getAttribute("data-open") !== "true") return;
        syncFloatingPanel(
          root,
          true,
          BELL_PANEL_SELECTOR,
          BELL_TRIGGER_SELECTOR,
          bellFloatingOpts(align)
        );
      });
    }
    return root;
  }
};
function normalisePath(raw) {
  if (!raw) return "/";
  let value = String(raw);
  if (value.startsWith("#")) value = value.slice(1);
  const queryAt = value.indexOf("?");
  if (queryAt >= 0) value = value.slice(0, queryAt);
  value = value.replace(/\/{2,}/g, "/");
  if (!value || value === "/") return "/";
  if (!value.startsWith("/")) value = "/" + value;
  if (value.length > 1 && value.endsWith("/")) value = value.slice(0, -1);
  return value;
}
const EVENT_PROPS = [
  // Pointer / click
  "onclick",
  "ondblclick",
  "oncontextmenu",
  "onmousedown",
  "onmouseup",
  "onmousemove",
  "onmouseenter",
  "onmouseleave",
  "onmouseover",
  "onmouseout",
  "onpointerdown",
  "onpointerup",
  "onpointermove",
  "onpointerenter",
  "onpointerleave",
  "onpointercancel",
  // Scroll / wheel
  "onscroll",
  "onwheel",
  // Drag & drop
  "ondrag",
  "ondragstart",
  "ondragend",
  "ondragenter",
  "ondragleave",
  "ondragover",
  "ondrop",
  // Touch
  "ontouchstart",
  "ontouchmove",
  "ontouchend",
  "ontouchcancel",
  // Form / input
  "oninput",
  "onchange",
  "onsubmit",
  "onreset",
  "oninvalid",
  "ontoggle",
  // Keyboard
  "onkeydown",
  "onkeyup",
  "onkeypress",
  // Focus
  "onfocus",
  "onblur",
  // Animation / transition lifecycle (self-cleaning decorations, exit hooks)
  "onanimationend",
  "ontransitionend",
  // Media / misc
  "onerror",
  // Media playback and resource loading. These were missing, so a handler
  // assigned by AudioPlayer / VideoPlayer / Image / Gallery / FileUpload was
  // dropped the first time the reconciler kept its node — the player stopped
  // reflecting play/pause state, and an image's load/decode hook never fired
  // again. Diffed the whole list against every `.onX =` assignment in
  // src/library/components to find them.
  "onload",
  "onplay",
  "onpause",
  "onended"
];
function morphChildren(container, newRoot) {
  const newChildren = newRoot.nodeType === Node.DOCUMENT_FRAGMENT_NODE ? Array.from(newRoot.childNodes) : [newRoot];
  reconcileChildren(container, newChildren);
}
function morphNode(oldNode, newNode) {
  if (oldNode === newNode) return oldNode;
  if (oldNode.nodeType !== newNode.nodeType) {
    return replaceNode(oldNode, newNode);
  }
  if (oldNode.nodeType === Node.TEXT_NODE) {
    if (oldNode.textContent !== newNode.textContent) {
      oldNode.textContent = newNode.textContent;
    }
    return oldNode;
  }
  if (oldNode.nodeType !== Node.ELEMENT_NODE) {
    return oldNode;
  }
  const oldEl = oldNode;
  const newEl = newNode;
  if (oldEl.tagName !== newEl.tagName) {
    return replaceNode(oldEl, newEl);
  }
  patchElement(oldEl, newEl);
  return oldEl;
}
function replaceNode(oldNode, newNode) {
  oldNode.parentNode?.replaceChild(newNode, oldNode);
  return newNode;
}
function patchElement(oldEl, newEl) {
  if (oldEl.hasAttribute("data-rui-preserve") || newEl.hasAttribute("data-rui-preserve")) {
    syncAttributesAdditive(oldEl, newEl);
    syncEventHandlers(oldEl, newEl);
    return;
  }
  syncAttributes(oldEl, newEl);
  syncEventHandlers(oldEl, newEl);
  const desiredSelectValue = oldEl instanceof HTMLSelectElement && newEl instanceof HTMLSelectElement ? newEl.value : null;
  reconcileChildren(oldEl, Array.from(newEl.childNodes));
  syncFormState(oldEl, newEl, desiredSelectValue);
}
const FLOATING_OWNED_ATTRS = /* @__PURE__ */ new Set(["popover", "style", "data-floating-side"]);
const isFloatingPromoted = (el2) => el2.hasAttribute("data-floating-side");
function syncAttributesAdditive(oldEl, newEl) {
  const promoted = isFloatingPromoted(oldEl);
  const newAttrs = newEl.attributes;
  for (let i = 0; i < newAttrs.length; i += 1) {
    const attr = newAttrs[i];
    if (promoted && FLOATING_OWNED_ATTRS.has(attr.name)) continue;
    if (oldEl.getAttribute(attr.name) !== attr.value) {
      oldEl.setAttribute(attr.name, attr.value);
    }
  }
}
function syncAttributes(oldEl, newEl) {
  const promoted = isFloatingPromoted(oldEl);
  const oldAttrs = oldEl.attributes;
  for (let i = oldAttrs.length - 1; i >= 0; i -= 1) {
    const attr = oldAttrs[i];
    if (newEl.hasAttribute(attr.name)) continue;
    if (promoted && FLOATING_OWNED_ATTRS.has(attr.name)) continue;
    if (attr.name === "open" && oldEl.tagName === "DETAILS") continue;
    if (oldEl.tagName === "CANVAS" && (attr.name === "width" || attr.name === "height")) continue;
    oldEl.removeAttribute(attr.name);
  }
  const newAttrs = newEl.attributes;
  for (let i = 0; i < newAttrs.length; i += 1) {
    const attr = newAttrs[i];
    if (promoted && FLOATING_OWNED_ATTRS.has(attr.name)) continue;
    if (oldEl.getAttribute(attr.name) !== attr.value) {
      oldEl.setAttribute(attr.name, attr.value);
    }
  }
}
function syncEventHandlers(oldEl, newEl) {
  for (const key of EVENT_PROPS) {
    const fresh = newEl[key];
    const current = oldEl[key];
    if (fresh === current) continue;
    oldEl[key] = fresh ?? null;
  }
}
function syncFormState(oldEl, newEl, desiredSelectValue) {
  if (oldEl instanceof HTMLInputElement && newEl instanceof HTMLInputElement) {
    syncInput(oldEl, newEl);
    return;
  }
  if (oldEl instanceof HTMLTextAreaElement && newEl instanceof HTMLTextAreaElement) {
    syncTextArea(oldEl, newEl);
    return;
  }
  if (oldEl instanceof HTMLSelectElement && newEl instanceof HTMLSelectElement) {
    const desired = desiredSelectValue ?? newEl.value;
    if (oldEl.value !== desired) {
      oldEl.value = desired;
    }
    return;
  }
}
function syncInput(oldEl, newEl) {
  if (oldEl.type === "file") return;
  if (oldEl.type === "checkbox" || oldEl.type === "radio") {
    if (!newEl.hasAttribute("checked") && !newEl.checked) return;
    const desired2 = newEl.hasAttribute("checked") || newEl.checked;
    if (oldEl.checked !== desired2) {
      oldEl.checked = desired2;
    }
    return;
  }
  if (!newEl.hasAttribute("value")) return;
  const desired = newEl.getAttribute("value") ?? "";
  if (oldEl.value !== desired) {
    assignValuePreservingCaret(oldEl, desired);
  }
}
function syncTextArea(oldEl, newEl) {
  const desired = newEl.value ?? newEl.textContent ?? "";
  if (oldEl.value !== desired) {
    assignValuePreservingCaret(oldEl, desired);
  }
}
function assignValuePreservingCaret(el2, desired) {
  if (!isFocused(el2)) {
    el2.value = desired;
    return;
  }
  let start = null;
  let end = null;
  try {
    start = el2.selectionStart;
    end = el2.selectionEnd;
  } catch {
  }
  const prevLen = el2.value.length;
  el2.value = desired;
  if (start == null || end == null) return;
  const atEnd = start >= prevLen && end >= prevLen;
  const nextStart = atEnd ? desired.length : Math.min(start, desired.length);
  const nextEnd = atEnd ? desired.length : Math.min(end, desired.length);
  try {
    el2.setSelectionRange(nextStart, nextEnd);
  } catch {
  }
}
function isFocused(el2) {
  const root = el2.getRootNode();
  if (root instanceof ShadowRoot || root instanceof Document) {
    return root.activeElement === el2;
  }
  return el2.ownerDocument?.activeElement === el2;
}
function reconcileChildren(parent, newChildren) {
  const keyedOld = /* @__PURE__ */ new Map();
  for (const child of Array.from(parent.childNodes)) {
    const key = keyFor(child);
    if (key) keyedOld.set(key, child);
  }
  let cursorIndex = 0;
  for (const newChild of newChildren) {
    const live = parent.childNodes[cursorIndex] ?? null;
    const newKey = keyFor(newChild);
    if (newKey && keyedOld.has(newKey)) {
      const matched = keyedOld.get(newKey);
      keyedOld.delete(newKey);
      if (matched !== live) {
        parent.insertBefore(matched, live);
      }
      morphNode(matched, newChild);
      cursorIndex += 1;
      continue;
    }
    if (!live) {
      parent.appendChild(newChild);
      cursorIndex += 1;
      continue;
    }
    const liveKey = keyFor(live);
    if (liveKey && (!newKey || liveKey !== newKey)) {
      parent.insertBefore(newChild, live);
      cursorIndex += 1;
      continue;
    }
    morphNode(live, newChild);
    cursorIndex += 1;
  }
  while (parent.childNodes.length > newChildren.length) {
    const surplus = parent.childNodes[parent.childNodes.length - 1];
    parent.removeChild(surplus);
  }
}
function keyFor(node) {
  if (node.nodeType !== Node.ELEMENT_NODE) return null;
  const el2 = node;
  if (el2.id) return `#${el2.id}`;
  const dataKey = el2.getAttribute("data-rui-key");
  if (dataKey) return `@${dataKey}`;
  return null;
}
const renderChild$2 = (helpers, value) => {
  if (value == null) return document.createDocumentFragment();
  return helpers.renderNode(value);
};
const actionButton = (label, className) => el("button", {
  type: "button",
  class: `rui-button ${className}`,
  "data-variant": "secondary",
  "data-size": "sm"
}, [label]);
const isResourceShape = (value) => {
  if (!value || typeof value !== "object" || Array.isArray(value)) return false;
  const obj = value;
  return "state" in obj || "loading" in obj || "error" in obj || "data" in obj;
};
const isEmptyPayload = (value) => value === void 0 || value === null || Array.isArray(value) && value.length === 0;
const Async = {
  name: "Async",
  description: "Render `loading`, `error`, `empty`, or `data` slot based on an `$http({...})` resource's state. A value that is not a resource (a plain array or object) is treated as already-resolved data. Pass `retry` to add a Retry button to the error branch. State transitions are announced to screen readers.",
  props: [
    { name: "resource", type: "any", positional: true, required: true },
    { name: "loading", type: "Node", optional: true },
    { name: "error", type: "Node", optional: true },
    { name: "empty", type: "Node", optional: true },
    { name: "data", type: "Node", optional: true },
    { name: "retry", type: "callable", optional: true, aliases: ["onRetry"], description: "Called when the user presses Retry in the error branch (re-run the request)" }
  ],
  render: (_node, props, helpers) => {
    const resource = props.resource;
    const slots = {
      loading: props.loading,
      error: props.error,
      empty: props.empty,
      data: props.data
    };
    const branch = (content, status, busy) => {
      const wrapper = el("div", {
        class: "rui-async",
        "data-state": status,
        "aria-busy": busy ? "true" : null
      });
      wrapper.append(el("span", {
        class: "rui-visually-hidden rui-async-status",
        role: "status",
        "aria-live": "polite"
      }, [statusText(status)]));
      wrapper.append(renderChild$2(helpers, content));
      return wrapper;
    };
    const errorBranch = () => {
      const wrapper = branch(slots.error ?? null, "error", false);
      if (props.retry != null) {
        const retry = actionButton("Retry", "rui-async-retry");
        retry.onclick = () => {
          helpers.invoke(props.retry);
        };
        wrapper.appendChild(retry);
      }
      return wrapper;
    };
    if (!isResourceShape(resource)) {
      return isEmptyPayload(resource) ? branch(slots.empty ?? slots.data ?? null, "empty", false) : branch(slots.data ?? slots.empty ?? null, "loaded", false);
    }
    const state = resource.state;
    if (state === "loading" || resource.loading) {
      return branch(slots.loading ?? null, "loading", true);
    }
    if (state === "error" || resource.error) {
      return errorBranch();
    }
    const data = resource.data;
    if (isEmptyPayload(data)) {
      return branch(slots.empty ?? slots.data ?? null, "empty", false);
    }
    return branch(slots.data ?? null, "loaded", false);
  }
};
function statusText(status) {
  if (status === "loading") return "Loading…";
  if (status === "error") return "Could not load the data";
  if (status === "empty") return "No results";
  return "Loaded";
}
const Show = {
  name: "Show",
  description: "Conditional renderer. Sugar over `if expr { children } else { fallback }`.",
  props: [
    { name: "when", type: "any", positional: true, required: true },
    { name: "children", aliases: ["child"], type: "Node[]", optional: true },
    { name: "fallback", type: "Node", optional: true }
  ],
  render: (_node, props, helpers) => {
    if (props.when) {
      return renderChild$2(helpers, props.children ?? null);
    }
    return renderChild$2(helpers, props.fallback ?? null);
  }
};
function portalLayer(root) {
  const existing = root.querySelector(".rui-portal-layer");
  if (existing) return existing;
  const layer = el("div", {
    class: "rui-portal-layer",
    // Inline rather than stylesheet-dependent: the layer has to stack above the
    // app content even if a consumer overrides the sheet.
    style: "position:relative;z-index:var(--rui-z-popover, 1350)"
  });
  root.appendChild(layer);
  return layer;
}
function portalMount(anchor, target) {
  const root = anchor.getRootNode();
  const scope = root instanceof ShadowRoot || root instanceof Document ? root : null;
  if (target) {
    try {
      const scoped = scope?.querySelector(target);
      if (scoped) return scoped;
      const outer = document.querySelector(target);
      if (outer) return outer;
    } catch {
    }
  }
  return scope ? portalLayer(scope) : null;
}
const Portal = {
  name: "Portal",
  description: "Render children outside the parent subtree — into a layer at the top of the app root by default, or into `target` (a CSS selector, resolved inside the app first, then the document).",
  props: [
    { name: "target", type: "string", optional: true },
    { name: "children", aliases: ["child"], type: "Node[]", positional: true }
  ],
  render: (_node, props, helpers) => {
    const target = asString(props.target);
    const anchor = el("span", { class: "rui-portal-anchor", "data-portal": target || "app-root" });
    const slot = helpers.useInstanceState("rui-portal", null);
    let entry = slot.get();
    if (!entry) {
      const container2 = el("div", { class: "rui-portal" });
      entry = {
        container: container2,
        dispose: () => {
          container2.remove();
          slot.set(null);
        }
      };
      slot.set(entry);
    }
    const container = entry.container;
    helpers.registerDisposer(entry.dispose, "rui-portal-container");
    const fresh = document.createDocumentFragment();
    for (const child of asArray(props.children)) {
      fresh.append(helpers.renderNode(child));
    }
    morphChildren(container, fresh);
    deferToPaint(() => {
      if (!anchor.isConnected) return;
      const mount = portalMount(anchor, target);
      if (mount && container.parentNode !== mount) mount.append(container);
    });
    return anchor;
  }
};
function navigateReplacing(router, path) {
  const history = typeof window !== "undefined" ? window.history : void 0;
  if (!history || typeof history.replaceState !== "function") {
    router.navigate(path);
    return;
  }
  if (router.getMode() === "history") {
    const push = history.pushState;
    try {
      history.pushState = (data, unused, url) => history.replaceState(data, unused, url);
      router.navigate(path);
    } finally {
      history.pushState = push;
    }
    return;
  }
  try {
    history.replaceState(history.state, "", `#${normalisePath(path)}`);
  } catch {
  }
  router.navigate(path);
}
const Redirect = {
  name: "Redirect",
  description: "Navigate to `path` and unmount the rest of the subtree. Replaces the current history entry by default (guard semantics — Back must not return to the route that redirected); pass `replace: false` to push one instead.",
  props: [
    { name: "path", type: "string", positional: true, required: true },
    { name: "replace", type: "boolean", optional: true, description: "Replace the current history entry (default true)" }
  ],
  render: (_node, props, helpers) => {
    const path = asString(props.path);
    const replace = props.replace === void 0 ? true : props.replace !== false && props.replace !== "false";
    if (path) {
      try {
        if (replace) navigateReplacing(helpers.router, path);
        else helpers.router.navigate(path);
      } catch {
      }
    }
    return el("span", { class: "rui-redirect", "data-path": path, hidden: "true" });
  }
};
const Lazy = {
  name: "Lazy",
  description: "Defer rendering until an async `loader` resolves: show `fallback` while pending, then render the resolved value (or `children`). If the loader rejects, the `error` slot renders and `onError(err)` fires; `retry` adds a Retry button that runs the loader again. A synchronous loader value renders immediately.",
  props: [
    { name: "loader", type: "any", positional: true, required: true },
    { name: "fallback", type: "Node", optional: true },
    { name: "children", aliases: ["child"], type: "Node[]", optional: true },
    { name: "error", type: "Node", optional: true, description: "Rendered when the loader rejects (defaults to the fallback)" },
    { name: "onError", type: "callable", optional: true, description: "(err) => … fired once when the loader rejects" },
    { name: "retry", type: "callable", optional: true, description: "Called after the built-in Retry button re-runs the loader" }
  ],
  render: (_node, props, helpers) => {
    const loader = props.loader;
    const slot = helpers.useInstanceState("rui-lazy", { status: "init" });
    const wrapper = el("span", { class: "rui-lazy", style: "display: contents;" });
    const paint = (target, value) => {
      target.replaceChildren(renderChild$2(helpers, value));
    };
    const fallback = () => props.fallback ?? null;
    const resolvedContent = (value) => value ?? props.children ?? null;
    const paintError = (target) => {
      const content = document.createDocumentFragment();
      content.append(renderChild$2(helpers, props.error ?? fallback()));
      if (props.retry != null) {
        const retry = actionButton("Retry", "rui-lazy-retry");
        retry.onclick = (event) => {
          const live = (event.currentTarget ?? event.target)?.closest(".rui-lazy") ?? target;
          slot.set({ status: "init" });
          attempt(live);
          helpers.invoke(props.retry);
        };
        content.append(retry);
      }
      target.replaceChildren(content);
    };
    const fail = (target, err) => {
      slot.set({ status: "error", error: err });
      helpers.invoke(props.onError, err);
      paintError(target);
    };
    const attempt = (target) => {
      let result;
      try {
        result = typeof loader === "function" ? loader() : loader;
      } catch (err) {
        fail(target, err);
        return;
      }
      if (result && typeof result.then === "function") {
        slot.set({ status: "pending" });
        paint(target, fallback());
        let cancelled = false;
        helpers.registerDisposer(() => {
          cancelled = true;
        }, "rui-lazy-cancel");
        void result.then(
          (value) => {
            if (cancelled) return;
            slot.set({ status: "resolved", value });
            paint(target, resolvedContent(value));
          },
          (err) => {
            if (cancelled) return;
            fail(target, err);
          }
        );
        return;
      }
      slot.set({ status: "resolved", value: result });
      paint(target, resolvedContent(result));
    };
    const state = slot.get();
    if (state.status === "resolved") {
      paint(wrapper, resolvedContent(state.value));
      return wrapper;
    }
    if (state.status === "pending") {
      paint(wrapper, fallback());
      return wrapper;
    }
    if (state.status === "error") {
      paintError(wrapper);
      return wrapper;
    }
    attempt(wrapper);
    return wrapper;
  }
};
const ErrorBoundary = {
  name: "ErrorBoundary",
  description: "Render a fallback subtree when rendering children fails. Pass a `fallback` node (or omit it for a built-in friendly error card, which shows a Retry button when `onRetry` is set); `onError(err)` fires with the error. Set `showDetails=true` to reveal the message inline (great in dev).",
  props: [
    { name: "fallback", type: "Node", optional: true },
    { name: "onError", type: "callable", optional: true },
    { name: "showDetails", type: "boolean", optional: true, description: "Show the error message inline (default false)" },
    { name: "onRetry", type: "callable", optional: true, description: "Called when the user presses Retry on the built-in error card" },
    { name: "children", aliases: ["child"], type: "Node[]", positional: true }
  ],
  render: (_node, props, helpers) => {
    const wrapper = el("div", { class: "rui-error-boundary" });
    let caught = null;
    try {
      for (const child of asArray(props.children)) {
        wrapper.append(helpers.renderNode(child));
      }
      const substituted = wrapper.querySelector(".rui-render-error");
      if (!substituted) return wrapper;
      caught = new Error(substituted.textContent?.trim() || "render error");
    } catch (err) {
      caught = err;
    }
    try {
      helpers.invoke(props.onError, caught);
    } catch {
    }
    const message = caught instanceof Error ? caught.message : String(caught);
    const errorWrapper = el("div", { class: "rui-error-boundary rui-error-boundary--fallback" });
    errorWrapper.setAttribute("role", "alert");
    if (props.fallback != null) {
      errorWrapper.append(renderChild$2(helpers, props.fallback));
    } else {
      const card = el("div", { class: "rui-error-card" });
      const icon = renderIcon("triangle-exclamation", { className: "rui-error-card-icon" });
      if (icon) card.append(icon);
      card.append(el("div", { class: "rui-error-card-title" }, ["Something went wrong"]));
      if (props.showDetails === true || props.showDetails === "true") {
        card.append(el("div", { class: "rui-error-card-message" }, [message]));
      }
      if (props.onRetry != null) {
        const retry = actionButton("Retry", "rui-error-card-retry");
        retry.onclick = () => {
          helpers.invoke(props.onRetry);
        };
        card.append(retry);
      }
      errorWrapper.append(card);
    }
    errorWrapper.setAttribute("data-error", message);
    return errorWrapper;
  }
};
const ALLOWED_TAGS = /* @__PURE__ */ new Set([
  "div",
  "span",
  "p",
  "section",
  "article",
  "header",
  "footer",
  "main",
  "nav",
  "aside",
  "figure",
  "figcaption",
  "details",
  "summary",
  "h1",
  "h2",
  "h3",
  "h4",
  "h5",
  "h6",
  "ul",
  "ol",
  "li",
  "dl",
  "dt",
  "dd",
  "table",
  "thead",
  "tbody",
  "tfoot",
  "tr",
  "td",
  "th",
  "caption",
  "colgroup",
  "col",
  "a",
  "img",
  "picture",
  "source",
  "video",
  "audio",
  "track",
  "small",
  "strong",
  "em",
  "b",
  "i",
  "u",
  "s",
  "mark",
  "code",
  "pre",
  "kbd",
  "samp",
  "var",
  "sub",
  "sup",
  "abbr",
  "cite",
  "blockquote",
  "q",
  "time",
  "address",
  "ins",
  "del",
  "ruby",
  "rt",
  "rp",
  "bdi",
  "bdo",
  "br",
  "hr",
  "wbr",
  "hgroup",
  "label",
  "fieldset",
  "legend",
  "progress",
  "meter",
  "output"
]);
const ATTR_NAME_RE = /^[a-zA-Z][a-zA-Z0-9_:-]*$/;
const URL_ATTRS_HREF = /* @__PURE__ */ new Set(["href", "ping", "action", "formaction"]);
const URL_ATTRS_SRC = /* @__PURE__ */ new Set(["src", "poster"]);
const BLOCKED_ATTRS = /* @__PURE__ */ new Set([
  "srcset",
  "imagesrcset",
  "srcdoc",
  "data",
  "background",
  "manifest",
  "http-equiv",
  "html",
  "innerhtml",
  "outerhtml",
  "textcontent",
  // Custom-element upgrade: `is` asks the browser to construct a
  // *different*, host-defined element than the tag we allow-listed.
  "is"
]);
function sanitiseInlineStyleAttr(value) {
  const raw = asString(value).trim();
  if (!raw) return "";
  if (/[<>]/.test(raw)) return "";
  if (/\bexpression\s*\(|\bjavascript\s*:|\bbehavior\s*:|@import\b/i.test(raw)) return "";
  return raw;
}
const warnedTags = /* @__PURE__ */ new Set();
function resolveTagName(input) {
  const name = asString(input).trim().toLowerCase();
  if (!name) return "div";
  if (!ALLOWED_TAGS.has(name)) {
    if (!warnedTags.has(name)) {
      warnedTags.add(name);
      console.warn(
        `[aktion] HTMLTag: tag "${name}" is not in the allow-list — rendered as <div>. For vector graphics use \`Svg\` or \`Icon\`.`
      );
    }
    return "div";
  }
  return name;
}
function buildAttrs(input) {
  if (!input || typeof input !== "object" || Array.isArray(input)) return {};
  const out = {};
  for (const [rawKey, rawValue] of Object.entries(input)) {
    const key = rawKey.trim();
    if (!key) continue;
    if (!ATTR_NAME_RE.test(key)) continue;
    const lower = key.toLowerCase();
    if (lower.startsWith("on")) continue;
    if (rawValue === null || rawValue === void 0 || rawValue === false) continue;
    const local = lower.includes(":") ? lower.slice(lower.lastIndexOf(":") + 1) : lower;
    if (local === "style") {
      const style = sanitiseInlineStyleAttr(rawValue);
      if (style) out.style = style;
      continue;
    }
    if (URL_ATTRS_HREF.has(local)) {
      const safe = sanitiseHref(rawValue, "");
      if (safe) out[key] = safe;
      continue;
    }
    if (URL_ATTRS_SRC.has(local)) {
      const safe = sanitiseImageSrc(rawValue);
      if (safe) out[key] = safe;
      continue;
    }
    if (BLOCKED_ATTRS.has(lower) || BLOCKED_ATTRS.has(local)) continue;
    out[key] = rawValue === true ? "" : asString(rawValue);
  }
  const target = out.target ?? out.Target;
  if (target && target !== "_self" && target !== "_parent" && target !== "_top") {
    const rel = (out.rel ?? "").split(/\s+/).filter(Boolean);
    if (!rel.includes("noopener")) rel.push("noopener");
    if (!rel.includes("noreferrer")) rel.push("noreferrer");
    out.rel = rel.join(" ");
  }
  return out;
}
const HTMLTag = {
  name: "HTMLTag",
  description: "Escape-hatch primitive that renders an allow-listed HTML tag with the given attributes and children. Use ONLY when the standard component catalogue cannot express the markup (custom semantic elements, third-party widget mounts). SVG is NOT supported here — use `Svg` for vector markup and `Icon` for glyphs. Tag names outside the allow-list collapse to `div` (with a console warning). Attribute names matching `on*` (event handlers) are dropped, `href`/`src` are sanitised, `srcset`/`srcdoc` are dropped, and `style` is filtered for `expression()` / `javascript:` / `@import`. Pass children as an array of components — strings render as text nodes.",
  props: [
    { name: "tag", type: "string", positional: true, required: true, description: 'HTML tag name (e.g. "div", "section", "figure"). Falls back to `div` when outside the allow-list — SVG tags are not accepted, use `Svg`.' },
    { name: "attributes", type: "object", optional: true, aliases: ["attrs", "props"], description: 'Plain object of attribute name → value pairs (e.g. `{ class: "hero", "data-id": 1 }`). `on*` handlers are dropped.' },
    { name: "children", aliases: ["child"], type: "Node[]", optional: true, description: "Child components or text nodes to render inside the tag." }
  ],
  render: (_node, props, helpers) => {
    const tag = resolveTagName(props.tag);
    const attrs = buildAttrs(props.attributes);
    const root = document.createElement(tag);
    for (const [key, value] of Object.entries(attrs)) root.setAttribute(key, value);
    for (const child of asArray(props.children)) {
      if (child === null || child === void 0) continue;
      if (typeof child === "string") {
        root.append(document.createTextNode(child));
        continue;
      }
      if (typeof child === "number" || typeof child === "boolean") {
        root.append(document.createTextNode(String(child)));
        continue;
      }
      root.append(helpers.renderNode(child));
    }
    return root;
  }
};
const STYLES_BLOCK_RE = /<\/style|<script|expression\s*\(|javascript\s*:|behavior\s*:|@import\b/i;
const STYLES_MAX_LENGTH = 64 * 1024;
function sanitiseStyleSheet(input) {
  const raw = asString(input);
  if (!raw) return "";
  if (raw.length > STYLES_MAX_LENGTH) {
    console.warn(
      `[aktion] Styles: payload rejected — ${raw.length} characters exceeds the ${STYLES_MAX_LENGTH}-character cap.`
    );
    return "";
  }
  if (STYLES_BLOCK_RE.test(raw)) {
    console.warn(
      "[aktion] Styles: payload rejected — it contains one of `</style`, `<script`, `expression(`, `javascript:`, `behavior:` or `@import`."
    );
    return "";
  }
  return raw;
}
const SCOPE_SELECTOR_RE = /^[.#]?[A-Za-z_][\w-]*(?:\s*[>+~]?\s*[.#]?[A-Za-z_][\w-]*)*$/;
const SCOPE_MAX_LENGTH = 128;
function sanitiseScopeSelector(input) {
  const raw = asString(input).trim();
  if (!raw) return "";
  if (raw.length > SCOPE_MAX_LENGTH) return null;
  if (STYLES_BLOCK_RE.test(raw)) return null;
  if (!SCOPE_SELECTOR_RE.test(raw)) return null;
  return raw;
}
function interpolateTokens(css) {
  return css.replace(/\{\s*([a-zA-Z]+)\.([a-zA-Z0-9-]+)\s*\}/g, (whole, group, rawKey) => {
    const key = rawKey.replace(/([a-z])([A-Z])/g, "$1-$2").toLowerCase();
    const prefix = group === "colors" || group === "color" ? "color" : group === "spacing" ? "spacing" : group === "radius" ? "radius" : group === "shadows" || group === "shadow" ? "shadow" : group === "gradients" || group === "gradient" ? "gradient" : group === "font" || group === "fonts" ? "font" : null;
    if (!prefix) return whole;
    return `var(--rui-${prefix}-${key})`;
  });
}
const NESTED_AT_RULES = /* @__PURE__ */ new Set([
  "@media",
  "@supports",
  "@container",
  "@layer",
  "@scope",
  "@document"
]);
function splitSelectorList(prelude) {
  const out = [];
  let depth = 0;
  let quote = "";
  let start = 0;
  for (let i = 0; i < prelude.length; i += 1) {
    const ch = prelude[i];
    if (quote) {
      if (ch === "\\") {
        i += 1;
        continue;
      }
      if (ch === quote) quote = "";
      continue;
    }
    if (ch === '"' || ch === "'") {
      quote = ch;
      continue;
    }
    if (ch === "(" || ch === "[") {
      depth += 1;
      continue;
    }
    if ((ch === ")" || ch === "]") && depth > 0) {
      depth -= 1;
      continue;
    }
    if (ch === "," && depth === 0) {
      out.push(prelude.slice(start, i));
      start = i + 1;
    }
  }
  out.push(prelude.slice(start));
  return out;
}
function scopeSelector(sel, scopeSel) {
  const s = sel.trim();
  if (!s) return s;
  if (s.startsWith(":root") || s.startsWith(":host")) return s;
  if (s.startsWith("&")) return `${scopeSel}${s.slice(1)}`;
  if (s === scopeSel) return s;
  if (s.startsWith(scopeSel)) {
    const next = s.charAt(scopeSel.length);
    if (next === ":" || next === "." || next === "#" || next === "[") return s;
  }
  return `${scopeSel} ${s}`;
}
function scopeCss(css, scopeSel) {
  const out = [];
  let i = 0;
  const n = css.length;
  while (i < n) {
    const brace = css.indexOf("{", i);
    if (brace === -1) {
      out.push(css.slice(i));
      break;
    }
    const prelude = css.slice(i, brace).trim();
    let depth = 1;
    let j = brace + 1;
    for (; j < n && depth > 0; j += 1) {
      if (css[j] === "{") depth += 1;
      else if (css[j] === "}") depth -= 1;
    }
    const body = depth === 0 ? css.slice(brace + 1, j - 1) : css.slice(brace + 1, n);
    const atRule = /^@[-a-zA-Z]+/.exec(prelude)?.[0]?.toLowerCase();
    if (atRule && NESTED_AT_RULES.has(atRule)) {
      out.push(`${prelude} { ${scopeCss(body, scopeSel)} }`);
    } else if (atRule) {
      out.push(`${prelude} { ${body} }`);
    } else {
      const scoped = splitSelectorList(prelude).map((sel) => scopeSelector(sel, scopeSel)).join(", ");
      out.push(`${scoped} { ${body} }`);
    }
    i = j;
  }
  return out.join("\n");
}
const Styles = {
  name: "Styles",
  description: "Escape-hatch primitive that injects a `<style>` block containing the given CSS rules. Use ONLY when a layout cannot be expressed via component props or the `$theme(...)` token map. The CSS is rendered verbatim into the document so authors can target their own `HTMLTag` markup or scope rules to a wrapper class. Payloads containing `</style>`, `<script>`, `expression(`, `javascript:`, `behavior:`, or `@import` are dropped for safety — as is a `scope` that is not a plain selector. Every rejection is logged with `console.warn`, so an empty `<style>` is always explained.",
  props: [
    { name: "css", type: "string", positional: true, required: true, aliases: ["content", "rules"], description: 'Raw CSS text (e.g. `".hero { color: red; }"`).' },
    { name: "scope", type: "string", optional: true, description: 'Selector to scope every rule under, e.g. `".my-widget"` (I.6). Must be a plain class/id/tag selector — anything else drops the sheet.' },
    { name: "tokens", type: "boolean", optional: true, description: "Interpolate `{group.key}` token refs to CSS vars (default true)" }
  ],
  render: (_node, props) => {
    let css = sanitiseStyleSheet(props.css);
    if (css && (props.tokens === void 0 || props.tokens === true || props.tokens === "true")) {
      css = interpolateTokens(css);
    }
    const scope = sanitiseScopeSelector(props.scope);
    if (scope === null) {
      console.warn(
        `[aktion] Styles: scope="${asString(props.scope)}" is not a plain selector (class/id/tag with combinators) — the stylesheet was dropped.`
      );
      css = "";
    } else if (css && scope) {
      css = scopeCss(css, scope);
    }
    const node = document.createElement("style");
    node.setAttribute("class", "rui-styles");
    if (css) node.textContent = css;
    return node;
  }
};
function asRecord(value) {
  return value && typeof value === "object" && !Array.isArray(value) ? value : {};
}
function isPlainObject(value) {
  if (!value || typeof value !== "object") return false;
  const proto = Object.getPrototypeOf(value);
  return proto === Object.prototype || proto === null;
}
const COMPARE_DEPTH = 4;
function sameValue(a, b, depth) {
  if (Object.is(a, b)) return true;
  if (depth <= 0) return false;
  if (Array.isArray(a) && Array.isArray(b)) {
    if (a.length !== b.length) return false;
    for (let i = 0; i < a.length; i += 1) {
      if (!sameValue(a[i], b[i], depth - 1)) return false;
    }
    return true;
  }
  if (isPlainObject(a) && isPlainObject(b)) {
    const ak = Object.keys(a);
    if (ak.length !== Object.keys(b).length) return false;
    for (const key of ak) {
      if (!Object.prototype.hasOwnProperty.call(b, key)) return false;
      if (!sameValue(a[key], b[key], depth - 1)) return false;
    }
    return true;
  }
  return false;
}
function sameBag(a, b) {
  return sameValue(a, b, COMPARE_DEPTH);
}
function defer(run) {
  if (typeof queueMicrotask === "function") queueMicrotask(run);
  else void Promise.resolve().then(run);
}
let HOST_SEQ = 0;
function hostKey(explicitKey, prefix) {
  const authored = explicitKey == null ? "" : String(explicitKey);
  if (authored && /^[A-Za-z0-9_.:-]+$/.test(authored)) return authored;
  return `${prefix}-${HOST_SEQ += 1}`;
}
function queryDeep(root, selector, depth) {
  const direct = root.querySelector(selector);
  if (direct) return direct;
  if (depth <= 0) return null;
  for (const candidate of Array.from(root.querySelectorAll("*"))) {
    if (!candidate.tagName.includes("-")) continue;
    const shadow = candidate.shadowRoot;
    if (!shadow) continue;
    const hit = queryDeep(shadow, selector, depth - 1);
    if (hit) return hit;
  }
  return null;
}
function resolveHost(rendered, key) {
  if (rendered.isConnected) return rendered;
  if (typeof document === "undefined") return null;
  return queryDeep(document, `[data-rui-key="${key}"]`, 4);
}
const HOST_TAGS = /* @__PURE__ */ new Set([
  "div",
  "span",
  "section",
  "article",
  "aside",
  "figure",
  "canvas",
  "p",
  "pre",
  "form"
]);
function resolveHostTag(input) {
  const name = asString(input).trim().toLowerCase();
  return HOST_TAGS.has(name) ? name : "div";
}
function reportMountError(state, invoke, stage, err) {
  console.error(`[aktion] Mount ${stage} threw`, err);
  if (invoke && state.onError != null) invoke(state.onError, err, stage);
}
function runMountCleanup(state) {
  const cleanup = state.cleanup;
  const instance = state.instance;
  state.instance = void 0;
  state.node = null;
  state.started = false;
  if (typeof cleanup !== "function") return;
  defer(() => {
    try {
      cleanup(instance);
    } catch (err) {
      console.error("[aktion] Mount cleanup threw", err);
    }
  });
}
const Mount = {
  name: "Mount",
  description: "First-class host for an imperative / third-party widget that owns its own DOM (chart, map, editor, payment element, captcha, video SDK). Aktion creates the host element; `setup(node, props)` runs once after it attaches and returns an instance handle, `update(instance, props)` runs when `props` change (or when `deps` change, if you pass them), and `cleanup(instance)` runs on unmount. `onError(err, stage)` fires if `setup`/`update` throws, so a failed map / payment element / captcha can show a fallback. The host is preserved across re-renders so the widget is never rebuilt. Apply layout with `sx`.",
  props: [
    { name: "setup", type: "callable", required: true, description: "`(node, props) => instance` — runs once after the host attaches; return value is the instance handle passed to `update`/`cleanup`." },
    { name: "update", type: "callable", optional: true, description: "`(instance, props) => void` — runs when `props` (or `deps`) change." },
    { name: "cleanup", type: "callable", optional: true, description: "`(instance) => void` — runs when the component leaves the tree (destroy/teardown)." },
    { name: "props", type: "object", optional: true, description: "Reactive prop bag handed to `setup`/`update`. Bind `$state` here to drive the widget." },
    { name: "tag", type: "string", optional: true, enum: ["div", "span", "section", "article", "aside", "figure", "canvas", "p", "pre", "form"], description: 'Host element tag (default "div").' },
    { name: "deps", type: "any[]", optional: true, description: "Explicit dependency list gating `update`. Use when the `props` bag is rebuilt on every commit, or to force an update after an in-place mutation." },
    { name: "onError", type: "callable", optional: true, description: '`(err, stage) => void` — fired when `setup` or `update` throws (stage is "setup" / "update").' }
  ],
  render: (node, props, helpers) => {
    const tag = resolveHostTag(props.tag);
    const widgetProps = asRecord(props.props);
    const deps = props.deps === void 0 ? null : asArray(props.deps);
    const slot = helpers.useInstanceState("rui-mount", {
      started: false,
      instance: void 0,
      props: widgetProps,
      prevProps: null,
      deps: null,
      cleanup: null,
      onError: null,
      node: null,
      tag: "",
      key: "",
      disposer: null
    });
    const state = slot.get();
    if (!state.key) state.key = hostKey(node.explicitKey, "rui-mount");
    state.cleanup = props.cleanup;
    state.onError = props.onError;
    const host = el(tag, {
      class: "rui-mount",
      "data-rui-preserve": "",
      // Stable identity. Without it the reconciler matches the host purely by
      // sibling position, so a Mount that first appears on a later commit was
      // patched onto whatever element already sat in that slot while `setup` ran
      // against the detached fresh one. The key reaches the incumbent through
      // the preserve-aware additive attribute sync, which is what lets
      // `resolveHost` find the element the user is actually looking at.
      "data-rui-key": state.key
    });
    if (state.tag && state.tag !== tag) runMountCleanup(state);
    state.tag = tag;
    if (!state.disposer) state.disposer = () => runMountCleanup(state);
    helpers.registerDisposer(state.disposer, "rui-mount-cleanup");
    if (!state.started) {
      state.prevProps = null;
      state.props = widgetProps;
      state.deps = deps;
      defer(() => {
        if (state.started) return;
        const live = resolveHost(host, state.key);
        if (!live) return;
        state.started = true;
        state.node = live;
        if (live !== host) live.replaceChildren();
        try {
          state.instance = typeof props.setup === "function" ? props.setup(live, state.props) : void 0;
        } catch (err) {
          reportMountError(state, helpers.invoke, "setup", err);
        }
      });
      return host;
    }
    state.prevProps = state.props;
    state.props = widgetProps;
    const prevDeps = state.deps;
    state.deps = deps;
    const changed = deps !== null ? !sameBag(prevDeps, deps) : !sameBag(state.prevProps, widgetProps);
    if (state.instance !== void 0 && typeof props.update === "function" && changed) {
      const update = props.update;
      const instance = state.instance;
      defer(() => {
        try {
          update(instance, widgetProps);
        } catch (err) {
          reportMountError(state, helpers.invoke, "update", err);
        }
      });
    }
    return host;
  }
};
const CUSTOM_ELEMENT_RE = /^[a-z][a-z0-9]*(-[a-z0-9]+)+$/;
const WARNED_TAGS = /* @__PURE__ */ new Set();
function resolveCustomTag(input) {
  const name = asString(input).trim().toLowerCase();
  if (CUSTOM_ELEMENT_RE.test(name)) return name;
  if (name && !WARNED_TAGS.has(name)) {
    WARNED_TAGS.add(name);
    console.warn(
      `[aktion] WebComponent("${name}") is not a valid custom-element name (it must contain a hyphen) — rendering a <div> instead.`
    );
  }
  return "div";
}
const URL_ATTRIBUTES = /* @__PURE__ */ new Set(["href", "src", "action", "formaction", "poster", "ping"]);
function applyAttributes(node, attributes) {
  for (const [key, value] of Object.entries(attributes)) {
    if (/^on/i.test(key)) continue;
    const lower = key.toLowerCase();
    if (lower === "srcdoc") continue;
    if (value === null || value === void 0 || value === false) {
      node.removeAttribute(key);
      continue;
    }
    if (URL_ATTRIBUTES.has(lower)) {
      const safe = lower === "src" || lower === "poster" ? sanitiseImageSrc(value) : sanitiseHref(value, "");
      if (safe) node.setAttribute(key, safe);
      else node.removeAttribute(key);
      continue;
    }
    node.setAttribute(key, value === true ? "" : String(value));
  }
}
const BLOCKED_PROPERTIES = /* @__PURE__ */ new Set([
  "innerhtml",
  "outerhtml",
  "insertadjacenthtml",
  "srcdoc",
  "src",
  "href",
  "action",
  "formaction",
  "style",
  "id",
  "attributes",
  "shadowroot",
  "contenteditable",
  "constructor",
  "__proto__",
  "prototype"
]);
function applyProperties(node, properties) {
  const target = node;
  for (const [key, value] of Object.entries(properties)) {
    const lower = key.toLowerCase();
    if (lower.startsWith("on")) continue;
    if (BLOCKED_PROPERTIES.has(lower)) continue;
    try {
      target[key] = value;
    } catch {
    }
  }
}
const WebComponent = {
  name: "WebComponent",
  description: "Render and hydrate any native custom element / web component with reactive `attributes`, JS `properties`, and `on` event hooks. Use to drop a third-party web component (`<stripe-pricing-table>`, `<model-viewer>`, a design-system element) into an Aktion app. The element is preserved across re-renders; attribute changes flow through reactively and listeners stay current. Invalid (hyphen-less) tag names fall back to a `div`.",
  props: [
    { name: "tag", type: "string", positional: true, required: true, description: 'Custom-element tag name (must contain a hyphen, e.g. "stripe-pricing-table").' },
    { name: "attributes", type: "object", optional: true, aliases: ["attrs"], description: "Reactive attribute map. `$state` values update the element on change; `on*` keys are ignored." },
    { name: "properties", type: "object", optional: true, aliases: ["props"], description: "JS properties assigned on the element (for components that take rich, non-string props)." },
    { name: "on", type: "object", optional: true, aliases: ["events"], description: "Event map `{ eventName: handler }` bound once to the live element (handlers stay current across re-renders)." },
    { name: "children", aliases: ["child"], type: "Node[]", optional: true, description: "Light-DOM child nodes / text to slot inside the element." }
  ],
  render: (node, props, helpers) => {
    const tag = resolveCustomTag(props.tag);
    const handlers = asRecord(props.on);
    const attributes = asRecord(props.attributes);
    const properties = asRecord(props.properties);
    const slot = helpers.useInstanceState("rui-web-component", {
      bound: /* @__PURE__ */ new Set(),
      handlers,
      node: null,
      key: ""
    });
    const state = slot.get();
    state.handlers = handlers;
    if (!state.key) state.key = hostKey(node.explicitKey, "rui-web-component");
    const element = el(tag, {
      class: "rui-web-component",
      "data-rui-preserve": "",
      // See Mount: a stable key is what makes the live element findable after
      // the reconciler has kept an incumbent and discarded this one.
      "data-rui-key": state.key
    });
    applyAttributes(element, attributes);
    applyProperties(element, properties);
    const children = Array.isArray(props.children) ? props.children : props.children != null ? [props.children] : [];
    for (const child of children) {
      if (child == null) continue;
      element.append(typeof child === "string" ? document.createTextNode(child) : helpers.renderNode(child));
    }
    defer(() => {
      const live = resolveHost(element, state.key);
      if (!live) return;
      if (state.node !== live) {
        state.node = live;
        state.bound = /* @__PURE__ */ new Set();
      }
      for (const eventName of Object.keys(state.handlers)) {
        if (state.bound.has(eventName)) continue;
        state.bound.add(eventName);
        live.addEventListener(eventName, (event) => {
          const fn = state.handlers[eventName];
          if (typeof fn === "function") helpers.invoke(fn, event);
        });
      }
      if (live !== element) {
        applyAttributes(live, attributes);
        applyProperties(live, properties);
      }
    });
    return element;
  }
};
const GradientText = {
  name: "GradientText",
  description: 'Inline text painted with a brand gradient. Use inside a Display/Heading title to emphasise a phrase: `Display(["Build in ", GradientText("record time")])`. `gradient` selects a named theme gradient (brand|accent|warm|cool|success|danger).',
  props: [
    { name: "text", type: "string", positional: true, required: true, aliases: ["children", "label"] },
    { name: "gradient", type: "string", optional: true, enum: ["brand", "accent", "warm", "cool", "success", "danger"] }
  ],
  render: (_node, props) => {
    const span = el("span", { class: "rui-gradient-text", "data-gradient": asString(props.gradient, "brand") });
    span.append(document.createTextNode(asString(props.text)));
    return span;
  }
};
function appendInline(host, value, helpers) {
  for (const item of asArray(value)) {
    if (item == null) continue;
    if (typeof item === "string" || typeof item === "number") {
      host.append(document.createTextNode(String(item)));
    } else {
      host.append(helpers.renderNode(item));
    }
  }
}
const Display = {
  name: "Display",
  description: "Oversized display headline with built-in responsive `clamp()` sizing. The positional argument may be a plain string OR an array mixing strings and `GradientText(...)` runs. `size`: hero|xl|lg. `balance` wraps for even line lengths.",
  props: [
    { name: "content", type: "string | Node[]", positional: true, required: true, aliases: ["children", "title"] },
    { name: "size", type: "string", optional: true, enum: ["hero", "xl", "lg"] },
    { name: "align", type: "string", optional: true, enum: ["left", "center", "right"] },
    { name: "balance", type: "boolean", optional: true },
    { name: "weight", type: "number", optional: true }
  ],
  render: (_node, props, helpers) => {
    const root = el("h1", {
      class: "rui-display",
      "data-size": asString(props.size, "hero"),
      "data-align": asString(props.align) || null,
      "data-balance": asBoolean(props.balance) ? "true" : null,
      style: props.weight != null ? `font-weight:${asNumber(props.weight, 900)}` : null
    });
    appendInline(root, props.content, helpers);
    return root;
  }
};
const Heading = {
  name: "Heading",
  description: "Section/subsection heading with token-driven sizing. `level` sets the semantic tag (1–6); `size`: section|lg|md|sm. Accepts string or mixed string/GradientText array like Display.",
  props: [
    { name: "content", type: "string | Node[]", positional: true, required: true, aliases: ["children", "title"] },
    { name: "level", type: "number", optional: true, description: "Heading level 1–6 (default 2)" },
    { name: "size", type: "string", optional: true, enum: ["section", "lg", "md", "sm"] },
    { name: "align", type: "string", optional: true, enum: ["left", "center", "right"] }
  ],
  render: (_node, props, helpers) => {
    const level = Math.min(6, Math.max(1, Math.round(asNumber(props.level, 2))));
    const root = el(`h${level}`, {
      class: "rui-heading",
      "data-size": asString(props.size, "section"),
      "data-align": asString(props.align) || null
    });
    appendInline(root, props.content, helpers);
    return root;
  }
};
const Eyebrow = {
  name: "Eyebrow",
  description: "Small uppercase, letter-spaced, primary-toned label above a heading.",
  props: [{ name: "text", type: "string", positional: true, required: true, aliases: ["label", "children"] }],
  render: (_node, props) => el("span", { class: "rui-eyebrow" }, [asString(props.text)])
};
const Section = {
  name: "Section",
  description: "Full-bleed page band → centered max-width container → optional tinted background → optional eyebrow/title/subtitle header. The single most useful primitive for marketing, docs, and settings pages. Children render below the header.",
  props: [
    { name: "children", aliases: ["child"], type: "Node[]", positional: true, required: true },
    { name: "background", type: "string", optional: true, enum: ["base", "soft", "surface", "muted", "brand"], aliases: ["bg"] },
    { name: "width", type: "string", optional: true, enum: ["sm", "md", "lg", "xl", "full"] },
    { name: "padding", type: "string", optional: true, enum: ["none", "xs", "sm", "md", "lg", "xl"], description: "Band padding preset (default md)" },
    { name: "align", type: "string", optional: true, enum: ["left", "center"], description: "Centers the generated eyebrow/title/subtitle header; children keep their own alignment" },
    { name: "eyebrow", type: "string", optional: true },
    { name: "title", type: "string | Node[]", optional: true },
    { name: "subtitle", type: "string", optional: true, aliases: ["description"] },
    { name: "id", type: "string", optional: true },
    { name: "actions", type: "Node[]", optional: true, description: "Trailing header actions (e.g. a 'See all' Button), aligned opposite the title" }
  ],
  render: (_node, props, helpers) => {
    const bg2 = asString(props.background, "base");
    const root = el("section", {
      class: "rui-section",
      "data-bg": bg2 !== "base" ? bg2 : null,
      "data-pad": normalizeSpacingToken(props.padding, asString(props.padding)) || null,
      "data-align": asString(props.align) || null,
      id: asString(props.id) || null
    });
    const inner = el("div", { class: "rui-section-inner", "data-w": asString(props.width, "lg") });
    const eyebrow = asString(props.eyebrow);
    const hasTitle = props.title != null && (typeof props.title !== "string" || props.title !== "");
    const subtitle = asString(props.subtitle);
    const actions = asArray(props.actions);
    if (eyebrow || hasTitle || subtitle || actions.length > 0) {
      const head = el("div", { class: "rui-section-head", "data-has-actions": actions.length > 0 ? "true" : null });
      const textCol = actions.length > 0 ? el("div", { class: "rui-section-head-text" }) : head;
      if (eyebrow) textCol.append(el("span", { class: "rui-eyebrow" }, [eyebrow]));
      if (hasTitle) {
        const h = el("h2", { class: "rui-section-title" });
        appendInline(h, props.title, helpers);
        textCol.append(h);
      }
      if (subtitle) textCol.append(el("p", { class: "rui-section-sub" }, [subtitle]));
      if (textCol !== head) {
        head.append(textCol);
        const actionRow = el("div", { class: "rui-section-actions" });
        for (const action of actions) actionRow.append(helpers.renderNode(action));
        head.append(actionRow);
      }
      inner.append(head);
    }
    const decor = [];
    for (const child of asArray(props.children)) {
      const rendered = helpers.renderNode(child);
      if (rendered instanceof HTMLElement && rendered.classList.contains("rui-backdrop")) decor.push(rendered);
      else inner.append(rendered);
    }
    if (decor.length > 0) {
      root.append(...decor);
      inner.setAttribute("style", "position:relative;z-index:1");
    }
    root.append(inner);
    return root;
  }
};
const OverlayItem = {
  name: "OverlayItem",
  description: "A child positioned over an Overlay's base. `anchor` picks a corner/edge/center.",
  props: [
    { name: "child", type: "Node", positional: true, required: true, aliases: ["children"] },
    { name: "anchor", type: "string", optional: true, enum: ["top-left", "top-right", "bottom-left", "bottom-right", "top", "bottom", "left", "right", "center"] },
    { name: "offset", type: "string", optional: true, description: "Inset distance (CSS length, e.g. 8px or 1rem)" }
  ],
  render: (_node, props, helpers) => {
    const wrap = el("div", {
      class: "rui-overlay-item",
      "data-anchor": asString(props.anchor, "top-right"),
      // Validated as a CSS length: a raw value could otherwise close the custom
      // property with `;` and append arbitrary declarations to this element.
      style: props.offset ? `--ak-ov-off:${sanitiseCssLength(props.offset, "8px")}` : null
    });
    if (props.child != null) wrap.append(helpers.renderNode(props.child));
    return wrap;
  }
};
const Overlay = {
  name: "Overlay",
  description: "Layers OverlayItem children on top of a base node — corner badges, play buttons over thumbnails, sale ribbons, floating actions. Pass the base as the positional argument and `items` as OverlayItem entries.",
  props: [
    { name: "base", type: "Node", positional: true, required: true },
    { name: "items", type: "OverlayItem[]", optional: true, aliases: ["overlays"] }
  ],
  render: (_node, props, helpers) => {
    const root = el("div", { class: "rui-overlay" });
    if (props.base != null) {
      const base = el("div", { class: "rui-overlay-base" });
      base.append(helpers.renderNode(props.base));
      root.append(base);
    }
    for (const item of asArray(props.items)) root.append(helpers.renderNode(item));
    return root;
  }
};
const Brand = {
  name: "Brand",
  description: "Logo + product name (+ optional version pill), linking home. Use in NavBar/Footer.",
  props: [
    { name: "name", type: "string", positional: true, required: true, aliases: ["label"] },
    // `logo` is the original spelling and is still used by shipped .aktion demo
    // programs; validate.ts treats an undeclared prop as a fatal error, so the
    // alias is what keeps those programs compiling.
    { name: "logoSrc", type: "string", optional: true, aliases: ["logo"], description: "Logo image src URL, e.g. https://example.com/logo.png" },
    { name: "version", type: "string", optional: true },
    { name: "href", type: "string", optional: true }
  ],
  render: (_node, props) => {
    const root = el("a", { class: "rui-brand", href: sanitiseHref(props.href, "#") });
    const logo = sanitiseImageSrc(props.logoSrc);
    const name = asString(props.name);
    if (logo) root.append(el("img", { class: "rui-brand-logo", src: logo, alt: name || "" }));
    root.append(el("span", {}, [name]));
    const version = asString(props.version);
    if (version) root.append(el("span", { class: "rui-brand-version" }, [version]));
    return root;
  }
};
let NAVBAR_MENU_SEQ = 0;
function paintNavMenu(live, open) {
  live.setAttribute("data-menu-open", open ? "true" : "false");
  live.querySelector(".rui-navbar2-burger")?.setAttribute("aria-expanded", open ? "true" : "false");
}
const NavBar = {
  name: "NavBar",
  description: "Marketing top navigation: brand on the left, links in the middle, actions (buttons/toggles) on the right. `sticky` pins it; `blur` adds a frosted-glass backdrop. On narrow viewports the links collapse behind a burger toggle. Distinct from the app-shell Navbar/TopBar.",
  props: [
    { name: "brand", type: "Node", optional: true },
    // `items` is accepted too: this component and the app-shell `Navbar` differ by
    // one letter of case, so an author who reaches for the wrong one should not
    // also hit a hard validator error on the slot name.
    { name: "links", type: "Node[]", optional: true, aliases: ["items"], description: "NavLink / Link nodes" },
    { name: "actions", type: "Node[]", optional: true, description: "Buttons, ThemeToggle, IconButton, etc." },
    { name: "sticky", type: "boolean", optional: true },
    { name: "blur", type: "boolean", optional: true }
  ],
  render: (_node, props, helpers) => {
    const openSlot = helpers.useInstanceState("rui-navbar-menu", false);
    const menuOpen = openSlot.get();
    const links = asArray(props.links);
    const root = el("header", {
      class: "rui-navbar2",
      "data-sticky": asBoolean(props.sticky) ? "true" : null,
      "data-blur": asBoolean(props.blur) ? "true" : null,
      "data-menu-open": links.length > 0 ? menuOpen ? "true" : "false" : null
    });
    if (props.brand != null) root.append(helpers.renderNode(props.brand));
    if (links.length > 0) {
      const idSlot = helpers.useInstanceState("rui-navbar-menu-id", "");
      let menuId = idSlot.get();
      if (!menuId) {
        NAVBAR_MENU_SEQ += 1;
        menuId = `rui-navbar2-menu-${NAVBAR_MENU_SEQ}`;
        idSlot.set(menuId);
      }
      const nav = el("nav", { class: "rui-navbar2-links", id: menuId, "aria-label": "Primary" });
      for (const l of links) nav.append(helpers.renderNode(l));
      root.append(nav);
      const burger = el("button", {
        class: "rui-navbar2-burger",
        type: "button",
        "aria-label": "Menu",
        "aria-expanded": menuOpen ? "true" : "false",
        "aria-controls": menuId
      });
      const burgerIcon = renderIcon("bars");
      if (burgerIcon) burger.append(burgerIcon);
      else burger.textContent = "≡";
      burger.onclick = (event) => {
        const liveBtn = event.currentTarget ?? event.target;
        const live = liveBtn?.closest(".rui-navbar2");
        if (!live) return;
        const next = live.getAttribute("data-menu-open") !== "true";
        paintNavMenu(live, next);
        openSlot.set(next);
        if (!next) {
          disposeDismissListeners(live);
          return;
        }
        installDismissListeners({
          liveRoot: live,
          key: "rui-navbar2-menu",
          onDismiss: () => {
            paintNavMenu(live, false);
            openSlot.set(false);
          }
        });
        live.querySelector(".rui-navbar2-links a, .rui-navbar2-links button")?.focus?.();
      };
      root.append(burger);
    }
    const actions = el("div", { class: "rui-navbar2-actions" });
    for (const a of asArray(props.actions)) actions.append(helpers.renderNode(a));
    root.append(actions);
    return root;
  }
};
const FooterColumn = {
  name: "FooterColumn",
  description: "A titled column of links inside a Footer.",
  props: [
    { name: "title", type: "string", positional: true, required: true },
    { name: "links", type: "Node[]", optional: true, aliases: ["children", "items"] }
  ],
  render: (_node, props, helpers) => {
    const col = el("div", { class: "rui-footer-col" });
    col.append(el("h5", {}, [asString(props.title)]));
    const list = el("div", { class: "rui-footer-col-links" });
    for (const l of asArray(props.links)) list.append(helpers.renderNode(l));
    col.append(list);
    return col;
  }
};
const Footer = {
  name: "Footer",
  description: "Site footer: brand + tagline in a wide first column, then link columns, and a legal/copyright line. Pass `columns` as FooterColumn nodes.",
  props: [
    { name: "brand", type: "Node", optional: true },
    { name: "tagline", type: "string", optional: true },
    { name: "columns", type: "FooterColumn[]", optional: true },
    { name: "legal", type: "string | Node[]", optional: true, aliases: ["copyright"], description: "Copyright line — a string, or Link nodes for Privacy/Terms/Cookies" },
    { name: "social", type: "Node[]", optional: true, description: "Brand/social icon row under the tagline (IconButton / Link nodes)" }
  ],
  render: (_node, props, helpers) => {
    const root = el("footer", { class: "rui-footer" });
    const cols = asArray(props.columns);
    const grid = el("div", {
      class: "rui-footer-grid",
      "data-cols": String(cols.length),
      style: cols.length > 0 ? `--ak-foot-cols:${cols.length}` : "grid-template-columns:1fr"
    });
    const brandCol = el("div", { class: "rui-footer-brand" });
    if (props.brand != null) brandCol.append(helpers.renderNode(props.brand));
    const tagline = asString(props.tagline);
    if (tagline) brandCol.append(el("p", { class: "rui-footer-tagline" }, [tagline]));
    const social = asArray(props.social);
    if (social.length > 0) {
      const row = el("div", { class: "rui-footer-social" });
      for (const s of social) row.append(helpers.renderNode(s));
      brandCol.append(row);
    }
    grid.append(brandCol);
    for (const c of cols) grid.append(helpers.renderNode(c));
    root.append(grid);
    const legalIsEmpty = props.legal == null || typeof props.legal === "string" && props.legal === "";
    if (!legalIsEmpty) {
      const legal = el("div", { class: "rui-footer-legal" });
      appendInline(legal, props.legal, helpers);
      root.append(legal);
    }
    return root;
  }
};
const LogoChip = {
  name: "LogoChip",
  description: "A single labelled chip for a LogoCloud: a Font Awesome `icon` or a customer wordmark `image`, plus the name. Give it an `href` to link the chip at its integration/partner page.",
  props: [
    { name: "label", type: "string", positional: true, required: true },
    { name: "icon", type: "string", optional: true, description: "Font Awesome name (e.g. brands:react)" },
    { name: "imageSrc", type: "string", optional: true, aliases: ["logo", "image", "src"], description: "Logo image src URL (wins over `icon`)" },
    { name: "href", type: "string", optional: true, description: "Link target — the chip's hover lift already advertises interactivity" }
  ],
  render: (_node, props) => {
    const label = asString(props.label);
    const href = props.href != null ? sanitiseHref(props.href, "") : "";
    const chip = href ? el("a", { class: "rui-logochip", href }) : el("span", { class: "rui-logochip" });
    const image = sanitiseImageSrc(props.imageSrc);
    if (image) {
      chip.append(el("img", { class: "rui-logochip-logo", src: image, alt: label, loading: "lazy" }));
    } else {
      const icon = renderIcon(props.icon);
      if (icon) chip.append(icon);
    }
    chip.append(document.createTextNode(label));
    return chip;
  }
};
const LogoCloud = {
  name: "LogoCloud",
  description: "A centered, wrapping row of LogoChip items with an optional label — 'works with' / 'trusted by' bands.",
  props: [
    { name: "items", type: "LogoChip[]", positional: true, required: true, aliases: ["children", "chips"] },
    { name: "label", type: "string", optional: true }
  ],
  render: (_node, props, helpers) => {
    const root = el("div", { class: "rui-logocloud" });
    const label = asString(props.label);
    if (label) root.append(el("p", { class: "rui-logocloud-label" }, [label]));
    const row = el("div", { class: "rui-logocloud-row" });
    for (const item of asArray(props.items)) row.append(helpers.renderNode(item));
    root.append(row);
    return root;
  }
};
const COUNTUP_WIRED = /* @__PURE__ */ new WeakSet();
function decimalsOf(value) {
  if (!Number.isFinite(value) || Number.isInteger(value)) return 0;
  const text = String(value);
  const dot = text.indexOf(".");
  return dot === -1 ? 0 : Math.min(4, text.length - dot - 1);
}
function formatCountValue(value, cfg2) {
  const decimals = cfg2.decimals;
  if (cfg2.group) {
    try {
      return new Intl.NumberFormat(void 0, {
        minimumFractionDigits: decimals,
        maximumFractionDigits: decimals
      }).format(value);
    } catch {
    }
  }
  return decimals > 0 ? value.toFixed(decimals) : String(Math.round(value));
}
function wireCountUp(node, cfg2, helpers, slotKey) {
  const valueSlot = helpers.useInstanceState(`${slotKey}-value`, 0);
  const paint = (host, v) => {
    host.textContent = `${cfg2.prefix}${formatCountValue(v, cfg2)}${cfg2.suffix}`;
  };
  paint(node, valueSlot.get());
  const run = (live) => {
    const start = performance.now();
    const tick = (now) => {
      const t = Math.min(1, (now - start) / cfg2.duration);
      const eased = 1 - Math.pow(1 - t, 3);
      const v = cfg2.target * eased;
      valueSlot.set(v);
      paint(live, v);
      if (t < 1 && live.isConnected) requestAnimationFrame(tick);
    };
    requestAnimationFrame(tick);
  };
  const cancel = deferToPaint(() => {
    if (!node.isConnected || COUNTUP_WIRED.has(node)) return;
    COUNTUP_WIRED.add(node);
    if (typeof IntersectionObserver === "undefined") {
      run(node);
      return;
    }
    const io = new IntersectionObserver((entries) => {
      for (const e of entries) {
        if (e.isIntersecting) {
          io.disconnect();
          run(node);
        }
      }
    }, { threshold: 0.4 });
    io.observe(node);
    helpers.registerDisposer(() => io.disconnect(), `${slotKey}-io`);
  });
  helpers.registerDisposer(() => {
    if (!node.isConnected) cancel();
  }, `${slotKey}-defer`);
}
const CountUp = {
  name: "CountUp",
  description: "A number that animates from 0 to `value` when scrolled into view. `prefix`/`suffix` wrap it (e.g. `$`, `+`, `%`). `decimals` keeps fraction digits (default: as many as `value` has, so 99.9 stays 99.9). Respects reduced motion.",
  props: [
    { name: "value", type: "number", positional: true, required: true },
    { name: "suffix", type: "string", optional: true },
    { name: "prefix", type: "string", optional: true },
    { name: "duration", type: "number", optional: true, description: "Animation ms (default 1000)" },
    { name: "decimals", type: "number", optional: true, description: "Fraction digits to display (default: inferred from `value`)" }
  ],
  render: (_node, props, helpers) => {
    const span = el("span", { class: "rui-countup" });
    const target = asNumber(props.value, 0);
    const cfg2 = {
      target,
      prefix: asString(props.prefix),
      suffix: asString(props.suffix),
      duration: Math.max(200, asNumber(props.duration, 1e3)),
      decimals: props.decimals != null ? Math.max(0, Math.min(4, Math.round(asNumber(props.decimals, 0)))) : decimalsOf(target),
      group: false
    };
    const reduce = typeof matchMedia !== "undefined" && matchMedia("(prefers-reduced-motion: reduce)").matches;
    if (reduce) {
      span.textContent = `${cfg2.prefix}${formatCountValue(target, cfg2)}${cfg2.suffix}`;
    } else {
      wireCountUp(span, cfg2, helpers, "rui-countup");
    }
    return span;
  }
};
function splitMetricValue(raw) {
  const match = /^(\D*?)(\d[\d.,\s\u00A0\u202F]*)([\s\S]*)$/.exec(raw.trim());
  if (!match) return { prefix: "", numeric: NaN, suffix: "", decimals: 0, group: false };
  const prefix = match[1] ?? "";
  let digits = match[2] ?? "";
  let suffix = match[3] ?? "";
  while (digits.length > 1 && /[.,\s\u00A0\u202F]$/.test(digits)) {
    suffix = digits.slice(-1) + suffix;
    digits = digits.slice(0, -1);
  }
  const group = /\d[,\s\u00A0\u202F]\d/.test(digits);
  const plain = digits.replace(/[,\s\u00A0\u202F]/g, "");
  const dot = plain.indexOf(".");
  return {
    prefix,
    numeric: Number(plain),
    suffix,
    decimals: dot === -1 ? 0 : Math.min(4, plain.length - dot - 1),
    group
  };
}
const Metric = {
  name: "Metric",
  description: "A single big-number metric tile: the value (gradient-painted by default), an optional label and an optional `trend` delta. Use inside MetricStrip. Set `countUp` to animate the number in on scroll — currency prefixes, thousands separators and decimals survive the animation.",
  props: [
    { name: "value", type: "string | number", positional: true, required: true },
    { name: "label", type: "string", optional: true },
    { name: "gradient", type: "boolean", optional: true, description: "Paint the number with the brand gradient" },
    { name: "countUp", type: "boolean", optional: true, description: "Animate numeric value from 0 on scroll-in" },
    { name: "duration", type: "number", optional: true, description: "countUp animation ms (default 1000)" },
    { name: "trend", type: "string", optional: true, aliases: ["delta", "change"], description: 'Period-over-period delta, e.g. "+12.5%" or "-3 vs last week"' }
  ],
  render: (_node, props, helpers) => {
    const root = el("div", { class: "rui-metric" });
    const gradient = props.gradient === void 0 ? true : asBoolean(props.gradient);
    const valueEl = el("div", { class: "rui-metric-value", "data-gradient": gradient ? "true" : null });
    const raw = asString(props.value);
    const parts = splitMetricValue(raw);
    const reduce = typeof matchMedia !== "undefined" && matchMedia("(prefers-reduced-motion: reduce)").matches;
    if (asBoolean(props.countUp) && Number.isFinite(parts.numeric) && parts.numeric > 0 && !reduce) {
      wireCountUp(valueEl, {
        target: parts.numeric,
        prefix: parts.prefix,
        suffix: parts.suffix,
        duration: Math.max(200, asNumber(props.duration, 1e3)),
        decimals: parts.decimals,
        group: parts.group
      }, helpers, "rui-metric-countup");
    } else {
      valueEl.textContent = raw;
    }
    root.append(valueEl);
    const label = asString(props.label);
    if (label) root.append(el("div", { class: "rui-metric-label" }, [label]));
    const trend = asString(props.trend);
    if (trend) {
      const dir = /^\s*[+↑▲]/.test(trend) ? "up" : /^\s*[-−↓▼]/.test(trend) ? "down" : null;
      root.append(el("div", { class: "rui-metric-trend", "data-dir": dir }, [trend]));
    }
    return root;
  }
};
const MetricStrip = {
  name: "MetricStrip",
  description: "A responsive row/grid of Metric tiles. `columns` sets the preferred count.",
  props: [
    { name: "items", type: "Metric[]", positional: true, required: true, aliases: ["children", "metrics"] },
    { name: "columns", type: "number", optional: true }
  ],
  render: (_node, props, helpers) => {
    const cols = Math.max(1, Math.min(6, Math.round(asNumber(props.columns, 4))));
    const root = el("div", { class: "rui-metricstrip", style: `--ak-metric-cols:${cols}` });
    for (const item of asArray(props.items)) root.append(helpers.renderNode(item));
    return root;
  }
};
function windowBar(file, status, helpers, action) {
  const bar = el("div", { class: "rui-window-bar" });
  const dots = el("span", { class: "rui-window-dots" });
  dots.append(el("i"), el("i"), el("i"));
  bar.append(dots);
  if (file) bar.append(el("span", { class: "rui-window-file" }, [file]));
  if (status != null) {
    const s = el("span", { class: "rui-window-status" });
    s.append(status instanceof Node ? status : helpers.renderNode(status));
    bar.append(s);
  }
  if (action) {
    const a = el("span", { class: "rui-window-action" });
    a.append(action);
    bar.append(a);
  }
  return bar;
}
function extractCodeString(codeNode) {
  if (typeof codeNode === "string") return codeNode;
  if (codeNode == null || typeof codeNode !== "object") return "";
  const node = codeNode;
  if (node.__kind !== "Component" || !Array.isArray(node.args)) return "";
  if (node.name === "CodeBlock") {
    return asString(mapPositionalArgs(CodeBlock, node.args).codeString);
  }
  return asString(node.args.find((a) => typeof a === "string"));
}
const FILE_LANGUAGES = {
  aktion: "aktion",
  js: "js",
  mjs: "js",
  cjs: "js",
  jsx: "jsx",
  ts: "ts",
  tsx: "tsx",
  py: "py",
  css: "css",
  scss: "scss",
  less: "less",
  json: "json",
  html: "html",
  xml: "xml",
  svg: "svg",
  vue: "vue",
  sh: "bash",
  bash: "bash",
  zsh: "bash",
  md: "md",
  yml: "yaml",
  yaml: "yaml",
  sql: "sql"
};
function languageFromFile(file) {
  const ext = /\.([a-z0-9]+)$/i.exec(file.trim())?.[1]?.toLowerCase();
  return ext ? FILE_LANGUAGES[ext] ?? "" : "";
}
const PREVIEW_THEME_WIRED = /* @__PURE__ */ new WeakSet();
const CodeWindow = {
  name: "CodeWindow",
  description: "An editor/IDE window chrome (traffic-light dots + filename + status area) around source code. Pass the code as a plain STRING — it renders through a chromeless CodeBlock that fills the pane (no inner border, language label, or copy button), syntax-highlighted via `language` (default: inferred from the `file` extension, else aktion). Set `preview: true` to run the source as a LIVE app beside the code (split view with a pulsing 'Live render' badge); a custom node is also accepted for non-Aktion previews. `copy` adds a copy-to-clipboard button to the title bar, `height`/`maxHeight` cap the pane (long snippets scroll), and `showLineNumbers`/`highlightLines` point the reader at a specific line. The canonical way to show code in docs/marketing.",
  props: [
    { name: "code", type: "string", positional: true, required: true, aliases: ["codeString"], description: "Source text (a legacy CodeBlock(...) node is also accepted)" },
    { name: "file", type: "string", optional: true, description: "Filename shown in the title bar" },
    { name: "language", type: "string", optional: true, description: "Highlight language (default: from the file extension, else aktion)" },
    { name: "status", type: "Node", optional: true, description: "A Badge shown at the right of the bar" },
    { name: "preview", type: "boolean | Node", optional: true, description: "true → run the source as a live Aktion preview; or a custom node" },
    { name: "copy", type: "boolean", optional: true, description: "Show a copy-the-source button in the title bar" },
    { name: "height", type: "string", optional: true, description: "Explicit code-pane height (CSS length); overflow scrolls" },
    { name: "maxHeight", type: "string", optional: true, description: "Cap the code-pane height (CSS length); overflow scrolls" },
    { name: "showLineNumbers", type: "boolean", optional: true, description: "Render a left-side line-number gutter" },
    { name: "highlightLines", type: "string", optional: true, description: 'Highlight ranges, e.g. "3-5,8"' }
  ],
  render: (_node, props, helpers) => {
    const file = asString(props.file);
    const legacyNode = props.code != null && typeof props.code === "object" ? props.code : null;
    const source = extractCodeString(props.code);
    let language = asString(props.language);
    if (!language && legacyNode?.name === "CodeBlock" && Array.isArray(legacyNode.args)) {
      language = asString(mapPositionalArgs(CodeBlock, legacyNode.args).language);
    }
    if (!language) language = languageFromFile(file) || "aktion";
    const livePreview = props.preview === true || props.preview === "true";
    const nodePreview = !livePreview && props.preview != null && props.preview !== false && props.preview !== "false" ? props.preview : null;
    const split = livePreview || nodePreview != null;
    const root = el("div", { class: "rui-codewindow" });
    const status = split && props.status == null ? el("span", { class: "rui-window-live" }, [el("span", { class: "rui-window-live-dot" }), "Live render"]) : props.status;
    const copyBtn = asBoolean(props.copy) && source ? CopyButton.render(
      { __kind: "Component", name: "CopyButton", args: [], argMeta: [] },
      { text: source, label: "Copy code", iconOnly: true },
      helpers
    ) : null;
    root.append(windowBar(file, status, helpers, copyBtn));
    const height = sanitiseCssLength(props.height, "");
    const maxHeight = sanitiseCssLength(props.maxHeight, "");
    const body = el("div", { class: "rui-codewindow-body", "data-split": split ? "true" : null });
    const codeCol = el("div", {
      class: "rui-codewindow-code",
      style: maxHeight ? `max-height:${maxHeight};overflow:auto` : null
    });
    if (source || legacyNode == null) {
      codeCol.append(CodeBlock.render(
        { __kind: "Component", name: "CodeBlock", args: [], argMeta: [] },
        {
          codeString: source,
          language,
          header: false,
          height: height || null,
          showLineNumbers: props.showLineNumbers,
          highlightLines: props.highlightLines
        },
        helpers
      ));
    } else {
      codeCol.append(helpers.renderNode(legacyNode));
    }
    body.append(codeCol);
    if (split) {
      const prev = el("div", { class: "rui-codewindow-preview" });
      if (livePreview) {
        prev.append(buildLivePreview(source, root, helpers));
      } else {
        prev.append(helpers.renderNode(nodePreview));
      }
      body.append(prev);
    }
    root.append(body);
    return root;
  }
};
function buildLivePreview(source, root, helpers) {
  const app = document.createElement("aktion-app");
  app.setAttribute("response", source.replace(/preview\s*:\s*true\b/g, "preview: false"));
  const themeSlot = helpers.useInstanceState("rui-codewindow-theme", "");
  if (themeSlot.get()) app.setAttribute("theme", themeSlot.get());
  setTimeout(() => {
    if (!app.isConnected || PREVIEW_THEME_WIRED.has(app)) return;
    PREVIEW_THEME_WIRED.add(app);
    const shadow = root.getRootNode();
    const host = shadow instanceof ShadowRoot ? shadow.host : null;
    if (!host) return;
    const sync = () => {
      const theme = host.getAttribute("theme") ?? "";
      themeSlot.set(theme);
      if (theme) app.setAttribute("theme", theme);
      else app.removeAttribute("theme");
    };
    sync();
    const observer = new MutationObserver(sync);
    observer.observe(host, { attributes: true, attributeFilter: ["theme"] });
    helpers.registerDisposer(() => observer.disconnect(), "rui-codewindow-theme-sync");
  }, 0);
  return app;
}
const BrowserFrame = {
  name: "BrowserFrame",
  description: "A browser-window chrome (dots + URL bar) wrapping a child — for screenshots/demos of web UIs. `height` fixes the mock viewport and scrolls inside it. The frame does NOT clip its child, so dropdowns/tooltips in the wrapped UI stay visible; set `clip: true` for a hard-edged screenshot look.",
  props: [
    { name: "child", type: "Node", positional: true, required: true, aliases: ["children", "content"] },
    { name: "url", type: "string", optional: true },
    { name: "height", type: "string", optional: true, description: "Mock viewport height (CSS length); the body scrolls" },
    { name: "clip", type: "boolean", optional: true, description: "Clip the child at the frame edge (hides popovers that overflow it)" }
  ],
  render: (_node, props, helpers) => {
    const clip = asBoolean(props.clip);
    const root = el("div", { class: "rui-browserframe", "data-clip": clip ? "true" : null });
    const bar = el("div", { class: "rui-browserframe-bar" });
    const dots = el("span", { class: "rui-window-dots" });
    dots.append(el("i"), el("i"), el("i"));
    bar.append(dots);
    bar.append(el("span", { class: "rui-browserframe-url" }, [asString(props.url, "example.com")]));
    root.append(bar);
    const height = sanitiseCssLength(props.height, "");
    const body = el("div", {
      class: "rui-browserframe-body",
      style: height ? `height:${height};overflow:auto` : null
    });
    if (props.child != null) body.append(helpers.renderNode(props.child));
    root.append(body);
    return root;
  }
};
const Terminal = {
  name: "Terminal",
  description: 'A terminal window rendering monospace lines. Pass `lines` as an array of strings. `prompt` (e.g. "$" or "❯") prefixes each command line with a non-selectable marker — indented lines count as output and get none, so copying the block yields just the commands. `height`/`maxHeight` cap a long log and scroll it.',
  props: [
    { name: "lines", type: "string[]", positional: true, required: true, aliases: ["children", "content"] },
    { name: "file", type: "string", optional: true, description: "Title-bar label (default 'bash')" },
    { name: "prompt", type: "string", optional: true, description: 'Prompt marker for command lines (e.g. "$"); indented lines are treated as output' },
    { name: "height", type: "string", optional: true, description: "Explicit body height (CSS length); overflow scrolls" },
    { name: "maxHeight", type: "string", optional: true, description: "Cap the body height (CSS length); overflow scrolls" }
  ],
  render: (_node, props, helpers) => {
    const file = asString(props.file, "bash");
    const root = el("div", { class: "rui-terminal", role: "group", "aria-label": file || "Terminal output" });
    root.append(windowBar(file, null, helpers));
    const height = sanitiseCssLength(props.height, "");
    const maxHeight = sanitiseCssLength(props.maxHeight, "");
    const sizeStyle = [
      height ? `height:${height}` : "",
      maxHeight ? `max-height:${maxHeight}` : "",
      height || maxHeight ? "overflow:auto" : ""
    ].filter(Boolean).join(";");
    const body = el("pre", { class: "rui-terminal-body", style: sizeStyle || null });
    const lines = asArray(props.lines).map((l) => asString(l));
    const prompt = asString(props.prompt);
    if (!prompt) {
      body.textContent = lines.join("\n");
    } else {
      lines.forEach((line, i) => {
        const row = el("span", { class: "rui-terminal-line" });
        if (!/^\s/.test(line) && line !== "") {
          row.append(el("span", { class: "rui-terminal-prompt", "aria-hidden": "true" }, [`${prompt} `]));
        }
        row.append(document.createTextNode(line));
        body.append(row);
        if (i < lines.length - 1) body.append(document.createTextNode("\n"));
      });
    }
    root.append(body);
    return root;
  }
};
const BACKDROP_BLOB_POS = [
  { t: "-160px", l: "-120px", d: "0s" },
  { t: "-80px", r: "-120px", d: "-6s" },
  { t: "46%", l: "34%", d: "-12s" }
];
const BACKDROP_TYPES = ["network", "drift", "snow", "stars", "bubbles"];
const BACKDROP_PALETTE = ["#6366f1", "#8b5cf6", "#ec4899", "#22d3ee"];
const safeCssColor = (c, fallback) => /^[#a-zA-Z0-9(),.%\s-]+$/.test(c) && c.length <= 64 && c.trim() !== "" ? c.trim() : fallback;
const BACKDROP_ENGINES = /* @__PURE__ */ new WeakSet();
function startBackdropEngine(canvas, helpers) {
  if (!canvas.isConnected || BACKDROP_ENGINES.has(canvas)) return;
  const ctx = canvas.getContext("2d");
  if (!ctx) return;
  BACKDROP_ENGINES.add(canvas);
  const reduce = typeof matchMedia !== "undefined" && matchMedia("(prefers-reduced-motion: reduce)").matches;
  let particles = [];
  let w = 0;
  let h = 0;
  let dpr = 1;
  let signature = "";
  let raf = 0;
  let tick = 0;
  let boxEmpty = true;
  const readConfig = () => {
    const count = Math.max(0, Math.min(120, Math.round(Number(canvas.dataset.count) || 0)));
    const typeRaw = canvas.dataset.type ?? "network";
    const type = BACKDROP_TYPES.includes(typeRaw) ? typeRaw : "network";
    const speed = Math.max(0.1, Math.min(4, Number(canvas.dataset.speed) || 1));
    const link = Math.max(40, Math.min(240, Number(canvas.dataset.link) || 130));
    const size2 = Math.max(0.5, Math.min(6, Number(canvas.dataset.size) || 2.2));
    const colors = (canvas.dataset.colors ?? "").split(",").map((c) => c.trim()).filter(Boolean);
    return { count, type, speed, link, size: size2, colors: colors.length > 0 ? colors : BACKDROP_PALETTE };
  };
  const seed = () => {
    const cfg2 = readConfig();
    particles = Array.from({ length: cfg2.count }, (_unused, i) => ({
      x: Math.random() * w,
      y: Math.random() * h,
      vx: (Math.random() - 0.5) * 0.5 * dpr,
      vy: (Math.random() - 0.5) * 0.5 * dpr,
      r: (Math.random() * (cfg2.size - 0.5) + 0.5) * dpr,
      c: cfg2.colors[i % cfg2.colors.length],
      phase: Math.random() * Math.PI * 2
    }));
  };
  const resize = () => {
    const host = canvas.parentElement ?? canvas;
    const rect = host.getBoundingClientRect();
    dpr = Math.min((typeof window !== "undefined" ? window.devicePixelRatio : 1) || 1, 2);
    boxEmpty = rect.width * rect.height === 0;
    w = Math.max(1, Math.round(rect.width * dpr));
    h = Math.max(1, Math.round(rect.height * dpr));
    canvas.width = w;
    canvas.height = h;
    if (boxEmpty) return;
    seed();
    if (reduce) drawFrame();
  };
  const drawFrame = () => {
    if (boxEmpty) return;
    const cfg2 = readConfig();
    const sig = `${cfg2.count}|${cfg2.type}|${cfg2.colors.join(",")}`;
    if (sig !== signature) {
      signature = sig;
      seed();
    }
    if (canvas.width !== w) canvas.width = w;
    if (canvas.height !== h) canvas.height = h;
    ctx.clearRect(0, 0, w, h);
    tick += 1;
    const sp = cfg2.speed;
    for (const p of particles) {
      switch (cfg2.type) {
        case "drift":
          p.x += p.vx * 0.5 * sp;
          p.y += p.vy * 0.5 * sp;
          if (p.x < -10) p.x = w + 10;
          if (p.x > w + 10) p.x = -10;
          if (p.y < -10) p.y = h + 10;
          if (p.y > h + 10) p.y = -10;
          ctx.globalAlpha = 0.35 + 0.3 * Math.abs(Math.sin(tick / 60 + p.phase));
          break;
        case "snow":
          p.y += (0.4 + Math.abs(p.vy)) * sp;
          p.x += Math.sin(tick / 50 + p.phase) * 0.4 * dpr;
          if (p.y > h + 6) {
            p.y = -6;
            p.x = Math.random() * w;
          }
          ctx.globalAlpha = 0.8;
          break;
        case "stars":
          ctx.globalAlpha = 0.15 + 0.65 * Math.abs(Math.sin(tick / 40 * sp + p.phase));
          break;
        case "bubbles":
          p.y -= (0.5 + Math.abs(p.vy)) * sp;
          p.x += Math.sin(tick / 40 + p.phase) * 0.5 * dpr;
          if (p.y < -10) {
            p.y = h + 10;
            p.x = Math.random() * w;
          }
          ctx.globalAlpha = 0.5;
          break;
        default:
          p.x += p.vx * sp;
          p.y += p.vy * sp;
          if (p.x < 0 || p.x > w) p.vx *= -1;
          if (p.y < 0 || p.y > h) p.vy *= -1;
          ctx.globalAlpha = 0.7;
      }
      ctx.beginPath();
      ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);
      if (cfg2.type === "bubbles") {
        ctx.strokeStyle = p.c;
        ctx.lineWidth = dpr;
        ctx.stroke();
        ctx.globalAlpha = 0.08;
        ctx.fillStyle = p.c;
        ctx.fill();
      } else {
        ctx.fillStyle = p.c;
        ctx.fill();
      }
    }
    if (cfg2.type === "network") {
      const link = cfg2.link * dpr;
      for (let i = 0; i < particles.length; i += 1) {
        for (let j = i + 1; j < particles.length; j += 1) {
          const a = particles[i];
          const b = particles[j];
          const dist = Math.hypot(a.x - b.x, a.y - b.y);
          if (dist < link) {
            ctx.beginPath();
            ctx.moveTo(a.x, a.y);
            ctx.lineTo(b.x, b.y);
            ctx.strokeStyle = a.c;
            ctx.globalAlpha = (1 - dist / link) * 0.18;
            ctx.lineWidth = dpr * 0.6;
            ctx.stroke();
          }
        }
      }
    }
    ctx.globalAlpha = 1;
  };
  const loop = () => {
    raf = 0;
    if (!canvas.isConnected) {
      BACKDROP_ENGINES.delete(canvas);
      return;
    }
    drawFrame();
    raf = requestAnimationFrame(loop);
  };
  resize();
  if (reduce) {
    drawFrame();
  } else {
    raf = requestAnimationFrame(loop);
  }
  const observer = typeof ResizeObserver !== "undefined" ? new ResizeObserver(resize) : null;
  observer?.observe(canvas.parentElement ?? canvas);
  helpers.registerDisposer(() => {
    if (raf) cancelAnimationFrame(raf);
    observer?.disconnect();
    BACKDROP_ENGINES.delete(canvas);
  }, "rui-backdrop-engine");
}
const Backdrop = {
  name: "Backdrop",
  description: "Decorative background layer for hero/marketing sections: a masked grid, floating blurred color 'blobs', and an animated canvas particle field. `type` picks the particle behaviour — `network` (drifting dots joined by constellation lines, the default), `drift`, `snow`, `stars`, or `bubbles` — tuned via `particleColors`, `speed`, `linkDistance`, and `particleSize`. All theme-aware and motion-safe. Place as the first child of a Section (which is `position: relative`).",
  props: [
    { name: "grid", type: "boolean", optional: true, description: "Show the masked line grid" },
    { name: "blobs", type: "string[]", optional: true, description: "Colors for floating blur orbs (CSS colors)" },
    { name: "particles", type: "number", optional: true, description: "Particle count (0 = none, max 120)" },
    { name: "type", type: "string", optional: true, enum: [...BACKDROP_TYPES], aliases: ["particleType"], description: "Particle behaviour (default network)" },
    { name: "particleColors", type: "string[]", optional: true, aliases: ["colors"], description: "Particle palette (CSS colors; defaults to the brand palette)" },
    { name: "speed", type: "number", optional: true, description: "Animation speed multiplier 0.1–4 (default 1)" },
    { name: "linkDistance", type: "number", optional: true, description: "Max px between linked network particles (default 130)" },
    { name: "particleSize", type: "number", optional: true, description: "Max particle radius in px (default 2.2)" },
    { name: "fixed", type: "boolean", optional: true, description: "Pin to the viewport instead of the section" }
  ],
  render: (_node, props, helpers) => {
    const root = el("div", { class: "rui-backdrop", "data-fixed": asBoolean(props.fixed) ? "true" : null, "aria-hidden": "true" });
    if (asBoolean(props.grid)) root.append(el("div", { class: "rui-backdrop-grid" }));
    const blobs = asArray(props.blobs).slice(0, 3);
    blobs.forEach((c, i) => {
      const color2 = asString(c);
      const pos = BACKDROP_BLOB_POS[i];
      const size2 = 460 - i * 40;
      const style = [
        `width:${size2}px`,
        `height:${size2}px`,
        pos.t ? `top:${pos.t}` : "",
        pos.l ? `left:${pos.l}` : "",
        pos.r ? `right:${pos.r}` : "",
        `background:radial-gradient(circle, ${safeCssColor(color2, "#6366f1")}, transparent 70%)`,
        `animation-delay:${pos.d}`
      ].filter(Boolean).join(";");
      root.append(el("div", { class: "rui-backdrop-blob", style }));
    });
    const count = Math.max(0, Math.min(120, Math.round(asNumber(props.particles, 0))));
    if (count > 0) {
      const typeRaw = asString(props.type, "network");
      const palette = asArray(props.particleColors).map((c) => safeCssColor(asString(c), "")).filter(Boolean).slice(0, 8);
      const canvas = el("canvas", {
        class: "rui-backdrop-canvas",
        "data-count": String(count),
        "data-type": BACKDROP_TYPES.includes(typeRaw) ? typeRaw : "network",
        "data-speed": String(Math.max(0.1, Math.min(4, asNumber(props.speed, 1)))),
        "data-link": String(Math.max(40, Math.min(240, asNumber(props.linkDistance, 130)))),
        "data-size": String(Math.max(0.5, Math.min(6, asNumber(props.particleSize, 2.2)))),
        "data-colors": (palette.length > 0 ? palette : BACKDROP_PALETTE).join(",")
      });
      root.append(canvas);
      setTimeout(() => startBackdropEngine(canvas, helpers), 0);
    }
    return root;
  }
};
const ThemeToggle = {
  name: "ThemeToggle",
  description: "A sun/moon button that toggles the host between light and dark themes — no host glue required. It flips the <aktion-app> `theme` attribute and dispatches a `theme-change` event the host can listen for.",
  props: [
    { name: "light", type: "string", optional: true, description: "Theme name when toggled to light (default 'light')" },
    { name: "dark", type: "string", optional: true, description: "Theme name when toggled to dark (default 'dark')" }
  ],
  render: (_node, props, helpers) => {
    const lightName = asString(props.light, "light");
    const darkName = asString(props.dark, "dark");
    const darkSlot = helpers.useInstanceState("rui-theme-dark", false);
    const isDark = darkSlot.get();
    const btn = el("button", {
      class: "rui-theme-toggle",
      type: "button",
      "aria-pressed": isDark ? "true" : "false",
      "aria-label": isDark ? "Switch to light theme" : "Switch to dark theme",
      title: "Toggle theme"
    });
    const icon = renderIcon(isDark ? "sun" : "moon");
    if (icon) btn.append(icon);
    const hostFrom = (node) => {
      const root = node?.getRootNode?.();
      return root && root.host ? root.host : void 0;
    };
    const paint = (live, dark) => {
      live.setAttribute("aria-pressed", dark ? "true" : "false");
      live.setAttribute("aria-label", dark ? "Switch to light theme" : "Switch to dark theme");
      live.replaceChildren(renderIcon(dark ? "sun" : "moon") ?? document.createTextNode(""));
    };
    deferToPaint(() => {
      if (!btn.isConnected) return;
      const host = hostFrom(btn);
      const dark = (host?.getAttribute("theme") || "").toLowerCase().includes("dark");
      if (dark === darkSlot.get()) return;
      darkSlot.set(dark);
      paint(btn, dark);
    });
    btn.onclick = (event) => {
      const target = event.currentTarget ?? event.target;
      const host = hostFrom(target);
      if (!host) return;
      const cur = (host.getAttribute("theme") || lightName).toLowerCase();
      const next = cur.includes("dark") ? lightName : darkName;
      host.setAttribute("theme", next);
      host.dispatchEvent(new CustomEvent("theme-change", { detail: { theme: next }, bubbles: true, composed: true }));
      const dark = next.toLowerCase().includes("dark");
      darkSlot.set(dark);
      if (target) paint(target, dark);
    };
    return btn;
  }
};
const Swatch = {
  name: "Swatch",
  description: "A theme/palette preview tile — color dots over a named background. For theming galleries: give it `onClick` to make the tile pick that theme and `selected` to mark the active one.",
  props: [
    { name: "name", type: "string", positional: true, required: true },
    { name: "background", type: "string", optional: true, description: "Tile background CSS color" },
    { name: "foreground", type: "string", optional: true, description: "Tile text CSS color" },
    { name: "colors", type: "string[]", optional: true, description: "Accent dot colors" },
    { name: "onClick", type: "callable", optional: true, aliases: ["onSelect", "onclick"], description: "Fired when the tile is picked" },
    { name: "selected", type: "boolean", optional: true, aliases: ["active"], description: "Mark this tile as the active theme" }
  ],
  render: (_node, props, helpers) => {
    const safe = (c, fb) => /^[#a-zA-Z0-9(),.%\s-]+$/.test(c) && c.length <= 64 ? c : fb;
    const bg2 = safe(asString(props.background, "#ffffff"), "#ffffff");
    const fg = safe(asString(props.foreground, "#0f172a"), "#0f172a");
    const name = asString(props.name);
    const selected = asBoolean(props.selected);
    const interactive = props.onClick != null;
    const attrs = {
      class: "rui-swatch",
      style: `background:${bg2};color:${fg}`,
      "data-selected": selected ? "true" : null
    };
    const root = interactive ? el("button", { ...attrs, type: "button", "aria-pressed": selected ? "true" : "false" }) : el("div", attrs);
    if (interactive) {
      root.onclick = () => helpers.invoke(props.onClick, name);
    }
    const dots = el("div", { class: "rui-swatch-dots" });
    for (const c of asArray(props.colors).slice(0, 5)) {
      const color2 = safe(asString(c), "#6366f1");
      dots.append(el("i", { style: `background:${color2}`, role: "img", "aria-label": color2, title: color2 }));
    }
    root.append(dots);
    root.append(el("div", { class: "rui-swatch-name" }, [name]));
    return root;
  }
};
const CopyButton = {
  name: "CopyButton",
  description: "A button that copies `text` to the clipboard and confirms it — the label flips to `copiedLabel` (default 'Copied!') with a check icon for a couple of seconds, then reverts, and the confirmation is announced to screen readers. `iconOnly` drops the label for tight corners (code blocks, API-key fields). Bounded, no host glue.",
  props: [
    { name: "text", type: "string", positional: true, required: true, aliases: ["value"] },
    { name: "label", type: "string", optional: true, description: "Button label (default 'Copy')" },
    { name: "copiedLabel", type: "string", optional: true, description: "Confirmation label (default 'Copied!')" },
    { name: "iconOnly", type: "boolean", optional: true, aliases: ["variant"], description: "Render the icon alone; `label` becomes the accessible name" }
  ],
  render: (_node, props, helpers) => {
    const label = asString(props.label, "Copy");
    const copiedLabel = asString(props.copiedLabel, "Copied!");
    const iconOnly = asBoolean(props.iconOnly);
    const copiedSlot = helpers.useInstanceState("rui-copy-copied", false);
    const copied = copiedSlot.get();
    const wrap = el("span", { class: "rui-copy-button-wrap" });
    const btn = el("button", {
      class: "rui-copy-button",
      type: "button",
      "data-copied": copied ? "true" : null,
      "data-icon-only": iconOnly ? "true" : null,
      "aria-label": iconOnly ? copied ? copiedLabel : label : null
    });
    const icon = renderIcon(copied ? "check" : "copy", { className: "rui-copy-button-icon" });
    if (icon) btn.append(icon);
    if (!iconOnly) btn.append(el("span", { class: "rui-copy-button-label" }, [copied ? copiedLabel : label]));
    const status = el("span", {
      class: "rui-copy-button-status rui-visually-hidden",
      role: "status",
      "aria-live": "polite"
    }, [copied ? copiedLabel : ""]);
    wrap.append(btn, status);
    btn.onclick = (event) => {
      const live = event.currentTarget ?? event.target ?? btn;
      const text = asString(props.text);
      const fallbackCopy = () => {
        try {
          const ta = document.createElement("textarea");
          ta.value = text;
          ta.style.cssText = "position:fixed;top:0;left:0;opacity:0;pointer-events:none";
          document.body.append(ta);
          ta.select();
          document.execCommand("copy");
          ta.remove();
        } catch {
        }
      };
      try {
        const written = navigator.clipboard?.writeText(text);
        if (written && typeof written.catch === "function") written.catch(fallbackCopy);
        else if (!navigator.clipboard) fallbackCopy();
      } catch {
        fallbackCopy();
      }
      const paint = (isCopied) => {
        if (isCopied) live.setAttribute("data-copied", "true");
        else live.removeAttribute("data-copied");
        if (iconOnly) live.setAttribute("aria-label", isCopied ? copiedLabel : label);
        const labelEl = live.querySelector(".rui-copy-button-label");
        if (labelEl) labelEl.textContent = isCopied ? copiedLabel : label;
        const iconEl = live.querySelector(".rui-copy-button-icon");
        if (iconEl) {
          const next = renderIcon(isCopied ? "check" : "copy", { className: "rui-copy-button-icon" });
          if (next) iconEl.replaceWith(next);
        }
        const region = live.parentElement?.querySelector(".rui-copy-button-status");
        if (region) region.textContent = isCopied ? copiedLabel : "";
      };
      copiedSlot.set(true);
      paint(true);
      const timer = setTimeout(() => {
        copiedSlot.set(false);
        paint(false);
      }, 2e3);
      helpers.registerDisposer(() => clearTimeout(timer), "rui-copy-reset");
    };
    return wrap;
  }
};
const SegmentedControl = {
  name: "SegmentedControl",
  description: "A compact segmented toggle (iOS-style). `options` is an array of strings or {label, value, icon?, disabled?}. Bind `value` to a $variable; `onChange(value)` fires on select. Left/Right arrows move between segments; `size` sets the height and `disabled` locks the whole control.",
  props: [
    { name: "options", type: "any[]", positional: true, required: true, aliases: ["items"] },
    { name: "value", type: "string", optional: true },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"] },
    { name: "disabled", type: "boolean", optional: true, description: "Lock every segment" },
    { name: "size", type: "string", optional: true, enum: ["sm", "md", "lg"] },
    { name: "label", type: "string", optional: true, aliases: ["ariaLabel"], description: "Accessible name for the group" }
  ],
  render: (node, props, helpers) => {
    const groupDisabled = asBoolean(props.disabled);
    const root = el("div", {
      class: "rui-segmented rui-segmented-control",
      role: "group",
      "aria-label": asString(props.label) || null,
      "data-size": canonicalSizeToken(asString(props.size, "md")),
      "data-disabled": groupDisabled ? "true" : null
    });
    const current = asString(props.value);
    for (const raw of asArray(props.options)) {
      const opt = raw && typeof raw === "object" ? raw : { label: raw, value: raw, icon: void 0, disabled: void 0 };
      const value = asString(opt.value ?? opt.label);
      const label = asString(opt.label ?? opt.value);
      const active = value === current;
      const disabled = groupDisabled || asBoolean(opt.disabled);
      const btn = el("button", {
        class: "rui-segmented-control-option",
        type: "button",
        "aria-pressed": active ? "true" : "false",
        "data-active": active ? "true" : null,
        disabled: disabled ? "" : null
      });
      const icon = renderIcon(opt.icon);
      if (icon) btn.append(icon);
      if (label) btn.append(document.createTextNode(label));
      btn.onclick = () => {
        if (disabled) return;
        const meta = node.argMeta?.[1];
        if (meta?.stateRef) helpers.setState(meta.stateRef, value);
        helpers.invoke(props.onChange, value);
      };
      root.append(btn);
    }
    root.onkeydown = (event) => {
      const keys = ["ArrowLeft", "ArrowRight", "ArrowUp", "ArrowDown", "Home", "End"];
      if (!keys.includes(event.key)) return;
      const live = event.currentTarget ?? event.target;
      if (!live) return;
      const options = [...live.querySelectorAll(".rui-segmented-control-option:not([disabled])")];
      if (options.length === 0) return;
      const origin = event.target?.closest(".rui-segmented-control-option");
      const from = origin ? options.indexOf(origin) : options.findIndex((b) => b.getAttribute("aria-pressed") === "true");
      let next = from;
      if (event.key === "Home") next = 0;
      else if (event.key === "End") next = options.length - 1;
      else if (event.key === "ArrowLeft" || event.key === "ArrowUp") next = (from <= 0 ? options.length : from) - 1;
      else next = (from + 1) % options.length;
      const target = options[next];
      if (!target) return;
      event.preventDefault();
      target.focus();
      target.click();
    };
    return root;
  }
};
const FAB_POSITIONS = ["bottom-right", "bottom-left", "bottom-center"];
const fabPosition = (raw) => {
  const value = asString(raw, "bottom-right");
  return FAB_POSITIONS.includes(value) ? value : "bottom-right";
};
const FloatingActionButton = {
  name: "FloatingActionButton",
  description: "A fixed circular action button (FAB). `icon` + `onClick`; `position` picks the corner (bottom-right default — move it to sit beside a BackToTop or a chat launcher, or for RTL), `extended: true` shows `label` as visible text next to the icon, and `disabled` locks it while a request is in flight.",
  props: [
    { name: "icon", type: "string", positional: true, required: true },
    { name: "label", type: "string", optional: true, description: "Accessible label (visible text when `extended`)" },
    { name: "onClick", type: "callable", optional: true, aliases: ["action", "onclick"] },
    { name: "position", type: "string", optional: true, enum: FAB_POSITIONS, description: "Screen corner (default bottom-right)" },
    { name: "extended", type: "boolean", optional: true, description: "Pill-shaped icon + text FAB" },
    { name: "disabled", type: "boolean", optional: true }
  ],
  render: (_node, props, helpers) => {
    const label = asString(props.label, "Action");
    const extended = asBoolean(props.extended);
    const disabled = asBoolean(props.disabled);
    const btn = el("button", {
      class: "rui-fab",
      type: "button",
      "data-position": fabPosition(props.position),
      "data-extended": extended ? "true" : null,
      "aria-label": extended ? null : label,
      disabled: disabled ? "" : null
    });
    const icon = renderIcon(props.icon, { size: "lg" });
    if (icon) btn.append(icon);
    if (extended) btn.append(el("span", { class: "rui-fab-label" }, [label]));
    btn.onclick = () => {
      if (disabled) return;
      helpers.invoke(props.onClick);
    };
    return btn;
  }
};
const Prose = {
  name: "Prose",
  description: "A styled long-form reading container — applies typographic defaults to headings, lists, blockquotes, code, links inside. Wrap Markdown(...) or raw nodes for blog posts, docs bodies, product descriptions.",
  props: [
    { name: "children", type: "Node[]", positional: true, required: true, aliases: ["child", "content"] },
    { name: "size", type: "string", optional: true, enum: ["sm", "md", "lg"] }
  ],
  render: (_node, props, helpers) => {
    const root = el("div", { class: "rui-prose", "data-size": canonicalSizeToken(asString(props.size, "md")) });
    for (const child of asArray(props.children)) {
      if (typeof child === "string") root.append(document.createTextNode(child));
      else root.append(helpers.renderNode(child));
    }
    return root;
  }
};
const RELATIVE_UNITS = [
  ["year", 31536e6],
  ["month", 2592e6],
  ["week", 6048e5],
  ["day", 864e5],
  ["hour", 36e5],
  ["minute", 6e4],
  ["second", 1e3]
];
function relativeTimeLabel(date) {
  const diffMs = date.getTime() - Date.now();
  const abs = Math.abs(diffMs);
  try {
    const rtf = new Intl.RelativeTimeFormat(void 0, { numeric: "auto" });
    for (const [unit, ms] of RELATIVE_UNITS) {
      if (abs >= ms || unit === "second") return rtf.format(Math.trunc(diffMs / ms), unit);
    }
  } catch {
    return date.toLocaleString();
  }
  return "just now";
}
const RelativeTime = {
  name: "RelativeTime",
  description: "Renders a human relative time ('3m ago', 'in 2 days') from an ISO date/timestamp, localized via Intl. Emits a `<time datetime=…>` carrying the absolute instant and refreshes itself on a coarse schedule, so a list left open does not keep claiming 'just now'.",
  props: [
    { name: "value", type: "string | number", positional: true, required: true, aliases: ["date", "time"] }
  ],
  render: (_node, props, helpers) => {
    const raw = props.value;
    const date = typeof raw === "number" ? new Date(raw) : new Date(asString(raw));
    if (Number.isNaN(date.getTime())) {
      return el("span", { class: "rui-relative-time" }, [asString(raw)]);
    }
    const absolute = date.toLocaleString();
    const root = el("time", {
      class: "rui-relative-time",
      datetime: date.toISOString(),
      title: absolute
    });
    const labelEl = el("span", { class: "rui-relative-time-label" }, [relativeTimeLabel(date)]);
    root.append(labelEl, el("span", { class: "rui-visually-hidden" }, [` (${absolute})`]));
    const cancel = deferToPaint(() => {
      if (!root.isConnected) return;
      let timer;
      const run = () => {
        const live = root.querySelector(".rui-relative-time-label");
        if (!root.isConnected || !live) {
          clearTimeout(timer);
          return;
        }
        live.textContent = relativeTimeLabel(date);
        schedule();
      };
      const schedule = () => {
        const delay = Math.abs(date.getTime() - Date.now()) < 6e4 ? 1e3 : 3e4;
        timer = setTimeout(run, delay);
      };
      schedule();
      helpers.registerDisposer(() => clearTimeout(timer), "rui-relativetime");
    });
    helpers.registerDisposer(() => {
      if (!root.isConnected) cancel();
    }, "rui-relativetime-defer");
    return root;
  }
};
function priceNumber(value) {
  if (typeof value === "number") return Number.isFinite(value) ? value : null;
  const text = asString(value).trim();
  if (!text || !/^-?[\d.,\s]+$/.test(text)) return null;
  const n = Number(text.replace(/[,\s]/g, ""));
  return Number.isFinite(n) ? n : null;
}
const PriceTag = {
  name: "PriceTag",
  description: 'A formatted price with an optional struck-through compare-at price and a discount badge. Numeric values are grouped and given matching decimals (9.5 → $9.50, 1299 → $1,299); pre-formatted strings pass through untouched. `currencyPosition: "suffix"` writes "12,50 €"; `period` adds a trailing unit ("$29 /month").',
  props: [
    { name: "price", type: "string | number", positional: true, required: true },
    { name: "compareAt", type: "string | number", optional: true, aliases: ["was", "original"] },
    { name: "currency", type: "string", optional: true, description: "Currency symbol/prefix (default '$')" },
    { name: "size", type: "string", optional: true, enum: ["sm", "md", "lg"] },
    { name: "currencyPosition", type: "string", optional: true, enum: ["prefix", "suffix"], description: "Where the symbol sits (default prefix)" },
    { name: "period", type: "string", optional: true, aliases: ["suffix", "per"], description: 'Trailing unit, e.g. "month" → "$29 /month"' }
  ],
  render: (_node, props) => {
    const cur = asString(props.currency, "$");
    const suffixed = asString(props.currencyPosition, "prefix") === "suffix";
    const now = priceNumber(props.price);
    const was = priceNumber(props.compareAt);
    const cents = now != null && !Number.isInteger(now) || was != null && !Number.isInteger(was) ? 2 : 0;
    const fmt = (value, numeric) => {
      if (numeric == null) return `${suffixed ? "" : cur}${asString(value)}${suffixed ? ` ${cur}` : ""}`;
      let body;
      try {
        body = new Intl.NumberFormat(void 0, {
          minimumFractionDigits: cents,
          maximumFractionDigits: cents
        }).format(numeric);
      } catch {
        body = cents > 0 ? numeric.toFixed(cents) : String(numeric);
      }
      return suffixed ? `${body} ${cur}` : `${cur}${body}`;
    };
    const root = el("span", { class: "rui-pricetag", "data-size": canonicalSizeToken(asString(props.size, "md")) });
    const hasCompare = props.compareAt != null && asString(props.compareAt) !== "";
    if (hasCompare) root.append(el("span", { class: "rui-visually-hidden" }, ["Now "]));
    root.append(el("span", { class: "rui-pricetag-now" }, [fmt(props.price, now)]));
    const period = asString(props.period);
    if (period) {
      root.append(el("span", { class: "rui-pricetag-period" }, [period.startsWith("/") ? period : `/${period}`]));
    }
    if (hasCompare) {
      root.append(el("span", { class: "rui-visually-hidden" }, [" was "]));
      root.append(el("s", { class: "rui-pricetag-was" }, [fmt(props.compareAt, was)]));
      if (now != null && was != null && was > now && was > 0) {
        const pct = Math.round((1 - now / was) * 100);
        root.append(el("span", { class: "rui-pricetag-off" }, [`-${pct}%`]));
      }
    }
    return root;
  }
};
const QuantityStepper = {
  name: "QuantityStepper",
  description: "A −/value/+ numeric stepper. Bind `value` to a $variable; `onChange(value)` fires on change. Respects min/max (an out-of-range bound value is corrected once), exposes the value as a spinbutton with Arrow/Home/End keys, and `label` names it so several steppers in one cart are distinguishable.",
  props: [
    { name: "value", type: "number", positional: true, required: true },
    { name: "min", type: "number", optional: true },
    { name: "max", type: "number", optional: true },
    { name: "step", type: "number", optional: true },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"] },
    { name: "disabled", type: "boolean", optional: true, description: "Lock the whole control (read-only cart, out of stock)" },
    { name: "label", type: "string", optional: true, aliases: ["ariaLabel"], description: "What this quantity is for, e.g. the product name" },
    { name: "size", type: "string", optional: true, enum: ["sm", "md", "lg"] }
  ],
  render: (node, props, helpers) => {
    const min = props.min != null ? asNumber(props.min) : -Infinity;
    const max = props.max != null ? asNumber(props.max) : Infinity;
    const step = Math.abs(asNumber(props.step, 1)) || 1;
    const incoming = asNumber(props.value, 0);
    const current = Math.min(max, Math.max(min, incoming));
    const disabled = asBoolean(props.disabled);
    const label = asString(props.label);
    const ref = node.argMeta?.[0]?.stateRef;
    const root = el("div", { class: "rui-qty", "data-size": canonicalSizeToken(asString(props.size, "md")), "data-disabled": disabled ? "true" : null });
    const set = (next) => {
      const clamped = Math.min(max, Math.max(min, next));
      if (ref) helpers.setState(ref, clamped);
      helpers.invoke(props.onChange, clamped);
    };
    const minus = el("button", {
      type: "button",
      class: "rui-qty-minus",
      "aria-label": label ? `Decrease ${label}` : "Decrease",
      disabled: disabled || current <= min ? "" : null
    }, ["−"]);
    minus.onclick = () => {
      if (!disabled) set(current - step);
    };
    const valueEl = el("span", {
      class: "rui-qty-value",
      role: "spinbutton",
      tabindex: disabled ? null : "0",
      "aria-label": label ? `${label} quantity` : "Quantity",
      "aria-valuenow": String(current),
      "aria-valuemin": Number.isFinite(min) ? String(min) : null,
      "aria-valuemax": Number.isFinite(max) ? String(max) : null,
      "aria-live": "polite",
      "aria-disabled": disabled ? "true" : null
    }, [String(current)]);
    valueEl.onkeydown = (event) => {
      if (disabled) return;
      let next = null;
      if (event.key === "ArrowUp" || event.key === "ArrowRight") next = current + step;
      else if (event.key === "ArrowDown" || event.key === "ArrowLeft") next = current - step;
      else if (event.key === "PageUp") next = current + step * 10;
      else if (event.key === "PageDown") next = current - step * 10;
      else if (event.key === "Home" && Number.isFinite(min)) next = min;
      else if (event.key === "End" && Number.isFinite(max)) next = max;
      if (next == null) return;
      event.preventDefault();
      set(next);
    };
    const plus = el("button", {
      type: "button",
      class: "rui-qty-plus",
      "aria-label": label ? `Increase ${label}` : "Increase",
      disabled: disabled || current >= max ? "" : null
    }, ["+"]);
    plus.onclick = () => {
      if (!disabled) set(current + step);
    };
    root.append(minus, valueEl, plus);
    const correctedSlot = helpers.useInstanceState("rui-qty-corrected", null);
    if (current !== incoming && !disabled) {
      deferToPaint(() => {
        if (!root.isConnected || correctedSlot.get() === current) return;
        correctedSlot.set(current);
        set(current);
      });
    }
    return root;
  }
};
const ProductCard = {
  name: "ProductCard",
  description: "An e-commerce product card: image, title, optional rating, a PriceTag, and an add-to-cart action. Pass `price`/`compareAt` directly or a custom `price` node. Give it `href` or `onClick` to make the whole card open the product (the card's hover lift promises it), `rating` + `reviewCount` for credible stars, and `soldOut` to dim it and stop the add button firing.",
  props: [
    { name: "title", type: "string", positional: true, required: true },
    { name: "image", type: "string", optional: true, aliases: ["src"] },
    { name: "price", type: "string | number", optional: true },
    { name: "compareAt", type: "string | number", optional: true },
    { name: "currency", type: "string", optional: true },
    { name: "rating", type: "number", optional: true, description: "0–5 stars" },
    { name: "badge", type: "string", optional: true, description: "Corner ribbon label (e.g. 'Sale')" },
    { name: "action", type: "Node", optional: true, description: "Add-to-cart Button; omit it and pass `onAdd` for the built-in icon button" },
    { name: "onAdd", type: "callable", optional: true, description: "Fired by the default add button" },
    { name: "href", type: "string", optional: true, description: "Product detail page — makes the whole card a link" },
    { name: "onClick", type: "callable", optional: true, aliases: ["onSelect", "onclick"], description: "Fired when the card is opened (alternative to `href`)" },
    { name: "reviewCount", type: "number", optional: true, aliases: ["reviews"], description: "Number of ratings shown beside the stars" },
    { name: "soldOut", type: "boolean", optional: true, aliases: ["disabled"], description: "Dim the card and disable the add action" }
  ],
  render: (_node, props, helpers) => {
    const title = asString(props.title);
    const soldOut = asBoolean(props.soldOut);
    const root = el("div", { class: "rui-product-card", "data-sold-out": soldOut ? "true" : null });
    const media = el("div", { class: "rui-product-media" });
    const img = sanitiseImageSrc(props.image);
    if (img) media.append(el("img", { src: img, alt: title, loading: "lazy" }));
    const badge = asString(props.badge);
    if (badge) media.append(el("span", { class: "rui-product-badge" }, [badge]));
    else if (soldOut) media.append(el("span", { class: "rui-product-badge" }, ["Sold out"]));
    root.append(media);
    const body = el("div", { class: "rui-product-body" });
    const href = props.href != null ? sanitiseHref(props.href, "") : "";
    const heading = el("h3", { class: "rui-product-title" });
    if (href) {
      heading.append(el("a", { class: "rui-product-title-link", href }, [title]));
    } else if (props.onClick != null) {
      const link = el("button", { class: "rui-product-title-link", type: "button" }, [title]);
      link.onclick = () => helpers.invoke(props.onClick);
      heading.append(link);
    } else {
      heading.append(document.createTextNode(title));
    }
    body.append(heading);
    const rating = Math.max(0, Math.min(5, Math.round(asNumber(props.rating, 0))));
    const reviewCount = Math.max(0, Math.round(asNumber(props.reviewCount, 0)));
    if (rating > 0) {
      const stars = el("div", {
        class: "rui-product-rating",
        role: "img",
        "aria-label": reviewCount > 0 ? `Rated ${rating} out of 5 from ${reviewCount} reviews` : `Rated ${rating} out of 5`
      });
      for (let i = 0; i < 5; i += 1) {
        const ic = renderIcon(i < rating ? "star" : "regular:star");
        if (ic) stars.append(ic);
      }
      if (reviewCount > 0) {
        stars.append(el("span", { class: "rui-product-reviews", "aria-hidden": "true" }, [`(${reviewCount})`]));
      }
      body.append(stars);
    }
    const row = el("div", { class: "rui-product-foot" });
    if (props.price != null) {
      row.append(PriceTag.render(
        { __kind: "Component", name: "PriceTag", args: [], argMeta: [] },
        { price: props.price, compareAt: props.compareAt, currency: props.currency },
        helpers
      ));
    }
    if (props.action != null) {
      row.append(helpers.renderNode(props.action));
    } else if (typeof props.onAdd === "function") {
      const btn = el("button", {
        class: "rui-product-add",
        type: "button",
        "aria-label": soldOut ? `${title} is sold out` : "Add to cart",
        disabled: soldOut ? "" : null
      });
      const ic = renderIcon("cart-plus");
      if (ic) btn.append(ic);
      btn.onclick = () => {
        if (!soldOut) helpers.invoke(props.onAdd);
      };
      row.append(btn);
    }
    body.append(row);
    root.append(body);
    return root;
  }
};
const TableOfContents = {
  name: "TableOfContents",
  description: "A navigable table of contents. `items` is an array of {label, href, level?}. A `#fragment` href scrolls to the matching `id` INSIDE the app (the browser cannot resolve a fragment into the shadow tree, and touching `location.hash` would drive the router), marks that entry active, and fires `onSelect(href)`. `activeHref` sets the current entry from state.",
  props: [
    { name: "items", type: "object[]", positional: true, required: true, aliases: ["children"] },
    { name: "title", type: "string", optional: true },
    { name: "activeHref", type: "string", optional: true, aliases: ["active"], description: "href of the entry to mark current" },
    { name: "onSelect", type: "callable", optional: true, aliases: ["onChange"], description: "Fired with the clicked href" }
  ],
  render: (_node, props, helpers) => {
    const root = el("nav", { class: "rui-toc", "aria-label": "Table of contents" });
    const title = asString(props.title);
    if (title) root.append(el("div", { class: "rui-toc-title" }, [title]));
    const activeSlot = helpers.useInstanceState("rui-toc-active", "");
    const active = asString(props.activeHref) || activeSlot.get();
    const list = el("ul", { class: "rui-toc-list" });
    for (const raw of asArray(props.items)) {
      const item = raw ?? {};
      const href = sanitiseHref(item.href, "#");
      const isActive = href !== "#" && href === active;
      const li = el("li", {
        class: "rui-toc-item",
        "data-level": String(Math.max(1, Math.min(4, Math.round(asNumber(item.level, 1))))),
        "data-active": isActive ? "true" : null
      });
      const link = el("a", { href, "aria-current": isActive ? "location" : null }, [asString(item.label)]);
      link.onclick = (event) => {
        const origin = event.currentTarget ?? event.target;
        const target = origin?.getAttribute("href") ?? "";
        helpers.invoke(props.onSelect, target);
        if (!target.startsWith("#") || target.length < 2 || !origin) return;
        event.preventDefault();
        activeSlot.set(target);
        const scope = origin.getRootNode();
        const id = decodeURIComponent(target.slice(1));
        scope.getElementById?.(id)?.scrollIntoView?.({ behavior: "smooth", block: "start" });
        const nav = origin.closest(".rui-toc");
        nav?.querySelectorAll(".rui-toc-item[data-active]").forEach((n) => {
          n.removeAttribute("data-active");
          n.querySelector("a")?.removeAttribute("aria-current");
        });
        const item2 = origin.closest(".rui-toc-item");
        item2?.setAttribute("data-active", "true");
        origin.setAttribute("aria-current", "location");
      };
      li.append(link);
      list.append(li);
    }
    root.append(list);
    return root;
  }
};
const TypingIndicator = {
  name: "TypingIndicator",
  description: "Three animated bouncing dots — a chat 'is typing…' affordance. Optional `name` prefix.",
  props: [{ name: "name", type: "string", optional: true, positional: true }],
  render: (_node, props) => {
    const root = el("div", { class: "rui-typing", role: "status", "aria-live": "polite" });
    const name = asString(props.name);
    if (name) root.append(el("span", { class: "rui-typing-name" }, [`${name} is typing`]));
    else root.append(el("span", { class: "rui-visually-hidden" }, ["Someone is typing"]));
    const dots = el("span", { class: "rui-typing-dots", "aria-hidden": "true" });
    dots.append(el("i"), el("i"), el("i"));
    root.append(dots);
    return root;
  }
};
const COUNTDOWN_UNITS = ["days", "hours", "minutes", "seconds"];
const COUNTDOWN_LABELS = {
  days: "days",
  hours: "hrs",
  minutes: "min",
  seconds: "sec"
};
const COUNTDOWN_RUNNING = /* @__PURE__ */ new WeakSet();
const CountdownTimer = {
  name: "CountdownTimer",
  description: "Live countdown to a target date/time (ISO string or timestamp). Ticks every second, then shows `endLabel` and fires `onEnd` (enable checkout, reveal a link, refresh a price). `units` trims the boxes — a 10-minute flash sale should not render two zeroed day/hour cells.",
  props: [
    { name: "to", type: "string | number", positional: true, required: true, aliases: ["target", "date"] },
    { name: "endLabel", type: "string", optional: true, description: "Shown when the countdown finishes (default 'Done')" },
    { name: "onEnd", type: "callable", optional: true, aliases: ["onFinish", "onComplete"], description: "Fired once when the countdown reaches zero" },
    { name: "units", type: "string[]", optional: true, enum: COUNTDOWN_UNITS, description: "Which units to show (default all four)" },
    { name: "showDays", type: "boolean", optional: true, description: "Set false to drop the days cell" }
  ],
  render: (_node, props, helpers) => {
    const target = typeof props.to === "number" ? props.to : new Date(asString(props.to)).getTime();
    const endLabel = asString(props.endLabel, "Done");
    const requested = asArray(props.units).map((u) => asString(u));
    let units = requested.filter((u) => COUNTDOWN_UNITS.includes(u));
    if (units.length === 0) units = [...COUNTDOWN_UNITS];
    if (props.showDays != null && !asBoolean(props.showDays)) units = units.filter((u) => u !== "days");
    if (units.length === 0) units = ["seconds"];
    const root = el("div", { class: "rui-countdown", role: "timer", "aria-live": "off", "aria-atomic": "true" });
    for (const unit of units) {
      const u = el("div", { class: "rui-countdown-unit", "data-unit": unit });
      u.append(
        el("div", { class: "rui-countdown-value" }, ["00"]),
        el("div", { class: "rui-countdown-label" }, [COUNTDOWN_LABELS[unit] ?? unit])
      );
      root.append(u);
    }
    const summary = el("span", { class: "rui-countdown-summary rui-visually-hidden", role: "status", "aria-live": "polite" });
    root.append(summary);
    const endedSlot = helpers.useInstanceState("rui-countdown-ended", false);
    const fireEnd = () => {
      if (endedSlot.get()) return;
      endedSlot.set(true);
      helpers.invoke(props.onEnd);
    };
    const pad = (n) => String(Math.max(0, n)).padStart(2, "0");
    const paint = (host) => {
      const diff = target - Date.now();
      if (!Number.isFinite(target) || diff <= 0) {
        host.replaceChildren(el("div", { class: "rui-countdown-done" }, [endLabel]));
        host.setAttribute("data-done", "true");
        return false;
      }
      const sec = Math.floor(diff / 1e3);
      const values = {
        days: Math.floor(sec / 86400),
        hours: Math.floor(sec % 86400 / 3600),
        minutes: Math.floor(sec % 3600 / 60),
        seconds: sec % 60
      };
      for (const unit of units) {
        const cell = host.querySelector(`.rui-countdown-unit[data-unit="${unit}"] .rui-countdown-value`);
        if (cell) cell.textContent = pad(values[unit] ?? 0);
      }
      const live = host.querySelector(".rui-countdown-summary");
      if (live) {
        const spoken = units.filter((u) => u !== "seconds" || sec < 60).map((u) => `${values[u] ?? 0} ${u}`).join(", ");
        const next = `${spoken} remaining`;
        if (live.textContent !== next && (sec % 60 === 0 || live.textContent === "")) live.textContent = next;
      }
      return true;
    };
    paint(root);
    const cancel = deferToPaint(() => {
      if (!root.isConnected || COUNTDOWN_RUNNING.has(root)) return;
      if (!paint(root)) {
        fireEnd();
        return;
      }
      COUNTDOWN_RUNNING.add(root);
      const id = setInterval(() => {
        if (!root.isConnected) {
          clearInterval(id);
          COUNTDOWN_RUNNING.delete(root);
          return;
        }
        if (!paint(root)) {
          clearInterval(id);
          COUNTDOWN_RUNNING.delete(root);
          fireEnd();
        }
      }, 1e3);
      helpers.registerDisposer(() => {
        clearInterval(id);
        COUNTDOWN_RUNNING.delete(root);
      }, "countdown");
    });
    helpers.registerDisposer(() => {
      if (!root.isConnected) cancel();
    }, "countdown-defer");
    return root;
  }
};
function scrolledDistance(from) {
  let max = typeof window !== "undefined" ? window.scrollY || document.documentElement.scrollTop || 0 : 0;
  const rootNode = from.getRootNode();
  let node = rootNode instanceof ShadowRoot ? rootNode.host.parentElement : from.parentElement;
  while (node) {
    if (node.scrollTop > max) max = node.scrollTop;
    node = node.parentElement;
  }
  return max;
}
const BackToTop = {
  name: "BackToTop",
  description: "A floating button that smoothly scrolls the page (and the host) back to the top. It appears only after `showAfter` px of scrolling (default 400) so it does not sit over the hero, and stacks above a FloatingActionButton rather than on top of it.",
  props: [
    { name: "label", type: "string", optional: true },
    { name: "showAfter", type: "number", optional: true, description: "Scroll distance in px before the button appears (0 = always)" },
    { name: "position", type: "string", optional: true, enum: FAB_POSITIONS, description: "Screen corner (default bottom-right)" }
  ],
  render: (_node, props, helpers) => {
    const showAfter = Math.max(0, asNumber(props.showAfter, 400));
    const visibleSlot = helpers.useInstanceState("rui-backtotop-visible", false);
    const visible = showAfter === 0 || visibleSlot.get();
    const btn = el("button", {
      class: "rui-fab rui-backtotop",
      type: "button",
      "data-position": fabPosition(props.position),
      "data-visible": visible ? "true" : "false",
      // Inline so the affordance works with or without a themed transition.
      style: visible ? null : "display:none",
      "aria-label": asString(props.label, "Back to top")
    });
    const ic = renderIcon("arrow-up", { size: "lg" });
    if (ic) btn.append(ic);
    btn.onclick = (event) => {
      try {
        window.scrollTo({ top: 0, behavior: "smooth" });
      } catch {
        window.scrollTo(0, 0);
      }
      const live = event.currentTarget ?? event.target;
      const host = live?.getRootNode?.()?.host;
      host?.scrollIntoView?.({ behavior: "smooth", block: "start" });
    };
    if (showAfter > 0) {
      const cancel = deferToPaint(() => {
        if (!btn.isConnected) return;
        const apply = () => {
          const next = scrolledDistance(btn) > showAfter;
          if (next === (btn.getAttribute("data-visible") === "true")) return;
          visibleSlot.set(next);
          btn.setAttribute("data-visible", next ? "true" : "false");
          btn.style.display = next ? "" : "none";
        };
        apply();
        window.addEventListener("scroll", apply, { passive: true, capture: true });
        helpers.registerDisposer(
          () => window.removeEventListener("scroll", apply, true),
          "rui-backtotop-scroll"
        );
      });
      helpers.registerDisposer(() => {
        if (!btn.isConnected) cancel();
      }, "rui-backtotop-defer");
    }
    return btn;
  }
};
function ancestorOf(node) {
  const parent = node.parentElement;
  if (parent) return parent;
  const root = node.getRootNode();
  return root instanceof ShadowRoot ? root.host : null;
}
function nearestScrollable(node) {
  if (typeof getComputedStyle !== "function") return null;
  let cur = ancestorOf(node);
  while (cur) {
    const overflowY = getComputedStyle(cur).overflowY;
    if ((overflowY === "auto" || overflowY === "scroll" || overflowY === "overlay") && cur.scrollHeight > cur.clientHeight + 1) return cur;
    cur = ancestorOf(cur);
  }
  return null;
}
function renderChild$1(helpers, child) {
  if (child == null) return document.createTextNode("");
  if (Array.isArray(child)) {
    const frag = document.createDocumentFragment();
    for (const c of child) {
      if (c == null) continue;
      frag.append(typeof c === "string" ? document.createTextNode(c) : helpers.renderNode(c));
    }
    return frag;
  }
  if (typeof child === "string") return document.createTextNode(child);
  return helpers.renderNode(child);
}
const RATIO_MAP = {
  "1/1": "1fr 1fr",
  "1/2": "1fr 2fr",
  "2/1": "2fr 1fr",
  "2/3": "2fr 3fr",
  "3/2": "3fr 2fr",
  "1/3": "1fr 3fr",
  "3/1": "3fr 1fr",
  "2/5": "2fr 5fr",
  "5/2": "5fr 2fr"
};
const RATIO_PAIR = /^(\d{1,3}(?:\.\d{1,2})?)\s*[/:]\s*(\d{1,3}(?:\.\d{1,2})?)$/;
function resolveRatio(raw) {
  const key = asString(raw, "1/1").trim();
  const named = RATIO_MAP[key];
  if (named) return named;
  const pair2 = RATIO_PAIR.exec(key);
  if (pair2) {
    const left = Number(pair2[1]);
    const right = Number(pair2[2]);
    if (left > 0 && right > 0) return `${left}fr ${right}fr`;
  }
  return "1fr 1fr";
}
const Split = {
  name: "Split",
  description: "Two-pane layout: a left/primary node and a right/secondary node, with a controllable `ratio` (1/1, 3/2, 2/3 — or any `a/b` pair like \"60/40\"), optional `divider`, an optional `sticky` pane that pins on scroll (`stickyOffset` sets how far below the top it pins), and a `stackAt` breakpoint where it collapses to a single column — `reverseOnStack` puts the right pane first once stacked, the usual want for 'text + media'. The canonical 'text + media' / 'code + preview' / 'content + sidebar' section.",
  props: [
    { name: "left", type: "Node", positional: true, required: true, aliases: ["primary"] },
    { name: "right", type: "Node", required: true, aliases: ["secondary"] },
    { name: "ratio", type: "string", optional: true, description: 'Column ratio — a named split (1/1, 3/2, 2/3, 1/3, 2/1…) or any `a/b` pair, e.g. "60/40"' },
    { name: "gap", type: "string", optional: true, enum: SPACING_TOKENS },
    { name: "divider", type: "boolean", optional: true },
    { name: "sticky", type: "string", optional: true, enum: ["left", "right"], description: "Pin one pane while the other scrolls" },
    { name: "stickyOffset", type: "string", optional: true, description: "How far below the viewport top the sticky pane pins (CSS length, default 88px) — set it to your header height" },
    { name: "stackAt", type: "string", optional: true, enum: ["sm", "md", "lg"], description: "Breakpoint below which it stacks: sm 640px, md 768px, lg 1024px (default md)" },
    { name: "reverseOnStack", type: "boolean", optional: true, description: "Show the right pane FIRST once stacked (media-above-text on mobile)" },
    { name: "align", type: "string", optional: true, enum: ["start", "center", "stretch"] }
  ],
  render: (_node, props, helpers) => {
    const ratio = resolveRatio(props.ratio);
    const gap = spacingCssValue(asString(props.gap, "lg")) || "var(--rui-spacing-l)";
    const root = el("div", {
      class: "rui-split",
      "data-divider": asBoolean(props.divider) ? "true" : null,
      "data-stack": asString(props.stackAt, "md"),
      "data-reverse-stack": asBoolean(props.reverseOnStack) ? "true" : null,
      "data-align": asString(props.align) || null,
      style: `grid-template-columns:${ratio};gap:${gap}`
    });
    const stickyTop = sanitiseCssLength(props.stickyOffset, "");
    const paneStyle = (sticky) => sticky && stickyTop ? `top:${stickyTop}` : null;
    const leftSticky = asString(props.sticky) === "left";
    const rightSticky = asString(props.sticky) === "right";
    const leftPane = el("div", { class: "rui-split-pane", "data-sticky": leftSticky ? "true" : null, style: paneStyle(leftSticky) });
    leftPane.append(renderChild$1(helpers, props.left));
    const rightPane = el("div", { class: "rui-split-pane", "data-sticky": rightSticky ? "true" : null, style: paneStyle(rightSticky) });
    rightPane.append(renderChild$1(helpers, props.right));
    root.append(leftPane, rightPane);
    return root;
  }
};
const SPAN_NAMES = {
  tile: { col: 1, row: 1 },
  wide: { col: 2, row: 1 },
  tall: { col: 1, row: 2 },
  hero: { col: 2, row: 2 }
};
function parseBentoSpan(span) {
  const clampCol = (n) => Math.max(1, Math.min(8, Math.round(n)));
  const clampRow = (n) => Math.max(1, Math.min(4, Math.round(n)));
  if (typeof span === "number") return { col: clampCol(span), row: 1, name: null };
  if (typeof span === "string") {
    const key = span.trim().toLowerCase();
    if (key === "full") return { col: 1, row: 1, name: "full" };
    if (SPAN_NAMES[key]) return { ...SPAN_NAMES[key], name: key };
    const grid = /^(\d+)\s*[x×]\s*(\d+)$/.exec(key);
    if (grid) return { col: clampCol(Number(grid[1])), row: clampRow(Number(grid[2])), name: null };
    const num2 = Number(key);
    if (Number.isFinite(num2) && num2 > 0) return { col: clampCol(num2), row: 1, name: null };
    return { col: 1, row: 1, name: null };
  }
  if (span && typeof span === "object") {
    const s = span;
    return { col: clampCol(asNumber(s.col, 1)), row: clampRow(asNumber(s.row, 1)), name: null };
  }
  return { col: 1, row: 1, name: null };
}
function canonicalSpanName(col, row, name) {
  if (name === "full") return "full";
  if (col >= 2) return row >= 2 ? "hero" : "wide";
  return row >= 2 ? "tall" : "tile";
}
const BENTO_SPAN_VALUES = [
  ...Object.keys(SPAN_NAMES),
  "full",
  ...Array.from({ length: 8 }, (_, c) => Array.from({ length: 4 }, (_2, r) => `${c + 1}x${r + 1}`)).flat(),
  ...Array.from({ length: 8 }, (_, c) => String(c + 1))
];
const BentoCell = {
  name: "BentoCell",
  description: 'A single cell in a Bento grid. `span` names a size — `tile` 1×1, `wide` 2×1 (2 columns), `tall` 1×2 (2 rows), `hero` 2×2, `full` (a whole row) — or use a "CxR" string ("2x1"), a bare column-span number, or `{ col, row }`; `rowSpan` adds rows to a named/numeric span. The child stretches to fill the cell, so images/cards crop to the cell\'s shape. Pick spans that tile the parent Bento with no leftover tracks.',
  props: [
    { name: "child", type: "Node", positional: true, required: true, aliases: ["children"] },
    { name: "span", type: "string | number | object", optional: true, enum: BENTO_SPAN_VALUES, description: 'tile (1×1) | wide (2×1) | tall (1×2) | hero (2×2) | full (whole row), "2x1", 2, or { col, row }' },
    { name: "rowSpan", type: "number", optional: true, description: "Rows to span (combines with a numeric/named span)" }
  ],
  render: (_node, props, helpers) => {
    let { col, row, name } = parseBentoSpan(props.span);
    if (props.rowSpan !== void 0) {
      row = Math.max(1, Math.min(4, Math.round(asNumber(props.rowSpan, row))));
    }
    const cell = el("div", {
      class: "rui-bento-cell",
      "data-span": canonicalSpanName(col, row, name),
      style: `--rui-cell-col:${col};--rui-cell-row:${row}`
    });
    cell.append(renderChild$1(helpers, props.child));
    return cell;
  }
};
function ensureBentoCell(rendered) {
  if (rendered instanceof HTMLElement && rendered.classList.contains("rui-bento-cell")) return rendered;
  const cell = el("div", {
    class: "rui-bento-cell",
    "data-span": "tile",
    style: "--rui-cell-col:1;--rui-cell-row:1"
  });
  if (rendered instanceof HTMLElement) {
    const key = rendered.getAttribute("data-rui-key");
    if (key != null) cell.setAttribute("data-rui-key", key);
  }
  cell.append(rendered);
  return cell;
}
const Bento = {
  name: "Bento",
  description: "Asymmetric 'bento box' grid — the marquee feature-section layout. Children must be BentoCell nodes (a plain node becomes a 1×1 tile). Two rules make it look right: (1) spans must tile the grid exactly — each row's column spans sum to `columns` and row-spans pair up with neighbouring cells, never leaving a dangling track (e.g. `columns: 3` with 4 cells: hero 2×2 + tall 1×2 fill rows 1–2, wide 2×1 + tile fill row 3); (2) set a fixed `rowHeight` (e.g. \"180px\") whenever cells hold images or cards — the default auto row stretches to the tallest cell and makes the mosaic ragged. Give 1–2 standout cells a big span (hero/wide/tall) and keep the rest 1×1 tiles. `dense` (default true) backfills gaps. Collapses to 2 columns below 920px and 1 below 640px.",
  props: [
    { name: "items", type: "BentoCell[]", positional: true, required: true, aliases: ["children", "cells"] },
    { name: "columns", type: "number", optional: true, description: "Track count 1–8 (default 6) — pick it so the cell spans in every row can sum to it" },
    { name: "gap", type: "string", optional: true, enum: SPACING_TOKENS },
    { name: "rowHeight", type: "string | object", optional: true, description: 'Fixed row track size, e.g. "180px" — set it whenever any cell spans rows or holds images (default minmax(110px, auto) stretches rows to content). May be a responsive map like `{base: "220px", md: "180px"}` since the same track is far too short once the grid collapses to one column' },
    { name: "dense", type: "boolean", optional: true, description: "Backfill holes with later cells (default true)" }
  ],
  render: (_node, props, helpers) => {
    const cols = Math.max(1, Math.min(8, Math.round(asNumber(props.columns, 6))));
    const gapKey = normalizeSpacingToken(props.gap, "md");
    const dense = props.dense === void 0 ? true : asBoolean(props.dense);
    const attrs = {
      class: "rui-bento",
      "data-cols": String(cols),
      "data-gap": gapKey,
      "data-dense": dense ? "true" : null
    };
    const styleParts = [];
    const rowHeight = readResponsiveProp(props.rowHeight);
    if (rowHeight.kind === "single") {
      const len = sanitiseCssLength(rowHeight.value, "");
      if (len) styleParts.push(`--rui-bento-row:${len}`);
    } else {
      attrs["data-responsive-row"] = "true";
      for (const bp of RESPONSIVE_BREAKPOINTS) {
        const len = sanitiseCssLength(rowHeight.values[bp], "");
        if (len) styleParts.push(`--rui-bento-row-${bp}:${len}`);
      }
    }
    if (styleParts.length > 0) attrs.style = styleParts.join(";");
    const root = el("div", attrs);
    for (const item of asArray(props.items)) {
      const rendered = ensureBentoCell(helpers.renderNode(item));
      if (rendered instanceof HTMLElement) {
        const span = Number.parseInt(rendered.style.getPropertyValue("--rui-cell-col"), 10);
        if (Number.isFinite(span) && span > cols) rendered.style.setProperty("--rui-cell-col", String(cols));
      }
      root.append(rendered);
    }
    return root;
  }
};
const REVEAL_PRESETS = /* @__PURE__ */ new Set(["fade", "fade-up", "fade-down", "fade-left", "fade-right", "zoom", "slide-up"]);
const Reveal = {
  name: "Reveal",
  description: "Animates its child in the first time it scrolls into view (scroll choreography). `animation` is a preset (fade-up|fade|zoom|…), `delay` staggers it (ms), `duration` sets the animation length (default 600ms), `threshold` how much of it must be visible to trigger (0–1, default 0.15), `once` (default true) plays a single time. Honours prefers-reduced-motion (renders immediately).",
  props: [
    { name: "child", type: "Node", positional: true, required: true, aliases: ["children"] },
    { name: "animation", type: "string", optional: true, enum: [...REVEAL_PRESETS] },
    { name: "delay", type: "number", optional: true, description: "Stagger delay in ms" },
    { name: "duration", type: "number", optional: true, description: "Animation duration in ms (default 600) — match it to the rest of the page's motion scale" },
    { name: "threshold", type: "number", optional: true, description: "Fraction of the child that must be visible to trigger, 0–1 (default 0.15). Use a low value for a tall hero, a higher one for a small card" },
    { name: "once", type: "boolean", optional: true }
  ],
  render: (_node, props, helpers) => {
    const preset = asString(props.animation, "fade-up");
    const anim = REVEAL_PRESETS.has(preset) ? preset : "fade-up";
    const delay = Math.max(0, Math.min(5e3, asNumber(props.delay, 0)));
    const duration = Math.max(0, Math.min(5e3, asNumber(props.duration, 0)));
    const threshold = Math.max(0, Math.min(1, asNumber(props.threshold, 0.15)));
    const reduce = typeof matchMedia !== "undefined" && matchMedia("(prefers-reduced-motion: reduce)").matches;
    const revealed = helpers.useInstanceState("rui-reveal-shown", false);
    const shownNow = revealed.get() || reduce || typeof IntersectionObserver === "undefined";
    const styleParts = [];
    if (delay) styleParts.push(`transition-delay:${delay}ms`);
    if (duration && !reduce) styleParts.push(`transition-duration:${duration}ms`);
    const wrapper = el("div", {
      class: shownNow ? "rui-reveal is-revealed" : "rui-reveal",
      "data-anim": anim,
      style: styleParts.length > 0 ? styleParts.join(";") : null
    });
    wrapper.append(renderChild$1(helpers, props.child));
    if (shownNow && (props.once === void 0 ? true : asBoolean(props.once))) return wrapper;
    if (reduce || typeof IntersectionObserver === "undefined") return wrapper;
    const once = props.once === void 0 ? true : asBoolean(props.once);
    const show = (node) => {
      revealed.set(true);
      node.classList.add("is-revealed");
    };
    deferToPaint(() => {
      if (!wrapper.isConnected) return;
      const io = new IntersectionObserver((entries) => {
        for (const e of entries) {
          if (e.isIntersecting) {
            show(e.target);
            if (once) {
              io.disconnect();
              return;
            }
          } else if (!once) {
            revealed.set(false);
            e.target.classList.remove("is-revealed");
          }
        }
      }, { threshold, rootMargin: "0px" });
      io.observe(wrapper);
      helpers.registerDisposer(() => io.disconnect(), "rui-reveal-io");
      const net = setTimeout(() => {
        if (!wrapper.isConnected || revealed.get()) return;
        const rect = wrapper.getBoundingClientRect();
        const viewportH = window.innerHeight || document.documentElement.clientHeight || 0;
        if (rect.bottom > 0 && rect.top < viewportH) show(wrapper);
      }, 700);
      helpers.registerDisposer(() => clearTimeout(net), "rui-reveal-net");
    });
    return wrapper;
  }
};
const GESTURE_STATE = /* @__PURE__ */ new WeakMap();
function gestureState(host) {
  let state = GESTURE_STATE.get(host);
  if (!state) {
    state = { x: 0, y: 0, lastTap: 0, longTimer: null, longFired: false };
    GESTURE_STATE.set(host, state);
  }
  return state;
}
function clearLongPress(state) {
  if (state.longTimer) {
    clearTimeout(state.longTimer);
    state.longTimer = null;
  }
}
const OnGesture = {
  name: "OnGesture",
  description: "Attach high-level pointer gestures to any component without raw DOM: `swipe(dir)` (left/right/up/down), `longPress()`, `doubleTap()`, `pan({dx, dy})` during a drag and `onPanEnd({dx, dy})` on release — `onPanEnd` is what lets a pan snap back or commit, so pull-to-refresh and swipe-to-dismiss need it. `disabled` suppresses every gesture. Gestures are pointer-only, so the wrapper is also focusable and mirrors them onto the keyboard (Enter/Space → doubleTap, arrows → swipe, the context-menu key → longPress); pass `ariaLabel` to say what it does, or keep a visible control for the same action.",
  props: [
    { name: "child", type: "Node", positional: true, required: true, aliases: ["children"] },
    { name: "swipe", type: "callable", optional: true, aliases: ["onSwipe"], description: "(dir) => … — fired on a swipe; dir is left|right|up|down" },
    { name: "longPress", type: "callable", optional: true, aliases: ["onLongPress"] },
    { name: "doubleTap", type: "callable", optional: true, aliases: ["onDoubleTap"] },
    { name: "pan", type: "callable", optional: true, aliases: ["onPan"], description: "({dx, dy}) => … — fired during a drag" },
    { name: "onPanEnd", type: "callable", optional: true, aliases: ["panEnd", "onRelease"], description: "({dx, dy}) => … — fired on release with the FINAL offset, however short the drag was. Use it to snap back or commit a pan" },
    { name: "threshold", type: "number", optional: true, description: "Swipe distance in px (default 40)" },
    { name: "disabled", type: "boolean", optional: true, description: "Ignore every gesture (a sheet is open, the row is in edit mode, a mutation is in flight)" },
    { name: "ariaLabel", type: "string", optional: true, description: 'What the gesture does, announced to screen readers (e.g. "Swipe left to delete")' }
  ],
  render: (_node, props, helpers) => {
    const disabled = asBoolean(props.disabled);
    const hasGesture = !disabled && (props.swipe != null || props.longPress != null || props.doubleTap != null || props.pan != null || props.onPanEnd != null);
    const wrap = el("div", {
      class: "rui-gesture",
      style: `touch-action:${props.pan != null && !disabled ? "none" : "pan-y"}`,
      "aria-disabled": disabled ? "true" : null,
      "aria-label": asString(props.ariaLabel) || null,
      // A gesture with no keyboard path is unreachable for keyboard and screen
      // reader users, and nothing in the markup hints it exists. Focusable +
      // labelled is the minimum; the key handler below is the equivalent.
      tabindex: hasGesture ? "0" : null
    });
    wrap.append(renderChild$1(helpers, props.child));
    if (disabled) return wrap;
    const threshold = Math.max(8, asNumber(props.threshold, 40));
    const liveHost = (e) => e.currentTarget ?? wrap;
    const wantsCapture = props.pan != null || props.swipe != null;
    const releaseCapture = (host, pointerId) => {
      try {
        if (host.hasPointerCapture?.(pointerId)) host.releasePointerCapture(pointerId);
      } catch {
      }
    };
    wrap.onpointerdown = (e) => {
      const host = liveHost(e);
      const state = gestureState(host);
      state.x = e.clientX;
      state.y = e.clientY;
      state.longFired = false;
      clearLongPress(state);
      if (wantsCapture) {
        try {
          host.setPointerCapture(e.pointerId);
        } catch {
        }
      }
      if (props.longPress != null) {
        const timer = setTimeout(() => {
          state.longTimer = null;
          if (!host.isConnected) return;
          state.longFired = true;
          helpers.invoke(props.longPress);
        }, 500);
        state.longTimer = timer;
        helpers.registerDisposer(() => clearTimeout(timer), "rui-gesture-longpress");
      }
    };
    wrap.onpointermove = (e) => {
      const state = gestureState(liveHost(e));
      const dx = e.clientX - state.x;
      const dy = e.clientY - state.y;
      if (Math.abs(dx) > 6 || Math.abs(dy) > 6) clearLongPress(state);
      if (props.pan != null && e.buttons & 1) helpers.invoke(props.pan, { dx, dy });
    };
    wrap.onpointerup = (e) => {
      const host = liveHost(e);
      const state = gestureState(host);
      clearLongPress(state);
      releaseCapture(host, e.pointerId);
      const dx = e.clientX - state.x;
      const dy = e.clientY - state.y;
      if (props.onPanEnd != null) helpers.invoke(props.onPanEnd, { dx, dy });
      if (state.longFired) {
        state.longFired = false;
        state.lastTap = 0;
        return;
      }
      const adx = Math.abs(dx);
      const ady = Math.abs(dy);
      if (props.swipe != null && (adx > threshold || ady > threshold)) {
        const dir = adx > ady ? dx > 0 ? "right" : "left" : dy > 0 ? "down" : "up";
        state.lastTap = 0;
        helpers.invoke(props.swipe, dir);
        return;
      }
      if (props.doubleTap != null && adx < 8 && ady < 8) {
        const now = Date.now();
        if (now - state.lastTap < 300) {
          state.lastTap = 0;
          helpers.invoke(props.doubleTap);
        } else state.lastTap = now;
      }
    };
    wrap.onpointercancel = (e) => {
      const host = liveHost(e);
      const state = gestureState(host);
      clearLongPress(state);
      releaseCapture(host, e.pointerId);
      state.longFired = false;
      if (props.onPanEnd != null) {
        helpers.invoke(props.onPanEnd, { dx: e.clientX - state.x, dy: e.clientY - state.y });
      }
    };
    if (hasGesture) {
      wrap.onkeydown = (e) => {
        const dir = e.key === "ArrowLeft" ? "left" : e.key === "ArrowRight" ? "right" : e.key === "ArrowUp" ? "up" : e.key === "ArrowDown" ? "down" : null;
        if (dir && props.swipe != null) {
          e.preventDefault();
          helpers.invoke(props.swipe, dir);
          return;
        }
        if ((e.key === "Enter" || e.key === " ") && props.doubleTap != null) {
          e.preventDefault();
          helpers.invoke(props.doubleTap);
          return;
        }
        if ((e.key === "ContextMenu" || e.key === "F10" && e.shiftKey) && props.longPress != null) {
          e.preventDefault();
          helpers.invoke(props.longPress);
        }
      };
    }
    return wrap;
  }
};
const SORTABLE_IDLE = { from: -1, over: -1, keyboard: false };
function sortableStatus(drag, count) {
  if (drag.from < 0 || !drag.keyboard) return "";
  return `Item ${drag.from + 1} of ${count} picked up. Use the arrow keys to move it, Enter to drop, Escape to cancel.`;
}
const Sortable = {
  name: "Sortable",
  description: "A list whose items can be reordered by dragging a row (or, for keyboard users, by focusing one, pressing Space to pick it up and using the arrow keys). Pass already-rendered `items`; `onReorder(fromIndex, toIndex)` fires after a drop so you can reorder the backing reactive array. `horizontal` lays the rows out in a row instead of a column; `disabled` freezes it. Dragging uses native HTML5 drag-and-drop, which fires no events on touch devices — on mobile, expose the keyboard path (the rows are focusable) or move/reorder buttons instead.",
  props: [
    { name: "items", type: "Node[]", positional: true, required: true, aliases: ["children"] },
    { name: "onReorder", type: "callable", optional: true, description: "(fromIndex, toIndex) => …" },
    { name: "handle", type: "boolean", optional: true, description: "Show a drag handle on each row (default true)" },
    { name: "horizontal", type: "boolean", optional: true, description: "Lay the rows out horizontally (chip rows, kanban column headers) — the arrow keys follow the axis" },
    { name: "disabled", type: "boolean", optional: true, description: "Freeze the list: no dragging, no keyboard reordering (read-only, or a reorder mutation is in flight)" },
    { name: "ariaLabel", type: "string", optional: true, description: 'Name for the list, announced to screen readers (e.g. "Task order")' }
  ],
  render: (_node, props, helpers) => {
    const items = asArray(props.items);
    const disabled = asBoolean(props.disabled);
    const horizontal = asBoolean(props.horizontal);
    const showHandle = props.handle === void 0 ? true : asBoolean(props.handle);
    const dragSlot = helpers.useInstanceState("rui-sortable-drag", SORTABLE_IDLE);
    const drag = dragSlot.get();
    const root = el("div", {
      class: "rui-sortable",
      role: "listbox",
      "aria-label": asString(props.ariaLabel) || null,
      "aria-orientation": horizontal ? "horizontal" : "vertical",
      "aria-disabled": disabled ? "true" : null,
      "data-orientation": horizontal ? "horizontal" : null,
      // Inline so the axis switch needs no extra stylesheet rule.
      style: horizontal ? "flex-direction:row" : null
    });
    const liveRow = (e) => (e.currentTarget ?? e.target)?.closest?.(".rui-sortable-item") ?? null;
    const indexOf = (row) => {
      const raw = row?.getAttribute("data-index");
      if (raw == null) return -1;
      const n = Number(raw);
      return Number.isInteger(n) && n >= 0 ? n : -1;
    };
    const containerOf = (row) => row?.closest(".rui-sortable") ?? null;
    const setDrag = (container, next) => {
      dragSlot.set(next);
      if (!container) return;
      for (const row of Array.from(container.querySelectorAll(".rui-sortable-item"))) {
        const i = indexOf(row);
        row.classList.toggle("is-dragging", i >= 0 && i === next.from);
        row.classList.toggle("is-over", i >= 0 && i === next.over);
        row.setAttribute("aria-selected", i >= 0 && i === next.from ? "true" : "false");
      }
      const status = container.querySelector(".rui-sortable-status");
      if (status) status.textContent = sortableStatus(next, items.length);
    };
    const move = (container, from, to) => {
      setDrag(container, SORTABLE_IDLE);
      if (from < 0 || to < 0 || from === to || to >= items.length) return;
      helpers.invoke(props.onReorder, from, to);
      deferToPaint(() => {
        const live = container?.isConnected ? container : null;
        live?.querySelector(`.rui-sortable-item[data-index="${to}"]`)?.focus();
      });
    };
    items.forEach((item, index) => {
      const row = el("div", {
        class: classNames(
          "rui-sortable-item",
          index === drag.from && "is-dragging",
          index === drag.over && "is-over"
        ),
        draggable: disabled ? "false" : "true",
        "data-index": String(index),
        role: "option",
        "aria-selected": index === drag.from ? "true" : "false",
        tabindex: disabled ? null : "0"
      });
      if (showHandle) {
        const h = el("span", { class: "rui-sortable-handle", "aria-hidden": "true" });
        h.append(el("i", { class: "rui-icon fa-solid fa-grip-vertical" }));
        row.append(h);
      }
      const body = el("div", { class: "rui-sortable-body" });
      body.append(helpers.renderNode(item));
      row.append(body);
      if (!disabled) {
        row.ondragstart = (e) => {
          const live = liveRow(e);
          const from = indexOf(live);
          if (from < 0) return;
          try {
            e.dataTransfer?.setData("text/plain", String(from));
            if (e.dataTransfer) e.dataTransfer.effectAllowed = "move";
          } catch {
          }
          setDrag(containerOf(live), { from, over: -1, keyboard: false });
        };
        row.ondragend = (e) => setDrag(containerOf(liveRow(e)), SORTABLE_IDLE);
        row.ondragover = (e) => {
          e.preventDefault();
          const live = liveRow(e);
          const over = indexOf(live);
          const current = dragSlot.get();
          if (current.over === over) return;
          setDrag(containerOf(live), { ...current, over });
        };
        row.ondragleave = (e) => {
          const live = liveRow(e);
          const current = dragSlot.get();
          if (current.over !== indexOf(live)) return;
          setDrag(containerOf(live), { ...current, over: -1 });
        };
        row.ondrop = (e) => {
          e.preventDefault();
          const live = liveRow(e);
          const to = indexOf(live);
          const carried = (e.dataTransfer?.getData("text/plain") ?? "").trim();
          const from = /^\d+$/.test(carried) ? Number(carried) : dragSlot.get().from;
          move(containerOf(live), from, to);
        };
        row.onkeydown = (e) => {
          const live = liveRow(e);
          const index2 = indexOf(live);
          const container = containerOf(live);
          const current = dragSlot.get();
          const back = horizontal ? "ArrowLeft" : "ArrowUp";
          const forward = horizontal ? "ArrowRight" : "ArrowDown";
          if (e.key === " " || e.key === "Enter" && current.from < 0) {
            e.preventDefault();
            setDrag(container, current.from === index2 ? SORTABLE_IDLE : { from: index2, over: -1, keyboard: true });
            return;
          }
          if (e.key === "Escape" && current.from >= 0) {
            e.preventDefault();
            setDrag(container, SORTABLE_IDLE);
            return;
          }
          if (e.key === "Enter" && current.from >= 0) {
            e.preventDefault();
            move(container, current.from, index2);
            return;
          }
          if (e.key !== back && e.key !== forward) return;
          e.preventDefault();
          const step = e.key === back ? -1 : 1;
          const target = index2 + step;
          if (target < 0 || target >= items.length) return;
          if (current.from === index2 && current.keyboard) move(container, index2, target);
          else container?.querySelector(`.rui-sortable-item[data-index="${target}"]`)?.focus();
        };
      }
      root.append(row);
    });
    root.append(el(
      "span",
      {
        class: "rui-sortable-status rui-visually-hidden",
        "data-rui-key": "rui-sortable-status",
        "aria-live": "polite",
        "aria-atomic": "true"
      },
      [sortableStatus(drag, items.length)]
    ));
    return root;
  }
};
const DRAG_TYPE_PREFIX = "application/x-aktion.";
const dragTypeFormat = (type) => `${DRAG_TYPE_PREFIX}${type}`;
function normaliseDragType(raw) {
  return asString(raw).trim().toLowerCase().replace(/[^a-z0-9._-]/g, "").slice(0, 40);
}
let KEYBOARD_DRAG = null;
function clearKeyboardGrab(from) {
  const root = from?.getRootNode?.();
  if (!(root instanceof ShadowRoot) && !(root instanceof Document)) return;
  for (const node of Array.from(root.querySelectorAll(".rui-draggable[aria-grabbed='true']"))) {
    node.classList.remove("is-dragging");
    node.removeAttribute("aria-grabbed");
  }
}
const Draggable = {
  name: "Draggable",
  description: "Makes its child draggable, carrying a `data` payload picked up by a DropZone. `type` tags the payload so a DropZone can `accept` (or refuse) it before the drop; `disabled` makes it immovable without re-rendering a different child. `onDragStart`/`onDragEnd` optional. Keyboard users focus it and press Space/Enter to pick up (Escape cancels), then Space/Enter on a DropZone to drop — give it an `ariaLabel` so they know what it is.",
  props: [
    { name: "child", type: "Node", positional: true, required: true, aliases: ["children"] },
    { name: "data", type: "any", optional: true, description: "Payload (stringified) handed to the DropZone" },
    { name: "type", type: "string", optional: true, description: 'Payload kind, e.g. "card" / "file" / "tag" — a DropZone\'s `accept` matches against it' },
    { name: "disabled", type: "boolean", optional: true, description: "Not draggable (locked, in flight, not the user's to move)" },
    { name: "ariaLabel", type: "string", optional: true, description: "What is being dragged, announced to screen readers" },
    { name: "onDragStart", type: "callable", optional: true },
    { name: "onDragEnd", type: "callable", optional: true }
  ],
  render: (_node, props, helpers) => {
    const disabled = asBoolean(props.disabled);
    const type = normaliseDragType(props.type);
    const modeSlot = helpers.useInstanceState(
      "rui-draggable-drag",
      { mode: "none" }
    );
    const token = helpers.useInstanceState("rui-draggable-token", { id: {} }).get().id;
    const mode = modeSlot.get().mode;
    const dragging = !disabled && (mode === "pointer" || mode === "keyboard" && KEYBOARD_DRAG?.owner === token);
    const wrap = el("div", {
      class: classNames("rui-draggable", dragging && "is-dragging"),
      draggable: disabled ? "false" : "true",
      "data-drag-type": type || null,
      "data-disabled": disabled ? "true" : null,
      "aria-disabled": disabled ? "true" : null,
      "aria-label": asString(props.ariaLabel) || null,
      "aria-grabbed": dragging ? "true" : null,
      tabindex: disabled ? null : "0"
    });
    wrap.append(renderChild$1(helpers, props.child));
    if (disabled) return wrap;
    const payload = () => typeof props.data === "string" ? props.data : JSON.stringify(props.data ?? null);
    const setDragging = (node, next) => {
      modeSlot.set({ mode: next });
      if (!node) return;
      node.classList.toggle("is-dragging", next !== "none");
      if (next === "none") node.removeAttribute("aria-grabbed");
      else node.setAttribute("aria-grabbed", "true");
    };
    const liveWrap = (e) => (e.currentTarget ?? e.target)?.closest?.(".rui-draggable") ?? null;
    wrap.ondragstart = (e) => {
      try {
        const text = payload();
        e.dataTransfer?.setData("text/plain", text);
        if (type) e.dataTransfer?.setData(dragTypeFormat(type), text);
      } catch {
      }
      setDragging(liveWrap(e), "pointer");
      helpers.invoke(props.onDragStart, props.data);
    };
    wrap.ondragend = (e) => {
      setDragging(liveWrap(e), "none");
      helpers.invoke(props.onDragEnd);
    };
    wrap.onkeydown = (e) => {
      const held = KEYBOARD_DRAG?.owner === token;
      if (e.key === "Escape" && held) {
        e.preventDefault();
        KEYBOARD_DRAG = null;
        setDragging(liveWrap(e), "none");
        helpers.invoke(props.onDragEnd);
        return;
      }
      if (e.key !== " " && e.key !== "Enter") return;
      e.preventDefault();
      if (held) {
        KEYBOARD_DRAG = null;
        setDragging(liveWrap(e), "none");
        helpers.invoke(props.onDragEnd);
        return;
      }
      KEYBOARD_DRAG = { data: props.data, type, owner: token };
      setDragging(liveWrap(e), "keyboard");
      helpers.invoke(props.onDragStart, props.data);
    };
    return wrap;
  }
};
const DropZone = {
  name: "DropZone",
  description: "A target that accepts a Draggable. `onDrop(data)` receives the dropped payload (parsed JSON when possible). `accept` lists the Draggable `type`s this zone can take (comma-separated) — anything else is refused before it is dropped, so a 'To do' / 'Done' / 'Archive' board can express what goes where; `disabled` makes the zone inert. Pass `child` for the zone's content, or just `label` for a bare labelled target. Keyboard users focus the zone and press Space/Enter to drop what a Draggable picked up.",
  props: [
    { name: "child", type: "Node", positional: true, optional: true, aliases: ["children"], description: "Zone content. Omit it and pass `label` for a plain labelled target" },
    { name: "onDrop", type: "callable", optional: true },
    { name: "label", type: "string", optional: true, description: "Text shown when no `child` is given" },
    { name: "accept", type: "string", optional: true, description: 'Draggable `type`s this zone accepts, e.g. "card" or "card, file". Omit to accept anything' },
    { name: "disabled", type: "boolean", optional: true, description: "Inert: no highlight, no drop (the zone is full, locked, or read-only)" },
    { name: "ariaLabel", type: "string", optional: true, description: "Name for the zone, announced to screen readers" }
  ],
  render: (_node, props, helpers) => {
    const disabled = asBoolean(props.disabled);
    const accept = asString(props.accept).split(/[,\s]+/).map(normaliseDragType).filter(Boolean);
    const overSlot = helpers.useInstanceState("rui-dropzone-over", false);
    const isOver = overSlot.get() && !disabled;
    const zone = el("div", {
      class: classNames("rui-dropzone", isOver && "is-over"),
      "data-accept": accept.length > 0 ? accept.join(",") : null,
      "data-disabled": disabled ? "true" : null,
      "aria-disabled": disabled ? "true" : null,
      "aria-label": asString(props.ariaLabel) || null,
      tabindex: disabled ? null : "0"
    });
    if (props.child != null) zone.append(renderChild$1(helpers, props.child));
    else if (asString(props.label)) zone.append(el("span", { class: "rui-dropzone-label" }, [asString(props.label)]));
    if (disabled) return zone;
    const liveZone = (e) => (e.currentTarget ?? e.target)?.closest?.(".rui-dropzone") ?? null;
    const setOver = (node, next) => {
      overSlot.set(next);
      node?.classList.toggle("is-over", next);
    };
    const accepts = (transfer) => {
      if (accept.length === 0) return true;
      const types = Array.from(transfer?.types ?? []);
      return accept.some((t) => types.includes(dragTypeFormat(t)));
    };
    const drop = (node, data) => {
      setOver(node, false);
      helpers.invoke(props.onDrop, data);
    };
    zone.ondragover = (e) => {
      const node = liveZone(e);
      if (!accepts(e.dataTransfer)) {
        if (e.dataTransfer) e.dataTransfer.dropEffect = "none";
        return;
      }
      e.preventDefault();
      setOver(node, true);
    };
    zone.ondragleave = (e) => {
      const node = liveZone(e);
      const to = e.relatedTarget;
      if (to instanceof Node && node?.contains(to)) return;
      setOver(node, false);
    };
    zone.ondrop = (e) => {
      const node = liveZone(e);
      if (!accepts(e.dataTransfer)) {
        setOver(node, false);
        return;
      }
      e.preventDefault();
      const raw = e.dataTransfer?.getData("text/plain") ?? "";
      let data = raw;
      try {
        data = JSON.parse(raw);
      } catch {
      }
      drop(node, data);
    };
    zone.onkeydown = (e) => {
      if (e.key !== " " && e.key !== "Enter") return;
      const pending = KEYBOARD_DRAG;
      if (!pending) return;
      if (accept.length > 0 && !accept.includes(pending.type)) return;
      e.preventDefault();
      KEYBOARD_DRAG = null;
      const node = liveZone(e);
      clearKeyboardGrab(node);
      drop(node, pending.data);
    };
    return zone;
  }
};
function parallaxLimit(raw, height) {
  const value = sanitiseCssLength(raw, "");
  const px = /^(\d+(?:\.\d+)?)(px)?$/.exec(value);
  if (px) return Number(px[1]);
  const pct = /^(\d+(?:\.\d+)?)%$/.exec(value);
  if (pct) return Number(pct[1]) / 100 * height;
  return height / 2;
}
const Parallax = {
  name: "Parallax",
  description: "Translates its child vertically as the page scrolls for a depth effect. `speed` (−1…1) sets intensity/direction. Travel is capped by `maxOffset` (default: half the layer's own height) so the layer cannot slide out of its section and over the content above or below it. Honours prefers-reduced-motion.",
  props: [
    { name: "child", type: "Node", positional: true, required: true, aliases: ["children"] },
    { name: "speed", type: "number", optional: true, description: "−1…1 (default 0.3)" },
    { name: "maxOffset", type: "string", optional: true, description: `Maximum travel in either direction — a length ("120px") or a percentage of the layer's height ("40%"). Default 50%` }
  ],
  render: (_node, props, helpers) => {
    const offsetSlot = helpers.useInstanceState("rui-parallax-y", 0);
    const offset = offsetSlot.get();
    const wrap = el("div", {
      class: "rui-parallax",
      style: `will-change:transform;transform:translateY(${offset.toFixed(1)}px)`
    });
    wrap.append(renderChild$1(helpers, props.child));
    const reduce = typeof matchMedia !== "undefined" && matchMedia("(prefers-reduced-motion: reduce)").matches;
    if (reduce || typeof window === "undefined") return wrap;
    const cfgSlot = helpers.useInstanceState(
      "rui-parallax-cfg",
      { speed: 0.3, maxOffset: null }
    );
    cfgSlot.set({
      speed: Math.max(-1, Math.min(1, asNumber(props.speed, 0.3))),
      maxOffset: props.maxOffset
    });
    deferToPaint(() => {
      if (!wrap.isConnected) return;
      let raf = 0;
      const update = () => {
        raf = 0;
        const rect = wrap.getBoundingClientRect();
        const viewportH = window.innerHeight || 800;
        const center = rect.top + rect.height / 2 - viewportH / 2;
        const cfg2 = cfgSlot.get();
        const limit = parallaxLimit(cfg2.maxOffset, rect.height);
        const y = Math.max(-limit, Math.min(limit, -center * cfg2.speed));
        offsetSlot.set(y);
        wrap.style.transform = `translateY(${y.toFixed(1)}px)`;
      };
      const onScroll = () => {
        if (!raf) raf = requestAnimationFrame(update);
      };
      update();
      window.addEventListener("scroll", onScroll, { passive: true });
      window.addEventListener("resize", onScroll, { passive: true });
      helpers.registerDisposer(() => {
        window.removeEventListener("scroll", onScroll);
        window.removeEventListener("resize", onScroll);
        if (raf) cancelAnimationFrame(raf);
      }, "rui-parallax");
    });
    return wrap;
  }
};
const ReadingProgress = {
  name: "ReadingProgress",
  description: 'A thin progress bar that fills as the reader scrolls — article/docs reading affordance. Optional `gradient` fill or a `color` override. By default it measures whichever container actually scrolls the app (the page when the app owns the page scroll, otherwise the nearest scrolling ancestor — a chat pane, a dashboard panel, a modal body), and pins itself inside that container instead of the browser viewport. `target` overrides the choice with a CSS selector, or "page" to force the document.',
  props: [
    { name: "gradient", type: "boolean", optional: true },
    { name: "height", type: "string", optional: true, description: "Bar thickness (CSS length, default 3px)" },
    { name: "target", type: "string", optional: true, aliases: ["scrollContainer"], description: 'What to measure: a CSS selector for the scrolling element, or "page" for the document. Default: auto-detect' },
    { name: "color", type: "string", optional: true, description: "Fill colour (CSS colour or var(--token)) — overrides `gradient`" }
  ],
  render: (_node, props, helpers) => {
    const pctSlot = helpers.useInstanceState("rui-reading-progress-pct", 0);
    const embeddedSlot = helpers.useInstanceState("rui-reading-progress-embedded", false);
    const target = asString(props.target).trim();
    const rootStyle = [`height:${sanitiseCssLength(props.height, "3px")}`];
    if (embeddedSlot.get()) rootStyle.push("position:sticky", "left:auto", "right:auto");
    const root = el("div", { class: "rui-reading-progress", style: rootStyle.join(";") });
    const color2 = sanitiseCssColor(props.color);
    const bar = el("div", {
      class: "rui-reading-progress-bar",
      "data-gradient": asBoolean(props.gradient) ? "true" : null,
      style: `width:${pctSlot.get().toFixed(2)}%${color2 ? `;background:${color2}` : ""}`
    });
    root.append(bar);
    if (typeof window === "undefined") return root;
    deferToPaint(() => {
      if (!root.isConnected) return;
      let selected = null;
      if (target && target !== "page") {
        const scope = root.getRootNode();
        try {
          selected = scope.querySelector(target) ?? document.querySelector(target);
        } catch {
        }
      }
      const scroller = selected ?? (target === "page" ? null : nearestScrollable(root));
      if (scroller) {
        embeddedSlot.set(true);
        root.style.setProperty("position", "sticky");
        root.style.setProperty("left", "auto");
        root.style.setProperty("right", "auto");
      }
      const measured = () => scroller ?? document.scrollingElement ?? document.documentElement;
      let raf = 0;
      const update = () => {
        raf = 0;
        const box = measured();
        const max = box.scrollHeight - box.clientHeight || 1;
        const pct = Math.max(0, Math.min(100, box.scrollTop / max * 100));
        pctSlot.set(pct);
        bar.style.width = `${pct.toFixed(2)}%`;
      };
      const onScroll = () => {
        if (!raf) raf = requestAnimationFrame(update);
      };
      update();
      const source = scroller ?? window;
      source.addEventListener("scroll", onScroll, { passive: true });
      window.addEventListener("resize", onScroll, { passive: true });
      helpers.registerDisposer(() => {
        source.removeEventListener("scroll", onScroll);
        window.removeEventListener("resize", onScroll);
        if (raf) cancelAnimationFrame(raf);
      }, "rui-reading-progress");
    });
    return root;
  }
};
const TRANSITION_PRESETS = /* @__PURE__ */ new Set(["fade", "scale", "slide-up", "slide-down", "slide-left", "slide-right"]);
const Transition = {
  name: "Transition",
  description: "Animates its child IN when `show` becomes true and OUT when it becomes false (mount/unmount choreography) — modals, toasts, dropdowns, list rows. `preset` is fade|scale|slide-up|slide-down|slide-left|slide-right, `duration` is ms. The child stays mounted through the exit animation, then is removed and `onExited` fires — use it to drop the row from the backing array, release a lock, or restore focus. Honours prefers-reduced-motion (instant swap).",
  props: [
    { name: "child", type: "Node", positional: true, required: true, aliases: ["children"] },
    { name: "show", type: "boolean", description: "Whether the child is visible" },
    { name: "preset", type: "string", optional: true, enum: [...TRANSITION_PRESETS] },
    { name: "duration", type: "number", optional: true, description: "Animation duration in ms (default 280)" },
    { name: "onExited", type: "callable", optional: true, description: "Fired once the exit animation has finished and the child has been removed" }
  ],
  render: (_node, props, helpers) => {
    const show = asBoolean(props.show);
    const presetRaw = asString(props.preset, "fade");
    const preset = TRANSITION_PRESETS.has(presetRaw) ? presetRaw : "fade";
    const duration = Math.max(0, Math.min(5e3, asNumber(props.duration, 280)));
    const reduce = typeof matchMedia !== "undefined" && matchMedia("(prefers-reduced-motion: reduce)").matches;
    const slot = helpers.useInstanceState("rui-transition", { phase: "hidden", ever: false });
    const prev = slot.get();
    const first = !prev.ever;
    const wrapper = el("div", {
      class: "rui-transition",
      "data-preset": preset,
      style: `--rui-transition-ms:${duration}ms`
    });
    const nodeSlot = helpers.useInstanceState("rui-transition-node", null);
    if (first) nodeSlot.set(wrapper);
    const live = () => {
      if (wrapper.isConnected) {
        nodeSlot.set(wrapper);
        return wrapper;
      }
      const node = nodeSlot.get();
      return node && node.isConnected ? node : null;
    };
    let phase;
    let mountChild;
    let state;
    if (show) {
      phase = "shown";
      mountChild = true;
      const needsFlip = !reduce && duration > 0 && prev.phase === "hidden";
      state = needsFlip ? "exit" : "enter";
      if (needsFlip) {
        deferToPaint(() => {
          if (slot.get().phase !== "shown") return;
          const node = live();
          if (!node) return;
          void node.offsetHeight;
          node.setAttribute("data-state", "enter");
        });
      }
      if (prev.phase === "exiting") helpers.registerDisposer(() => {
      }, "rui-transition-exit");
    } else if (prev.phase === "exiting") {
      phase = "exiting";
      mountChild = true;
      state = "exit";
    } else if (prev.phase === "shown" && duration > 0 && !reduce) {
      phase = "exiting";
      mountChild = true;
      state = "exit";
      const timer = setTimeout(() => {
        live()?.replaceChildren();
        slot.set({ phase: "hidden", ever: true });
        helpers.invoke(props.onExited);
      }, duration);
      helpers.registerDisposer(() => clearTimeout(timer), "rui-transition-exit");
    } else {
      phase = "hidden";
      mountChild = false;
      state = "exit";
      if (prev.phase !== "hidden") {
        const timer = setTimeout(() => helpers.invoke(props.onExited), 0);
        helpers.registerDisposer(() => clearTimeout(timer), "rui-transition-exit");
      }
    }
    wrapper.setAttribute("data-state", state);
    if (mountChild) wrapper.append(renderChild$1(helpers, props.child));
    slot.set({ phase, ever: true });
    return wrapper;
  }
};
const FLIP_EASING = "cubic-bezier(.22,1,.36,1)";
const FlipList = {
  name: "FlipList",
  description: "Smoothly animates its children when they reorder or are added (the FLIP technique — First/Last/Invert/Play). Wrap a keyed list whose order changes (drag-sort, filter, sort toggle) so items glide to their new positions instead of jumping, and new items fade in rather than popping. Removed items are dropped straight away — wrap a row in `Transition` if it needs to animate out. `horizontal` lays the items out in a row; `duration` is ms. Honours prefers-reduced-motion (no animation). Use stable `key:`s on the items so their DOM nodes persist across the reorder.",
  props: [
    { name: "children", aliases: ["child"], type: "Node[]", description: "The list items (use stable keys)" },
    { name: "duration", type: "number", optional: true, description: "Animation duration in ms (default 300)" },
    { name: "horizontal", type: "boolean", optional: true, description: "Lay the items out horizontally (tag chips, a filter bar, kanban columns)" }
  ],
  render: (_node, props, helpers) => {
    const duration = Math.max(0, Math.min(2e3, asNumber(props.duration, 300)));
    const horizontal = asBoolean(props.horizontal);
    const root = el("div", {
      class: "rui-flip-list",
      "data-orientation": horizontal ? "horizontal" : null,
      // Inline so the axis switch needs no extra stylesheet rule.
      style: horizontal ? "flex-direction:row" : null
    });
    for (const child of asArray(props.children)) root.append(renderChild$1(helpers, child));
    const reduce = typeof matchMedia !== "undefined" && matchMedia("(prefers-reduced-motion: reduce)").matches;
    if (reduce || typeof requestAnimationFrame === "undefined" || typeof MutationObserver === "undefined") return root;
    const durationSlot = helpers.useInstanceState("rui-flip-duration", duration);
    durationSlot.set(duration);
    const setup = helpers.useInstanceState("rui-flip-setup", { done: false });
    if (setup.get().done) return root;
    const positions = /* @__PURE__ */ new WeakMap();
    const seed = () => {
      for (const child of Array.from(root.children)) {
        const r = child.getBoundingClientRect();
        positions.set(child, { left: r.left, top: r.top });
      }
    };
    const runFlip = () => {
      const ms = durationSlot.get();
      const kids = Array.from(root.children);
      const rects = kids.map((k) => k.getBoundingClientRect());
      kids.forEach((child, i) => {
        const prev = positions.get(child);
        const rect = rects[i];
        if (typeof child.animate !== "function" || ms <= 0) return;
        if (prev) {
          const dx = prev.left - rect.left;
          const dy = prev.top - rect.top;
          if (dx !== 0 || dy !== 0) {
            child.animate(
              [{ transform: `translate(${dx}px, ${dy}px)` }, { transform: "none" }],
              { duration: ms, easing: FLIP_EASING }
            );
          }
          return;
        }
        child.animate(
          [{ opacity: 0, transform: "scale(.96)" }, { opacity: 1, transform: "none" }],
          { duration: ms, easing: FLIP_EASING }
        );
      });
      kids.forEach((child, i) => positions.set(child, { left: rects[i].left, top: rects[i].top }));
    };
    let raf = 0;
    const observer = new MutationObserver(() => {
      if (raf) return;
      raf = requestAnimationFrame(() => {
        raf = 0;
        runFlip();
      });
    });
    deferToPaint(() => {
      if (!root.isConnected) return;
      setup.set({ done: true });
      seed();
      observer.observe(root, { childList: true });
      helpers.registerDisposer(
        () => {
          observer.disconnect();
          if (raf) cancelAnimationFrame(raf);
        },
        "rui-flip-list"
      );
    });
    return root;
  }
};
const ROUTE_ANIMS = /* @__PURE__ */ new Set(["fade", "fade-up", "fade-down", "fade-left", "fade-right", "zoom", "slide-up"]);
const RouteView = {
  name: "RouteView",
  description: "Wraps a router's output so the page animates whenever the route changes. Pass `routeKey: route.path` so it knows when to replay the entrance animation; `animation` is fade|fade-up|zoom|… and `duration` is ms. Works by swapping a keyed inner wrapper, so the fresh page element plays its CSS entrance on mount. On every route change it also does what a real navigation does: scrolls back to the top (`scrollToTop`, default true), moves focus to the new page so keyboard users continue from there, and announces the new route politely (`announce` overrides the announced text). Honours prefers-reduced-motion.",
  props: [
    { name: "children", aliases: ["child"], type: "Node", positional: true, required: true, description: "The $router(...) result" },
    { name: "routeKey", type: "string", description: "Pass route.path so the view animates on change" },
    { name: "animation", type: "string", optional: true, enum: [...ROUTE_ANIMS] },
    { name: "duration", type: "number", optional: true, description: "Animation duration in ms (default 300)" },
    { name: "scrollToTop", type: "boolean", optional: true, description: "Reset the scroll position on every route change (default true) — without it a link clicked deep in a long page lands the reader mid-document" },
    { name: "announce", type: "string", optional: true, description: "Text read out on a route change (default: the routeKey). Pass the page title" }
  ],
  render: (_node, props, helpers) => {
    const animRaw = asString(props.animation, "fade");
    const animation = ROUTE_ANIMS.has(animRaw) ? animRaw : "fade";
    const duration = Math.max(0, Math.min(2e3, asNumber(props.duration, 300)));
    const routeKey = asString(props.routeKey, "page") || "page";
    const scrollToTop = props.scrollToTop === void 0 ? true : asBoolean(props.scrollToTop);
    const reduce = typeof matchMedia !== "undefined" && matchMedia("(prefers-reduced-motion: reduce)").matches;
    const root = el("div", { class: "rui-route-view" });
    const seen = helpers.useInstanceState("rui-route-seen", null);
    const changed = seen.get() != null && seen.get() !== routeKey;
    seen.set(routeKey);
    const page = el("div", {
      class: "rui-route-page",
      "data-rui-key": routeKey,
      "data-anim": reduce ? null : animation,
      style: reduce ? null : `--rui-route-ms:${duration}ms`,
      tabindex: "-1"
    });
    page.append(renderChild$1(helpers, props.children));
    root.append(page);
    root.append(el(
      "span",
      {
        class: "rui-visually-hidden",
        "data-rui-key": "rui-route-announce",
        "aria-live": "polite",
        "aria-atomic": "true"
      },
      [asString(props.announce) || routeKey]
    ));
    if (changed) {
      deferToPaint(() => {
        if (!page.isConnected) return;
        try {
          page.focus({ preventScroll: true });
        } catch {
          page.focus();
        }
        if (!scrollToTop) return;
        const scroller = nearestScrollable(page);
        if (scroller) {
          scroller.scrollTop = 0;
          return;
        }
        const doc = document.scrollingElement ?? document.documentElement;
        if (doc) doc.scrollTop = 0;
      });
    }
    return root;
  }
};
function deepActiveElement() {
  if (typeof document === "undefined") return null;
  let node = document.activeElement;
  while (node) {
    const inner = node.shadowRoot?.activeElement;
    if (!inner || inner === node) break;
    node = inner;
  }
  if (!node || node === document.body || node === document.documentElement) return null;
  return node;
}
function renderChildren(helpers, children) {
  const frag = document.createDocumentFragment();
  for (const child of asArray(children)) {
    if (child == null) continue;
    frag.append(typeof child === "string" ? document.createTextNode(child) : helpers.renderNode(child));
  }
  return frag;
}
const VisuallyHidden = {
  name: "VisuallyHidden",
  description: "Renders content that is invisible on screen but available to screen readers (the `sr-only` pattern). Use for icon-button labels, form hints, and context that sighted users get from layout but assistive tech needs.",
  props: [
    { name: "children", aliases: ["child"], type: "Node[]", description: "Content exposed only to assistive tech" }
  ],
  render: (_node, props, helpers) => {
    const root = el("span", { class: "rui-visually-hidden" });
    root.append(renderChildren(helpers, props.children));
    return root;
  }
};
const SkipLink = {
  name: "SkipLink",
  description: "A keyboard-only 'skip to content' link that is hidden until focused, then jumps to the element with the given id. Place it as the first node in a page so keyboard users can bypass the nav.",
  props: [
    { name: "to", type: "string", positional: true, required: true, description: "Target element id (with or without #)" },
    { name: "label", type: "string", optional: true, description: "Link text (default 'Skip to content')" }
  ],
  render: (_node, props) => {
    const to = asString(props.to, "main");
    const id = to.replace(/^#/, "");
    const link = el("a", { class: "rui-skip-link", href: `#${id}` });
    link.textContent = asString(props.label, "Skip to content");
    link.onclick = (event) => {
      const live = event.currentTarget ?? event.target ?? link;
      const scope = live.getRootNode();
      event.preventDefault();
      let target = null;
      try {
        target = scope.getElementById?.(id) ?? document.getElementById(id);
      } catch {
        target = null;
      }
      if (!target) {
        try {
          target = scope.querySelector?.(
            "main, [role='main'], .rui-root, h1"
          );
        } catch {
          target = null;
        }
      }
      if (!target) return;
      if (!target.hasAttribute("tabindex")) target.setAttribute("tabindex", "-1");
      try {
        target.focus({ preventScroll: true });
      } catch {
        target.focus();
      }
      try {
        target.scrollIntoView?.({ behavior: "smooth", block: "start" });
      } catch {
      }
    };
    return link;
  }
};
const LiveRegion = {
  name: "LiveRegion",
  description: "An aria-live region that announces its text to screen readers whenever it changes — toasts, validation summaries, async status. `politeness` is `polite` (default) or `assertive`. Visually hidden by default; set `visible` to also show it.",
  props: [
    { name: "text", type: "string", positional: true, aliases: ["children"] },
    { name: "politeness", type: "string", optional: true, enum: ["polite", "assertive"] },
    { name: "visible", type: "boolean", optional: true }
  ],
  render: (_node, props, helpers) => {
    const politeness = asString(props.politeness, "polite") === "assertive" ? "assertive" : "polite";
    const visible = props.visible === true || props.visible === "true";
    const root = el("div", {
      class: visible ? "rui-live-region" : "rui-live-region rui-visually-hidden",
      // Keep the role and the live setting in agreement: `role="status"` carries
      // an implicit `aria-live="polite"`, and screen readers are inconsistent
      // when that contradicts an explicit `assertive` — the urgent message ends
      // up queued behind whatever is being read.
      role: politeness === "assertive" ? "alert" : "status",
      "aria-live": politeness,
      "aria-atomic": "true"
    });
    if (props.text != null && typeof props.text !== "object") {
      root.textContent = String(props.text);
    } else {
      root.append(renderChildren(helpers, props.text));
    }
    return root;
  }
};
const FOCUSABLE = [
  "a[href]",
  "button:not([disabled])",
  "input:not([disabled])",
  "select:not([disabled])",
  "textarea:not([disabled])",
  "[tabindex]:not([tabindex='-1'])"
].join(",");
const FocusTrap = {
  name: "FocusTrap",
  description: "Confines keyboard focus to its subtree while `active` (default true) — Tab from the last focusable wraps to the first and vice-versa. Use inside modals, drawers, and command palettes. Auto-focuses the first focusable when it opens (`autoFocus: false` to opt out, or a CSS selector to pick the control), returns focus to whatever opened it on close (`restoreFocus: false` to opt out), and calls `onEscape` on the Escape key.",
  props: [
    { name: "child", type: "Node", positional: true, required: true, aliases: ["children"] },
    { name: "active", type: "boolean", optional: true },
    { name: "restoreFocus", type: "boolean", optional: true, description: "Return focus to the element that opened the trap when it closes (default true)" },
    { name: "onEscape", type: "callable", optional: true, description: "Called when Escape is pressed inside the trap — wire it to close the dialog/drawer" },
    { name: "autoFocus", type: "boolean | string", optional: true, description: '`false` to skip the initial focus, or a CSS selector inside the trap to focus instead of the first control (e.g. "[data-cancel]")' }
  ],
  render: (_node, props, helpers) => {
    const active = props.active === void 0 ? true : props.active === true || props.active === "true";
    const root = el("div", { class: "rui-focus-trap" });
    root.append(renderChildren(helpers, props.child));
    const focusState = helpers.useInstanceState("rui-focus-trap-focus", {
      armed: false,
      prev: null,
      restore: true,
      installed: false
    }).get();
    const restoreFocus2 = props.restoreFocus === void 0 ? true : asBoolean(props.restoreFocus, true);
    focusState.restore = restoreFocus2;
    const restorePreviousFocus = () => {
      const target = focusState.prev;
      focusState.armed = false;
      if (!focusState.restore || !target || typeof target.focus !== "function") {
        focusState.prev = null;
        return;
      }
      deferToPaint(() => {
        if (focusState.prev !== target) return;
        focusState.prev = null;
        if (target.isConnected) target.focus();
      });
    };
    if (!focusState.installed) {
      focusState.installed = true;
      helpers.registerDisposer(() => {
        const target = focusState.prev;
        focusState.armed = false;
        focusState.prev = null;
        if (focusState.restore && target?.isConnected && typeof target.focus === "function") {
          target.focus();
        }
      }, "rui-focus-trap-unmount");
    }
    if (!active) {
      if (focusState.armed) restorePreviousFocus();
      return root;
    }
    root.onkeydown = (event) => {
      const e = event;
      const live = e.currentTarget ?? e.target ?? root;
      const scopeRoot = live.getRootNode();
      if (e.key === "Escape") {
        if (typeof props.onEscape !== "function") return;
        e.preventDefault();
        e.stopPropagation();
        helpers.invoke(props.onEscape);
        return;
      }
      if (e.key !== "Tab") return;
      const nodes = Array.from(live.querySelectorAll(FOCUSABLE)).filter((n) => n.offsetParent !== null || n === scopeRoot.activeElement);
      if (nodes.length === 0) return;
      const first = nodes[0];
      const last = nodes[nodes.length - 1];
      const activeEl = scopeRoot.activeElement;
      if (e.shiftKey && activeEl === first) {
        e.preventDefault();
        last.focus();
      } else if (!e.shiftKey && activeEl === last) {
        e.preventDefault();
        first.focus();
      }
    };
    if (!focusState.armed) {
      focusState.armed = true;
      focusState.prev = deepActiveElement();
      const autoFocusRaw = props.autoFocus;
      const selector = typeof autoFocusRaw === "string" && autoFocusRaw !== "true" && autoFocusRaw !== "false" ? autoFocusRaw.trim() : "";
      const autoFocus = autoFocusRaw === void 0 || !!selector || asBoolean(autoFocusRaw, true);
      if (autoFocus) {
        const cancel = deferToPaint(() => {
          if (!root.isConnected) return;
          let target = null;
          if (selector) {
            try {
              target = root.querySelector(selector);
            } catch {
              target = null;
            }
          }
          target ?? (target = root.querySelectorAll(FOCUSABLE)[0] ?? null);
          if (target && typeof target.focus === "function") target.focus();
        });
        helpers.registerDisposer(cancel, "rui-focus-trap-autofocus");
      }
    }
    return root;
  }
};
const ECC_CODEWORDS_PER_BLOCK = {
  L: [-1, 7, 10, 15, 20, 26, 18, 20, 24, 30, 18, 20, 24, 26, 30, 22, 24, 28, 30, 28, 28, 28, 28, 30, 30, 26, 28, 30, 30, 30, 30, 30, 30, 30, 30, 30, 30, 30, 30, 30, 30],
  M: [-1, 10, 16, 26, 18, 24, 16, 18, 22, 22, 26, 30, 22, 22, 24, 24, 28, 28, 26, 26, 26, 26, 28, 28, 28, 28, 28, 28, 28, 28, 28, 28, 28, 28, 28, 28, 28, 28, 28, 28, 28],
  Q: [-1, 13, 22, 18, 26, 18, 24, 18, 22, 20, 24, 28, 26, 24, 20, 30, 24, 28, 28, 26, 30, 28, 30, 30, 30, 30, 28, 30, 30, 30, 30, 30, 30, 30, 30, 30, 30, 30, 30, 30, 30],
  H: [-1, 17, 28, 22, 16, 22, 28, 26, 26, 24, 28, 24, 28, 22, 24, 24, 30, 28, 28, 26, 28, 30, 24, 30, 30, 30, 30, 30, 30, 30, 30, 30, 30, 30, 30, 30, 30, 30, 30, 30, 30]
};
const NUM_ERROR_CORRECTION_BLOCKS = {
  L: [-1, 1, 1, 1, 1, 1, 2, 2, 2, 2, 4, 4, 4, 4, 4, 6, 6, 6, 6, 7, 8, 8, 9, 9, 10, 12, 12, 12, 13, 14, 15, 16, 17, 18, 19, 19, 20, 21, 22, 24, 25],
  M: [-1, 1, 1, 1, 2, 2, 4, 4, 4, 5, 5, 5, 8, 9, 9, 10, 10, 11, 13, 14, 16, 17, 17, 18, 20, 21, 23, 25, 26, 28, 29, 31, 33, 35, 37, 38, 40, 43, 45, 47, 49],
  Q: [-1, 1, 1, 2, 2, 4, 4, 6, 6, 8, 8, 8, 10, 12, 16, 12, 17, 16, 18, 21, 20, 23, 23, 25, 27, 29, 34, 34, 35, 38, 40, 43, 45, 48, 51, 53, 56, 59, 62, 65, 68],
  H: [-1, 1, 1, 2, 4, 4, 4, 5, 6, 8, 8, 11, 11, 16, 16, 18, 16, 19, 21, 25, 25, 25, 34, 30, 32, 35, 37, 40, 42, 45, 48, 51, 54, 57, 60, 63, 66, 70, 74, 77, 81]
};
const ALIGN_POSITIONS = (() => {
  const out = [[]];
  for (let v = 1; v <= 40; v += 1) {
    if (v === 1) {
      out.push([]);
      continue;
    }
    const numAlign = Math.floor(v / 7) + 2;
    const step = v === 32 ? 26 : Math.ceil((v * 4 + 4) / (numAlign * 2 - 2)) * 2;
    const result = [6];
    for (let pos = v * 4 + 10; result.length < numAlign; pos -= step) result.splice(1, 0, pos);
    out.push(result);
  }
  return out;
})();
const GF_EXP = new Uint8Array(512);
const GF_LOG = new Uint8Array(256);
(() => {
  let x = 1;
  for (let i = 0; i < 255; i += 1) {
    GF_EXP[i] = x;
    GF_LOG[x] = i;
    x <<= 1;
    if (x & 256) x ^= 285;
  }
  for (let i = 255; i < 512; i += 1) GF_EXP[i] = GF_EXP[i - 255];
})();
function gfMul(a, b) {
  if (a === 0 || b === 0) return 0;
  return GF_EXP[GF_LOG[a] + GF_LOG[b]];
}
function rsComputeDivisor(degree) {
  const result = new Uint8Array(degree);
  result[degree - 1] = 1;
  let root = 1;
  for (let i = 0; i < degree; i += 1) {
    for (let j = 0; j < degree; j += 1) {
      result[j] = gfMul(result[j], root);
      if (j + 1 < degree) result[j] = result[j] ^ result[j + 1];
    }
    root = gfMul(root, 2);
  }
  return result;
}
function rsComputeRemainder(data, divisor) {
  const result = new Uint8Array(divisor.length);
  for (const b of data) {
    const factor = b ^ result[0];
    result.copyWithin(0, 1);
    result[result.length - 1] = 0;
    for (let i = 0; i < result.length; i += 1) result[i] = result[i] ^ gfMul(divisor[i], factor);
  }
  return result;
}
function getNumDataCodewords(version, ecc) {
  const totalCodewords = getNumRawDataModules(version) >>> 3;
  const blocks = NUM_ERROR_CORRECTION_BLOCKS[ecc][version];
  const eccPerBlock = ECC_CODEWORDS_PER_BLOCK[ecc][version];
  return totalCodewords - eccPerBlock * blocks;
}
function getNumRawDataModules(version) {
  let result = (16 * version + 128) * version + 64;
  if (version >= 2) {
    const numAlign = Math.floor(version / 7) + 2;
    result -= (25 * numAlign - 10) * numAlign - 55;
    if (version >= 7) result -= 36;
  }
  return result;
}
const ECC_FORMAT_BITS = { L: 1, M: 0, Q: 3, H: 2 };
function encodeQr(text, ecc = "M") {
  const bytes = utf8Bytes(text);
  let version = 1;
  for (; version <= 40; version += 1) {
    const capacityBits = getNumDataCodewords(version, ecc) * 8;
    const charCountBits = version <= 9 ? 8 : 16;
    const usedBits = 4 + charCountBits + bytes.length * 8;
    if (usedBits <= capacityBits) break;
  }
  if (version > 40) throw new Error("Data too long for QR code");
  const bits = [];
  const appendBits = (val, len) => {
    for (let i = len - 1; i >= 0; i -= 1) bits.push(val >>> i & 1);
  };
  appendBits(4, 4);
  appendBits(bytes.length, version <= 9 ? 8 : 16);
  for (const b of bytes) appendBits(b, 8);
  const dataCapacityBits = getNumDataCodewords(version, ecc) * 8;
  appendBits(0, Math.min(4, dataCapacityBits - bits.length));
  while (bits.length % 8 !== 0) bits.push(0);
  for (let pad = 236; bits.length < dataCapacityBits; pad ^= 236 ^ 17) appendBits(pad, 8);
  const dataCodewords = new Uint8Array(bits.length >>> 3);
  for (let i = 0; i < bits.length; i += 1) dataCodewords[i >>> 3] = dataCodewords[i >>> 3] | bits[i] << 7 - (i & 7);
  const allCodewords = addEccAndInterleave(dataCodewords, version, ecc);
  return buildMatrix(version, ecc, allCodewords);
}
function addEccAndInterleave(data, version, ecc) {
  const numBlocks = NUM_ERROR_CORRECTION_BLOCKS[ecc][version];
  const blockEccLen = ECC_CODEWORDS_PER_BLOCK[ecc][version];
  const rawCodewords = getNumRawDataModules(version) >>> 3;
  const numShortBlocks = numBlocks - rawCodewords % numBlocks;
  const shortBlockLen = Math.floor(rawCodewords / numBlocks);
  const blocks = [];
  const divisor = rsComputeDivisor(blockEccLen);
  let k = 0;
  for (let i = 0; i < numBlocks; i += 1) {
    const datLen = shortBlockLen - blockEccLen + (i < numShortBlocks ? 0 : 1);
    const dat = data.slice(k, k + datLen);
    k += datLen;
    const eccBytes = rsComputeRemainder(dat, divisor);
    const block = new Uint8Array(shortBlockLen + 1);
    block.set(dat, 0);
    block.set(eccBytes, block.length - blockEccLen);
    blocks.push(block);
  }
  const result = new Uint8Array(rawCodewords);
  let idx = 0;
  for (let i = 0; i < blocks[0].length; i += 1) {
    for (let j = 0; j < blocks.length; j += 1) {
      if (i !== shortBlockLen - blockEccLen || j >= numShortBlocks) {
        result[idx++] = blocks[j][i];
      }
    }
  }
  return result;
}
function buildMatrix(version, ecc, codewords) {
  const size2 = version * 4 + 17;
  const modules = Array.from({ length: size2 }, () => new Array(size2).fill(false));
  const isFunction = Array.from({ length: size2 }, () => new Array(size2).fill(false));
  const setFunc = (x, y, dark) => {
    modules[y][x] = dark;
    isFunction[y][x] = true;
  };
  const drawFinder = (cx, cy) => {
    for (let dy = -4; dy <= 4; dy += 1) {
      for (let dx = -4; dx <= 4; dx += 1) {
        const dist = Math.max(Math.abs(dx), Math.abs(dy));
        const x = cx + dx, y = cy + dy;
        if (x >= 0 && x < size2 && y >= 0 && y < size2) setFunc(x, y, dist !== 2 && dist !== 4);
      }
    }
  };
  for (let i = 0; i < size2; i += 1) {
    setFunc(6, i, i % 2 === 0);
    setFunc(i, 6, i % 2 === 0);
  }
  drawFinder(3, 3);
  drawFinder(size2 - 4, 3);
  drawFinder(3, size2 - 4);
  const align = ALIGN_POSITIONS[version];
  for (const ay of align) {
    for (const ax of align) {
      if (ax === 6 && ay === 6 || ax === 6 && ay === size2 - 7 || ax === size2 - 7 && ay === 6) continue;
      for (let dy = -2; dy <= 2; dy += 1) {
        for (let dx = -2; dx <= 2; dx += 1) {
          setFunc(ax + dx, ay + dy, Math.max(Math.abs(dx), Math.abs(dy)) !== 1);
        }
      }
    }
  }
  const reserveFormat = () => {
    for (let i = 0; i < 9; i += 1) {
      if (!isFunction[i][8]) setFunc(8, i, false);
      if (!isFunction[8][i]) setFunc(i, 8, false);
    }
    for (let i = 0; i < 8; i += 1) {
      setFunc(size2 - 1 - i, 8, false);
      setFunc(8, size2 - 1 - i, false);
    }
    setFunc(8, size2 - 8, true);
  };
  reserveFormat();
  if (version >= 7) {
    for (let i = 0; i < 18; i += 1) {
      const x = i % 3, y = Math.floor(i / 3);
      setFunc(size2 - 11 + x, y, false);
      setFunc(y, size2 - 11 + x, false);
    }
  }
  let bitIdx = 0;
  const totalBits = codewords.length * 8;
  for (let right = size2 - 1; right >= 1; right -= 2) {
    const col = right === 6 ? 5 : right;
    for (let vert = 0; vert < size2; vert += 1) {
      for (let j = 0; j < 2; j += 1) {
        const x = col - j;
        const upward = (col + 1 & 2) === 0;
        const y = upward ? size2 - 1 - vert : vert;
        if (isFunction[y][x]) continue;
        let dark = false;
        if (bitIdx < totalBits) {
          dark = (codewords[bitIdx >>> 3] >>> 7 - (bitIdx & 7) & 1) !== 0;
          bitIdx += 1;
        }
        modules[y][x] = dark;
      }
    }
  }
  let minPenalty = Infinity;
  let bestModules = modules;
  for (let mask = 0; mask < 8; mask += 1) {
    const trial = modules.map((row) => row.slice());
    applyMask(trial, isFunction, mask);
    drawFormatBits(trial, isFunction, ecc, mask, size2);
    const penalty = penaltyScore(trial, size2);
    if (penalty < minPenalty) {
      minPenalty = penalty;
      bestModules = trial;
    }
  }
  return bestModules;
}
function applyMask(modules, isFunction, mask) {
  const size2 = modules.length;
  for (let y = 0; y < size2; y += 1) {
    for (let x = 0; x < size2; x += 1) {
      if (isFunction[y][x]) continue;
      let invert = false;
      switch (mask) {
        case 0:
          invert = (x + y) % 2 === 0;
          break;
        case 1:
          invert = y % 2 === 0;
          break;
        case 2:
          invert = x % 3 === 0;
          break;
        case 3:
          invert = (x + y) % 3 === 0;
          break;
        case 4:
          invert = (Math.floor(x / 3) + Math.floor(y / 2)) % 2 === 0;
          break;
        case 5:
          invert = x * y % 2 + x * y % 3 === 0;
          break;
        case 6:
          invert = (x * y % 2 + x * y % 3) % 2 === 0;
          break;
        case 7:
          invert = ((x + y) % 2 + x * y % 3) % 2 === 0;
          break;
      }
      if (invert) modules[y][x] = !modules[y][x];
    }
  }
}
function drawFormatBits(modules, isFunction, ecc, mask, size2) {
  const data = ECC_FORMAT_BITS[ecc] << 3 | mask;
  let rem = data;
  for (let i = 0; i < 10; i += 1) rem = rem << 1 ^ (rem >>> 9) * 1335;
  const bits = (data << 10 | rem) ^ 21522;
  const get = (i) => (bits >>> i & 1) !== 0;
  for (let i = 0; i <= 5; i += 1) modules[i][8] = get(i);
  modules[7][8] = get(6);
  modules[8][8] = get(7);
  modules[8][7] = get(8);
  for (let i = 9; i < 15; i += 1) modules[8][14 - i] = get(i);
  for (let i = 0; i < 8; i += 1) modules[8][size2 - 1 - i] = get(i);
  for (let i = 8; i < 15; i += 1) modules[size2 - 15 + i][8] = get(i);
  modules[size2 - 8][8] = true;
}
function penaltyScore(modules, size2) {
  let penalty = 0;
  for (let y = 0; y < size2; y += 1) {
    let runColor = modules[y][0], runLen = 1;
    for (let x = 1; x < size2; x += 1) {
      if (modules[y][x] === runColor) {
        runLen += 1;
        if (runLen === 5) penalty += 3;
        else if (runLen > 5) penalty += 1;
      } else {
        runColor = modules[y][x];
        runLen = 1;
      }
    }
  }
  for (let x = 0; x < size2; x += 1) {
    let runColor = modules[0][x], runLen = 1;
    for (let y = 1; y < size2; y += 1) {
      if (modules[y][x] === runColor) {
        runLen += 1;
        if (runLen === 5) penalty += 3;
        else if (runLen > 5) penalty += 1;
      } else {
        runColor = modules[y][x];
        runLen = 1;
      }
    }
  }
  for (let y = 0; y < size2 - 1; y += 1) {
    for (let x = 0; x < size2 - 1; x += 1) {
      const c = modules[y][x];
      if (c === modules[y][x + 1] && c === modules[y + 1][x] && c === modules[y + 1][x + 1]) penalty += 3;
    }
  }
  return penalty;
}
function utf8Bytes(text) {
  if (typeof TextEncoder !== "undefined") return new TextEncoder().encode(text);
  const out = [];
  for (const ch of text) {
    const cp = ch.codePointAt(0);
    if (cp < 128) out.push(cp);
    else if (cp < 2048) out.push(192 | cp >> 6, 128 | cp & 63);
    else out.push(224 | cp >> 12, 128 | cp >> 6 & 63, 128 | cp & 63);
  }
  return new Uint8Array(out);
}
const SVG_NS = "http://www.w3.org/2000/svg";
function svgEl(tag, attrs) {
  const node = document.createElementNS(SVG_NS, tag);
  for (const [k, v] of Object.entries(attrs)) {
    if (v != null) node.setAttribute(k, v);
  }
  return node;
}
const QRCode = {
  name: "QRCode",
  description: "Renders an offline QR code as crisp SVG from `data` (a URL or text). `size` is the pixel side length and acts as a maximum — the code scales down to fit a narrower container. `ecc` is the error-correction level (L|M|Q|H, default M), `color`/`background` override the modules, `label` sets the accessible name. No network — encodes fully in the browser.",
  props: [
    { name: "data", type: "string", positional: true, required: true, aliases: ["value", "text"], description: "URL or text to encode" },
    { name: "size", type: "number", optional: true, description: "Side length in px (default 160); scales down on narrower viewports" },
    { name: "ecc", type: "string", optional: true, enum: ["L", "M", "Q", "H"] },
    { name: "color", type: "string", optional: true, description: "Module (dark) color" },
    { name: "background", type: "string", optional: true, description: 'Background color — default opaque white, which reliable scanning needs; pass "transparent" to drop the backing rect' },
    { name: "margin", type: "number", optional: true, description: "Quiet-zone modules (default 2)" },
    { name: "label", type: "string", optional: true, aliases: ["alt"], description: 'Accessible name (default "QR code for <data>") — use it so screen readers do not read out a whole signed URL' }
  ],
  render: (_node, props) => {
    const data = asString(props.data);
    const size2 = Math.max(48, Math.min(1024, asNumber(props.size, 160)));
    const label = asString(props.label);
    const eccRaw = asString(props.ecc, "M").toUpperCase();
    const ecc = ["L", "M", "Q", "H"].includes(eccRaw) ? eccRaw : "M";
    const color2 = sanitiseCssColor(props.color) || "#000000";
    const background = sanitiseCssColor(props.background) || "#ffffff";
    const margin = Math.max(0, Math.min(8, asNumber(props.margin, 2)));
    const root = el("div", { class: "rui-qrcode" });
    let matrix;
    try {
      matrix = encodeQr(data || " ", ecc);
    } catch {
      root.append(el("div", { class: "rui-qrcode-error" }, ["Data too long for QR"]));
      return root;
    }
    const count = matrix.length;
    const dim = count + margin * 2;
    const svg = svgEl("svg", {
      viewBox: `0 0 ${dim} ${dim}`,
      width: String(size2),
      height: String(size2),
      role: "img",
      // A raw payload (a signed checkout URL, say) is an unusable accessible
      // name, so `label` wins verbatim when the author supplies one.
      "aria-label": label || `QR code for ${data}`,
      "shape-rendering": "crispEdges",
      // `size` is a max, not a fixed dimension: a 600px code inside a 375px
      // phone column would otherwise force the page to scroll sideways. Inline
      // so the constraint holds no matter which theme is adopted.
      style: "max-width:100%;height:auto"
    });
    svg.append(svgEl("rect", { x: "0", y: "0", width: String(dim), height: String(dim), fill: background }));
    let d = "";
    for (let y = 0; y < count; y += 1) {
      for (let x = 0; x < count; x += 1) {
        if (matrix[y][x]) d += `M${x + margin},${y + margin}h1v1h-1z`;
      }
    }
    svg.append(svgEl("path", { d, fill: color2 }));
    root.append(svg);
    return root;
  }
};
const ReactionPicker = {
  name: "ReactionPicker",
  description: 'A row of emoji reactions with counts. `reactions` is a list of { emoji, count?, active?, label? } — `label` names the reaction ("You and 2 others") for the tooltip and the accessible name. `onReact(emoji)` fires on click; `disabled` makes the whole bar read-only. Use under messages, comments, and posts.',
  props: [
    { name: "reactions", type: "object[]", positional: true, required: true, description: "[{ emoji, count?, active?, label? }]" },
    { name: "onReact", type: "callable", optional: true, description: "(emoji) => …" },
    { name: "disabled", type: "boolean", optional: true, description: "Render every pill inert — archived threads, locked issues, logged-out visitors" }
  ],
  render: (_node, props, helpers) => {
    const reactions = asArray(props.reactions);
    const disabled = asBoolean(props.disabled);
    const root = el("div", { class: "rui-reaction-picker", role: "group", "aria-label": "Reactions" });
    for (const r of reactions) {
      const rec = r && typeof r === "object" ? r : {};
      const emoji = asString(rec.emoji, "👍");
      const count = asNumber(rec.count, 0);
      const active = asBoolean(rec.active);
      const label = asString(rec.label);
      const name = label || `React with ${emoji}`;
      const btn = el("button", {
        type: "button",
        class: "rui-reaction",
        "data-active": active ? "true" : null,
        "aria-pressed": active ? "true" : "false",
        "aria-label": count > 0 ? `${name} (${count})` : name,
        title: label || null,
        disabled
      });
      btn.append(el("span", { class: "rui-reaction-emoji" }, [emoji]));
      if (count > 0) btn.append(el("span", { class: "rui-reaction-count" }, [String(count)]));
      if (!disabled) btn.onclick = () => helpers.invoke(props.onReact, emoji);
      root.append(btn);
    }
    return root;
  }
};
const CURSOR_LABEL_GAP = 14;
function cursorLabelStyle(color2, side) {
  const flip = side === "left" ? `;right:${CURSOR_LABEL_GAP}px;left:auto` : "";
  return `background:${color2}${flip}`;
}
const LiveCursor = {
  name: "LiveCursor",
  description: 'A floating collaborator cursor positioned at `x`/`y` with a name label — drive it from presence/`$socket` state for multiplayer UIs. `space` declares what x/y are measured against: "parent" (default) is the nearest positioned ancestor, so give the shared surface `position: relative`; "viewport" treats them as viewport coordinates and escapes `overflow: hidden` clipping. `smooth` glides between presence ticks, `typing` shows a typing indicator. Pointer-events are disabled so it never blocks interaction.',
  props: [
    { name: "x", type: "number", description: "X position in px, relative to `space`" },
    { name: "y", type: "number", description: "Y position in px, relative to `space`" },
    { name: "label", type: "string", optional: true, description: "Collaborator name" },
    { name: "color", type: "string", optional: true },
    { name: "space", type: "string", optional: true, enum: ["parent", "viewport"], description: "Coordinate frame for x/y (default `parent`)" },
    { name: "smooth", type: "boolean", optional: true, description: "Interpolate movement between presence updates (default true; ignored under prefers-reduced-motion)" },
    { name: "typing", type: "boolean", optional: true, description: "Show a typing indicator on the cursor" }
  ],
  render: (_node, props, helpers) => {
    const x = asNumber(props.x, 0);
    const y = asNumber(props.y, 0);
    const color2 = sanitiseCssColor(props.color) || "var(--rui-color-primary)";
    const space2 = asString(props.space, "parent") === "viewport" ? "viewport" : "parent";
    const reduce = typeof matchMedia !== "undefined" && matchMedia("(prefers-reduced-motion: reduce)").matches;
    const smooth = asBoolean(props.smooth, true) && !reduce;
    const typing = asBoolean(props.typing);
    const label = asString(props.label);
    const metricsSlot = helpers.useInstanceState(
      "rui-cursor-label-metrics",
      { frame: 0, label: 0 }
    );
    const metrics = metricsSlot.get();
    const side = metrics.frame > 0 && x + CURSOR_LABEL_GAP + metrics.label > metrics.frame ? "left" : "right";
    const root = el("div", {
      class: "rui-live-cursor",
      "data-space": space2,
      "data-typing": typing ? "true" : null,
      // `position` and `transition` are inline so both behaviours hold without
      // a theme rule: `fixed` is what lets `space: "viewport"` escape a
      // clipping ancestor, and without a transition a 10Hz presence feed makes
      // every remote cursor teleport in ~40px steps.
      style: [
        `transform:translate(${x}px,${y}px)`,
        `--rui-cursor-color:${color2}`,
        space2 === "viewport" ? "position:fixed" : "",
        smooth ? "transition:transform 90ms linear" : ""
      ].filter(Boolean).join(";")
    });
    const arrow = svgEl("svg", { width: "20", height: "20", viewBox: "0 0 20 20", fill: "none" });
    arrow.append(svgEl("path", { d: "M3 3l14 6-6 2-2 6z", fill: color2, stroke: "#fff", "stroke-width": "1" }));
    root.append(arrow);
    let bubble = null;
    if (label || typing) {
      bubble = el("span", {
        class: "rui-live-cursor-label",
        "data-side": side,
        style: cursorLabelStyle(color2, side)
      }, [label]);
      if (typing) bubble.append(el("span", { class: "rui-live-cursor-typing", "aria-hidden": "true" }, ["…"]));
      root.append(bubble);
    }
    if (bubble && metrics.frame === 0) {
      const labelNode = bubble;
      deferToPaint(() => {
        if (!root.isConnected) return;
        const frame = space2 === "viewport" ? typeof window === "undefined" ? 0 : window.innerWidth : root.offsetParent?.clientWidth ?? 0;
        const width = labelNode.offsetWidth;
        if (frame <= 0 || width <= 0) return;
        const current = metricsSlot.get();
        if (current.frame === frame && current.label === width) return;
        metricsSlot.set({ frame, label: width });
        const next = x + CURSOR_LABEL_GAP + width > frame ? "left" : "right";
        labelNode.setAttribute("data-side", next);
        labelNode.setAttribute("style", cursorLabelStyle(color2, next));
      });
    }
    return root;
  }
};
const TabBar = {
  name: "TabBar",
  description: 'Bottom navigation bar for mobile layouts — a row of icon+label tabs. `items` is [{ id, label, icon?, badge?, href?, disabled? }]; an item with `href` renders a real link ("/reports" targets a route, so middle-click and copy-link work); `active` is the current id; `onChange(id)` fires on tap. `pinned` (default true) sticks the bar to the bottom of its scroll container; safe-area padding is always applied.',
  props: [
    { name: "items", type: "object[]", positional: true, required: true, description: "[{ id, label, icon?, badge?, href?, disabled? }]" },
    { name: "active", type: "string", optional: true },
    { name: "onChange", type: "callable", optional: true, description: "(id) => …" },
    { name: "pinned", type: "boolean", optional: true, description: "Stick to the bottom of the scroll container (default true)" }
  ],
  render: (_node, props, helpers) => {
    const items = asArray(props.items);
    const active = asString(props.active);
    const pinned = asBoolean(props.pinned, true);
    const root = el("nav", {
      class: "rui-tabbar",
      "aria-label": "Primary",
      "data-pinned": pinned ? "true" : null,
      // Inline so pinning does not depend on a theme rule. `sticky` rather than
      // `fixed`: it survives the transformed / filtered ancestors the library
      // creates itself (`animate`, the glass theme's backdrop filters).
      style: pinned ? "position:sticky;bottom:0;z-index:var(--rui-z-sticky,100)" : null
    });
    for (const it of items) {
      const rec = it && typeof it === "object" ? it : {};
      const id = asString(rec.id ?? rec.label);
      const isActive = id === active && id !== "";
      const disabled = asBoolean(rec.disabled);
      const rawHref = asString(rec.href);
      const href = disabled ? "" : rawHref.startsWith("/") ? `#${rawHref}` : sanitiseHref(rawHref, "");
      const tab = href ? el("a", {
        class: "rui-tabbar-item",
        href,
        "data-active": isActive ? "true" : null,
        "aria-current": isActive ? "page" : null,
        style: "text-decoration:none"
      }) : el("button", {
        type: "button",
        class: "rui-tabbar-item",
        disabled,
        "data-active": isActive ? "true" : null,
        "aria-current": isActive ? "page" : null
      });
      const icon = renderIcon(rec.icon, { className: "rui-tabbar-icon" });
      const badge = asString(rec.badge);
      if (icon || badge) {
        const wrap = el("span", {
          class: "rui-tabbar-icon-wrap",
          style: icon ? null : "min-width:1em;min-height:1em;display:inline-block"
        });
        if (icon) wrap.append(icon);
        if (badge) wrap.append(el("span", { class: "rui-tabbar-badge" }, [badge]));
        tab.append(wrap);
      }
      const label = asString(rec.label);
      if (label) tab.append(el("span", { class: "rui-tabbar-label" }, [label]));
      if (id && !disabled) tab.onclick = () => helpers.invoke(props.onChange, id);
      root.append(tab);
    }
    return root;
  }
};
function renderChild(helpers, child) {
  if (child == null) return null;
  return typeof child === "string" ? document.createTextNode(child) : helpers.renderNode(child);
}
const Cart = {
  name: "Cart",
  description: "A shopping-cart line list: each item shows image, name, unit price, a quantity stepper, and a remove button, with a running subtotal. `items` is [{ id, name, price, qty, image?, max? }] — `max` caps the stepper at available stock. `onQty(id, qty)` and `onRemove(id)` fire on interaction; omit `onQty` for a read-only receipt view. `disabled` freezes every control (e.g. while checkout submits), `loading` shows placeholder lines, `error` renders a message above the lines. `currency` is an ISO code (default USD).",
  props: [
    { name: "items", type: "object[]", positional: true, required: true, description: "[{ id, name, price, qty, image?, max? }]" },
    { name: "onQty", type: "callable", optional: true, description: "(id, qty) => …" },
    { name: "onRemove", type: "callable", optional: true, description: "(id) => …" },
    { name: "currency", type: "string", optional: true },
    { name: "footer", type: "Node", optional: true, description: "Custom footer (e.g. a checkout button)" },
    { name: "disabled", type: "boolean", optional: true, aliases: ["busy"], description: "Freeze every stepper and remove button — use while a mutation or the checkout round-trip is outstanding" },
    { name: "loading", type: "boolean", optional: true, description: "Show placeholder lines while the cart hydrates (keeps scroll position)" },
    { name: "error", type: "string", optional: true, description: "Error message rendered above the lines (a failed quantity update, a stock conflict)" }
  ],
  render: (_node, props, helpers) => {
    const items = asArray(props.items);
    const currency = asString(props.currency, "USD") || "USD";
    const disabled = asBoolean(props.disabled);
    const loading = asBoolean(props.loading);
    const error = asString(props.error);
    const canEditQty = typeof props.onQty === "function";
    const canRemove = typeof props.onRemove === "function";
    const fmt = (n) => {
      try {
        return new Intl.NumberFormat(void 0, { style: "currency", currency }).format(n);
      } catch {
        return `${n.toFixed(2)} ${currency}`;
      }
    };
    const root = el("div", {
      class: "rui-cart",
      "aria-busy": loading ? "true" : null,
      "data-disabled": disabled ? "true" : null
    });
    if (error) root.append(el("div", { class: "rui-cart-error", role: "alert" }, [error]));
    if (loading) {
      for (let i = 0; i < 3; i += 1) {
        const row = el("div", { class: "rui-cart-line", "aria-hidden": "true" });
        row.append(el("div", { class: "rui-skeleton-line", style: "width:48px;height:48px;flex-shrink:0" }));
        const body = el("div", { class: "rui-cart-body" });
        body.append(el("div", { class: "rui-skeleton-line", style: "height:12px;width:55%" }));
        body.append(el("div", { class: "rui-skeleton-line", style: "height:10px;width:30%;margin-top:6px" }));
        row.append(body);
        root.append(row);
      }
      return root;
    }
    if (items.length === 0) {
      root.append(el("div", { class: "rui-cart-empty" }, ["Your cart is empty"]));
      return root;
    }
    let subtotal = 0;
    for (const it of items) {
      const rec = it && typeof it === "object" ? it : {};
      const id = asString(rec.id ?? rec.name);
      const name = asString(rec.name);
      const price = asNumber(rec.price, 0);
      const qty = Math.max(0, asNumber(rec.qty, 1));
      const max = rec.max == null ? Infinity : Math.max(0, asNumber(rec.max, Infinity));
      subtotal += price * qty;
      const row = el("div", { class: "rui-cart-line" });
      const image = sanitiseImageSrc(rec.image);
      if (image) row.append(el("img", { class: "rui-cart-thumb", src: image, alt: name, loading: "lazy" }));
      const body = el("div", { class: "rui-cart-body" });
      body.append(el("div", { class: "rui-cart-name" }, [name]));
      body.append(el("div", { class: "rui-cart-price" }, [fmt(price)]));
      row.append(body);
      const stepper = el("div", { class: "rui-cart-qty" });
      if (canEditQty && id) {
        const dec = el("button", {
          type: "button",
          class: "rui-cart-qty-btn",
          "aria-label": "Decrease quantity",
          disabled: disabled || qty <= 0
        }, ["−"]);
        const inc = el("button", {
          type: "button",
          class: "rui-cart-qty-btn",
          "aria-label": "Increase quantity",
          disabled: disabled || qty >= max
        }, ["+"]);
        const step = (event, delta) => {
          const btn = event.currentTarget ?? event.target;
          const cell = btn?.closest(".rui-cart-line")?.querySelector(".rui-cart-qty-value") ?? null;
          const shownText = asString(cell?.textContent).trim();
          const shown = shownText === "" ? NaN : Number(shownText);
          const base = Number.isFinite(shown) ? shown : qty;
          const next = Math.min(max, Math.max(0, base + delta));
          if (cell) cell.textContent = String(next);
          helpers.invoke(props.onQty, id, next);
        };
        dec.onclick = (event) => step(event, -1);
        inc.onclick = (event) => step(event, 1);
        stepper.append(dec, el("span", { class: "rui-cart-qty-value" }, [String(qty)]), inc);
      } else {
        stepper.append(el("span", { class: "rui-cart-qty-value" }, [`×${qty}`]));
      }
      row.append(stepper);
      row.append(el("div", { class: "rui-cart-line-total" }, [fmt(price * qty)]));
      if (canRemove && id) {
        const rm = el("button", {
          type: "button",
          class: "rui-cart-remove",
          "aria-label": `Remove ${name}`,
          disabled
        });
        const icon = renderIcon("xmark", { className: "rui-cart-remove-icon" });
        if (icon) rm.append(icon);
        else rm.append(document.createTextNode("×"));
        rm.onclick = () => helpers.invoke(props.onRemove, id);
        row.append(rm);
      }
      root.append(row);
    }
    const foot = el("div", { class: "rui-cart-foot" });
    foot.append(el("span", { class: "rui-cart-subtotal-label" }, ["Subtotal"]));
    foot.append(el("span", { class: "rui-cart-subtotal-value" }, [fmt(subtotal)]));
    root.append(foot);
    const footer = renderChild(helpers, props.footer);
    if (footer) root.append(footer);
    return root;
  }
};
const WEEKDAY_LABELS = ["SUN", "MON", "TUE", "WED", "THU", "FRI", "SAT"];
const MONTH_NAMES = [
  "January",
  "February",
  "March",
  "April",
  "May",
  "June",
  "July",
  "August",
  "September",
  "October",
  "November",
  "December"
];
const CHIP_TONES = {
  primary: "var(--rui-color-primary)",
  success: "var(--rui-color-success, #10b981)",
  warning: "var(--rui-color-warning, #f59e0b)",
  danger: "var(--rui-color-danger, #ef4444)",
  info: "var(--rui-color-info, #06b6d4)"
};
const pad2 = (n) => String(n).padStart(2, "0");
const isoOf = (y, m, d) => `${y}-${pad2(m + 1)}-${pad2(d)}`;
const isoDay = (raw) => asString(raw).slice(0, 10);
function readGcalEvents(raw) {
  const chips = /* @__PURE__ */ new Map();
  const dots = /* @__PURE__ */ new Map();
  for (const entry of asArray(raw)) {
    if (entry == null) continue;
    if (typeof entry === "string") {
      const key = entry.slice(0, 10);
      if (key) dots.set(key, (dots.get(key) ?? 0) + 1);
      continue;
    }
    if (typeof entry !== "object") continue;
    const e = entry;
    const date = asString(e.date).slice(0, 10);
    if (!date) continue;
    const label = asString(e.label ?? e.title);
    if (!label) {
      dots.set(date, (dots.get(date) ?? 0) + 1);
      continue;
    }
    const colorRaw = asString(e.color ?? e.tone, "primary");
    const color2 = CHIP_TONES[colorRaw] ?? sanitiseCssColor(colorRaw) ?? CHIP_TONES.primary;
    const list = chips.get(date) ?? [];
    list.push({ label, color: color2, time: asString(e.time), raw: entry });
    chips.set(date, list);
  }
  return { chips, dots };
}
function resolveLabels(locale, weekdayLabels, monthLabels) {
  let weekdays = WEEKDAY_LABELS.slice();
  let months = MONTH_NAMES.slice();
  if (locale && typeof Intl !== "undefined") {
    try {
      const wd = new Intl.DateTimeFormat(locale, { weekday: "short", timeZone: "UTC" });
      weekdays = Array.from({ length: 7 }, (_unused, i) => wd.format(new Date(Date.UTC(1970, 0, 4 + i))).toUpperCase());
      const mo = new Intl.DateTimeFormat(locale, { month: "long", timeZone: "UTC" });
      months = Array.from({ length: 12 }, (_unused, i) => mo.format(new Date(Date.UTC(2021, i, 15))));
    } catch {
    }
  }
  const wdOverride = asArray(weekdayLabels).map((v) => asString(v)).filter(Boolean);
  if (wdOverride.length === 7) weekdays = wdOverride;
  const moOverride = asArray(monthLabels).map((v) => asString(v)).filter(Boolean);
  if (moOverride.length === 12) months = moOverride;
  return { weekdays, months };
}
function isBlocked(cfg2, iso) {
  if (cfg2.minIso && iso < cfg2.minIso) return true;
  if (cfg2.maxIso && iso > cfg2.maxIso) return true;
  return cfg2.blocked.has(iso);
}
function captureFocus(root) {
  const scope = root.getRootNode();
  const active = scope.activeElement;
  if (!active || !root.contains(active)) return null;
  if (active.classList.contains("rui-gcal-today")) return ".rui-gcal-today";
  const dir = active.getAttribute("data-dir");
  if (dir) return `.rui-gcal-nav[data-dir="${dir}"]`;
  const iso = active.closest(".rui-gcal-day")?.getAttribute("data-iso");
  return iso ? `.rui-gcal-day[data-iso="${iso}"]` : null;
}
function restoreFocus(root, selector) {
  if (!selector) return;
  const target = root.querySelector(selector) ?? (selector.startsWith(".rui-gcal-day") ? root.querySelector('.rui-gcal-day[tabindex="0"]') : null);
  target?.focus();
}
function buildCalendarInto(root, year, month, cfg2) {
  const liveRootOf = (event) => (event.currentTarget ?? event.target)?.closest(".rui-gcal") ?? root;
  const navigateTo = (event, y, m) => {
    const live = liveRootOf(event);
    const prev = cfg2.viewSlot.get();
    cfg2.viewSlot.set({
      propsYear: prev?.propsYear ?? y,
      propsMonth: prev?.propsMonth ?? m,
      year: y,
      month: m
    });
    buildCalendarInto(live, y, m, cfg2);
    cfg2.helpers.invoke(cfg2.onNavigate, y, m);
  };
  const navigateBy = (event, delta) => {
    const current = cfg2.viewSlot.get();
    const baseYear = current?.year ?? year;
    const baseMonth = current?.month ?? month;
    const shifted = new Date(baseYear, baseMonth + delta, 1);
    navigateTo(event, shifted.getFullYear(), shifted.getMonth());
  };
  const title = `${cfg2.months[month] ?? MONTH_NAMES[month]} ${year}`;
  const toolbar = el("div", { class: "rui-gcal-toolbar" });
  if (cfg2.navigable) {
    const todayBtn = el("button", { type: "button", class: "rui-gcal-today" }, ["Today"]);
    todayBtn.onclick = (event) => {
      const now = /* @__PURE__ */ new Date();
      navigateTo(event, now.getFullYear(), now.getMonth());
    };
    const prevBtn = el("button", { type: "button", class: "rui-gcal-nav", "data-dir": "prev", "aria-label": "Previous month" });
    const prevIcon = renderIcon("chevron-left");
    if (prevIcon) prevBtn.append(prevIcon);
    else prevBtn.textContent = "‹";
    prevBtn.onclick = (event) => navigateBy(event, -1);
    const nextBtn = el("button", { type: "button", class: "rui-gcal-nav", "data-dir": "next", "aria-label": "Next month" });
    const nextIcon = renderIcon("chevron-right");
    if (nextIcon) nextBtn.append(nextIcon);
    else nextBtn.textContent = "›";
    nextBtn.onclick = (event) => navigateBy(event, 1);
    toolbar.append(todayBtn, prevBtn, nextBtn);
  }
  toolbar.append(el("span", { class: "rui-gcal-title" }, [title]));
  const grid = el("div", { class: "rui-gcal-grid", role: "grid", "aria-label": title });
  const headRow = el("div", { class: "rui-gcal-row rui-gcal-head", role: "row", style: "display:contents" });
  for (let i = 0; i < 7; i += 1) {
    headRow.append(el("div", { class: "rui-gcal-weekday", role: "columnheader" }, [cfg2.weekdays[(cfg2.firstDay + i) % 7]]));
  }
  grid.append(headRow);
  grid.onkeydown = (event) => {
    const key = event.key;
    if (!["ArrowLeft", "ArrowRight", "ArrowUp", "ArrowDown", "Home", "End"].includes(key)) return;
    const live = liveRootOf(event);
    const days = Array.from(live.querySelectorAll(".rui-gcal-day"));
    const doc = live.getRootNode();
    const idx = days.indexOf(doc.activeElement);
    if (idx < 0) return;
    const seek = (from, step) => {
      for (let i = from; i >= 0 && i < days.length; i += step) {
        if (!days[i].disabled) return i;
      }
      return -1;
    };
    let next = -1;
    if (key === "ArrowLeft") next = seek(idx - 1, -1);
    else if (key === "ArrowRight") next = seek(idx + 1, 1);
    else if (key === "ArrowUp") next = seek(idx - 7, -7);
    else if (key === "ArrowDown") next = seek(idx + 7, 7);
    else if (key === "Home") next = seek(idx - idx % 7, 1);
    else if (key === "End") next = seek(idx - idx % 7 + 6, -1);
    const target = next >= 0 ? days[next] : void 0;
    if (!target) return;
    event.preventDefault();
    for (const day of days) day.setAttribute("tabindex", "-1");
    target.setAttribute("tabindex", "0");
    cfg2.activeSlot.set(target.getAttribute("data-iso"));
    target.focus();
  };
  const firstOfMonth = new Date(year, month, 1);
  const daysInMonth = new Date(year, month + 1, 0).getDate();
  const lead = (firstOfMonth.getDay() - cfg2.firstDay + 7) % 7;
  const totalCells = Math.ceil((lead + daysInMonth) / 7) * 7;
  const start = new Date(year, month, 1 - lead);
  const cells = [];
  let week = null;
  for (let i = 0; i < totalCells; i += 1) {
    if (i % 7 === 0) {
      week = el("div", { class: "rui-gcal-row", role: "row", style: "display:contents" });
      grid.append(week);
    }
    const date = new Date(start.getFullYear(), start.getMonth(), start.getDate() + i);
    const iso = isoOf(date.getFullYear(), date.getMonth(), date.getDate());
    const inMonth = date.getMonth() === month;
    const isSelected = Boolean(cfg2.selected) && iso === cfg2.selected;
    const blocked = isBlocked(cfg2, iso);
    const cell = el("button", {
      type: "button",
      class: "rui-gcal-day",
      role: "gridcell",
      "data-in-month": inMonth ? "true" : "false",
      "data-today": iso === cfg2.todayIso ? "true" : null,
      "data-selected": isSelected ? "true" : null,
      "data-iso": iso,
      // Human-readable for screen readers ("June 10, 2026", not the ISO).
      "aria-label": cfg2.dayLabel(date),
      // `aria-pressed` is not permitted on `role="gridcell"`; selection state
      // belongs in `aria-selected`.
      "aria-selected": isSelected ? "true" : "false",
      // Roving tabindex — the active cell is chosen after the loop, so every
      // cell starts out of the tab order instead of adding 42 tab stops.
      tabindex: "-1",
      disabled: blocked ? "" : null,
      "data-disabled": blocked ? "true" : null
    });
    cell.append(el("span", { class: "rui-gcal-daynum" }, [String(date.getDate())]));
    const chipEvents = cfg2.chips.get(iso) ?? [];
    const dotCount = cfg2.dots.get(iso) ?? 0;
    if (chipEvents.length > 0 || dotCount > 0) {
      const eventsEl = el("div", { class: "rui-gcal-events" });
      const visible = chipEvents.slice(0, 2);
      for (const evt of visible) {
        const chip = el("span", {
          class: "rui-gcal-chip",
          style: `--rui-gcal-chip:${evt.color}`,
          title: evt.time ? `${evt.time} — ${evt.label}` : evt.label,
          "data-clickable": cfg2.onEventClick != null ? "true" : null
        }, [evt.time ? `${evt.time} ${evt.label}` : evt.label]);
        if (cfg2.onEventClick != null) {
          chip.onclick = (event) => {
            event.stopPropagation();
            cfg2.helpers.invoke(cfg2.onEventClick, evt.raw, iso);
          };
        }
        eventsEl.append(chip);
      }
      if (dotCount > 0) {
        const dotsEl = el("div", { class: "rui-gcal-dots" });
        for (let d = 0; d < Math.min(3, dotCount); d += 1) dotsEl.append(el("span", { class: "rui-gcal-dot" }));
        eventsEl.append(dotsEl);
      }
      const hidden = chipEvents.length - visible.length;
      if (hidden > 0) eventsEl.append(el("span", { class: "rui-gcal-more" }, [`+${hidden} more`]));
      cell.append(eventsEl);
    }
    if (!blocked) {
      cell.onclick = (event) => {
        const live = liveRootOf(event);
        for (const sel of live.querySelectorAll('.rui-gcal-day[data-selected="true"]')) {
          sel.removeAttribute("data-selected");
          sel.setAttribute("aria-selected", "false");
        }
        const origin = event.currentTarget ?? event.target;
        const liveCell = origin?.closest?.(".rui-gcal-day");
        liveCell?.setAttribute("data-selected", "true");
        liveCell?.setAttribute("aria-selected", "true");
        cfg2.selected = iso;
        cfg2.activeSlot.set(iso);
        if (cfg2.selRef) cfg2.helpers.setState(cfg2.selRef, iso);
        else cfg2.selSlot.set({ prop: cfg2.propSelected, value: iso });
        cfg2.helpers.invoke(cfg2.onSelect, iso);
      };
    }
    (week ?? grid).append(cell);
    cells.push(cell);
  }
  const preferred = [cfg2.activeSlot.get() ?? "", cfg2.selected, cfg2.todayIso].filter(Boolean);
  let active;
  for (const iso of preferred) {
    active = cells.find((cell) => cell.getAttribute("data-iso") === iso && !cell.disabled);
    if (active) break;
  }
  active ?? (active = cells.find((cell) => !cell.disabled && cell.getAttribute("data-in-month") === "true") ?? cells.find((cell) => !cell.disabled));
  active?.setAttribute("tabindex", "0");
  const focused = captureFocus(root);
  root.replaceChildren(toolbar, grid);
  restoreFocus(root, focused);
}
const Calendar = {
  name: "Calendar",
  description: "A Google Calendar-style month grid with Today/prev/next navigation. `month` (0–11) and `year` set the view (default current month); `selected` is an ISO date (YYYY-MM-DD) to highlight — bind a $variable for two-way selection; `onSelect(iso)` fires on a day click. `events` mixes bare ISO strings (rendered as dots) and `{date, label, color?, time?}` objects (rendered as colored chips with a '+N more' overflow; `color` is a tone name or CSS color); `onEventClick(event, iso)` fires when a chip is clicked. `minDate`/`maxDate`/`disabledDates` block days from being selected at all (booking windows, blackout days). `onNavigate(year, month)` fires when the user pages months; `navigable=false` hides the toolbar buttons; `locale` (or `weekdayLabels`/`monthLabels`) localises the names. Use for schedules, bookings, and availability calendars.",
  props: [
    { name: "month", type: "number", optional: true, description: "0–11 (default current month)" },
    { name: "year", type: "number", optional: true, description: "Full year (default current)" },
    { name: "selected", type: "string", optional: true, description: "ISO date to highlight; bind a $variable" },
    { name: "onSelect", type: "callable", optional: true, description: "(isoDate) => …" },
    { name: "events", type: "any[]", optional: true, description: "ISO strings (dots) and/or {date, label, color?, time?} chips" },
    { name: "firstDay", type: "number", optional: true, description: "0=Sunday (default), 1=Monday" },
    { name: "navigable", type: "boolean", optional: true, description: "Show Today/prev/next controls (default true)" },
    { name: "onNavigate", type: "callable", optional: true, description: "(year, month) => … after paging" },
    { name: "minDate", type: "string", optional: true, description: "ISO date — earlier days are not selectable (e.g. no past bookings)" },
    { name: "maxDate", type: "string", optional: true, description: "ISO date — later days are not selectable (e.g. a 90-day horizon)" },
    { name: "disabledDates", type: "any[]", optional: true, aliases: ["blackoutDates"], description: "ISO dates that cannot be selected (holidays, fully-booked days)" },
    { name: "onEventClick", type: "callable", optional: true, description: "(event, isoDate) => … when an event chip is clicked (the day cell still handles keyboard selection)" },
    { name: "locale", type: "string", optional: true, description: "BCP 47 tag for the weekday/month/day names (e.g. `de-DE`)" },
    { name: "weekdayLabels", type: "any[]", optional: true, description: "Exactly 7 labels, Sunday first — overrides `locale`" },
    { name: "monthLabels", type: "any[]", optional: true, description: "Exactly 12 month names — overrides `locale`" }
  ],
  render: (node, props, helpers) => {
    const now = /* @__PURE__ */ new Date();
    const hasMonth = props.month !== void 0 && Number.isFinite(Number(props.month));
    const hasYear = props.year !== void 0 && Number.isFinite(Number(props.year));
    const propsMonth = hasMonth ? Math.max(0, Math.min(11, Math.floor(asNumber(props.month, now.getMonth())))) : now.getMonth();
    const propsYear = hasYear ? Math.floor(asNumber(props.year, now.getFullYear())) : now.getFullYear();
    const viewSlot = helpers.useInstanceState("rui-gcal-view", null);
    const stored = viewSlot.get();
    const followProps = !stored || stored.propsYear !== propsYear || stored.propsMonth !== propsMonth;
    const view = followProps ? { propsYear, propsMonth, year: propsYear, month: propsMonth } : stored;
    viewSlot.set(view);
    const selRef = node.argMeta?.[2]?.stateRef;
    const propSelected = asString(props.selected);
    const selSlot = helpers.useInstanceState("rui-gcal-selected", null);
    const storedSel = selSlot.get();
    const followSel = !storedSel || storedSel.prop !== propSelected;
    if (!selRef && followSel) selSlot.set({ prop: propSelected, value: propSelected });
    const selected = selRef || followSel ? propSelected : storedSel.value;
    const { chips, dots } = readGcalEvents(props.events);
    const { weekdays, months } = resolveLabels(asString(props.locale), props.weekdayLabels, props.monthLabels);
    const locale = asString(props.locale);
    const cfg2 = {
      todayIso: isoOf(now.getFullYear(), now.getMonth(), now.getDate()),
      selected,
      propSelected,
      chips,
      dots,
      firstDay: (Math.round(asNumber(props.firstDay, 0)) % 7 + 7) % 7,
      navigable: props.navigable === void 0 ? true : asBoolean(props.navigable),
      minIso: isoDay(props.minDate),
      maxIso: isoDay(props.maxDate),
      blocked: new Set(asArray(props.disabledDates).map(isoDay).filter(Boolean)),
      weekdays,
      months,
      dayLabel: (date) => {
        if (locale && typeof Intl !== "undefined") {
          try {
            return new Intl.DateTimeFormat(locale, { dateStyle: "long" }).format(date);
          } catch {
          }
        }
        return `${months[date.getMonth()]} ${date.getDate()}, ${date.getFullYear()}`;
      },
      onSelect: props.onSelect,
      onNavigate: props.onNavigate,
      onEventClick: props.onEventClick,
      selRef,
      helpers,
      viewSlot,
      selSlot,
      activeSlot: helpers.useInstanceState("rui-gcal-active", null)
    };
    const root = el("div", { class: "rui-gcal" });
    buildCalendarInto(root, view.year, view.month, cfg2);
    return root;
  }
};
const PADS = /* @__PURE__ */ new WeakMap();
const clamp = (n, lo, hi) => Math.max(lo, Math.min(hi, n));
const devicePixels = () => typeof window !== "undefined" ? window.devicePixelRatio || 1 : 1;
function sizeCanvas(canvas, width, height, applyCssSize) {
  const dpr = devicePixels();
  canvas.width = Math.round(width * dpr);
  canvas.height = Math.round(height * dpr);
  if (applyCssSize) {
    canvas.style.width = `${width}px`;
    canvas.style.height = `${height}px`;
    canvas.style.maxWidth = "100%";
  }
  const ctx = canvas.getContext("2d");
  if (ctx) {
    ctx.setTransform(1, 0, 0, 1, 0, 0);
    ctx.scale(dpr, dpr);
    ctx.lineCap = "round";
    ctx.lineJoin = "round";
  }
  return ctx;
}
const isTransparent = (color2) => !color2 || color2 === "transparent" || /^rgba\(\s*0\s*,\s*0\s*,\s*0\s*,\s*0\s*\)$/.test(color2);
function padConfig(canvas) {
  let stroke = canvas.dataset.stroke ?? "";
  if (!stroke) {
    try {
      stroke = getComputedStyle(canvas).color || "#1a1a1a";
    } catch {
      stroke = "#1a1a1a";
    }
  }
  let background = canvas.dataset.bg ?? "";
  if (!background) {
    try {
      background = getComputedStyle(canvas).backgroundColor || "";
    } catch {
      background = "";
    }
  }
  return {
    stroke,
    lineWidth: clamp(Number(canvas.dataset.lineWidth) || 2, 1, 40),
    background
  };
}
function paintBackground(canvas, pad) {
  if (!pad.ctx) return;
  const { background } = padConfig(canvas);
  if (isTransparent(background)) return;
  pad.ctx.fillStyle = background;
  pad.ctx.fillRect(0, 0, pad.width, pad.height);
}
function applyInk(canvas, pad) {
  const { stroke, lineWidth } = padConfig(canvas);
  if (pad.ctx) {
    pad.ctx.strokeStyle = stroke;
    pad.ctx.fillStyle = stroke;
    pad.ctx.lineWidth = lineWidth;
  }
  return lineWidth;
}
function drawPath(ctx, path, lineWidth) {
  if (path.length === 0) return;
  if (path.length === 1) {
    const p = path[0];
    ctx.beginPath();
    ctx.arc(p.x, p.y, Math.max(0.5, lineWidth / 2), 0, Math.PI * 2);
    ctx.fill();
    return;
  }
  ctx.beginPath();
  ctx.moveTo(path[0].x, path[0].y);
  for (let i = 1; i < path.length; i += 1) ctx.lineTo(path[i].x, path[i].y);
  ctx.stroke();
}
function drawIncremental(canvas, pad, prev, next) {
  if (!pad.ctx) return;
  const lineWidth = applyInk(canvas, pad);
  if (!prev) {
    drawPath(pad.ctx, [next], lineWidth);
    return;
  }
  pad.ctx.beginPath();
  pad.ctx.moveTo(prev.x, prev.y);
  pad.ctx.lineTo(next.x, next.y);
  pad.ctx.stroke();
}
function redrawPad(canvas) {
  const pad = PADS.get(canvas);
  if (!pad?.ctx) return;
  pad.ctx.clearRect(0, 0, pad.width, pad.height);
  paintBackground(canvas, pad);
  if (pad.base) {
    try {
      pad.ctx.drawImage(pad.base, 0, 0, pad.width, pad.height);
    } catch {
    }
  }
  const lineWidth = applyInk(canvas, pad);
  for (const path of pad.strokes) drawPath(pad.ctx, path, lineWidth);
}
function clearPad(canvas) {
  const pad = PADS.get(canvas);
  if (!pad) return;
  pad.strokes = [];
  pad.current = [];
  pad.base = null;
  pad.value = "";
  redrawPad(canvas);
}
function padDataUrl(canvas) {
  try {
    return canvas.toDataURL("image/png");
  } catch {
    return "";
  }
}
const padResult = (pad) => ({
  strokes: pad.strokes.length,
  inked: pad.strokes.some((path) => path.length >= 2)
});
function resizePad(canvas, pad, width, height, applyCssSize) {
  const sx = pad.width > 0 ? width / pad.width : 1;
  const sy = pad.height > 0 ? height / pad.height : 1;
  if (sx !== 1 || sy !== 1) {
    for (const path of pad.strokes) {
      for (const point of path) {
        point.x *= sx;
        point.y *= sy;
      }
    }
  }
  pad.ctx = sizeCanvas(canvas, width, height, applyCssSize);
  pad.width = width;
  pad.height = height;
  pad.painted = true;
  redrawPad(canvas);
}
function refitPad(canvas) {
  const pad = PADS.get(canvas);
  if (!pad || pad.drawing) return;
  const rect = canvas.getBoundingClientRect();
  const width = Math.round(rect.width);
  const height = Math.round(rect.height);
  if (width < 1 || height < 1) return;
  if (Math.abs(width - pad.width) <= 1 && Math.abs(height - pad.height) <= 1) return;
  resizePad(canvas, pad, width, height, false);
}
function syncPad(canvas, cfg2) {
  const pad = PADS.get(canvas);
  if (!pad) return;
  const dpr = devicePixels();
  if (pad.propWidth !== cfg2.width || pad.propHeight !== cfg2.height) {
    pad.propWidth = cfg2.width;
    pad.propHeight = cfg2.height;
    resizePad(canvas, pad, cfg2.width, cfg2.height, true);
  } else if (canvas.width !== Math.round(pad.width * dpr) || canvas.height !== Math.round(pad.height * dpr)) {
    resizePad(canvas, pad, pad.width, pad.height, false);
  } else if (!pad.painted) {
    pad.painted = true;
    redrawPad(canvas);
  }
  if (cfg2.value !== pad.value) {
    pad.value = cfg2.value;
    importValue(canvas, cfg2.value);
  }
}
function importValue(canvas, url) {
  const pad = PADS.get(canvas);
  if (!pad) return;
  if (!url) {
    pad.base = null;
    pad.strokes = [];
    pad.current = [];
    redrawPad(canvas);
    return;
  }
  if (typeof Image === "undefined") return;
  const img = new Image();
  img.onload = () => {
    if (PADS.get(canvas) !== pad || pad.value !== url) return;
    pad.base = img;
    pad.strokes = [];
    pad.current = [];
    redrawPad(canvas);
  };
  img.onerror = () => {
  };
  img.src = url;
}
function makeDrawingSurface(spec) {
  const { width, height, stroke, lineWidth, background, rootClass, disabled, helpers } = spec;
  const root = el("div", {
    class: rootClass,
    style: `width:${width}px;max-width:100%`,
    "data-disabled": disabled ? "true" : null
  });
  const canvas = el("canvas", {
    class: "rui-canvas-surface",
    style: "touch-action:none",
    // Focusable + named so the surface is discoverable by a screen reader and
    // has something for a field error to be associated with.
    tabindex: disabled ? null : "0",
    "aria-label": spec.ariaLabel || null,
    "aria-disabled": disabled ? "true" : null,
    "data-stroke": stroke || null,
    "data-line-width": String(lineWidth),
    "data-bg": background || null
  });
  root.append(canvas);
  const ctx = sizeCanvas(canvas, width, height, true);
  const pad = {
    strokes: [],
    current: [],
    drawing: false,
    ctx,
    width,
    height,
    propWidth: width,
    propHeight: height,
    value: "",
    base: null,
    painted: false
  };
  PADS.set(canvas, pad);
  paintBackground(canvas, pad);
  const liveCanvas = (e) => e.currentTarget ?? canvas;
  const pointFrom = (cv, e) => {
    const live = PADS.get(cv);
    const rect = cv.getBoundingClientRect();
    const logicalW = live?.width ?? 0;
    const logicalH = live?.height ?? 0;
    const sx = rect.width > 0 && logicalW > 0 ? logicalW / rect.width : 1;
    const sy = rect.height > 0 && logicalH > 0 ? logicalH / rect.height : 1;
    return { x: (e.clientX - rect.left) * sx, y: (e.clientY - rect.top) * sy };
  };
  const finish = (cv, live) => {
    live.drawing = false;
    const url = padDataUrl(cv);
    live.value = url;
    const hidden = cv.closest(`.${rootClass}`)?.querySelector("input.rui-canvas-value");
    if (hidden) hidden.value = url;
    spec.onEnd?.(url, padResult(live));
  };
  if (!disabled) {
    canvas.onpointerdown = (e) => {
      const cv = liveCanvas(e);
      const live = PADS.get(cv);
      if (!live) return;
      live.drawing = true;
      try {
        cv.setPointerCapture(e.pointerId);
      } catch {
      }
      const first = pointFrom(cv, e);
      live.current = [first];
      live.strokes.push(live.current);
      drawIncremental(cv, live, null, first);
      spec.onCount?.(live.strokes.length);
    };
    canvas.onpointermove = (e) => {
      const cv = liveCanvas(e);
      const live = PADS.get(cv);
      if (!live?.drawing) return;
      if (e.buttons === 0 && e.pointerType === "mouse") {
        finish(cv, live);
        return;
      }
      const prev = live.current[live.current.length - 1] ?? null;
      const next = pointFrom(cv, e);
      live.current.push(next);
      drawIncremental(cv, live, prev, next);
    };
    const end = (e) => {
      const cv = liveCanvas(e);
      const live = PADS.get(cv);
      if (!live?.drawing) return;
      try {
        cv.releasePointerCapture(e.pointerId);
      } catch {
      }
      finish(cv, live);
    };
    canvas.onpointerup = end;
    canvas.onpointercancel = end;
  }
  const liveSlot = helpers.useInstanceState("rui-canvas-live", null);
  const obsSlot = helpers.useInstanceState("rui-canvas-observer", null);
  deferToPaint(() => {
    const live = canvas.isConnected ? canvas : liveSlot.get();
    if (!live?.isConnected) return;
    liveSlot.set(live);
    syncPad(live, spec);
    const existing = obsSlot.get();
    if (existing && existing.node !== live) {
      existing.ro.disconnect();
      obsSlot.set(null);
    }
    if (!obsSlot.get() && typeof ResizeObserver !== "undefined") {
      const ro = new ResizeObserver(() => refitPad(live));
      try {
        ro.observe(live);
      } catch {
      }
      obsSlot.set({ ro, node: live });
      helpers.registerDisposer(() => {
        ro.disconnect();
        obsSlot.set(null);
      }, "rui-canvas-observer");
    }
  });
  return { root, canvas };
}
function liveSurfaceCanvas(event, rootClass, fallback) {
  const origin = event.currentTarget ?? event.target;
  const liveRoot = origin?.closest?.(`.${rootClass}`);
  return liveRoot?.querySelector("canvas.rui-canvas-surface") ?? fallback;
}
function valueField(name, value) {
  if (!name) return null;
  return el("input", { type: "hidden", class: "rui-canvas-value", name, value: valueAttr(value) });
}
const DrawingCanvas = {
  name: "DrawingCanvas",
  description: "A freehand drawing surface (pointer / touch / stylus). `onChange(count)` fires when a stroke starts or the pad is cleared; `onEnd(dataUrl, count)` fires when a stroke finishes with a PNG data URL. Pass that URL back as `value` to restore the drawing after a re-render or route change. `color`/`lineWidth`/`background` style the ink (`background` defaults to the surface colour so the export is never ink-on-transparency). Includes a Clear button unless `clearable=false`; `disabled` locks the surface.",
  props: [
    { name: "width", type: "number", optional: true, description: "px (default 360)" },
    { name: "height", type: "number", optional: true, description: "px (default 220)" },
    { name: "color", type: "string", optional: true, description: "Ink color" },
    { name: "lineWidth", type: "number", optional: true },
    { name: "background", type: "string", optional: true, description: "Exported background color (`transparent` keeps the alpha)" },
    { name: "clearable", type: "boolean", optional: true },
    { name: "value", type: "string", optional: true, description: "PNG data URL to restore (the value `onEnd` hands you)" },
    { name: "onChange", type: "callable", optional: true, description: "(strokeCount) => …" },
    { name: "onEnd", type: "callable", optional: true, description: "(pngDataUrl, strokeCount) => …" },
    ...FIELD_SHELL_PROPS,
    { name: "ariaLabel", type: "string", optional: true, aliases: ["arialabel"], description: "Accessible name for the surface (defaults to `label`)" }
  ],
  render: (_node, props, helpers) => {
    const width = clamp(asNumber(props.width, 360), 80, 2e3);
    const height = clamp(asNumber(props.height, 220), 80, 2e3);
    const disabled = asBoolean(props.disabled);
    const label = asString(props.label);
    const surface = makeDrawingSurface({
      width,
      height,
      stroke: sanitiseCssColor(props.color) || "",
      lineWidth: clamp(asNumber(props.lineWidth, 2), 1, 40),
      background: sanitiseCssColor(props.background),
      rootClass: "rui-drawing-canvas",
      ariaLabel: asString(props.ariaLabel) || label || "Drawing canvas",
      disabled,
      value: asString(props.value),
      onCount: (count) => helpers.invoke(props.onChange, count),
      onEnd: (url, info) => helpers.invoke(props.onEnd, url, info.strokes),
      helpers
    });
    attachFocusHandlers(surface.canvas, props, helpers, (node) => padDataUrl(node));
    const hidden = valueField(asString(props.name), props.value);
    if (hidden) surface.root.append(hidden);
    if (props.clearable === void 0 ? true : asBoolean(props.clearable)) {
      const bar = el("div", { class: "rui-canvas-toolbar" });
      const clearBtn = el("button", {
        type: "button",
        class: "rui-canvas-clear",
        disabled: disabled ? "" : null
      }, ["Clear"]);
      clearBtn.onclick = (event) => {
        clearPad(liveSurfaceCanvas(event, "rui-drawing-canvas", surface.canvas));
        helpers.invoke(props.onChange, 0);
      };
      bar.append(clearBtn);
      surface.root.append(bar);
    }
    return withFieldShell(surface.root, props);
  }
};
const SignaturePad = {
  name: "SignaturePad",
  description: "A signature capture pad — a DrawingCanvas tuned for signing, with a baseline and a Clear button. `onChange(pngDataUrl, strokeCount)` fires when the signature changes (empty string when cleared, and also when the pad only received taps — so a stray tap cannot pass a truthiness check). Pass the URL back as `value` to restore a signature after a re-render, and `disabled` to lock the pad once it is submitted. `label`/`error`/`required` render the usual field shell. Use in contracts, delivery confirmation, and consent flows.",
  props: [
    { name: "width", type: "number", optional: true, description: "px (default 400)" },
    { name: "height", type: "number", optional: true, description: "px (default 160)" },
    { name: "color", type: "string", optional: true },
    { name: "lineWidth", type: "number", optional: true, description: "Pen width (default 2.5 — widen it for finger signing)" },
    { name: "background", type: "string", optional: true, description: "Exported background (default `#ffffff`; `transparent` composites onto a PDF/letterhead)" },
    { name: "clearable", type: "boolean", optional: true, description: "Show the Clear button (default true)" },
    { name: "value", type: "string", optional: true, description: "PNG data URL to restore a captured signature" },
    { name: "onChange", type: "callable", optional: true, description: '(pngDataUrl|"", strokeCount) => …' },
    ...FIELD_SHELL_PROPS,
    { name: "ariaLabel", type: "string", optional: true, aliases: ["arialabel"], description: "Accessible name for the pad (defaults to `label`)" }
  ],
  render: (_node, props, helpers) => {
    const width = clamp(asNumber(props.width, 400), 120, 1200);
    const height = clamp(asNumber(props.height, 160), 80, 600);
    const disabled = asBoolean(props.disabled);
    const label = asString(props.label);
    const surface = makeDrawingSurface({
      width,
      height,
      stroke: sanitiseCssColor(props.color) || "#1a1a1a",
      lineWidth: clamp(asNumber(props.lineWidth, 2.5), 1, 40),
      background: sanitiseCssColor(props.background) || "#ffffff",
      rootClass: "rui-signature-pad",
      ariaLabel: asString(props.ariaLabel) || label || "Signature pad",
      disabled,
      value: asString(props.value),
      // Taps alone are not a signature: reporting "" keeps a blank-but-truthy
      // data URL from satisfying `$signature != ""` after an accidental touch.
      onEnd: (url, info) => helpers.invoke(props.onChange, info.inked ? url : "", info.strokes),
      helpers
    });
    attachFocusHandlers(surface.canvas, props, helpers, (node) => padDataUrl(node));
    const surfaceBox = el("div", { class: "rui-signature-surface", style: "position:relative;display:block" });
    surfaceBox.append(surface.canvas, el("div", { class: "rui-signature-baseline", style: "bottom:14px" }));
    surface.root.append(surfaceBox);
    const hidden = valueField(asString(props.name), props.value);
    if (hidden) surface.root.append(hidden);
    if (props.clearable === void 0 ? true : asBoolean(props.clearable)) {
      const bar = el("div", { class: "rui-canvas-toolbar" });
      const clearBtn = el("button", {
        type: "button",
        class: "rui-canvas-clear",
        disabled: disabled ? "" : null
      }, ["Clear"]);
      clearBtn.onclick = (event) => {
        clearPad(liveSurfaceCanvas(event, "rui-signature-pad", surface.canvas));
        helpers.invoke(props.onChange, "", 0);
      };
      bar.append(clearBtn);
      surface.root.append(bar);
    }
    return withFieldShell(surface.root, props);
  }
};
let idSeq = 0;
function instanceId(helpers, key, prefix) {
  const slot = helpers.useInstanceState(key, "");
  if (!slot.get()) slot.set(`${prefix}-${idSeq += 1}`);
  return slot.get();
}
function onceMounted(root, helpers, key, install) {
  const wired = helpers.useInstanceState(`${key}-wired`, false);
  if (wired.get()) return;
  wired.set(true);
  const cancel = deferToPaint(() => {
    if (!root.isConnected) return;
    install(root);
  });
  helpers.registerDisposer(cancel, `${key}-timer`);
}
let scrollLocks = 0;
let savedRootOverflow = null;
function lockPageScroll(lock) {
  if (typeof document === "undefined" || !document.documentElement) return;
  const root = document.documentElement;
  if (lock) {
    if (scrollLocks === 0) {
      savedRootOverflow = root.style.overflow;
      root.style.overflow = "hidden";
    }
    scrollLocks += 1;
    return;
  }
  if (scrollLocks === 0) return;
  scrollLocks -= 1;
  if (scrollLocks === 0) {
    root.style.overflow = savedRootOverflow ?? "";
    savedRootOverflow = null;
  }
}
function wireOverlayLayer(root, helpers, reset, key) {
  if (typeof MutationObserver === "undefined") return;
  onceMounted(root, helpers, key, (live) => {
    let locked = false;
    const sync = () => {
      const open = live.getAttribute("data-open") === "true";
      if (open) promoteOverlay$1(live, reset);
      else releaseOverlay(live);
      if (open !== locked) {
        locked = open;
        lockPageScroll(open);
      }
    };
    sync();
    const observer = new MutationObserver(sync);
    observer.observe(live, { attributes: true, attributeFilter: ["data-open"] });
    helpers.registerDisposer(() => {
      observer.disconnect();
      releaseOverlay(live);
      if (locked) {
        locked = false;
        lockPageScroll(false);
      }
    }, key);
  });
}
const VIEWBOX_RE = /^[\d.\s-]+$/;
const SVG_PAINT_RE = /^[a-zA-Z#()0-9,.\s-]+$/;
const PRESERVE_AR_RE = /^[a-zA-Z\s]+$/;
const Svg = {
  name: "Svg",
  description: "Render inline SVG markup safely (paths, shapes, gradients) — for brand illustrations, custom icons, and data-viz overlays without the HTMLTag escape hatch. Pass the inner markup (everything inside <svg>) plus a `viewBox`, or paste a whole `<svg …>` element and its viewBox/fill/stroke are picked up. `label` names the graphic for assistive tech (unlabelled graphics are hidden as decorative). Script/event-handler payloads are stripped.",
  props: [
    { name: "content", type: "string", positional: true, required: true, aliases: ["paths", "markup"] },
    { name: "viewBox", type: "string", optional: true, description: 'e.g. "0 0 24 24" (default)' },
    { name: "width", type: "string", optional: true },
    { name: "height", type: "string", optional: true },
    { name: "fill", type: "string", optional: true, description: "currentColor (default), none, or a token color" },
    { name: "stroke", type: "string", optional: true, description: "Stroke colour — for outline icon sets (Lucide, Feather)" },
    { name: "strokeWidth", type: "string | number", optional: true, aliases: ["stroke-width"] },
    { name: "preserveAspectRatio", type: "string", optional: true, description: 'e.g. "none" to stretch to the box' },
    { name: "label", type: "string", optional: true, description: "Accessible name; omit for a decorative graphic" }
  ],
  render: (_node, props) => {
    const ns = "http://www.w3.org/2000/svg";
    const svg = document.createElementNS(ns, "svg");
    svg.setAttribute("class", "rui-svg");
    const safe = sanitiseSvgMarkup(asString(props.content));
    const rootAttrs = safe?.rootAttrs ?? {};
    const vb = asString(props.viewBox) || rootAttrs.viewbox || "0 0 24 24";
    svg.setAttribute("viewBox", VIEWBOX_RE.test(vb) ? vb : "0 0 24 24");
    const fill = asString(props.fill) || rootAttrs.fill || "currentColor";
    svg.setAttribute("fill", SVG_PAINT_RE.test(fill) ? fill : "currentColor");
    const stroke = asString(props.stroke) || rootAttrs.stroke || "";
    if (stroke && SVG_PAINT_RE.test(stroke)) svg.setAttribute("stroke", stroke);
    const strokeWidth = asString(props.strokeWidth) || rootAttrs["stroke-width"] || "";
    if (strokeWidth && SVG_PAINT_RE.test(strokeWidth)) svg.setAttribute("stroke-width", strokeWidth);
    const par = asString(props.preserveAspectRatio) || rootAttrs.preserveaspectratio || "";
    if (par && PRESERVE_AR_RE.test(par)) svg.setAttribute("preserveAspectRatio", par);
    const width = sanitiseCssLength(props.width, "");
    if (width) svg.setAttribute("width", width);
    const height = sanitiseCssLength(props.height, "");
    if (height) svg.setAttribute("height", height);
    const label = asString(props.label);
    if (label) {
      svg.setAttribute("role", "img");
      svg.setAttribute("aria-label", label);
    } else {
      svg.setAttribute("aria-hidden", "true");
    }
    if (safe) for (const child of safe.children) svg.appendChild(child);
    return svg;
  }
};
const Sheet = {
  name: "Sheet",
  description: "A panel that slides in from an edge over a dimmed backdrop. `side` is left|right|top|bottom. Bind `open` to a $variable; `onClose` fires on backdrop click / Escape (set `dismissible: false` to require an explicit action). `footer` pins an action row below the scrolling body. Use BottomSheet for the mobile bottom variant.",
  props: [
    { name: "children", type: "Node[]", positional: true, required: true, aliases: ["child", "content"] },
    { name: "open", type: "boolean", optional: true },
    { name: "side", type: "string", optional: true, enum: ["left", "right", "top", "bottom"] },
    { name: "title", type: "string", optional: true },
    { name: "onClose", type: "callable", optional: true, aliases: ["onclose"] },
    { name: "label", type: "string", optional: true, description: "Accessible name when there is no visible `title`" },
    { name: "width", type: "string", optional: true, description: "Panel width for left/right sides (default min(420px, 90vw))" },
    { name: "footer", type: "Node[]", optional: true, description: "Pinned action row (Cancel / Apply)" },
    { name: "dismissible", type: "boolean", optional: true, description: "Backdrop click + Escape close it (default true)" }
  ],
  render: (node, props, helpers) => {
    const open = asBoolean(props.open);
    const side = asString(props.side, "right");
    const root = el("div", { class: "rui-sheet-root", "data-open": open ? "true" : "false", "data-side": side });
    const ref = node.argMeta?.[1]?.stateRef;
    const dismissible = props.dismissible === void 0 ? true : asBoolean(props.dismissible);
    const close = () => {
      if (ref) helpers.setState(ref, false);
      helpers.invoke(props.onClose);
    };
    const backdrop = el("div", { class: "rui-sheet-backdrop" });
    if (dismissible) backdrop.onclick = close;
    const panelWidth = side === "left" || side === "right" ? sanitiseCssLength(props.width, "") : "";
    const panel = el("div", {
      class: "rui-sheet-panel",
      role: "dialog",
      "aria-modal": "true",
      tabindex: "-1",
      style: panelWidth ? `width:min(${panelWidth}, 100vw)` : null
    });
    const title = asString(props.title);
    const label = asString(props.label);
    const titleId = instanceId(helpers, "rui-sheet-label-id", "rui-sheet-label");
    if (title) panel.setAttribute("aria-labelledby", titleId);
    else if (label) panel.setAttribute("aria-label", label);
    const head = el("div", { class: "rui-sheet-head" });
    if (title) head.append(el("h3", { class: "rui-sheet-title", id: titleId }, [title]));
    const x = el("button", { class: "rui-sheet-close", type: "button", "aria-label": "Close" });
    const ic = renderIcon("xmark");
    if (ic) x.append(ic);
    x.onclick = close;
    head.append(x);
    panel.append(head);
    const body = el("div", { class: "rui-sheet-body" });
    for (const c of asArray(props.children)) body.append(helpers.renderNode(c));
    panel.append(body);
    const footer = asArray(props.footer);
    if (footer.length > 0) {
      const foot = el("div", { class: "rui-sheet-footer" });
      for (const c of footer) foot.append(helpers.renderNode(c));
      panel.append(foot);
    }
    root.append(backdrop, panel);
    root.onkeydown = dialogKeydownHandler(".rui-sheet-panel", () => {
      if (dismissible) close();
    });
    wireDialogFocus(root, ".rui-sheet-panel", helpers);
    wireOverlayLayer(root, helpers, OVERLAY_FILL, "rui-sheet-layer");
    return root;
  }
};
const BottomSheet = {
  name: "BottomSheet",
  description: "A mobile bottom sheet — Sheet pinned to the bottom edge. The grip is draggable: swiping it down dismisses the sheet. Bind `open`; `onClose` on dismiss. `height` fixes the panel size so a filtering list scrolls instead of resizing the sheet; `footer` pins a primary action.",
  props: [
    { name: "children", type: "Node[]", positional: true, required: true, aliases: ["child", "content"] },
    { name: "open", type: "boolean", optional: true },
    { name: "title", type: "string", optional: true },
    { name: "onClose", type: "callable", optional: true, aliases: ["onclose"] },
    { name: "label", type: "string", optional: true, description: "Accessible name when there is no visible `title`" },
    { name: "height", type: "string", optional: true, description: 'Fixed panel height, e.g. "50vh" (default: hugs content, max 85vh)' },
    { name: "showClose", type: "boolean", optional: true, description: "Render the × button (default true)" },
    { name: "footer", type: "Node[]", optional: true, description: "Pinned action row" }
  ],
  render: (node, props, helpers) => {
    const open = asBoolean(props.open);
    const root = el("div", { class: "rui-sheet-root rui-bottomsheet", "data-open": open ? "true" : "false", "data-side": "bottom" });
    const ref = node.argMeta?.[1]?.stateRef;
    const close = () => {
      if (ref) helpers.setState(ref, false);
      helpers.invoke(props.onClose);
    };
    const backdrop = el("div", { class: "rui-sheet-backdrop" });
    backdrop.onclick = close;
    const height = sanitiseCssLength(props.height, "");
    const panel = el("div", {
      class: "rui-sheet-panel",
      role: "dialog",
      "aria-modal": "true",
      tabindex: "-1",
      style: height ? `height:${height};max-height:100vh` : null
    });
    const title = asString(props.title);
    const label = asString(props.label);
    const titleId = instanceId(helpers, "rui-bottomsheet-label-id", "rui-bottomsheet-label");
    if (title) panel.setAttribute("aria-labelledby", titleId);
    else if (label) panel.setAttribute("aria-label", label);
    const drag = helpers.useInstanceState("rui-bottomsheet-drag", { y: 0, t: 0, active: false });
    const grip = el("div", { class: "rui-bottomsheet-grip", "aria-hidden": "true" });
    const panelOf = (event) => {
      const origin = event.currentTarget ?? event.target;
      return origin?.closest(".rui-sheet-panel") ?? null;
    };
    grip.onpointerdown = (event) => {
      const ev = event;
      const live = panelOf(ev);
      if (!live) return;
      drag.set({ y: ev.clientY, t: Date.now(), active: true });
      try {
        (ev.currentTarget ?? ev.target).setPointerCapture?.(ev.pointerId);
      } catch {
      }
      live.style.transition = "none";
    };
    grip.onpointermove = (event) => {
      const ev = event;
      const state = drag.get();
      if (!state.active) return;
      const live = panelOf(ev);
      if (!live) return;
      live.style.transform = `translateY(${Math.max(0, ev.clientY - state.y)}px)`;
    };
    const endDrag = (event, commit) => {
      const ev = event;
      const state = drag.get();
      if (!state.active) return;
      drag.set({ y: 0, t: 0, active: false });
      const live = panelOf(ev);
      if (live) {
        live.style.transition = "";
        live.style.transform = "";
      }
      if (!commit) return;
      const dy = ev.clientY - state.y;
      const elapsed = Math.max(1, Date.now() - state.t);
      if (dy > 90 || dy / elapsed > 0.6) close();
    };
    grip.onpointerup = (event) => endDrag(event, true);
    grip.onpointercancel = (event) => endDrag(event, false);
    panel.append(grip);
    const showClose = props.showClose === void 0 ? true : asBoolean(props.showClose);
    if (title || showClose) {
      const head = el("div", { class: "rui-sheet-head" });
      if (title) head.append(el("h3", { class: "rui-sheet-title", id: titleId }, [title]));
      if (showClose) {
        const x = el("button", { class: "rui-sheet-close", type: "button", "aria-label": "Close" });
        const ic = renderIcon("xmark");
        if (ic) x.append(ic);
        x.onclick = close;
        head.append(x);
      }
      panel.append(head);
    }
    const body = el("div", { class: "rui-sheet-body" });
    for (const c of asArray(props.children)) body.append(helpers.renderNode(c));
    panel.append(body);
    const footer = asArray(props.footer);
    if (footer.length > 0) {
      const foot = el("div", { class: "rui-sheet-footer" });
      for (const c of footer) foot.append(helpers.renderNode(c));
      panel.append(foot);
    }
    root.append(backdrop, panel);
    root.onkeydown = dialogKeydownHandler(".rui-sheet-panel", () => close());
    wireDialogFocus(root, ".rui-sheet-panel", helpers);
    wireOverlayLayer(root, helpers, OVERLAY_FILL, "rui-bottomsheet-layer");
    return root;
  }
};
const ConfirmDialog = {
  name: "ConfirmDialog",
  description: "A confirm/cancel modal dialog. Bind `open` to a $variable; `onConfirm` fires on accept, `onCancel`/backdrop on dismiss. `tone` colors the confirm button (danger for destructive actions) and picks a matching icon. Binding `loading` keeps the dialog open on confirm and disables both buttons so an async action can report progress — write `false` to `open` yourself once it resolves.",
  props: [
    { name: "title", type: "string", positional: true, required: true },
    { name: "open", type: "boolean", optional: true },
    { name: "message", type: "string", optional: true, aliases: ["body"] },
    { name: "confirmLabel", type: "string", optional: true },
    { name: "cancelLabel", type: "string", optional: true },
    { name: "tone", aliases: ["variant"], type: "string", optional: true, enum: ["primary", "danger", "warning"] },
    { name: "onConfirm", type: "callable", optional: true },
    { name: "onCancel", type: "callable", optional: true },
    { name: "loading", type: "boolean", optional: true, description: "Async confirm in flight — keeps the dialog open, disables both buttons" },
    { name: "confirmDisabled", type: "boolean", optional: true, description: "Guard the primary action until a condition is met" },
    { name: "icon", type: "string", optional: true, description: "Glyph beside the title (defaults from `tone`)" }
  ],
  render: (node, props, helpers) => {
    const open = asBoolean(props.open);
    const tone = asString(props.tone, "primary");
    const root = el("div", { class: "rui-confirm-root", "data-open": open ? "true" : "false" });
    const ref = node.argMeta?.[1]?.stateRef;
    const dismiss = () => {
      if (ref) helpers.setState(ref, false);
    };
    const backdrop = el("div", { class: "rui-confirm-backdrop" });
    const titleId = instanceId(helpers, "rui-confirm-label-id", "rui-confirm-label");
    const msgId = `${titleId}-desc`;
    const msg = asString(props.message);
    const managed = props.loading !== void 0;
    const loading = asBoolean(props.loading);
    const confirmBlocked = loading || asBoolean(props.confirmDisabled);
    const cancelAndDismiss = () => {
      if (loading) return;
      helpers.invoke(props.onCancel);
      dismiss();
    };
    backdrop.onclick = cancelAndDismiss;
    const card = el("div", {
      class: "rui-confirm-card",
      role: "alertdialog",
      "aria-modal": "true",
      "aria-labelledby": titleId,
      // The consequence text is the entire reason the dialog exists, so it has
      // to be part of the announcement, not a loose paragraph.
      "aria-describedby": msg ? msgId : null,
      "data-tone": tone,
      tabindex: "-1"
    });
    const heading = el("h3", { class: "rui-confirm-title", id: titleId });
    const iconName = props.icon !== void 0 ? asString(props.icon) : pickIconForTone(tone) ?? "";
    const toneIcon = tone === "primary" && props.icon === void 0 ? null : renderIcon(iconName, { className: "rui-confirm-icon" });
    if (toneIcon) heading.append(toneIcon);
    heading.append(document.createTextNode(asString(props.title)));
    card.append(heading);
    if (msg) card.append(el("p", { class: "rui-confirm-message", id: msgId }, [msg]));
    const actions = el("div", { class: "rui-confirm-actions" });
    const cancel = el("button", { class: "rui-confirm-cancel", type: "button", disabled: loading }, [asString(props.cancelLabel, "Cancel")]);
    cancel.onclick = cancelAndDismiss;
    const confirm = el("button", {
      class: "rui-confirm-ok",
      type: "button",
      "data-tone": tone,
      disabled: confirmBlocked,
      "aria-busy": loading ? "true" : null
    });
    if (loading) {
      confirm.append(el("span", { class: "rui-spinner", "data-size": "sm", "aria-hidden": "true" }, [
        el("span", { class: "rui-spinner-ring" })
      ]));
    }
    confirm.append(el("span", {}, [asString(props.confirmLabel, "Confirm")]));
    confirm.onclick = () => {
      if (confirmBlocked) return;
      helpers.invoke(props.onConfirm);
      if (!managed) dismiss();
    };
    actions.append(cancel, confirm);
    card.append(actions);
    root.append(backdrop, card);
    root.onkeydown = dialogKeydownHandler(".rui-confirm-card", () => cancelAndDismiss());
    wireDialogFocus(root, ".rui-confirm-card", helpers);
    wireOverlayLayer(root, helpers, OVERLAY_FILL, "rui-confirm-layer");
    return root;
  }
};
function wireAvatarFallback(img, name, fallbackClass) {
  img.onerror = (event) => {
    const ev = event;
    const live = ev.currentTarget ?? ev.target;
    live?.replaceWith(el("span", { class: fallbackClass }, [initialsFor(name)]));
  };
}
const PresenceAvatars = {
  name: "PresenceAvatars",
  description: "An overlapping stack of avatars with online status dots — 'who's here' for collab/social. Pass `people` as {name, src?, online?}. `onClick` makes each avatar activatable (receives the person object).",
  props: [
    { name: "people", type: "object[]", positional: true, required: true, aliases: ["users", "children"] },
    { name: "max", type: "number", optional: true, description: "Max avatars before a +N chip (default 5)" },
    { name: "size", type: "string", optional: true, enum: ["sm", "md", "lg"] },
    { name: "onClick", type: "callable", optional: true, aliases: ["onPersonClick", "onclick"], description: "Fires with the clicked person object" }
  ],
  render: (_node, props, helpers) => {
    const people = asArray(props.people);
    const max = Math.max(1, Math.round(asNumber(props.max, 5)));
    const size2 = asString(props.size, "md");
    const shown = people.slice(0, max);
    const online = people.filter((raw) => asBoolean(raw?.online)).length;
    const root = el("div", {
      class: "rui-presence",
      "data-size": size2,
      role: "group",
      "aria-label": `${online} of ${people.length} people online`
    });
    const clickable = props.onClick != null;
    shown.forEach((raw) => {
      const p = raw ?? {};
      const name = asString(p.name);
      const isOnline = asBoolean(p.online);
      const status = isOnline ? "online" : "offline";
      const av = el(clickable ? "button" : "div", {
        class: "rui-presence-avatar",
        type: clickable ? "button" : null,
        "data-online": isOnline ? "true" : null,
        title: name,
        role: clickable ? null : "img",
        "aria-label": name ? `${name} (${status})` : status
      });
      if (clickable) av.onclick = () => helpers.invoke(props.onClick, raw);
      const src = sanitiseImageSrc(p.src);
      if (src) {
        const img = el("img", { src, alt: "" });
        wireAvatarFallback(img, name, "rui-presence-initials");
        av.append(img);
      } else {
        av.append(el("span", { class: "rui-presence-initials" }, [initialsFor(name)]));
      }
      root.append(av);
    });
    if (people.length > max) {
      const rest = people.length - max;
      root.append(el("div", {
        class: "rui-presence-more",
        role: "img",
        "aria-label": `${rest} more`
      }, [`+${rest}`]));
    }
    return root;
  }
};
const SHARE_NETWORKS = ["twitter", "facebook", "linkedin", "reddit", "email", "whatsapp", "telegram", "mastodon", "copy"];
const SHARE_COPY_TIMERS = /* @__PURE__ */ new WeakMap();
const shareCopyLabel = (state) => state === "copied" ? "Copied" : state === "failed" ? "Copy failed" : "Copy link";
const shareCopyStatus = (state) => state === "copied" ? "Link copied" : state === "failed" ? "Could not copy link" : "";
let warnedShareUrl = false;
let warnedShareNetwork = false;
const ShareButtons = {
  name: "ShareButtons",
  description: "A row of social share buttons (twitter|facebook|linkedin|reddit|email|whatsapp|telegram|mastodon) plus a copy-link button, for the given `url`/`title`. `showLabels` renders text beside each icon.",
  props: [
    { name: "url", type: "string", positional: true, required: true },
    { name: "title", type: "string", optional: true },
    {
      name: "networks",
      type: "string[]",
      optional: true,
      enum: SHARE_NETWORKS,
      description: "Which to show (default twitter, facebook, linkedin, copy)"
    },
    { name: "showLabels", type: "boolean", optional: true, description: "Show a visible label beside each icon" }
  ],
  render: (_node, props, helpers) => {
    const url = sanitiseHref(props.url, "");
    const title = asString(props.title);
    const showLabels = asBoolean(props.showLabels);
    const root = el("div", { class: "rui-share", "data-labels": showLabels ? "true" : null });
    if (!url) {
      if (!warnedShareUrl && asString(props.url)) {
        warnedShareUrl = true;
        console.warn("[aktion] ShareButtons: unsafe `url` rejected — nothing to share.");
      }
      return root;
    }
    const eu = encodeURIComponent(url), et = encodeURIComponent(title);
    const requested = asArray(props.networks).map((n) => asString(n).toLowerCase());
    const targets = {
      twitter: { icon: "brands:x-twitter", href: `https://twitter.com/intent/tweet?url=${eu}&text=${et}`, label: "X" },
      facebook: { icon: "brands:facebook", href: `https://www.facebook.com/sharer/sharer.php?u=${eu}`, label: "Facebook" },
      linkedin: { icon: "brands:linkedin", href: `https://www.linkedin.com/sharing/share-offsite/?url=${eu}`, label: "LinkedIn" },
      reddit: { icon: "brands:reddit", href: `https://www.reddit.com/submit?url=${eu}&title=${et}`, label: "Reddit" },
      email: { icon: "envelope", href: `mailto:?subject=${et}&body=${eu}`, label: "Email" },
      whatsapp: { icon: "brands:whatsapp", href: `https://api.whatsapp.com/send?text=${et}%20${eu}`, label: "WhatsApp" },
      telegram: { icon: "brands:telegram", href: `https://t.me/share/url?url=${eu}&text=${et}`, label: "Telegram" },
      mastodon: { icon: "brands:mastodon", href: `https://mastodon.social/share?text=${et}%20${eu}`, label: "Mastodon" }
    };
    const known = requested.filter((n) => n === "copy" || targets[n]);
    if (requested.length > 0 && known.length < requested.length && !warnedShareNetwork) {
      warnedShareNetwork = true;
      console.warn(
        `[aktion] ShareButtons: unknown network(s) ${requested.filter((n) => !known.includes(n)).join(", ")}. Known: ${SHARE_NETWORKS.join(", ")}.`
      );
    }
    const list = known.length > 0 ? known : ["twitter", "facebook", "linkedin", "copy"];
    const copySlot = helpers.useInstanceState("rui-share-copy", null);
    const copyState = copySlot.get();
    const live = el("span", { class: "rui-visually-hidden", role: "status", "aria-live": "polite" }, [
      shareCopyStatus(copyState)
    ]);
    for (const net of list) {
      if (net === "copy") {
        const btn = el("button", {
          class: "rui-share-btn",
          type: "button",
          "aria-label": "Copy link",
          title: "Copy link",
          "data-state": copyState
        });
        const ic2 = renderIcon(copyState === "copied" ? "check" : "link");
        if (ic2) btn.append(ic2);
        if (showLabels) btn.append(el("span", { class: "rui-share-label" }, [shareCopyLabel(copyState)]));
        btn.onclick = (event) => {
          const target = event.currentTarget ?? event.target ?? btn;
          const status = target.closest(".rui-share")?.querySelector("[role='status']") ?? null;
          const paint = (state) => {
            copySlot.set(state);
            if (state) target.setAttribute("data-state", state);
            else target.removeAttribute("data-state");
            const next = renderIcon(state === "copied" ? "check" : "link");
            const current = target.querySelector(".rui-icon");
            if (next && current) current.replaceWith(next);
            const labelEl = target.querySelector(".rui-share-label");
            if (labelEl) labelEl.textContent = shareCopyLabel(state);
            if (status) status.textContent = shareCopyStatus(state);
          };
          const settle = (state) => {
            paint(state);
            const prior = SHARE_COPY_TIMERS.get(target);
            if (prior) clearTimeout(prior);
            SHARE_COPY_TIMERS.set(target, setTimeout(() => {
              SHARE_COPY_TIMERS.delete(target);
              paint(null);
            }, 2e3));
          };
          try {
            const written = navigator.clipboard?.writeText(url);
            if (written && typeof written.then === "function") {
              written.then(() => settle("copied")).catch(() => settle("failed"));
            } else {
              settle(navigator.clipboard ? "copied" : "failed");
            }
          } catch {
            settle("failed");
          }
        };
        root.append(btn);
        continue;
      }
      const t = targets[net];
      const a = el("a", { class: "rui-share-btn", href: t.href, target: "_blank", rel: "noopener noreferrer", "aria-label": `Share on ${t.label}` });
      const ic = renderIcon(t.icon);
      if (ic) a.append(ic);
      if (showLabels) a.append(el("span", { class: "rui-share-label" }, [`Share on ${t.label}`]));
      root.append(a);
    }
    if (list.includes("copy")) root.append(live);
    return root;
  }
};
const AuthorByline = {
  name: "AuthorByline",
  description: "An author byline: avatar, name, optional role, date and reading time — for articles/blog posts. `href` links the name to the author's page; `date` is emitted as a machine-readable <time>.",
  props: [
    { name: "name", type: "string", positional: true, required: true },
    { name: "avatar", type: "string", optional: true, aliases: ["src"] },
    { name: "role", type: "string", optional: true },
    { name: "date", type: "string", optional: true, description: "Display string or ISO date" },
    { name: "href", type: "string", optional: true, description: "Link target for the author name" },
    { name: "readingTime", type: "string", optional: true, description: 'e.g. "8 min read"' }
  ],
  render: (_node, props) => {
    const name = asString(props.name);
    const root = el("div", { class: "rui-byline" });
    const src = sanitiseImageSrc(props.avatar);
    const av = el("div", { class: "rui-byline-avatar" });
    if (src) {
      const img = el("img", { src, alt: "" });
      wireAvatarFallback(img, name, "rui-byline-initials");
      av.append(img);
    } else {
      av.append(el("span", { class: "rui-byline-initials" }, [initialsFor(name)]));
    }
    root.append(av);
    const meta = el("div", { class: "rui-byline-meta" });
    const href = sanitiseHref(props.href, "");
    const nameRow = el("div", { class: "rui-byline-name" });
    nameRow.append(href ? el("a", { href, rel: "author" }, [name]) : document.createTextNode(name));
    meta.append(nameRow);
    const dateText = asString(props.date);
    const parsed = dateText ? new Date(dateText) : null;
    const iso = parsed && !Number.isNaN(parsed.getTime()) ? parsed.toISOString().slice(0, 10) : "";
    const bits = [];
    const role = asString(props.role);
    if (role) bits.push(el("span", { class: "rui-byline-role" }, [role]));
    if (dateText) bits.push(el("time", { datetime: iso || null }, [dateText]));
    const reading = asString(props.readingTime);
    if (reading) bits.push(el("span", { class: "rui-byline-reading" }, [reading]));
    if (bits.length > 0) {
      const sub = el("div", { class: "rui-byline-sub" });
      bits.forEach((bit, i) => {
        if (i > 0) sub.append(el("span", { class: "rui-byline-dot", "aria-hidden": "true" }, [" · "]));
        sub.append(bit);
      });
      meta.append(sub);
    }
    root.append(meta);
    return root;
  }
};
const VariantSelector = {
  name: "VariantSelector",
  description: "Product variant picker — color swatches or size pills. `options` is an array of strings or {label, value, color?, disabled?}. Bind `value` to a $variable; `onChange(value)` fires on select. `multiple: true` toggles a set and binds an array. Arrow keys move between options.",
  props: [
    { name: "options", type: "any[]", positional: true, required: true, aliases: ["items"] },
    { name: "value", type: "string | string[]", optional: true },
    { name: "kind", type: "string", optional: true, enum: ["pill", "swatch"] },
    { name: "label", type: "string", optional: true },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"] },
    { name: "disabled", type: "boolean", optional: true, description: "Disable the whole group" },
    { name: "multiple", type: "boolean", optional: true, description: "Multi-select (binds an array of values)" },
    { name: "size", type: "string", optional: true, enum: ["sm", "md"] }
  ],
  render: (node, props, helpers) => {
    const kind = asString(props.kind, "pill");
    const multiple = asBoolean(props.multiple);
    const groupDisabled = asBoolean(props.disabled);
    const label = asString(props.label);
    const labelId = instanceId(helpers, "rui-variants-label-id", "rui-variants-label");
    const root = el("div", {
      class: "rui-variants",
      "data-kind": kind,
      "data-size": canonicalSizeToken(asString(props.size, "md")),
      // A bare row of toggle buttons announces N isolated "pressed" states with
      // no group name and no position; a radiogroup announces "3 of 12, Colour".
      role: multiple ? "group" : "radiogroup",
      "aria-labelledby": label ? labelId : null,
      "aria-disabled": groupDisabled ? "true" : null
    });
    if (label) root.append(el("div", { class: "rui-variants-label", id: labelId }, [label]));
    const row = el("div", { class: "rui-variants-row" });
    const selection = multiple ? asArray(props.value).map((v) => asString(v)) : [asString(props.value)];
    const ref = node.argMeta?.[1]?.stateRef;
    let firstEnabled = null;
    let hasSelected = false;
    for (const raw of asArray(props.options)) {
      const opt = raw && typeof raw === "object" ? raw : { label: raw, value: raw };
      const value = asString(opt.value ?? opt.label);
      const text = asString(opt.label ?? opt.value);
      const selected = selection.includes(value);
      const disabled = groupDisabled || asBoolean(opt.disabled);
      if (selected) hasSelected = true;
      const btn = el("button", {
        class: "rui-variant",
        type: "button",
        role: multiple ? null : "radio",
        "aria-pressed": multiple ? selected ? "true" : "false" : null,
        "aria-checked": multiple ? null : selected ? "true" : "false",
        "data-selected": selected ? "true" : null,
        disabled,
        "aria-label": kind === "swatch" ? text : null,
        // Roving tabindex: one tab stop for the whole group.
        tabindex: disabled ? null : selected ? "0" : "-1"
      });
      if (kind === "swatch") {
        const colour = sanitiseCssColor(opt.color ?? opt.value);
        btn.style.background = colour || "var(--rui-color-surface-muted)";
        btn.title = text;
        if (selected) {
          const tick = renderIcon("check", { className: "rui-variant-check" });
          if (tick) btn.append(tick);
        }
      } else {
        btn.textContent = text;
      }
      if (!disabled && !firstEnabled) firstEnabled = btn;
      btn.onclick = () => {
        if (disabled) return;
        const next = multiple ? selection.includes(value) ? selection.filter((v) => v !== value) : [...selection, value] : value;
        if (ref) helpers.setState(ref, next);
        helpers.invoke(props.onChange, next);
      };
      row.append(btn);
    }
    if (!hasSelected && firstEnabled) firstEnabled.setAttribute("tabindex", "0");
    row.onkeydown = (event) => {
      const keys = ["ArrowRight", "ArrowDown", "ArrowLeft", "ArrowUp", "Home", "End"];
      if (!keys.includes(event.key)) return;
      const origin = event.currentTarget ?? event.target;
      const items = Array.from(origin?.querySelectorAll(".rui-variant:not([disabled])") ?? []);
      if (items.length === 0) return;
      const active = origin?.getRootNode()?.activeElement ?? null;
      const at = items.findIndex((b) => b === active);
      const next = event.key === "Home" ? 0 : event.key === "End" ? items.length - 1 : event.key === "ArrowLeft" || event.key === "ArrowUp" ? at <= 0 ? items.length - 1 : at - 1 : at < 0 || at === items.length - 1 ? 0 : at + 1;
      event.preventDefault();
      items.forEach((b) => b.setAttribute("tabindex", "-1"));
      const target = items[next];
      target.setAttribute("tabindex", "0");
      target.focus();
    };
    root.append(row);
    return root;
  }
};
function moneyFormatter(currency, locale) {
  const iso = /^[A-Za-z]{3}$/.test(currency) ? currency.toUpperCase() : "";
  let fmt = null;
  try {
    fmt = new Intl.NumberFormat(locale || void 0, iso ? { style: "currency", currency: iso } : { minimumFractionDigits: 2, maximumFractionDigits: 2 });
  } catch {
    fmt = null;
  }
  return (value) => {
    if (value === null || value === void 0) return "";
    const raw = asString(value).trim();
    if (!raw) return "";
    const n = typeof value === "number" ? value : Number(raw);
    if (!Number.isFinite(n)) return raw;
    if (!fmt) return iso ? `${iso} ${n.toFixed(2)}` : `${currency}${n.toFixed(2)}`;
    return iso ? fmt.format(n) : `${currency}${fmt.format(n)}`;
  };
}
const OrderSummary = {
  name: "OrderSummary",
  description: 'An order/cart summary: line items, subtotal, discount, shipping, tax, and a bold total. Pass `items` as {label, amount, qty?} and the named totals. `currency` takes an ISO code ("EUR" — properly localised via `locale`) or a bare symbol prefix. `loading` shows placeholders while an async shipping/tax quote resolves.',
  props: [
    { name: "items", type: "object[]", positional: true, required: true, aliases: ["lines"] },
    { name: "subtotal", type: "string | number", optional: true },
    { name: "shipping", type: "string | number", optional: true },
    { name: "tax", type: "string | number", optional: true },
    { name: "total", type: "string | number", optional: true },
    { name: "currency", type: "string", optional: true, description: 'ISO code ("USD") or a symbol prefix (default "$")' },
    { name: "discount", type: "string | number", optional: true, description: "Coupon/promo line, shown after the subtotal" },
    { name: "locale", type: "string", optional: true, description: "BCP-47 tag for number formatting (default: the browser's)" },
    { name: "loading", type: "boolean", optional: true, description: "Show placeholders for the named totals" },
    { name: "note", type: "string", optional: true, description: "Fine print under the total ('Taxes calculated at checkout')" },
    { name: "empty", type: "string", optional: true, description: "Message when `items` is empty" }
  ],
  render: (_node, props) => {
    const cur = asString(props.currency, "$");
    const money = moneyFormatter(cur, asString(props.locale));
    const loading = asBoolean(props.loading);
    const root = el("div", { class: "rui-order-summary", "data-loading": loading ? "true" : null });
    const items = asArray(props.items);
    if (items.length > 0) {
      const lines = el("div", { class: "rui-order-lines" });
      for (const raw of items) {
        const it = raw ?? {};
        const line = el("div", { class: "rui-order-line" });
        const labelCell = el("span", {}, [asString(it.label)]);
        const qty = asNumber(it.qty ?? it.quantity, 0);
        if (qty > 0) labelCell.append(el("span", { class: "rui-order-qty" }, [` ×${qty}`]));
        line.append(labelCell, el("span", {}, [money(it.amount)]));
        lines.append(line);
      }
      root.append(lines);
    } else {
      const empty = asString(props.empty, "Your cart is empty");
      if (empty) root.append(el("div", { class: "rui-order-empty" }, [empty]));
    }
    const row = (label, v, opts = {}) => {
      const missing = v === null || v === void 0 || asString(v) === "";
      if (missing && !(loading && opts.quote !== false)) return;
      const r = el("div", { class: opts.strong ? "rui-order-total" : "rui-order-sub" });
      const amount = missing ? el("span", {
        class: "rui-skeleton-line rui-order-skeleton",
        style: "width:64px;height:12px;flex:none",
        "aria-hidden": "true"
      }) : el("span", {}, [money(v)]);
      r.append(el("span", {}, [label]), amount);
      root.append(r);
    };
    row("Subtotal", props.subtotal);
    row("Discount", props.discount, { quote: false });
    row("Shipping", props.shipping);
    row("Tax", props.tax);
    row("Total", props.total, { strong: true });
    const note = asString(props.note);
    if (note) root.append(el("div", { class: "rui-order-note" }, [note]));
    return root;
  }
};
const ScrollSpy = {
  name: "ScrollSpy",
  description: "A sticky in-page nav that highlights the section currently in view. `sections` is an array of {label, id} matching element ids on the page (set via the universal `id` prop). Clicking smooth-scrolls to a section; `offset` clears a sticky header and `top` sets the sticky offset.",
  props: [
    { name: "sections", type: "object[]", positional: true, required: true, aliases: ["items"] },
    { name: "title", type: "string", optional: true },
    { name: "offset", type: "number", optional: true, description: "Pixels to leave above the target (sticky header height)" },
    { name: "top", type: "string", optional: true, description: "Sticky offset from the viewport top (default 16px)" },
    { name: "onChange", type: "callable", optional: true, aliases: ["onchange"], description: "Fires with the id of the section entering view" }
  ],
  render: (_node, props, helpers) => {
    const offset = Math.max(0, Math.round(asNumber(props.offset, 0)));
    const top = sanitiseCssLength(props.top, "");
    const activeSlot = helpers.useInstanceState("rui-scrollspy-active", "");
    const root = el("nav", {
      class: "rui-scrollspy",
      "aria-label": "On this page",
      style: top ? `--rui-scrollspy-top:${top}` : null
    });
    const title = asString(props.title);
    if (title) root.append(el("div", { class: "rui-scrollspy-title" }, [title]));
    const list = el("ul", { class: "rui-scrollspy-list" });
    const sections = asArray(props.sections).map((raw) => {
      const s = raw ?? {};
      return { label: asString(s.label), id: asString(s.id).replace(/[^A-Za-z0-9_-]/g, "") };
    }).filter((s) => s.id);
    const active = activeSlot.get();
    for (const s of sections) {
      const li = el("li", { class: "rui-scrollspy-item" });
      const isActive = s.id === active;
      const a = el("a", {
        href: `#${s.id}`,
        class: isActive ? "is-active" : null,
        "aria-current": isActive ? "location" : null
      }, [s.label]);
      a.onclick = (event) => {
        event.preventDefault();
        const origin = event.currentTarget ?? event.target;
        const sr = origin?.getRootNode();
        const target = sr?.getElementById?.(s.id) ?? document.getElementById(s.id);
        if (!target) return;
        if (offset > 0 && typeof window !== "undefined" && typeof window.scrollTo === "function") {
          const y = target.getBoundingClientRect().top + (window.scrollY || 0) - offset;
          window.scrollTo({ top: Math.max(0, y), behavior: "smooth" });
        } else {
          target.scrollIntoView({ behavior: "smooth", block: "start" });
        }
      };
      li.append(a);
      list.append(li);
    }
    root.append(list);
    if (typeof IntersectionObserver !== "undefined" && sections.length > 0) {
      onceMounted(root, helpers, "rui-scrollspy-io", (live) => {
        const sr = live.getRootNode();
        const io = new IntersectionObserver((entries) => {
          for (const e of entries) {
            if (!e.isIntersecting) continue;
            const id = e.target.id;
            if (id === activeSlot.get()) continue;
            activeSlot.set(id);
            for (const link of live.querySelectorAll(".rui-scrollspy-item a")) {
              const on = link.getAttribute("href") === `#${id}`;
              link.classList.toggle("is-active", on);
              if (on) link.setAttribute("aria-current", "location");
              else link.removeAttribute("aria-current");
            }
            helpers.invoke(props.onChange, id);
          }
        }, { rootMargin: `-${offset}px 0px -70% 0px`, threshold: 0 });
        for (const s of sections) {
          const target = sr.getElementById?.(s.id) ?? document.getElementById(s.id);
          if (target) io.observe(target);
        }
        helpers.registerDisposer(() => io.disconnect(), "rui-scrollspy-io");
      });
    }
    return root;
  }
};
const DIAL_POSITIONS = ["bottom-right", "bottom-left", "top-right", "top-left"];
const DIAL_INSETS = {
  "bottom-right": "top:auto;left:auto;bottom:24px;right:24px",
  "bottom-left": "top:auto;right:auto;bottom:24px;left:24px",
  "top-right": "bottom:auto;left:auto;top:24px;right:24px",
  "top-left": "bottom:auto;right:auto;top:24px;left:24px"
};
const SpeedDial = {
  name: "SpeedDial",
  description: "A floating action button that expands a stack of mini-actions on click. Pass `actions` as {icon, label, onClick} (`action` is accepted as a synonym for `onClick`). Bind `open` to control it from outside; `position` picks the corner. Outside-click and Escape close it.",
  props: [
    { name: "actions", type: "object[]", positional: true, required: true, aliases: ["items"] },
    { name: "icon", type: "string", optional: true, description: "Main FAB icon (default 'plus')" },
    { name: "open", type: "boolean", optional: true, description: "Expanded state — usually a $variable" },
    { name: "onOpenChange", type: "callable", optional: true, description: "Fires with the new expanded state" },
    { name: "position", type: "string", optional: true, enum: DIAL_POSITIONS },
    { name: "label", type: "string", optional: true, description: "Accessible name for the FAB (default 'Actions')" }
  ],
  render: (node, props, helpers) => {
    const openSlot = helpers.useInstanceState("rui-speeddial-open", false);
    const stateRef = node.argMeta?.[2]?.stateRef;
    const open = props.open === void 0 ? openSlot.get() : asBoolean(props.open);
    const iconName = asString(props.icon, "plus");
    const position = DIAL_POSITIONS.includes(asString(props.position)) ? asString(props.position) : "bottom-right";
    const root = el("div", {
      class: open ? "rui-speeddial is-open" : "rui-speeddial",
      "data-position": position,
      // Lets the theme restrict the 45° FAB rotation to the default plus glyph.
      "data-icon": iconName
    });
    const menu = el("div", {
      class: "rui-speeddial-menu",
      role: "menu",
      // `opacity: 0; pointer-events: none` hides the closed menu from the mouse
      // only: the actions stayed in the tab order and a keyboard user could fire
      // 'Delete all' on an invisible button.
      inert: open ? null : true,
      "aria-hidden": open ? null : "true"
    });
    const paint = (live, next) => {
      live.classList.toggle("is-open", next);
      live.querySelector(".rui-speeddial-fab")?.setAttribute("aria-expanded", next ? "true" : "false");
      const m = live.querySelector(".rui-speeddial-menu");
      if (!m) return;
      if (next) {
        m.removeAttribute("inert");
        m.removeAttribute("aria-hidden");
      } else {
        m.setAttribute("inert", "");
        m.setAttribute("aria-hidden", "true");
      }
      m.querySelectorAll(".rui-speeddial-action").forEach((b) => b.setAttribute("tabindex", next ? "0" : "-1"));
    };
    const setOpen = (live, next) => {
      openSlot.set(next);
      paint(live, next);
      if (next) {
        installDismissListeners({ liveRoot: live, onDismiss: () => setOpen(live, false), key: "rui-speeddial" });
      } else {
        disposeDismissListeners(live);
      }
      if (stateRef) helpers.setState(stateRef, next);
      helpers.invoke(props.onOpenChange, next);
    };
    for (const raw of asArray(props.actions)) {
      const a = raw ?? {};
      const btn = el("button", {
        class: "rui-speeddial-action",
        type: "button",
        role: "menuitem",
        title: asString(a.label),
        "aria-label": asString(a.label),
        tabindex: open ? "0" : "-1"
      });
      const ic2 = renderIcon(a.icon ?? "circle");
      if (ic2) btn.append(ic2);
      btn.onclick = (event) => {
        helpers.invoke(a.onClick ?? a.action);
        const live = (event.currentTarget ?? event.target)?.closest(".rui-speeddial");
        if (live) setOpen(live, false);
      };
      menu.append(btn);
    }
    root.append(menu);
    const fab = el("button", {
      class: "rui-speeddial-fab",
      type: "button",
      "aria-label": asString(props.label, "Actions"),
      "aria-haspopup": "true",
      "aria-expanded": open ? "true" : "false"
    });
    const ic = renderIcon(iconName, { size: "lg" });
    if (ic) fab.append(ic);
    fab.onclick = (event) => {
      const liveFab = event.currentTarget ?? event.target ?? fab;
      const live = liveFab.closest(".rui-speeddial") ?? root;
      setOpen(live, !live.classList.contains("is-open"));
    };
    root.append(fab);
    root.onkeydown = (event) => {
      if (event.key !== "Escape") return;
      const live = event.currentTarget ?? event.target;
      if (!live || !live.classList.contains("is-open")) return;
      event.stopPropagation();
      setOpen(live, false);
      live.querySelector(".rui-speeddial-fab")?.focus();
    };
    onceMounted(root, helpers, "rui-speeddial-layer", (live) => {
      promoteOverlay$1(live, `${OVERLAY_CORNER};${DIAL_INSETS[position] ?? DIAL_INSETS["bottom-right"]}`);
      helpers.registerDisposer(() => {
        disposeDismissListeners(live);
        releaseOverlay(live);
      }, "rui-speeddial-layer");
    });
    return root;
  }
};
const CONFETTI_COLORS = [
  "var(--rui-color-primary, #6366f1)",
  "var(--rui-color-accent, #8b5cf6)",
  "var(--rui-color-danger, #ec4899)",
  "var(--rui-color-info, #22d3ee)",
  "var(--rui-color-success, #10b981)",
  "var(--rui-color-warning, #f59e0b)"
];
function burst(target, opts) {
  let longest = 0;
  for (let i = 0; i < opts.count; i += 1) {
    const left = Math.round((i * 53 + 7) % 100);
    const delay = i * 17 % 50 / 100;
    const dur = opts.duration * (0.7 + i * 7 % 10 / 30);
    const color2 = opts.colors[i % opts.colors.length];
    const rot = i * 47 % 360;
    longest = Math.max(longest, delay + dur);
    const piece = el("span", {
      class: "rui-confetti-piece",
      // The rotation goes through a custom property: an inline `transform` is
      // dead code against keyframes that declare `transform` at 0% and 100%,
      // so every piece span in lockstep.
      style: `left:${left}%;background:${color2};animation-delay:${delay}s;animation-duration:${dur}s;--rot:${rot}deg`
    });
    piece.onanimationend = (event) => {
      (event.currentTarget ?? event.target)?.remove();
    };
    target.append(piece);
  }
  return longest;
}
const Confetti = {
  name: "Confetti",
  description: "A one-shot confetti burst — celebration affordance after a success (checkout, completion). Renders a short CSS particle animation each time `fire` becomes true, once per transition. `colors`/`duration` tune the look; `onDone` fires when the burst finishes. Honours prefers-reduced-motion.",
  props: [
    { name: "fire", type: "boolean", optional: true, description: "Trigger the burst (default true)" },
    { name: "count", type: "number", optional: true, description: "Particle count (default 40, max 120)" },
    { name: "colors", type: "string[]", optional: true, description: "Piece colours (default: theme tokens)" },
    { name: "duration", type: "number", optional: true, description: "Fall time in seconds (default 1.5, max 10)" },
    { name: "onDone", type: "callable", optional: true, description: "Fires once the burst has finished" }
  ],
  render: (_node, props, helpers) => {
    const fire = props.fire === void 0 ? true : asBoolean(props.fire);
    const root = el("div", {
      class: "rui-confetti",
      "aria-hidden": "true",
      "data-rui-preserve": "true",
      "data-fire": fire ? "true" : "false"
    });
    const reduce = typeof matchMedia !== "undefined" && matchMedia("(prefers-reduced-motion: reduce)").matches;
    const colors = (() => {
      const raw = asArray(props.colors).map((c) => sanitiseCssColor(c)).filter(Boolean);
      return raw.length > 0 ? raw : CONFETTI_COLORS;
    })();
    const opts = {
      count: Math.max(1, Math.min(120, Math.round(asNumber(props.count, 40)))),
      colors,
      duration: Math.max(0.2, Math.min(10, asNumber(props.duration, 1.5)))
    };
    const finish = (runtime) => {
      if (props.onDone == null) return;
      const t = setTimeout(() => helpers.invoke(props.onDone), Math.round(runtime * 1e3) + 50);
      helpers.registerDisposer(() => clearTimeout(t), "rui-confetti-done");
    };
    const primed = helpers.useInstanceState("rui-confetti-primed", false);
    const fired = helpers.useInstanceState("rui-confetti-fired", false);
    const play = (target) => finish(reduce ? 0 : burst(target, opts));
    if (!primed.get()) {
      primed.set(true);
      if (fire) {
        fired.set(true);
        play(root);
      }
    }
    if (!reduce) {
      onceMounted(root, helpers, "rui-confetti-layer", (live) => {
        promoteOverlay$1(live, OVERLAY_FILL_CLIP);
        helpers.registerDisposer(() => releaseOverlay(live), "rui-confetti-layer");
      });
    }
    if (typeof MutationObserver !== "undefined") {
      onceMounted(root, helpers, "rui-confetti-watch", (live) => {
        const sync = () => {
          const now = live.getAttribute("data-fire") === "true";
          if (now === fired.get()) return;
          fired.set(now);
          live.replaceChildren();
          if (now) play(live);
        };
        sync();
        const observer = new MutationObserver(sync);
        observer.observe(live, { attributes: true, attributeFilter: ["data-fire"] });
        helpers.registerDisposer(() => observer.disconnect(), "rui-confetti-watch");
      });
    }
    return root;
  }
};
function splitChord(raw, separator) {
  if (separator !== "+") {
    return raw.split(separator).map((k) => k.trim()).filter(Boolean);
  }
  const out = [];
  let buf = "";
  for (const ch of raw) {
    if (ch !== "+") {
      buf += ch;
      continue;
    }
    if (buf.trim()) {
      out.push(buf.trim());
      buf = "";
      continue;
    }
    out.push("+");
    buf = "";
  }
  if (buf.trim()) out.push(buf.trim());
  return out;
}
const KbdShortcut = {
  name: "KbdShortcut",
  description: 'Render a keyboard shortcut as styled key caps — the multi-key form of `Kbd`, sharing its markup and size scale. Pass `keys` as an array (e.g. ["Cmd", "K"]) or a \'+\'-joined string; `separator` changes the joiner ("" for the compact ⌘K style, " then " for sequential chords).',
  props: [
    { name: "keys", type: "string | string[]", positional: true, required: true },
    { name: "size", type: "string", optional: true, enum: ["sm", "md"] },
    { name: "separator", type: "string", optional: true, description: 'Joiner between caps (default "+")' }
  ],
  render: (_node, props) => {
    const raw = props.keys;
    const separator = props.separator === void 0 ? "+" : asString(props.separator);
    const keys = Array.isArray(raw) ? raw.map((k) => asString(k)).filter(Boolean) : splitChord(asString(raw), separator || "+");
    const root = el("span", {
      class: "rui-kbd-group rui-kbd-shortcut",
      "data-size": canonicalSizeToken(asString(props.size, "md"))
    });
    keys.forEach((k, i) => {
      if (i > 0 && separator) root.append(el("span", { class: "rui-kbd-sep rui-kbd-plus" }, [separator]));
      root.append(el("kbd", { class: "rui-kbd rui-kbd-key" }, [k]));
    });
    return root;
  }
};
const readLottie = () => typeof window !== "undefined" ? window.lottie ?? null : null;
const Lottie = {
  name: "Lottie",
  description: "Plays a Lottie/Bodymovin vector animation from `src` (a .json URL) or inline `data`. Uses the `lottie-web` library when it is present on the page (`window.lottie`), waiting briefly for a deferred script; otherwise shows the `fallback` (or a poster image) so the layout never breaks. `loop`/`autoplay` default true; `playing` pauses/resumes, `speed` scales playback, `onComplete`/`onError` report the outcome. No dependency is bundled.",
  props: [
    { name: "src", type: "string", optional: true, description: "URL to a Lottie JSON file" },
    { name: "data", type: "object", optional: true, description: "Inline Lottie animation data" },
    { name: "loop", type: "boolean", optional: true },
    { name: "autoplay", type: "boolean", optional: true },
    { name: "speed", type: "number", optional: true, description: "Playback rate, clamped to 0.1–10" },
    { name: "width", type: "string", optional: true },
    { name: "height", type: "string", optional: true },
    { name: "poster", type: "string", optional: true, description: "Image shown when lottie-web is unavailable" },
    { name: "fallback", type: "Node", optional: true, description: "Node shown when lottie-web is unavailable and no poster" },
    { name: "label", type: "string", optional: true, description: "Accessible name; omit for a decorative animation" },
    { name: "playing", type: "boolean", optional: true, description: "Pause (false) / resume (true) an already-loaded animation" },
    { name: "onComplete", type: "callable", optional: true, description: "Fires when a non-looping animation ends" },
    { name: "onError", type: "callable", optional: true, description: 'Fires with "missing-library" or "load-failed"' }
  ],
  render: (_node, props, helpers) => {
    const width = sanitiseCssLength(props.width, "200px");
    const height = sanitiseCssLength(props.height, "200px");
    const label = asString(props.label);
    const playing = props.playing === void 0 ? null : asBoolean(props.playing);
    const root = el("div", {
      class: "rui-lottie",
      style: `width:${width};height:${height}`,
      role: label ? "img" : null,
      "aria-label": label || null,
      "aria-hidden": label ? null : "true",
      "data-playing": playing === null ? null : playing ? "true" : "false"
    });
    const data = props.data && typeof props.data === "object" ? props.data : null;
    const src = sanitiseImageSrc(props.src);
    if (!data && !src) {
      renderLottieFallback(root, props, helpers, label);
      return root;
    }
    if (!readLottie()) renderLottieFallback(root, props, helpers, label);
    const speed = Math.max(0.1, Math.min(10, asNumber(props.speed, 1)));
    const mount = (live, lottie) => {
      try {
        live.setAttribute("data-rui-preserve", "true");
        live.replaceChildren();
        live.classList.remove("rui-lottie-empty");
        const anim = lottie.loadAnimation({
          container: live,
          renderer: "svg",
          loop: props.loop === void 0 ? true : asBoolean(props.loop),
          autoplay: props.autoplay === void 0 ? true : asBoolean(props.autoplay),
          ...data ? { animationData: data } : { path: src }
        });
        if (speed !== 1) anim.setSpeed?.(speed);
        if (playing === false) anim.pause?.();
        if (props.onComplete != null) anim.addEventListener?.("complete", () => helpers.invoke(props.onComplete));
        helpers.registerDisposer(() => {
          try {
            anim.destroy?.();
          } catch {
          }
        }, "rui-lottie");
        if (playing !== null && typeof MutationObserver !== "undefined") {
          const observer = new MutationObserver(() => {
            if (live.getAttribute("data-playing") === "false") anim.pause?.();
            else anim.play?.();
          });
          observer.observe(live, { attributes: true, attributeFilter: ["data-playing"] });
          helpers.registerDisposer(() => observer.disconnect(), "rui-lottie-playing");
        }
        return true;
      } catch {
        live.classList.add("rui-lottie-empty");
        helpers.invoke(props.onError, "load-failed");
        return false;
      }
    };
    onceMounted(root, helpers, "rui-lottie-mount", (live) => {
      const ready = readLottie();
      if (ready) {
        mount(live, ready);
        return;
      }
      let tries = 0;
      const timer = setInterval(() => {
        tries += 1;
        const late = readLottie();
        if (late) {
          clearInterval(timer);
          mount(live, late);
          return;
        }
        if (tries >= 25) {
          clearInterval(timer);
          helpers.invoke(props.onError, "missing-library");
        }
      }, 200);
      helpers.registerDisposer(() => clearInterval(timer), "rui-lottie-wait");
    });
    return root;
  }
};
function renderLottieFallback(target, props, helpers, label) {
  const poster = sanitiseImageSrc(props.poster);
  if (poster) {
    target.append(el("img", {
      src: poster,
      alt: label,
      class: "rui-lottie-poster",
      style: "width:100%;height:100%;object-fit:contain"
    }));
  } else if (props.fallback != null) {
    target.append(helpers.renderNode(props.fallback));
  } else {
    target.classList.add("rui-lottie-empty");
  }
}
const components = [
  Row,
  Column,
  Center,
  Stack,
  StackItem,
  Grid,
  GridItem,
  Box,
  Fragment,
  Card,
  CardHeader,
  CardFooter,
  CardSection,
  Separator,
  Tabs,
  TabItem,
  Accordion,
  AccordionItem,
  Modal,
  Steps,
  AspectRatio,
  ScrollArea,
  Container,
  Spacer,
  Text,
  TextContent,
  Image$1,
  Link,
  Badge,
  BadgeList,
  Pill,
  Callout,
  CodeBlock,
  Skeleton,
  Markdown,
  Quote,
  Icon,
  Spinner,
  LoadingDots,
  Form,
  FormControl,
  Input,
  TextArea,
  Select,
  SelectItem,
  Checkbox,
  CheckBoxGroup,
  CheckBoxItem,
  Radio,
  Button,
  Buttons,
  ButtonGroup,
  InputGroup,
  SearchBar,
  Slider,
  NumberInput,
  DatePicker,
  FileUpload,
  Combobox,
  MultiSelect,
  DateRangePicker,
  Table,
  Col,
  List,
  ListItem,
  StatCard,
  Sparkline,
  Tree,
  TreeNode,
  BarChart,
  LineChart,
  PieChart,
  Series,
  SectionBlock,
  ListBlock,
  FollowUpBlock,
  FollowUpItem,
  ActionLink,
  ChatBubble,
  Avatar,
  AvatarGroup,
  Progress,
  ProgressRing,
  Switch,
  ToggleGroup,
  Tooltip,
  HoverCard,
  Popover,
  Toast,
  Toasts,
  Kbd,
  Rating,
  Breadcrumb,
  BreadcrumbItem,
  Pagination,
  Navbar,
  NavbarItem,
  DropdownMenu,
  MenuItem,
  MenuSeparator,
  MenuLabel,
  Hero,
  PageHeader,
  Stats,
  Tile,
  EmptyState,
  Timeline,
  TimelineItem,
  FeatureGrid,
  FeatureItem,
  Testimonial,
  ProfileCard,
  PersonChip,
  Comment,
  Banner,
  Notification,
  ActionStripe,
  MediaCard,
  KanbanBoard,
  KanbanColumn,
  KanbanCard,
  SectionHeader,
  Toolbar,
  Sidebar,
  SidebarSection,
  SidebarItem,
  AppShell,
  SplitView,
  DescriptionList,
  DescriptionItem,
  StatusDot,
  PricingTable,
  PricingCard,
  // Advanced data
  DataGrid,
  CalendarView,
  ActivityLog,
  ComparisonTable,
  InfiniteList,
  // Media
  VideoPlayer,
  AudioPlayer,
  Carousel,
  Gallery,
  Lightbox,
  Map$1,
  // Editors
  RichTextEditor,
  CodeEditor,
  ContextMenu,
  ColorPicker,
  // More charts
  Gauge,
  Heatmap,
  RadarChart,
  ScatterChart,
  Histogram,
  // Advanced forms
  PinInput,
  PasswordInput,
  TagInput,
  MentionInput,
  TimePicker,
  DateTimePicker,
  MaskedInput,
  FormSection,
  FieldSet,
  ValidationSummary,
  RequirementList,
  MultiStepForm,
  // Advanced patterns + state cards
  InboxPanel,
  OnboardingChecklist,
  LoadingState,
  ErrorState,
  SuccessState,
  Tour,
  Spotlight,
  Sticky,
  ResizablePanels,
  MasonryGrid,
  Drawer,
  TopBar,
  NavLink,
  IconButton,
  CommandPalette,
  FilterChips,
  FilterPill,
  FieldRepeater,
  VirtualList,
  VirtualGrid,
  QueryBuilder,
  DiffViewer,
  JsonTree,
  Gantt,
  Truncate,
  InlineEdit,
  NotificationBell,
  // Aktion 0.5 standard helpers
  Async,
  Show,
  Portal,
  Redirect,
  Lazy,
  ErrorBoundary,
  // Behavioural & styling wrappers.
  // `Link` is deliberately NOT repeated here: it is already registered above,
  // with the Content primitives. Registering it twice made `components.length`
  // one higher than the number of DISTINCT component names, which in turn made
  // every count derived from the array — the prompt, the skill reference, the
  // extension's description — off by one. (No literal counts in this comment:
  // they went stale the first time a component was added.)
  OnClick,
  OnMouse,
  OnKeyboard,
  OnFocus,
  OnIntersect,
  OnMount,
  Css,
  // Marketing / landing / utility composites (suggestions-global Parts II, VIII)
  GradientText,
  Display,
  Heading,
  Eyebrow,
  Section,
  Overlay,
  OverlayItem,
  Brand,
  NavBar,
  Footer,
  FooterColumn,
  LogoCloud,
  LogoChip,
  CountUp,
  Metric,
  MetricStrip,
  CodeWindow,
  BrowserFrame,
  Terminal,
  Backdrop,
  ThemeToggle,
  Swatch,
  CopyButton,
  SegmentedControl,
  FloatingActionButton,
  Prose,
  RelativeTime,
  PriceTag,
  QuantityStepper,
  ProductCard,
  TableOfContents,
  TypingIndicator,
  CountdownTimer,
  BackToTop,
  // Layout & motion (suggestions-global Parts II.2, III.2/3/4/5/6/7, IV.4)
  Split,
  Bento,
  BentoCell,
  Reveal,
  OnGesture,
  Sortable,
  Draggable,
  DropZone,
  Parallax,
  ReadingProgress,
  Transition,
  FlipList,
  RouteView,
  // Media / overlay / social / e-commerce / utility extras (Parts VIII, IX)
  Svg,
  Sheet,
  BottomSheet,
  ConfirmDialog,
  PresenceAvatars,
  ShareButtons,
  AuthorByline,
  VariantSelector,
  OrderSummary,
  ScrollSpy,
  SpeedDial,
  Confetti,
  KbdShortcut,
  Lottie,
  // Accessibility primitives (suggestions-global X.3)
  VisuallyHidden,
  SkipLink,
  LiveRegion,
  FocusTrap,
  // Wave-3: QR, reactions, presence, mobile tab bar, cart (VIII.2/4/8, XII.1)
  QRCode,
  ReactionPicker,
  LiveCursor,
  TabBar,
  Cart,
  // Scheduling (VIII.6)
  Calendar,
  // Interactive canvas / editor (VIII.7)
  DrawingCanvas,
  SignaturePad,
  // Escape hatches for raw HTML / CSS — last-resort primitives
  HTMLTag,
  Styles,
  // Imperative / third-party widget interop
  Mount,
  WebComponent
];
const componentGroups = [
  {
    name: "Layout",
    components: [
      "Column",
      "Row",
      "Center",
      "Stack",
      "StackItem",
      "Grid",
      "GridItem",
      "Box",
      "Container",
      "Spacer",
      "Card",
      "CardHeader",
      "CardFooter",
      "CardSection",
      "Separator",
      "Tabs",
      "TabItem",
      "Accordion",
      "AccordionItem",
      "Modal",
      "Drawer",
      "Steps",
      "AspectRatio",
      "ScrollArea",
      "Sticky",
      "ResizablePanels",
      "MasonryGrid",
      // Previously ungrouped: without a group entry a component ships with no
      // usage guidance in the generated prompt and is invisible in chat mode.
      "Fragment",
      "Section",
      "Split",
      "Bento",
      "BentoCell",
      "Overlay",
      "OverlayItem"
    ],
    notes: [
      "- THREE primitives cover almost everything: `Column` (stack top→bottom), `Row` (left→right), and `Grid` (equal columns / card walls). Reach for these first.",
      "- `root` is normally a `Column([...])` (or `Container([...])` for a centered page). A `Column` is the page body; put each major chunk in a `Card(...)`.",
      "- `Row([...])` keeps children at their natural width and vertically centered — ideal for toolbars, button rows, label+value pairs, nav bars. Use `justify` to distribute (`between`, `center`, `end`) and `gap` for spacing.",
      "- `Grid([...], { columns: N })` = N equal columns. Omit `columns` for auto-fit (wraps as many ≥`minChildWidth` columns as fit — best for KPI/card grids). Prefer `Grid` over a wrapping `Row` whenever cells should share a width.",
      '- For a 12-column dashboard / sidebar layout use `GridItem` spans: `Grid([GridItem(side, { span: "1/4" }), GridItem(main, { span: "3/4" })])`. Fractions `"1/2"`…`"1/12"` (or numbers 1–12) resolve on a 12-track grid; any `GridItem` child turns the grid on automatically.',
      '- `Center([...], { minHeight })` centers content on both axes — spinners, empty states, hero CTAs, modal bodies. Add `minHeight: "60vh"` to center vertically in a region.',
      '- `Stack` is the responsive escape hatch: use it ONLY when the direction itself must change across breakpoints, e.g. `Stack([...], { direction: {base: "column", md: "row"} })`.',
      "- Make one child in a `Row` expand with `StackItem(child, { grow: 1 })` (e.g. a search input beside a fixed button), or push items apart with a bare `Spacer()` between them.",
      "- `Container([...], { size })` centers a wide page within a comfortable max-width (sm/md/lg/xl/full) — landing pages, articles, marketing sections.",
      "- `Box([...], { padding?, margin?, border?, background?, maxWidth? })` is a plain spacing/surface wrapper for when a `Card` is too heavy.",
      '- `Separator(orientation?, label?)` adds a visual break between sections; pass a `label` for a centered "OR"-style divider.',
      "- `gap`/`padding` spacing tokens are `none|3xs|2xs|xs|sm|md|lg|xl|2xl|3xl` (`none` = 0); `align`/`justify`/`columns`/`direction`/`gap` all accept responsive maps like `{base: …, md: …, lg: …}`.",
      "- Use `Drawer` for side-panel detail views and `Modal` for centered dialogs. `Sticky(children, side?, offset?)` pins a toolbar/banner while content scrolls. `ResizablePanels(primary, secondary)` gives a user-resizable two-pane split. `MasonryGrid([...])` is for Pinterest-style mixed-height walls."
    ]
  },
  {
    name: "Content",
    components: [
      "Text",
      "Image",
      "Badge",
      "BadgeList",
      "Pill",
      "Callout",
      "Quote",
      "CodeBlock",
      "Skeleton",
      "Spinner",
      "LoadingDots",
      "Markdown",
      "Kbd",
      "Icon",
      // Previously ungrouped: without a group entry a component ships with no
      // usage guidance in the generated prompt and is invisible in chat mode.
      "TextContent",
      "GradientText",
      "Display",
      "Heading",
      "Eyebrow",
      "Prose",
      "RelativeTime",
      "Svg",
      "VisuallyHidden",
      "KbdShortcut",
      "CountUp",
      "CountdownTimer",
      "TableOfContents"
    ],
    notes: [
      "- Prefer `Markdown(...)` for rich paragraph text with inline formatting — the parser supports headings, blockquotes, fenced code, numbered/bullet lists, links, images, and bare-URL auto-linking.",
      "- Use `Callout(variant, title, description, icon?, compact?)` for highlighted notices; pass `compact: true` for a one-line inline note.",
      "- Use `Quote(text, cite?)` for inline pull-quotes inside articles and marketing sections (use `Testimonial` when you also have author/role/rating).",
      "- Use `CodeBlock(language, codeString, showLineNumbers?, highlightLines?)` for read-only code snippets. The header always renders a copy-to-clipboard button.",
      '- Use `Badge(label, variant?, icon?, size?)` for a single pill and `BadgeList(["a","b","c"], variant?, size?)` to render an array of strings as Badge pills.',
      '- `Badge` vs `Pill`: `Badge` is a SOLID high-attention chip ("Recommended", "Save 50 %"); `Pill(label, tone?, icon?)` is the softer tinted STATE label for the current status of a thing ("SSL active", "pending", "broken"). Pill tones: neutral, activating, success, warning, critical, promoting, corporate.',
      "- Use `Skeleton(variant?, lines?, height?, shape?, width?)` for loading placeholders; `variant` accepts `paragraph` (default), `card`, `table-row`, `avatar`, `image`.",
      "- Use `Spinner(size?, label?, tone?)` for tiny inline loading indicators inside buttons, toolbars, or table cells; `LoadingDots(label?, size?, tone?)` is the quieter three-dot pulse alternative.",
      '- Use `Image(src, alt?, caption?, ratio?, fit?, fallback?)` — `ratio` (e.g. `"16:9"`) makes the image self-constrain so you do not need an outer `AspectRatio`.',
      '- Use `Kbd(["Cmd", "K"])` when referring to keyboard shortcuts.',
      '- Use `Icon(name, variant?, size?)` to render a standalone Font Awesome icon (`name` is the FA name without the `fa-` prefix, e.g. `"house"`, `"chart-line"`, `"regular:star"`, `"brands:github"`).',
      '- For page-level titles reach for `PageHeader(...)` (top of dashboards/detail pages) or `SectionHeader(...)` (inside a Card). For tiny inline titles use `Text(value, variant="large-heavy")`.'
    ]
  },
  {
    name: "Forms",
    components: [
      "Form",
      "FormControl",
      "FormSection",
      "FieldSet",
      "ValidationSummary",
      "RequirementList",
      "Input",
      "TextArea",
      "PasswordInput",
      "MaskedInput",
      "MentionInput",
      "TagInput",
      "Select",
      "SelectItem",
      "Combobox",
      "MultiSelect",
      "Checkbox",
      "CheckBoxGroup",
      "CheckBoxItem",
      "Radio",
      "Switch",
      "ToggleGroup",
      "Button",
      "Buttons",
      "ButtonGroup",
      "InputGroup",
      "SearchBar",
      "Slider",
      "NumberInput",
      "ColorPicker",
      "DatePicker",
      "DateRangePicker",
      "TimePicker",
      "DateTimePicker",
      "FileUpload",
      "PinInput",
      "MultiStepForm",
      // Previously ungrouped: without a group entry a component ships with no
      // usage guidance in the generated prompt and is invisible in chat mode.
      "SegmentedControl",
      "QuantityStepper",
      "VariantSelector",
      "Swatch",
      "DrawingCanvas",
      "SignaturePad",
      "ReactionPicker"
    ],
    notes: [
      "- Each FormControl should be a separate reference for progressive streaming.",
      "- Pass a `$variable` as the last argument to `Input`, `Select`, `Checkbox`, `Switch`, `MultiSelect`, or `CheckBoxGroup` for two-way binding.",
      "- Prefer `Switch` over `Checkbox` for settings; use `ToggleGroup` for view-mode pickers and mutually-exclusive filters.",
      "- Reach for `SearchBar(id, placeholder?, value?, shortcut?)` instead of a raw `Input` whenever the field's purpose is to filter content. It ships with the magnifier icon and keyboard hint baked in.",
      "- `Slider(id, min?, max?, step?, value?, label?, showValue?)` is the canonical control for numeric ranges (volume, brightness, filters); pass a `$variable` as `value` for two-way binding.",
      '- `NumberInput(id, value?, min?, max?, step?, placeholder?)` is friendlier than `Input(type="number")` for quantity steppers and integer settings — it ships with +/- buttons that respect `min`/`max`.',
      "- `DatePicker(id, value?, label?, min?, max?, placeholder?)` wraps the native date picker; pass `value` as a `$variable` for two-way binding (ISO `YYYY-MM-DD`).",
      "- `DateRangePicker(id, from?, to?, label?, min?, max?)` is the paired-date variant — bind both `from` and `to` to `$variable`s for a single shared range.",
      "- `Combobox(id, items, value?, placeholder?, emptyLabel?)` is the searchable single-select alternative to `Select` — type to filter long option lists (countries, currencies, users).",
      "- `MultiSelect(id, items, value?, placeholder?, emptyLabel?, max?)` is the multi-select equivalent — bind a `$variable` array as `value` for two-way binding, the trigger renders the picks as removable chips.",
      "- `FileUpload(id, { label?, hint?, accept?, multiple?, action? })` is the styled file picker; the picked files cannot pass through a `$variable`, so wire the `action` prop to a `function` declaration.",
      '- Read what was picked with `$util.readFile(files, { as? })` — pass the whole pick straight through, it resolves the first file\'s contents as text (or `"dataUrl"` / `"base64"`) and resolves `""` rather than rejecting on any failure. `FileReader` is NOT reachable under the `"safe"` global policy, so this is the vetted read.',
      '- A submit button should call an `action` that awaits the relevant `$mutation` resource, optionally refetches a `$query`, and resets the form `$variable`s (e.g. `$title = ""`).',
      "- Button `size` accepts both `sm|md|lg` (canonical) and the legacy `small|normal|large`. Pass `icon` for an inline leading icon.",
      "- `FormSection(label, children, helper?)` is the canonical wrapper for related fields. Reach for it INSTEAD of nesting fields in Card + SectionHeader by hand.",
      "- `FieldSet(legend, children, helper?)` is the accessible `<fieldset>` for radio/checkbox groups; prefer `FormSection` for purely visual grouping.",
      "- `ValidationSummary(errors, title?)` renders an aggregate error panel at the top of the form. Pass `errors` as `{label, message}` objects.",
      "- `RequirementList(items)` marks each rule a value must satisfy met / unmet / not-yet-checked. Reach for it INSTEAD of restating every rule inside one `error` string — pair it with the field's `invalid` and `describedBy` so the border still reddens and the list is the explanation.",
      '- `PasswordInput(id, value?, placeholder?, strengthMeter?)` adds a show/hide toggle and an optional 4-step strength meter — prefer over `Input(type="password")` for sign-up flows.',
      "- `PinInput(id, length?, value?, type?)` renders per-digit code entry for 2FA / SMS verification (use `length=6` for OTP codes).",
      "- `TagInput(id, value?, placeholder?)` lets the user add comma- or Enter-separated chips bound to a `$variable` array.",
      "- `MentionInput(id, people, value?)` is a textarea with inline @-mention suggestions — use for comments, task notes, chat composers.",
      "- `MaskedInput(id, mask, value?)` formats input against a mask string (`9` digit, `A` letter, `*` any). Use for phone numbers, postal codes.",
      "- `TimePicker(id, value?)` and `DateTimePicker(id, value?)` wrap the corresponding native inputs with consistent styling.",
      "- `ColorPicker(id, value?, label?, swatches?)` pairs a color chip with a hex input and preset swatches — bind a $variable holding a hex string.",
      "- `MultiStepForm(steps, current, onSubmit?)` replaces ad-hoc `Steps` + content + manual prev/next wiring. Each step is `{title, details?, content}`."
    ]
  },
  {
    name: "Data",
    components: [
      "Table",
      "Col",
      "DataGrid",
      "List",
      "ListItem",
      "StatCard",
      "Stats",
      "Sparkline",
      "Tile",
      "Progress",
      "ProgressRing",
      "Pagination",
      "Tree",
      "TreeNode",
      "CalendarView",
      "ComparisonTable",
      "InfiniteList",
      // Previously ungrouped: without a group entry a component ships with no
      // usage guidance in the generated prompt and is invisible in chat mode.
      "VirtualGrid",
      "Metric",
      "MetricStrip",
      "Calendar",
      "OrderSummary",
      "Cart",
      "PriceTag"
    ],
    notes: [
      '- Build columns using array pluck: `Col("Title", data.rows.title, format?, align?)`.',
      "- For per-row controls inside a Col, use `for (let row of data.rows) { ... }` and reference `row.field` inline.",
      '- `Table(cols, caption?, density?, striped?, sticky?, emptyLabel?)` — pass `density="compact"` for dense data, `sticky=true` to pin the header in a scrolling parent, and `emptyLabel` for the zero-state cell.',
      '- `DataGrid(cols, rowIds?, caption?, sort, selectedIds, selectable?, page, perPage?, …)` is the advanced Table — adds sortable headers (`sortable=true` on Col), per-column filter chips (`filterable=true`), checkbox row selection bound to `$selectedIds`, sticky header / first column, pagination, and an optional bulk-action toolbar. Set `columnMenu=true` to let users hide / reorder / pin columns (the button overlays the top-right of the header — it takes no column of its own and stays put while the grid scrolls sideways; pinning moves the column to the front of the table and above a divider in the panel, reordering never crosses that divider, and the last visible column cannot be hidden). Bind `columnMenuOpen` + `onColumnMenuOpenChange` and point `columnMenuAnchor` at your own button to open the panel from outside the grid, with `columnMenuButton: false` to drop the in-header icon; `resizable=true` for draggable column widths (the first drag pins the columns at their current widths and switches to a fixed layout, so a narrowed column truncates instead of shoving its neighbours);`persistKey="myTable"` to save the layout in localStorage. A small chevron appears in the header band whenever there are columns off-screen left / right — `scrollArrows=false` turns it off. `wrapCells=false` renders single-line cells with ellipsis + hover tooltip. `globalSearch` adds a cross-column search bar. `rowNumbers=true` shows a leading number column. Reach for this whenever a user needs to sort, filter, or page through a list.',
      "- `CalendarView(value?, month?, events?, view?, firstDay?, onSelect?)` renders a full-month (or week) calendar grid for scheduling apps — distinct from the `DatePicker` input. Bind `value` to a `$variable` for the selected ISO date.",
      "- `ComparisonTable(columns, rows, highlightColumn?)` is the generic counterpart of `PricingTable` — pass rows of `{label, values, hint?, group?}`. Use for feature comparisons, spec sheets, plan grids.",
      "- `InfiniteList(items, onLoadMore?, loading?, hasMore?)` is a scroll-to-load list; the action fires when the sentinel scrolls into view.",
      "- Use `Progress(value, max?, label?, tone?, indeterminate?, segments?, buffered?)` for linear bars — `segments` renders an N-step strip (onboarding flows), `buffered` adds a secondary buffer indicator.",
      "- `ProgressRing(value, max?, label?, tone?, size?)` is the circular variant for quotas/completion.",
      '- `Stats([{label, value, hint?, tone?, spark?}, …], layout?)` — `layout="strip"` (default) for inline KPIs; `layout="grid"` for a responsive StatCard grid. Pass `spark` for an inline trend line.',
      "- `StatCard(label, value, trend?, delta?, icon?, spark?, tone?)` gains an optional inline `Sparkline` via the `spark` prop. Use `Sparkline(values, tone?)` standalone for tiny trend chips in table cells.",
      "- `Tile(label, icon?, value?, description?, tone?, action?)` is the dense icon tile for quick-action menus and category grids; pair with `Grid` for uniform rows.",
      "- `Tree([TreeNode(label, children?, icon?, expanded?, active?, badge?, action?)])` renders a hierarchical tree (file browsers, nested navigation, category pickers); use `expanded=true` to open a branch by default.",
      '- `Pagination(page, totalPages, siblings?, total?, perPage?, perPageOptions?, compact?)` — bind `page` (and optionally `perPage`) to a `$variable`; pass `total` to render the "Showing N–M of T" summary. Reuse the same variable when slicing data with `arr.filter(...)` / `for`.'
    ]
  },
  {
    name: "Charts",
    components: [
      "BarChart",
      "LineChart",
      "PieChart",
      "RadarChart",
      "ScatterChart",
      "Histogram",
      "Heatmap",
      "Gauge",
      "Series"
    ],
    notes: [
      "- Use `LineChart` for trends (pass `filled=true` for area-style charts), `BarChart` for comparisons, `PieChart` for proportions, `RadarChart` for multi-axis scorecards.",
      "- `Heatmap(xLabels, yLabels, values)` renders a color-intensity matrix — perfect for calendar heatmaps, correlation grids, schedule density.",
      "- `ScatterChart(series, xLabel?, yLabel?)` plots XY points; pass each Series as `Series(name, points)` where points are `{x, y, label?}`.",
      "- `Histogram(values, binCount?)` bins raw numbers; pass pre-computed `bins=[{label, count}]` instead when you control the bucketing.",
      "- `Gauge(value, min?, max?, label?, tone?, size?)` is the half-doughnut KPI indicator for thresholds (uptime %, score, NPS).",
      '- Pass series via `Series("Name", [...numbers])`.'
    ]
  },
  {
    name: "Feedback & Media",
    components: [
      "Avatar",
      "AvatarGroup",
      "PersonChip",
      "Tooltip",
      "HoverCard",
      "Popover",
      "Rating",
      "Toast",
      "Toasts",
      "VideoPlayer",
      "AudioPlayer",
      "Carousel",
      "Gallery",
      "Lightbox",
      "Map",
      // Previously ungrouped: without a group entry a component ships with no
      // usage guidance in the generated prompt and is invisible in chat mode.
      "TypingIndicator",
      "Confetti",
      "Lottie",
      "QRCode",
      "PresenceAvatars",
      "LiveCursor",
      "Backdrop"
    ],
    notes: [
      "- `Avatar(name, src?, size?, status?)` falls back to initials when the image is missing.",
      "- Use `AvatarGroup` to render contributor strips with a `+N` overflow chip.",
      "- `PersonChip(name, role?, avatarSrc?, size?, status?, action?)` is the inline avatar + name + role pill — use everywhere a person is referenced (table cells, list rows, sidebar footers, kanban cards) instead of a raw `Avatar` next to `Text`.",
      "- Wrap any node in `Tooltip(label, trigger)` for inline hints.",
      "- Use `HoverCard(trigger, content)` when the popover needs rich content (profile preview, link target) and the trigger should open on hover.",
      "- `Popover(trigger, content, title?, side?, align?, width?)` is the click-triggered counterpart of `HoverCard` — use for filter panels, color pickers, share menus, and small settings flyouts. Always renders an × close button in the header; clicking the trigger again, clicking outside, or pressing Escape also closes it.",
      '- `Rating(value, max?, label?, count?, size?, interactive?, halfStep?, icon?)` renders stars for product reviews, testimonials, and ranked lists. Pass a `$variable` as `value` with `interactive=true` to let users rate; add `halfStep=true` so clicking the left half of a star sets a half-value. Set `icon="heart"|"thumb"|"fire"|"bolt"` (or any FA name) to swap glyphs.',
      "- `Toast(title, message?, tone?, icon?, duration?, action?, onClose?, position?)` pins a transient notice; pass `duration` (ms) for auto-dismiss. Use `Banner` for top-of-page announcements and `Notification` for permanent inbox entries.",
      "- `Toasts(children, position?)` stacks toasts in a viewport corner. Usually unnecessary — the `$toast` namespace (`$toast.success(...)` etc.) auto-renders its own stack. Use `Toasts` only to place the stack yourself: `Toasts($toast.items.map(t => Toast({ title: t.message, tone: t.tone, onClose: () => $toast.dismiss(t.id) })))` (rendering `$toast.items` opts out of the auto-layer).",
      "- `NotificationBell(count?, items?, onOpen?)` — compact inbox trigger; `CommandPalette` for Cmd-K action search."
    ]
  },
  {
    name: "Navigation",
    components: [
      "Breadcrumb",
      "BreadcrumbItem",
      "Navbar",
      "NavbarItem",
      "DropdownMenu",
      "MenuItem",
      "MenuSeparator",
      "MenuLabel",
      // Previously ungrouped: without a group entry a component ships with no
      // usage guidance in the generated prompt and is invisible in chat mode.
      "NavBar",
      "TabBar",
      "BackToTop",
      "ScrollSpy",
      "Brand",
      "Footer",
      "FooterColumn",
      "SkipLink"
    ],
    notes: [
      '- Use `Breadcrumb(["Workspace", "Reports", "Q3"])` at the top of every detail page so users see the path.',
      "- Crumbs navigate by default: a string trail derives `/workspace`, `/workspace/reports`, … from its own labels, and the first crumb gets a home icon. Pass `{ label, to }` objects (or `BreadcrumbItem` nodes) when the real routes are not the slugified labels, `autoLink: false` for a trail that is pure text, and `homeIcon: false` to drop the icon.",
      "- `Navbar(brand?, items?, actions?, sticky?, variant?)` + `NavbarItem(label, to?, href?, icon?, active?, action?, external?)` produces a top navigation bar with brand on the left, links in the middle, and actions on the right — the canonical companion of `Sidebar` for marketing pages, docs, or any product surface without left-side nav.",
      '- `DropdownMenu(trigger, items, side?, align?, label?)` is the click-triggered dropdown menu — use it for user-profile menus, row "…" action menus, and any compact list of actions hanging off a single trigger. Children must be `MenuItem`, `MenuSeparator`, or `MenuLabel` entries.',
      '- `MenuItem(label, action?, icon?, shortcut?, variant?, disabled?)` renders a single row inside a `DropdownMenu`; use `variant="danger"` for destructive actions and `MenuSeparator()`/`MenuLabel(label)` to group related items.'
    ]
  },
  {
    name: "Chat",
    components: ["SectionBlock", "ListBlock", "FollowUpBlock", "FollowUpItem", "ActionLink", "ChatBubble"],
    notes: [
      "- End most responses with a `FollowUpBlock` of 2–4 short prompts to keep the conversation moving.",
      '- `ChatBubble(author, body, time?, avatarSrc?, from?)` renders a single message bubble; use `from="me"` for the active speaker and `from="agent"` for the assistant. Compose transcripts as `Stack([ChatBubble(...), ChatBubble(...), …])` inside a `Card`.'
    ]
  },
  {
    name: "Patterns",
    components: [
      "Hero",
      "PageHeader",
      "EmptyState",
      "Timeline",
      "TimelineItem",
      "ActivityLog",
      "FeatureGrid",
      "FeatureItem",
      "Testimonial",
      "ProfileCard",
      "Comment",
      "Banner",
      "Notification",
      "InboxPanel",
      "OnboardingChecklist",
      "MediaCard",
      "TopBar",
      "KanbanBoard",
      "KanbanColumn",
      "KanbanCard",
      "SectionHeader",
      "Toolbar",
      "DescriptionList",
      "DescriptionItem",
      "ActionStripe",
      "StatusDot",
      "PricingTable",
      "PricingCard",
      "LoadingState",
      "ErrorState",
      "SuccessState",
      "Tour",
      "Spotlight",
      // Previously ungrouped: without a group entry a component ships with no
      // usage guidance in the generated prompt and is invisible in chat mode.
      "LogoCloud",
      "LogoChip",
      "ProductCard",
      "ShareButtons",
      "AuthorByline",
      "CodeWindow",
      "BrowserFrame",
      "Terminal",
      "ThemeToggle",
      "CopyButton"
    ],
    notes: [
      "- Patterns are **opinionated composites** that pack an entire UI idiom into one component. Reach for them BEFORE composing equivalent layouts by hand with Card+Stack — the result will look more polished and require fewer tokens.",
      '- `Hero(title, subtitle, primary, secondary, eyebrow?, highlights?, tone?)` — landing-style text-first header. Use `layout="cover"` with `imageSrc`, `height`, and optional `caption` for image-backed hero bands.',
      '- `PageHeader(title, subtitle?, breadcrumbs?, actions?, status?)` — the canonical first child for any dashboard or detail page. If you omit `breadcrumbs`, the component auto-derives `["Home", title]`.',
      "- `TopBar(title?, search?, actions?, sticky?)` — compact top strip for a content surface (panels, dialogs, embedded views). Use `AppShell` when you need a full sidebar; use `TopBar` for narrower headers above scrolling content.",
      "- `SectionHeader(title, subtitle?, eyebrow?, status?, actions?)` — sub-header inside a Card or panel. Use instead of bare `CardHeader` when the section also needs eyebrow / actions / status.",
      '- `Stats(items, layout?)` — KPI strip (`layout="strip"`, default) or responsive grid (`layout="grid"` with StatCard children). Prefer over hand-rolled StatCard rows.',
      "- `Toolbar(left?, right?, center?, searchable?, searchPlaceholder?, searchValue?)` — filter/search/actions row above a list, table, or board. Pass `searchable: true` to auto-mount a `SearchBar` (use `searchValue` to bind it to a `$variable`).",
      "- `EmptyState(title, description?, icon?, illustration?, actions?, action?)` — render this when a list is empty rather than an empty Card. The icon is auto-picked from the title keywords if you omit it (inbox/messages → `inbox`, charts/analytics → `chart-pie`, files/folders → `folder-open`, etc.).",
      "- `Timeline([TimelineItem(...)])` — vertical event feed (audit log, changelog, activity).",
      '- `ActivityLog(entries, variant?)` — purpose-built feed of user actions. Pass `entries` of `{actor, title, description?, time?, icon?, tone?, meta?}`; use `variant="audit"` for security/admin trails with monospace meta.',
      "- `FeatureGrid([FeatureItem(...)])` — feature highlights with iconography.",
      "- `MediaCard(title, imageSrc?, description?, tags?, meta?, actions?, badge?, orientation?, ratio?)` — image + content card. Use for article previews, product cards, project highlights. Pair with `Grid` for uniform card rows.",
      '- `KanbanBoard([KanbanColumn("To do", [KanbanCard(...), ...])])` — task boards.',
      '- `DescriptionList([DescriptionItem("Status", Badge(...)), …])` — detail-page key/value summary. Always preferable to a Stack of Text rows on profile, billing, or metadata panels.',
      "- `StatusDot(label, tone?, pulse?)` — inline status pip. Use in toolbars, list rows, table cells, sidebars.",
      "- `Notification(title, message?, time?, icon?, avatarSrc?, tone?, unread?, actions?)` — inline notification card for notification panels / inboxes (prefer `Banner` for top-of-page announcements).",
      "- `InboxPanel(items, title?, onMarkAllRead?)` — `Notification` cards grouped into Unread / Earlier sections, with a shared mark-all-read action.",
      "- `OnboardingChecklist(items, title?, description?)` — checklist of `{title, description?, done?, action?}` items with progress. Use for product onboarding, setup wizards, getting-started panels.",
      "- `PricingTable([PricingCard(plan, price, period?, description?, features?, action?, badge?, featured?)])` — full pricing page block.",
      "- `LoadingState(title?, description?, tone?, action?)`, `ErrorState(title, description?, action?, secondaryAction?, retryLabel?)`, `SuccessState(title, description?, action?, secondaryAction?)` — full-card empty-state alternatives for asynchronous content states.",
      "- `Tour(steps, current, onFinish?)` and `Spotlight(title?, description?, action?)` are the product-tour primitives. `Spotlight` is a single highlighted call-out; `Tour` walks the user through multiple `{title, description, image?}` steps with Prev/Next/Skip controls and a progress dots indicator."
    ]
  },
  {
    name: "Editors & overlays",
    components: [
      "RichTextEditor",
      "CodeEditor",
      "ContextMenu",
      // Previously ungrouped: without a group entry a component ships with no
      // usage guidance in the generated prompt and is invisible in chat mode.
      "Sheet",
      "BottomSheet",
      "ConfirmDialog",
      "SpeedDial",
      "FloatingActionButton"
    ],
    notes: [
      "- `RichTextEditor(id, value?, placeholder?, minHeight?)` is the contenteditable WYSIWYG editor — toolbar ships with bold/italic/underline/strike/heading/lists/link. Bind `value` to a `$variable` holding HTML.",
      "- `CodeEditor(id, value?, language?, placeholder?, minHeight?)` is a lightweight `<textarea>` with a synced line-number gutter and tab indentation. Use for snippet editors, prompt sandboxes, settings JSON.",
      "- `ContextMenu(target, items)` attaches a right-click menu to any node. `items` are `MenuItem` or `{label, action?, icon?, shortcut?, variant?, disabled?}` objects; pass a `MenuSeparator()` to split groups."
    ]
  },
  {
    name: "App shell",
    components: [
      "AppShell",
      "Sidebar",
      "SidebarSection",
      "SidebarItem",
      "SplitView"
    ],
    notes: [
      "- App shell components produce a **full SaaS-style layout in a single statement**. Use them whenever the response represents a complete product surface (dashboards with nav, settings sections, admin consoles, inboxes).",
      "- `AppShell(sidebar, content, topbar?, collapsible?, sidebarOpen?)` — fixed-left navigation + scrollable main area. Pass `collapsible=true` to enable a hamburger that turns the sidebar into a slide-over drawer on mobile; bind `sidebarOpen` to a `$variable` if you want to drive that drawer programmatically.",
      "- `Sidebar(items, brand?, tagline?, footer?, collapsed?)` + `SidebarItem(label, icon?, active?, badge?, action?)` + `SidebarSection(label, items)` — group nav links into sections, mark the current page with `active=true`, attach badges for counts. Pass `collapsed=true` (or bind to a `$variable`) to collapse it to an icon rail.",
      "- `SplitView(primary, detail, primaryWidth?)` — master/detail layout (inboxes, file browsers, contact lists). Both panes are scrollable."
    ]
  },
  {
    name: "Advanced UI",
    components: [
      "IconButton",
      "CommandPalette",
      "FilterChips",
      "FilterPill",
      "FieldRepeater",
      "VirtualList",
      "QueryBuilder",
      "DiffViewer",
      "JsonTree",
      "Gantt",
      "Truncate",
      "InlineEdit",
      "NotificationBell",
      // Previously ungrouped: without a group entry a component ships with no
      // usage guidance in the generated prompt and is invisible in chat mode.
      "Reveal",
      "Transition",
      "FlipList",
      "Parallax",
      "ReadingProgress",
      "Sortable",
      "Draggable",
      "DropZone",
      "OnGesture"
    ],
    notes: [
      "- `IconButton(icon, label, action?, variant?, size?, disabled?)` — accessible icon-only control.",
      "- `CommandPalette(items, open?, placeholder?, shortcut?)` — Cmd-K searchable actions.",
      "- `FilterChips(chips, onRemove?, onClear?)` — applied filter pills with remove.",
      "- `FieldRepeater(items, fields, onAdd?, onRemove?)` — dynamic form rows.",
      "- `VirtualList(items, itemHeight?, renderItem)` — windowed long lists.",
      "- `QueryBuilder(fields, value?)` — visual AND/OR filter builder.",
      "- `DiffViewer(left, right, mode?)` — side-by-side or unified diff.",
      "- `JsonTree(data)` — collapsible JSON inspector.",
      "- `Gantt(tasks, startDate?, endDate?)` — horizontal schedule timeline.",
      "- `Truncate(text, maxLines?)` / `InlineEdit(value, onSave?)` / `NotificationBell(count?, items?)`."
    ]
  },
  {
    name: "Routing",
    components: [
      "NavLink",
      // Previously ungrouped: without a group entry a component ships with no
      // usage guidance in the generated prompt and is invisible in chat mode.
      "RouteView"
    ],
    notes: [
      '- Declare routes with the `$router({...})` call form: `pages = $router({ "/": Dashboard(), "/orders/:id": OrderDetail({ id: params.id }), default: NotFound() })`. The matched arm\'s `params` object is in scope on the right-hand side; nest `$router` inside components for layout-preserving sub-routes.',
      "- `Redirect(path)` is a router-aware component: returning it from a route's body navigates and unmounts the rest of the subtree.",
      "- `NavLink(label, { to })` is a thin link wrapper that reads `route.path` and dispatches `route.navigate(to)` — use it for sidebars, navbars and breadcrumbs.",
      "- Read URL params via `route.params.<name>` (e.g. `route.params.id` for `/users/:id`) and the current path via `route.path`."
    ]
  },
  {
    name: "Helpers",
    components: [
      "Async",
      "Show",
      "Portal",
      "Redirect",
      "Lazy",
      "ErrorBoundary",
      // Previously ungrouped: without a group entry a component ships with no
      // usage guidance in the generated prompt and is invisible in chat mode.
      "LiveRegion",
      "FocusTrap"
    ],
    notes: [
      "- `Async(resource, { loading:, error:, empty:, data: })` switches an `$http({...})` resource on its `state` field (`empty` shows for `null`/empty-array data).",
      "- `Show(when, { fallback?, children })` is sugar over `if (expr) { children } else { fallback }`.",
      "- `Portal(target?, children)` renders into a different DOM subtree (defaults to `document.body`).",
      "- `Redirect(path)` is a router-aware component — see Routing.",
      "- `Lazy(loader, fallback?)` defers rendering until the async `loader` resolves, showing `fallback` while pending (a synchronous loader value renders immediately).",
      "- `ErrorBoundary(fallback?, onError?, children)` catches rendering errors thrown by descendants."
    ]
  },
  {
    name: "Behaviour wrappers",
    components: [
      "OnClick",
      "OnMouse",
      "OnKeyboard",
      "OnFocus",
      "OnIntersect",
      "OnMount",
      "Css",
      "Link"
    ],
    notes: [
      "- Wrappers compose: any built-in component can be made clickable, hoverable, observable, or restyled by wrapping it in one of these primitives — no need for the underlying component to grow another prop.",
      "- `OnClick(child, { onClick, disabled?, stopPropagation? })` makes any component clickable / tappable (touch devices fire `click` too). Use for clickable cards, list rows, media tiles, and any custom layout that needs a tap target without a `<button>` baseline.",
      "- `OnMouse(child, { enter?, leave?, hover?, move?, down?, up?, click?, doubleClick?, contextMenu?, scroll?, wheel?, drag?, drop?, dragStart?, dragEnd?, dragEnter?, dragLeave?, dragOver?, draggable?, passiveScroll? })` attaches any combination of mouse / pointer / drag listeners. Pass `draggable: true` to make the wrapper itself draggable. Scroll / wheel listeners default to `{ passive: true }` for smooth scrolling.",
      "- `OnKeyboard(child, { onKeyDown?, onKeyUp?, onKeyPress?, focusable? })` attaches keyboard listeners. The wrapper is focusable by default; pass `focusable: false` when the child is already focusable (input, button).",
      "- `OnFocus(child, { onFocus?, onBlur? })` tracks focus moving into or out of a subtree (uses bubbling `focusin` / `focusout` so descendants count).",
      "- `OnIntersect(child, { onEnter?, onLeave?, onChange?, threshold?, rootMargin?, once? })` is the IntersectionObserver wrapper — perfect for lazy-load sentinels, infinite-scroll triggers, impression analytics, and reveal-on-scroll animations.",
      "- `OnMount(child, { onMount?, onUnmount? })` is the DOM-ref / lifecycle wrapper. `onMount(node)` fires once after the wrapped element attaches; `onUnmount(node)` fires when it leaves the tree. Use it to measure or focus an element, or to hand a node to an imperative library (chart / map / editor). Stash the node in a `$ref(...)`.",
      "- `Css(child, { style?, class? })` merges raw class tokens and inline styles onto the wrapped child. Reach for it ONLY when the standard component props can't express the styling — prefer `Box`/`Stack`/`Grid` for layout and `$theme(...)` for tokens.",
      "- `Link(label_or_child, { to?, href?, external?, variant? })` is the anchor primitive — accepts either a plain string label or a wrapped component. Use `to` for client-side router navigation and `href` (with `external: true`) for outbound links."
    ]
  },
  {
    name: "Interop",
    components: ["Mount", "WebComponent"],
    notes: [
      "- Interop primitives are the bridge to imperative / third-party libraries that own their own DOM. Reach for them ONLY when a real widget (chart, map, editor, payment element, captcha) cannot be expressed with built-in components.",
      '- `Mount({ setup, update?, cleanup?, props?, tag?, sx? })` is the managed imperative-component host. `setup(node, props)` runs once after the host attaches and returns an instance handle; `update(instance, props)` runs when the (shallow-compared) `props` bag changes; `cleanup(instance)` runs on unmount. Aktion owns + preserves the host element so the widget is never rebuilt mid-session. Example: `Mount({ sx: { h: "320px" }, setup: (node, p) => new Chart(node, p.config), update: (c, p) => { c.data = p.data; c.update() }, cleanup: c => c.destroy(), props: { config: $cfg, data: $series } })`.',
      '- `WebComponent(tag, { attributes?, properties?, on?, children? })` renders + hydrates a native custom element. `attributes` is reactive (re-applies on `$state` change); `properties` assigns rich JS props; `on` binds event listeners that stay current across renders. Example: `WebComponent("stripe-pricing-table", { attributes: { "pricing-table-id": $id, "publishable-key": $pk }, on: { checkout: e => route.navigate("/thanks") } })`.',
      "- Pair these with the `$script({ src, global? })` loader (gate a widget on its SDK being ready) and the `$dom` observer namespace (`$dom.onResize`/`onIntersect`/`measure`) for layout-aware widgets."
    ]
  },
  {
    name: "Escape hatches",
    components: ["HTMLTag", "Styles"],
    notes: [
      "- **Use only as a last resort.** Reach for these primitives when the standard catalogue cannot express the markup or styling you need; for everything else (typography, layout, surfaces, controls) the dedicated components produce a more consistent UI for fewer tokens.",
      '- `HTMLTag(tag, attributes?, children?)` renders an allow-listed HTML tag with the given attribute object and child nodes (e.g. `HTMLTag("section", attributes: {class: "hero", "data-id": 1}, children: [Text("Hello")])`). Tag names outside the allow-list collapse to `div`; `on*` attributes, `javascript:` URLs in `href`/`src`, and `expression()` / `@import` in inline `style` are stripped.',
      "- `Styles(css)` injects a `<style>` block containing the given CSS rules (e.g. `` Styles(`.hero { background: linear-gradient(...); }`) ``). Pair it with `HTMLTag` (or any built-in component's `class`-bearing wrapper) when scoping to a custom selector. Payloads containing `</style>`, `<script>`, `expression(`, `javascript:`, `behavior:`, or `@import` are dropped."
    ]
  }
];
assertOnePositionalMax(components);
const defaultLibrary = {
  root: "Column",
  components,
  componentGroups
};
const projectParam = (prop2) => {
  const param = {
    name: prop2.name,
    type: prop2.type,
    required: !prop2.optional
  };
  if (prop2.description) param.description = prop2.description;
  if (prop2.enum && prop2.enum.length > 0) param.enumValues = prop2.enum;
  return param;
};
const buildSignature = (spec) => {
  const parts = spec.props.map((p) => p.optional ? `${p.name}?` : p.name);
  return `${spec.name}(${parts.join(", ")})`;
};
const buildGroupIndex = (library) => {
  const index = /* @__PURE__ */ new Map();
  for (const group of library.componentGroups ?? []) {
    for (const name of group.components) index.set(name, group.name);
  }
  return index;
};
function getComponentCatalog(library = defaultLibrary) {
  const groupOf = buildGroupIndex(library);
  return library.components.map((spec) => ({
    name: spec.name,
    group: groupOf.get(spec.name) ?? "Other",
    description: spec.description,
    params: spec.props.map(projectParam),
    signature: buildSignature(spec)
  }));
}
const UNIVERSAL_PROP_DOCS = {
  sx: {
    type: "object",
    description: 'Style channel accepted by EVERY component: layout, colour, typography, spacing, borders, effects. Values may be responsive maps (`{base, sm, md, lg}`), interaction states (`{_hover, _focus, _active}`), or theme-token refs (`"primary"`, `"gradient.brand"`, `"space.md"`).'
  },
  animate: {
    type: "string | object",
    description: 'Motion channel accepted by EVERY component. A preset name (`"fade"`, `"slide-up"`, `"pulse"`, `"none"`) or an object with `{ preset, duration, delay, easing, repeat }`.'
  },
  id: { type: "string", description: "DOM id on the rendered root element." },
  anchor: {
    type: "string",
    description: "Scroll-anchor id — the target for `SkipLink`, `ScrollSpy` and `#hash` links."
  },
  className: { type: "string", description: "Extra CSS classes appended to the rendered root element." },
  class: { type: "string", description: "Alias of `className`." },
  style: { type: "string | object", description: "Inline CSS. Prefer `sx`, which is token- and breakpoint-aware." },
  aria: {
    type: "object",
    description: 'ARIA attributes as a plain object, e.g. `{ label: "Close", expanded: false }` → `aria-*`.'
  },
  data: { type: "object", description: '`data-*` attributes as a plain object, e.g. `{ testid: "row-1" }`.' },
  dataAttrs: {
    type: "object",
    description: "Second spelling of the `data` channel, for the components that declare a `data` prop of their own (LineChart, JsonTree, Async, Draggable, Lottie, QRCode) and would otherwise shadow it."
  },
  role: {
    type: "string",
    description: "Override the rendered ARIA role. An escape valve for accessibility defects; use sparingly."
  },
  tooltip: { type: "string", description: "Native hover tooltip (`title`) on the rendered root element." },
  hidden: { type: "boolean", description: "Remove the component from the accessibility tree and hide it visually." },
  testId: {
    type: "string",
    description: "End-to-end test hook: renders `data-testid` on the component's ROOT element. Works on every component, including the six that shadow the `data` channel. Prefer role/label queries; reach for this where they are genuinely ambiguous."
  },
  testid: { type: "string", description: "Alias of `testId`." }
};
const universalPropCatalog = [...UNIVERSAL_PROP_NAMES].map((name) => {
  const doc = UNIVERSAL_PROP_DOCS[name];
  return {
    name,
    type: doc?.type ?? "any",
    required: false,
    description: doc?.description ?? "Universal prop accepted by every component."
  };
});
function indexCatalog(entries) {
  const out = {};
  for (const entry of entries) out[entry.name] = entry;
  return out;
}
function getSignatureHelp(source, position, library = defaultLibrary) {
  const call = findActiveCall(source, position);
  if (!call || !call.callee) return null;
  const spec = findComponent(library, call.callee);
  if (spec) {
    const signature2 = libraryComponentSignature(call.callee, library);
    if (!signature2) return null;
    const activeParameter2 = resolveActiveSlot(call, spec, signature2.parameters.length);
    return { signatures: [signature2], activeSignature: 0, activeParameter: activeParameter2 };
  }
  const signature = builtinSignature(call.callee) ?? memberCallSignature(call.callee, source) ?? userSignature(call.callee, source);
  if (!signature) return null;
  const activeParameter = resolveDeclaredParam(call, signature.parameters.map((p) => p.label));
  return { signatures: [signature], activeSignature: 0, activeParameter };
}
function resolveActiveSlot(call, spec, paramCount) {
  const noHighlight = paramCount;
  const bagIdx = chooseNamedBagIndex(call.args, spec);
  if (call.objectArg !== null && call.argIndex === bagIdx) {
    const key = call.objectArg.activeKey;
    if (!key) return noHighlight;
    const exact = spec.props.findIndex((p) => p.name === key || (p.aliases?.includes(key) ?? false));
    if (exact >= 0) return exact;
    const prefix = spec.props.findIndex((p) => p.name.startsWith(key));
    return prefix >= 0 ? prefix : noHighlight;
  }
  let n = call.argIndex;
  if (bagIdx >= 0 && bagIdx < call.argIndex) n -= 1;
  const slot = slotForNthPositional(spec, n);
  if (!slot) return noHighlight;
  const index = spec.props.indexOf(slot);
  return index >= 0 ? index : noHighlight;
}
function resolveDeclaredParam(call, paramLabels) {
  if (paramLabels.length === 0) return 0;
  const names = paramLabels.map((label) => label.replace(/^\.\.\./, "").replace(/\?$/, ""));
  if (call.objectArg !== null) {
    const key = call.objectArg.activeKey;
    if (key) {
      const exact = names.indexOf(key);
      if (exact >= 0) return exact;
      const prefix = names.findIndex((name) => name.startsWith(key));
      if (prefix >= 0) return prefix;
    }
    return paramLabels.length;
  }
  return Math.min(call.argIndex, paramLabels.length - 1);
}
let cachedCatalog = null;
function catalogFor(library) {
  if (cachedCatalog && cachedCatalog.library === library) return cachedCatalog.byName;
  const byName = {};
  for (const entry of getComponentCatalog(library)) byName[entry.name] = entry;
  cachedCatalog = { library, byName };
  return byName;
}
function libraryComponentSignature(callee, library) {
  const entry = catalogFor(library)[callee];
  if (!entry) return null;
  return {
    label: entry.signature,
    documentation: entry.description,
    parameters: entry.params.map((p) => ({
      label: p.required ? p.name : `${p.name}?`,
      documentation: paramDoc(p.type, p.enumValues, p.description)
    }))
  };
}
function builtinSignature(callee) {
  if (!callee.startsWith("$")) return null;
  const builtin = findBuiltin(callee.slice(1));
  if (!builtin) return null;
  return { label: builtin.signature, documentation: builtin.summary, parameters: [] };
}
function memberCallSignature(callee, source) {
  const dot = callee.indexOf(".");
  if (dot < 0) return null;
  const root = callee.slice(0, dot);
  const memberPath = callee.slice(dot + 1);
  let member;
  if (root.startsWith("$") && isNamespaceName(root.slice(1))) {
    member = findNamespaceMember(root.slice(1), memberPath);
  } else if (root === "route" && !memberPath.includes(".")) {
    member = routeMembers.find((m) => m.name === memberPath);
  } else if (!memberPath.includes(".")) {
    const factory = factoryBindingFor$1(source, root);
    if (factory) member = findFactoryResource(factory)?.members.find((m) => m.name === memberPath);
  }
  if (!member) return null;
  return memberToSignature(callee, member);
}
function memberToSignature(callee, member) {
  const open = member.signature.indexOf("(");
  const close = member.signature.lastIndexOf(")");
  const params = open >= 0 && close > open ? member.signature.slice(open + 1, close).split(",").map((p) => p.trim()).filter((p) => p.length > 0).map((p) => ({ label: p })) : [];
  return { label: `${callee}${member.signature.slice(open >= 0 ? open : member.signature.length)}`, documentation: member.summary, parameters: params };
}
const FACTORY_ALTERNATION$1 = [...factoryResourceNames].join("|");
function factoryBindingFor$1(source, receiver) {
  if (!FACTORY_ALTERNATION$1) return void 0;
  const lhs = receiver.replace(/\$/g, "\\$");
  const re = new RegExp(`(?:^|[^\\w$.])${lhs}\\s*=\\s*(?:await\\s+)?\\$(${FACTORY_ALTERNATION$1})\\s*\\(`);
  const match = re.exec(source);
  return match ? match[1] : void 0;
}
function userSignature(callee, source) {
  let program;
  try {
    program = parse(source);
  } catch {
    return null;
  }
  for (const stmt of program.statements) {
    const matches = (stmt.kind === "ComponentDeclaration" || stmt.kind === "ActionDeclaration" || stmt.kind === "HookDeclaration") && stmt.name === callee;
    if (!matches) continue;
    const decl = stmt;
    const params = decl.params.map((p) => ({
      label: paramLabel(p)
    }));
    const label = `${callee}(${params.map((p) => p.label).join(", ")})`;
    return { label, documentation: `Declared in this file (${kindLabel(stmt.kind)}).`, parameters: params };
  }
  return null;
}
function paramLabel(p) {
  if (p.rest) return `...${p.name}`;
  return p.optional ? `${p.name}?` : p.name;
}
function kindLabel(kind) {
  if (kind === "ComponentDeclaration") return "component";
  if (kind === "ActionDeclaration") return "action";
  return "hook";
}
function paramDoc(type, enumValues, description) {
  const parts = [type];
  if (enumValues && enumValues.length > 0) {
    parts.push(`enum: ${enumValues.map((e) => `"${e}"`).join(" | ")}`);
  }
  const head = parts.join(" — ");
  return description ? `${head}

${description}` : head;
}
function analyseCallContext(source, position) {
  return findActiveCall(source, position);
}
function findActiveCall(source, position) {
  const offset = lineColumnToOffset$1(source, position);
  const stack = [];
  const currentArg = (frame) => {
    while (frame.args.length <= frame.commas) frame.args.push({ objectKeys: null });
    return frame.args[frame.commas];
  };
  const flushPendingKey = (obj, asShorthand) => {
    if (obj.pendingKey && asShorthand) obj.keys.push(obj.pendingKey);
    obj.pendingKey = "";
  };
  for (let i = 0; i < offset; i += 1) {
    const ch = source[i];
    if (ch === "/" && source[i + 1] === "/") {
      while (i < offset && source[i] !== "\n") i += 1;
      continue;
    }
    if (ch === "/" && source[i + 1] === "*") {
      i += 2;
      while (i < offset && !(source[i] === "*" && source[i + 1] === "/")) i += 1;
      i += 1;
      continue;
    }
    if (ch === '"' || ch === "'" || ch === "`") {
      i = skipString(source, i, ch, offset);
      continue;
    }
    const top = stack[stack.length - 1];
    if (ch === "(" || ch === "[" || ch === "{") {
      const callee = ch === "(" ? identifierBefore$1(source, i) : "";
      let argRecord = null;
      let object = null;
      if (ch === "{") {
        object = { keys: [], valueKey: null, expectingKey: true, pendingKey: "" };
        if (top && top.bracket === "(") {
          const arg = currentArg(top);
          if (arg.objectKeys === null) arg.objectKeys = object.keys;
          argRecord = arg;
        }
      }
      stack.push({ bracket: ch, callee, commas: 0, args: [], object, argRecord });
      continue;
    }
    if (ch === ")" || ch === "]" || ch === "}") {
      const closing = stack.pop();
      if (closing?.bracket === "{" && closing.object) {
        flushPendingKey(closing.object, closing.object.expectingKey);
      }
      continue;
    }
    if (!top) continue;
    if (ch === ",") {
      top.commas += 1;
      if (top.bracket === "(") currentArg(top);
      if (top.object) {
        flushPendingKey(top.object, top.object.expectingKey);
        top.object.expectingKey = true;
        top.object.valueKey = null;
      }
      continue;
    }
    if (top.object) {
      const obj = top.object;
      if (obj.expectingKey) {
        if (/[\w$]/.test(ch)) {
          obj.pendingKey += ch;
        } else if (ch === ":") {
          obj.keys.push(obj.pendingKey);
          obj.valueKey = obj.pendingKey;
          obj.pendingKey = "";
          obj.expectingKey = false;
        } else if (ch === "." && source.slice(i, i + 3) === "...") {
          obj.pendingKey = "";
          obj.expectingKey = false;
          i += 2;
        } else if (!/\s/.test(ch)) {
          obj.pendingKey = "";
        }
      }
      continue;
    }
  }
  for (let i = stack.length - 1; i >= 0; i -= 1) {
    const frame = stack[i];
    if (frame.bracket !== "(") continue;
    while (frame.args.length <= frame.commas) frame.args.push({ objectKeys: null });
    let objectArg = null;
    const inner = stack[i + 1];
    if (inner && inner.bracket === "{" && inner.object && inner.argRecord) {
      const obj = inner.object;
      objectArg = { activeKey: obj.expectingKey ? obj.pendingKey : obj.valueKey ?? "" };
    }
    return {
      callee: frame.callee,
      argIndex: frame.commas,
      args: frame.args.map((a) => ({ objectKeys: a.objectKeys })),
      objectArg
    };
  }
  return null;
}
function skipString(source, start, quote, limit) {
  let i = start + 1;
  while (i < limit) {
    const ch = source[i];
    if (ch === "\\") {
      i += 2;
      continue;
    }
    if (ch === quote) return i;
    i += 1;
  }
  return limit - 1;
}
function identifierBefore$1(source, parenIndex) {
  let end = parenIndex;
  while (end > 0 && /\s/.test(source[end - 1])) end -= 1;
  let start = end;
  while (start > 0 && /[\w$.]/.test(source[start - 1])) start -= 1;
  return source.slice(start, end);
}
function lineColumnToOffset$1(source, pos) {
  let line = 1;
  let col = 1;
  for (let i = 0; i < source.length; i += 1) {
    if (line === pos.line && col === pos.column) return i;
    if (source[i] === "\n") {
      line += 1;
      col = 1;
    } else {
      col += 1;
    }
  }
  return source.length;
}
const KEYWORDS = [
  { label: "function", detail: "Declare a component or action — either case works" },
  { label: "$effect", detail: "Reactive side-effect: $effect(() => { ... }, [deps])" },
  { label: "$router", detail: "pages = $router({ '/': Home(), default: NotFound() })" },
  { label: "switch", detail: "switch (value) { case …: …; break; default: … }" },
  { label: "for", detail: "for (let x of xs) { … }" },
  { label: "if", detail: "if (condition) { … } else { … }" },
  { label: "return", detail: "Return value from a component or action" },
  { label: "let", detail: "Declare a variable (reactive if $-prefixed)" },
  { label: "const", detail: "Declare a constant" },
  { label: "$emit", detail: "$emit('name', detail) — dispatch a CustomEvent" },
  { label: "cleanup", detail: "Register an effect teardown callback" }
];
function getDiagnostics(source, library) {
  const program = parse(source);
  const schemaErrors = validateProgramSchema(program, library);
  return [
    ...program.errors.map((e) => ({
      line: e.line,
      column: e.column,
      message: e.message,
      severity: "error"
    })),
    ...schemaErrors.map((e) => ({
      line: e.line,
      column: e.column,
      message: e.message,
      severity: "error"
    })),
    // Render-time lint warnings (issue #8). These don't block rendering but
    // flag silent footguns the schema validator can't see. Editors theme them
    // by `severity`. Kept narrow + precise to avoid false positives — the
    // worst silent bugs (#1 scope leak, #2 placeholder stripping, #3 Date
    // compares, #5 unicode escapes) are now fixed in the runtime, so linting
    // them would flag correct code.
    ...lintProgram(program, library)
  ];
}
function getLintWarnings(source, library) {
  return lintProgram(parse(source), library);
}
function lintProgram(program, library) {
  return [
    ...library ? lintUnknownComponents(program, library) : [],
    ...lintShadowedI18n(program),
    ...lintAwaitedValue(program)
  ];
}
function lintAwaitedValue(program) {
  const warnings = [];
  const seen = /* @__PURE__ */ new Set();
  const isAwait = (node) => {
    if (!node || typeof node !== "object" || Array.isArray(node)) return false;
    const rec = node;
    return rec["kind"] === "BuiltinCall" && rec["name"] === "__rui_await__";
  };
  const report = (node) => {
    const loc = node["loc"];
    const line = loc?.line ?? 0;
    const column = loc?.column ?? 0;
    const key = `${line}:${column}`;
    if (seen.has(key)) return;
    seen.add(key);
    warnings.push({
      line,
      column,
      severity: "warning",
      message: "The result of `await` is the PROMISE, not the resolved value — `await` parses but never suspends, so this is always truthy. Chain `.then(value => …)` instead, or use the resource's own `.onDone` callback."
    });
  };
  const visit = (node, skip) => {
    if (!node || typeof node !== "object") return;
    if (Array.isArray(node)) {
      for (const child of node) visit(child, skip);
      return;
    }
    const rec = node;
    if (isAwait(rec) && rec !== skip) {
      report(rec);
    }
    const nextSkip = rec["kind"] === "ExpressionStatement" ? rec["expression"] : skip;
    for (const key of Object.keys(rec)) {
      if (key === "loc") continue;
      visit(rec[key], nextSkip);
    }
  };
  visit(program.statements, null);
  return warnings;
}
function lintShadowedI18n(program) {
  const protectedNames = collectI18nBindingNames(program);
  if (protectedNames.size === 0) return [];
  const warnings = [];
  const seen = /* @__PURE__ */ new Set();
  const visit = (node) => {
    if (!node || typeof node !== "object") return;
    if (Array.isArray(node)) {
      for (const n of node) visit(n);
      return;
    }
    const rec = node;
    const loc = rec.loc;
    const flag = (name, what) => {
      if (typeof name !== "string" || !protectedNames.has(name)) return;
      const line = loc?.line ?? 0;
      const column = loc?.column ?? 0;
      const key = `${name}:${line}:${column}:${what}`;
      if (seen.has(key)) return;
      seen.add(key);
      warnings.push({
        line,
        column,
        severity: "warning",
        message: `${what} "${name}" shadows the i18n binding from $i18n(...). Inside this scope "${name}(...)" calls the ${what}, not the translator — rename it (e.g. "item").`
      });
    };
    switch (rec.kind) {
      case "Lambda":
      case "ComponentDeclaration":
      case "ActionDeclaration":
      case "HookDeclaration":
        for (const p of rec.params ?? []) flag(p.name, "parameter");
        break;
      case "ForOfStatement": {
        const pat = rec.pattern;
        if (pat) {
          for (const name of collectPatternNames(pat)) flag(name, "loop variable");
        } else {
          flag(rec.item, "loop variable");
        }
        break;
      }
      case "ForInStatement":
        flag(rec.item, "loop variable");
        break;
    }
    for (const key of Object.keys(rec)) {
      if (key !== "loc") visit(rec[key]);
    }
  };
  visit(program.statements);
  return warnings;
}
const JS_GLOBAL_CALLABLES = /* @__PURE__ */ new Set([
  // Type coercion / value constructors (called bare, without `new`)
  "Array",
  "BigInt",
  "Boolean",
  "Number",
  "Object",
  "String",
  "Symbol",
  // Namespaces occasionally called or shadow-checked
  "Date",
  "Error",
  "Function",
  "JSON",
  "Map",
  "Math",
  "Promise",
  "Proxy",
  "RangeError",
  "Reflect",
  "RegExp",
  "Set",
  "TypeError",
  "WeakMap",
  "WeakSet",
  // Web platform constructors reachable from an action body
  "AbortController",
  "Blob",
  "CustomEvent",
  "DOMParser",
  "Event",
  "EventSource",
  "File",
  "FormData",
  "Headers",
  "Image",
  "Intl",
  "IntersectionObserver",
  "MutationObserver",
  "Notification",
  "Request",
  "Response",
  "ResizeObserver",
  "TextDecoder",
  "TextEncoder",
  "URL",
  "URLSearchParams",
  "WebSocket",
  "Worker"
]);
function lintUnknownComponents(program, library) {
  const bound = collectBoundNames(program);
  const warnings = [];
  const seen = /* @__PURE__ */ new Set();
  const visit = (node) => {
    if (!node || typeof node !== "object") return;
    if (Array.isArray(node)) {
      for (const n of node) visit(n);
      return;
    }
    const rec = node;
    if (rec.kind === "Call" && typeof rec.callee === "string") {
      const name = rec.callee;
      if (/^[A-Z][A-Za-z0-9_]*$/.test(name) && !bound.has(name) && !JS_GLOBAL_CALLABLES.has(name) && !findComponent(library, name)) {
        const line = rec.loc?.line ?? 0;
        const column = rec.loc?.column ?? 0;
        const key = `${name}:${line}:${column}`;
        if (!seen.has(key)) {
          seen.add(key);
          const hint = suggestComponent(name, library, 3).filter((s) => s !== name);
          warnings.push({
            line,
            column,
            severity: "warning",
            message: `Unknown component <${name}> — it is not in the component library and this file does not declare or import it.` + (hint.length > 0 ? ` Did you mean ${hint.map((h) => `"${h}"`).join(" or ")}?` : "")
          });
        }
      }
    }
    for (const key of Object.keys(rec)) {
      if (key !== "loc") visit(rec[key]);
    }
  };
  visit(program.statements);
  return warnings;
}
function collectBoundNames(program) {
  const names = /* @__PURE__ */ new Set();
  const add = (name) => {
    if (typeof name === "string" && name.length > 0) names.add(name);
  };
  const addParams = (params) => {
    for (const p of params ?? []) {
      if (p.pattern) {
        for (const n of collectPatternNames(p.pattern)) add(n);
      } else {
        add(p.name);
      }
    }
  };
  const visit = (node) => {
    if (!node || typeof node !== "object") return;
    if (Array.isArray(node)) {
      for (const n of node) visit(n);
      return;
    }
    const rec = node;
    switch (rec.kind) {
      case "ComponentDeclaration":
      case "ActionDeclaration":
      case "HookDeclaration":
        add(rec.name);
        addParams(rec.params);
        break;
      case "Lambda":
        addParams(rec.params);
        break;
      case "Import":
        for (const s of rec.specifiers ?? []) add(s.local);
        break;
      case "Assignment":
        add(rec.identifier);
        break;
      case "DestructureStatement":
        for (const n of collectPatternNames({
          kind: rec.patternKind ?? "object",
          bindings: rec.bindings ?? []
        })) {
          add(n);
        }
        break;
      case "ForOfStatement": {
        const pattern = rec.pattern;
        if (pattern) {
          for (const n of collectPatternNames(pattern)) add(n);
        } else {
          add(rec.item);
        }
        break;
      }
      case "ForInStatement":
        add(rec.item);
        break;
      case "TryStatement":
        add(rec.catchParam);
        break;
    }
    for (const key of Object.keys(rec)) {
      if (key !== "loc") visit(rec[key]);
    }
  };
  visit(program.statements);
  return names;
}
function collectI18nBindingNames(program) {
  const names = /* @__PURE__ */ new Set();
  const isI18nCall = (expr) => {
    if (!expr || typeof expr !== "object") return false;
    const e = expr;
    if (e.kind === "Invoke") {
      const callee = e.callee;
      return callee?.kind === "StateRef" && callee?.name === "i18n";
    }
    return e.kind === "BuiltinCall" && e.name === "i18n";
  };
  const visit = (node) => {
    if (!node || typeof node !== "object") return;
    if (Array.isArray(node)) {
      for (const n of node) visit(n);
      return;
    }
    const rec = node;
    if (rec.kind === "DestructureStatement" && rec.patternKind === "object" && isI18nCall(rec.expression)) {
      for (const b of rec.bindings ?? []) {
        if (typeof b.name === "string" && b.name) names.add(b.name);
      }
    }
    for (const key of Object.keys(rec)) visit(rec[key]);
  };
  visit(program.statements);
  return names;
}
function getCompletions(source, position, library) {
  const ctx = analyseCursor(source, position);
  const user = collectUserSymbols(source);
  if (ctx.afterDollar) {
    return [
      ...user.atoms.map((name) => ({
        label: `$${name}`,
        kind: "state",
        detail: "Reactive atom declared in this file"
      })),
      { label: "$name = value", kind: "state", detail: "Declare or assign a reactive atom" },
      ...builtinCatalog.map((b) => ({
        label: b.signature,
        kind: "builtin",
        detail: b.summary
      }))
    ];
  }
  const member = analyseMemberAccess(source, position);
  if (member) {
    const items = memberCompletionsFor(member.objectExpr, source);
    if (items) return items;
  }
  const general = generalCompletions(library, user);
  const call = analyseCallContext(source, position);
  if (ctx.objectCallee) {
    const spec = findComponent(library, ctx.objectCallee);
    if (spec) {
      const isNamedBag = !call || call.objectArg === null || chooseNamedBagIndex(call.args, spec) === call.argIndex;
      if (isNamedBag) return [...propCompletions(spec), ...general];
    }
    if (ctx.objectCallee.startsWith("$")) {
      const config = findBuiltinConfig(ctx.objectCallee.slice(1));
      if (config) return [...configKeyCompletions(config, ctx.objectCallee), ...general];
    }
    return general;
  }
  if (call?.callee && call.objectArg === null) {
    const spec = findComponent(library, call.callee);
    if (spec) {
      const bagIdx = chooseNamedBagIndex(call.args, spec);
      if (bagIdx !== call.argIndex) {
        let n = call.argIndex;
        if (bagIdx >= 0 && bagIdx < call.argIndex) n -= 1;
        const slot = slotForNthPositional(spec, n);
        if (slot?.enum && slot.enum.length > 0) {
          const prefix = source.slice(0, lineColumnToOffset(source, position));
          const inString = /["']([\w-]*)$/.test(prefix);
          const values = slot.enum.map((value) => ({
            label: inString ? value : `"${value}"`,
            kind: "prop",
            detail: `${slot.name} value (${call.callee})`,
            documentation: slot.description
          }));
          return [...values, ...general];
        }
      }
    }
  }
  return general;
}
function generalCompletions(library, user) {
  const keywordItems = /* @__PURE__ */ new Map();
  for (const [label, doc] of Object.entries(keywordDocs)) {
    keywordItems.set(label, {
      label,
      kind: "keyword",
      detail: doc.summary,
      documentation: `${doc.syntax}

${doc.example}`
    });
  }
  for (const k of KEYWORDS) {
    if (!keywordItems.has(k.label)) {
      keywordItems.set(k.label, { label: k.label, kind: "keyword", detail: k.detail });
    }
  }
  return [
    // Author-declared symbols first so they rank above the large library
    // list in editors that preserve provider order.
    ...user.components.map((name) => ({
      label: name,
      kind: "component",
      detail: "Component declared in this file"
    })),
    ...user.actions.map((name) => ({
      label: name,
      kind: "builtin",
      detail: "Action declared in this file"
    })),
    ...user.atoms.map((name) => ({
      label: `$${name}`,
      kind: "state",
      detail: "Reactive atom declared in this file"
    })),
    ...keywordItems.values(),
    ...library.components.map((c) => ({
      label: c.name,
      kind: "component",
      detail: signaturePreview(c),
      documentation: c.description
    }))
  ];
}
function collectUserSymbols(source) {
  const atoms = /* @__PURE__ */ new Set();
  const components2 = /* @__PURE__ */ new Set();
  const actions = /* @__PURE__ */ new Set();
  try {
    const program = parse(source);
    for (const stmt of program.statements) {
      if (stmt.kind === "Assignment" && stmt.isState && stmt.identifier) {
        atoms.add(stmt.identifier);
      } else if (stmt.kind === "ComponentDeclaration" && stmt.name) {
        components2.add(stmt.name);
      } else if (stmt.kind === "ActionDeclaration" && stmt.name) {
        actions.add(stmt.name);
      }
    }
  } catch {
  }
  return { atoms: [...atoms], components: [...components2], actions: [...actions] };
}
const MEMBER_ACCESS_RE = /(\$?[A-Za-z_]\w*(?:\.[A-Za-z_]\w*)*)\.([A-Za-z_]\w*)?$/;
function analyseMemberAccess(source, position) {
  const prefix = source.slice(0, lineColumnToOffset(source, position));
  const match = MEMBER_ACCESS_RE.exec(prefix);
  if (!match) return null;
  return { objectExpr: match[1], typing: match[2] ?? "" };
}
function memberKind(m) {
  return m.kind === "method" ? "builtin" : "prop";
}
function toMemberCompletion(m, detail) {
  return {
    label: m.name,
    kind: memberKind(m),
    detail: `${detail} · ${m.signature}`,
    documentation: m.summary
  };
}
function configKeyCompletions(keys, callee) {
  return keys.map((k) => ({
    label: k.name,
    kind: "prop",
    detail: `${callee} config · ${k.name}: ${k.type}`,
    documentation: k.summary
  }));
}
function memberCompletionsFor(objectExpr, source) {
  const segments = objectExpr.split(".");
  const root = segments[0];
  if (root.startsWith("$")) {
    const bare = root.slice(1);
    if (isNamespaceName(bare)) {
      const members = namespaceMembersAt(bare, segments.slice(1));
      if (members.length > 0) {
        return members.map((m) => toMemberCompletion(m, root));
      }
      return [];
    }
  }
  if (segments.length === 1) {
    if (root === "route") return routeMembers.map((m) => toMemberCompletion(m, "route"));
    const factory = factoryBindingFor(source, root);
    if (factory) {
      const entry = findFactoryResource(factory);
      if (entry) return entry.members.map((m) => toMemberCompletion(m, root));
    }
  }
  return null;
}
const FACTORY_ALTERNATION = [...factoryResourceNames].join("|");
function factoryBindingFor(source, receiver) {
  if (!FACTORY_ALTERNATION) return void 0;
  const lhs = receiver.replace(/\$/g, "\\$");
  const re = new RegExp(`(?:^|[^\\w$.])${lhs}\\s*=\\s*(?:await\\s+)?\\$(${FACTORY_ALTERNATION})\\s*\\(`);
  const match = re.exec(source);
  return match ? match[1] : void 0;
}
function memberHoverAt(source, position, word) {
  const offset = lineColumnToOffset(source, position);
  let end = offset;
  while (end < source.length && /[\w$]/.test(source[end])) end += 1;
  const prefix = source.slice(0, end);
  const match = MEMBER_ACCESS_RE.exec(prefix);
  if (!match || (match[2] ?? "") !== word) return null;
  const objectExpr = match[1];
  const segments = objectExpr.split(".");
  const root = segments[0];
  if (root.startsWith("$")) {
    const bare = root.slice(1);
    if (isNamespaceName(bare)) {
      const path = [...segments.slice(1), word].join(".");
      const m = findNamespaceMember(bare, path);
      if (m) return memberHover(`${root}.${path}`, m);
      return null;
    }
    const factory = factoryBindingFor(source, root);
    if (factory && segments.length === 1) {
      const entry = findFactoryResource(factory);
      const m = entry?.members.find((x) => x.name === word);
      if (m) return memberHover(`${root}.${word}`, m);
    }
    return null;
  }
  if (segments.length === 1) {
    if (root === "route") {
      const m = routeMembers.find((x) => x.name === word);
      if (m) return memberHover(`route.${word}`, m);
    }
    const factory = factoryBindingFor(source, root);
    if (factory) {
      const entry = findFactoryResource(factory);
      const m = entry?.members.find((x) => x.name === word);
      if (m) return memberHover(`${root}.${word}`, m);
    }
  }
  return null;
}
function memberHover(qualified, m) {
  return {
    kind: m.kind === "method" ? "builtin" : "prop",
    contents: `**${qualified}** — ${m.summary}

Signature: \`${m.signature}\``
  };
}
function configKeyHoverAt(source, position, word) {
  const ctx = analyseCursor(source, position);
  if (!ctx.objectCallee || !ctx.objectCallee.startsWith("$")) return null;
  const keys = findBuiltinConfig(ctx.objectCallee.slice(1));
  const key = keys?.find((k) => k.name === word);
  if (!key) return null;
  return {
    kind: "prop",
    contents: `**${ctx.objectCallee} config · ${key.name}** — ${key.summary}

Type: \`${key.type}\``
  };
}
function getHoverInfo(source, position, library) {
  const word = wordAt(source, position);
  if (!word) return null;
  const memberHover2 = memberHoverAt(source, position, word);
  if (memberHover2) return memberHover2;
  const spec = findComponent(library, word);
  if (spec) {
    return {
      kind: "component",
      contents: `**${spec.name}** — ${spec.description ?? "Component."}

Signature: \`${signaturePreview(spec)}\``
    };
  }
  const configHover = configKeyHoverAt(source, position, word);
  if (configHover) return configHover;
  if (word.startsWith("$")) {
    const builtin = findBuiltin(word.slice(1));
    if (builtin) {
      return {
        kind: "builtin",
        contents: `**${builtin.sigil}** — ${builtin.summary}

Signature: \`${builtin.signature}\``
      };
    }
    return { kind: "state", contents: `**${word}** — reactive state atom` };
  }
  const doc = keywordDocs[word];
  if (doc) {
    return { kind: "unknown", contents: formatKeywordHover(word, doc) };
  }
  const kw = KEYWORDS.find((k) => k.label === word);
  if (kw) {
    return { kind: "unknown", contents: `**${word}** — ${kw.detail}` };
  }
  return null;
}
function formatKeywordHover(word, doc) {
  return `**${word}** — ${doc.summary}

**Syntax**

\`\`\`js
${doc.syntax}
\`\`\`

**Example**

\`\`\`js
${doc.example}
\`\`\``;
}
function analyseCursor(source, position) {
  const offset = lineColumnToOffset(source, position);
  const prefix = source.slice(0, offset);
  const afterDollar = /\$[A-Za-z_]*$/.test(prefix);
  const stack = [];
  for (let i = 0; i < prefix.length; i += 1) {
    const ch = prefix[i];
    if (ch === "/" && prefix[i + 1] === "/") {
      while (i < prefix.length && prefix[i] !== "\n") i += 1;
      continue;
    }
    if (ch === "/" && prefix[i + 1] === "*") {
      i += 2;
      while (i < prefix.length && !(prefix[i] === "*" && prefix[i + 1] === "/")) i += 1;
      i += 1;
      continue;
    }
    if (ch === '"' || ch === "'" || ch === "`") {
      i = skipStringLiteral(prefix, i, ch);
      continue;
    }
    if (ch === "(" || ch === "[" || ch === "{") {
      stack.push({ bracket: ch, callee: ch === "(" ? identifierBefore(prefix, i) : "" });
    } else if (ch === ")" || ch === "]" || ch === "}") {
      stack.pop();
    }
  }
  let objectCallee;
  const top = stack[stack.length - 1];
  if (top && top.bracket === "{") {
    for (let i = stack.length - 1; i >= 0; i -= 1) {
      if (stack[i].bracket === "(") {
        const callee = stack[i].callee;
        if (callee) objectCallee = callee;
        break;
      }
    }
  }
  return { objectCallee, afterDollar };
}
function skipStringLiteral(source, start, quote) {
  let i = start + 1;
  while (i < source.length) {
    const ch = source[i];
    if (ch === "\\") {
      i += 2;
      continue;
    }
    if (ch === quote) return i;
    i += 1;
  }
  return source.length - 1;
}
function identifierBefore(source, openIndex) {
  let end = openIndex;
  while (end > 0 && /\s/.test(source[end - 1])) end -= 1;
  let start = end;
  while (start > 0 && /[\w$]/.test(source[start - 1])) start -= 1;
  return source.slice(start, end);
}
function propCompletions(spec) {
  const positional = findPositionalProp(spec);
  return spec.props.map((prop2) => ({
    label: `${prop2.name}:`,
    kind: "prop",
    detail: propDetail(prop2, positional?.name === prop2.name),
    documentation: prop2.description
  }));
}
function propDetail(prop2, isPositional) {
  const tags = [];
  if (isPositional) tags.push("positional");
  if (prop2.required) tags.push("required");
  if (prop2.enum) tags.push(`enum: ${prop2.enum.map((e) => `"${e}"`).join(" | ")}`);
  const tagText = tags.length > 0 ? ` (${tags.join(", ")})` : "";
  return `${prop2.type}${tagText}`;
}
function signaturePreview(spec) {
  const positional = findPositionalProp(spec);
  const positionalLabel = positional ? positional.name : "";
  const rest = spec.props.filter((p) => p.name !== positional?.name).map((p) => `${p.name}${p.optional || !p.required ? "?" : ""}: ${p.type}`).join(", ");
  if (positionalLabel === "" && rest === "") return `${spec.name}()`;
  if (positionalLabel === "") return `${spec.name}(${rest})`;
  if (rest === "") return `${spec.name}(${positionalLabel})`;
  return `${spec.name}(${positionalLabel}, ${rest})`;
}
function lineColumnToOffset(source, pos) {
  let line = 1;
  let col = 1;
  for (let i = 0; i < source.length; i += 1) {
    if (line === pos.line && col === pos.column) return i;
    if (source[i] === "\n") {
      line += 1;
      col = 1;
    } else {
      col += 1;
    }
  }
  return source.length;
}
function wordAt(source, pos) {
  const offset = lineColumnToOffset(source, pos);
  const isWord = (c) => /[\w$]/.test(c);
  let start = offset;
  while (start > 0 && isWord(source[start - 1])) start -= 1;
  let end = offset;
  while (end < source.length && isWord(source[end])) end += 1;
  if (start === end) return null;
  return source.slice(start, end);
}
function getDefinition(source, position) {
  const tokens = tokenize(source);
  const tok = findNameTokenAt(tokens, position);
  if (!tok) return null;
  const sym = resolveSymbol(tokens, tok);
  return sym ? sym.range : null;
}
function getDefinitionTarget(source, position) {
  const tokens = tokenize(source);
  const imports = parseImports(tokens);
  for (const clause of imports) {
    if (clause.sourceRange && positionInRange(position, clause.sourceRange)) {
      return { kind: "module", moduleSource: clause.source };
    }
  }
  const tok = findNameTokenAt(tokens, position);
  if (!tok) return null;
  const wantsState = tok.type === "StateIdentifier";
  for (const clause of imports) {
    for (const spec of clause.specifiers) {
      if (spec.local === tok.value && spec.isState === wantsState) {
        return {
          kind: "import-binding",
          imported: spec.imported,
          isState: spec.isState,
          moduleSource: clause.source
        };
      }
    }
  }
  const sym = resolveSymbol(tokens, tok);
  if (sym && sym.kind !== "import") return { kind: "local", range: sym.range };
  return null;
}
function findDeclaration(source, name, isState) {
  const table = collectSymbols(tokenize(source));
  const decl = isState ? table.state.get(name) : table.ident.get(name);
  if (!decl || decl.kind === "import") return null;
  return decl.range;
}
function getReferences(source, position, options = {}) {
  const includeDeclaration = options.includeDeclaration ?? true;
  const tokens = tokenize(source);
  const tok = findNameTokenAt(tokens, position);
  if (!tok) return [];
  const refs = occurrences(tokens, tok.type, tok.value);
  if (includeDeclaration) return refs;
  const sym = resolveSymbol(tokens, tok);
  if (!sym) return refs;
  return refs.filter((r) => !rangesEqual(r, sym.range));
}
function getDocumentHighlights(source, position) {
  return getReferences(source, position, { includeDeclaration: true });
}
function getRenameEdits(source, position, newName) {
  const tokens = tokenize(source);
  const tok = findNameTokenAt(tokens, position);
  if (!tok) return { edits: [], error: "No symbol to rename at this position." };
  const sym = resolveSymbol(tokens, tok);
  if (!sym) {
    return {
      edits: [],
      error: "Only file-scoped components, actions, hooks, and reactive atoms can be renamed."
    };
  }
  const bare = sym.sigil ? newName.replace(/^\$/, "") : newName;
  if (!/^[A-Za-z_][A-Za-z0-9_]*$/.test(bare)) {
    return { edits: [], error: `"${newName}" is not a valid Aktion identifier.` };
  }
  const replacement = sym.sigil ? `$${bare}` : bare;
  const edits = occurrences(tokens, tok.type, tok.value).map((range) => ({
    range,
    newText: replacement
  }));
  return { edits };
}
function getDocumentSymbols(source) {
  const tokens = tokenize(source);
  const table = collectSymbols(tokens);
  return table.all.map((decl) => ({
    name: decl.sigil ? `$${decl.name}` : decl.name,
    detail: detailFor(decl.kind),
    kind: decl.kind,
    range: decl.range,
    selectionRange: decl.range
  }));
}
function resolveSymbol(tokens, tok) {
  const table = collectSymbols(tokens);
  if (tok.type === "StateIdentifier") return table.state.get(tok.value) ?? null;
  return table.ident.get(tok.value) ?? null;
}
function collectSymbols(tokens) {
  const table = { state: /* @__PURE__ */ new Map(), ident: /* @__PURE__ */ new Map(), all: [] };
  const add = (decl) => {
    const bucket = decl.sigil ? table.state : table.ident;
    if (bucket.has(decl.name)) return;
    bucket.set(decl.name, decl);
    table.all.push(decl);
  };
  let depth = 0;
  let stmtStart = true;
  for (let i = 0; i < tokens.length; i += 1) {
    const t = tokens[i];
    if (t.type === "Newline" || t.type === "Semicolon") {
      stmtStart = true;
      continue;
    }
    if (t.type === "EOF") break;
    if (t.type === "Keyword" && t.value === "import") {
      i = scanImport(tokens, i, add);
      stmtStart = true;
      continue;
    }
    if (t.type === "Keyword" && t.value === "function") {
      const nameTok = nextMeaningful$1(tokens, i + 1);
      if (nameTok) {
        if (nameTok.type === "StateIdentifier") {
          add({ name: nameTok.value, kind: "hook", sigil: true, range: tokenRange(nameTok) });
        } else if (nameTok.type === "Identifier") {
          add({
            name: nameTok.value,
            kind: isPascalCase(nameTok.value) ? "component" : "action",
            sigil: false,
            range: tokenRange(nameTok)
          });
        }
      }
    }
    if (t.type === "StateIdentifier" && stmtStart && depth === 0) {
      const next = nextMeaningful$1(tokens, i + 1);
      if (next && next.type === "Operator" && next.value === "=") {
        add({ name: t.value, kind: "state", sigil: true, range: tokenRange(t) });
      }
    }
    if (t.type === "Punctuation" && t.value === "{") {
      depth += 1;
      stmtStart = true;
    } else if (t.type === "Punctuation" && t.value === "}") {
      depth = Math.max(0, depth - 1);
      stmtStart = true;
    } else if (t.type === "Keyword" && t.value === "export") ;
    else {
      stmtStart = false;
    }
  }
  return table;
}
function scanImport(tokens, start, add) {
  const { clause, end } = readImportClause(tokens, start);
  for (const spec of clause.specifiers) {
    add({ name: spec.local, kind: "import", sigil: spec.isState, range: spec.localRange });
  }
  return end;
}
function parseImports(tokens) {
  const clauses = [];
  for (let i = 0; i < tokens.length; i += 1) {
    if (tokens[i].type === "Keyword" && tokens[i].value === "import") {
      const { clause, end } = readImportClause(tokens, i);
      clauses.push(clause);
      i = end;
    }
  }
  return clauses;
}
function readImportClause(tokens, start) {
  const names = [];
  let source = "";
  let sourceRange = null;
  let i = start + 1;
  for (; i < tokens.length; i += 1) {
    const t = tokens[i];
    if (t.type === "Newline" || t.type === "Semicolon" || t.type === "EOF") break;
    if (t.type === "Identifier" && t.value === "from") {
      const src = nextMeaningful$1(tokens, i + 1);
      if (src && src.type === "String") {
        source = src.value;
        sourceRange = {
          start: { line: src.line, column: src.column },
          end: { line: src.line, column: src.column + src.value.length + 2 }
        };
      }
      break;
    }
    if (t.type === "Punctuation") continue;
    if (t.type === "Identifier" || t.type === "StateIdentifier") names.push(t);
  }
  const specifiers = [];
  for (let n = 0; n < names.length; n += 1) {
    const nameTok = names[n];
    const asTok = names[n + 1];
    if (asTok && asTok.type === "Identifier" && asTok.value === "as" && names[n + 2]) {
      const aliasTok = names[n + 2];
      specifiers.push({
        imported: nameTok.value,
        local: aliasTok.value,
        isState: aliasTok.type === "StateIdentifier",
        localRange: tokenRange(aliasTok)
      });
      n += 2;
    } else {
      specifiers.push({
        imported: nameTok.value,
        local: nameTok.value,
        isState: nameTok.type === "StateIdentifier",
        localRange: tokenRange(nameTok)
      });
    }
  }
  return { clause: { source, sourceRange, specifiers }, end: i - 1 };
}
function positionInRange(position, range) {
  if (position.line !== range.start.line || position.line !== range.end.line) return false;
  return position.column >= range.start.column && position.column <= range.end.column;
}
function occurrences(tokens, type, value) {
  const out = [];
  for (const t of tokens) {
    if (t.type === type && t.value === value) out.push(tokenRange(t));
  }
  return out;
}
function findNameTokenAt(tokens, position) {
  let best = null;
  for (const t of tokens) {
    if (t.type !== "Identifier" && t.type !== "StateIdentifier") continue;
    if (t.line !== position.line) continue;
    const startCol = t.column;
    const endCol = t.column + tokenTextLength(t);
    if (position.column >= startCol && position.column <= endCol) {
      if (!best || t.column > best.column) best = t;
    }
  }
  return best;
}
function nextMeaningful$1(tokens, from) {
  for (let i = from; i < tokens.length; i += 1) {
    const t = tokens[i];
    if (t.type === "Newline") continue;
    return t;
  }
  return null;
}
function tokenTextLength(t) {
  return t.type === "StateIdentifier" ? t.value.length + 1 : t.value.length;
}
function tokenRange(t) {
  const length = tokenTextLength(t);
  return {
    start: { line: t.line, column: t.column },
    end: { line: t.line, column: t.column + length }
  };
}
function rangesEqual(a, b) {
  return a.start.line === b.start.line && a.start.column === b.start.column && a.end.line === b.end.line && a.end.column === b.end.column;
}
function isPascalCase(name) {
  return /^[A-Z]/.test(name);
}
function detailFor(kind) {
  switch (kind) {
    case "component":
      return "component";
    case "action":
      return "action";
    case "hook":
      return "hook";
    case "state":
      return "reactive atom";
    case "import":
      return "import";
    default:
      return "";
  }
}
const semanticTokenTypes = [
  "namespace",
  "class",
  "function",
  "variable",
  "property",
  "keyword",
  "number"
];
const semanticTokenModifiers = ["declaration", "defaultLibrary"];
const HANDLE_KEYWORDS = /* @__PURE__ */ new Set(["route", "aktion"]);
function getSemanticTokens(source, library = defaultLibrary) {
  const tokens = tokenize(source);
  const components2 = new Set(library.components.map((c) => c.name));
  const userComponents = /* @__PURE__ */ new Set();
  const userActions = /* @__PURE__ */ new Set();
  const hooks = /* @__PURE__ */ new Set();
  collectUserDeclarations(tokens, userComponents, userActions, hooks);
  const factories = collectFactoryBindings(tokens);
  const out = [];
  let prev;
  let chain = null;
  let dotPending = false;
  const brackets = [];
  const emit = (t, c) => {
    out.push({
      line: t.line,
      column: t.column,
      length: tokenLength(t),
      tokenType: c.type,
      tokenModifiers: c.modifiers
    });
  };
  for (let i = 0; i < tokens.length; i += 1) {
    const t = tokens[i];
    if (t.type === "Newline") continue;
    if (t.type === "Punctuation" && (t.value === "(" || t.value === "[" || t.value === "{")) {
      brackets.push({ bracket: t.value, isObject: t.value === "{" && isValuePosition(prev) });
    } else if (t.type === "Punctuation" && (t.value === ")" || t.value === "]" || t.value === "}")) {
      brackets.pop();
    }
    if (isMemberAccess(t)) {
      dotPending = chain !== null;
      prev = t;
      continue;
    }
    if (dotPending && chain && t.type === "Identifier") {
      const resolved = resolveMember(chain, t.value);
      emit(t, resolved.token);
      chain = resolved.next;
      dotPending = false;
      prev = t;
      continue;
    }
    dotPending = false;
    if (t.type === "Identifier" && brackets[brackets.length - 1]?.isObject && isEntryStart(prev) && nextMeaningful(tokens, i + 1)?.value === ":") {
      emit(t, { type: "property", modifiers: [] });
      chain = null;
      prev = t;
      continue;
    }
    const classified = classify(t, prev, { components: components2, userComponents, userActions, hooks });
    if (classified) emit(t, classified);
    chain = chainRootFor(t, factories);
    prev = t;
  }
  return out;
}
function isValuePosition(prev) {
  if (!prev) return false;
  if (prev.type === "Operator") return true;
  if (prev.type === "Keyword") return prev.value === "return";
  if (prev.type === "Punctuation") {
    return prev.value === "(" || prev.value === "[" || prev.value === "," || prev.value === ":" || prev.value === "?";
  }
  return false;
}
function isEntryStart(prev) {
  return Boolean(prev && prev.type === "Punctuation" && (prev.value === "{" || prev.value === ","));
}
function chainRootFor(t, factories) {
  if (t.type === "StateIdentifier") {
    if (isNamespaceName(t.value)) return { kind: "namespace", ns: t.value, path: [] };
    const factory = factories.state.get(t.value);
    if (factory) {
      const entry = findFactoryResource(factory);
      if (entry) return { kind: "factory", members: entry.members };
    }
    return null;
  }
  if (t.type === "Identifier") {
    if (t.value === "route") return { kind: "route" };
    const factory = factories.ident.get(t.value);
    if (factory) {
      const entry = findFactoryResource(factory);
      if (entry) return { kind: "factory", members: entry.members };
    }
  }
  return null;
}
function resolveMember(chain, name) {
  const asProperty = { type: "property", modifiers: [] };
  if (!chain) return { token: asProperty, next: null };
  if (chain.kind === "namespace") {
    const ns = findNamespace(chain.ns);
    const path = [...chain.path, name].join(".");
    const member2 = ns?.members.find((m) => m.name === path);
    if (member2) {
      if (member2.kind === "namespace") {
        return {
          token: { type: "namespace", modifiers: ["defaultLibrary"] },
          next: { kind: "namespace", ns: chain.ns, path: [...chain.path, name] }
        };
      }
      return {
        token: {
          type: member2.kind === "method" ? "function" : "property",
          modifiers: ["defaultLibrary"]
        },
        next: null
      };
    }
    return { token: asProperty, next: null };
  }
  const members = chain.kind === "factory" ? chain.members : routeMembers;
  const member = members.find((m) => m.name === name);
  if (member) {
    return {
      token: { type: member.kind === "method" ? "function" : "property", modifiers: [] },
      next: null
    };
  }
  return { token: asProperty, next: null };
}
function collectFactoryBindings(tokens) {
  const state = /* @__PURE__ */ new Map();
  const ident = /* @__PURE__ */ new Map();
  for (let i = 0; i < tokens.length; i += 1) {
    const t = tokens[i];
    if (!(t.type === "Operator" && t.value === "=")) continue;
    const receiver = prevMeaningful(tokens, i - 1);
    if (!receiver) continue;
    let j = i + 1;
    let rhs = nextMeaningful(tokens, j);
    if (rhs && rhs.type === "Keyword" && rhs.value === "await") {
      j = tokens.indexOf(rhs) + 1;
      rhs = nextMeaningful(tokens, j);
    }
    if (!rhs || rhs.type !== "StateIdentifier" || !factoryResourceNames.has(rhs.value)) continue;
    if (receiver.type === "StateIdentifier") state.set(receiver.value, rhs.value);
    else if (receiver.type === "Identifier") ident.set(receiver.value, rhs.value);
  }
  return { state, ident };
}
function prevMeaningful(tokens, from) {
  for (let i = from; i >= 0; i -= 1) {
    if (tokens[i].type !== "Newline") return tokens[i];
  }
  return void 0;
}
function classify(t, prev, scope) {
  if (t.type === "Keyword") return { type: "keyword", modifiers: [] };
  if (t.type === "Number") return { type: "number", modifiers: [] };
  if (t.type === "StateIdentifier") {
    const builtin = findBuiltin(t.value);
    if (builtin) {
      return {
        type: builtin.namespace ? "namespace" : "function",
        modifiers: ["defaultLibrary"]
      };
    }
    if (scope.hooks.has(t.value)) return { type: "function", modifiers: [] };
    return { type: "variable", modifiers: [] };
  }
  if (t.type === "Identifier") {
    if (HANDLE_KEYWORDS.has(t.value)) return { type: "keyword", modifiers: [] };
    if (isMemberAccess(prev)) return { type: "property", modifiers: [] };
    if (scope.components.has(t.value)) {
      return { type: "class", modifiers: ["defaultLibrary"] };
    }
    if (scope.userComponents.has(t.value)) return { type: "class", modifiers: [] };
    if (scope.userActions.has(t.value)) return { type: "function", modifiers: [] };
    if (/^[A-Z]/.test(t.value)) return { type: "class", modifiers: [] };
    return null;
  }
  return null;
}
function isMemberAccess(prev) {
  if (!prev) return false;
  return prev.type === "Punctuation" && prev.value === "." || prev.type === "Operator" && prev.value === "?.";
}
function collectUserDeclarations(tokens, components2, actions, hooks) {
  for (let i = 0; i < tokens.length; i += 1) {
    const t = tokens[i];
    if (t.type === "Keyword" && t.value === "function") {
      const next = nextMeaningful(tokens, i + 1);
      if (!next) continue;
      if (next.type === "StateIdentifier") hooks.add(next.value);
      else if (next.type === "Identifier") {
        if (/^[A-Z]/.test(next.value)) components2.add(next.value);
        else actions.add(next.value);
      }
    }
  }
}
function nextMeaningful(tokens, from) {
  for (let i = from; i < tokens.length; i += 1) {
    const t = tokens[i];
    if (t.type === "Newline") continue;
    return t;
  }
  return null;
}
function tokenLength(t) {
  return t.type === "StateIdentifier" ? t.value.length + 1 : t.value.length;
}
const baseFonts = {
  fontFamily: "system-ui, -apple-system, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif",
  fontFamilyHeading: "system-ui, -apple-system, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif",
  fontFamilyMono: "ui-monospace, SFMono-Regular, Menlo, Consolas, monospace"
};
const baseFontScale = {
  fontSizeBase: "14px",
  fontSizeSm: "12px",
  fontSizeLg: "16px",
  fontSizeHeading: "16px",
  fontSizeTitle: "22px",
  fontWeightBody: "400",
  fontWeightHeading: "700",
  lineHeightBody: "1.5",
  lineHeightHeading: "1.2",
  letterSpacingHeading: "0",
  headingTextTransform: "none"
};
const baseSpacing = {
  spacingXs: "4px",
  spacingS: "8px",
  spacingM: "12px",
  spacingL: "20px",
  spacingXl: "32px",
  spacing2xl: "48px",
  spacing3xl: "80px"
};
const baseGradients = {
  gradientBrand: "linear-gradient(120deg, #6366f1 0%, #8b5cf6 50%, #ec4899 100%)",
  gradientAccent: "linear-gradient(120deg, #22d3ee 0%, #6366f1 100%)",
  gradientWarm: "linear-gradient(120deg, #f59e0b 0%, #ec4899 100%)",
  gradientCool: "linear-gradient(120deg, #3b82f6 0%, #06b6d4 100%)",
  gradientSuccess: "linear-gradient(120deg, #10b981 0%, #22d3ee 100%)",
  gradientDanger: "linear-gradient(120deg, #f43f5e 0%, #ec4899 100%)"
};
const baseRadii = {
  radiusXs: "4px",
  radiusSm: "6px",
  radiusMd: "10px",
  radiusLg: "16px",
  radiusPill: "999px",
  radiusButton: "6px",
  radiusInput: "6px"
};
const baseButtons = {
  buttonFontWeight: "600",
  buttonTextTransform: "none",
  buttonLetterSpacing: "0",
  buttonPaddingY: "8px",
  buttonPaddingX: "14px"
};
const baseMotion = {
  transitionDuration: "120ms"
};
const lightTheme = {
  colorBg: "#ffffff",
  colorBgSubtle: "#f8fafc",
  colorSurface: "#ffffff",
  colorSurfaceMuted: "#f1f5f9",
  colorBorder: "#e2e8f0",
  colorBorderControl: "#767f8c",
  colorBorderSubtle: "rgba(15, 23, 42, 0.08)",
  colorText: "#0f172a",
  colorTextMuted: "#475569",
  // Was #6366f1 (indigo-500), which measures 4.47:1 on this theme's white
  // surface — just under the 4.5:1 body-text minimum, and symmetric, so it
  // failed BOTH as text (125 rules paint `color: var(--rui-color-primary)`) and
  // for the #ffffff label on a primary button. #4f46e5 is indigo-600, the value
  // this theme already used for `colorPrimaryHover`, so the brand hue is
  // unchanged; hover steps down to indigo-700. #4f46e5 on white = 6.29:1.
  colorPrimary: "#4f46e5",
  colorPrimaryHover: "#4338ca",
  colorPrimaryText: "#ffffff",
  colorAccent: "#4f46e5",
  colorAccentHover: "#4338ca",
  colorAccentText: "#ffffff",
  colorFocusRing: "#4f46e5",
  colorSuccess: "#10b981",
  colorWarning: "#f59e0b",
  colorDanger: "#ef4444",
  colorInfo: "#06b6d4",
  // Same hues, darkened until they clear 4.5:1 on both #ffffff and #f8fafc:
  // 5.48 / 5.22 / 4.83 / 5.36:1 respectively (the fills above are 2.54 / 2.15 /
  // 3.76 / 2.43:1, which is fine for a shape and not for a glyph).
  colorSuccessText: "#047857",
  colorWarningText: "#a35a00",
  colorDangerText: "#d92d20",
  colorInfoText: "#0e7490",
  // Ink ON the fills above, where the literal #fff used to be: 6.17 / 6.97 /
  // 4.91 / 6.08:1 against success / warning / danger / info respectively.
  // Danger is the tight one — white is 3.76:1 and even #450a0a only reaches
  // 4.29:1, so the ink has to be this dark to clear the bar without moving the
  // hue itself (which draws borders, dots and progress bars elsewhere).
  colorOnSuccess: "#04291e",
  colorOnWarning: "#451a03",
  colorOnDanger: "#2c0606",
  colorOnInfo: "#0c2b3a",
  // Light syntax palette — CodeBlock's surface is --rui-color-surface-muted,
  // which is light in every theme but dark. 4.6-11.7:1 on #f1f5f9.
  hlKeyword: "#cf222e",
  hlString: "#0a3069",
  hlNumber: "#0550ae",
  hlComment: "#57606a",
  hlFn: "#6f42c1",
  hlTag: "#116329",
  hlAttr: "#953800",
  hlPunct: "#475569",
  shadowSm: "0 1px 2px rgba(15, 23, 42, 0.06)",
  shadowMd: "0 6px 24px rgba(15, 23, 42, 0.08)",
  shadowLg: "0 18px 60px rgba(15, 23, 42, 0.12)",
  borderWidth: "1px",
  chart1: "#6366f1",
  chart2: "#10b981",
  chart3: "#f59e0b",
  chart4: "#ef4444",
  chart5: "#06b6d4",
  chart6: "#8b5cf6",
  ...baseFonts,
  ...baseFontScale,
  ...baseSpacing,
  ...baseGradients,
  ...baseRadii,
  ...baseButtons,
  ...baseMotion
};
const darkTheme = {
  ...lightTheme,
  colorBg: "#0b1220",
  colorBgSubtle: "#0f172a",
  colorSurface: "#111827",
  colorSurfaceMuted: "#1e293b",
  colorBorder: "#1f2937",
  colorBorderControl: "#5b6674",
  colorBorderSubtle: "rgba(248, 250, 252, 0.08)",
  colorText: "#f8fafc",
  colorTextMuted: "#94a3b8",
  colorPrimary: "#818cf8",
  colorPrimaryHover: "#6366f1",
  colorPrimaryText: "#0b1220",
  colorAccent: "#818cf8",
  colorAccentHover: "#6366f1",
  colorAccentText: "#0b1220",
  // colorLink derives from the accent, which is 5.95:1 on #111827 — fine. The
  // HOVER does not: #6366f1 is 3.97:1 here, i.e. a link that fails the moment
  // the pointer lands on it. On a dark surface hover has to BRIGHTEN, so this is
  // indigo-300 rather than light's indigo-700 step: 8.90:1.
  colorLinkHover: "#a5b4fc",
  colorFocusRing: "#818cf8",
  // On #111827 the status fills are ALREADY text-safe — 6.99 / 8.26 / 4.71 /
  // 7.31:1 — so the -Text partners are the hues themselves. Stated explicitly
  // rather than inherited, because light's darkened values (#047857 = 1.75:1
  // here) would be unreadable on a dark surface.
  colorSuccessText: "#10b981",
  colorWarningText: "#f59e0b",
  colorDangerText: "#ef4444",
  colorInfoText: "#06b6d4",
  // colorOn* is NOT restated: this theme keeps light's four status FILLS, so
  // light's inks are measured against exactly the same colours here.
  shadowSm: "0 1px 2px rgba(0, 0, 0, 0.4)",
  shadowMd: "0 8px 24px rgba(0, 0, 0, 0.4)",
  shadowLg: "0 22px 60px rgba(0, 0, 0, 0.55)",
  // One Dark, which is what this palette was drawn for: 4.6-7.3:1 on the dark
  // theme's #1e293b code surface. Comment lifted from #7f848e (3.90:1).
  hlKeyword: "#c678dd",
  hlString: "#98c379",
  hlNumber: "#d19a66",
  hlComment: "#9ca3af",
  hlFn: "#61afef",
  hlTag: "#e06c75",
  hlAttr: "#d19a66",
  hlPunct: "#abb2bf"
};
const softTheme = {
  ...lightTheme,
  colorBg: "#fdf6ff",
  colorBgSubtle: "#fbf2ff",
  colorSurface: "#ffffff",
  colorSurfaceMuted: "#f4ecff",
  colorBorder: "#ead8ff",
  colorBorderControl: "#8d78a8",
  colorBorderSubtle: "rgba(168, 132, 232, 0.18)",
  colorText: "#3b1f56",
  colorTextMuted: "#7d6193",
  // 2.72:1 on this theme's white surface, so it fails the 4.5:1 text minimum
  // both in the 125 rules that paint it as a text colour and for the #ffffff
  // label on a primary button. Deepening it to #7c4ddb (5.33:1, same lavender)
  // is the fix, but tests/element.test.ts:282 pins this exact value as its probe
  // for "applies CSS custom properties for built-in themes", so the change needs
  // that assertion updated in the same commit. Left alone here deliberately.
  // Darkened from #a78bfa / #8b5cf6. The old pair measured 2.72:1 on this
  // theme's own white surface, which fails 4.5:1 in BOTH directions: 124 rules
  // paint `color: var(--rui-color-primary)` as body text, and the same value is
  // the fill behind the #ffffff label on every primary button. Same lavender
  // hue, verified: 5.33:1 and 6.65:1.
  colorPrimary: "#7c4ddb",
  colorPrimaryHover: "#6d3fc4",
  colorPrimaryText: "#ffffff",
  colorAccent: "#5eead4",
  colorAccentHover: "#2dd4bf",
  colorAccentText: "#0f3a35",
  // The mint accent CANNOT double as link text: 1.48:1 on this theme's white
  // surface and 1.36:1 on the page tint, i.e. unreadable. It stays exactly as it
  // is, because it is also the FILL that carries the #0f3a35 ink above, and
  // darkening it would break that pairing. The same mint hue at text depth
  // carries the interactive text instead: 6.38:1 on #ffffff and 5.55:1 on
  // #f4ecff, the deepest surface in the theme. Hover deepens (7.65 / 6.66:1).
  colorLink: "#0b6b62",
  colorLinkHover: "#075e56",
  // Darkened from #a78bfa. The focus indicator is the border-colour change on
  // this token, so it has to clear the 3:1 non-text contrast minimum (WCAG
  // 1.4.11 / 2.4.11); the 22% box-shadow glow beside it is only ~1.2:1 and
  // cannot carry the indicator.
  colorFocusRing: "#8b66f8",
  // 3.92:1 on #ffffff
  colorSuccess: "#5eead4",
  colorWarning: "#fcd34d",
  colorDanger: "#fda4af",
  colorInfo: "#93c5fd",
  // The pastel status hues are 1.4-1.9:1 as text. Same hues at full depth:
  // 5.47 / 5.50 / 6.29 / 6.70:1 on #ffffff, all >= 4.88:1 on the page tints.
  colorSuccessText: "#0f766e",
  colorWarningText: "#856400",
  colorDangerText: "#be123c",
  colorInfoText: "#1d4ed8",
  // The pastel fills are the lightest in the library, so the ink on them is the
  // theme's own deep tints: 8.47 / 9.87 / 8.27 / 8.15:1. onSuccess reuses the
  // mint pairing that colorAccentText already established.
  colorOnSuccess: "#0f3a35",
  colorOnWarning: "#3d2600",
  colorOnDanger: "#4c0519",
  colorOnInfo: "#172554",
  fontFamily: "'Quicksand', 'Nunito', system-ui, -apple-system, 'Segoe UI', Roboto, sans-serif",
  fontFamilyHeading: "'Quicksand', 'Nunito', system-ui, -apple-system, 'Segoe UI', Roboto, sans-serif",
  fontFamilyMono: "ui-monospace, SFMono-Regular, Menlo, Consolas, monospace",
  radiusXs: "8px",
  radiusSm: "12px",
  radiusMd: "20px",
  radiusLg: "28px",
  radiusButton: "16px",
  radiusInput: "16px",
  shadowSm: "0 4px 14px rgba(168, 132, 232, 0.12)",
  shadowMd: "0 18px 40px rgba(168, 132, 232, 0.18)",
  shadowLg: "0 28px 70px rgba(168, 132, 232, 0.22)",
  spacingXs: "6px",
  spacingS: "12px",
  spacingM: "18px",
  spacingL: "28px",
  spacingXl: "44px",
  // The top two rungs are NOT inherited. Every theme spreads lightTheme, so a
  // theme that rescaled xs-xl silently kept light's 48px/80px: soft went
  // 44 -> 48 -> 80, a 4px step in a scale where every other rung roughly
  // doubles, and Section(pad: "sm") landed almost on top of pad: "md".
  // Continued at light's own xl x 1.5 / xl x 2.5 ratio.
  spacing2xl: "66px",
  spacing3xl: "110px",
  // This theme had no gradient of its own, so it inherited light's
  // indigo/violet/pink while the stylesheet separately inlined
  // linear-gradient(135deg, #8b5cf6, #f9a8d4) for card and section titles.
  // Both problems close here: the lavender-to-rose identity becomes the token
  // (so $theme({ gradients: { brand } }) reaches the titles), and both stops
  // clear 4.5:1 on this theme's white surface — 5.70:1 and 6.04:1, against
  // 4.23:1 and 1.81:1 for the pair they replace.
  gradientBrand: "linear-gradient(135deg, #7c3aed 0%, #be185d 100%)",
  chart1: "#a78bfa",
  chart2: "#5eead4",
  chart3: "#fcd34d",
  chart4: "#fda4af",
  chart5: "#93c5fd",
  chart6: "#f9a8d4"
};
const visionTheme = {
  ...lightTheme,
  /* Surface & semantic — palette */
  colorBg: "#f4f7fa",
  // neutral-1 · --default-background-color
  colorBgSubtle: "#eaeff4",
  // neutral-1 → neutral-2 blend
  colorSurface: "#ffffff",
  // white · cards / tables / sheets / panels
  colorSurfaceMuted: "#f4f7fa",
  // neutral-1 · muted fills, hovered neutrals
  colorBorder: "#bcc8d4",
  // neutral-3 · --tertiary-shape-color (hairlines, dividers, table rows)
  colorBorderControl: "#6b7a8a",
  colorBorderSubtle: "#dbe2e8",
  // neutral-2
  colorText: "#001b41",
  // corporate-8 · --default-text-color
  colorTextMuted: "#465a75",
  // neutral-6 · --secondary-text-color
  colorPrimary: "#0b2a63",
  // corporate-7 · --primary-button-background-color
  colorPrimaryHover: "#1474c4",
  // corporate-4 · primaries BRIGHTEN on hover
  colorPrimaryText: "#ffffff",
  colorAccent: "#1474c4",
  // corporate-4 · --interactive-text-color (links)
  colorAccentHover: "#095bb1",
  // corporate-5 · --hovered-interactive-text-color
  colorAccentText: "#ffffff",
  colorFocusRing: "#1474c4",
  // corporate-4 · --interactive-shape-color
  colorSuccess: "#0fa954",
  // success-4 · --success-shape-color
  colorWarning: "#ef8300",
  // warning-4 · accessible amber (badges use -3 #ffaa00)
  colorDanger: "#c80a00",
  // critical-5 · --critical-text-color
  colorInfo: "#08a5c5",
  // activating-4 · cyan --activating-shape-color
  // The shape tokens above are 2.7-3.1:1 as text on white (danger already
  // passes at 6.00:1). Same hues one step down the palette: 6.20 / 6.80 / 6.00 /
  // 6.14:1 on #ffffff, >= 5.19:1 on #eaeff4.
  colorSuccessText: "#0a7038",
  colorWarningText: "#8a4b00",
  colorDangerText: "#c80a00",
  colorInfoText: "#066b80",
  // Ink ON the fills. Danger is the one hue in the library dark enough that
  // white is the RIGHT answer (#ffffff on critical-5 #c80a00 = 6.00:1); the other
  // three need dark ink at 5.09 / 5.65 / 5.06:1.
  // colorLink is left deriving from the accent on purpose: corporate-4 #1474c4 is
  // the design system's own --interactive-text-color and measures 4.86:1 on the
  // white surfaces links actually sit on (4.52:1 on the #f4f7fa page). It is
  // 4.20:1 on the #eaeff4 bg-subtle tint — the one shortfall, left as-is because
  // overriding it would break the verified parity of the resting link colour.
  colorOnSuccess: "#0a7038",
  colorOnWarning: "#8a4b00",
  // #ffffff, not the fill colour: this token is the ink drawn ON critical-5, and
  // copying the fill here made every danger-filled surface invisible — a solid
  // red ConfirmDialog button with a red label, a red badge with a red count.
  // (It held `#c80a00` — the fill — for a while, contradicting the note directly
  // above it, and the whole family shipped red-on-red with it:
  // `.rui-confirm-ok[data-tone="danger"]`, `.rui-button[data-variant="danger"]`,
  // `.rui-icon-button[data-variant="danger"]`, the danger toast icon,
  // `.rui-product-badge`, `.rui-tabbar-badge` and the error step marker all paint
  // this token on `colorDanger`.) #ffffff on critical-5 #c80a00 is 6.00:1.
  colorOnDanger: "#ffffff",
  colorOnInfo: "#066b80",
  /* Typography — Open Sans body, Overpass display, 14px/20px */
  fontFamily: "'Open Sans', system-ui, -apple-system, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif",
  fontFamilyHeading: "'Overpass', 'Open Sans', system-ui, -apple-system, 'Segoe UI', Roboto, sans-serif",
  fontFamilyMono: "'JetBrains Mono', ui-monospace, SFMono-Regular, Menlo, Consolas, monospace",
  fontSizeBase: "14px",
  // --default-text-size
  fontSizeSm: "12px",
  // --small-text-size
  fontSizeLg: "16px",
  // --large-text-size
  fontSizeHeading: "16px",
  // --third-level-headline-size
  fontSizeTitle: "22px",
  // --second-level-headline-size
  fontWeightBody: "400",
  fontWeightHeading: "600",
  // Open Sans Semibold / Overpass Semibold
  lineHeightBody: "1.4286",
  // 20/14 · --default-text-line-height
  lineHeightHeading: "1.3",
  letterSpacingHeading: "0",
  headingTextTransform: "none",
  /* Shape — radii scale */
  radiusXs: "4px",
  // --xsmall-border-radius
  radiusSm: "8px",
  // --small-border-radius (inputs, list items)
  radiusMd: "12px",
  // --medium-border-radius
  radiusLg: "16px",
  // --default-border-radius (cards, tables, alerts)
  radiusPill: "999px",
  radiusButton: "24px",
  // --button-border-radius (pill buttons)
  radiusInput: "8px",
  // --small-border-radius
  borderWidth: "1px",
  // --default-border-width
  /* Shadows — is deliberately flat; shadows only on floating layers */
  shadowSm: "0 1px 2px 0 rgba(113, 128, 149, 0.5)",
  // --default-shadow (exact alpha)
  shadowMd: "0 2px 8px 0 rgba(113, 128, 149, 0.5)",
  // --primary-shadow (exact alpha)
  shadowLg: "0 12px 32px rgba(2, 16, 43, 0.16)",
  // floating overlays / modals
  /* Spacing — 4 / 8 / 12 / 20 / 32 rhythm */
  spacingXs: "4px",
  spacingS: "8px",
  spacingM: "12px",
  spacingL: "20px",
  spacingXl: "32px",
  /* Gradients — advertising gradient is corporate-6 → corporate-4 */
  gradientBrand: "linear-gradient(120deg, #003d8f 0%, #1474c4 100%)",
  gradientAccent: "linear-gradient(120deg, #1474c4 0%, #11c7e6 100%)",
  gradientWarm: "linear-gradient(120deg, #ffaa00 0%, #ff6159 100%)",
  gradientCool: "linear-gradient(120deg, #0b2a63 0%, #08a5c5 100%)",
  gradientSuccess: "linear-gradient(120deg, #0fa954 0%, #11c7e6 100%)",
  gradientDanger: "linear-gradient(120deg, #ff6159 0%, #c80a00 100%)",
  /* Buttons — semibold, pill, 20px horizontal padding */
  buttonFontWeight: "600",
  buttonTextTransform: "none",
  buttonLetterSpacing: "0",
  buttonPaddingY: "4px",
  // --button-padding: 4px 20px
  buttonPaddingX: "20px",
  /* Motion — uses snappy 0.1s ease-out transitions */
  transitionDuration: "100ms",
  motionFast: "80ms",
  motionBase: "120ms",
  motionSlow: "240ms",
  motionEase: "ease-out",
  /* Chart palette — brand hues */
  chart1: "#1474c4",
  // corporate-4 (blue)
  chart2: "#08a5c5",
  // activating-4 (cyan)
  chart3: "#0fa954",
  // success-4 (green)
  chart4: "#ffaa00",
  // warning-3 (amber)
  chart5: "#b410e7",
  // promoting-5 (magenta)
  chart6: "#0b2a63"
  // corporate-7 (navy)
};
const shadcnLightTheme = {
  ...lightTheme,
  /* ----- Surface & semantic ----- */
  colorBg: "#ffffff",
  colorBgSubtle: "#fafafa",
  colorSurface: "#ffffff",
  colorSurfaceMuted: "#f5f5f5",
  colorBorder: "#e5e5e5",
  colorBorderSubtle: "rgba(10, 10, 10, 0.06)",
  colorBorderControl: "#8f8f8f",
  colorText: "#0a0a0a",
  colorTextMuted: "#737373",
  colorPrimary: "#171717",
  // shadcn hovers a solid button by dropping the fill to `bg-primary/90`,
  // which over a white page LIGHTENS it. This is that composite.
  colorPrimaryHover: "#2e2e2e",
  colorPrimaryText: "#fafafa",
  // shadcn has no second brand hue — `--accent` is the same neutral wash as
  // `--muted`, and anything that wants attention uses the ink primary.
  colorAccent: "#171717",
  colorAccentHover: "#2e2e2e",
  colorAccentText: "#fafafa",
  colorLink: "#171717",
  colorLinkHover: "#404040",
  colorFocusRing: "#737373",
  // shadcn ships one semantic colour (`--destructive`); the other three are
  // the Tailwind v4 hues its own examples and charts reach for.
  colorSuccess: "#16a34a",
  colorWarning: "#f59e0b",
  colorDanger: "#e7000b",
  colorInfo: "#2563eb",
  colorSuccessText: "#15803d",
  colorWarningText: "#a35a00",
  colorDangerText: "#c10007",
  colorInfoText: "#1d4ed8",
  // shadcn paints white on `--destructive`, and white clears 4.5:1 on that
  // fill (4.77:1) — so unlike most themes here, three of the four inks are
  // simply white. Success and warning are too light for it.
  colorOnSuccess: "#04291e",
  colorOnWarning: "#451a03",
  colorOnDanger: "#ffffff",
  colorOnInfo: "#ffffff",
  colorSurfaceHover: "rgba(10, 10, 10, 0.05)",
  /* ----- Typography — Geist, 14px (`text-sm`) ----- */
  fontFamily: "'Geist', 'Inter', ui-sans-serif, system-ui, -apple-system, 'Segoe UI', Roboto, sans-serif",
  fontFamilyHeading: "'Geist', 'Inter', ui-sans-serif, system-ui, -apple-system, 'Segoe UI', Roboto, sans-serif",
  fontFamilyMono: "'Geist Mono', ui-monospace, SFMono-Regular, Menlo, Consolas, monospace",
  fontSizeBase: "14px",
  fontSizeSm: "12px",
  fontSizeLg: "16px",
  fontSizeHeading: "16px",
  fontSizeTitle: "24px",
  fontWeightBody: "400",
  fontWeightHeading: "600",
  lineHeightBody: "1.5",
  lineHeightHeading: "1.25",
  letterSpacingHeading: "-0.015em",
  headingTextTransform: "none",
  /* ----- Shape — `--radius: 0.625rem` and its four derived rungs ----- */
  radiusXs: "4px",
  radiusSm: "6px",
  radiusMd: "10px",
  radiusLg: "14px",
  radiusPill: "999px",
  radiusButton: "8px",
  radiusInput: "8px",
  borderWidth: "1px",
  /* ----- Shadows — Tailwind's `shadow-xs` / `shadow-md` / `shadow-lg` ----- */
  shadowSm: "0 1px 2px 0 rgba(0, 0, 0, 0.05)",
  shadowMd: "0 4px 6px -1px rgba(0, 0, 0, 0.10), 0 2px 4px -2px rgba(0, 0, 0, 0.10)",
  shadowLg: "0 10px 15px -3px rgba(0, 0, 0, 0.10), 0 4px 6px -4px rgba(0, 0, 0, 0.10)",
  /* ----- Spacing — Tailwind's 4px grid, `p-6` cards ----- */
  spacingXs: "4px",
  spacingS: "8px",
  spacingM: "12px",
  spacingL: "24px",
  spacingXl: "36px",
  spacing2xl: "56px",
  spacing3xl: "88px",
  /* ----- Gradients — neutral-first, the way shadcn's own marketing reads ----- */
  gradientBrand: "linear-gradient(120deg, #0a0a0a 0%, #404040 55%, #737373 100%)",
  gradientAccent: "linear-gradient(120deg, #171717 0%, #525252 100%)",
  gradientWarm: "linear-gradient(120deg, #f59e0b 0%, #e7000b 100%)",
  gradientCool: "linear-gradient(120deg, #2563eb 0%, #06b6d4 100%)",
  gradientSuccess: "linear-gradient(120deg, #16a34a 0%, #22c55e 100%)",
  gradientDanger: "linear-gradient(120deg, #e7000b 0%, #9f0712 100%)",
  /* ----- Buttons — `h-9 px-4 text-sm font-medium` ----- */
  buttonFontWeight: "500",
  buttonTextTransform: "none",
  buttonLetterSpacing: "0",
  buttonPaddingY: "8px",
  buttonPaddingX: "16px",
  /* ----- Motion — Tailwind's 150ms / `ease-in-out` default ----- */
  transitionDuration: "150ms",
  motionFast: "100ms",
  motionBase: "150ms",
  motionSlow: "300ms",
  motionEase: "cubic-bezier(0.4, 0, 0.2, 1)",
  /* ----- Charts — the five `--chart-*` values from shadcn's light block ----- */
  chart1: "#e76e50",
  chart2: "#2a9d90",
  chart3: "#274754",
  chart4: "#e8c468",
  chart5: "#f4a462",
  chart6: "#9c6644"
};
const shadcnDarkTheme = {
  ...shadcnLightTheme,
  colorBg: "#0a0a0a",
  colorBgSubtle: "#0f0f0f",
  colorSurface: "#171717",
  colorSurfaceMuted: "#262626",
  colorBorder: "#262626",
  colorBorderSubtle: "rgba(255, 255, 255, 0.08)",
  colorBorderControl: "#737373",
  colorText: "#fafafa",
  colorTextMuted: "#a1a1a1",
  colorPrimary: "#e5e5e5",
  colorPrimaryHover: "#cfcfcf",
  colorPrimaryText: "#171717",
  colorAccent: "#e5e5e5",
  colorAccentHover: "#cfcfcf",
  colorAccentText: "#171717",
  colorLink: "#fafafa",
  colorLinkHover: "#d4d4d4",
  colorFocusRing: "#737373",
  colorSuccess: "#22c55e",
  colorWarning: "#f59e0b",
  colorDanger: "#ff6467",
  colorInfo: "#3b82f6",
  // On #171717 all four fills already clear the 4.5:1 text bar (7.9 / 10.4 /
  // 6.4 / 4.9:1), so the text partners are the hues themselves — light's
  // darkened values would be unreadable here.
  colorSuccessText: "#22c55e",
  colorWarningText: "#f59e0b",
  colorDangerText: "#ff6467",
  colorInfoText: "#3b82f6",
  // The dark fills are bright, so the ink flips the other way: white on
  // #ff6467 is 2.4:1 and on #3b82f6 3.7:1.
  colorOnSuccess: "#04291e",
  colorOnWarning: "#451a03",
  colorOnDanger: "#2c0606",
  colorOnInfo: "#08131f",
  colorSurfaceHover: "rgba(255, 255, 255, 0.06)",
  shadowSm: "0 1px 2px 0 rgba(0, 0, 0, 0.35)",
  shadowMd: "0 4px 6px -1px rgba(0, 0, 0, 0.45), 0 2px 4px -2px rgba(0, 0, 0, 0.45)",
  shadowLg: "0 10px 15px -3px rgba(0, 0, 0, 0.55), 0 4px 6px -4px rgba(0, 0, 0, 0.50)",
  gradientBrand: "linear-gradient(120deg, #fafafa 0%, #a1a1a1 55%, #525252 100%)",
  gradientAccent: "linear-gradient(120deg, #e5e5e5 0%, #a1a1a1 100%)",
  // One Dark — CodeBlock's surface is `--rui-color-surface-muted`, #262626 here.
  hlKeyword: "#c678dd",
  hlString: "#98c379",
  hlNumber: "#d19a66",
  hlComment: "#9ca3af",
  hlFn: "#61afef",
  hlTag: "#e06c75",
  hlAttr: "#d19a66",
  hlPunct: "#abb2bf",
  /* The `--chart-*` values from shadcn's `.dark` block. */
  chart1: "#2662d9",
  chart2: "#2eb88a",
  chart3: "#e88c30",
  chart4: "#af57db",
  chart5: "#e23670",
  chart6: "#3ec9d6"
};
const muiLightTheme = {
  ...lightTheme,
  /* ----- Surface & semantic ----- */
  colorBg: "#ffffff",
  colorBgSubtle: "#f5f5f5",
  colorSurface: "#ffffff",
  colorSurfaceMuted: "#f5f5f5",
  colorBorder: "rgba(0, 0, 0, 0.12)",
  colorBorderSubtle: "rgba(0, 0, 0, 0.07)",
  colorBorderControl: "#8c8c8c",
  colorText: "rgba(0, 0, 0, 0.87)",
  colorTextMuted: "rgba(0, 0, 0, 0.6)",
  colorPrimary: "#1976d2",
  colorPrimaryHover: "#1565c0",
  colorPrimaryText: "#ffffff",
  colorAccent: "#9c27b0",
  colorAccentHover: "#7b1fa2",
  colorAccentText: "#ffffff",
  // MUI's Link is `primary.main`, which is 4.22:1 on the grey-100 band this
  // theme paints table headers and muted panels with — under the 4.5:1 text
  // bar. One step down the same ramp (`primary.dark`) gives 5.27:1 there and
  // 5.75:1 on white; hover continues to blue-900.
  colorLink: "#1565c0",
  colorLinkHover: "#0d47a1",
  colorFocusRing: "#1976d2",
  colorSuccess: "#2e7d32",
  colorWarning: "#ed6c02",
  colorDanger: "#d32f2f",
  colorInfo: "#0288d1",
  colorSuccessText: "#1e4620",
  colorWarningText: "#663c00",
  colorDangerText: "#5f2120",
  colorInfoText: "#014361",
  // Success and error are dark enough for MUI's own white label (5.13 / 4.98:1).
  // Warning and info are not (3.11 / 3.86:1), so those two take a hue-matched
  // dark ink instead — the one place this theme knowingly departs from MUI,
  // and only on the ~45 rules that paint a glyph ON the fill.
  colorOnSuccess: "#ffffff",
  colorOnWarning: "#451a03",
  colorOnDanger: "#ffffff",
  colorOnInfo: "#001724",
  colorSurfaceHover: "rgba(0, 0, 0, 0.04)",
  /* ----- Typography — Roboto, 16px body, MUI's h5/h6 for title/heading ----- */
  fontFamily: "'Roboto', 'Helvetica Neue', Helvetica, Arial, sans-serif",
  fontFamilyHeading: "'Roboto', 'Helvetica Neue', Helvetica, Arial, sans-serif",
  fontFamilyMono: "'Roboto Mono', ui-monospace, SFMono-Regular, Menlo, Consolas, monospace",
  fontSizeBase: "16px",
  fontSizeSm: "14px",
  fontSizeLg: "18px",
  fontSizeHeading: "20px",
  fontSizeTitle: "24px",
  fontWeightBody: "400",
  fontWeightHeading: "500",
  lineHeightBody: "1.5",
  lineHeightHeading: "1.334",
  letterSpacingHeading: "0.0075em",
  headingTextTransform: "none",
  /* ----- Shape — `shape.borderRadius: 4` really is the whole scale ----- */
  radiusXs: "2px",
  radiusSm: "4px",
  radiusMd: "4px",
  radiusLg: "4px",
  radiusPill: "999px",
  radiusButton: "4px",
  radiusInput: "4px",
  borderWidth: "1px",
  /* ----- Shadows — MUI elevation 1 / 4 / 24, verbatim ----- */
  shadowSm: "0px 2px 1px -1px rgba(0, 0, 0, 0.2), 0px 1px 1px 0px rgba(0, 0, 0, 0.14), 0px 1px 3px 0px rgba(0, 0, 0, 0.12)",
  shadowMd: "0px 2px 4px -1px rgba(0, 0, 0, 0.2), 0px 4px 5px 0px rgba(0, 0, 0, 0.14), 0px 1px 10px 0px rgba(0, 0, 0, 0.12)",
  shadowLg: "0px 11px 15px -7px rgba(0, 0, 0, 0.2), 0px 24px 38px 3px rgba(0, 0, 0, 0.14), 0px 9px 46px 8px rgba(0, 0, 0, 0.12)",
  /* ----- Spacing — `theme.spacing(n)` = n * 8 ----- */
  spacingXs: "4px",
  spacingS: "8px",
  spacingM: "16px",
  spacingL: "24px",
  spacingXl: "32px",
  spacing2xl: "48px",
  spacing3xl: "80px",
  /* ----- Gradients — primary → secondary, the only two brand hues MUI has -- */
  gradientBrand: "linear-gradient(120deg, #1976d2 0%, #9c27b0 100%)",
  gradientAccent: "linear-gradient(120deg, #9c27b0 0%, #1976d2 100%)",
  gradientWarm: "linear-gradient(120deg, #ed6c02 0%, #d32f2f 100%)",
  gradientCool: "linear-gradient(120deg, #0288d1 0%, #1976d2 100%)",
  gradientSuccess: "linear-gradient(120deg, #2e7d32 0%, #0288d1 100%)",
  gradientDanger: "linear-gradient(120deg, #d32f2f 0%, #9c27b0 100%)",
  /* ----- Buttons — MUI's `MuiButton` typography, exactly ----- */
  buttonFontWeight: "500",
  buttonTextTransform: "uppercase",
  buttonLetterSpacing: "0.02857em",
  buttonPaddingY: "6px",
  buttonPaddingX: "16px",
  /* ----- Motion — `transitions.duration` shortest / standard / complex ----- */
  transitionDuration: "150ms",
  motionFast: "150ms",
  motionBase: "250ms",
  motionSlow: "300ms",
  motionEase: "cubic-bezier(0.4, 0, 0.2, 1)",
  /* ----- Charts — MUI X's default `blueberryTwilightPalette` (light) ----- */
  chart1: "#02b2af",
  chart2: "#2e96ff",
  chart3: "#b800d8",
  chart4: "#60009b",
  chart5: "#2731c8",
  chart6: "#03008d"
};
const muiDarkTheme = {
  ...muiLightTheme,
  colorBg: "#121212",
  colorBgSubtle: "#181818",
  colorSurface: "#1e1e1e",
  colorSurfaceMuted: "#272727",
  colorBorder: "rgba(255, 255, 255, 0.12)",
  colorBorderSubtle: "rgba(255, 255, 255, 0.08)",
  colorBorderControl: "#7a7a7a",
  colorText: "#ffffff",
  colorTextMuted: "rgba(255, 255, 255, 0.7)",
  colorPrimary: "#90caf9",
  colorPrimaryHover: "#42a5f5",
  colorPrimaryText: "rgba(0, 0, 0, 0.87)",
  colorAccent: "#ce93d8",
  colorAccentHover: "#ba68c8",
  colorAccentText: "rgba(0, 0, 0, 0.87)",
  colorLink: "#90caf9",
  colorLinkHover: "#bbdefb",
  colorFocusRing: "#90caf9",
  colorSuccess: "#66bb6a",
  colorWarning: "#ffa726",
  colorDanger: "#f44336",
  colorInfo: "#29b6f6",
  // 7.0 / 10.5 / 4.5 / 8.4:1 on #1e1e1e — the dark palette's fills are already
  // text-safe, so light's very dark Alert inks are replaced by the hues.
  colorSuccessText: "#66bb6a",
  colorWarningText: "#ffa726",
  colorDangerText: "#f44336",
  colorInfoText: "#29b6f6",
  colorOnSuccess: "#04291e",
  colorOnWarning: "#451a03",
  colorOnDanger: "#2c0606",
  colorOnInfo: "#08131f",
  colorSurfaceHover: "rgba(255, 255, 255, 0.08)",
  gradientBrand: "linear-gradient(120deg, #90caf9 0%, #ce93d8 100%)",
  gradientAccent: "linear-gradient(120deg, #ce93d8 0%, #90caf9 100%)",
  gradientWarm: "linear-gradient(120deg, #ffa726 0%, #f44336 100%)",
  gradientCool: "linear-gradient(120deg, #29b6f6 0%, #90caf9 100%)",
  gradientSuccess: "linear-gradient(120deg, #66bb6a 0%, #29b6f6 100%)",
  gradientDanger: "linear-gradient(120deg, #f44336 0%, #ce93d8 100%)",
  hlKeyword: "#c678dd",
  hlString: "#98c379",
  hlNumber: "#d19a66",
  hlComment: "#9ca3af",
  hlFn: "#61afef",
  hlTag: "#e06c75",
  hlAttr: "#d19a66",
  hlPunct: "#abb2bf",
  /* MUI X's `blueberryTwilightPalette` (dark). */
  chart1: "#02b2af",
  chart2: "#72ccff",
  chart3: "#da00ff",
  chart4: "#9001cb",
  chart5: "#2e96ff",
  chart6: "#b800d8"
};
const herouiLightTheme = {
  ...lightTheme,
  /* ----- Surface & semantic ----- */
  colorBg: "#ffffff",
  colorBgSubtle: "#fafafa",
  colorSurface: "#ffffff",
  colorSurfaceMuted: "#f4f4f5",
  colorBorder: "#e4e4e7",
  colorBorderSubtle: "rgba(17, 17, 17, 0.08)",
  colorBorderControl: "#8b8b93",
  colorText: "#11181c",
  // HeroUI's `default-500` #71717a measures 4.40:1 on the `default-100` fill
  // that backs chips, pills and flat inputs — just under the body-text bar.
  // #63636b is the same zinc a rung darker: 5.42:1 there, 5.95:1 on white.
  colorTextMuted: "#63636b",
  colorPrimary: "#006fee",
  colorPrimaryHover: "#005bc4",
  colorPrimaryText: "#ffffff",
  colorAccent: "#7828c8",
  colorAccentHover: "#6020a0",
  colorAccentText: "#ffffff",
  // HeroUI paints links in `primary` itself, which is 4.46:1 on this theme's
  // `default-50` page tint — a hair under the body-text bar. The link takes
  // the 600 rung instead (6.38:1 on white, 6.10:1 on the tint); the FILL
  // above is untouched, so buttons and chips stay HeroUI blue.
  colorLink: "#005bc4",
  colorLinkHover: "#004493",
  colorFocusRing: "#006fee",
  colorSuccess: "#17c964",
  colorWarning: "#f5a524",
  colorDanger: "#f31260",
  // HeroUI's semantic set has no `info`; the primary blue is what its own
  // informational chips and alerts use.
  colorInfo: "#006fee",
  // The 700 rung of each HeroUI colour scale — the shade its flat chips and
  // alerts already paint their label in.
  colorSuccessText: "#0e793c",
  colorWarningText: "#936316",
  colorDangerText: "#920b3a",
  colorInfoText: "#005bc4",
  colorOnSuccess: "#04291e",
  colorOnWarning: "#451a03",
  colorOnDanger: "#2a0413",
  colorOnInfo: "#ffffff",
  colorSurfaceHover: "rgba(17, 24, 28, 0.05)",
  /* ----- Typography — Inter at 16px (`text-medium`) ----- */
  fontFamily: "'Inter', ui-sans-serif, system-ui, -apple-system, 'Segoe UI', Roboto, sans-serif",
  fontFamilyHeading: "'Inter', ui-sans-serif, system-ui, -apple-system, 'Segoe UI', Roboto, sans-serif",
  fontFamilyMono: "ui-monospace, SFMono-Regular, Menlo, Consolas, monospace",
  fontSizeBase: "16px",
  fontSizeSm: "14px",
  fontSizeLg: "18px",
  fontSizeHeading: "18px",
  fontSizeTitle: "30px",
  fontWeightBody: "400",
  fontWeightHeading: "600",
  lineHeightBody: "1.5",
  lineHeightHeading: "1.25",
  letterSpacingHeading: "-0.02em",
  headingTextTransform: "none",
  /* ----- Shape — `--heroui-radius-small / -medium / -large` ----- */
  radiusXs: "6px",
  radiusSm: "8px",
  radiusMd: "12px",
  radiusLg: "14px",
  radiusPill: "999px",
  radiusButton: "12px",
  radiusInput: "12px",
  borderWidth: "1px",
  /* ----- Shadows — HeroUI's `shadow-small / -medium / -large`, verbatim ----- */
  shadowSm: "0px 0px 5px 0px rgba(0, 0, 0, 0.02), 0px 2px 10px 0px rgba(0, 0, 0, 0.06), 0px 0px 1px 0px rgba(0, 0, 0, 0.3)",
  shadowMd: "0px 0px 15px 0px rgba(0, 0, 0, 0.03), 0px 2px 30px 0px rgba(0, 0, 0, 0.08), 0px 0px 1px 0px rgba(0, 0, 0, 0.3)",
  shadowLg: "0px 0px 30px 0px rgba(0, 0, 0, 0.04), 0px 30px 60px 0px rgba(0, 0, 0, 0.12), 0px 0px 1px 0px rgba(0, 0, 0, 0.3)",
  /* ----- Spacing ----- */
  spacingXs: "4px",
  spacingS: "8px",
  spacingM: "12px",
  spacingL: "16px",
  spacingXl: "28px",
  spacing2xl: "48px",
  spacing3xl: "80px",
  /* ----- Gradients — the pairs HeroUI's own docs put on their hero copy ----- */
  gradientBrand: "linear-gradient(120deg, #5ea2ef 0%, #0072f5 100%)",
  gradientAccent: "linear-gradient(120deg, #ff1cf7 0%, #b249f8 100%)",
  gradientWarm: "linear-gradient(120deg, #ff705b 0%, #ffb457 100%)",
  gradientCool: "linear-gradient(120deg, #5ea2ef 0%, #17c964 100%)",
  gradientSuccess: "linear-gradient(120deg, #6fee8d 0%, #17c964 100%)",
  gradientDanger: "linear-gradient(120deg, #f54180 0%, #f31260 100%)",
  /* ----- Buttons — `h-10 px-4 text-small font-medium`, `rounded-medium` ----- */
  buttonFontWeight: "500",
  buttonTextTransform: "none",
  buttonLetterSpacing: "0",
  buttonPaddingY: "10px",
  buttonPaddingX: "16px",
  /* ----- Motion — HeroUI's 250ms `transition-transform-colors-opacity` ----- */
  transitionDuration: "250ms",
  motionFast: "150ms",
  motionBase: "250ms",
  motionSlow: "300ms",
  motionEase: "cubic-bezier(0, 0, 0.2, 1)",
  /* ----- Charts — the semantic palette, primary first ----- */
  chart1: "#006fee",
  chart2: "#7828c8",
  chart3: "#17c964",
  chart4: "#f5a524",
  chart5: "#f31260",
  chart6: "#ff705b"
};
const herouiDarkTheme = {
  ...herouiLightTheme,
  colorBg: "#000000",
  colorBgSubtle: "#09090b",
  colorSurface: "#18181b",
  colorSurfaceMuted: "#27272a",
  colorBorder: "#27272a",
  colorBorderSubtle: "rgba(255, 255, 255, 0.10)",
  colorBorderControl: "#8b8b93",
  colorText: "#ecedee",
  colorTextMuted: "#a1a1aa",
  colorPrimary: "#006fee",
  // Dark-mode primaries BRIGHTEN on hover — #338ef7 is the dark scale's 500.
  colorPrimaryHover: "#338ef7",
  colorPrimaryText: "#ffffff",
  colorAccent: "#9353d3",
  colorAccentHover: "#ae7ede",
  colorAccentText: "#ffffff",
  colorLink: "#66aaf9",
  colorLinkHover: "#99c7fb",
  colorFocusRing: "#006fee",
  colorSuccess: "#17c964",
  colorWarning: "#f5a524",
  colorDanger: "#f31260",
  colorInfo: "#006fee",
  colorSuccessText: "#17c964",
  colorWarningText: "#f5a524",
  // #f31260 is 4.27:1 on content1; #f54180 is the same ramp one step up, 5.02:1.
  colorDangerText: "#f54180",
  colorInfoText: "#66aaf9",
  colorOnSuccess: "#04291e",
  colorOnWarning: "#451a03",
  colorOnDanger: "#2a0413",
  colorOnInfo: "#ffffff",
  colorSurfaceHover: "rgba(255, 255, 255, 0.07)",
  shadowSm: "0px 0px 5px 0px rgba(0, 0, 0, 0.05), 0px 2px 10px 0px rgba(0, 0, 0, 0.2), inset 0px 0px 1px 0px rgba(255, 255, 255, 0.15)",
  shadowMd: "0px 0px 15px 0px rgba(0, 0, 0, 0.06), 0px 2px 30px 0px rgba(0, 0, 0, 0.22), inset 0px 0px 1px 0px rgba(255, 255, 255, 0.15)",
  shadowLg: "0px 0px 30px 0px rgba(0, 0, 0, 0.07), 0px 30px 60px 0px rgba(0, 0, 0, 0.26), inset 0px 0px 1px 0px rgba(255, 255, 255, 0.15)",
  hlKeyword: "#c678dd",
  hlString: "#98c379",
  hlNumber: "#d19a66",
  hlComment: "#9ca3af",
  hlFn: "#61afef",
  hlTag: "#e06c75",
  hlAttr: "#d19a66",
  hlPunct: "#abb2bf",
  chart1: "#338ef7",
  chart2: "#9353d3",
  chart3: "#45d483",
  chart4: "#f7b750",
  chart5: "#f54180",
  chart6: "#ff8f7c"
};
const SHADCN_FONTS = ["Geist:400,500,600,700", "Geist Mono:400,500"];
const MUI_FONTS = ["Roboto:300,400,500,700", "Roboto Mono:400,500"];
const HEROUI_FONTS = ["Inter:400,500,600,700"];
const builtInThemeFonts = {
  shadcn: { import: SHADCN_FONTS },
  "shadcn-light": { import: SHADCN_FONTS },
  "shadcn-dark": { import: SHADCN_FONTS },
  mui: { import: MUI_FONTS },
  "mui-light": { import: MUI_FONTS },
  "mui-dark": { import: MUI_FONTS },
  heroui: { import: HEROUI_FONTS },
  "heroui-light": { import: HEROUI_FONTS },
  "heroui-dark": { import: HEROUI_FONTS },
  vision: { import: ["Open Sans:400,600", "Overpass:400,600"] }
};
const builtInThemes = {
  light: lightTheme,
  dark: darkTheme,
  shadcn: shadcnLightTheme,
  "shadcn-light": shadcnLightTheme,
  "shadcn-dark": shadcnDarkTheme,
  mui: muiLightTheme,
  "mui-light": muiLightTheme,
  "mui-dark": muiDarkTheme,
  heroui: herouiLightTheme,
  "heroui-light": herouiLightTheme,
  "heroui-dark": herouiDarkTheme,
  soft: softTheme
};
const deprecatedThemeAliases = {
  modern: "shadcn-light",
  glass: "mui-light",
  corporate: "heroui-light"
};
function canonicalThemeName(key) {
  return deprecatedThemeAliases[key] ?? key;
}
const privateThemes = {
  vision: visionTheme
};
function findThemeByName(name) {
  if (typeof name !== "string") return null;
  const key = name.trim().toLowerCase();
  if (!key) return null;
  const canonical = canonicalThemeName(key);
  return builtInThemes[canonical] ?? privateThemes[canonical] ?? null;
}
const TOKEN_TO_CSS = {
  colorBg: "--rui-color-bg",
  colorBgSubtle: "--rui-color-bg-subtle",
  colorSurface: "--rui-color-surface",
  colorSurfaceMuted: "--rui-color-surface-muted",
  colorBorder: "--rui-color-border",
  colorBorderSubtle: "--rui-color-border-subtle",
  colorBorderControl: "--rui-color-border-control",
  colorText: "--rui-color-text",
  colorTextMuted: "--rui-color-text-muted",
  colorPrimary: "--rui-color-primary",
  colorPrimaryHover: "--rui-color-primary-hover",
  colorPrimaryText: "--rui-color-primary-text",
  colorAccent: "--rui-color-accent",
  colorAccentHover: "--rui-color-accent-hover",
  colorAccentText: "--rui-color-accent-text",
  colorLink: "--rui-color-link",
  colorLinkHover: "--rui-color-link-hover",
  colorFocusRing: "--rui-color-focus-ring",
  colorSuccess: "--rui-color-success",
  colorWarning: "--rui-color-warning",
  colorDanger: "--rui-color-danger",
  colorInfo: "--rui-color-info",
  colorSuccessText: "--rui-color-success-text",
  colorWarningText: "--rui-color-warning-text",
  colorDangerText: "--rui-color-danger-text",
  colorInfoText: "--rui-color-info-text",
  colorOnSuccess: "--rui-color-on-success",
  colorOnWarning: "--rui-color-on-warning",
  colorOnDanger: "--rui-color-on-danger",
  colorOnInfo: "--rui-color-on-info",
  colorSurfaceHover: "--rui-color-surface-hover",
  fontFamily: "--rui-font-family",
  fontFamilyHeading: "--rui-font-family-heading",
  fontFamilyMono: "--rui-font-family-mono",
  fontSizeBase: "--rui-font-size-base",
  fontSize10: "--rui-font-size-10",
  fontSize11: "--rui-font-size-11",
  fontSize13: "--rui-font-size-13",
  fontSize15: "--rui-font-size-15",
  fontSize18: "--rui-font-size-18",
  fontSize20: "--rui-font-size-20",
  fontSize24: "--rui-font-size-24",
  fontSize32: "--rui-font-size-32",
  fontSizeSm: "--rui-font-size-sm",
  fontSizeLg: "--rui-font-size-lg",
  fontSizeHeading: "--rui-font-size-heading",
  fontSizeTitle: "--rui-font-size-title",
  fontWeightBody: "--rui-font-weight-body",
  fontWeightHeading: "--rui-font-weight-heading",
  lineHeightBody: "--rui-line-height-body",
  lineHeightHeading: "--rui-line-height-heading",
  letterSpacingHeading: "--rui-letter-spacing-heading",
  headingTextTransform: "--rui-heading-text-transform",
  radiusXs: "--rui-radius-xs",
  radiusSm: "--rui-radius-sm",
  radiusMd: "--rui-radius-md",
  radiusLg: "--rui-radius-lg",
  radiusPill: "--rui-radius-pill",
  radiusButton: "--rui-radius-button",
  radiusInput: "--rui-radius-input",
  borderWidth: "--rui-border-width",
  shadowSm: "--rui-shadow-sm",
  shadowMd: "--rui-shadow-md",
  shadowLg: "--rui-shadow-lg",
  spacing3xs: "--rui-spacing-3xs",
  spacing2xs: "--rui-spacing-2xs",
  spacingXs: "--rui-spacing-xs",
  spacingS: "--rui-spacing-s",
  spacingM: "--rui-spacing-m",
  spacingL: "--rui-spacing-l",
  spacingXl: "--rui-spacing-xl",
  spacing2xl: "--rui-spacing-2xl",
  spacing3xl: "--rui-spacing-3xl",
  gradientBrand: "--rui-gradient-brand",
  gradientAccent: "--rui-gradient-accent",
  gradientWarm: "--rui-gradient-warm",
  gradientCool: "--rui-gradient-cool",
  gradientSuccess: "--rui-gradient-success",
  gradientDanger: "--rui-gradient-danger",
  buttonFontWeight: "--rui-button-font-weight",
  buttonTextTransform: "--rui-button-text-transform",
  buttonLetterSpacing: "--rui-button-letter-spacing",
  buttonPaddingY: "--rui-button-padding-y",
  buttonPaddingX: "--rui-button-padding-x",
  transitionDuration: "--rui-transition-duration",
  motionFast: "--rui-motion-fast",
  motionBase: "--rui-motion-base",
  motionSlow: "--rui-motion-slow",
  motionEase: "--rui-motion-ease",
  zBase: "--rui-z-base",
  zRaised: "--rui-z-raised",
  zDropdown: "--rui-z-dropdown",
  zSticky: "--rui-z-sticky",
  zBanner: "--rui-z-banner",
  zOverlay: "--rui-z-overlay",
  zModal: "--rui-z-modal",
  zPopover: "--rui-z-popover",
  zToast: "--rui-z-toast",
  zTooltip: "--rui-z-tooltip",
  hlKeyword: "--rui-hl-keyword",
  hlString: "--rui-hl-string",
  hlNumber: "--rui-hl-number",
  hlComment: "--rui-hl-comment",
  hlFn: "--rui-hl-fn",
  hlTag: "--rui-hl-tag",
  hlAttr: "--rui-hl-attr",
  hlPunct: "--rui-hl-punct",
  chart1: "--rui-chart-1",
  chart2: "--rui-chart-2",
  chart3: "--rui-chart-3",
  chart4: "--rui-chart-4",
  chart5: "--rui-chart-5",
  chart6: "--rui-chart-6"
};
function resolveTheme(input) {
  if (!input) return { name: "light", tokens: lightTheme };
  if (typeof input === "string") {
    const key = input.trim().toLowerCase();
    if (key.startsWith("{")) {
      try {
        return { name: "custom", tokens: mergeTheme(JSON.parse(input)) };
      } catch {
        return { name: "light", tokens: lightTheme };
      }
    }
    const canonical = canonicalThemeName(key);
    const tokens = findThemeByName(canonical);
    if (tokens) return { name: canonical, tokens };
    return { name: "light", tokens: lightTheme };
  }
  return { name: "custom", tokens: mergeTheme(input) };
}
function sanitiseThemeTokens(input) {
  if (!input || typeof input !== "object" || Array.isArray(input)) return {};
  const out = {};
  for (const [key, value] of Object.entries(input)) {
    if (!(key in TOKEN_TO_CSS)) continue;
    if (value == null) continue;
    if (typeof value === "string") {
      if (value.trim() === "") continue;
      out[key] = value;
    } else if (typeof value === "number") {
      out[key] = String(value);
    }
  }
  return out;
}
function mergeTheme(partial) {
  return { ...lightTheme, ...partial };
}
const snippetCatalog = [
  {
    name: "App",
    description: "Top-level `$app(...)` root — every program needs one.",
    template: '$app(Column([\n  ${1:Card([CardHeader("${2:Hello}")])}\n]))'
  },
  {
    name: "Card",
    description: "Card with header + body block.",
    template: 'card${1} = Card([\n  CardHeader("${2:Title}", { subtitle: "${3:Subtitle}" }),\n  Column([\n    Text("${4:Body copy goes here.}")\n  ])\n])'
  },
  {
    name: "Row",
    description: "Horizontal toolbar — label on the left, actions pushed to the right.",
    template: 'toolbar${1} = Row([\n  Text("${2:Section title}", { variant: "large-heavy" }),\n  Spacer(),\n  Button("${3:Action}", { variant: "primary" })\n], { gap: "md" })'
  },
  {
    name: "Center",
    description: "Center content on both axes inside a tall region.",
    template: 'empty${1} = Center([\n  EmptyState("${2:Nothing here yet}", { description: "${3:Create your first item to get started.}" })\n], { minHeight: "${4:50vh}" })'
  },
  {
    name: "Hero",
    description: "Landing-page hero with eyebrow, title, subtitle, and a CTA.",
    template: 'function ${4:start}() { route.navigate("/start") }\nfunction ${5:openDemo}() { route.navigate("/demo") }\nhero${1} = Hero(\n  "${2:Ship faster}",\n  { subtitle: "${3:From idea to production in minutes.}",\n    primary: Button("${6:Get started}", { action: ${4:start}, variant: "primary" }),\n    secondary: Button("${7:Live demo}", { action: ${5:openDemo}, variant: "ghost" }),\n    eyebrow: "${8:v2 launch}" }\n)'
  },
  {
    name: "PageHeader",
    description: "Dashboard page header with breadcrumbs and actions.",
    template: 'function ${6:runAction}() { /* TODO: implement */ }\nheader${1} = PageHeader(\n  "${2:Page title}",\n  { subtitle: "${3:Subtitle / meta line}",\n    breadcrumbs: ["${4:Workspace}", "${5:Section}"],\n    actions: [Button("${7:Action}", { action: ${6:runAction}, variant: "primary" })],\n    status: Badge("${8:On track}", { variant: "success" }) }\n)'
  },
  {
    name: "Stats",
    description: "Responsive KPI strip with four StatCards.",
    template: 'metrics${1} = Stats([\n  StatCard("${2:Active}", { value: "${3:12}", trend: "flat" }),\n  StatCard("${4:At risk}", { value: "${5:4}", trend: "up", delta: "+2" }),\n  StatCard("${6:Shipped}", { value: "${7:8}", trend: "up", delta: "+3" }),\n  StatCard("${8:On-time}", { value: "${9:87%}", trend: "down", delta: "-3%" })\n])'
  },
  {
    name: "GridLayout",
    description: "12-column sidebar layout with named GridItem spans.",
    template: 'sidebar${1} = Card([CardHeader("${2:Sidebar}")])\nmain${1}    = Card([CardHeader("${3:Main}")])\n$app(Grid([\n  GridItem(sidebar${1}, { span: "1/4" }),\n  GridItem(main${1}, { span: "3/4" })\n], { columns: 12, gap: "lg" }))'
  },
  {
    name: "KanbanBoard",
    description: "Three-column kanban board with sample cards.",
    template: 'board${1} = KanbanBoard([\n  KanbanColumn("To do", { items: [\n    KanbanCard("${2:Migrate auth}", { description: "${3:Roll out the new SDK.}", tags: ["auth"], assignee: "${4:Asha}" })\n  ] }),\n  KanbanColumn("Doing", { items: [\n    KanbanCard("${5:Streaming UI v2}", { description: "${6:Ship 20 new components.}", tags: ["frontend"], assignee: "${7:Alex}", tone: "primary" })\n  ] }),\n  KanbanColumn("Done", { items: [\n    KanbanCard("${8:Activity timeline}", { description: "${9:Shipped to 100%.}", tags: ["shipped"], assignee: "${10:Mira}", tone: "success" })\n  ] })\n])'
  },
  {
    name: "FollowUpBlock",
    description: "Chat follow-up prompts.",
    template: 'follow${1} = FollowUpBlock([\n  FollowUpItem("${2:Show me a chart}"),\n  FollowUpItem("${3:Add a filter}"),\n  FollowUpItem("${4:Export as CSV}")\n])'
  },
  {
    name: "Router",
    description: "Multi-page router via $router({…}) with NavLink sidebar.",
    template: 'pages = $router({\n  "/":            HomePage(),\n  "/dashboard":   DashboardPage(),\n  "/users/:id":   UserPage(params.id),\n  "/docs/*":      DocsPage(params._),\n  default:        NotFoundPage()\n})\n\nfunction HomePage() {\n  return Card([CardHeader("Welcome")])\n}\nfunction DashboardPage() {\n  return Card([CardHeader("Dashboard")])\n}\nfunction UserPage(id) {\n  return Card([CardHeader(`User ${id}`)])\n}\nfunction DocsPage(rest) {\n  return Card([CardHeader(`Docs · ${rest}`)])\n}\nfunction NotFoundPage() {\n  return Callout("Not found", { variant: "warning", description: `We couldn\'t find ${route.path}.` })\n}\n\nnav${1} = Row([\n  NavLink("Home",      { to: "/",          variant: "ghost", exact: true }),\n  NavLink("Dashboard", { to: "/dashboard", variant: "ghost" }),\n  NavLink("Users",     { to: "/users",     variant: "ghost" })\n], { gap: "sm" })'
  },
  {
    name: "Effect",
    description: "Lifecycle-managed effect (clock/interval) with cleanup.",
    template: '$${3:now} = ""\n\n$effect(() => {\n  let id = setInterval(() => { $${3:now} = new Date().toISOString() }, 1000)\n  cleanup(() => clearInterval(id))\n}, ["mount"])\n\nbody = Text($${3:now})'
  },
  {
    name: "Action",
    description: "Action declaration that POSTs through the $http() builtin.",
    template: '$${1:items} = []\n\nfunction add(text) {\n  $${1:items} = [...$${1:items}, { id: $${1:items}.length + 1, text: text }]\n  $response = $http({ url: "https://api.example.com/save", method: "POST", body: { item: { text: text } } })\n}'
  },
  {
    name: "MapList",
    description: "Map an array of items into a list of components using `.map`.",
    template: "list${1} = $items.map(item => Card([Text(item.${2:name})]))"
  },
  {
    name: "ForLoop",
    description: "Iterate over an array statement-style — usable in a function body.",
    template: "function build${1:List}() {\n  let out = []\n  for (let item of $items) {\n    out.push(Card([Text(item.${2:name})]))\n  }\n  return Column(out)\n}"
  },
  {
    name: "FormReactive",
    description: "Two-way bound input with submit action.",
    template: '$draft = ""\n$items = []\n\nfunction add() {\n  $items = [...$items, { id: $items.length + 1, text: $draft }]\n  $draft = ""\n}\n\nform${1} = Column([\n  Input("${2:draft}", { placeholder: "What needs doing?", type: "text", value: $draft }),\n  Button("Add", { action: add, variant: "primary" })\n])'
  },
  {
    name: "Theme",
    description: "Brand-style theme override applied on top of the base theme.",
    template: '$theme({\n  name: "${1:brand}",\n  colors: {\n    primary:      "${2:#0969da}",\n    primaryHover: "${3:#0860c4}",\n    bg:           "${4:#ffffff}",\n    text:         "${5:#1f2328}"\n  },\n  radius: { button: "${6:6px}", input: "${7:6px}" },\n  font: { family: "${8:-apple-system, BlinkMacSystemFont, sans-serif}" }\n})'
  },
  {
    name: "Component",
    description: "Reusable component declaration — first-letter case is not significant; a function with no `return` simply renders nothing.",
    template: "function ${1:UserCard}(${2:user}) {\n  return Card([\n    Avatar(${2:user}.name),\n    Text(${2:user}.role)\n  ])\n}\n\nlist = $users.map(u => ${1:UserCard}(u))"
  },
  {
    name: "Ternary",
    description: "Pick between two expressions — the JS way to express a value-producing if.",
    template: "body${1} = ${2:condition} ? ${3:trueBranch} : ${4:falseBranch}"
  },
  {
    name: "IfStatement",
    description: "Imperative `if (cond) { … } else { … }` — usable inside function / effect bodies.",
    template: "if (${1:condition}) {\n  ${2:// then branch}\n} else {\n  ${3:// else branch}\n}"
  },
  {
    name: "SwitchStatement",
    description: "Switch on a value with case/default/break — statement form.",
    template: 'switch (${1:$tab}) {\n  case "overview": ${2:// …}; break\n  case "billing":  ${3:// …}; break\n  case "security": ${4:// …}; break\n  default: ${5:// …}\n}'
  },
  {
    name: "TemplateLiteral",
    description: "Template literal with `${}` interpolation.",
    template: "greeting${1} = `Hello ${${2:$user.name}}, you have ${${3:$messages.length}} messages`"
  },
  {
    name: "Http",
    description: "Fire an $http() request and bind the reactive resource bag.",
    template: '$${1:response} = $http({\n  url: "${2:https://api.example.com/items}",\n  method: "${3:GET}",\n  headers: { "Content-Type": "application/json" }\n})'
  },
  {
    name: "State",
    description: "Reactive state atom — declared with `$name = value`.",
    template: '$${1:name} = ${2:"default"}'
  },
  {
    name: "Store",
    description: "Global store — colocated state + actions shared across components (like Zustand/Pinia).",
    template: "${1:cart} = $store({\n  ${2:items}: [],\n  ${3:count}: (s) => s.${2:items}.length,\n  ${4:add}: (s, item) => { s.${2:items} = [...s.${2:items}, item] },\n})"
  },
  {
    name: "useState",
    description: "`$state` hook — per-instance state with a [value, setValue] pair (like React's useState).",
    template: 'const [${1:value}, set${2:Value}] = $state(${3:"default"})'
  },
  {
    name: "useMemo",
    description: "`$memo` hook — value recomputed only when a dependency changes (like React's useMemo).",
    template: "const ${1:memoized} = $memo(() => ${2:compute}, [${3:deps}])"
  },
  {
    name: "Hook",
    description: "Custom hook declaration — a `$`-prefixed function that composes $state / $memo.",
    template: "function $use${1:Counter}(${2:start}) {\n  const [${3:count}, set${4:Count}] = $state(${2:start})\n  return { ${3:count}: ${3:count}, increment: () => set${4:Count}(v => v + 1) }\n}"
  },
  {
    name: "ResponsiveGrid",
    description: "Grid with a responsive column map per breakpoint.",
    template: 'cards${1} = Grid(${2:items}, { columns: {sm: 1, md: 2, lg: 4}, gap: "${3:lg}" })'
  },
  // ── New feature snippets ──────────────────────────────────────────────────
  {
    name: "Query",
    description: "$query — cached + deduplicated read with TTL and polling.",
    template: '${1:$data} = $query({\n  url: "${2:https://api.example.com/items}",\n  key: "${3:items}",\n  ttl: ${4:30000}\n})'
  },
  {
    name: "InfiniteQuery",
    description: "$query infinite mode — paginated list with .loadMore() and .hasMore.",
    template: '${1:$feed} = $query({\n  url: "${2:https://api.example.com/posts}",\n  key: "${3:feed}",\n  infinite: { param: "${4:page}", limit: ${5:20}, mode: "${6:page}", select: b => b.${7:items} }\n})\n\nButton("Load more", { onClick: () => ${1:$feed}.loadMore(), disabled: !${1:$feed}.hasMore })'
  },
  {
    name: "Mutation",
    description: "$mutation — deferred write with optimistic update and cache invalidation.",
    template: '${1:$save} = $mutation({\n  url: "${2:https://api.example.com/items}",\n  optimistic: (o) => { ${3:$items} = [...${3:$items}, o.body] },\n  invalidates: ["${4:items}"]\n})\n\nButton("${5:Save}", { onClick: () => ${1:$save}.mutate({ body: { ${6:title}: ${7:$title} } }) })'
  },
  {
    name: "Form",
    description: "$form — managed form with $util.rules validators and handleSubmit().",
    template: '${1:form} = $form({\n  values: { ${2:email}: "", ${3:password}: "" },\n  rules: {\n    ${2:email}:    [$util.rules.required(), $util.rules.email()],\n    ${3:password}: [$util.rules.required(), $util.rules.minLength(8)]\n  },\n  onSubmit: (v) => { ${4:// handle submit} }\n})\n\nColumn([\n  Input("${2:email}", { label: "${5:Email}", value: ${1:form}.values.${2:email}, error: ${1:form}.errors.${2:email}, onBlur: () => ${1:form}.touch("${2:email}") }),\n  Button("${6:Submit}", { onClick: () => ${1:form}.handleSubmit(), disabled: ${1:form}.submitting })\n])'
  },
  {
    name: "Socket",
    description: "$socket — reactive WebSocket bag (.connected / .last / .messages / .send()).",
    template: '${1:$chat} = $socket({\n  url: "${2:wss://example.com/room/42}",\n  bufferSize: ${3:50}\n})\n\nButton("Send", { onClick: () => ${1:$chat}.send({ text: ${4:$draft} }) })'
  },
  {
    name: "Sse",
    description: "$sse — reactive Server-Sent Events stream (.connected / .last / .messages).",
    template: '${1:$stream} = $sse({\n  url: "${2:https://api.example.com/events}",\n  event: "${3:message}"\n})'
  },
  {
    name: "Persist",
    description: "$store with persist + history — survives reload with undo/redo.",
    template: '${1:doc} = $store({\n  persist: "${2:my-doc}",\n  history: true,\n  ${3:title}: "",\n  set${4:Title}: (s, v) => { s.${3:title} = v }\n})\n\nButton("Undo", { onClick: () => ${1:doc}.undo(), disabled: !${1:doc}.canUndo })'
  },
  {
    name: "SxCard",
    description: "Card using the sx styling channel — responsive, interaction states, gradient.",
    template: 'Card([Text("${1:Content}")], {\n  sx: {\n    p: "${2:lg}",\n    radius: "${3:lg}",\n    bg: "${4:surface}",\n    shadow: "${5:md}",\n    states: { hover: { scale: ${6:1.03}, shadow: "${7:lg}" } }\n  }\n})'
  },
  {
    name: "SxResponsive",
    description: "sx with responsive breakpoint maps + logical spacing (RTL-safe).",
    template: 'Box([${1:children}], {\n  sx: {\n    p: { base: "${2:md}", md: "${3:xl}" },\n    direction: { base: "column", lg: "row" },\n    px: "${4:lg}",\n    fontSize: "${5:lg}",\n    weight: "${6:600}"\n  }\n})'
  },
  {
    name: "FormAsync",
    description: "$form with an async uniqueness rule + validating/submitting states.",
    template: '${1:form} = $form({\n  values: { ${2:user}: "" },\n  rules: { ${2:user}: [$util.rules.required(), $util.rules.asyncCustom((v) => ${3:checkAvailable}(v), "${4:Already taken}")] },\n  onSubmit: (values) => ${5:save}(values)\n})\n\nColumn([\n  Input("${2:user}", { value: ${1:form}.values.${2:user}, error: ${1:form}.errors.${2:user}, label: "${6:Username}", onBlur: () => ${1:form}.touch("${2:user}") }),\n  Button("Submit", { onClick: () => ${1:form}.submit(), disabled: ${1:form}.submitting || ${1:form}.validating, loading: ${1:form}.submitting })\n])'
  },
  {
    name: "SocketReconnect",
    description: "$socket with auto-reconnect + status-driven UI.",
    template: '${1:$chat} = $socket({ url: "${2:wss://example.com/room}", reconnect: true })\n\nColumn([\n  Badge(${1:$chat}.status, { tone: ${1:$chat}.status == "open" ? "success" : "warning" }),\n  List(${1:$chat}.messages.map((m) => ListItem(m.text))),\n  Row([\n    Input("draft", { value: ${3:$draft} }),\n    Button("Send", { onClick: () => { ${1:$chat}.send({ text: ${3:$draft} }); ${3:$draft} = "" } })\n  ], { gap: "sm" })\n])'
  },
  {
    name: "MarketingSection",
    description: "Landing-page band: Section + eyebrow/title/subtitle + Metric strip.",
    template: 'Section([\n  MetricStrip([\n    Metric("${1:170+}", { label: "${2:Components}", countUp: true }),\n    Metric("${3:7}", { label: "${4:Themes}", countUp: true })\n  ], { columns: ${5:2} })\n], {\n  eyebrow: "${6:Why teams switch}",\n  title: "${7:Everything a frontend needs}",\n  subtitle: "${8:Components, state, routing, theming and data — built in.}",\n  background: "${9:soft}", align: "center"\n})'
  },
  {
    name: "ConfirmDanger",
    description: "ConfirmDialog gated destructive action (Escape cancels, focus lands on Cancel).",
    template: '$confirmOpen = false\n\nColumn([\n  Button("${1:Delete}", { tone: "danger", onClick: () => { $confirmOpen = true } }),\n  ConfirmDialog("${2:Delete item?}", {\n    open: $confirmOpen,\n    message: "${3:This cannot be undone.}",\n    tone: "danger",\n    onConfirm: () => ${4:remove}(),\n    onCancel: () => {}\n  })\n])'
  }
];
function getSnippets() {
  return snippetCatalog;
}
function getLanguageSpec(library = defaultLibrary) {
  const components2 = getComponentCatalog(library);
  return {
    grammar: grammarSpec,
    tokenizer: createStreamTokenizer(grammarSpec),
    tagMap: defaultTagMap,
    components: components2,
    componentsByName: indexCatalog(components2),
    builtins: builtinCatalog,
    snippets: getSnippets(),
    keywordDocs,
    themeNames: Object.keys(builtInThemes),
    severityTokenMap: { "parse-error": "error" },
    iconAliases: COMMON_ICON_ALIASES
  };
}
const COMMON_ICON_ALIASES = [
  "house",
  "user",
  "users",
  "gear",
  "bell",
  "envelope",
  "magnifying-glass",
  "plus",
  "minus",
  "pen",
  "trash",
  "check",
  "xmark",
  "arrow-right",
  "arrow-left",
  "arrow-up",
  "arrow-down",
  "chevron-right",
  "chevron-left",
  "chevron-down",
  "chevron-up",
  "ellipsis",
  "ellipsis-vertical",
  "star",
  "heart",
  "bookmark",
  "calendar",
  "clock",
  "file",
  "folder",
  "image",
  "video",
  "music",
  "chart-line",
  "chart-bar",
  "chart-pie",
  "gauge",
  "table",
  "circle-info",
  "circle-check",
  "circle-exclamation",
  "circle-question",
  "triangle-exclamation",
  "shield",
  "lock",
  "unlock",
  "key",
  "rocket",
  "bolt",
  "fire",
  "snowflake",
  "sun",
  "moon",
  "cloud",
  "code",
  "terminal",
  "database",
  "server",
  "globe",
  "link",
  "share",
  "download",
  "upload",
  "filter",
  "sort",
  "list",
  "grid",
  "columns",
  "sidebar",
  "bars",
  "tag",
  "tags",
  "flag",
  "trophy",
  "medal",
  "gift",
  "cart-shopping",
  "credit-card",
  "money-bill",
  "receipt",
  "wallet",
  "comment",
  "comments",
  "paper-plane",
  "inbox",
  "paper-clip",
  "phone",
  "video-camera",
  "play",
  "pause",
  "stop",
  "forward",
  "backward",
  "expand",
  "compress",
  "eye",
  "eye-slash",
  "thumbs-up",
  "thumbs-down",
  "thumbtack"
];
const INDENT = "  ";
const SAFE_IDENT = /^[A-Za-z_$][A-Za-z0-9_$]*$/;
const NEEDS_DOUBLE_QUOTE = /[\\"]/;
function printPattern(pattern, indent) {
  const open = pattern.kind === "array" ? "[" : "{";
  const close = pattern.kind === "array" ? "]" : "}";
  const parts = pattern.bindings.map((b) => {
    const lead = b.rest ? "..." : "";
    const target = b.pattern ? pattern.kind === "object" && b.sourceKey ? `${b.sourceKey}: ${printPattern(b.pattern, indent)}` : printPattern(b.pattern, indent) : b.sourceKey ? `${b.sourceKey}: ${b.name}` : b.name || "";
    const def = b.defaultValue ? ` = ${printExpression(b.defaultValue, indent)}` : "";
    return `${lead}${target}${def}`;
  });
  return `${open}${parts.join(", ")}${close}`;
}
function formatProgram(source) {
  const program = parse(source);
  if (program.errors.length > 0) {
    return { formatted: source, errors: [...program.errors] };
  }
  const out = printProgram(program);
  const second = parse(out);
  if (second.errors.length > 0) {
    return { formatted: source, errors: [] };
  }
  return { formatted: out, errors: [] };
}
function printProgram(program) {
  const lines = [];
  let prev = null;
  for (const stmt of program.statements) {
    if (prev && needsBlankLineBetween(prev, stmt)) lines.push("");
    lines.push(printStatement(stmt, 0));
    prev = stmt;
  }
  return lines.join("\n") + "\n";
}
function needsBlankLineBetween(prev, next) {
  const heavy = /* @__PURE__ */ new Set([
    "ComponentDeclaration",
    "EffectDeclaration",
    "ActionDeclaration",
    "HookDeclaration"
  ]);
  if (heavy.has(prev.kind) || heavy.has(next.kind)) return true;
  return false;
}
function printStatement(stmt, indent) {
  const pad = INDENT.repeat(indent);
  const exp = "exported" in stmt && stmt.exported ? "export " : "";
  switch (stmt.kind) {
    case "Import": {
      const specs = stmt.specifiers.map((s) => {
        const imported = s.isState ? `$${s.imported}` : s.imported;
        if (s.local === s.imported) return imported;
        const local = s.isState ? `$${s.local}` : s.local;
        return `${imported} as ${local}`;
      }).join(", ");
      return `${pad}import { ${specs} } from "${stmt.source}"`;
    }
    case "Assignment": {
      const lhs = stmt.isState ? `$${stmt.identifier}` : stmt.identifier;
      const expr = printExpression(stmt.expression, indent);
      return `${pad}${exp}${lhs} = ${expr}`;
    }
    case "ComponentDeclaration": {
      const params = stmt.params.map(printDeclParam).join(", ");
      const head = `${pad}${exp}function ${stmt.name}(${params}) {`;
      const body = printBlock(stmt.body.body, indent + 1);
      return body.length > 0 ? `${head}
${body}
${pad}}` : `${head}
${pad}}`;
    }
    case "EffectDeclaration": {
      const deps = stmt.triggers.map(printTrigger).filter((s) => s.length > 0);
      if (stmt.rateLimit) {
        deps.push(`"${stmt.rateLimit.kind}(${stmt.rateLimit.ms})"`);
      }
      const body = printBlock(stmt.body.body, indent + 1);
      const depsArray = `[${deps.join(", ")}]`;
      return `${pad}$effect(() => {
${body}
${pad}}, ${depsArray})`;
    }
    case "ActionDeclaration": {
      const params = stmt.params.map(printDeclParam).join(", ");
      const head = `${pad}${exp}function ${stmt.name}(${params}) {`;
      const body = printBlock(stmt.body.body, indent + 1);
      return `${head}
${body}
${pad}}`;
    }
    case "HookDeclaration": {
      const params = stmt.params.map(printDeclParam).join(", ");
      const head = `${pad}${exp}function $${stmt.name}(${params}) {`;
      const body = printBlock(stmt.body.body, indent + 1);
      return `${head}
${body}
${pad}}`;
    }
    case "Await": {
      return `${pad}await ${printExpression(stmt.argument, indent)}`;
    }
    case "Return": {
      return stmt.argument ? `${pad}return ${printExpression(stmt.argument, indent)}` : `${pad}return`;
    }
    case "ExpressionStatement": {
      return `${pad}${printExpression(stmt.expression, indent)}`;
    }
    case "IfStatement": {
      const test = printExpression(stmt.test, indent);
      const cons = `{
${printBlock(stmt.consequent.body, indent + 1)}
${pad}}`;
      if (!stmt.alternate) return `${pad}if (${test}) ${cons}`;
      const alt = stmt.alternate.kind === "IfStatement" ? printStatement(stmt.alternate, indent).trimStart() : `{
${printBlock(stmt.alternate.body, indent + 1)}
${pad}}`;
      return `${pad}if (${test}) ${cons} else ${alt}`;
    }
    case "SwitchStatement": {
      const disc = printExpression(stmt.discriminant, indent);
      const cases = stmt.cases.map((c) => printSwitchCase(c, indent + 1)).join("\n");
      return `${pad}switch (${disc}) {
${cases}
${pad}}`;
    }
    case "ForOfStatement": {
      const iter = printExpression(stmt.iterable, indent);
      const body = `{
${printBlock(stmt.body.body, indent + 1)}
${pad}}`;
      const binding = stmt.pattern ? printPattern(stmt.pattern, indent) : stmt.item;
      return `${pad}for (let ${binding} of ${iter}) ${body}`;
    }
    case "ForClassicStatement": {
      const init = stmt.init ? printStatement(stmt.init, 0).trimStart() : "";
      const test = stmt.test ? printExpression(stmt.test, indent) : "";
      const update = stmt.update ? printExpression(stmt.update, indent) : "";
      const body = `{
${printBlock(stmt.body.body, indent + 1)}
${pad}}`;
      return `${pad}for (${init}; ${test}; ${update}) ${body}`;
    }
    case "WhileStatement": {
      const test = printExpression(stmt.test, indent);
      const body = `{
${printBlock(stmt.body.body, indent + 1)}
${pad}}`;
      return `${pad}while (${test}) ${body}`;
    }
    case "DoWhileStatement": {
      const test = printExpression(stmt.test, indent);
      const body = `{
${printBlock(stmt.body.body, indent + 1)}
${pad}}`;
      return `${pad}do ${body} while (${test})`;
    }
    case "ForInStatement": {
      const iter = printExpression(stmt.iterable, indent);
      const body = `{
${printBlock(stmt.body.body, indent + 1)}
${pad}}`;
      return `${pad}for (let ${stmt.item} in ${iter}) ${body}`;
    }
    case "DestructureStatement": {
      const pattern = printPattern({ kind: stmt.patternKind, bindings: stmt.bindings }, indent);
      const expr = printExpression(stmt.expression, indent);
      return `${pad}let ${pattern} = ${expr}`;
    }
    case "BreakStatement":
      return `${pad}break`;
    case "ContinueStatement":
      return `${pad}continue`;
    case "ThrowStatement":
      return `${pad}throw ${printExpression(stmt.argument, indent)}`;
    case "TryStatement": {
      const block = `{
${printBlock(stmt.block.body, indent + 1)}
${pad}}`;
      let out = `${pad}try ${block}`;
      if (stmt.catchBlock) {
        const catchHead = stmt.catchParam ? ` (${stmt.catchParam})` : "";
        const catchBody = `{
${printBlock(stmt.catchBlock.body, indent + 1)}
${pad}}`;
        out += ` catch${catchHead} ${catchBody}`;
      }
      if (stmt.finallyBlock) {
        const finBody = `{
${printBlock(stmt.finallyBlock.body, indent + 1)}
${pad}}`;
        out += ` finally ${finBody}`;
      }
      return out;
    }
  }
}
function printDeclParam(p) {
  if (p.defaultValue) {
    return `${p.name} = ${printExpression(p.defaultValue, 0)}`;
  }
  return p.name;
}
function printTrigger(t) {
  if (t.kind === "lifecycle") return `"${t.name}"`;
  if (t.kind === "every") return `"every(${t.intervalMs})"`;
  if (t.kind === "state") return `$${t.name}`;
  return "";
}
function printBlock(stmts, indent) {
  return stmts.map((s) => printStatement(s, indent)).join("\n");
}
function printExpression(expr, indent) {
  switch (expr.kind) {
    case "Literal":
      return printLiteral(expr.value);
    case "Identifier":
      return expr.name;
    case "StateRef":
      return `$${expr.name}`;
    case "Array": {
      if (expr.elements.length === 0) return "[]";
      const items = expr.elements.map((e) => printExpression(e, indent));
      const inline = `[${items.join(", ")}]`;
      if (inline.length <= 80 && !items.some((s) => s.includes("\n"))) return inline;
      const pad = INDENT.repeat(indent + 1);
      return `[
${items.map((s) => `${pad}${s}`).join(",\n")}
${INDENT.repeat(indent)}]`;
    }
    case "Object": {
      if (expr.properties.length === 0) return "{}";
      const items = expr.properties.map((p) => printObjectProp(p, indent));
      const inline = `{ ${items.join(", ")} }`;
      if (inline.length <= 80 && !items.some((s) => s.includes("\n"))) return inline;
      const pad = INDENT.repeat(indent + 1);
      return `{
${items.map((s) => `${pad}${s}`).join(",\n")}
${INDENT.repeat(indent)}}`;
    }
    case "Member": {
      const obj = printExpression(expr.object, indent);
      const dot = expr.optional ? "?." : ".";
      if (expr.property) return `${obj}${dot}${expr.property}`;
      if (expr.computed) {
        const inner = printExpression(expr.computed, indent);
        return expr.optional ? `${obj}?.[${inner}]` : `${obj}[${inner}]`;
      }
      return obj;
    }
    case "Unary":
      return `${expr.operator}${printExpression(expr.argument, indent)}`;
    case "Binary":
      return `${printExpression(expr.left, indent)} ${expr.operator} ${printExpression(expr.right, indent)}`;
    case "Ternary":
      return `${printExpression(expr.test, indent)} ? ${printExpression(expr.consequent, indent)} : ${printExpression(expr.alternate, indent)}`;
    case "Call":
      return printCall(expr.callee, expr.arguments, indent);
    case "MethodCall": {
      const target = printExpression(expr.object, indent);
      const sep = expr.optional ? "?." : ".";
      return printCall(`${target}${sep}${expr.method}`, expr.arguments, indent);
    }
    case "Invoke": {
      const callee = printExpression(expr.callee, indent);
      const sep = expr.optional ? "?." : "";
      return printCall(`${callee}${sep}`, expr.arguments, indent);
    }
    case "New": {
      const callee = printExpression(expr.callee, indent);
      return `new ${printCall(callee, expr.arguments, indent)}`;
    }
    case "BuiltinCall":
      return printCall(`@${expr.name}`, expr.arguments, indent);
    case "Template":
      return printTemplate(expr.quasis, expr.expressions, indent);
    case "Spread":
      return `...${printExpression(expr.argument, indent)}`;
    case "Lambda": {
      const params = expr.params.map((p) => {
        const prefix = p.rest ? "..." : "";
        return p.defaultValue ? `${prefix}${p.name} = ${printExpression(p.defaultValue, indent)}` : `${prefix}${p.name}`;
      }).join(", ");
      const head = expr.params.length === 1 && !expr.params[0].defaultValue && !expr.params[0].rest ? expr.params[0].name : `(${params})`;
      return `${head} => ${printExpression(expr.body, indent)}`;
    }
    case "Block":
      return `{
${printBlock(expr.body, indent + 1)}
${INDENT.repeat(indent)}}`;
  }
}
function printCall(callee, args, indent) {
  if (args.length === 0) return `${callee}()`;
  const parts = args.map((a) => printExpression(a, indent));
  const inline = `${callee}(${parts.join(", ")})`;
  if (inline.length <= 80 && !parts.some((s) => s.includes("\n"))) return inline;
  const pad = INDENT.repeat(indent + 1);
  return `${callee}(
${parts.map((s) => `${pad}${s}`).join(",\n")}
${INDENT.repeat(indent)})`;
}
function printSwitchCase(c, indent) {
  const pad = INDENT.repeat(indent);
  const body = c.body.map((s) => printStatement(s, indent + 1)).join("\n");
  if (c.test === null) {
    return `${pad}default:
${body}
${printStatement({ kind: "ExpressionStatement", expression: { kind: "Identifier", name: "break" } }, indent + 1)}`;
  }
  return `${pad}case ${printExpression(c.test, indent)}:
${body}
${INDENT.repeat(indent + 1)}break`;
}
function printObjectProp(prop2, indent) {
  if (prop2.spread) return `...${printExpression(prop2.value, indent)}`;
  const value = printExpression(prop2.value, indent);
  if (prop2.value.kind === "Identifier" && prop2.value.name === prop2.key && SAFE_IDENT.test(prop2.key)) {
    return prop2.key;
  }
  const key = SAFE_IDENT.test(prop2.key) ? prop2.key : printStringLiteral(prop2.key);
  return `${key}: ${value}`;
}
function printLiteral(value) {
  if (value === null) return "null";
  if (typeof value === "string") return printStringLiteral(value);
  if (typeof value === "boolean") return value ? "true" : "false";
  return String(value);
}
function printStringLiteral(value) {
  if (NEEDS_DOUBLE_QUOTE.test(value)) {
    const escaped = value.replace(/\\/g, "\\\\").replace(/"/g, '\\"');
    return `"${escaped}"`;
  }
  return `"${value}"`;
}
function printTemplate(quasis, expressions, indent) {
  const parts = [];
  for (let i = 0; i < quasis.length; i += 1) {
    parts.push(quasis[i] ?? "");
    if (i < expressions.length) {
      parts.push("${");
      parts.push(printExpression(expressions[i], indent));
      parts.push("}");
    }
  }
  return `\`${parts.join("")}\``;
}
function moduleLocalSymbol(moduleId, name) {
  return `__a${moduleId}_${name}`;
}
function linkProgram(entrySource, entryPath, resolver) {
  const modules = /* @__PURE__ */ new Map();
  const order = [];
  const visiting = /* @__PURE__ */ new Set();
  const diagnostics = [];
  let nextId = 0;
  const fail = (path, line, column, message) => {
    diagnostics.push({
      line,
      column,
      message: path === entryPath ? message : `${path}: ${message}`,
      severity: "error"
    });
  };
  function load(path, sourceOverride) {
    const existing = modules.get(path);
    if (existing) return existing;
    if (visiting.has(path)) return void 0;
    visiting.add(path);
    let src;
    if (sourceOverride !== null) {
      src = sourceOverride;
    } else {
      try {
        src = resolver.load(path);
      } catch {
        visiting.delete(path);
        fail(entryPath, 0, 0, `Failed to load imported module "${path}".`);
        return void 0;
      }
    }
    const program2 = parse(src);
    for (const e of program2.errors) fail(path, e.line, e.column, e.message);
    const rec = {
      id: nextId++,
      path,
      program: program2,
      edges: [],
      declaredPlain: /* @__PURE__ */ new Set(),
      declaredState: /* @__PURE__ */ new Set(),
      exportedPlain: /* @__PURE__ */ new Set(),
      exportedState: /* @__PURE__ */ new Set(),
      renamePlain: /* @__PURE__ */ new Map(),
      renameState: /* @__PURE__ */ new Map()
    };
    modules.set(path, rec);
    buildSymbolTable(rec);
    for (const stmt of program2.statements) {
      if (stmt.kind !== "Import") continue;
      const resolved = resolver.resolve(stmt.source, path);
      rec.edges.push({ stmt, resolvedPath: resolved });
      if (resolved === null) {
        fail(path, stmt.loc?.line ?? 0, stmt.loc?.column ?? 0, `Cannot resolve import "${stmt.source}".`);
        continue;
      }
      load(resolved, null);
    }
    visiting.delete(path);
    order.push(path);
    return rec;
  }
  load(entryPath, entrySource);
  for (const rec of modules.values()) {
    if (rec.path === entryPath) continue;
    for (const name of rec.declaredPlain) rec.renamePlain.set(name, moduleLocalSymbol(rec.id, name));
    for (const name of rec.declaredState) rec.renameState.set(name, moduleLocalSymbol(rec.id, name));
  }
  for (const rec of modules.values()) {
    for (const { stmt, resolvedPath } of rec.edges) {
      if (resolvedPath === null) continue;
      const src = modules.get(resolvedPath);
      if (!src) continue;
      for (const spec of stmt.specifiers) {
        const line = stmt.loc?.line ?? 0;
        const column = stmt.loc?.column ?? 0;
        if (spec.isState) {
          if (!src.exportedState.has(spec.imported)) {
            fail(rec.path, line, column, `"${stmt.source}" does not export \`$${spec.imported}\`.`);
            continue;
          }
          rec.renameState.set(spec.local, src.renameState.get(spec.imported) ?? moduleLocalSymbol(src.id, spec.imported));
        } else {
          if (!src.exportedPlain.has(spec.imported)) {
            fail(rec.path, line, column, `"${stmt.source}" does not export \`${spec.imported}\`.`);
            continue;
          }
          rec.renamePlain.set(spec.local, src.renamePlain.get(spec.imported) ?? moduleLocalSymbol(src.id, spec.imported));
        }
      }
    }
  }
  const merged = [];
  const sources = [entryPath];
  const sourceIndex = /* @__PURE__ */ new Map([[entryPath, 0]]);
  for (const path of order) {
    if (sourceIndex.has(path)) continue;
    sourceIndex.set(path, sources.length);
    sources.push(path);
  }
  const multiModule = sources.length > 1;
  for (const path of order) {
    const rec = modules.get(path);
    const renamer = makeRenamer(rec);
    const index = sourceIndex.get(path);
    for (const stmt of rec.program.statements) {
      if (stmt.kind === "Import") continue;
      renamer.renameTopLevel(stmt);
      stripExported(stmt);
      if (multiModule) stampSourceIndex(stmt, index);
      if (stmt.kind === "EffectDeclaration" && rec.path !== entryPath) {
        stmt.name = `__effect_a${rec.id}_${stmt.name.replace(/^__effect_/, "")}`;
      }
      merged.push(stmt);
    }
  }
  const entryRec = modules.get(entryPath);
  const program = {
    statements: merged,
    errors: entryRec ? entryRec.program.errors : []
  };
  if (multiModule) program.sources = sources;
  return {
    program,
    diagnostics,
    dependencies: order.filter((p) => p !== entryPath)
  };
}
function buildSymbolTable(rec) {
  for (const stmt of rec.program.statements) {
    switch (stmt.kind) {
      case "Assignment":
        if (stmt.isState) {
          rec.declaredState.add(stmt.identifier);
          if (stmt.exported) rec.exportedState.add(stmt.identifier);
        } else {
          rec.declaredPlain.add(stmt.identifier);
          if (stmt.exported) rec.exportedPlain.add(stmt.identifier);
        }
        break;
      case "ComponentDeclaration":
      case "ActionDeclaration":
        rec.declaredPlain.add(stmt.name);
        if (stmt.exported) rec.exportedPlain.add(stmt.name);
        break;
      case "HookDeclaration":
        rec.declaredState.add(stmt.name);
        if (stmt.exported) rec.exportedState.add(stmt.name);
        break;
      case "DestructureStatement":
        for (const name of collectPatternNames({ kind: stmt.patternKind, bindings: stmt.bindings })) {
          rec.declaredPlain.add(name);
        }
        break;
    }
  }
}
function stripExported(stmt) {
  if (stmt.kind === "Assignment" || stmt.kind === "ComponentDeclaration" || stmt.kind === "ActionDeclaration" || stmt.kind === "HookDeclaration") {
    delete stmt.exported;
  }
}
function makeRenamer(rec) {
  const shadow = [];
  const shadowed = (name) => {
    for (const set of shadow) if (set.has(name)) return true;
    return false;
  };
  const rPlain = (name) => shadowed(name) ? name : rec.renamePlain.get(name) ?? name;
  const rState = (name) => rec.renameState.get(name) ?? name;
  const push = (names) => {
    shadow.push(new Set(names));
  };
  const pop = () => {
    shadow.pop();
  };
  const patternNames = (p) => collectPatternNames(p);
  const paramNames = (params) => {
    const out = [];
    for (const p of params) {
      if (p.name) out.push(p.name);
      if (p.pattern) out.push(...patternNames(p.pattern));
    }
    return out;
  };
  const renameParamDefaults = (params) => {
    for (const p of params) if (p.defaultValue) renameExpr(p.defaultValue);
  };
  function renameExpr(expr) {
    switch (expr.kind) {
      case "Literal":
        return;
      case "Identifier":
        expr.name = rPlain(expr.name);
        return;
      case "StateRef":
        expr.name = rState(expr.name);
        return;
      case "Array":
        for (const el2 of expr.elements) renameExpr(el2);
        return;
      case "Object":
        for (const prop2 of expr.properties) {
          if (prop2.computedKey) renameExpr(prop2.computedKey);
          renameExpr(prop2.value);
        }
        return;
      case "Member":
        renameExpr(expr.object);
        if (expr.computed) renameExpr(expr.computed);
        return;
      case "Unary":
        renameExpr(expr.argument);
        return;
      case "Binary":
        renameExpr(expr.left);
        renameExpr(expr.right);
        return;
      case "Ternary":
        renameExpr(expr.test);
        renameExpr(expr.consequent);
        renameExpr(expr.alternate);
        return;
      case "Call":
        expr.callee = rPlain(expr.callee);
        for (const a of expr.arguments) renameExpr(a);
        return;
      case "MethodCall":
        renameExpr(expr.object);
        for (const a of expr.arguments) renameExpr(a);
        return;
      case "Invoke":
        renameExpr(expr.callee);
        for (const a of expr.arguments) renameExpr(a);
        return;
      case "BuiltinCall":
        for (const a of expr.arguments) renameExpr(a);
        return;
      case "New":
        renameExpr(expr.callee);
        for (const a of expr.arguments) renameExpr(a);
        return;
      case "Template":
        for (const e of expr.expressions) renameExpr(e);
        return;
      case "Spread":
        renameExpr(expr.argument);
        return;
      case "Lambda":
        push(paramNames(expr.params));
        renameParamDefaults(expr.params);
        renameExpr(expr.body);
        pop();
        return;
      case "Block":
        renameBlock(expr);
        return;
    }
  }
  function renameBlock(block) {
    push([]);
    const scope = shadow[shadow.length - 1];
    for (const stmt of block.body) {
      renameStatement(stmt, false);
      addBlockLocals(stmt, scope);
    }
    pop();
  }
  function addBlockLocals(stmt, scope) {
    if (stmt.kind === "DestructureStatement") {
      for (const name of collectPatternNames({ kind: stmt.patternKind, bindings: stmt.bindings })) scope.add(name);
    } else if (stmt.kind === "ComponentDeclaration" || stmt.kind === "ActionDeclaration") {
      scope.add(stmt.name);
    }
  }
  function renameTopLevel(stmt) {
    if (stmt.kind === "DestructureStatement") {
      renameExpr(stmt.expression);
      const renamePatternBindings = (bindings) => {
        for (const b of bindings) {
          if (b.defaultValue) renameExpr(b.defaultValue);
          if (b.pattern) renamePatternBindings(b.pattern.bindings);
          else b.name = rPlain(b.name);
        }
      };
      renamePatternBindings(stmt.bindings);
      return;
    }
    renameStatement(stmt, true);
  }
  function renameStatement(stmt, topLevel) {
    switch (stmt.kind) {
      case "Import":
        return;
      case "Assignment":
        stmt.identifier = stmt.isState ? rState(stmt.identifier) : rPlain(stmt.identifier);
        renameExpr(stmt.expression);
        return;
      case "ComponentDeclaration":
      case "ActionDeclaration":
        stmt.name = rPlain(stmt.name);
        push(paramNames(stmt.params));
        renameParamDefaults(stmt.params);
        renameBlock(stmt.body);
        pop();
        return;
      case "HookDeclaration":
        stmt.name = rState(stmt.name);
        push(paramNames(stmt.params));
        renameParamDefaults(stmt.params);
        renameBlock(stmt.body);
        pop();
        return;
      case "EffectDeclaration":
        for (const t of stmt.triggers) if (t.kind === "state") t.name = rState(t.name);
        renameBlock(stmt.body);
        return;
      case "Await":
        renameExpr(stmt.argument);
        return;
      case "Return":
        if (stmt.argument) renameExpr(stmt.argument);
        return;
      case "ExpressionStatement":
        renameExpr(stmt.expression);
        return;
      case "IfStatement":
        renameExpr(stmt.test);
        renameBlock(stmt.consequent);
        if (stmt.alternate) {
          if (stmt.alternate.kind === "IfStatement") renameStatement(stmt.alternate, false);
          else renameBlock(stmt.alternate);
        }
        return;
      case "SwitchStatement":
        renameExpr(stmt.discriminant);
        for (const c of stmt.cases) {
          if (c.test) renameExpr(c.test);
          for (const s of c.body) renameStatement(s, false);
        }
        return;
      case "ForOfStatement": {
        renameExpr(stmt.iterable);
        const names = stmt.pattern ? collectPatternNames(stmt.pattern) : [stmt.item];
        push(names);
        renameBlock(stmt.body);
        pop();
        return;
      }
      case "ForInStatement":
        renameExpr(stmt.iterable);
        push([stmt.item]);
        renameBlock(stmt.body);
        pop();
        return;
      case "ForClassicStatement": {
        const initNames = [];
        if (stmt.init && stmt.init.kind === "Assignment" && stmt.init.identifier) {
          initNames.push(stmt.init.identifier);
        }
        push(initNames);
        if (stmt.init) renameStatement(stmt.init, false);
        if (stmt.test) renameExpr(stmt.test);
        if (stmt.update) renameExpr(stmt.update);
        renameBlock(stmt.body);
        pop();
        return;
      }
      case "WhileStatement":
      case "DoWhileStatement":
        renameExpr(stmt.test);
        renameBlock(stmt.body);
        return;
      case "DestructureStatement":
        renameExpr(stmt.expression);
        for (const b of stmt.bindings) if (b.defaultValue) renameExpr(b.defaultValue);
        if (topLevel) for (const b of stmt.bindings) b.name = rPlain(b.name);
        return;
      case "ThrowStatement":
        renameExpr(stmt.argument);
        return;
      case "TryStatement":
        renameBlock(stmt.block);
        if (stmt.catchBlock) {
          push(stmt.catchParam ? [stmt.catchParam] : []);
          renameBlock(stmt.catchBlock);
          pop();
        }
        if (stmt.finallyBlock) renameBlock(stmt.finallyBlock);
        return;
      case "BreakStatement":
      case "ContinueStatement":
        return;
    }
  }
  return { renameTopLevel };
}
function isUrl(spec) {
  return /^[a-z][a-z0-9+.-]*:\/\//i.test(spec);
}
function normalizeSegments(parts) {
  const out = [];
  for (const part of parts) {
    if (part === "" || part === ".") continue;
    if (part === "..") out.pop();
    else out.push(part);
  }
  return out.join("/");
}
function dirOf(path) {
  const slash = path.lastIndexOf("/");
  return slash < 0 ? "" : path.slice(0, slash);
}
function resolveSpecifier(spec, importerPath) {
  if (isUrl(spec)) {
    try {
      return new URL(spec).href;
    } catch {
      return null;
    }
  }
  if (isUrl(importerPath)) {
    try {
      return new URL(spec, importerPath).href;
    } catch {
      return null;
    }
  }
  if (spec.startsWith("/")) {
    return normalizeSegments(spec.split("/"));
  }
  if (spec.startsWith("./") || spec.startsWith("../")) {
    return normalizeSegments(`${dirOf(importerPath)}/${spec}`.split("/"));
  }
  return null;
}
function createMemoryResolver(files) {
  return {
    resolve: (spec, importer) => resolveSpecifier(spec, importer),
    load: (path) => {
      const src = files[path];
      if (src === void 0) throw new Error(`not found: ${path}`);
      return src;
    }
  };
}
async function defaultFetch(url) {
  if (typeof fetch !== "function") {
    throw new Error("global fetch is unavailable; cannot load URL imports");
  }
  const res = await fetch(url);
  if (!res.ok) throw new Error(`HTTP ${res.status}`);
  return res.text();
}
async function linkProject(options) {
  const { entry, files } = options;
  const fetchImpl = options.fetch ?? defaultFetch;
  const fetchDiagnostics = [];
  const sources = { ...files };
  const seen = /* @__PURE__ */ new Set();
  async function walk(path) {
    if (seen.has(path)) return;
    seen.add(path);
    if (sources[path] === void 0) {
      if (isUrl(path)) {
        try {
          sources[path] = await fetchImpl(path);
        } catch (err) {
          fetchDiagnostics.push({
            line: 0,
            column: 0,
            message: `Failed to fetch module "${path}": ${err.message ?? err}`,
            severity: "error"
          });
          return;
        }
      } else {
        return;
      }
    }
    let program;
    try {
      program = parse(sources[path]);
    } catch {
      return;
    }
    const children = [];
    for (const stmt of program.statements) {
      if (stmt.kind !== "Import") continue;
      const resolved = resolveSpecifier(stmt.source, path);
      if (resolved !== null) children.push(walk(resolved));
    }
    await Promise.all(children);
  }
  if (sources[entry] === void 0) {
    return {
      program: { statements: [], errors: [] },
      source: "",
      diagnostics: [
        { line: 0, column: 0, message: `Entry module "${entry}" was not found.`, severity: "error" }
      ],
      dependencies: []
    };
  }
  await walk(entry);
  const linked = linkProgram(sources[entry], entry, createMemoryResolver(sources));
  let source;
  try {
    source = printProgram(linked.program);
  } catch {
    source = sources[entry];
  }
  return {
    program: linked.program,
    source,
    diagnostics: [...fetchDiagnostics, ...linked.diagnostics],
    dependencies: linked.dependencies
  };
}
export {
  builtInThemeFonts,
  builtInThemes,
  builtinCatalog,
  createMemoryResolver,
  defaultLibrary,
  factoryResourceCatalog,
  factoryResourceNames,
  findBuiltin,
  findBuiltinConfig,
  findDeclaration,
  findFactoryResource,
  findNamespace,
  findNamespaceMember,
  formatProgram,
  getCompletions,
  getComponentCatalog,
  getDefinition,
  getDefinitionTarget,
  getDiagnostics,
  getDocumentHighlights,
  getDocumentSymbols,
  getHoverInfo,
  getLanguageSpec,
  getLintWarnings,
  getReferences,
  getRenameEdits,
  getSemanticTokens,
  getSignatureHelp,
  getSnippets,
  grammarSpec,
  i18nResultMembers,
  isBuiltinName,
  isNamespaceName,
  keywordDocs,
  linkProgram,
  linkProject,
  namespaceCatalog,
  namespaceMembersAt,
  parse,
  printProgram,
  resolveSpecifier,
  resolveTheme,
  routeMembers,
  sanitiseThemeTokens,
  semanticTokenModifiers,
  semanticTokenTypes,
  suggestComponent,
  universalPropCatalog,
  validateProgram,
  validateProgramSchema
};
//# sourceMappingURL=language.js.map
