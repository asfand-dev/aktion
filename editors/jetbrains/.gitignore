# Gradle / build output
.gradle/
build/
out/

# The Gradle wrapper JAR is not committed — see docs/README.md § Prerequisites.
# Run `gradle wrapper` once to materialise ./gradlew.
gradle/wrapper/gradle-wrapper.jar
gradlew
gradlew.bat

# GENERATED by scripts/sync-assets.mjs — never hand-edit or commit these.
# `server/` is the bundled Aktion language server (built from editors/lsp);
# `textmate/` is the grammar generated from the runtime's own builtin catalog.
server/
textmate/
scripts/.sync-assets.tmp.mjs

# Signing / publishing secrets
*.pem
.env
