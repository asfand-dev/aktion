<!--
  GENERATED by scripts/emit-skill.mjs — do not edit by hand.
  Source of truth: `namespaceCatalog`, `factoryResourceCatalog`, `routeMembers`, `i18nResultMembers`
  Regenerate with `npm run build:skill` (or `npm run build`).
-->

# Namespaces & reactive resource bags

154 members across 5 namespaces, plus the bag each
data builtin returns (8 of them).

**This is the list to check before inventing a helper.** Most of what a program needs —
formatting, dates, collections, validation, storage, clipboard, geolocation, workers — is
already a `$util.*` or `$storage.*` member. Reaching for raw JavaScript when a member
exists is the most common way an Aktion program ends up longer and less reliable than it
needs to be.

## Namespaces

### `$util`

Runtime helper + reactive-environment namespace.

117 members.

| member | kind | signature | notes |
| --- | --- | --- | --- |
| `count` | method | `count(items)` | Number of items in an array. |
| `sum` | method | `sum(items)` | Sum of the numeric values. |
| `avg` | method | `avg(items)` | Arithmetic mean of the values. |
| `min` | method | `min(items)` | Smallest numeric value. |
| `max` | method | `max(items)` | Largest numeric value. |
| `first` | method | `first(items)` | First element, or null when empty. |
| `last` | method | `last(items)` | Last element, or null when empty. |
| `filter` | method | `filter(items, field?, op?, value?)` | Keep items where field matches. Operators: == != > < >= <= contains startsWith endsWith. |
| `find` | method | `find(items, field?, op?, value?)` | First item matching the comparator, or null. |
| `sort` | method | `sort(items, field?, direction?)` | Sort by field, "asc" (default) or "desc". |
| `groupBy` | method | `groupBy(items, field?)` | Group items into an object keyed by the field value. |
| `slice` | method | `slice(items, start?, end?)` | Subarray between start and end indices. |
| `unique` | method | `unique(items, field?)` | De-duplicate an array (optionally by a field). |
| `reverse` | method | `reverse(items)` | Reversed copy of the array. |
| `range` | method | `range(start, end, step?)` | Array of numbers from start to end (inclusive). |
| `repeat` | method | `repeat(value, n)` | Array with the value repeated n times. |
| `pick` | method | `pick(obj, keys)` | Object containing only the listed keys. |
| `omit` | method | `omit(obj, keys)` | Object without the listed keys. |
| `chunk` | method | `chunk(items, size)` | Split an array into chunks of the given size. |
| `flatten` | method | `flatten(items, depth?)` | Flatten nested arrays to the given depth (default 1). |
| `zip` | method | `zip(...arrays)` | Combine arrays element-wise into tuples. |
| `partition` | method | `partition(items, field?, op?, value?)` | Split into [pass, fail] by a comparator. |
| `keyBy` | method | `keyBy(items, field?)` | Index an array into an object keyed by a field. |
| `cloneDeep` | method | `cloneDeep(value)` | Deep-clone a value (arrays / objects / dates). |
| `merge` | method | `merge(target, ...sources)` | Deep-merge source objects onto a clone of the target. |
| `format` | method | `format(value, mode?, options?)` | Locale number format: "number" \| "currency" \| "percent" \| "compact". |
| `formatDate` | method | `formatDate(value, format?)` | Format a date — a token pattern (MMM D, YYYY-MM-DD) or "relative"/"date"/"time"/"datetime"/"iso". |
| `plural` | method | `plural(count, singular, plural?)` | Count + correctly pluralised noun: `3 items` / `1 item`. |
| `capitalize` | method | `capitalize(text)` | Capitalise the first letter. |
| `lowercase` | method | `lowercase(text)` | Lowercase the whole string. |
| `uppercase` | method | `uppercase(text)` | Uppercase the whole string. |
| `titlecase` | method | `titlecase(text)` | Title-case each word. |
| `case` | method | `case(text, kind?)` | Recase: "camel" \| "pascal" \| "snake" \| "kebab". |
| `now` | method | `now()` | Current time as epoch milliseconds. |
| `today` | method | `today()` | Start of today as an ISO string. |
| `addDays` | method | `addDays(date, days)` | Date shifted by n days (ISO). |
| `addHours` | method | `addHours(date, hours)` | Date shifted by n hours (ISO). |
| `diffDays` | method | `diffDays(start, end)` | Whole days between two dates. |
| `startOfWeek` | method | `startOfWeek(date)` | Start of the week containing the date (ISO). |
| `endOfMonth` | method | `endOfMonth(date)` | End of the month containing the date (ISO). |
| `join` | method | `join(items, sep?)` | Join an array into a string with a separator. |
| `split` | method | `split(text, sep?)` | Split a string into an array on a separator. |
| `trim` | method | `trim(text)` | Trim surrounding whitespace. |
| `replace` | method | `replace(text, search, replacement?)` | Replace every occurrence of a substring. |
| `substring` | method | `substring(text, start, end?)` | Substring between two indices. |
| `startsWith` | method | `startsWith(text, prefix)` | True when the text starts with the prefix. |
| `endsWith` | method | `endsWith(text, suffix)` | True when the text ends with the suffix. |
| `contains` | method | `contains(text, needle)` | True when the text contains the needle. |
| `match` | method | `match(text, pattern)` | Test the text against a regular-expression pattern. |
| `round` | method | `round(value, decimals?)` | Round to n decimal places. |
| `floor` | method | `floor(value)` | Round down to an integer. |
| `ceil` | method | `ceil(value)` | Round up to an integer. |
| `abs` | method | `abs(value)` | Absolute value. |
| `clamp` | method | `clamp(value, min, max)` | Constrain a number to a [min, max] range. |
| `pow` | method | `pow(base, exp)` | Exponentiation (base^exp). |
| `sqrt` | method | `sqrt(value)` | Square root. |
| `random` | method | `random()` | Pseudo-random number in [0, 1). |
| `log` | method | `log(value)` | Natural logarithm. |
| `slugify` | method | `slugify(text)` | URL-safe slug: "Hello World" → "hello-world". |
| `truncate` | method | `truncate(text, length?, ellipsis?)` | Cut text to n chars with an ellipsis. |
| `initials` | method | `initials(name, max?)` | Up-to-`max` initials from a full name. |
| `currency` | method | `currency(value, code?, locale?)` | Locale currency string. |
| `percent` | method | `percent(value, decimals?)` | Locale percent string (0.42 → "42%"). |
| `bytes` | method | `bytes(value)` | Human-readable byte size (1536 → "1.5 KB"). |
| `relativeTime` | method | `relativeTime(value)` | "3 minutes ago" / "in 2 days" via Intl. |
| `copy` | method | `copy(text)` | Copy to the clipboard. Returns a PROMISE that resolves true only when the write succeeds — `await` never suspends in Aktion, so read it with `$util.copy(x).then(ok => …)`. |
| `sleep` | method | `sleep(ms?)` | Pause, capped at 60s. Returns a promise — `await` never suspends, so continue in `$util.sleep(ms).then(() => …)`. |
| `uuid` | method | `uuid()` | Random UUID v4 string. |
| `debounceFn` | method | `debounceFn(fn, wait?)` | Wrap a function so it fires `wait` ms after the LAST call. |
| `throttleFn` | method | `throttleFn(fn, wait?)` | Wrap a function to fire at most once per `wait` ms (leading + one trailing). |
| `derived` | method | `derived(fn)` | Computed reactive value — recomputes from the atoms the lambda reads. |
| `onError` | method | `onError(fn)` | Program-level error sink — fires with { error, source } when an action throws. |
| `onNavigate` | method | `onNavigate(fn)` | Navigation guard: return false to block, a path string to redirect, anything else to allow. |
| `onRequest` | method | `onRequest(fn)` | HTTP request interceptor — a partial return merges over every outgoing request. |
| `onResponse` | method | `onResponse(fn)` | HTTP response interceptor — replace the response, or `return retry()` to re-issue once (the client awaits what you return; `await` inside the body does not suspend). |
| `invalidate` | method | `invalidate(keys)` | Refetch every cached $query whose key contains one of the substrings. |
| `scroll` | property | `scroll` | Reactive scroll: .x / .y / .progress (0–1) / .direction ("up"\|"down"). |
| `viewport` | property | `viewport` | Reactive viewport: .width / .height. |
| `breakpoint` | property | `breakpoint` | Reactive breakpoint: .active ("base"\|"sm"\|"md"\|"lg"\|"xl") + boolean .sm/.md/.lg/.xl. |
| `media` | property | `media` | Reactive media flags: .prefersDark / .prefersReducedMotion / .online / .pointer / .portrait. |
| `mouse` | property | `mouse` | Reactive pointer position: .x / .y. |
| `url` | property | `url` | Reactive URL snapshot: .path / .params / .query / .hash + .navigate(to). |
| `url.setQuery` | method | `url.setQuery(key, value)` | Write a query param in place (null/"" drops it) — shareable filter/tab state. |
| `url.removeQuery` | method | `url.removeQuery(key)` | Drop a query param from the URL. |
| `style` | namespace | `style` | Safe styling helpers: .cx / .gradient / .alpha / .clamp / .token / .toStyle. |
| `style.cx` | method | `style.cx(...args)` | clsx-style class composer — strings, arrays, { name: cond } objects. |
| `style.gradient` | method | `style.gradient(stops, angle?)` | Safe linear-gradient() from color stops + angle. |
| `style.alpha` | method | `style.alpha(color, amount)` | color-mix transparency: token or color at 0–1 alpha. |
| `style.clamp` | method | `style.clamp(min, preferred, max)` | Responsive clamp(min, preferred, max) size. |
| `style.token` | method | `style.token(path)` | Resolve a theme token path to its CSS var: "colors.primary" → var(--rui-color-primary). |
| `style.toStyle` | method | `style.toStyle(obj)` | Serialise a CSS-declarations object to a sanitised style string. |
| `rules` | namespace | `rules` | Composable validators — compose per field; run with .validate / .validateAll or hand to $form. |
| `rules.required` | method | `rules.required(message?)` | Non-empty value. |
| `rules.email` | method | `rules.email(message?)` | Valid email address. |
| `rules.url` | method | `rules.url(message?)` | Valid http(s) URL. |
| `rules.min` | method | `rules.min(n, message?)` | Number ≥ n. |
| `rules.max` | method | `rules.max(n, message?)` | Number ≤ n. |
| `rules.minLength` | method | `rules.minLength(n, message?)` | String length ≥ n. |
| `rules.maxLength` | method | `rules.maxLength(n, message?)` | String length ≤ n. |
| `rules.pattern` | method | `rules.pattern(re, message?)` | Match a regular expression. |
| `rules.oneOf` | method | `rules.oneOf(options, message?)` | Value is in the allowed list. |
| `rules.matches` | method | `rules.matches(other, message?)` | Equals another value (password confirmation). |
| `rules.custom` | method | `rules.custom(fn, message?)` | Custom sync rule — return true/null (valid), false, or an error string. |
| `rules.asyncCustom` | method | `rules.asyncCustom(fn, message?)` | Async rule (Promise) — server-side checks; $form awaits it before submitting. |
| `rules.validate` | method | `rules.validate(value, validators)` | Run validators — first error message or null (a Promise when an async rule is hit). |
| `rules.validateAll` | method | `rules.validateAll(values, schema)` | Validate an object against { field: [validators] } → { field: message }. |
| `vibrate` | method | `vibrate(pattern?)` | Haptic pulse (ms or pattern array) on supporting devices. |
| `share` | method | `share(data)` | Native share sheet (Web Share API) — resolves true on share. |
| `readClipboard` | method | `readClipboard()` | Read clipboard text (async, permission-gated). |
| `geolocate` | method | `geolocate(options?)` | Resolve { lat, lng, accuracy } via the Geolocation API. |
| `isOnline` | method | `isOnline()` | Current navigator.onLine flag. |
| `deviceType` | method | `deviceType()` | "mobile" \| "tablet" \| "desktop" heuristic. |
| `worker` | method | `worker(fn, ...args)` | Run a closure-free function in a Web Worker; resolves its return value. |
| `registerServiceWorker` | method | `registerServiceWorker(url, scope?)` | Register a service worker for PWA/offline. |
| `webManifest` | method | `webManifest(config)` | Build a sanitised web-app manifest (name, icons, themeColor…). |
| `nativeShell` | method | `nativeShell()` | Detect the wrapper: capacitor/cordova/tauri/electron/react-native or "web". |
| `isNativeApp` | method | `isNativeApp()` | True when running inside a native shell. |

### `$storage`

Browser storage namespace (local / session / cookies).

19 members.

| member | kind | signature | notes |
| --- | --- | --- | --- |
| `set` | method | `set(key, value, options?)` | localStorage (default namespace) write. |
| `get` | method | `get(key)` | localStorage (default namespace) read. Returns null when missing. |
| `remove` | method | `remove(key, options?)` | Delete a key from localstorage (default namespace). |
| `clear` | method | `clear()` | Wipe every localstorage (default namespace) entry. |
| `local` | namespace | `local` | localStorage backend — set/get/remove/clear. |
| `local.set` | method | `local.set(key, value, options?)` | localStorage write. |
| `local.get` | method | `local.get(key)` | localStorage read. Returns null when missing. |
| `local.remove` | method | `local.remove(key, options?)` | Delete a key from localstorage. |
| `local.clear` | method | `local.clear()` | Wipe every localstorage entry. |
| `session` | namespace | `session` | sessionStorage backend (per-tab) — set/get/remove/clear. |
| `session.set` | method | `session.set(key, value, options?)` | Per-tab sessionStorage write. |
| `session.get` | method | `session.get(key)` | Per-tab sessionStorage read. Returns null when missing. |
| `session.remove` | method | `session.remove(key, options?)` | Delete a key from per-tab sessionstorage. |
| `session.clear` | method | `session.clear()` | Wipe every per-tab sessionstorage entry. |
| `cookies` | namespace | `cookies` | Document cookies — set/get/remove/clear (options: expires, maxAge, path, domain, secure, sameSite). |
| `cookies.set` | method | `cookies.set(key, value, options?)` | Set a cookie. Options: expires, maxAge, path, domain, secure, sameSite. |
| `cookies.get` | method | `cookies.get(key)` | Read a cookie value. |
| `cookies.remove` | method | `cookies.remove(key, options?)` | Delete a cookie. Path/domain must match the original set call. |
| `cookies.clear` | method | `cookies.clear()` | Clear every cookie on this document. |

### `$console`

Console namespace forwarding to the browser console.

5 members.

| member | kind | signature | notes |
| --- | --- | --- | --- |
| `log` | method | `log(...args)` | Log a message at the default level. |
| `error` | method | `error(...args)` | Log an error. |
| `warn` | method | `warn(...args)` | Log a warning. |
| `info` | method | `info(...args)` | Log an informational message. |
| `debug` | method | `debug(...args)` | Log a verbose debug message. |

### `$toast`

Imperative toast namespace.

9 members.

| member | kind | signature | notes |
| --- | --- | --- | --- |
| `show` | method | `show(message, options?)` | Show a toast; returns its id. Options: { title?, tone?, duration? }. |
| `success` | method | `success(message, options?)` | `show` with tone "success". |
| `error` | method | `error(message, options?)` | `show` with tone "danger". |
| `info` | method | `info(message, options?)` | `show` with tone "info". |
| `warning` | method | `warning(message, options?)` | `show` with tone "warning". |
| `dismiss` | method | `dismiss(id)` | Remove a single toast by id. |
| `clear` | method | `clear()` | Remove every toast. |
| `configure` | method | `configure(options)` | Settings for the auto-rendered stack, e.g. { position: "bottom-center" }. Call once at the top level of the program. |
| `items` | property | `items` | Reactive list of live toasts (newest last). Treat as read-only. |

### `$dom`

Managed DOM-observer namespace (resize / intersection / mutation / measure).

4 members.

| member | kind | signature | notes |
| --- | --- | --- | --- |
| `onResize` | method | `onResize(node, callback)` | Observe element size with a ResizeObserver; callback gets { width, height, entry }. Returns a disposer; auto-disposed on replan. |
| `onIntersect` | method | `onIntersect(node, callback, options?)` | Observe viewport intersection with an IntersectionObserver; callback gets the entry. Options: { root?, rootMargin?, threshold? }. |
| `onMutation` | method | `onMutation(node, callback, options?)` | Observe DOM mutations with a MutationObserver. Options: { childList?, attributes?, subtree?, characterData? }. |
| `measure` | method | `measure(node)` | One-shot read → { rect, scroll, viewport } (getBoundingClientRect + scroll offsets + window size). |

## Reactive resource bags

A data builtin returns a bag, not a value: `$todos = $http({ url })` makes `$todos.data`,
`$todos.loading`, `$todos.error`, and `$todos.refetch()` available and reactive. Read the
bag's members — never treat the binding itself as the payload.

### `$http({ … })` →

Reactive HTTP resource bag.

| member | kind | signature | notes |
| --- | --- | --- | --- |
| `data` | property | `data` | Parsed response body — `null` until the request resolves. |
| `error` | property | `error` | `null` on success; `{ status, body }` on a non-2xx; the thrown error on network failure. |
| `status` | property | `status` | HTTP status code of the last response, e.g. `200`. |
| `loading` | property | `loading` | `true` while a request is in flight. |
| `headers` | property | `headers` | Response headers as a plain object. |
| `lastUpdated` | property | `lastUpdated` | Epoch-ms of the last successful response. |
| `refetch` | method | `refetch()` | Re-issue the original request. |
| `cancel` | method | `cancel()` | Abort the in-flight request. |
| `onDone` | property | `onDone` | Settable callback fired each time the request settles (success or error). |

### `$query({ … })` →

Cached query bag (HTTP + pagination).

| member | kind | signature | notes |
| --- | --- | --- | --- |
| `data` | property | `data` | Parsed response body — `null` until the request resolves. |
| `error` | property | `error` | `null` on success; `{ status, body }` on a non-2xx; the thrown error on network failure. |
| `status` | property | `status` | HTTP status code of the last response, e.g. `200`. |
| `loading` | property | `loading` | `true` while a request is in flight. |
| `headers` | property | `headers` | Response headers as a plain object. |
| `lastUpdated` | property | `lastUpdated` | Epoch-ms of the last successful response. |
| `refetch` | method | `refetch()` | Re-issue the original request. |
| `cancel` | method | `cancel()` | Abort the in-flight request. |
| `onDone` | property | `onDone` | Settable callback fired each time the request settles (success or error). |
| `loadMore` | method | `loadMore()` | Fetch the next page (infinite mode). |
| `hasMore` | property | `hasMore` | `true` while more pages are available (infinite mode). |
| `loadingMore` | property | `loadingMore` | `true` while a `loadMore()` page is in flight. |
| `pages` | property | `pages` | Raw page bodies loaded so far (infinite mode); `.data` is the flattened items. |

### `$mutation({ … })` →

Deferred mutation bag (fires on .mutate()).

| member | kind | signature | notes |
| --- | --- | --- | --- |
| `mutate` | method | `mutate(overrides?)` | Fire the request; overrides shallow-merge over the config. `optimistic` applies instantly and rolls back on failure. |
| `data` | property | `data` | Response body of the last successful mutation. |
| `error` | property | `error` | `null` on success; error details on failure. |
| `loading` | property | `loading` | `true` while the mutation request is in flight. |
| `status` | property | `status` | HTTP status code of the last response. |
| `onDone` | property | `onDone` | Settable callback fired when the mutation settles. |

### `$socket({ … })` →

Reactive WebSocket bag.

| member | kind | signature | notes |
| --- | --- | --- | --- |
| `status` | property | `status` | Connection lifecycle: "connecting" \| "open" \| "closed". |
| `connected` | property | `connected` | `true` while status is "open". |
| `last` | property | `last` | Most recent message (JSON auto-parsed), or null. |
| `messages` | property | `messages` | Buffered messages, newest last (capped to bufferSize). |
| `attempts` | property | `attempts` | Reconnect attempts in the current streak (resets on success). |
| `error` | property | `error` | Last socket error event, if any. |
| `send` | method | `send(data)` | Send a message (objects JSON-stringified). Queues while connecting; flushes on open. |
| `close` | method | `close()` | Close for good — disables auto-reconnect. |

### `$sse({ … })` →

Reactive Server-Sent Events bag.

| member | kind | signature | notes |
| --- | --- | --- | --- |
| `status` | property | `status` | "connecting" \| "open" \| "closed" (EventSource retries natively). |
| `connected` | property | `connected` | `true` while the stream is open. |
| `last` | property | `last` | Most recent event payload (JSON auto-parsed). |
| `messages` | property | `messages` | Buffered events, newest last (capped to bufferSize). |
| `error` | property | `error` | Last stream error, if any. |
| `close` | method | `close()` | Close the stream. |

### `$script({ … })` →

External script / stylesheet load bag.

| member | kind | signature | notes |
| --- | --- | --- | --- |
| `ready` | property | `ready` | `true` once the external script / stylesheet has loaded successfully. |
| `loading` | property | `loading` | `true` while the resource is still downloading. |
| `error` | property | `error` | The load error, or `null` on success. |
| `value` | property | `value` | The resolved value — `window[global]` for a script with a `global` (e.g. window.Stripe), else `true`. `null` until ready. |

### `$form({ … })` →

Managed form engine bag.

| member | kind | signature | notes |
| --- | --- | --- | --- |
| `values` | property | `values` | Reactive field values — two-way bind with an input's `value`. |
| `errors` | property | `errors` | Per-field error messages (set after validate/touch/submit). |
| `touched` | property | `touched` | Per-field booleans — true once the user has interacted. |
| `dirty` | property | `dirty` | `true` once any value differs from the clean snapshot. |
| `valid` | property | `valid` | `true` when the last validation pass found no errors. |
| `submitting` | property | `submitting` | `true` from submit() until an async onSubmit settles. |
| `validating` | property | `validating` | `true` while async rules ($util.rules.asyncCustom) are in flight. |
| `field` | method | `field(name)` | Controlled prop bag: { value, error, name, onChange, onBlur } — spread onto an input. |
| `touch` | method | `touch(name)` | Mark a field touched + validate it (wire to `onBlur`). |
| `setField` | method | `setField(name, value)` | Set one field value (clears its error). |
| `setValues` | method | `setValues(values)` | Merge several field values at once. |
| `validate` | method | `validate()` | Validate every field → boolean (a Promise when async rules exist). |
| `validateField` | method | `validateField(name)` | Validate one field → message \| null (Promise for async rules). |
| `submit` | method | `submit()` | Touch all → validate → onSubmit(values) when valid. Alias: handleSubmit(). |
| `handleSubmit` | method | `handleSubmit()` | Alias of submit(). |
| `reset` | method | `reset()` | Restore initial values; clears errors/touched/dirty. |

### `$store({ … })` →

Global store handle (built-in history methods).

| member | kind | signature | notes |
| --- | --- | --- | --- |
| `undo` | method | `undo()` | Undo the last change (`history: true\|depth` stores). |
| `redo` | method | `redo()` | Redo the last undone change. |
| `canUndo` | property | `canUndo` | Reactive — `true` when an undo step is available. |
| `canRedo` | property | `canRedo` | Reactive — `true` when a redo step is available. |
| `clearHistory` | method | `clearHistory()` | Drop the undo/redo stacks. |

## The `route` handle

Always in scope; reactive. Read the current location, navigate imperatively.

| member | kind | signature | notes |
| --- | --- | --- | --- |
| `path` | property | `path` | Current URL path, e.g. `/users/42`. |
| `params` | property | `params` | Captured path segments from the matched route pattern (`route.params.id`). |
| `query` | property | `query` | Parsed query-string parameters as an object. |
| `pattern` | property | `pattern` | The matched route pattern, or `null`. |
| `navigate` | method | `navigate(to)` | Imperatively navigate to a path. |

## What `$i18n({ … })` destructures to

| member | kind | signature | notes |
| --- | --- | --- | --- |
| `t` | method | `t(key, vars?)` | Translate a key for the current language; interpolates {name} placeholders + ICU plural/select. |
| `setCurrentLanguage` | method | `setCurrentLanguage(lang)` | Switch the active language. |
| `getCurrentLanguage` | method | `getCurrentLanguage()` | Return the active language code. |

