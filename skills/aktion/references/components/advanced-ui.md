<!--
  GENERATED by scripts/emit-skill.mjs — do not edit by hand.
  Source of truth: `getComponentCatalog()` + `defaultLibrary.componentGroups`
  Regenerate with `npm run build:skill` (or `npm run build`).
-->

# Advanced UI

22 components. [← back to the catalogue index](./index.md)

## When to use what

These are the library's own authoring notes — they encode the intended choice
between similar components, which the prop tables below cannot express.

- `IconButton(icon, label, action?, variant?, size?, disabled?)` — accessible icon-only control.
- `CommandPalette(items, open?, placeholder?, shortcut?)` — Cmd-K searchable actions.
- `FilterChips(chips, onRemove?, onClear?)` — applied filter pills with remove.
- `FieldRepeater(items, fields, onAdd?, onRemove?)` — dynamic form rows.
- `VirtualList(items, itemHeight?, renderItem)` — windowed long lists.
- `QueryBuilder(fields, value?)` — visual AND/OR filter builder.
- `DiffViewer(left, right, mode?)` — side-by-side or unified diff.
- `JsonTree(data)` — collapsible JSON inspector.
- `Gantt(tasks, startDate?, endDate?)` — horizontal schedule timeline.
- `Truncate(text, maxLines?)` / `InlineEdit(value, onSave?)` / `NotificationBell(count?, items?)`.

## Reference

### IconButton

```
IconButton(icon, label, onClick?, variant?, size?, disabled?, active?, loading?, type?, href?)
```

Icon-only button with an accessible label. Use for toolbars, table row actions, and compact controls. `active` makes it a toggle (reflected as `aria-pressed`), `loading` shows a spinner and blocks clicks, and `href` renders it as a link instead of a button.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `icon` | `string` | **yes** | Font Awesome icon name |
| `label` | `string` | **yes** | Accessible label — the control's only name, so never omit it |
| `onClick` | `callable` | no |  |
| `variant` | `"primary"` \| `"secondary"` \| `"outline"` \| `"ghost"` \| `"link"` \| `"danger"` \| `"default"` | no | Same vocabulary as `Button` — `primary`/`default`, `secondary`, `outline`, `ghost` (the default here), `link`, `danger` |
| `size` | `"xs"` \| `"sm"` \| `"md"` \| `"lg"` \| `"xl"` | no | Size token `xs\|sm\|md\|lg\|xl` (default `md`) |
| `disabled` | `boolean` | no |  |
| `active` | `boolean` | no | Toggle state — adds `aria-pressed` for assistive tech |
| `loading` | `boolean` | no | Show a spinner and ignore clicks |
| `type` | `"button"` \| `"submit"` \| `"reset"` | no | HTML button type (default `button`) |
| `href` | `string` | no | Render as a link (sanitised) so middle-click and ctrl-click work |

### CommandPalette

```
CommandPalette(items, open?, placeholder?, shortcut?, onSelect?, onClose?, loading?, emptyLabel?, label?, maxResults?)
```

Cmd-K style searchable command list. Pass `items` as `{label, value, group?, shortcut?, action?}` objects. Arrow keys move the selection, Enter runs it, Escape closes. Bind `open` to a `$variable` (or pass `onClose`) so dismissing the palette is written back — otherwise the next re-render re-opens it.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `any[]` | **yes** |  |
| `open` | `boolean` | no | Whether the palette is visible (default true) |
| `placeholder` | `string` | no |  |
| `shortcut` | `string` | no | Hint label, e.g. Cmd+K |
| `onSelect` | `callable` | no | Receives the selected item's `value` |
| `onClose` | `callable` | no | Called with `false` whenever the palette is dismissed |
| `loading` | `boolean` | no | Show a pending state instead of the empty row |
| `emptyLabel` | `string` | no | Text shown when nothing matches (default "No commands found") |
| `label` | `string` | no | Accessible name for the dialog (default "Command palette") |
| `maxResults` | `number` | no | Rows rendered before the list is truncated (default 50) |

### FilterChips

```
FilterChips(chips, onRemove?, onClear?, clearLabel?, max?, disabled?)
```

Removable filter chips with an optional clear-all control. Set `max` to collapse the tail into a "+N" chip, and `disabled` to freeze the row while a filtered query is in flight.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `chips` | `any[]` | **yes** | Array of strings or {label, value} objects |
| `onRemove` | `callable` | no | Receives the removed chip value as an argument |
| `onClear` | `callable` | no |  |
| `clearLabel` | `string` | no | Text of the clear-all control (default "Clear all") |
| `max` | `number` | no | Chips shown before the rest collapse into a "+N" chip |
| `disabled` | `boolean` | no | Block removal while a request is in flight |

### FilterPill

```
FilterPill(label, active?, count?, icon?, disabled?, onToggle?)
```

Toggleable filter pill — click to switch a filter on/off. Reflects state through `active` (and `aria-pressed` for assistive tech), with an optional leading `icon` and trailing `count`. Use for filter bars; use `FilterChips` to show already-applied filters that can be removed.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `label` | `string` | **yes** |  |
| `active` | `boolean` | no |  |
| `count` | `number` | no | Optional trailing match count |
| `icon` | `string` | no |  |
| `disabled` | `boolean` | no |  |
| `onToggle` | `callable` | no | Receives the next active state |

### FieldRepeater

```
FieldRepeater(items, fields, onAdd?, onRemove?, addLabel?, onChange?, removeLabel?, min?, max?)
```

Dynamic list of field groups (invoice lines, recipients, key/value pairs). Pass `items` as row objects and `fields` as `{name, label, type?, options?, placeholder?}` definitions — `type` accepts the native input types plus `checkbox`, `textarea` and `select` (with `options`). Bind `items` to a `$variable` for two-way capture, or read edits from `onChange(index, field, value, rows)`.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `any[]` | **yes** |  |
| `fields` | `any[]` | **yes** |  |
| `onAdd` | `callable` | no |  |
| `onRemove` | `callable` | no | Receives the removed row's 0-indexed position |
| `addLabel` | `string` | no |  |
| `onChange` | `callable` | no | Receives `(index, fieldName, value, rows)` on every edit |
| `removeLabel` | `string` | no | Text of the per-row remove control (default "Remove") |
| `min` | `number` | no | Rows that can never be removed (default 0) |
| `max` | `number` | no | Maximum rows — "Add row" is disabled at the cap |

### VirtualList

```
VirtualList(items, itemHeight?, renderItem?, height?, onItemClick?, empty?, loading?)
```

Windowed vertical list for large datasets. Pass pre-rendered nodes as `items` or plain row objects plus a `renderItem` lambda (`row => Node`) invoked per row. Every row is boxed at `itemHeight` so the virtualization maths matches the real layout.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `any[]` | **yes** |  |
| `itemHeight` | `number` | no | Fixed row height in px (default 40) |
| `renderItem` | `callable` | no | Lambda `(row, index) => Node` rendered per data row |
| `height` | `number` | no | Viewport height in px (default 12 rows) |
| `onItemClick` | `callable` | no | Receives the clicked row and its index |
| `empty` | `any` | no | Node or text shown when `items` is empty |
| `loading` | `boolean` | no | Show a pending state instead of the empty state |

### QueryBuilder

```
QueryBuilder(fields, value?, onChange?, operators?, disabled?, maxRules?)
```

Visual AND/OR filter builder. Pass `fields` as `{name, label, type?, operators?}` and bind `value` to a rule array of `{field, op, value, combinator?}`. Every rule after the first carries an AND/OR combinator; operators default to a sensible set per field `type` and can be overridden globally (`operators`) or per field.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `fields` | `any[]` | **yes** |  |
| `value` | `any[]` | no |  |
| `onChange` | `callable` | no | Receives the next rule array |
| `operators` | `any[]` | no | Operator tokens or `{value, label, type?}` objects — `type` scopes them to matching fields |
| `disabled` | `boolean` | no | Freeze the builder while a query is in flight |
| `maxRules` | `number` | no | Maximum number of rules (default unlimited) |

### DiffViewer

```
DiffViewer(left, right, mode?, leftTitle?, rightTitle?, lineNumbers?, contextLines?, maxHeight?)
```

Side-by-side or unified diff of two text blobs, aligned by a line-level LCS diff so an inserted line does not mark the rest of the file as changed. Set `leftTitle`/`rightTitle` to label the columns, `lineNumbers` to number them, and `contextLines` to collapse unchanged runs.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `left` | `string` | **yes** |  |
| `right` | `string` | **yes** |  |
| `mode` | `"split"` \| `"unified"` | no | Default split |
| `leftTitle` | `string` | no | Header for the left column (e.g. "Before") |
| `rightTitle` | `string` | no | Header for the right column (e.g. "After") |
| `lineNumbers` | `boolean` | no | Show original line numbers |
| `contextLines` | `number` | no | Unchanged lines kept around each change (others collapse) |
| `maxHeight` | `number` | no | Scroller height in px (default 320) |

### JsonTree

```
JsonTree(data, expanded?, expandedDepth?, maxHeight?)
```

Expandable JSON tree viewer for objects and arrays. Arrow keys walk the tree, Enter/Space toggles a branch. Cycles render as `[Circular]`, so a normalised store or a parent back-reference is safe to inspect.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `data` | `any` | **yes** |  |
| `expanded` | `boolean` | no | Expand all nodes (default: first level only) |
| `expandedDepth` | `number` | no | Levels open by default (default 1) |
| `maxHeight` | `number` | no | Scroll the tree beyond this height in px |

### Gantt

```
Gantt(tasks, startDate?, endDate?, axis?, ticks?, today?, onTaskClick?)
```

Simple Gantt chart. Pass `tasks` as `{id, label, start, end, progress?, tone?}` with ISO date strings; `progress` accepts either a 0-1 fraction or a 0-100 percentage, and `tone` colours the bar (`success`, `warning`, `danger`, …). Set `axis` for a date scale and `today` for a now marker.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `tasks` | `any[]` | **yes** |  |
| `startDate` | `string` | no |  |
| `endDate` | `string` | no |  |
| `axis` | `boolean` | no | Render a date axis above the bars |
| `ticks` | `number` | no | Axis tick count (default 5, implies `axis`) |
| `today` | `boolean | string` | no | Draw a marker line at now, or at the given ISO date |
| `onTaskClick` | `callable` | no | Receives the clicked task's id |

### Truncate

```
Truncate(text, maxLines?, expandLabel?, collapseLabel?, expanded?, onToggle?, child?)
```

Clamp long text (or a `child` node) with an expand control. The toggle hides itself when the content already fits, and `expanded` + `onToggle` make the state controllable for "expand all" flows.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `text` | `string` | **yes** |  |
| `maxLines` | `number` | no | Lines before clamping (default 3) |
| `expandLabel` | `string` | no |  |
| `collapseLabel` | `string` | no | Text of the collapse control (default "Show less") |
| `expanded` | `boolean` | no | Controlled expansion state — bind to a $variable |
| `onToggle` | `callable` | no | Receives the next expanded state |
| `child` | `Node` | no | Formatted content to clamp instead of `text` |

### InlineEdit

```
InlineEdit(value, label?, onSave?, placeholder?, type?, onCancel?, disabled?, hint?, error?, required?, onBlur?, onFocus?, name?, labelHidden?)
```

Click-to-edit inline field. Enter or blur commits (only when the value actually changed), Escape discards. Pass `type: "textarea"` for multi-line, `placeholder` so an empty field still has an affordance, and `error`/`required` to surface a rejected save.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `value` | `string` | **yes** |  |
| `label` | `string` | no |  |
| `onSave` | `callable` | no | Receives the committed draft string |
| `placeholder` | `string` | no | Shown when the value is empty (also names the trigger) |
| `type` | `string` | no | Input type, or `textarea` for multi-line |
| `onCancel` | `callable` | no | Called when the edit is discarded with Escape |
| `disabled` | `boolean` | no | Disable the control (non-editable, skipped by tab order) |
| `hint` | `string` | no | Helper text rendered below the control |
| `error` | `string` | no | Validation error rendered below the control (marks it invalid) |
| `required` | `boolean` | no | Mark the field required (adds a `*` and the `required` attribute) |
| `onBlur` | `callable` | no | Called with the current value when focus leaves the control (validate-on-blur, `form.touch`) |
| `onFocus` | `callable` | no | Called when the control gains focus |
| `name` | `string` | no | Form field name submitted to the server (defaults to `id`) |
| `labelHidden` | `boolean` | no | Keep the label in the accessibility tree but hide it visually — for a field whose purpose is already clear from context |

### NotificationBell

```
NotificationBell(count?, items?, onOpen?, onItemClick?, onMarkAllRead?, align?, loading?, emptyLabel?, label?, markAllLabel?, maxCount?)
```

Bell icon with unread count badge and dropdown notification list. Pass `items` as `{title, message?, time?, href?, unread?}` objects; `onItemClick` receives the clicked item and `onMarkAllRead` renders a footer control. Arrow keys walk the list, Escape closes.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `count` | `number` | no |  |
| `items` | `any[]` | no | {title, message?, time?, href?, unread?} objects |
| `onOpen` | `callable` | no |  |
| `onItemClick` | `callable` | no | Receives the clicked notification object and its index |
| `onMarkAllRead` | `callable` | no | Renders a "mark all read" footer control |
| `align` | `"left"` \| `"right"` | no | Which edge the panel aligns to (default right) |
| `loading` | `boolean` | no | Show a pending state instead of the empty message |
| `emptyLabel` | `string` | no | Text shown with no notifications (default "No notifications") |
| `label` | `string` | no | Accessible name for the bell (default "Notifications") |
| `markAllLabel` | `string` | no | Text of the mark-all-read control |
| `maxCount` | `number` | no | Badge cap before it renders as "N+" (default 99) |

### Reveal

```
Reveal(child, animation?, delay?, duration?, threshold?, once?)
```

Animates its child in the first time it scrolls into view (scroll choreography). `animation` is a preset (fade-up|fade|zoom|…), `delay` staggers it (ms), `duration` sets the animation length (default 600ms), `threshold` how much of it must be visible to trigger (0–1, default 0.15), `once` (default true) plays a single time. Honours prefers-reduced-motion (renders immediately).

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `child` | `Node` | **yes** |  |
| `animation` | `"fade"` \| `"fade-up"` \| `"fade-down"` \| `"fade-left"` \| `"fade-right"` \| `"zoom"` \| `"slide-up"` | no |  |
| `delay` | `number` | no | Stagger delay in ms |
| `duration` | `number` | no | Animation duration in ms (default 600) — match it to the rest of the page's motion scale |
| `threshold` | `number` | no | Fraction of the child that must be visible to trigger, 0–1 (default 0.15). Use a low value for a tall hero, a higher one for a small card |
| `once` | `boolean` | no |  |

### Transition

```
Transition(child, show, preset?, duration?, onExited?)
```

Animates its child IN when `show` becomes true and OUT when it becomes false (mount/unmount choreography) — modals, toasts, dropdowns, list rows. `preset` is fade|scale|slide-up|slide-down|slide-left|slide-right, `duration` is ms. The child stays mounted through the exit animation, then is removed and `onExited` fires — use it to drop the row from the backing array, release a lock, or restore focus. Honours prefers-reduced-motion (instant swap).

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `child` | `Node` | **yes** |  |
| `show` | `boolean` | **yes** | Whether the child is visible |
| `preset` | `"fade"` \| `"scale"` \| `"slide-up"` \| `"slide-down"` \| `"slide-left"` \| `"slide-right"` | no |  |
| `duration` | `number` | no | Animation duration in ms (default 280) |
| `onExited` | `callable` | no | Fired once the exit animation has finished and the child has been removed |

### FlipList

```
FlipList(children, duration?, horizontal?)
```

Smoothly animates its children when they reorder or are added (the FLIP technique — First/Last/Invert/Play). Wrap a keyed list whose order changes (drag-sort, filter, sort toggle) so items glide to their new positions instead of jumping, and new items fade in rather than popping. Removed items are dropped straight away — wrap a row in `Transition` if it needs to animate out. `horizontal` lays the items out in a row; `duration` is ms. Honours prefers-reduced-motion (no animation). Use stable `key:`s on the items so their DOM nodes persist across the reorder.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `children` | `Node[]` | **yes** | The list items (use stable keys) |
| `duration` | `number` | no | Animation duration in ms (default 300) |
| `horizontal` | `boolean` | no | Lay the items out horizontally (tag chips, a filter bar, kanban columns) |

### Parallax

```
Parallax(child, speed?, maxOffset?)
```

Translates its child vertically as the page scrolls for a depth effect. `speed` (−1…1) sets intensity/direction. Travel is capped by `maxOffset` (default: half the layer's own height) so the layer cannot slide out of its section and over the content above or below it. Honours prefers-reduced-motion.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `child` | `Node` | **yes** |  |
| `speed` | `number` | no | −1…1 (default 0.3) |
| `maxOffset` | `string` | no | Maximum travel in either direction — a length ("120px") or a percentage of the layer's height ("40%"). Default 50% |

### ReadingProgress

```
ReadingProgress(gradient?, height?, target?, color?)
```

A thin progress bar that fills as the reader scrolls — article/docs reading affordance. Optional `gradient` fill or a `color` override. By default it measures whichever container actually scrolls the app (the page when the app owns the page scroll, otherwise the nearest scrolling ancestor — a chat pane, a dashboard panel, a modal body), and pins itself inside that container instead of the browser viewport. `target` overrides the choice with a CSS selector, or "page" to force the document.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `gradient` | `boolean` | no |  |
| `height` | `string` | no | Bar thickness (CSS length, default 3px) |
| `target` | `string` | no | What to measure: a CSS selector for the scrolling element, or "page" for the document. Default: auto-detect |
| `color` | `string` | no | Fill colour (CSS colour or var(--token)) — overrides `gradient` |

### Sortable

```
Sortable(items, onReorder?, handle?, horizontal?, disabled?, ariaLabel?)
```

A list whose items can be reordered by dragging a row (or, for keyboard users, by focusing one, pressing Space to pick it up and using the arrow keys). Pass already-rendered `items`; `onReorder(fromIndex, toIndex)` fires after a drop so you can reorder the backing reactive array. `horizontal` lays the rows out in a row instead of a column; `disabled` freezes it. Dragging uses native HTML5 drag-and-drop, which fires no events on touch devices — on mobile, expose the keyboard path (the rows are focusable) or move/reorder buttons instead.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `Node[]` | **yes** |  |
| `onReorder` | `callable` | no | (fromIndex, toIndex) => … |
| `handle` | `boolean` | no | Show a drag handle on each row (default true) |
| `horizontal` | `boolean` | no | Lay the rows out horizontally (chip rows, kanban column headers) — the arrow keys follow the axis |
| `disabled` | `boolean` | no | Freeze the list: no dragging, no keyboard reordering (read-only, or a reorder mutation is in flight) |
| `ariaLabel` | `string` | no | Name for the list, announced to screen readers (e.g. "Task order") |

### Draggable

```
Draggable(child, data?, type?, disabled?, ariaLabel?, onDragStart?, onDragEnd?)
```

Makes its child draggable, carrying a `data` payload picked up by a DropZone. `type` tags the payload so a DropZone can `accept` (or refuse) it before the drop; `disabled` makes it immovable without re-rendering a different child. `onDragStart`/`onDragEnd` optional. Keyboard users focus it and press Space/Enter to pick up (Escape cancels), then Space/Enter on a DropZone to drop — give it an `ariaLabel` so they know what it is.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `child` | `Node` | **yes** |  |
| `data` | `any` | no | Payload (stringified) handed to the DropZone |
| `type` | `string` | no | Payload kind, e.g. "card" / "file" / "tag" — a DropZone's `accept` matches against it |
| `disabled` | `boolean` | no | Not draggable (locked, in flight, not the user's to move) |
| `ariaLabel` | `string` | no | What is being dragged, announced to screen readers |
| `onDragStart` | `callable` | no |  |
| `onDragEnd` | `callable` | no |  |

### DropZone

```
DropZone(child?, onDrop?, label?, accept?, disabled?, ariaLabel?)
```

A target that accepts a Draggable. `onDrop(data)` receives the dropped payload (parsed JSON when possible). `accept` lists the Draggable `type`s this zone can take (comma-separated) — anything else is refused before it is dropped, so a 'To do' / 'Done' / 'Archive' board can express what goes where; `disabled` makes the zone inert. Pass `child` for the zone's content, or just `label` for a bare labelled target. Keyboard users focus the zone and press Space/Enter to drop what a Draggable picked up.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `child` | `Node` | no | Zone content. Omit it and pass `label` for a plain labelled target |
| `onDrop` | `callable` | no |  |
| `label` | `string` | no | Text shown when no `child` is given |
| `accept` | `string` | no | Draggable `type`s this zone accepts, e.g. "card" or "card, file". Omit to accept anything |
| `disabled` | `boolean` | no | Inert: no highlight, no drop (the zone is full, locked, or read-only) |
| `ariaLabel` | `string` | no | Name for the zone, announced to screen readers |

### OnGesture

```
OnGesture(child, swipe?, longPress?, doubleTap?, pan?, onPanEnd?, threshold?, disabled?, ariaLabel?)
```

Attach high-level pointer gestures to any component without raw DOM: `swipe(dir)` (left/right/up/down), `longPress()`, `doubleTap()`, `pan({dx, dy})` during a drag and `onPanEnd({dx, dy})` on release — `onPanEnd` is what lets a pan snap back or commit, so pull-to-refresh and swipe-to-dismiss need it. `disabled` suppresses every gesture. Gestures are pointer-only, so the wrapper is also focusable and mirrors them onto the keyboard (Enter/Space → doubleTap, arrows → swipe, the context-menu key → longPress); pass `ariaLabel` to say what it does, or keep a visible control for the same action.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `child` | `Node` | **yes** |  |
| `swipe` | `callable` | no | (dir) => … — fired on a swipe; dir is left\|right\|up\|down |
| `longPress` | `callable` | no |  |
| `doubleTap` | `callable` | no |  |
| `pan` | `callable` | no | ({dx, dy}) => … — fired during a drag |
| `onPanEnd` | `callable` | no | ({dx, dy}) => … — fired on release with the FINAL offset, however short the drag was. Use it to snap back or commit a pan |
| `threshold` | `number` | no | Swipe distance in px (default 40) |
| `disabled` | `boolean` | no | Ignore every gesture (a sheet is open, the row is in edit mode, a mutation is in flight) |
| `ariaLabel` | `string` | no | What the gesture does, announced to screen readers (e.g. "Swipe left to delete") |

