<!--
  GENERATED by scripts/emit-skill.mjs — do not edit by hand.
  Source of truth: `getComponentCatalog()` + `defaultLibrary.componentGroups`
  Regenerate with `npm run build:skill` (or `npm run build`).
-->

# Layout

34 components. [← back to the catalogue index](./index.md)

## When to use what

These are the library's own authoring notes — they encode the intended choice
between similar components, which the prop tables below cannot express.

- THREE primitives cover almost everything: `Column` (stack top→bottom), `Row` (left→right), and `Grid` (equal columns / card walls). Reach for these first.
- `root` is normally a `Column([...])` (or `Container([...])` for a centered page). A `Column` is the page body; put each major chunk in a `Card(...)`.
- `Row([...])` keeps children at their natural width and vertically centered — ideal for toolbars, button rows, label+value pairs, nav bars. Use `justify` to distribute (`between`, `center`, `end`) and `gap` for spacing.
- `Grid([...], { columns: N })` = N equal columns. Omit `columns` for auto-fit (wraps as many ≥`minChildWidth` columns as fit — best for KPI/card grids). Prefer `Grid` over a wrapping `Row` whenever cells should share a width.
- For a 12-column dashboard / sidebar layout use `GridItem` spans: `Grid([GridItem(side, { span: "1/4" }), GridItem(main, { span: "3/4" })])`. Fractions `"1/2"`…`"1/12"` (or numbers 1–12) resolve on a 12-track grid; any `GridItem` child turns the grid on automatically.
- `Center([...], { minHeight })` centers content on both axes — spinners, empty states, hero CTAs, modal bodies. Add `minHeight: "60vh"` to center vertically in a region.
- `Stack` is the responsive escape hatch: use it ONLY when the direction itself must change across breakpoints, e.g. `Stack([...], { direction: {base: "column", md: "row"} })`.
- Make one child in a `Row` expand with `StackItem(child, { grow: 1 })` (e.g. a search input beside a fixed button), or push items apart with a bare `Spacer()` between them.
- `Container([...], { size })` centers a wide page within a comfortable max-width (sm/md/lg/xl/full) — landing pages, articles, marketing sections.
- `Box([...], { padding?, margin?, border?, background?, maxWidth? })` is a plain spacing/surface wrapper for when a `Card` is too heavy.
- `Separator(orientation?, label?)` adds a visual break between sections; pass a `label` for a centered "OR"-style divider.
- `gap`/`padding` spacing tokens are `none|3xs|2xs|xs|sm|md|lg|xl|2xl|3xl` (`none` = 0); `align`/`justify`/`columns`/`direction`/`gap` all accept responsive maps like `{base: …, md: …, lg: …}`.
- Use `Drawer` for side-panel detail views and `Modal` for centered dialogs. `Sticky(children, side?, offset?)` pins a toolbar/banner while content scrolls. `ResizablePanels(primary, secondary)` gives a user-resizable two-pane split. `MasonryGrid([...])` is for Pinterest-style mixed-height walls.

## Reference

### Column

```
Column(children, gap?, align?, justify?, wrap?, reverse?, padding?, inline?, alignContent?)
```

Lay children out vertically (top → bottom) with even spacing — the most common page/section layout. Children stretch to the full width by default; set `align` to `start`/`center`/`end` to change that. This is the recommended root container for a page or a card body.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `children` | `Node[]` | **yes** | Children laid out top → bottom |
| `gap` | `"none"` \| `"3xs"` \| `"2xs"` \| `"xs"` \| `"sm"` \| `"md"` \| `"lg"` \| `"xl"` \| `"2xl"` \| `"3xl"` | no | Vertical spacing between children (default `md`). May be a responsive map. |
| `align` | `"start"` \| `"center"` \| `"end"` \| `"stretch"` | no | Horizontal alignment of children (default `stretch`). May be a responsive map. |
| `justify` | `"start"` \| `"center"` \| `"end"` \| `"between"` \| `"around"` \| `"evenly"` | no | Main-axis distribution. May be a responsive map. |
| `wrap` | `boolean` | no | Wrap children onto multiple lines when they overflow |
| `reverse` | `boolean` | no | Reverse the visual order of children |
| `padding` | `"none"` \| `"3xs"` \| `"2xs"` \| `"xs"` \| `"sm"` \| `"md"` \| `"lg"` \| `"xl"` \| `"2xl"` \| `"3xl"` | no | Inner padding token. May be a responsive map. |
| `inline` | `boolean` | no | Use inline-flex (shrink-to-fit) instead of a full-width block |
| `alignContent` | `"start"` \| `"center"` \| `"end"` \| `"between"` \| `"around"` \| `"stretch"` | no | Alignment of wrapped lines (only when `wrap` is on) |

### Row

```
Row(children, gap?, align?, justify?, grow?, wrap?, reverse?, padding?, inline?, alignContent?)
```

Lay children out horizontally (left → right) with even spacing. The developer-friendly default: children keep their natural width and are vertically centered. Set `grow=true` to make children share the row equally (for equal-width columns prefer `Grid(columns: N)`); drop a `Spacer()` between children to push them apart; wrap a child in `StackItem` for per-child grow/shrink/alignment. Use `wrap=true` for chips/tags that should flow onto multiple lines.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `children` | `Node[]` | **yes** | Children laid out left → right |
| `gap` | `"none"` \| `"3xs"` \| `"2xs"` \| `"xs"` \| `"sm"` \| `"md"` \| `"lg"` \| `"xl"` \| `"2xl"` \| `"3xl"` | no | Horizontal spacing between children (default `md`). May be a responsive map. |
| `align` | `"start"` \| `"center"` \| `"end"` \| `"stretch"` | no | Vertical alignment of children (default `center`). May be a responsive map. |
| `justify` | `"start"` \| `"center"` \| `"end"` \| `"between"` \| `"around"` \| `"evenly"` | no | Main-axis distribution. May be a responsive map. |
| `grow` | `boolean` | no | Children share the row width equally (replaces the old `uniform`) |
| `wrap` | `boolean` | no | Wrap children onto multiple lines when they overflow |
| `reverse` | `boolean` | no | Reverse the visual order of children |
| `padding` | `"none"` \| `"3xs"` \| `"2xs"` \| `"xs"` \| `"sm"` \| `"md"` \| `"lg"` \| `"xl"` \| `"2xl"` \| `"3xl"` | no | Inner padding token. May be a responsive map. |
| `inline` | `boolean` | no | Use inline-flex (shrink-to-fit) instead of a full-width block |
| `alignContent` | `"start"` \| `"center"` \| `"end"` \| `"between"` \| `"around"` \| `"stretch"` | no | Alignment of wrapped lines (only when `wrap` is on) |

### Center

```
Center(children, axis?, minHeight?, gap?, padding?, inline?)
```

Centers its children on both axes — the easy way to drop a spinner, an empty state, a hero call-to-action, or a modal body into the middle of a region. Give it `minHeight` (e.g. `"60vh"`) to center vertically inside a tall area, or `axis` to center on only one axis. Multiple children stack in a column and are centered as a group.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `children` | `Node[]` | **yes** | Content to center |
| `axis` | `"both"` \| `"horizontal"` \| `"vertical"` | no | Which axis to center on (default both) |
| `minHeight` | `string` | no | CSS min-height — set to center vertically inside a tall region (e.g. `60vh`, `400px`) |
| `gap` | `"none"` \| `"3xs"` \| `"2xs"` \| `"xs"` \| `"sm"` \| `"md"` \| `"lg"` \| `"xl"` \| `"2xl"` \| `"3xl"` | no | Spacing between stacked children (default `md`) |
| `padding` | `"none"` \| `"3xs"` \| `"2xs"` \| `"xs"` \| `"sm"` \| `"md"` \| `"lg"` \| `"xl"` \| `"2xl"` \| `"3xl"` | no | Inner padding token |
| `inline` | `boolean` | no | Shrink to fit content instead of filling the available width |

### Stack

```
Stack(children, direction?, gap?, align?, justify?, alignContent?, wrap?, reverse?, uniform?, inline?, padding?)
```

Low-level flex container with a configurable, optionally responsive `direction`. Reach for `Row` or `Column` first — they are clearer for fixed-direction layouts. Use `Stack` when the direction itself must change across breakpoints, e.g. `direction: {base: "column", md: "row"}` for a sidebar that stacks on mobile. `gap`, `align`, `justify`, and `padding` also accept responsive maps. NOTE: a `row` Stack grows its children equally by default (`uniform=true`); set `uniform=false` for natural widths (this is `Row`'s default).

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `children` | `Node[]` | **yes** | Child components to stack |
| `direction` | `"column"` \| `"row"` | no | Layout direction (default column). May be a responsive map like `{base: "column", md: "row"}`. |
| `gap` | `"none"` \| `"3xs"` \| `"2xs"` \| `"xs"` \| `"sm"` \| `"md"` \| `"lg"` \| `"xl"` \| `"2xl"` \| `"3xl"` | no | Spacing between children. May be a responsive map. |
| `align` | `"start"` \| `"center"` \| `"end"` \| `"stretch"` | no | Cross-axis alignment (default stretch). May be a responsive map. |
| `justify` | `"start"` \| `"center"` \| `"end"` \| `"between"` \| `"around"` \| `"evenly"` | no | Main-axis distribution. May be a responsive map. |
| `alignContent` | `"start"` \| `"center"` \| `"end"` \| `"between"` \| `"around"` \| `"stretch"` | no | Alignment of wrapped lines (only when `wrap` is on) |
| `wrap` | `boolean` | no | Wrap children onto multiple lines when they overflow |
| `reverse` | `boolean` | no | Reverse the visual order of children |
| `uniform` | `boolean` | no | Row children share space equally (default true for row stacks; use `Row` for natural widths) |
| `inline` | `boolean` | no | Use inline-flex (shrink-to-fit) instead of a full-width block |
| `padding` | `"none"` \| `"3xs"` \| `"2xs"` \| `"xs"` \| `"sm"` \| `"md"` \| `"lg"` \| `"xl"` \| `"2xl"` \| `"3xl"` | no | Inner padding token. May be a responsive map. |

### StackItem

```
StackItem(child, grow?, shrink?, basis?, alignSelf?, order?, minWidth?, maxWidth?)
```

Per-child flex control inside a `Row`, `Column`, or `Stack`. Wraps one child so it can `grow` to fill leftover space, `shrink`, set a `basis`, override `alignSelf`, or change visual `order` — the building block for asymmetric rows like `Row([StackItem(searchInput, { grow: 1 }), saveButton])` (input expands, button hugs).

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `child` | `Node` | **yes** | Child node to wrap |
| `grow` | `number` | no | flex-grow (0 or 1 typical) |
| `shrink` | `number` | no | flex-shrink (0 or 1 typical) |
| `basis` | `string` | no | flex-basis (`auto`, `0`, or CSS length) |
| `alignSelf` | `"start"` \| `"center"` \| `"end"` \| `"stretch"` | no | Per-item cross-axis alignment |
| `order` | `number` | no | Visual order override |
| `minWidth` | `string` | no | CSS min-width |
| `maxWidth` | `string` | no | CSS max-width |

### Grid

```
Grid(children, columns?, gap?, rowGap?, columnGap?, minChildWidth?, alignItems?, justifyItems?, dense?)
```

Two-dimensional grid with three modes:
  1. AUTO-FIT (default, no `columns`): wraps as many equal columns as fit, each at least `minChildWidth` wide (default 220px) — perfect for card/KPI grids that should reflow on their own.
  2. FIXED (`columns: N`, 1–12): exactly N equal columns.
  3. SPAN (`columns: 12` or any `GridItem` children): a 12-track grid where each `GridItem` sets its own `span` (a number 1–12 or a fraction like `"1/3"`) for dashboards and asymmetric layouts.
`columns` and `gap` accept responsive maps like `{base: 1, md: 2, lg: 4}`.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `children` | `Node[]` | **yes** |  |
| `columns` | `number | object` | no | Fixed column count 1–12. Omit for auto-fit; `12` (or `GridItem` children) enables the 12-track span system. May be a responsive map like `{base: 1, md: 3}`. |
| `gap` | `"none"` \| `"3xs"` \| `"2xs"` \| `"xs"` \| `"sm"` \| `"md"` \| `"lg"` \| `"xl"` \| `"2xl"` \| `"3xl"` | no | Gap on both axes (default `md`). May be a responsive map. |
| `rowGap` | `"none"` \| `"3xs"` \| `"2xs"` \| `"xs"` \| `"sm"` \| `"md"` \| `"lg"` \| `"xl"` \| `"2xl"` \| `"3xl"` | no | Row gap override. May be a responsive map. |
| `columnGap` | `"none"` \| `"3xs"` \| `"2xs"` \| `"xs"` \| `"sm"` \| `"md"` \| `"lg"` \| `"xl"` \| `"2xl"` \| `"3xl"` | no | Column gap override. May be a responsive map. |
| `minChildWidth` | `string` | no | Minimum column width for AUTO-FIT mode, e.g. `"240px"` (default 220px). Also caps the floor in FIXED mode. |
| `alignItems` | `"start"` \| `"center"` \| `"end"` \| `"stretch"` | no | Vertical alignment of items within their cells |
| `justifyItems` | `"start"` \| `"center"` \| `"end"` \| `"stretch"` | no | Horizontal alignment of items within their cells |
| `dense` | `boolean` | no | Dense auto-flow packing — let later items backfill earlier gaps |

### GridItem

```
GridItem(child, span?, offset?, spanAt?, rowSpan?)
```

Wraps a child in a 12-column grid cell with `span`, `offset`, `rowSpan`, and responsive `spanAt` maps. Parent `Grid` auto-enables 12-column mode when any child is a `GridItem`. Fraction spans like `"1/3"` resolve against the 12-column track. With no `span` the item takes one cell, so `GridItem` wrappers are safe inside a `Grid(columns: N)`.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `child` | `Node` | **yes** | Child node to place in the grid |
| `span` | `number | string` | no | Columns to span (1–12) or fraction like "1/2", "1/3" (default: one cell) |
| `offset` | `number` | no | Empty columns before this item (0–11) |
| `spanAt` | `object` | no | Responsive span map `{sm: 12, md: 6, lg: 4}` |
| `rowSpan` | `number` | no | Rows to span — the tall dashboard cell next to stacked KPI cards |

### Box

```
Box(children, padding?, margin?, border?, background?, maxWidth?, radius?)
```

Spacing and surface wrapper for padding, margin, borders, semantic backgrounds, and max-width constraints. Use when a `Card` is too heavy but the content needs a subtle surface or inset. `radius` rounds the surface independently of `border`.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `children` | `Node[]` | **yes** |  |
| `padding` | `"none"` \| `"3xs"` \| `"2xs"` \| `"xs"` \| `"sm"` \| `"md"` \| `"lg"` \| `"xl"` \| `"2xl"` \| `"3xl"` | no | Inner padding. May be a responsive map. |
| `margin` | `"none"` \| `"3xs"` \| `"2xs"` \| `"xs"` \| `"sm"` \| `"md"` \| `"lg"` \| `"xl"` \| `"2xl"` \| `"3xl"` | no | Outer margin. May be a responsive map. |
| `border` | `"none"` \| `"subtle"` \| `"default"` | no | Border preset (default none) |
| `background` | `"none"` \| `"surface"` \| `"muted"` \| `"primary"` \| `"success"` \| `"warning"` \| `"danger"` \| `"info"` | no | Semantic background token |
| `maxWidth` | `string` | no | CSS max-width |
| `radius` | `"none"` \| `"sm"` \| `"md"` \| `"lg"` \| `"pill"` | no | Corner rounding — independent of `border` |

### Container

```
Container(children, size?, maxWidth?, padding?)
```

Centered, max-width content wrapper. Use when a page is wider than comfortable reading width — landing pages, marketing sections, long documents. Picks a sensible default max-width per size; pass `maxWidth` to override with any CSS value.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `children` | `Node[]` | **yes** |  |
| `size` | `"sm"` \| `"md"` \| `"lg"` \| `"xl"` \| `"full"` | no | sm=640 / md=820 / lg=1040 / xl=1280 / full=100% (default lg) |
| `maxWidth` | `string` | no | Custom CSS max-width (overrides `size`) |
| `padding` | `"none"` \| `"3xs"` \| `"2xs"` \| `"xs"` \| `"sm"` \| `"md"` \| `"lg"` \| `"xl"` \| `"2xl"` \| `"3xl"` | no | Horizontal padding (default md) |

### Spacer

```
Spacer(size?, flex?)
```

Explicit space element for fine layout control. By default acts as a flex spacer that pushes following content to the far edge (use inside `Stack(direction="row")`). Pass `size` to render a fixed vertical/horizontal gap instead.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `size` | `"none"` \| `"3xs"` \| `"2xs"` \| `"xs"` \| `"sm"` \| `"md"` \| `"lg"` \| `"xl"` \| `"2xl"` \| `"3xl"` | no | Fixed gap; omit to flex-grow |
| `flex` | `boolean` | no | Flex-grow even when size is set (default true when size omitted) |

### Card

```
Card(children, variant?, padding?, onClick?, href?)
```

Vertical card container. Set `padding: "none"` for a full-bleed body (a Table, image, or list that should meet the card's edges). Pass `onClick` or `href` to make the whole card a single interactive target — it then renders as a `button` / `a` so it is keyboard-operable.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `children` | `Node[]` | **yes** | Card contents |
| `variant` | `"default"` \| `"outlined"` \| `"elevated"` | no |  |
| `padding` | `"none"` \| `"3xs"` \| `"2xs"` \| `"xs"` \| `"sm"` \| `"md"` \| `"lg"` \| `"xl"` \| `"2xl"` \| `"3xl"` | no | Inner padding (default `lg`). Use `none` for a full-bleed body. |
| `onClick` | `callable` | no | Makes the whole card clickable (renders as a `button`) |
| `href` | `string` | no | Makes the whole card a link (renders as an `a`) |

### CardHeader

```
CardHeader(title, subtitle?, eyebrow?, actions?, level?)
```

Card header with title, an optional `eyebrow` line rendered ABOVE the title (category / kicker / pre-headline), and an optional `subtitle` rendered below it. `actions` puts nodes (a Badge, an overflow Menu, an "Edit" Button) on the trailing edge of the title row; `level` sets the heading tag so a page of cards keeps a sane document outline.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `title` | `string` | **yes** |  |
| `subtitle` | `string` | no |  |
| `eyebrow` | `string` | no | Short line shown above the title |
| `actions` | `Node[]` | no | Nodes pinned to the trailing edge of the title row (badge / menu / button) |
| `level` | `number` | no | Heading level for the title, 2–6 (default 3) |

### CardFooter

```
CardFooter(children, justify?)
```

Card footer for actions. Defaults to trailing-aligned buttons; use `justify: "between"` for the "destructive action far left, confirm actions far right" shape, or `start` to left-align them.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `children` | `Node[]` | **yes** |  |
| `justify` | `"start"` \| `"center"` \| `"end"` \| `"between"` | no | Horizontal distribution of the actions (default `end`) |

### CardSection

```
CardSection(children, tone?, align?)
```

Full-bleed, colour-coded band inside a `Card` that groups and semantically tints a chunk of the card's content (edge-to-edge tinted background with a rule above/below). Use to mark a region of a card as activating / success / warning / critical / neutral. For a standalone bordered notice use `Callout` instead.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `children` | `Node[]` | **yes** |  |
| `tone` | `"default"` \| `"activating"` \| `"success"` \| `"warning"` \| `"critical"` \| `"neutral"` \| `"corporate"` \| `"promoting"` | no |  |
| `align` | `"left"` \| `"center"` \| `"right"` | no |  |

### Separator

```
Separator(orientation?, label?, decorative?)
```

Visual divider between content sections. Supports horizontal or vertical orientation, and an optional center `label` (lifted from the legacy `Divider`). Use `decorative=false` to expose the separator to assistive tech.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `orientation` | `"horizontal"` \| `"vertical"` | no |  |
| `label` | `string` | no | Optional label rendered in the middle (horizontal only) |
| `decorative` | `boolean` | no | Hides the separator from assistive tech when true (default) |

### Tabs

```
Tabs(items, defaultValue?, orientation?, onChange?, value?, fitted?)
```

Tabbed container. Children must be TabItem components. Supports `orientation="vertical"` for sidebar-style tabs and built-in keyboard navigation (←/→ or ↑/↓, Home, End). Provide `onChange` to react when the user switches tabs (called with the new tab's value). Pass a `$variable` as `value` for a controlled strip — it is kept in sync both ways, so a button elsewhere on the page (or a route) can switch tabs and a user click updates the variable; `defaultValue` is the initial tab only. Set `fitted` for a strip that spans its container with equal-width triggers (the shape a two- or three-tab section header usually wants).

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `TabItem[]` | **yes** | Tab definitions |
| `defaultValue` | `string` | no | Initially active tab value |
| `orientation` | `"horizontal"` \| `"vertical"` | no | Layout direction (default `horizontal`) |
| `onChange` | `callable` | no | Called with the newly-activated tab value when the user switches tabs |
| `value` | `string` | no | Active tab value — pass a `$variable` to control the strip from host state (written back when the user switches tabs) |
| `fitted` | `boolean` | no | Stretch the strip across its container and split it evenly between the triggers (ignored for `orientation="vertical"`) |

### TabItem

```
TabItem(value, label, children, badge?, icon?, disabled?)
```

Single tab definition (used inside Tabs). Add `badge` for a count chip in the tab trigger, `icon` for a leading Font Awesome icon, and `disabled` for a tab that cannot be selected yet.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `value` | `string` | **yes** | Stable identifier for the tab |
| `label` | `string` | **yes** | Display label |
| `children` | `Node[]` | **yes** | Tab content |
| `badge` | `string` | no | Trailing chip rendered in the tab trigger (count / status) |
| `icon` | `string` | no | Optional Font Awesome icon name shown before the label |
| `disabled` | `boolean` | no | Render the trigger as unavailable (not selectable, skipped by keyboard nav) |

### Accordion

```
Accordion(items, showArrow?, type?, onChange?)
```

Accordion container. Children must be AccordionItem components. Set `showArrow: true` to add a chevron indicator to every item; individual `AccordionItem`s can override via their own `showArrow` prop. `type: "single"` keeps only one section open at a time (the browser closes the previous one); `onChange` is called with the section's title and its new open state whenever any section toggles.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `AccordionItem[]` | **yes** |  |
| `showArrow` | `boolean` | no | Show chevron icon on every item (default false). |
| `type` | `"single"` \| `"multiple"` | no | `single` allows one open section at a time; `multiple` (default) allows any number. |
| `onChange` | `callable` | no | Called with (title, open) when any section toggles |

### AccordionItem

```
AccordionItem(title, children, open?, subtitle?, showArrow?, variant?, disabled?, onToggle?)
```

Single accordion section. Pass a `$variable` as `open` to control it from host state (a "Collapse all" button then works); `onToggle` fires with the new open state, which is where lazy-loading a section's content belongs. `variant` paints a semantic left-edge stripe for check-list style accordions, and `disabled` marks a section that cannot be opened yet.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `title` | `string` | **yes** |  |
| `children` | `Node[]` | **yes** |  |
| `open` | `boolean` | no |  |
| `subtitle` | `string` | no | Second line inside the trigger, under the title — a preview of what the collapsed section holds ("Labels \| Taints \| Maintenance window") |
| `showArrow` | `boolean` | no | Show a chevron icon on the right (default false). Inherits from parent Accordion when unset. |
| `variant` | `"success"` \| `"warning"` \| `"danger"` \| `"neutral"` \| `"info"` | no | Semantic left-edge stripe (success / warning / danger / neutral / info) |
| `disabled` | `boolean` | no | Section cannot be expanded (e.g. a step that is not available yet) |
| `onToggle` | `callable` | no | Called with the new open state whenever the section expands or collapses |

### Modal

```
Modal(title, open, children, size?, footer?, closable?, closeOnBackdrop?, onClose?, onRequestClose?, lazy?)
```

Dialog overlay shown when `open` is true. Pass a `$variable` as `open` to control it — the × button, Escape and the backdrop close the dialog by writing that variable, so a literal `true` or an expression needs `onRequestClose` instead or the dialog cannot be dismissed. The header always renders a × close button (disable via `closable: false`); the optional `footer` slot is the canonical place for action buttons. `closeOnBackdrop=true` opts in to backdrop-click dismissal. `onClose` fires once every time the modal actually closes, however it closed (× button, Escape, backdrop, or a state write from a Cancel/Save button). `lazy: true` skips rendering the body while closed. Accessible by default: the dialog is labelled by its title, renders in the browser's top layer so no transformed ancestor can clip it, moves focus into itself on open and restores it on close, traps Tab, locks background scrolling, and closes on Escape (unless `closable: false`).

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `title` | `string` | **yes** |  |
| `open` | `boolean` | **yes** | Open/closed state — usually a $variable |
| `children` | `Node[]` | **yes** |  |
| `size` | `"sm"` \| `"md"` \| `"lg"` \| `"xl"` \| `"full"` | no | Width preset (default `md`) |
| `footer` | `Node[]` | no | Footer slot — typically a row of action Buttons |
| `closable` | `boolean` | no | Render the header × button (default true) |
| `closeOnBackdrop` | `boolean` | no | Close when the overlay is clicked (default false) |
| `onClose` | `callable` | no | Callable invoked once whenever the modal closes, by any route |
| `onRequestClose` | `callable` | no | Called when the user asks to close (× / Escape / backdrop). Required when `open` is not a plain $variable — that is the only way the dialog can be dismissed. |
| `lazy` | `boolean` | no | Skip rendering `children`/`footer` while closed (defer charts, tables, timers) |

### Drawer

```
Drawer(title, open, children, side?, footer?, onClose?, width?, closeOnBackdrop?)
```

Side drawer overlay shown when `open` is true. Pass a `$variable` as `open` to control it. Choose `side` for slide direction (default right) and `width` for a wide detail drawer. `onClose` fires whenever the drawer is dismissed (× button, Escape, or backdrop click — set `closeOnBackdrop: false` to keep a form safe from stray clicks).

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `title` | `string` | **yes** |  |
| `open` | `boolean` | **yes** | Open/closed state — usually a $variable |
| `children` | `Node[]` | **yes** |  |
| `side` | `"right"` \| `"left"` \| `"top"` \| `"bottom"` | no |  |
| `footer` | `Node[]` | no | Optional footer actions row |
| `onClose` | `callable` | no | Callable invoked when the drawer is dismissed |
| `width` | `string` | no | Panel width for left/right drawers, height for top/bottom (default 420px), e.g. "720px" or "60vw" |
| `closeOnBackdrop` | `boolean` | no | Whether clicking the backdrop dismisses (default true) |

### Steps

```
Steps(items, orientation?)
```

Numbered step-by-step guide. Pass items as `{title, details?, active?, status?}` objects. `active` marks the current step; `status` (`pending|active|complete|error`) additionally distinguishes finished and failed steps. `orientation: "horizontal"` lays the steps across the top of a wizard instead of down the page.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `object[]` | **yes** |  |
| `orientation` | `"vertical"` \| `"horizontal"` | no | Layout direction (default `vertical`) |

### AspectRatio

```
AspectRatio(ratio, children)
```

Container that constrains its child to a fixed aspect ratio (e.g. 16:9 for video embeds, 1:1 for thumbnails). The FIRST child fills the box; any further children are overlays positioned on top of it (a "LIVE" badge over a thumbnail), not stacked below.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `ratio` | `string` | **yes** | `width:height` (e.g. `16:9`, `4:3`) or a decimal like `1.78` |
| `children` | `Node[]` | **yes** |  |

### ScrollArea

```
ScrollArea(children, maxHeight?, direction?, height?, stickToBottom?)
```

Bounded scroll container. Use to clip long lists / logs / chat panels to a fixed max height with a clean scrollbar. `height` gives the pane a stable box (it does not grow with its content); `maxHeight` lets it grow up to a cap. `stickToBottom` keeps the newest line in view as content is appended, until the user scrolls up.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `children` | `Node[]` | **yes** |  |
| `maxHeight` | `string` | no | CSS max-height — the pane grows with its content up to this cap (default 320px) |
| `direction` | `"vertical"` \| `"horizontal"` \| `"both"` | no |  |
| `height` | `string` | no | CSS height — a FIXED box that neither grows nor shrinks with its content |
| `stickToBottom` | `boolean` | no | Auto-scroll to the newest content (logs / chat), until the user scrolls up |

### Sticky

```
Sticky(children, side?, offset?, zIndex?)
```

Wraps content in a `position: sticky` container so it pins to the top (or bottom / left / right) of the nearest scrollable ancestor. Use for toolbar action rows above tables, in-page navs, status banners, and pinned first/last columns in a horizontally scrolling row. Sets `data-stuck="true"` on itself once pinned, so a CSS hook (a shadow/border) can flag the pinned state.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `children` | `Node[]` | **yes** |  |
| `side` | `"top"` \| `"bottom"` \| `"left"` \| `"right"` | no |  |
| `offset` | `string` | no | CSS offset (default 0) |
| `zIndex` | `number` | no | Z-index (default 10) |

### ResizablePanels

```
ResizablePanels(primary, secondary, initialPrimaryWidth?, minPrimaryWidth?, minSecondaryWidth?, onResize?)
```

Two-pane horizontal split with a draggable divider. The user can drag the divider (or focus it and use the arrow keys / Home / End) to resize the primary pane; `onResize` reports the new percentage so the split can be persisted. Use for code editors, file browsers, master/detail layouts that need user-controllable proportions.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `primary` | `Node[]` | **yes** |  |
| `secondary` | `Node[]` | **yes** |  |
| `initialPrimaryWidth` | `string` | no | CSS width for the primary pane (default 40%) |
| `minPrimaryWidth` | `string` | no | Min width of the PRIMARY pane (default 240px) |
| `minSecondaryWidth` | `string` | no | Min width of the secondary pane (default 0) |
| `onResize` | `callable` | no | Called with the primary pane's new width percentage when a drag or key press ends |

### MasonryGrid

```
MasonryGrid(items, columns?, gap?)
```

Pinterest-style column grid. Children flow into columns that reflow on viewport changes. Use for galleries, social-style feeds, and mixed-height card walls. `columns` accepts a responsive map like `{base: 1, md: 2, lg: 4}`. Prefer `Grid` when children should share the same height per row.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `Node[]` | **yes** |  |
| `columns` | `number | object` | no | Preferred column count 1–6 (default 3). May be a responsive map like `{base: 1, md: 2, lg: 4}`. |
| `gap` | `"none"` \| `"3xs"` \| `"2xs"` \| `"xs"` \| `"sm"` \| `"md"` \| `"lg"` \| `"xl"` \| `"2xl"` \| `"3xl"` | no |  |

### Fragment

```
Fragment(children)
```

Groups several siblings into one value WITHOUT adding a layout box — the children become direct children of the parent, so they participate in its flex/grid layout exactly as if they had been written inline. Use to return multiple nodes from a component, or to conditionally group siblings, without a stray `div` that would break a Grid/Stack's spacing. Because there is no element, universal style props (`sx`, `animate`, `id`) have nothing to attach to — wrap the group in a `Box` if you need those.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `children` | `Node[]` | **yes** | Sibling nodes to group |

### Section

```
Section(children, background?, width?, padding?, align?, eyebrow?, title?, subtitle?, id?, actions?)
```

Full-bleed page band → centered max-width container → optional tinted background → optional eyebrow/title/subtitle header. The single most useful primitive for marketing, docs, and settings pages. Children render below the header.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `children` | `Node[]` | **yes** |  |
| `background` | `"base"` \| `"soft"` \| `"surface"` \| `"muted"` \| `"brand"` | no |  |
| `width` | `"sm"` \| `"md"` \| `"lg"` \| `"xl"` \| `"full"` | no |  |
| `padding` | `"none"` \| `"xs"` \| `"sm"` \| `"md"` \| `"lg"` \| `"xl"` | no | Band padding preset (default md) |
| `align` | `"left"` \| `"center"` | no | Centers the generated eyebrow/title/subtitle header; children keep their own alignment |
| `eyebrow` | `string` | no |  |
| `title` | `string | Node[]` | no |  |
| `subtitle` | `string` | no |  |
| `id` | `string` | no |  |
| `actions` | `Node[]` | no | Trailing header actions (e.g. a 'See all' Button), aligned opposite the title |

### Split

```
Split(left, right, ratio?, gap?, divider?, sticky?, stickyOffset?, stackAt?, reverseOnStack?, align?)
```

Two-pane layout: a left/primary node and a right/secondary node, with a controllable `ratio` (1/1, 3/2, 2/3 — or any `a/b` pair like "60/40"), optional `divider`, an optional `sticky` pane that pins on scroll (`stickyOffset` sets how far below the top it pins), and a `stackAt` breakpoint where it collapses to a single column — `reverseOnStack` puts the right pane first once stacked, the usual want for 'text + media'. The canonical 'text + media' / 'code + preview' / 'content + sidebar' section.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `left` | `Node` | **yes** |  |
| `right` | `Node` | **yes** |  |
| `ratio` | `string` | no | Column ratio — a named split (1/1, 3/2, 2/3, 1/3, 2/1…) or any `a/b` pair, e.g. "60/40" |
| `gap` | `"none"` \| `"3xs"` \| `"2xs"` \| `"xs"` \| `"sm"` \| `"md"` \| `"lg"` \| `"xl"` \| `"2xl"` \| `"3xl"` | no |  |
| `divider` | `boolean` | no |  |
| `sticky` | `"left"` \| `"right"` | no | Pin one pane while the other scrolls |
| `stickyOffset` | `string` | no | How far below the viewport top the sticky pane pins (CSS length, default 88px) — set it to your header height |
| `stackAt` | `"sm"` \| `"md"` \| `"lg"` | no | Breakpoint below which it stacks: sm 640px, md 768px, lg 1024px (default md) |
| `reverseOnStack` | `boolean` | no | Show the right pane FIRST once stacked (media-above-text on mobile) |
| `align` | `"start"` \| `"center"` \| `"stretch"` | no |  |

### Bento

```
Bento(items, columns?, gap?, rowHeight?, dense?)
```

Asymmetric 'bento box' grid — the marquee feature-section layout. Children must be BentoCell nodes (a plain node becomes a 1×1 tile). Two rules make it look right: (1) spans must tile the grid exactly — each row's column spans sum to `columns` and row-spans pair up with neighbouring cells, never leaving a dangling track (e.g. `columns: 3` with 4 cells: hero 2×2 + tall 1×2 fill rows 1–2, wide 2×1 + tile fill row 3); (2) set a fixed `rowHeight` (e.g. "180px") whenever cells hold images or cards — the default auto row stretches to the tallest cell and makes the mosaic ragged. Give 1–2 standout cells a big span (hero/wide/tall) and keep the rest 1×1 tiles. `dense` (default true) backfills gaps. Collapses to 2 columns below 920px and 1 below 640px.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `BentoCell[]` | **yes** |  |
| `columns` | `number` | no | Track count 1–8 (default 6) — pick it so the cell spans in every row can sum to it |
| `gap` | `"none"` \| `"3xs"` \| `"2xs"` \| `"xs"` \| `"sm"` \| `"md"` \| `"lg"` \| `"xl"` \| `"2xl"` \| `"3xl"` | no |  |
| `rowHeight` | `string | object` | no | Fixed row track size, e.g. "180px" — set it whenever any cell spans rows or holds images (default minmax(110px, auto) stretches rows to content). May be a responsive map like `{base: "220px", md: "180px"}` since the same track is far too short once the grid collapses to one column |
| `dense` | `boolean` | no | Backfill holes with later cells (default true) |

### BentoCell

```
BentoCell(child, span?, rowSpan?)
```

A single cell in a Bento grid. `span` names a size — `tile` 1×1, `wide` 2×1 (2 columns), `tall` 1×2 (2 rows), `hero` 2×2, `full` (a whole row) — or use a "CxR" string ("2x1"), a bare column-span number, or `{ col, row }`; `rowSpan` adds rows to a named/numeric span. The child stretches to fill the cell, so images/cards crop to the cell's shape. Pick spans that tile the parent Bento with no leftover tracks.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `child` | `Node` | **yes** |  |
| `span` | `"tile"` \| `"wide"` \| `"tall"` \| `"hero"` \| `"full"` \| `"1x1"` \| `"1x2"` \| `"1x3"` \| `"1x4"` \| `"2x1"` \| `"2x2"` \| `"2x3"` \| `"2x4"` \| `"3x1"` \| `"3x2"` \| `"3x3"` \| `"3x4"` \| `"4x1"` \| `"4x2"` \| `"4x3"` \| `"4x4"` \| `"5x1"` \| `"5x2"` \| `"5x3"` \| `"5x4"` \| `"6x1"` \| `"6x2"` \| `"6x3"` \| `"6x4"` \| `"7x1"` \| `"7x2"` \| `"7x3"` \| `"7x4"` \| `"8x1"` \| `"8x2"` \| `"8x3"` \| `"8x4"` \| `"1"` \| `"2"` \| `"3"` \| `"4"` \| `"5"` \| `"6"` \| `"7"` \| `"8"` | no | tile (1×1) \| wide (2×1) \| tall (1×2) \| hero (2×2) \| full (whole row), "2x1", 2, or { col, row } |
| `rowSpan` | `number` | no | Rows to span (combines with a numeric/named span) |

### Overlay

```
Overlay(base, items?)
```

Layers OverlayItem children on top of a base node — corner badges, play buttons over thumbnails, sale ribbons, floating actions. Pass the base as the positional argument and `items` as OverlayItem entries.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `base` | `Node` | **yes** |  |
| `items` | `OverlayItem[]` | no |  |

### OverlayItem

```
OverlayItem(child, anchor?, offset?)
```

A child positioned over an Overlay's base. `anchor` picks a corner/edge/center.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `child` | `Node` | **yes** |  |
| `anchor` | `"top-left"` \| `"top-right"` \| `"bottom-left"` \| `"bottom-right"` \| `"top"` \| `"bottom"` \| `"left"` \| `"right"` \| `"center"` | no |  |
| `offset` | `string` | no | Inset distance (CSS length, e.g. 8px or 1rem) |

