<!--
  GENERATED by scripts/emit-skill.mjs — do not edit by hand.
  Source of truth: `getComponentCatalog()` + `defaultLibrary.componentGroups`
  Regenerate with `npm run build:skill` (or `npm run build`).
-->

# Navigation

16 components. [← back to the catalogue index](./index.md)

## When to use what

These are the library's own authoring notes — they encode the intended choice
between similar components, which the prop tables below cannot express.

- Use `Breadcrumb(["Workspace", "Reports", "Q3"])` at the top of every detail page so users see the path.
- Crumbs navigate by default: a string trail derives `/workspace`, `/workspace/reports`, … from its own labels, and the first crumb gets a home icon. Pass `{ label, to }` objects (or `BreadcrumbItem` nodes) when the real routes are not the slugified labels, `autoLink: false` for a trail that is pure text, and `homeIcon: false` to drop the icon.
- `Navbar(brand?, items?, actions?, sticky?, variant?)` + `NavbarItem(label, to?, href?, icon?, active?, action?, external?)` produces a top navigation bar with brand on the left, links in the middle, and actions on the right — the canonical companion of `Sidebar` for marketing pages, docs, or any product surface without left-side nav.
- `DropdownMenu(trigger, items, side?, align?, label?)` is the click-triggered dropdown menu — use it for user-profile menus, row "…" action menus, and any compact list of actions hanging off a single trigger. Children must be `MenuItem`, `MenuSeparator`, or `MenuLabel` entries.
- `MenuItem(label, action?, icon?, shortcut?, variant?, disabled?)` renders a single row inside a `DropdownMenu`; use `variant="danger"` for destructive actions and `MenuSeparator()`/`MenuLabel(label)` to group related items.

## Reference

### Breadcrumb

```
Breadcrumb(items, separator?, maxItems?, onItemClick?, homeIcon?, autoLink?)
```

Trail showing the user's location. Every crumb except the last one is a link that navigates. Items may be BreadcrumbItem(label, { to?, href? }) nodes, `{ label, to }` / `{ label, href }` objects, or plain strings — a string trail derives its own cumulative route from the labels (`["Workspace", "Reports", "Q3"]` → `/workspace`, `/workspace/reports`), which `autoLink: false` turns off. The first crumb carries a home icon unless `homeIcon: false`; pass an icon name to change it. `maxItems` collapses the middle of a long trail behind an ellipsis.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `BreadcrumbItem[] | string[] | {label, to}[]` | **yes** |  |
| `separator` | `string` | no | Default `/` |
| `maxItems` | `number` | no | Collapse the middle of the trail to an ellipsis once there are more items than this (keeps the first crumb and the tail) |
| `onItemClick` | `callable` | no | Called with (index, label) when a crumb is clicked — fires alongside any navigation |
| `homeIcon` | `boolean | string` | no | Leading icon on the FIRST crumb — `true` (default) uses `house`, `false` removes it, a string picks another Font Awesome name |
| `autoLink` | `boolean` | no | Derive a cumulative route from plain-string labels so they navigate (default `true`). Set `false` for a trail that is pure text unless an item names its own `to`/`href` |

### BreadcrumbItem

```
BreadcrumbItem(label, href?, icon?, to?, onClick?)
```

Single item inside a Breadcrumb trail. Pass `to` for an in-app route (navigated through the built-in router), `href` for an external URL, or `onClick` to handle the click yourself. Omit all three for the current/leaf page (rendered with emphasis).

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `label` | `string` | **yes** |  |
| `href` | `string` | no | External URL — use `to` for in-app routes |
| `icon` | `string` | no | Optional Font Awesome icon name |
| `to` | `string` | no | In-app route, e.g. "/projects" — navigated through the router without reloading the app |
| `onClick` | `callable` | no | Callable fired on click — use instead of `to`/`href` to pop a step without changing the URL |

### Navbar

```
Navbar(brand?, items?, actions?, sticky?, variant?, collapsible?)
```

Top navigation bar with a brand on the left, primary nav items in the middle, and a right-aligned actions slot (user avatar, DropdownMenu, CTA buttons, …). Use `sticky=true` to pin it to the top of the page and `collapsible=true` to fold the item row behind a burger toggle on narrow viewports. The canonical companion of `Sidebar` for product surfaces; prefer Navbar for marketing/docs pages without a sidebar.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `brand` | `string | Node` | no | Workspace/product name (string) or a node (e.g. logo Image) |
| `items` | `NavbarItem[]` | no | Center navigation items |
| `actions` | `Node[]` | no | Right-side controls (Buttons, Avatar, DropdownMenu, …) |
| `sticky` | `boolean` | no | Pin the bar to the top of the viewport |
| `variant` | `"default"` \| `"transparent"` | no | Visual variant |
| `collapsible` | `boolean` | no | Collapse the item row behind a burger toggle below 640px (recommended with more than ~4 items, and with `sticky`) |

### NavbarItem

```
NavbarItem(label, to?, href?, icon?, active?, onClick?, external?, disabled?)
```

Single link inside a Navbar's main item slot. Renders as an inline anchor / button — pass `to` for a router-aware link, `href` for an external link, or `onClick` for a click handler (`onClick` also runs alongside `to`/`href`, e.g. to log the navigation). `active=true` highlights the current page; `disabled=true` greys it out and makes it unclickable.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `label` | `string` | **yes** |  |
| `to` | `string` | no | Internal route (consumes the built-in router) |
| `href` | `string` | no | External href; opens in a new tab when set with `external=true` |
| `icon` | `string` | no |  |
| `active` | `boolean` | no |  |
| `onClick` | `callable` | no | Callable fired on click — runs alongside `to`/`href` navigation, not instead of it |
| `external` | `boolean` | no |  |
| `disabled` | `boolean` | no | Grey out and de-activate the entry (gated feature / missing permission) |

### DropdownMenu

```
DropdownMenu(trigger, items, side?, align?, label?, open?, onOpenChange?, disabled?)
```

Click-triggered dropdown menu. Click the trigger to toggle, click a MenuItem to run its action and close, click outside or press Escape to close without acting; ArrowUp/ArrowDown/Home/End and typeahead move between items. Items may be MenuItem / MenuSeparator / MenuLabel nodes, nested arrays of them, or plain `{label, onClick, icon?, shortcut?, disabled?, checked?, separator?}` objects. Bind a `$variable` to `open` for two-way control, or watch `onOpenChange(isOpen)`.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `trigger` | `Node` | **yes** | Clickable trigger element (typically a Button or Avatar) |
| `items` | `(MenuItem | MenuSeparator | MenuLabel)[]` | **yes** |  |
| `side` | `"bottom"` \| `"top"` \| `"left"` \| `"right"` | no | Where the menu opens relative to the trigger (default "bottom") |
| `align` | `"start"` \| `"center"` \| `"end"` | no | How the menu aligns along the trigger edge (default "start") |
| `label` | `string` | no | Optional ARIA label for the menu |
| `open` | `boolean` | no | Open state — bind a `$variable` for two-way control (an action can then close the menu); a plain `true` pre-opens it |
| `onOpenChange` | `callable` | no | (isOpen) => … fired whenever the menu opens or closes (lazy-load contents, keep a row highlighted, log usage) |
| `disabled` | `boolean` | no | Disable the trigger entirely (read-only or in-flight record) |

### MenuItem

```
MenuItem(label, onClick?, icon?, shortcut?, variant?, disabled?, checked?, role?, keepOpen?)
```

Single item inside a DropdownMenu. Renders a button-style row with an optional leading icon and trailing keyboard-shortcut hint. `onClick` (legacy: `action`) runs when clicked and the menu closes afterwards — set `keepOpen=true` for a toggle the user flips several times. Pass `checked` for a checkbox item (or `role="menuitemradio"` with `checked` for a radio group, e.g. a sort direction or theme picker).

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `label` | `string` | **yes** |  |
| `onClick` | `callable` | no | Callable to execute on click |
| `icon` | `string` | no | Font Awesome icon name shown before the label |
| `shortcut` | `string` | no | Trailing keyboard-shortcut hint (e.g. "⌘ K") |
| `variant` | `"default"` \| `"danger"` | no | Use "danger" for destructive actions |
| `disabled` | `boolean` | no | Unavailable action — the row stays visible and reachable by keyboard, is announced as disabled, and does nothing when activated |
| `checked` | `boolean` | no | Render as a checkable item showing this state (implies `role="menuitemcheckbox"`) |
| `role` | `"menuitem"` \| `"menuitemcheckbox"` \| `"menuitemradio"` | no | ARIA role — use "menuitemradio" with `checked` for a one-of-many group |
| `keepOpen` | `boolean` | no | Leave the menu open after this item is activated (toggles, multi-select) |

### MenuSeparator

```
MenuSeparator()
```

Thin horizontal rule used inside a DropdownMenu to group items.

_No props._


### MenuLabel

```
MenuLabel(label)
```

Small uppercase section header inside a DropdownMenu. Use to group related MenuItems (e.g. "Account", "Workspace", "Danger zone").

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `label` | `string` | **yes** |  |

### NavBar

```
NavBar(brand?, links?, actions?, sticky?, blur?)
```

Marketing top navigation: brand on the left, links in the middle, actions (buttons/toggles) on the right. `sticky` pins it; `blur` adds a frosted-glass backdrop. On narrow viewports the links collapse behind a burger toggle. Distinct from the app-shell Navbar/TopBar.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `brand` | `Node` | no |  |
| `links` | `Node[]` | no | NavLink / Link nodes |
| `actions` | `Node[]` | no | Buttons, ThemeToggle, IconButton, etc. |
| `sticky` | `boolean` | no |  |
| `blur` | `boolean` | no |  |

### TabBar

```
TabBar(items, active?, onChange?, pinned?)
```

Bottom navigation bar for mobile layouts — a row of icon+label tabs. `items` is [{ id, label, icon?, badge?, href?, disabled? }]; an item with `href` renders a real link ("/reports" targets a route, so middle-click and copy-link work); `active` is the current id; `onChange(id)` fires on tap. `pinned` (default true) sticks the bar to the bottom of its scroll container; safe-area padding is always applied.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `object[]` | **yes** | [{ id, label, icon?, badge?, href?, disabled? }] |
| `active` | `string` | no |  |
| `onChange` | `callable` | no | (id) => … |
| `pinned` | `boolean` | no | Stick to the bottom of the scroll container (default true) |

### BackToTop

```
BackToTop(label?, showAfter?, position?)
```

A floating button that smoothly scrolls the page (and the host) back to the top. It appears only after `showAfter` px of scrolling (default 400) so it does not sit over the hero, and stacks above a FloatingActionButton rather than on top of it.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `label` | `string` | no |  |
| `showAfter` | `number` | no | Scroll distance in px before the button appears (0 = always) |
| `position` | `"bottom-right"` \| `"bottom-left"` \| `"bottom-center"` | no | Screen corner (default bottom-right) |

### ScrollSpy

```
ScrollSpy(sections, title?, offset?, top?, onChange?)
```

A sticky in-page nav that highlights the section currently in view. `sections` is an array of {label, id} matching element ids on the page (set via the universal `id` prop). Clicking smooth-scrolls to a section; `offset` clears a sticky header and `top` sets the sticky offset.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `sections` | `object[]` | **yes** |  |
| `title` | `string` | no |  |
| `offset` | `number` | no | Pixels to leave above the target (sticky header height) |
| `top` | `string` | no | Sticky offset from the viewport top (default 16px) |
| `onChange` | `callable` | no | Fires with the id of the section entering view |

### Brand

```
Brand(name, logoSrc?, version?, href?)
```

Logo + product name (+ optional version pill), linking home. Use in NavBar/Footer.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `name` | `string` | **yes** |  |
| `logoSrc` | `string` | no | Logo image src URL, e.g. https://example.com/logo.png |
| `version` | `string` | no |  |
| `href` | `string` | no |  |

### Footer

```
Footer(brand?, tagline?, columns?, legal?, social?)
```

Site footer: brand + tagline in a wide first column, then link columns, and a legal/copyright line. Pass `columns` as FooterColumn nodes.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `brand` | `Node` | no |  |
| `tagline` | `string` | no |  |
| `columns` | `FooterColumn[]` | no |  |
| `legal` | `string | Node[]` | no | Copyright line — a string, or Link nodes for Privacy/Terms/Cookies |
| `social` | `Node[]` | no | Brand/social icon row under the tagline (IconButton / Link nodes) |

### FooterColumn

```
FooterColumn(title, links?)
```

A titled column of links inside a Footer.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `title` | `string` | **yes** |  |
| `links` | `Node[]` | no |  |

### SkipLink

```
SkipLink(to, label?)
```

A keyboard-only 'skip to content' link that is hidden until focused, then jumps to the element with the given id. Place it as the first node in a page so keyboard users can bypass the nav.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `to` | `string` | **yes** | Target element id (with or without #) |
| `label` | `string` | no | Link text (default 'Skip to content') |

