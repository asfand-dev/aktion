<!--
  GENERATED by scripts/emit-skill.mjs — do not edit by hand.
  Source of truth: `getComponentCatalog()` + `defaultLibrary.componentGroups`
  Regenerate with `npm run build:skill` (or `npm run build`).
-->

# Editors & overlays

8 components. [← back to the catalogue index](./index.md)

## When to use what

These are the library's own authoring notes — they encode the intended choice
between similar components, which the prop tables below cannot express.

- `RichTextEditor(id, value?, placeholder?, minHeight?)` is the contenteditable WYSIWYG editor — toolbar ships with bold/italic/underline/strike/heading/lists/link. Bind `value` to a `$variable` holding HTML.
- `CodeEditor(id, value?, language?, placeholder?, minHeight?)` is a lightweight `<textarea>` with a synced line-number gutter and tab indentation. Use for snippet editors, prompt sandboxes, settings JSON.
- `ContextMenu(target, items)` attaches a right-click menu to any node. `items` are `MenuItem` or `{label, action?, icon?, shortcut?, variant?, disabled?}` objects; pass a `MenuSeparator()` to split groups.

## Reference

### RichTextEditor

```
RichTextEditor(id, value?, placeholder?, minHeight?, disabled?, onChange?, tools?, maxHeight?, readonly?, name?, label?, hint?, error?, required?)
```

Rich-text WYSIWYG editor for CMS, email, and comment surfaces. Renders a small toolbar (bold / italic / underline / strikethrough / headings / lists / quote / link) above a `contenteditable` region; pass `tools` to subset it. Pass `$variable` as `value` for two-way binding — the HTML body is written back to state on every edit. Provide `placeholder` for the empty-state prompt, `maxHeight` to make a long document scroll, `readonly` for review flows, and `label`/`hint`/`error`/`required` for a labelled field shell.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `id` | `string` | **yes** |  |
| `value` | `string` | no | Bound HTML body (typically $variable) |
| `placeholder` | `string` | no | Empty-state prompt |
| `minHeight` | `string` | no | CSS min-height for the editor area (default 160px) |
| `disabled` | `boolean` | no |  |
| `onChange` | `callable` | no | Called with the current HTML on every keystroke |
| `tools` | `"bold"` \| `"italic"` \| `"underline"` \| `"strikethrough"` \| `"heading"` \| `"quote"` \| `"bulletList"` \| `"numberedList"` \| `"link"` | no | Subset (and order) of toolbar tools — defaults to all of them |
| `maxHeight` | `string` | no | CSS max-height for the editor area; the body scrolls past it |
| `readonly` | `boolean` | no | Content stays readable and selectable at full contrast but cannot be edited |
| `name` | `string` | no | Form field name — the HTML body is mirrored into a hidden input for native submission |
| `label` | `string` | no | Field label rendered above the editor |
| `hint` | `string` | no | Helper text rendered below the editor |
| `error` | `string` | no | Validation error rendered below the editor (marks it invalid) |
| `required` | `boolean` | no | Mark the field required (adds a `*`) |

### CodeEditor

```
CodeEditor(id, value?, language?, placeholder?, minHeight?, tabSize?, showGutter?, readonly?, onChange?, maxHeight?, filename?, copyable?, onSave?, name?, disabled?, label?, hint?, error?, required?)
```

Lightweight, dependency-free code editor. Pairs a styled textarea with a synchronised line-number gutter — no syntax highlighting, but the editor stays a single rendered node so it works inside Shadow DOM. Tab / Shift+Tab indent and unindent the selected lines, and Ctrl/Cmd+S calls `onSave`. Use for dev tooling, snippet editing, prompt playgrounds. Pass a `$variable` as `value` for two-way binding, `maxHeight` for a fixed-height scrolling pane, and `label`/`hint`/`error` to surface a parse error under the field. For read-only rendering with highlights prefer `CodeBlock`.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `id` | `string` | **yes** |  |
| `value` | `string` | no | Bound source text (typically $variable) |
| `language` | `"text"` \| `"javascript"` \| `"typescript"` \| `"json"` \| `"html"` \| `"css"` \| `"bash"` \| `"python"` \| `"sql"` \| `"markdown"` | no | Language label shown on the header (default `text`) |
| `placeholder` | `string` | no |  |
| `minHeight` | `string` | no | CSS min-height for the editor area (default 200px) |
| `tabSize` | `number` | no | Spaces per Tab (default 2) |
| `showGutter` | `boolean` | no | Show line-number gutter (default true) |
| `readonly` | `boolean` | no | Content stays focusable and submittable but cannot be edited |
| `onChange` | `callable` | no | Called with the current source on every keystroke |
| `maxHeight` | `string` | no | CSS max-height for the editor area; the body scrolls past it |
| `filename` | `string` | no | Filename shown on the left of the header |
| `copyable` | `boolean` | no | Show a copy-to-clipboard button on the right of the header |
| `onSave` | `callable` | no | Called with the current source on Ctrl/Cmd+S |
| `name` | `string` | no | Form field name (defaults to `id`) |
| `disabled` | `boolean` | no | Disable the control (non-editable, skipped by tab order, not submitted) |
| `label` | `string` | no | Field label rendered above the editor |
| `hint` | `string` | no | Helper text rendered below the editor |
| `error` | `string` | no | Validation error rendered below the editor (marks it invalid) |
| `required` | `boolean` | no | Mark the field required (adds a `*`) |

### ContextMenu

```
ContextMenu(target, items, label?, trigger?, disabled?, placement?, offset?, open?, onOpenChange?)
```

Right-click (or long-press on touch) menu that attaches to a child node. Wraps `target` and shows the menu at the pointer. Items are `MenuItem(...)` nodes, `MenuSeparator()` entries, or `{label, action, icon?, shortcut?, variant?, disabled?, separator?}` objects. Set `trigger: "click"` for the overflow-menu pattern, `disabled` to suppress it on locked rows, and pass a `$variable` as `open` to observe or drive the open state. Shift+F10 (or the ContextMenu key) opens it from the keyboard, and ArrowUp/ArrowDown/Home/End plus typeahead move between items while Escape closes it and returns focus to the target. Use on table rows, tree nodes, kanban cards, file browser entries.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `target` | `Node` | **yes** | Child node the menu is bound to |
| `items` | `any[]` | **yes** | MenuItem nodes or {label, action} objects |
| `label` | `string` | no | ARIA label for the menu |
| `trigger` | `"contextmenu"` \| `"click"` \| `"longpress"` | no | What opens the menu (default `contextmenu`) |
| `disabled` | `boolean` | no | Never open the menu (locked / read-only rows) |
| `placement` | `"bottom"` \| `"top"` \| `"left"` \| `"right"` | no | Which side of the anchor the menu opens on (default `bottom`) |
| `offset` | `number` | no | Gap in px between the anchor and the menu (default 0) |
| `open` | `boolean` | no | Open state — pass a $variable for two-way binding |
| `onOpenChange` | `callable` | no | Called with the new boolean open state |

### Sheet

```
Sheet(children, open?, side?, title?, onClose?, label?, width?, footer?, dismissible?)
```

A panel that slides in from an edge over a dimmed backdrop. `side` is left|right|top|bottom. Bind `open` to a $variable; `onClose` fires on backdrop click / Escape (set `dismissible: false` to require an explicit action). `footer` pins an action row below the scrolling body. Use BottomSheet for the mobile bottom variant.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `children` | `Node[]` | **yes** |  |
| `open` | `boolean` | no |  |
| `side` | `"left"` \| `"right"` \| `"top"` \| `"bottom"` | no |  |
| `title` | `string` | no |  |
| `onClose` | `callable` | no |  |
| `label` | `string` | no | Accessible name when there is no visible `title` |
| `width` | `string` | no | Panel width for left/right sides (default min(420px, 90vw)) |
| `footer` | `Node[]` | no | Pinned action row (Cancel / Apply) |
| `dismissible` | `boolean` | no | Backdrop click + Escape close it (default true) |

### BottomSheet

```
BottomSheet(children, open?, title?, onClose?, label?, height?, showClose?, footer?)
```

A mobile bottom sheet — Sheet pinned to the bottom edge. The grip is draggable: swiping it down dismisses the sheet. Bind `open`; `onClose` on dismiss. `height` fixes the panel size so a filtering list scrolls instead of resizing the sheet; `footer` pins a primary action.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `children` | `Node[]` | **yes** |  |
| `open` | `boolean` | no |  |
| `title` | `string` | no |  |
| `onClose` | `callable` | no |  |
| `label` | `string` | no | Accessible name when there is no visible `title` |
| `height` | `string` | no | Fixed panel height, e.g. "50vh" (default: hugs content, max 85vh) |
| `showClose` | `boolean` | no | Render the × button (default true) |
| `footer` | `Node[]` | no | Pinned action row |

### ConfirmDialog

```
ConfirmDialog(title, open?, message?, confirmLabel?, cancelLabel?, tone?, onConfirm?, onCancel?, loading?, confirmDisabled?, icon?)
```

A confirm/cancel modal dialog. Bind `open` to a $variable; `onConfirm` fires on accept, `onCancel`/backdrop on dismiss. `tone` colors the confirm button (danger for destructive actions) and picks a matching icon. Binding `loading` keeps the dialog open on confirm and disables both buttons so an async action can report progress — write `false` to `open` yourself once it resolves.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `title` | `string` | **yes** |  |
| `open` | `boolean` | no |  |
| `message` | `string` | no |  |
| `confirmLabel` | `string` | no |  |
| `cancelLabel` | `string` | no |  |
| `tone` | `"primary"` \| `"danger"` \| `"warning"` | no |  |
| `onConfirm` | `callable` | no |  |
| `onCancel` | `callable` | no |  |
| `loading` | `boolean` | no | Async confirm in flight — keeps the dialog open, disables both buttons |
| `confirmDisabled` | `boolean` | no | Guard the primary action until a condition is met |
| `icon` | `string` | no | Glyph beside the title (defaults from `tone`) |

### SpeedDial

```
SpeedDial(actions, icon?, open?, onOpenChange?, position?, label?)
```

A floating action button that expands a stack of mini-actions on click. Pass `actions` as {icon, label, onClick} (`action` is accepted as a synonym for `onClick`). Bind `open` to control it from outside; `position` picks the corner. Outside-click and Escape close it.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `actions` | `object[]` | **yes** |  |
| `icon` | `string` | no | Main FAB icon (default 'plus') |
| `open` | `boolean` | no | Expanded state — usually a $variable |
| `onOpenChange` | `callable` | no | Fires with the new expanded state |
| `position` | `"bottom-right"` \| `"bottom-left"` \| `"top-right"` \| `"top-left"` | no |  |
| `label` | `string` | no | Accessible name for the FAB (default 'Actions') |

### FloatingActionButton

```
FloatingActionButton(icon, label?, onClick?, position?, extended?, disabled?)
```

A fixed circular action button (FAB). `icon` + `onClick`; `position` picks the corner (bottom-right default — move it to sit beside a BackToTop or a chat launcher, or for RTL), `extended: true` shows `label` as visible text next to the icon, and `disabled` locks it while a request is in flight.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `icon` | `string` | **yes** |  |
| `label` | `string` | no | Accessible label (visible text when `extended`) |
| `onClick` | `callable` | no |  |
| `position` | `"bottom-right"` \| `"bottom-left"` \| `"bottom-center"` | no | Screen corner (default bottom-right) |
| `extended` | `boolean` | no | Pill-shaped icon + text FAB |
| `disabled` | `boolean` | no |  |

