<!--
  GENERATED by scripts/emit-skill.mjs — do not edit by hand.
  Source of truth: `getComponentCatalog()` + `defaultLibrary.componentGroups`
  Regenerate with `npm run build:skill` (or `npm run build`).
-->

# Chat

6 components. [← back to the catalogue index](./index.md)

## When to use what

These are the library's own authoring notes — they encode the intended choice
between similar components, which the prop tables below cannot express.

- End most responses with a `FollowUpBlock` of 2–4 short prompts to keep the conversation moving.
- `ChatBubble(author, body, time?, avatarSrc?, from?)` renders a single message bubble; use `from="me"` for the active speaker and `from="agent"` for the assistant. Compose transcripts as `Stack([ChatBubble(...), ChatBubble(...), …])` inside a `Card`.

## Reference

### SectionBlock

```
SectionBlock(title, children, description?, actions?, level?, icon?)
```

Titled chat block with a description and child content. `level` sets the heading level (2-6, default 3) so nested sections keep a valid outline; `actions` renders trailing controls in the header row (a "View all" link, an overflow menu).

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `title` | `string` | **yes** |  |
| `children` | `Node[]` | **yes** |  |
| `description` | `string` | no |  |
| `actions` | `Node[]` | no | Header-right controls, e.g. a Link or DropdownMenu |
| `level` | `number` | no | Heading level 2-6 (default 3); the visual size is unchanged |
| `icon` | `string` | no | Font Awesome icon shown before the title |

### ListBlock

```
ListBlock(items, ordered?, marker?, start?)
```

Chat-styled list with bullets, useful for steps or summaries. `items` takes plain strings or component nodes (Text, ActionLink, Badge, …), which render inline. `marker` switches to an unmarked or checklist list; `start` continues an `ordered` list that was split across blocks.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `any[]` | **yes** | Strings, or component nodes rendered inside the list item |
| `ordered` | `boolean` | no |  |
| `marker` | `"bullet"` \| `"none"` \| `"check"` | no | Bullet style (default `bullet`); `none` drops the marker, `check` shows a checkmark |
| `start` | `number` | no | First number of an `ordered` list (default 1) |

### FollowUpBlock

```
FollowUpBlock(items, title?, onSelect?, disabled?, layout?)
```

Suggested follow-up prompts shown as buttons. Each item dispatches its label as an assistant message (equivalent to `emit "assistant-message" { message }`) unless `onSelect` is supplied, which receives `(message, label)` instead. `disabled` makes the row inert while the assistant is responding; `layout: "stack"` puts one suggestion per line.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `FollowUpItem[]` | **yes** | Array of FollowUpItem(label, message?), {label, message, disabled?} objects, or plain strings |
| `title` | `string` | no |  |
| `onSelect` | `callable` | no | (message, label) => … — replaces the default assistant-message dispatch |
| `disabled` | `boolean` | no | Render every chip inert (e.g. once one has been picked) |
| `layout` | `"wrap"` \| `"stack"` | no | `wrap` (default) flows chips in a row; `stack` gives each a full line — better for long suggestion sentences |

### FollowUpItem

```
FollowUpItem(label, message?)
```

Single follow-up item.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `label` | `string` | **yes** |  |
| `message` | `string` | no | Defaults to label |

### ActionLink

```
ActionLink(label, onClick, disabled?, icon?, iconPosition?, ariaLabel?)
```

Inline link that runs an action when clicked instead of navigating. `disabled` makes it inert while the work is in flight; `icon` adds a glyph before the label (or after it with `iconPosition: "end"`).

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `label` | `string` | **yes** |  |
| `onClick` | `callable` | **yes** |  |
| `disabled` | `boolean` | no | Make the action inert, e.g. while a Retry is already running |
| `icon` | `string` | no | Font Awesome icon shown with the label |
| `iconPosition` | `"start"` \| `"end"` | no | Which side the icon sits on (default `start`) |
| `ariaLabel` | `string` | no | Accessible name, when the visible label alone does not identify the target — e.g. one "Rebuild" link per table row, where every link would otherwise be announced identically |

### ChatBubble

```
ChatBubble(author, body, time?, avatarSrc?, from?, status?, content?, onRetry?)
```

Single chat-style message bubble with author, time, and body. Use for conversation threads, agent transcripts, support chats, and any message-style UI. Set `from="me"` for the active speaker — the bubble aligns to the right with a primary tint. `from="agent"` (default) renders as the canonical incoming bubble on the left, and `from="system"` as an avatar-less transcript annotation. Pass `content: [...]` for rich bodies (CodeBlock, Image, Link, Table) and `onRetry` alongside `status: "error"` so a failed send is recoverable.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `author` | `string` | **yes** |  |
| `body` | `string` | **yes** |  |
| `time` | `string` | no |  |
| `avatarSrc` | `string` | no |  |
| `from` | `"agent"` \| `"me"` \| `"system"` | no | Lane (default agent) |
| `status` | `"sending"` \| `"sent"` \| `"delivered"` \| `"read"` \| `"error"` | no |  |
| `content` | `Node[]` | no | Rich body nodes rendered under `body` (CodeBlock, Image, Link, Table, …) |
| `onRetry` | `callable` | no | Shown as a Retry action next to `status: "error"` |

