<!--
  GENERATED by scripts/emit-skill.mjs — do not edit by hand.
  Source of truth: `getComponentCatalog()` + `defaultLibrary.componentGroups`
  Regenerate with `npm run build:skill` (or `npm run build`).
-->

# Charts

9 components. [← back to the catalogue index](./index.md)

## When to use what

These are the library's own authoring notes — they encode the intended choice
between similar components, which the prop tables below cannot express.

- Use `LineChart` for trends (pass `filled=true` for area-style charts), `BarChart` for comparisons, `PieChart` for proportions, `RadarChart` for multi-axis scorecards.
- `Heatmap(xLabels, yLabels, values)` renders a color-intensity matrix — perfect for calendar heatmaps, correlation grids, schedule density.
- `ScatterChart(series, xLabel?, yLabel?)` plots XY points; pass each Series as `Series(name, points)` where points are `{x, y, label?}`.
- `Histogram(values, binCount?)` bins raw numbers; pass pre-computed `bins=[{label, count}]` instead when you control the bucketing.
- `Gauge(value, min?, max?, label?, tone?, size?)` is the half-doughnut KPI indicator for thresholds (uptime %, score, NPS).
- Pass series via `Series("Name", [...numbers])`.

## Reference

### BarChart

```
BarChart(labels, series, title?, stacked?, horizontal?, xAxisLabel?, yAxisLabel?, showLegend?, height?, loading?, emptyText?, onBarClick?, ariaLabel?, decorative?)
```

Bar chart. `labels` define the category axis, `series` define the bars. Pass `stacked: true` to stack the series instead of grouping them, `horizontal: true` when the category names are too long for a vertical axis, and `onBarClick(label, value, seriesName)` for drill-down.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `labels` | `string[]` | **yes** |  |
| `series` | `Series[]` | **yes** |  |
| `title` | `string` | no |  |
| `stacked` | `boolean` | no | Stack the series on top of each other instead of grouping them side by side |
| `horizontal` | `boolean` | no | Draw the bars left-to-right — the readable layout for long category names |
| `xAxisLabel` | `string` | no | Caption for the category axis |
| `yAxisLabel` | `string` | no | Caption for the value axis (e.g. "Revenue (EUR)") |
| `showLegend` | `boolean` | no | Render the series legend (default true) |
| `height` | `number` | no | Plot height in px (default 240) |
| `loading` | `boolean` | no | Render a loading placeholder instead of the plot |
| `emptyText` | `string` | no | Message shown when there is no data (default "No data") |
| `onBarClick` | `callable` | no | (label, value, seriesName) => void, fired when a bar is activated |
| `ariaLabel` | `string` | no | Accessible name for the graphic, overriding the generated one — the way to name a chart that has no visible `title` |
| `decorative` | `boolean` | no | Take the graphic out of the accessibility tree. Only for a chart that repeats information already present in the surrounding text |

### LineChart

```
LineChart(labels?, series?, data?, title?, filled?, stacked?, yMin?, yMax?, xAxisLabel?, yAxisLabel?, showLegend?, height?, loading?, emptyText?, onPointClick?, ariaLabel?, decorative?)
```

Line chart. `labels` define the x-axis, each Series is a line. As a shortcut you can pass `data=[{x: "Jan", revenue: 12, signups: 4}, …]` and the labels + series will be derived automatically (one line per non-`x` numeric key; a row that omits a key leaves a gap in that line). Use `data` when the dataset is already row-shaped; use `series` when you have explicit Series objects.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `labels` | `string[]` | no |  |
| `series` | `Series[]` | no |  |
| `data` | `{x: string, [key: string]: number}[]` | no | Row-shaped data — labels and series are auto-derived |
| `title` | `string` | no |  |
| `filled` | `boolean` | no | Fill the area beneath each line (area-chart style) |
| `stacked` | `boolean` | no | Stack series when filled=true |
| `yMin` | `number` | no | Pin the bottom of the value axis (default 0 or the data minimum) |
| `yMax` | `number` | no | Pin the top of the value axis (default the data maximum) |
| `xAxisLabel` | `string` | no | Caption for the x axis |
| `yAxisLabel` | `string` | no | Caption for the value axis (e.g. "requests/sec") |
| `showLegend` | `boolean` | no | Render the series legend (default true) |
| `height` | `number` | no | Plot height in px (default 240) |
| `loading` | `boolean` | no | Render a loading placeholder instead of the plot |
| `emptyText` | `string` | no | Message shown when there is no data (default "No data") |
| `onPointClick` | `callable` | no | (label, value, seriesName) => void, fired when a data point is activated |
| `ariaLabel` | `string` | no | Accessible name for the graphic, overriding the generated one — the way to name a chart that has no visible `title` |
| `decorative` | `boolean` | no | Take the graphic out of the accessibility tree. Only for a chart that repeats information already present in the surrounding text |

### PieChart

```
PieChart(labels, values, title?, showValues?, valueFormat?, donut?, innerRadius?, size?, showLegend?, legendPosition?, loading?, emptyText?, onSliceClick?, ariaLabel?, decorative?)
```

Pie / Donut chart. Each segment maps to a label/value pair. Pass `donut: true` (or an `innerRadius` fraction) for a donut. Numeric labels are rendered on every segment by default — set `showValues: false` to hide them, or `valueFormat: "percent"` to show the share instead of the raw value.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `labels` | `string[]` | **yes** |  |
| `values` | `number[]` | **yes** |  |
| `title` | `string` | no |  |
| `showValues` | `boolean` | no | Render numeric labels on each slice (default true) |
| `valueFormat` | `"value"` \| `"percent"` \| `"both"` | no | How to format the on-segment label (default "value") |
| `donut` | `boolean` | no | Cut a hole in the middle (default innerRadius 0.6) |
| `innerRadius` | `number` | no | Hole size as a fraction of the radius, 0–0.9 (implies donut) |
| `size` | `number` | no | Diameter in px (default 240) |
| `showLegend` | `boolean` | no | Render the label legend (default true) |
| `legendPosition` | `"bottom"` \| `"right"` | no | Where the legend sits relative to the pie (default "bottom") |
| `loading` | `boolean` | no | Render a loading placeholder instead of the pie |
| `emptyText` | `string` | no | Message shown when there is no positive value (default "No data") |
| `onSliceClick` | `callable` | no | (label, value, share) => void, fired when a slice is activated |
| `ariaLabel` | `string` | no | Accessible name for the graphic, overriding the generated one — the way to name a chart that has no visible `title` |
| `decorative` | `boolean` | no | Take the graphic out of the accessibility tree. Only for a chart that repeats information already present in the surrounding text |

### RadarChart

```
RadarChart(axes, series, max?, title?, size?, showDots?, showValues?, emptyText?, ariaLabel?, decorative?)
```

Polygon chart with one axis per category (at least three). Use for skill maps, scorecards, capability comparisons, and any multi-dimensional snapshot. Each Series renders as a filled polygon — overlapping is expected for comparisons. Set `showValues: true` to print the number at each vertex.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `axes` | `string[]` | **yes** | Category labels — one per radial axis (minimum 3) |
| `series` | `Series[]` | **yes** |  |
| `max` | `number` | no | Outer ring value (default = max across series) |
| `title` | `string` | no |  |
| `size` | `number` | no | Diameter in px (default 280) — raise it for long axis names |
| `showDots` | `boolean` | no | Draw a marker at each vertex (default true) |
| `showValues` | `boolean` | no | Print each series value next to its vertex |
| `emptyText` | `string` | no | Message shown when there is no data (default "No data") |
| `ariaLabel` | `string` | no | Accessible name for the graphic, overriding the generated one — the way to name a chart that has no visible `title` |
| `decorative` | `boolean` | no | Take the graphic out of the accessibility tree. Only for a chart that repeats information already present in the surrounding text |

### ScatterChart

```
ScatterChart(series, xLabel?, yLabel?, title?, pointSize?, pointOpacity?, xMin?, xMax?, yMin?, yMax?, height?, showLegend?, emptyText?, onPointClick?, ariaLabel?, decorative?)
```

XY scatter plot — one dot per data point, optionally grouped by series. Pass each `Series(name, points)` with points as `{x, y, label?}` objects or `[x, y, label?]` tuples. Tune `pointSize` / `pointOpacity` for dense distributions and pin `xMin`/`xMax`/`yMin`/`yMax` when two plots must share a scale. Use for correlations, distributions, and "price vs. rating" style charts.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `series` | `Series[]` | **yes** |  |
| `xLabel` | `string` | no |  |
| `yLabel` | `string` | no |  |
| `title` | `string` | no |  |
| `pointSize` | `number` | no | Dot radius in px (default 7 — lower it for dense plots) |
| `pointOpacity` | `number` | no | Dot fill opacity 0–1 (default 0.8) |
| `xMin` | `number` | no | Pin the left of the x axis (default the data minimum) |
| `xMax` | `number` | no | Pin the right of the x axis (default the data maximum) |
| `yMin` | `number` | no | Pin the bottom of the y axis (default the data minimum) |
| `yMax` | `number` | no | Pin the top of the y axis (default the data maximum) |
| `height` | `number` | no | Plot height in px (default 280) |
| `showLegend` | `boolean` | no | Render the series legend (default true) |
| `emptyText` | `string` | no | Message shown when there are no points (default "No points") |
| `onPointClick` | `callable` | no | (x, y, label, seriesName) => void, fired when a point is activated |
| `ariaLabel` | `string` | no | Accessible name for the graphic, overriding the generated one — the way to name a chart that has no visible `title` |
| `decorative` | `boolean` | no | Take the graphic out of the accessibility tree. Only for a chart that repeats information already present in the surrounding text |

### Histogram

```
Histogram(values?, binCount?, bins?, title?, tone?, xLabel?, yLabel?, height?, emptyText?, loading?, onBinClick?, ariaLabel?, decorative?)
```

Frequency distribution from raw numeric values. Pass `values` directly (the component bins them automatically, `binCount` bins) or pre-computed `bins` of `{label, count}` objects. Use for response-time histograms, score distributions, age buckets.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `values` | `number[]` | no | Raw observations (binned automatically) |
| `binCount` | `number` | no | Number of bins when computing from `values` (default 10) |
| `bins` | `object[]` | no | Pre-computed {label, count} entries (overrides `values`) |
| `title` | `string` | no |  |
| `tone` | `"primary"` \| `"success"` \| `"warning"` \| `"danger"` \| `"info"` | no | Bar colour token (default the first palette colour) |
| `xLabel` | `string` | no | Caption for the value axis (e.g. "ms") |
| `yLabel` | `string` | no | Caption for the frequency axis (e.g. "requests") |
| `height` | `number` | no | Plot height in px (default 240) |
| `emptyText` | `string` | no | Message shown when there is no data (default "No data") |
| `loading` | `boolean` | no | Render a loading placeholder instead of the plot |
| `onBinClick` | `callable` | no | (binLabel, count, binIndex) => void, fired when a bar is activated |
| `ariaLabel` | `string` | no | Accessible name for the graphic, overriding the generated one — the way to name a chart that has no visible `title` |
| `decorative` | `boolean` | no | Take the graphic out of the accessibility tree. Only for a chart that repeats information already present in the surrounding text |

### Heatmap

```
Heatmap(xLabels, yLabels, values, title?, tone?, showValues?, min?, max?, valueFormat?, emptyText?, onCellClick?, ariaLabel?, decorative?)
```

Color-intensity matrix grid (calendar-style or correlation-style). Pass `xLabels`, `yLabels`, and a `values` array of arrays (rows × columns). Cell intensity scales across the data range — pin it with `min`/`max` when two heatmaps must be comparable (or for a -1..1 correlation matrix). Set `showValues: false` for a GitHub-style colour-only grid. Use for activity heatmaps, schedule density, correlation matrices.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `xLabels` | `string[]` | **yes** |  |
| `yLabels` | `string[]` | **yes** |  |
| `values` | `number[][]` | **yes** | Matrix indexed by row (y), then column (x) |
| `title` | `string` | no |  |
| `tone` | `"primary"` \| `"success"` \| `"warning"` \| `"danger"` \| `"info"` | no |  |
| `showValues` | `boolean` | no | Print the number inside each cell (default true) |
| `min` | `number` | no | Value mapped to the lightest cell (default 0 or the data minimum) |
| `max` | `number` | no | Value mapped to the most saturated cell (default the data maximum) |
| `valueFormat` | `"value"` \| `"compact"` \| `"percent"` | no | How cell numbers are formatted (default "value") |
| `emptyText` | `string` | no | Message shown when there is no data (default "No data") |
| `onCellClick` | `callable` | no | (value, xLabel, yLabel) => void, fired when a cell is activated |
| `ariaLabel` | `string` | no | Accessible name for the graphic, overriding the generated one — the way to name a chart that has no visible `title` |
| `decorative` | `boolean` | no | Take the graphic out of the accessibility tree. Only for a chart that repeats information already present in the surrounding text |

### Gauge

```
Gauge(value, min?, max?, label?, tone?, size?, caption?, unit?, format?, thresholds?, showRange?, ariaLabel?, decorative?)
```

Half-doughnut gauge indicator for a single value between `min` and `max`. The inner value renders as a plain number — pass `unit` ("%", "ms", "pts") or `format: "percent"` for a suffix, or `label` to override the text entirely. Pass `thresholds: [{value, tone}]` to band the track and recolour the arc as the value crosses each band. Use for KPI thresholds (uptime %, score, capacity, NPS, page-speed).

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `value` | `number` | **yes** |  |
| `min` | `number` | no | Lower bound (default 0) |
| `max` | `number` | no | Upper bound (default 100) |
| `label` | `string` | no | Inner label override (defaults to the formatted value) |
| `tone` | `"primary"` \| `"success"` \| `"warning"` \| `"danger"` \| `"info"` | no |  |
| `size` | `"sm"` \| `"md"` \| `"lg"` | no |  |
| `caption` | `string` | no | Small caption below the gauge |
| `unit` | `string` | no | Suffix appended to the value ("%", "ms", "pts") |
| `format` | `"number"` \| `"percent"` \| `"compact"` | no | Number formatting for the inner label (default "number") |
| `thresholds` | `{value: number, tone: string}[]` | no | Bands drawn on the track; the highest one the value passes colours the arc |
| `showRange` | `boolean` | no | Print the min / max end labels so the scale is readable |
| `ariaLabel` | `string` | no | Accessible name for the graphic, overriding the generated one — the way to name a chart that has no visible `title` |
| `decorative` | `boolean` | no | Take the graphic out of the accessibility tree. Only for a chart that repeats information already present in the surrounding text |

### Series

```
Series(name, values?, color?, points?)
```

Named data series for charts. Used inside BarChart, LineChart, PieChart, RadarChart and ScatterChart — the chart reads the series definition itself, so a Series is never placed on its own. Pass `values` for numeric charts, `points` for ScatterChart, and `color` to override this series' palette slot.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `name` | `string` | **yes** |  |
| `values` | `number[]` | no | One value per x-axis label (ScatterChart also accepts its {x, y} points here) |
| `color` | `string` | no | Override the palette colour for this series (any CSS colour or var()) |
| `points` | `{x: number, y: number, label?: string}[]` | no | XY points for ScatterChart — the explicit alternative to passing them as `values` |

