<!--
  GENERATED by scripts/emit-skill.mjs — do not edit by hand.
  Source of truth: `getComponentCatalog()` + `defaultLibrary.componentGroups`
  Regenerate with `npm run build:skill` (or `npm run build`).
-->

# App shell

5 components. [← back to the catalogue index](./index.md)

## When to use what

These are the library's own authoring notes — they encode the intended choice
between similar components, which the prop tables below cannot express.

- App shell components produce a **full SaaS-style layout in a single statement**. Use them whenever the response represents a complete product surface (dashboards with nav, settings sections, admin consoles, inboxes).
- `AppShell(sidebar, content, topbar?, collapsible?, sidebarOpen?)` — fixed-left navigation + scrollable main area. Pass `collapsible=true` to enable a hamburger that turns the sidebar into a slide-over drawer on mobile; bind `sidebarOpen` to a `$variable` if you want to drive that drawer programmatically.
- `Sidebar(items, brand?, tagline?, footer?, collapsed?)` + `SidebarItem(label, icon?, active?, badge?, action?)` + `SidebarSection(label, items)` — group nav links into sections, mark the current page with `active=true`, attach badges for counts. Pass `collapsed=true` (or bind to a `$variable`) to collapse it to an icon rail.
- `SplitView(primary, detail, primaryWidth?)` — master/detail layout (inboxes, file browsers, contact lists). Both panes are scrollable.

## Reference

### AppShell

```
AppShell(sidebar, content, topbar?, collapsible?, sidebarOpen?, onSidebarOpenChange?)
```

Canonical SaaS application shell: optional top bar, fixed left Sidebar, and scrollable main content. Reach for this whenever a response represents a full product surface (dashboard with nav, settings + sections, admin panels). Pass `collapsible=true` to render a hamburger that turns the sidebar into a slide-over drawer on narrow viewports; bind `sidebarOpen` to a `$variable` (or handle `onSidebarOpenChange`) to drive/observe that drawer — e.g. to close it after a nav click. Do NOT declare a Modal/Sheet inside `sidebar`: the drawer is a transformed box, so an overlay inside it is positioned against the drawer instead of the viewport.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `sidebar` | `Sidebar` | **yes** | Pass a Sidebar(...) node |
| `content` | `Node[]` | **yes** | Main content (typically starts with a PageHeader) |
| `topbar` | `Node[]` | no | Optional thin top bar above the content |
| `collapsible` | `boolean` | no | Show a hamburger that toggles the sidebar drawer on mobile (default true) |
| `sidebarOpen` | `boolean` | no | $variable controlling whether the mobile drawer is open (two-way: the hamburger, scrim and Escape write back to it) |
| `onSidebarOpenChange` | `callable` | no | Called with the new open state whenever the drawer is toggled |

### Sidebar

```
Sidebar(items, brand?, tagline?, footer?, collapsed?, fullHeight?, label?)
```

Vertical app navigation panel. Supports a brand header, navigation items (`SidebarItem` or `SidebarSection`), an optional footer, and a `collapsed` mode that hides labels to leave just an icon rail. Use inside `AppShell` for SaaS-style left navigation.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `(SidebarItem | SidebarSection)[]` | **yes** |  |
| `brand` | `string` | no | Product name / workspace label at the top |
| `tagline` | `string` | no |  |
| `footer` | `Node[]` | no | Footer block (Avatar + name, upgrade CTA, …) |
| `collapsed` | `boolean` | no | Render as an icon-only rail (hides labels/badges; brand shrinks to its initial) |
| `fullHeight` | `boolean` | no | Pin the rail to the full viewport height so the main content scrolls beneath it (default true). Set false for an auto-height sidebar that scrolls with the page. |
| `label` | `string` | no | Accessible name for the nav landmark (defaults to `brand`, then "Main navigation") |

### SidebarSection

```
SidebarSection(label, items)
```

Grouping inside a Sidebar — small uppercase label followed by SidebarItem entries. Use this to chunk a long sidebar into sections.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `label` | `string` | **yes** |  |
| `items` | `SidebarItem[]` | **yes** |  |

### SidebarItem

```
SidebarItem(label, icon?, active?, badge?, to?, onClick?, href?, disabled?)
```

Single navigation item inside a Sidebar. Pass `active=true` to mark as the current page, a `to` path to navigate via the runtime router on click, an `onClick` callable for arbitrary click handling, or an optional `badge` (string/number) for a trailing chip. `to` and `onClick` can coexist — `onClick` is invoked AFTER the router navigates so authors can do extra work (analytics, side-effects). Items with `to`/`href` render as real links, so Cmd/middle-click opens a new tab; `disabled: true` greys a gated item out.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `label` | `string` | **yes** |  |
| `icon` | `string` | no | Font Awesome icon name rendered before the label |
| `active` | `boolean` | no | Mark this item as the current page. When `to` is provided and `active` is omitted, the item auto-detects the active state from the current router path. |
| `badge` | `string` | no | Trailing chip (count or status) |
| `to` | `string` | no | Router path to navigate to on click (e.g. "/", "/orders"). Uses the runtime router — no full page reload. |
| `onClick` | `callable` | no | Callable invoked on click. Runs in addition to `to`-based navigation. |
| `href` | `string` | no | External/absolute URL — rendered as a plain link (no router) |
| `disabled` | `boolean` | no | Render a non-interactive, greyed item (gated features) |

### SplitView

```
SplitView(primary, detail, primaryWidth?, showDetail?)
```

Two-pane master/detail layout — a narrow primary pane on the left, wider detail pane on the right. Collapses to a single column on narrow viewports. Use for inboxes, file browsers, contact lists. On a phone pass `showDetail` (typically a `$variable`) so only the list or only the selected item is shown.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `primary` | `Node[]` | **yes** | Master pane content (list, filters) |
| `detail` | `Node[]` | **yes** | Detail pane content (selected item, empty state) |
| `primaryWidth` | `string` | no | CSS width for the primary pane (default 320px) |
| `showDetail` | `boolean` | no | Which pane wins on narrow viewports: true = detail, false = list. Omit to keep both stacked. |

