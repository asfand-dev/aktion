<!--
  GENERATED by scripts/emit-skill.mjs — do not edit by hand.
  Source of truth: `getComponentCatalog()` + `defaultLibrary.componentGroups`
  Regenerate with `npm run build:skill` (or `npm run build`).
-->

# Content

27 components. [← back to the catalogue index](./index.md)

## When to use what

These are the library's own authoring notes — they encode the intended choice
between similar components, which the prop tables below cannot express.

- Prefer `Markdown(...)` for rich paragraph text with inline formatting — the parser supports headings, blockquotes, fenced code, numbered/bullet lists, links, images, and bare-URL auto-linking.
- Use `Callout(variant, title, description, icon?, compact?)` for highlighted notices; pass `compact: true` for a one-line inline note.
- Use `Quote(text, cite?)` for inline pull-quotes inside articles and marketing sections (use `Testimonial` when you also have author/role/rating).
- Use `CodeBlock(language, codeString, showLineNumbers?, highlightLines?)` for read-only code snippets. The header always renders a copy-to-clipboard button.
- Use `Badge(label, variant?, icon?, size?)` for a single pill and `BadgeList(["a","b","c"], variant?, size?)` to render an array of strings as Badge pills.
- `Badge` vs `Pill`: `Badge` is a SOLID high-attention chip ("Recommended", "Save 50 %"); `Pill(label, tone?, icon?)` is the softer tinted STATE label for the current status of a thing ("SSL active", "pending", "broken"). Pill tones: neutral, activating, success, warning, critical, promoting, corporate.
- Use `Skeleton(variant?, lines?, height?, shape?, width?)` for loading placeholders; `variant` accepts `paragraph` (default), `card`, `table-row`, `avatar`, `image`.
- Use `Spinner(size?, label?, tone?)` for tiny inline loading indicators inside buttons, toolbars, or table cells; `LoadingDots(label?, size?, tone?)` is the quieter three-dot pulse alternative.
- Use `Image(src, alt?, caption?, ratio?, fit?, fallback?)` — `ratio` (e.g. `"16:9"`) makes the image self-constrain so you do not need an outer `AspectRatio`.
- Use `Kbd(["Cmd", "K"])` when referring to keyboard shortcuts.
- Use `Icon(name, variant?, size?)` to render a standalone Font Awesome icon (`name` is the FA name without the `fa-` prefix, e.g. `"house"`, `"chart-line"`, `"regular:star"`, `"brands:github"`).
- For page-level titles reach for `PageHeader(...)` (top of dashboards/detail pages) or `SectionHeader(...)` (inside a Card). For tiny inline titles use `Text(value, variant="large-heavy")`.

## Reference

### Text

```
Text(value, variant?, tone?, align?, style?, as?, truncate?, lines?)
```

Renders plain text with a typographic variant. `align` (left|center|right) sets the horizontal alignment (the text renders as its own block). `as` picks the element — pass `h1`..`h6` for real heading semantics (or use `Heading`); the `heading`/`title` variants otherwise get `role="heading"` so they still appear in the document outline. `truncate` clips to one line with an ellipsis and `lines: N` clamps to N lines. Optional `style` prop accepts a CSS declaration string (e.g. "font-size: 16px; color: #000;") applied directly to the rendered element.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `value` | `string` | **yes** |  |
| `variant` | `"small"` \| `"small-heavy"` \| `"body"` \| `"body-heavy"` \| `"large"` \| `"large-heavy"` \| `"heading"` \| `"title"` | no |  |
| `tone` | `"default"` \| `"muted"` \| `"primary"` \| `"success"` \| `"warning"` \| `"danger"` | no | Visual accent |
| `align` | `"left"` \| `"center"` \| `"right"` | no | Horizontal text alignment — the text becomes its own block-level line |
| `style` | `string` | no | Inline CSS declarations applied to the rendered element (e.g. "font-size: 16px; font-weight: bold; color: #000;"). |
| `as` | `"span"` \| `"p"` \| `"div"` \| `"h1"` \| `"h2"` \| `"h3"` \| `"h4"` \| `"h5"` \| `"h6"` | no | Element to render (default `span`). Use `h1`..`h6` so a heading-styled variant is a real heading. |
| `truncate` | `boolean` | no | Clip to a single line with a trailing ellipsis |
| `lines` | `number` | no | Clamp to N lines with a trailing ellipsis (implies `truncate`) |

### Image

```
Image(src, alt?, caption?, ratio?, fit?, fallback?, placeholder?, loading?, sizes?, srcset?, onClick?)
```

Inline image. `ratio` constrains the box to a fixed aspect ratio (e.g. `16:9`, `1:1`) so callers do not need an outer `AspectRatio` (and it reserves space to avoid layout shift). `fit` controls how the image fills that box. `placeholder: "blur"` fades the image in once loaded; `sizes`/`srcset` enable responsive loading; `loading: "eager"` opts out of lazy-loading. When `src` is missing/unsafe or fails to load it shows the `fallback` text/icon.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `src` | `string` | **yes** |  |
| `alt` | `string` | no |  |
| `caption` | `string` | no |  |
| `ratio` | `string` | no | Aspect ratio shorthand (e.g. `16:9`, `1:1`, `4:3`) |
| `fit` | `"cover"` \| `"contain"` \| `"fill"` \| `"none"` \| `"scale-down"` | no | object-fit value (default `cover`) |
| `fallback` | `string` | no | Text label or Font Awesome icon shown when src is missing/unsafe/errored |
| `placeholder` | `"blur"` \| `"none"` | no | `blur` fades the image in on load |
| `loading` | `"lazy"` \| `"eager"` | no | Native loading strategy (default lazy) |
| `sizes` | `string` | no | Responsive `sizes` attribute |
| `srcset` | `string` | no | Responsive `srcset` candidates |
| `onClick` | `callable` | no | Makes the image activatable (gallery thumbnail, clickable avatar) — adds button semantics and keyboard activation |

### Badge

```
Badge(label, tone?, icon?, size?)
```

Small pill-style tag for status, counts, categories. Accepts an optional leading `icon` and a `size`.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `label` | `string` | **yes** |  |
| `tone` | `"neutral"` \| `"primary"` \| `"success"` \| `"warning"` \| `"danger"` \| `"info"` | no | Visual tone |
| `icon` | `string` | no | Optional Font Awesome icon name (e.g. "star") |
| `size` | `"xs"` \| `"sm"` \| `"md"` \| `"lg"` \| `"xl"` | no |  |

### BadgeList

```
BadgeList(labels, tone?, size?, tones?, icons?, max?)
```

Cluster of Badge pills rendered from an array of strings. `tone` sets the whole cluster; `tones` / `icons` are index-aligned arrays that override it per item (so one item can be `success` and the next `danger`). `max` caps how many pills render and appends a `+N` overflow pill for the rest.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `labels` | `string[]` | **yes** | Array of badge labels |
| `tone` | `"neutral"` \| `"primary"` \| `"success"` \| `"warning"` \| `"danger"` \| `"info"` | no |  |
| `size` | `"xs"` \| `"sm"` \| `"md"` \| `"lg"` \| `"xl"` | no |  |
| `tones` | `string[]` | no | Per-item tones, index-aligned with `labels` (falls back to `tone`) |
| `icons` | `string[]` | no | Per-item leading Font Awesome icon names, index-aligned with `labels` |
| `max` | `number` | no | Render at most N pills, then a `+N` overflow pill |

### Pill

```
Pill(label, tone?, icon?)
```

Soft, tinted **state** label — pale semantic background with dark semantic text, regular weight, fully rounded. Use for the current state of a thing ("SSL active", "pending", "broken", "open ticket"). For a solid, high-attention marketing/status chip use `Badge` instead. Tones: neutral, activating, success, warning, critical, promoting, corporate (danger/error/info/primary are accepted as synonyms).

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `label` | `string` | **yes** |  |
| `tone` | `"neutral"` \| `"activating"` \| `"success"` \| `"warning"` \| `"critical"` \| `"promoting"` \| `"corporate"` \| `"danger"` \| `"error"` \| `"info"` \| `"primary"` | no | Semantic state tone |
| `icon` | `string` | no | Optional leading Font Awesome icon name |

### Callout

```
Callout(tone?, title, description?, icon?, compact?, actions?, hideIcon?, live?, dismissible?, onDismiss?)
```

Highlighted callout banner with variant, title, description, and leading icon. This is the library's alert primitive: it is announced to assistive tech by default (`role="alert"` for danger/error/warning, `role="status"` otherwise) — pass `live: false` for a static, decorative note that should stay quiet. Pass `compact: true` for a one-line inline-note rendering, `hideIcon: true` to drop the icon medallion, and `dismissible: true` (with an optional `onDismiss`) for a closable banner.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `tone` | `"neutral"` \| `"info"` \| `"success"` \| `"warning"` \| `"danger"` \| `"error"` | no |  |
| `title` | `string` | **yes** |  |
| `description` | `string` | no | Body text |
| `icon` | `string` | no | Optional Font Awesome icon name |
| `compact` | `boolean` | no | Render with the dense, one-line note shape. |
| `actions` | `Node[]` | no | Optional action row (buttons/links) rendered under the body |
| `hideIcon` | `boolean` | no | Render without the leading icon medallion |
| `live` | `boolean` | no | Announce the banner to assistive tech (default true; `false` for a static note) |
| `dismissible` | `boolean` | no | Render a × close button that removes the banner |
| `onDismiss` | `callable` | no | Called when the banner is dismissed (implies `dismissible`) |

### Quote

```
Quote(text, cite?, tone?)
```

Inline pull-quote with optional citation. Lighter than `Testimonial` — use inside articles, blog posts, marketing sections, or anywhere you need to highlight a sentence without the full quote/author/role + rating shape.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `text` | `string` | **yes** |  |
| `cite` | `string` | no | Attribution text shown below the quote |
| `tone` | `"default"` \| `"primary"` \| `"success"` \| `"warning"` \| `"danger"` \| `"info"` | no |  |

### CodeBlock

```
CodeBlock(language?, codeString, showLineNumbers?, highlightLines?, highlight?, copy?, header?, width?, height?, filename?, wrap?)
```

Read-only code block with a language label and a copy-to-clipboard button. Pass `showLineNumbers=true` to render a gutter; `highlightLines` accepts a string like `"3-5,8"` to emphasise specific lines. `header=false` renders a chromeless variant — no language label or copy button, no border or rounding — that fills its container (100%×100%), for embedding in your own frame (CodeWindow uses it). `filename` shows a file path in the header instead of the language token. `wrap=true` soft-wraps long lines instead of scrolling horizontally. `width`/`height` set an explicit size; overflowing code scrolls either way.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `language` | `string` | no | Display label + syntax highlighting (e.g. aktion, ts, bash) |
| `codeString` | `string` | **yes** | Raw source text |
| `showLineNumbers` | `boolean` | no | Render a left-side line-number gutter |
| `highlightLines` | `string` | no | Highlight ranges, e.g. "3-5,8" |
| `highlight` | `boolean` | no | Syntax-highlight tokens when a language is set (default true) |
| `copy` | `boolean` | no | Show the copy-to-clipboard button (default true) |
| `header` | `boolean` | no | Show the header bar (default true); false = chromeless, fills its container |
| `width` | `string` | no | Explicit width (CSS length) |
| `height` | `string` | no | Explicit height (CSS length); overflowing code scrolls |
| `filename` | `string` | no | File path shown in the header in place of the language token (e.g. `src/index.ts`) |
| `wrap` | `boolean` | no | Soft-wrap long lines instead of scrolling horizontally |

### Skeleton

```
Skeleton(variant?, lines?, height?, shape?, width?, label?, live?)
```

Loading placeholder. Pass a `variant` for common shapes — `paragraph` (default), `card`, `table-row`, `avatar`, `image` — or use `shape` / `width` / `height` to build a custom one. All variants use a shimmer animation that respects `prefers-reduced-motion`. The placeholder is a polite live region announcing `label` (default "Loading"), so a screen reader hears the wait instead of silence — when you stack several Skeletons into one loading view, pass `live: false` on all but the first so the announcement happens once rather than once per placeholder.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `variant` | `"paragraph"` \| `"card"` \| `"table-row"` \| `"avatar"` \| `"image"` | no |  |
| `lines` | `number` | no | Lines for the `paragraph` variant (default 3) / cells for `table-row` (default 4) |
| `height` | `number | string` | no | Line height in px (paragraph) or CSS height for custom shape; a bare number is px |
| `shape` | `"rect"` \| `"circle"` | no | Force a primitive shape (rect/circle) |
| `width` | `string` | no | CSS width for shape-only skeletons |
| `label` | `string` | no | What is loading, announced to assistive tech (default "Loading") |
| `live` | `boolean` | no | Announce the wait (default true); `false` keeps this placeholder silent when a sibling already announces |

### Spinner

```
Spinner(size?, label?, tone?)
```

Indeterminate inline loader (a rotating ring). Use for tiny loading states inside buttons, toolbars, table cells, or chat bubbles where `Skeleton` and `Progress(indeterminate=true)` are too heavy. For a quieter three-dot pulse instead of a ring use `LoadingDots`. Pass `label` to render an inline caption beside the spinner (also announced via `aria-label`).

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `size` | `"xs"` \| `"sm"` \| `"md"` \| `"lg"` \| `"xl"` | no | Default `md` |
| `label` | `string` | no | Caption rendered beside the spinner (also announced) |
| `tone` | `"default"` \| `"neutral"` \| `"primary"` \| `"success"` \| `"warning"` \| `"danger"` \| `"info"` | no | Visual accent (default `primary`) |

### LoadingDots

```
LoadingDots(label?, size?, tone?)
```

Three dots that pulse in sequence — an inline indeterminate loader. Lighter and quieter than `Spinner`'s rotating ring; use inside buttons, beside labels, or in table cells. Pass `label` for an announced caption.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `label` | `string` | no | Caption rendered beside the dots (also announced) |
| `size` | `"xs"` \| `"sm"` \| `"md"` \| `"lg"` \| `"xl"` | no | Default `md` |
| `tone` | `"default"` \| `"neutral"` \| `"primary"` \| `"success"` \| `"warning"` \| `"danger"` \| `"info"` | no | Visual accent (default `primary`) |

### Markdown

```
Markdown(content, linkTarget?)
```

Render markdown-flavoured text. Supports **bold**, *italic*, `code`, headings (`#` through `######`), blockquotes (`>`), bullet (`-`/`*`) and numbered (`1.`) lists, thematic breaks (`---`), fenced code blocks (```), images (`![alt](src)`), inline links, and auto-linked bare URLs. Multi-line paragraphs collapse into `<p>` blocks. Links to a fragment or a root-relative path stay in the tab; absolute URLs open in a new one — pass `linkTarget` to force one or the other.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `content` | `string` | **yes** |  |
| `linkTarget` | `"_self"` \| `"_blank"` | no | Force every link's target (default: `_self` for in-app links, `_blank` for absolute URLs) |

### Kbd

```
Kbd(keys, size?)
```

Renders a keyboard shortcut chip (e.g. `Cmd+K`). Pass a single label, or multiple labels as an array to render a `key + key + …` combo.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `keys` | `string | string[]` | **yes** |  |
| `size` | `"sm"` \| `"md"` | no |  |

### Icon

```
Icon(name, variant?, size?, color?, label?, title?)
```

Single Font Awesome icon. `name` is the FA name without the `fa-` prefix (e.g. `"house"`, `"chart-line"`). Use `variant` for non-solid styles (`regular`/`brands`) or prefix the name (`"regular:star"`). `color` accepts any CSS colour (`"#00ff00"`, `"tomato"`, `"var(--rui-color-primary)"`). Icons are decorative (hidden from screen readers) by default — pass `label` when the glyph carries the only meaning in its slot (a tick meaning "verified", a padlock next to a plan name) and it becomes an announced `role="img"`. `title` adds a native hover tooltip.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `name` | `string` | **yes** | FA name without the fa- prefix |
| `variant` | `"solid"` \| `"regular"` \| `"brands"` | no |  |
| `size` | `"xs"` \| `"sm"` \| `"md"` \| `"lg"` \| `"xl"` | no |  |
| `color` | `string` | no | CSS colour applied to the glyph (hex, named, rgb()/hsl(), or var(--token)) |
| `label` | `string` | no | Accessible name — set when the icon is not decorative; announced as role="img" |
| `title` | `string` | no | Native hover tooltip (also used as the accessible name when `label` is omitted) |

### TextContent

```
TextContent(value, variant?, tone?, align?, style?, as?, truncate?, lines?)
```

Deprecated alias for `Text`. Prefer `Text(...)` — both render identically.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `value` | `string` | **yes** |  |
| `variant` | `"small"` \| `"small-heavy"` \| `"body"` \| `"body-heavy"` \| `"large"` \| `"large-heavy"` \| `"heading"` \| `"title"` | no |  |
| `tone` | `"default"` \| `"muted"` \| `"primary"` \| `"success"` \| `"warning"` \| `"danger"` | no | Visual accent |
| `align` | `"left"` \| `"center"` \| `"right"` | no | Horizontal text alignment — the text becomes its own block-level line |
| `style` | `string` | no | Inline CSS declarations applied to the rendered element (e.g. "font-size: 16px; font-weight: bold; color: #000;"). |
| `as` | `"span"` \| `"p"` \| `"div"` \| `"h1"` \| `"h2"` \| `"h3"` \| `"h4"` \| `"h5"` \| `"h6"` | no | Element to render (default `span`). Use `h1`..`h6` so a heading-styled variant is a real heading. |
| `truncate` | `boolean` | no | Clip to a single line with a trailing ellipsis |
| `lines` | `number` | no | Clamp to N lines with a trailing ellipsis (implies `truncate`) |

### GradientText

```
GradientText(text, gradient?)
```

Inline text painted with a brand gradient. Use inside a Display/Heading title to emphasise a phrase: `Display(["Build in ", GradientText("record time")])`. `gradient` selects a named theme gradient (brand|accent|warm|cool|success|danger).

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `text` | `string` | **yes** |  |
| `gradient` | `"brand"` \| `"accent"` \| `"warm"` \| `"cool"` \| `"success"` \| `"danger"` | no |  |

### Display

```
Display(content, size?, align?, balance?, weight?)
```

Oversized display headline with built-in responsive `clamp()` sizing. The positional argument may be a plain string OR an array mixing strings and `GradientText(...)` runs. `size`: hero|xl|lg. `balance` wraps for even line lengths.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `content` | `string | Node[]` | **yes** |  |
| `size` | `"hero"` \| `"xl"` \| `"lg"` | no |  |
| `align` | `"left"` \| `"center"` \| `"right"` | no |  |
| `balance` | `boolean` | no |  |
| `weight` | `number` | no |  |

### Heading

```
Heading(content, level?, size?, align?)
```

Section/subsection heading with token-driven sizing. `level` sets the semantic tag (1–6); `size`: section|lg|md|sm. Accepts string or mixed string/GradientText array like Display.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `content` | `string | Node[]` | **yes** |  |
| `level` | `number` | no | Heading level 1–6 (default 2) |
| `size` | `"section"` \| `"lg"` \| `"md"` \| `"sm"` | no |  |
| `align` | `"left"` \| `"center"` \| `"right"` | no |  |

### Eyebrow

```
Eyebrow(text)
```

Small uppercase, letter-spaced, primary-toned label above a heading.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `text` | `string` | **yes** |  |

### Prose

```
Prose(children, size?)
```

A styled long-form reading container — applies typographic defaults to headings, lists, blockquotes, code, links inside. Wrap Markdown(...) or raw nodes for blog posts, docs bodies, product descriptions.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `children` | `Node[]` | **yes** |  |
| `size` | `"sm"` \| `"md"` \| `"lg"` | no |  |

### RelativeTime

```
RelativeTime(value)
```

Renders a human relative time ('3m ago', 'in 2 days') from an ISO date/timestamp, localized via Intl. Emits a `<time datetime=…>` carrying the absolute instant and refreshes itself on a coarse schedule, so a list left open does not keep claiming 'just now'.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `value` | `string | number` | **yes** |  |

### Svg

```
Svg(content, viewBox?, width?, height?, fill?, stroke?, strokeWidth?, preserveAspectRatio?, label?)
```

Render inline SVG markup safely (paths, shapes, gradients) — for brand illustrations, custom icons, and data-viz overlays without the HTMLTag escape hatch. Pass the inner markup (everything inside <svg>) plus a `viewBox`, or paste a whole `<svg …>` element and its viewBox/fill/stroke are picked up. `label` names the graphic for assistive tech (unlabelled graphics are hidden as decorative). Script/event-handler payloads are stripped.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `content` | `string` | **yes** |  |
| `viewBox` | `string` | no | e.g. "0 0 24 24" (default) |
| `width` | `string` | no |  |
| `height` | `string` | no |  |
| `fill` | `string` | no | currentColor (default), none, or a token color |
| `stroke` | `string` | no | Stroke colour — for outline icon sets (Lucide, Feather) |
| `strokeWidth` | `string | number` | no |  |
| `preserveAspectRatio` | `string` | no | e.g. "none" to stretch to the box |
| `label` | `string` | no | Accessible name; omit for a decorative graphic |

### VisuallyHidden

```
VisuallyHidden(children)
```

Renders content that is invisible on screen but available to screen readers (the `sr-only` pattern). Use for icon-button labels, form hints, and context that sighted users get from layout but assistive tech needs.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `children` | `Node[]` | **yes** | Content exposed only to assistive tech |

### KbdShortcut

```
KbdShortcut(keys, size?, separator?)
```

Render a keyboard shortcut as styled key caps — the multi-key form of `Kbd`, sharing its markup and size scale. Pass `keys` as an array (e.g. ["Cmd", "K"]) or a '+'-joined string; `separator` changes the joiner ("" for the compact ⌘K style, " then " for sequential chords).

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `keys` | `string | string[]` | **yes** |  |
| `size` | `"sm"` \| `"md"` | no |  |
| `separator` | `string` | no | Joiner between caps (default "+") |

### CountUp

```
CountUp(value, suffix?, prefix?, duration?, decimals?)
```

A number that animates from 0 to `value` when scrolled into view. `prefix`/`suffix` wrap it (e.g. `$`, `+`, `%`). `decimals` keeps fraction digits (default: as many as `value` has, so 99.9 stays 99.9). Respects reduced motion.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `value` | `number` | **yes** |  |
| `suffix` | `string` | no |  |
| `prefix` | `string` | no |  |
| `duration` | `number` | no | Animation ms (default 1000) |
| `decimals` | `number` | no | Fraction digits to display (default: inferred from `value`) |

### CountdownTimer

```
CountdownTimer(to, endLabel?, onEnd?, units?, showDays?)
```

Live countdown to a target date/time (ISO string or timestamp). Ticks every second, then shows `endLabel` and fires `onEnd` (enable checkout, reveal a link, refresh a price). `units` trims the boxes — a 10-minute flash sale should not render two zeroed day/hour cells.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `to` | `string | number` | **yes** |  |
| `endLabel` | `string` | no | Shown when the countdown finishes (default 'Done') |
| `onEnd` | `callable` | no | Fired once when the countdown reaches zero |
| `units` | `"days"` \| `"hours"` \| `"minutes"` \| `"seconds"` | no | Which units to show (default all four) |
| `showDays` | `boolean` | no | Set false to drop the days cell |

### TableOfContents

```
TableOfContents(items, title?, activeHref?, onSelect?)
```

A navigable table of contents. `items` is an array of {label, href, level?}. A `#fragment` href scrolls to the matching `id` INSIDE the app (the browser cannot resolve a fragment into the shadow tree, and touching `location.hash` would drive the router), marks that entry active, and fires `onSelect(href)`. `activeHref` sets the current entry from state.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `object[]` | **yes** |  |
| `title` | `string` | no |  |
| `activeHref` | `string` | no | href of the entry to mark current |
| `onSelect` | `callable` | no | Fired with the clicked href |

