<!--
  GENERATED by scripts/emit-skill.mjs — do not edit by hand.
  Source of truth: `getComponentCatalog()` + `defaultLibrary.componentGroups`
  Regenerate with `npm run build:skill` (or `npm run build`).
-->

# Feedback & Media

22 components. [← back to the catalogue index](./index.md)

## When to use what

These are the library's own authoring notes — they encode the intended choice
between similar components, which the prop tables below cannot express.

- `Avatar(name, src?, size?, status?)` falls back to initials when the image is missing.
- Use `AvatarGroup` to render contributor strips with a `+N` overflow chip.
- `PersonChip(name, role?, avatarSrc?, size?, status?, action?)` is the inline avatar + name + role pill — use everywhere a person is referenced (table cells, list rows, sidebar footers, kanban cards) instead of a raw `Avatar` next to `Text`.
- Wrap any node in `Tooltip(label, trigger)` for inline hints.
- Use `HoverCard(trigger, content)` when the popover needs rich content (profile preview, link target) and the trigger should open on hover.
- `Popover(trigger, content, title?, side?, align?, width?)` is the click-triggered counterpart of `HoverCard` — use for filter panels, color pickers, share menus, and small settings flyouts. Always renders an × close button in the header; clicking the trigger again, clicking outside, or pressing Escape also closes it.
- `Rating(value, max?, label?, count?, size?, interactive?, halfStep?, icon?)` renders stars for product reviews, testimonials, and ranked lists. Pass a `$variable` as `value` with `interactive=true` to let users rate; add `halfStep=true` so clicking the left half of a star sets a half-value. Set `icon="heart"|"thumb"|"fire"|"bolt"` (or any FA name) to swap glyphs.
- `Toast(title, message?, tone?, icon?, duration?, action?, onClose?, position?)` pins a transient notice; pass `duration` (ms) for auto-dismiss. Use `Banner` for top-of-page announcements and `Notification` for permanent inbox entries.
- `Toasts(children, position?)` stacks toasts in a viewport corner. Usually unnecessary — the `$toast` namespace (`$toast.success(...)` etc.) auto-renders its own stack. Use `Toasts` only to place the stack yourself: `Toasts($toast.items.map(t => Toast({ title: t.message, tone: t.tone, onClose: () => $toast.dismiss(t.id) })))` (rendering `$toast.items` opts out of the auto-layer).
- `NotificationBell(count?, items?, onOpen?)` — compact inbox trigger; `CommandPalette` for Cmd-K action search.

## Reference

### Avatar

```
Avatar(name, src?, size?, status?, fallback?)
```

User avatar. Shows the image at `src`. When `src` is missing, falls back to a deterministic DiceBear illustration seeded by `name` (pass `fallback="initials"` to render two-letter initials instead, or `"gradient"` for an offline seeded swatch). If the image errors at runtime the avatar gracefully degrades to initials. `status` is announced as part of the accessible name ("Ada Lovelace, busy").

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `name` | `string` | **yes** | Accessible name + initials fallback |
| `src` | `string` | no | Image URL |
| `size` | `"xs"` \| `"sm"` \| `"md"` \| `"lg"` \| `"xl"` | no |  |
| `status` | `"online"` \| `"offline"` \| `"busy"` \| `"away"` | no |  |
| `fallback` | `"initials"` \| `"dicebear"` \| `"gradient"` | no | How to render when `src` is missing (default: dicebear illustration; pass `initials` for the two-letter pill) |

### AvatarGroup

```
AvatarGroup(items, max?, size?, total?, fallback?)
```

Stack of overlapping avatars with a `+N` chip when the list overflows. Pass either Avatar(...) nodes or plain {name, src, status?, fallback?} objects. Set `total` when the list is only a page of a larger set (5 avatars out of 200 members renders `+195`), and `fallback: "initials"` to keep the whole pile offline (the default DiceBear illustration is a network request per member).

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `Avatar[]` | **yes** | Avatar(...) nodes or {name, src, status?, fallback?} objects |
| `max` | `number` | no | Maximum avatars to show (default 4) |
| `size` | `"xs"` \| `"sm"` \| `"md"` \| `"lg"` \| `"xl"` | no |  |
| `total` | `number` | no | Total member count when `items` is only a page of it — drives the `+N` chip |
| `fallback` | `"initials"` \| `"dicebear"` \| `"gradient"` | no | Default fallback for `{name}` items that have no `src` (per-item `fallback` wins) |

### PersonChip

```
PersonChip(name, role?, avatarSrc?, size?, status?, onClick?)
```

Inline avatar + name + optional role/meta pill. Use anywhere a person needs to be referenced compactly: table cells, list rows, comments, kanban cards, sidebar footers. Pair multiple chips with `Stack(direction="row", wrap=true)` for assignee lists.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `name` | `string` | **yes** |  |
| `role` | `string` | no | Sub-line below the name (role, email, handle, …) |
| `avatarSrc` | `string` | no |  |
| `size` | `"sm"` \| `"md"` \| `"lg"` | no |  |
| `status` | `"online"` \| `"offline"` \| `"busy"` \| `"away"` | no |  |
| `onClick` | `callable` | no |  |

### Tooltip

```
Tooltip(label, trigger, side?, delay?, open?, align?, onOpenChange?)
```

Wraps a trigger node and shows `label` when the user hovers or focuses it — text, or a node when the hint needs a `Kbd` chip. The tooltip hides on click/touch (so it does not stay stuck on touch devices) and on Escape, and `side` + `align` place it on any of the 12 edge positions — it is never clipped by a scrolling or `overflow: hidden` container. Pass `delay` (ms) so sweeping across a toolbar does not pop every hint, or `open: true` to force one open for a product tour. `onOpenChange(open)` fires on every transition, so a tour step can advance when the hint is dismissed. Use for short hints (≤6 words); reach for HoverCard when you need rich content.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `label` | `string | Node` | **yes** | Hint text, or a node when the hint needs a Kbd chip or emphasis |
| `trigger` | `Node` | **yes** |  |
| `side` | `"top"` \| `"bottom"` \| `"left"` \| `"right"` | no |  |
| `delay` | `number` | no | Milliseconds to wait before showing on hover/focus (default 0) |
| `open` | `boolean` | no | Force the hint open (coach-marks, screenshots) |
| `align` | `"start"` \| `"center"` \| `"end"` | no | Alignment along the trigger edge (default "center") — `end` keeps a hint on a right-edge button inside the viewport |
| `onOpenChange` | `callable` | no | Called with the new boolean whenever the hint is revealed or hidden (hover, focus, Escape, tap) |

### HoverCard

```
HoverCard(trigger, content, side?, open?, align?, width?, openDelay?, closeDelay?, label?, onOpenChange?)
```

Wraps a trigger node and reveals a card with rich content on hover/focus (tap toggles it on touch devices, Escape closes it). Use for previewing a referenced item (profile, link target, definition). `openDelay` / `closeDelay` keep a paragraph full of mentions from flashing cards and give the user time to move onto the card; `width` and `align` size and place it. `onOpenChange(open)` fires on every transition, so the preview can be fetched on open and cancelled on close. Give it a `label` when the card is interactive so the popup has an accessible name.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `trigger` | `Node` | **yes** |  |
| `content` | `Node[]` | **yes** |  |
| `side` | `"top"` \| `"bottom"` \| `"left"` \| `"right"` | no |  |
| `open` | `boolean` | no | Force the card open (otherwise reveal on hover/focus) |
| `align` | `"start"` \| `"center"` \| `"end"` | no | Alignment along the trigger edge (default "start") |
| `width` | `string` | no | CSS width for the card (default 240–320px) |
| `openDelay` | `number` | no | Milliseconds to wait before revealing (default 0; 700 is a good hover-card value) |
| `closeDelay` | `number` | no | Milliseconds to keep the card after the pointer leaves, so the user can move onto it |
| `label` | `string` | no | Accessible name for the card |
| `onOpenChange` | `callable` | no | Called with the new boolean whenever the card is revealed or hidden (hover, focus, tap, Escape) — use it to lazy-load the preview |

### Popover

```
Popover(trigger, content, title?, side?, align?, width?, open?, onOpenChange?, showClose?, disabled?)
```

Click-triggered popup with arbitrary rich content. Use when HoverCard's hover trigger is too eager and Modal/Sheet is too heavy — perfect for filter panels, color pickers, share menus, and small settings flyouts. The trigger stays visible while the popover is open — clicking it again, clicking outside, pressing Escape, or clicking the built-in × button all close it (pass `showClose: false` to drop the × and its header row). Focus moves into the panel while it is open and returns to the trigger on close. `onOpenChange(open)` fires on every transition, so a filter flyout can apply its draft on close.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `trigger` | `Node` | **yes** | Clickable trigger element (Button, Avatar, IconButton, …). The trigger remains visible while the popover is open. |
| `content` | `Node[]` | **yes** | Body rendered inside the popover |
| `title` | `string` | no | Optional bold heading rendered above the content (also names the dialog) |
| `side` | `"bottom"` \| `"top"` \| `"left"` \| `"right"` | no | Where the popover opens relative to the trigger (default "bottom") |
| `align` | `"start"` \| `"center"` \| `"end"` | no | Alignment along the trigger edge (default "start") |
| `width` | `string` | no | CSS width for the popover panel (default "280px") |
| `open` | `boolean` | no | Initial open state — use to demo or pre-open the popover |
| `onOpenChange` | `callable` | no | Called with the new boolean whenever the popover opens or closes |
| `showClose` | `boolean` | no | Render the × button and its header row (default true) |
| `disabled` | `boolean` | no | Make the trigger inert (e.g. while a request is in flight) |

### Rating

```
Rating(value, max?, label?, count?, size?, interactive?, readonly?, halfStep?, icon?, onChange?, allowClear?, tone?)
```

Compact 0–5 star rating with optional numeric badge and review count. Use in product cards, testimonials, reviews, and KPI rows. Pass `interactive: true` (or an `onChange` handler) to let users pick a rating; bind `value: $rating` for two-way binding, or read the new value from `onChange: (v) => …`. With `halfStep: true` clicking the left half of a star sets a fractional value; keyboard users get whole steps with Enter/Space and half steps with the arrow keys. `allowClear: true` lets clicking the current rating remove it. `icon` swaps the glyph family — `star` (default), `heart`, `thumb`, `fire`, `bolt`, or any custom Font Awesome name — and `tone` recolours it (amber by default).

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `value` | `number` | **yes** | 0–max; can be a $variable when interactive |
| `max` | `number` | no | Maximum number of stars (default 5) |
| `label` | `string` | no | Inline text shown after the stars (e.g. "4.2 of 5"); also names the group when interactive |
| `count` | `number` | no | Review/voter count rendered in parentheses (0 renders "(0)") |
| `size` | `"sm"` \| `"md"` \| `"lg"` | no |  |
| `interactive` | `boolean` | no | Allow clicking a star to set the value |
| `readonly` | `boolean` | no | Force read-only (overrides `interactive`) |
| `halfStep` | `boolean` | no | Allow half-star resolution when interactive |
| `icon` | `string` | no | Icon family — `star` (default), `heart`, `thumb`, `fire`, `bolt`, or any FA name |
| `onChange` | `callable` | no | Called with the new rating when the user picks a star (interactive mode) |
| `allowClear` | `boolean` | no | Clicking the current rating again clears it to 0 ("remove my rating") |
| `tone` | `"primary"` \| `"success"` \| `"warning"` \| `"danger"` \| `"info"` | no | Colour of the filled icons (default warning/amber) |

### Toast

```
Toast(title, message?, tone?, icon?, duration?, action?, onClose?, position?, pauseOnHover?)
```

Single transient notification card. Always shows a close (×) button that removes the toast from the DOM (and fires `onClose` if set). Pass `duration` (ms) to auto-dismiss — with `pauseOnHover: true` the countdown stops while the pointer is over the card — or `position` for a standalone one-off toast (the renderer will pin it to the viewport corner so you do not have to wrap a single notification in `Stack(...)`). Use `Toasts` for grouped stacks; prefer `Banner` for top-of-page announcements and `Notification` for permanent inbox entries.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `title` | `string` | **yes** |  |
| `message` | `string` | no |  |
| `tone` | `"default"` \| `"primary"` \| `"success"` \| `"warning"` \| `"danger"` \| `"info"` | no | Visual accent (default "default") |
| `icon` | `string` | no | Font Awesome icon name (default picked from tone) |
| `duration` | `number` | no | Auto-dismiss after N milliseconds (e.g. 4000). Omit to keep the toast until the user closes it. |
| `action` | `Button` | no | Optional inline `Button` action shown above the message |
| `onClose` | `callable` | no | Callable invoked when the toast is dismissed (× button, auto-dismiss, or programmatic) |
| `position` | `"top-right"` \| `"top-left"` \| `"top-center"` \| `"bottom-right"` \| `"bottom-left"` \| `"bottom-center"` | no | Pin a standalone Toast to a viewport corner without wrapping it in `Stack(...)` |
| `pauseOnHover` | `boolean` | no | Pause the auto-dismiss countdown while the pointer is over the toast |

### Toasts

```
Toasts(children, position?, max?)
```

Stacked container for transient `Toast` notifications, pinned to a viewport corner. Usually unnecessary: `$toast.success(...)` (and `.show/.error/.info/.warning`) auto-render their own stack. Reach for `Toasts` only for custom placement — render the reactive `$toast.items` list into it and the auto-layer steps aside: `Toasts($toast.items.map(t => Toast({ title: t.message, tone: t.tone, onClose: () => $toast.dismiss(t.id) })))` — add `key: t.id` there when the list can reorder. `max` caps how many are shown at once (the rest are summarised as `+N more`). Use a standalone `Toast` with `position` for a single one-off notice.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `children` | `Node[]` | **yes** | Toast components to stack |
| `position` | `"top-right"` \| `"top-left"` \| `"top-center"` \| `"bottom-right"` \| `"bottom-left"` \| `"bottom-center"` | no | Viewport corner the stack pins to (default "top-right") |
| `max` | `number` | no | Maximum toasts shown at once; the overflow is summarised as `+N more` |

### VideoPlayer

```
VideoPlayer(src?, sources?, poster?, caption?, controls?, autoplay?, loop?, muted?, ratio?, tracks?, onEnded?, fallback?, onError?)
```

Themed native `<video>` wrapper. Pass a `src` URL (or `sources` array for multi-codec fallback) and optional `poster`. Standard controls are visible by default; `controls=false` swaps them for a single play/pause button (click the video too). `autoplay` only works alongside `muted` — browsers block unmuted autoplay. Add `tracks` for captions/subtitles (required for prerecorded video, WCAG 1.2.2). `onEnded` fires on completion; a missing, unsafe, or failing source shows `fallback` and calls `onError`. Use for product demos, tutorials, and any inline video.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `src` | `string` | no | Video URL (mp4 / webm / etc.) |
| `sources` | `object[]` | no | Array of {src, type} entries for multi-codec fallback |
| `poster` | `string` | no | Thumbnail image URL shown before playback |
| `caption` | `string` | no |  |
| `controls` | `boolean` | no | Show native controls (default true); false renders a play/pause button instead |
| `autoplay` | `boolean` | no | Requires `muted: true` — browsers block unmuted autoplay |
| `loop` | `boolean` | no |  |
| `muted` | `boolean` | no |  |
| `ratio` | `string` | no | Aspect ratio (default 16:9) |
| `tracks` | `object[]` | no | Caption/subtitle tracks: {src, label?, srclang?, kind?, default?} (kind: subtitles\|captions\|descriptions\|chapters\|metadata) |
| `onEnded` | `callable` | no | Callable invoked when playback reaches the end |
| `fallback` | `string` | no | Message shown when the source is missing/unsafe or fails to load |
| `onError` | `callable` | no | Callable invoked when the video fails to load |

### AudioPlayer

```
AudioPlayer(src?, sources?, title?, artist?, controls?, autoplay?, loop?, icon?, muted?, onEnded?)
```

Themed native `<audio>` wrapper with a title, optional artist, and standard transport controls. Pass `src` (or `sources`) plus a `title` — the title names the player for assistive tech. `controls=false` replaces the native bar with a single play/pause button so the row still works. `autoplay` needs `muted: true` to be allowed by the browser. `onEnded` fires on completion (advance a playlist, mark an episode listened). Use for podcasts, voice notes, and demo audio.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `src` | `string` | no | Audio URL (mp3 / ogg / wav / etc.) |
| `sources` | `object[]` | no | Array of {src, type} entries for multi-codec fallback |
| `title` | `string` | no |  |
| `artist` | `string` | no |  |
| `controls` | `boolean` | no | Show native controls (default true); false renders a play/pause button instead |
| `autoplay` | `boolean` | no | Requires `muted: true` — browsers block unmuted autoplay |
| `loop` | `boolean` | no |  |
| `icon` | `string` | no | Leading icon (default `music`) |
| `muted` | `boolean` | no |  |
| `onEnded` | `callable` | no | Callable invoked when playback reaches the end |

### Carousel

```
Carousel(items, activeIndex?, ratio?, showDots?, showArrows?, onChange?, autoplay?, interval?, label?, empty?, emptyText?)
```

Horizontal slider with prev/next buttons, dot navigation, arrow-key and swipe support. Each child slide takes full width. Slides may be Component nodes (Image, Card, MediaCard, …), URL strings, or plain `{src, alt, caption?}` image objects — bare image objects are auto-wrapped into a captioned figure. Bind a `$variable` to `activeIndex` to drive or observe the active slide, or use `onChange`. `autoplay` rotates every `interval` ms (paused while hovered or focused) for hero banners. Give it a `label` so screen readers can tell two carousels apart, and `empty`/`emptyText` for the no-slides case. The active slide is preserved across re-renders via instance state.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `Node[]` | **yes** | Slide nodes, image URLs, or {src, alt, caption?} objects |
| `activeIndex` | `number` | no | 0-indexed active slide (default 0); bind a $variable to control it |
| `ratio` | `string` | no | Aspect ratio of the frame (default `16:9`) |
| `showDots` | `boolean` | no | Show indicator dots (default true) |
| `showArrows` | `boolean` | no | Show prev/next arrows (default true) |
| `onChange` | `callable` | no | Callable invoked with the new slide index whenever it changes |
| `autoplay` | `boolean` | no | Rotate slides automatically (pauses on hover / focus) |
| `interval` | `number` | no | Autoplay delay in ms (default 5000, minimum 1500) |
| `label` | `string` | no | Accessible name for the carousel region |
| `empty` | `Node` | no | Node rendered when `items` is empty |
| `emptyText` | `string` | no | Message shown when `items` is empty (default `No slides to show.`) |

### Gallery

```
Gallery(items, columns?, ratio?, onSelect?, fit?, label?, empty?, emptyText?)
```

Responsive image grid. Pass items as plain URL strings, `{src, alt, caption?}` objects, or `Image(...)` nodes (rendered in full, so their own `fit`/`fallback`/`srcset` apply). `fit: "contain"` stops logos and screenshots being centre-cropped. When `onSelect` is provided each tile becomes a button; bind it through an Action that opens a `Lightbox`. `empty`/`emptyText` cover the no-results case.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `any[]` | **yes** |  |
| `columns` | `number` | no | Preferred column count 1–6 (default auto) |
| `ratio` | `string` | no | Per-tile aspect ratio (default `1:1`) |
| `onSelect` | `callable` | no | Callable fired when a tile is clicked |
| `fit` | `"cover"` \| `"contain"` | no | How each image fills its tile (default `cover`) |
| `label` | `string` | no | Accessible name for the grid |
| `empty` | `Node` | no | Node rendered when `items` is empty |
| `emptyText` | `string` | no | Message shown when `items` is empty (default `No images to show.`) |

### Lightbox

```
Lightbox(items, open?, index?, onClose?, showThumbnail?)
```

Image overlay. Pass `items` (string URLs or `{src, alt, caption?}` objects). `open` and `index` work as literals or as `$variable` bindings; without a bound `open` the component manages itself and renders a clickable thumbnail of the current image (suppress it with `showThumbnail: false` when something else does the opening). Clicking the backdrop or ×, or pressing Escape, closes and fires `onClose`; the arrows and ArrowLeft/ArrowRight step through the array. Focus moves into the viewer on open, is trapped while it is open, and is restored on close.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `any[]` | **yes** |  |
| `open` | `boolean` | no | Open/closed; bind a $variable to control externally |
| `index` | `number` | no | 0-indexed current image; typically a $variable |
| `onClose` | `callable` | no | Callable invoked whenever the viewer closes |
| `showThumbnail` | `boolean` | no | Render the clickable thumbnail (default: only when `open` is not bound) |

### Map

```
Map(lat, lng, zoom?, markers?, height?, caption?, title?)
```

Static map view centered on a lat/lng coordinate. Renders an OpenStreetMap embed inside a sandboxed `<iframe>` (no external JS, no API key). Pass `lat` (-90…90) and `lng` (-180…180) as bare numbers; `zoom` controls the level (1–18, default 13). Optional `markers` are projected onto the map as pins and listed as labelled chips beneath it. Use for store locators, address cards, itinerary previews.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `lat` | `number` | **yes** | Latitude of the map center (-90…90) |
| `lng` | `number` | **yes** | Longitude of the map center (-180…180) |
| `zoom` | `number` | no | 1–18 (default 13) |
| `markers` | `object[]` | no | Array of {lat, lng, label?} — drawn as pins over the map and listed beneath it |
| `height` | `string` | no | CSS height (default 320px) |
| `caption` | `string` | no |  |
| `title` | `string` | no | Accessible name for the map frame (defaults to the caption) |

### TypingIndicator

```
TypingIndicator(name?)
```

Three animated bouncing dots — a chat 'is typing…' affordance. Optional `name` prefix.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `name` | `string` | no |  |

### Confetti

```
Confetti(fire?, count?, colors?, duration?, onDone?)
```

A one-shot confetti burst — celebration affordance after a success (checkout, completion). Renders a short CSS particle animation each time `fire` becomes true, once per transition. `colors`/`duration` tune the look; `onDone` fires when the burst finishes. Honours prefers-reduced-motion.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `fire` | `boolean` | no | Trigger the burst (default true) |
| `count` | `number` | no | Particle count (default 40, max 120) |
| `colors` | `string[]` | no | Piece colours (default: theme tokens) |
| `duration` | `number` | no | Fall time in seconds (default 1.5, max 10) |
| `onDone` | `callable` | no | Fires once the burst has finished |

### Lottie

```
Lottie(src?, data?, loop?, autoplay?, speed?, width?, height?, poster?, fallback?, label?, playing?, onComplete?, onError?)
```

Plays a Lottie/Bodymovin vector animation from `src` (a .json URL) or inline `data`. Uses the `lottie-web` library when it is present on the page (`window.lottie`), waiting briefly for a deferred script; otherwise shows the `fallback` (or a poster image) so the layout never breaks. `loop`/`autoplay` default true; `playing` pauses/resumes, `speed` scales playback, `onComplete`/`onError` report the outcome. No dependency is bundled.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `src` | `string` | no | URL to a Lottie JSON file |
| `data` | `object` | no | Inline Lottie animation data |
| `loop` | `boolean` | no |  |
| `autoplay` | `boolean` | no |  |
| `speed` | `number` | no | Playback rate, clamped to 0.1–10 |
| `width` | `string` | no |  |
| `height` | `string` | no |  |
| `poster` | `string` | no | Image shown when lottie-web is unavailable |
| `fallback` | `Node` | no | Node shown when lottie-web is unavailable and no poster |
| `label` | `string` | no | Accessible name; omit for a decorative animation |
| `playing` | `boolean` | no | Pause (false) / resume (true) an already-loaded animation |
| `onComplete` | `callable` | no | Fires when a non-looping animation ends |
| `onError` | `callable` | no | Fires with "missing-library" or "load-failed" |

### QRCode

```
QRCode(data, size?, ecc?, color?, background?, margin?, label?)
```

Renders an offline QR code as crisp SVG from `data` (a URL or text). `size` is the pixel side length and acts as a maximum — the code scales down to fit a narrower container. `ecc` is the error-correction level (L|M|Q|H, default M), `color`/`background` override the modules, `label` sets the accessible name. No network — encodes fully in the browser.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `data` | `string` | **yes** | URL or text to encode |
| `size` | `number` | no | Side length in px (default 160); scales down on narrower viewports |
| `ecc` | `"L"` \| `"M"` \| `"Q"` \| `"H"` | no |  |
| `color` | `string` | no | Module (dark) color |
| `background` | `string` | no | Background color — default opaque white, which reliable scanning needs; pass "transparent" to drop the backing rect |
| `margin` | `number` | no | Quiet-zone modules (default 2) |
| `label` | `string` | no | Accessible name (default "QR code for <data>") — use it so screen readers do not read out a whole signed URL |

### PresenceAvatars

```
PresenceAvatars(people, max?, size?, onClick?)
```

An overlapping stack of avatars with online status dots — 'who's here' for collab/social. Pass `people` as {name, src?, online?}. `onClick` makes each avatar activatable (receives the person object).

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `people` | `object[]` | **yes** |  |
| `max` | `number` | no | Max avatars before a +N chip (default 5) |
| `size` | `"sm"` \| `"md"` \| `"lg"` | no |  |
| `onClick` | `callable` | no | Fires with the clicked person object |

### LiveCursor

```
LiveCursor(x, y, label?, color?, space?, smooth?, typing?)
```

A floating collaborator cursor positioned at `x`/`y` with a name label — drive it from presence/`$socket` state for multiplayer UIs. `space` declares what x/y are measured against: "parent" (default) is the nearest positioned ancestor, so give the shared surface `position: relative`; "viewport" treats them as viewport coordinates and escapes `overflow: hidden` clipping. `smooth` glides between presence ticks, `typing` shows a typing indicator. Pointer-events are disabled so it never blocks interaction.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `x` | `number` | **yes** | X position in px, relative to `space` |
| `y` | `number` | **yes** | Y position in px, relative to `space` |
| `label` | `string` | no | Collaborator name |
| `color` | `string` | no |  |
| `space` | `"parent"` \| `"viewport"` | no | Coordinate frame for x/y (default `parent`) |
| `smooth` | `boolean` | no | Interpolate movement between presence updates (default true; ignored under prefers-reduced-motion) |
| `typing` | `boolean` | no | Show a typing indicator on the cursor |

### Backdrop

```
Backdrop(grid?, blobs?, particles?, type?, particleColors?, speed?, linkDistance?, particleSize?, fixed?)
```

Decorative background layer for hero/marketing sections: a masked grid, floating blurred color 'blobs', and an animated canvas particle field. `type` picks the particle behaviour — `network` (drifting dots joined by constellation lines, the default), `drift`, `snow`, `stars`, or `bubbles` — tuned via `particleColors`, `speed`, `linkDistance`, and `particleSize`. All theme-aware and motion-safe. Place as the first child of a Section (which is `position: relative`).

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `grid` | `boolean` | no | Show the masked line grid |
| `blobs` | `string[]` | no | Colors for floating blur orbs (CSS colors) |
| `particles` | `number` | no | Particle count (0 = none, max 120) |
| `type` | `"network"` \| `"drift"` \| `"snow"` \| `"stars"` \| `"bubbles"` | no | Particle behaviour (default network) |
| `particleColors` | `string[]` | no | Particle palette (CSS colors; defaults to the brand palette) |
| `speed` | `number` | no | Animation speed multiplier 0.1–4 (default 1) |
| `linkDistance` | `number` | no | Max px between linked network particles (default 130) |
| `particleSize` | `number` | no | Max particle radius in px (default 2.2) |
| `fixed` | `boolean` | no | Pin to the viewport instead of the section |

