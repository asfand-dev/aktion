<!--
  GENERATED by scripts/emit-skill.mjs — do not edit by hand.
  Source of truth: `getComponentCatalog()` + `defaultLibrary.componentGroups`
  Regenerate with `npm run build:skill` (or `npm run build`).
-->

# Data

24 components. [← back to the catalogue index](./index.md)

## When to use what

These are the library's own authoring notes — they encode the intended choice
between similar components, which the prop tables below cannot express.

- Build columns using array pluck: `Col("Title", data.rows.title, format?, align?)`.
- For per-row controls inside a Col, use `for (let row of data.rows) { ... }` and reference `row.field` inline.
- `Table(cols, caption?, density?, striped?, sticky?, emptyLabel?)` — pass `density="compact"` for dense data, `sticky=true` to pin the header in a scrolling parent, and `emptyLabel` for the zero-state cell.
- `DataGrid(cols, rowIds?, caption?, sort, selectedIds, selectable?, page, perPage?, …)` is the advanced Table — adds sortable headers (`sortable=true` on Col), per-column filter chips (`filterable=true`), checkbox row selection bound to `$selectedIds`, sticky header / first column, pagination, and an optional bulk-action toolbar. Reach for this whenever a user needs to sort, filter, or page through a list.
- `CalendarView(value?, month?, events?, view?, firstDay?, onSelect?)` renders a full-month (or week) calendar grid for scheduling apps — distinct from the `DatePicker` input. Bind `value` to a `$variable` for the selected ISO date.
- `ComparisonTable(columns, rows, highlightColumn?)` is the generic counterpart of `PricingTable` — pass rows of `{label, values, hint?, group?}`. Use for feature comparisons, spec sheets, plan grids.
- `InfiniteList(items, onLoadMore?, loading?, hasMore?)` is a scroll-to-load list; the action fires when the sentinel scrolls into view.
- Use `Progress(value, max?, label?, tone?, indeterminate?, segments?, buffered?)` for linear bars — `segments` renders an N-step strip (onboarding flows), `buffered` adds a secondary buffer indicator.
- `ProgressRing(value, max?, label?, tone?, size?)` is the circular variant for quotas/completion.
- `Stats([{label, value, hint?, tone?, spark?}, …], layout?)` — `layout="strip"` (default) for inline KPIs; `layout="grid"` for a responsive StatCard grid. Pass `spark` for an inline trend line.
- `StatCard(label, value, trend?, delta?, icon?, spark?, tone?)` gains an optional inline `Sparkline` via the `spark` prop. Use `Sparkline(values, tone?)` standalone for tiny trend chips in table cells.
- `Tile(label, icon?, value?, description?, tone?, action?)` is the dense icon tile for quick-action menus and category grids; pair with `Grid` for uniform rows.
- `Tree([TreeNode(label, children?, icon?, expanded?, active?, badge?, action?)])` renders a hierarchical tree (file browsers, nested navigation, category pickers); use `expanded=true` to open a branch by default.
- `Pagination(page, totalPages, siblings?, total?, perPage?, perPageOptions?, compact?)` — bind `page` (and optionally `perPage`) to a `$variable`; pass `total` to render the "Showing N–M of T" summary. Reuse the same variable when slicing data with `arr.filter(...)` / `for`.

## Reference

### Table

```
Table(columns, caption?, density?, striped?, sticky?, emptyLabel?, loading?, loadingRows?, maxHeight?, onRowClick?, allowOverflow?, ariaLabel?, locale?)
```

Tabular data view. Children must be Col components. `density="compact"` tightens row padding for dense data, `striped=true` zebra-stripes the rows, `sticky=true` pins the header row when the table scrolls, and `maxHeight` sizes that scroll box. Set `loading=true` while a query is in flight so the table shows skeleton rows instead of its empty state, `onRowClick` for row-level navigation, and `allowOverflow=true` when a cell renders an overlay that must escape the scroll box. The empty-state row uses `emptyLabel` when set.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `columns` | `Col[]` | **yes** |  |
| `caption` | `string` | no |  |
| `density` | `"comfortable"` \| `"compact"` | no | Row padding (default `comfortable`) |
| `striped` | `boolean` | no | Zebra-stripe alternating rows |
| `sticky` | `boolean` | no | Pin the header row when the table scrolls |
| `emptyLabel` | `string` | no | Text shown when the table has no rows (default `No data`) |
| `loading` | `boolean` | no | Show shimmering skeleton rows while data loads, instead of the empty state |
| `loadingRows` | `number` | no | Skeleton rows drawn while `loading` (default 3) |
| `maxHeight` | `string` | no | CSS max-height for the scroll box (`320px`, `50vh`) — overrides the default cap `sticky` applies |
| `onRowClick` | `callable` | no | `(rowIndex, row) => void` — fired when a row is clicked or activated via keyboard. `row` is header-keyed. |
| `allowOverflow` | `boolean` | no | Drop the wrapper's scroll clipping so an overlay rendered inside a cell is not cut off (the table then widens the page instead of scrolling internally). |
| `ariaLabel` | `string` | no | Accessible name for the table when there is no `caption` |
| `locale` | `string` | no | BCP-47 tag (`de-DE`, `en-GB`) applied to every `number`/`currency`/`date` column that does not set its own `locale` — an invoice table formats in the customer's locale, not the browser's. |

### Col

```
Col(header, values, format?, align?, sortable?, filterable?, render?, onClick?, currency?, width?, wrap?, headerTooltip?, locale?)
```

Single column inside a Table or DataGrid. Use `align` for per-column text alignment, `format` for cell rendering (`text|number|currency|date`), `currency` for the money code used by `format: "currency"`, `locale` for the BCP-47 tag those formats are rendered in, and `width`/`wrap` to stop one long column from forcing the whole table into horizontal scroll. `values` may be plain values OR an array of component nodes — e.g. `Col("Status", rows.map(r => Badge(r.status)))` or `Col("Actions", rows.map(r => Button("Edit")))` — each component renders directly in its cell. Pass `render: (value, index, row) => …` for the same effect when you prefer to keep `values` as the raw row data (return a component, string, or array). `row` is the whole row (header-keyed) and stays correct even when DataGrid sorts — prefer `row.otherColumn` over indexing a sibling array. Pass `onClick: (value, index, row) => …` to make the whole cell clickable (pointer + keyboard). `sortable` and `filterable` only take effect inside `DataGrid` (Table ignores them).

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `header` | `string` | **yes** |  |
| `values` | `any[]` | **yes** | Column values. Plain values are formatted as text; component nodes (e.g. `rows.map(r => Badge(r.status))`) render directly. You can also pass the full row array and map each cell with `render`. |
| `format` | `"text"` \| `"number"` \| `"currency"` \| `"date"` | no |  |
| `align` | `"left"` \| `"center"` \| `"right"` | no | Per-column horizontal alignment |
| `sortable` | `boolean` | no | DataGrid: enable click-to-sort on this column |
| `filterable` | `boolean` | no | DataGrid: enable a per-column filter chip |
| `render` | `callable` | no | `(value, index, row) => Component \| string \| array` — map each cell to arbitrary content (buttons, badges, links). `row` is the whole row (header-keyed), so it stays correct even when DataGrid sorts internally — prefer `row.otherColumn` over an `index` lookup into a sibling array. |
| `onClick` | `callable` | no | `(value, index, row) => void` — fired when a cell in this column is clicked or activated via keyboard. |
| `currency` | `string` | no | ISO 4217 code used by `format: "currency"` — `EUR`, `GBP`, `CHF`, … (default `USD`). |
| `width` | `string` | no | CSS width for the column (`240px`, `30%`). Caps the column instead of letting one long value stretch the table. |
| `wrap` | `boolean` | no | Let long cell text wrap onto several lines instead of pushing the table into horizontal scroll. |
| `headerTooltip` | `string` | no | Explanation shown on hover/focus of the header cell — e.g. `MRR = monthly recurring revenue`. |
| `locale` | `string` | no | BCP-47 tag (`de-DE`, `en-GB`, `fr-CH`) used by `format: "number"\|"currency"\|"date"` for separators, digit grouping and date order. Defaults to the Table's `locale`, then the viewer's browser. |

### DataGrid

```
DataGrid(columns, rowIds?, caption?, sort?, selectedIds?, selectable?, page?, perPage?, emptyLabel?, onRowClick?, toolbar?, density?, striped?, stickyHeader?, stickyFirstColumn?, exportable?, exportFilename?, loading?, error?, loadingLabel?, maxHeight?, allowOverflow?, onSort?, onSelectionChange?, perPageOptions?, onPerPageChange?)
```

Advanced data table with sortable headers, per-column filter chips, row selection (checkboxes), sticky header / first column, optional pagination, an optional bulk-action toolbar slot, and click-to-act rows. Columns are Col(header, values, format?, align?, sortable?, filterable?) entries. Sorting, selection and pagination work on their own; bind `$sort` (`{key, direction}` object), `$selectedIds` (string[]) and `$page` (number) when the host needs to read or drive them, or use `onSort` / `onSelectionChange` for server-side work. Use `loading` / `error` for query states. Use INSTEAD of `Table` when you need any of those interactions.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `columns` | `Col[]` | **yes** | Columns; pass sortable=true / filterable=true on each Col. |
| `rowIds` | `any[]` | no | Stable id per row (used by `selectedIds` and as the row's morph key); defaults to row index. |
| `caption` | `string` | no |  |
| `sort` | `object` | no | `{key, direction}` — pass a $variable for two-way binding |
| `selectedIds` | `any[]` | no | Array of selected row ids — bind a $variable |
| `selectable` | `boolean` | no | Render leading selection checkboxes |
| `page` | `number` | no | 1-indexed current page — bind a $variable |
| `perPage` | `number` | no | Page size (default 20) |
| `emptyLabel` | `string` | no | Text shown when no rows match (default `No results`) |
| `onRowClick` | `callable` | no | Callable fired when a row is clicked. Receives (rowIndex, row, rowId) where `row` is header-keyed. |
| `toolbar` | `Node[]` | no | Bulk-action toolbar shown above the table when any rows are selected |
| `density` | `"comfortable"` \| `"compact"` | no |  |
| `striped` | `boolean` | no |  |
| `stickyHeader` | `boolean` | no | Pin the header row (default true). Constrains the grid's height to `maxHeight` so there is something to scroll. |
| `stickyFirstColumn` | `boolean` | no | Pin the first column horizontally |
| `exportable` | `boolean` | no | Show an Export CSV button that downloads the filtered rows |
| `exportFilename` | `string` | no | CSV download filename (default `data.csv`) |
| `loading` | `boolean` | no | Show a loading row instead of the empty message while rows are in flight |
| `error` | `string` | no | Failure message shown instead of the rows (takes precedence over `loading`) |
| `loadingLabel` | `string` | no | Label for the loading row (default `Loading…`) |
| `maxHeight` | `string` | no | Scroll-area height cap, e.g. `70vh` / `480px`. Applied when `stickyHeader` is on (default `70vh`) — without it the header has no scrollport to stick to. |
| `allowOverflow` | `boolean` | no | Let cell content (menus, popovers) escape the scroll box instead of clipping it. Disables the sticky header. |
| `onSort` | `callable` | no | Callable fired with (columnKey, direction) when a header is activated — use for server-side sorting |
| `onSelectionChange` | `callable` | no | Callable fired with the array of selected row ids |
| `perPageOptions` | `number[]` | no | Page sizes offered in a footer dropdown, e.g. [20, 50, 100] |
| `onPerPageChange` | `callable` | no | Callable fired with the newly chosen page size |

### List

```
List(items, ordered?, emptyLabel?, divided?, gap?)
```

Vertical list. `items` are normally `ListItem(...)` nodes, but plain strings and `{title, description, icon}` objects are accepted and coerced into rows. `divided=true` renders a flush list with hairline separators instead of a bordered card per row, `gap` spaces the rows, and `emptyLabel` is shown when `items` is empty.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `ListItem[]` | **yes** |  |
| `ordered` | `boolean` | no |  |
| `emptyLabel` | `string` | no | Text shown when the list has no items (default `No items`) |
| `divided` | `boolean` | no | Flush list separated by hairlines instead of one bordered card per row |
| `gap` | `"none"` \| `"3xs"` \| `"2xs"` \| `"xs"` \| `"sm"` \| `"md"` \| `"lg"` \| `"xl"` \| `"2xl"` \| `"3xl"` | no | Space between rows |

### ListItem

```
ListItem(title, description?, icon?, onClick?, href?, trailing?, active?, tone?)
```

Single list row: title, optional description, optional leading icon, and an optional `trailing` slot for a badge / switch / chevron. Pass `onClick` (or `href`) to make the row activatable, `active=true` to mark it as the current selection, and `tone` for per-row status emphasis.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `title` | `string` | **yes** |  |
| `description` | `string` | no |  |
| `icon` | `string` | no | Font Awesome icon name |
| `onClick` | `callable` | no | Callable fired when the row is activated (click or Enter/Space) |
| `href` | `string` | no | Render the row as a real link so middle-click / open-in-new-tab work |
| `trailing` | `Node[]` | no | Components pinned to the right of the row (Badge, Switch, icon button) |
| `active` | `boolean` | no | Marks the row as the current selection (list-as-navigation, list-as-picker) |
| `tone` | `"default"` \| `"primary"` \| `"success"` \| `"warning"` \| `"danger"` \| `"info"` | no | Status emphasis for the row (failed / queued / done lists) |

### StatCard

```
StatCard(label, value, trend?, delta?, icon?, spark?, tone?, hint?, onClick?)
```

Single KPI card with label, value, optional delta, optional icon, optional `hint` ("vs. last quarter"), and optional inline sparkline (`spark=[…numbers]`). Pass `onClick` to make the whole tile a drill-down target. Use inside `Stats` for a uniform KPI strip.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `label` | `string` | **yes** |  |
| `value` | `string` | **yes** |  |
| `trend` | `"up"` \| `"down"` \| `"flat"` | no |  |
| `delta` | `string` | no | Change vs previous period |
| `icon` | `string` | no | Font Awesome icon name shown in a chip beside the label. Omit it and one is guessed from the label; pass `"none"` for no icon at all. |
| `spark` | `number[]` | no | Optional inline sparkline values |
| `tone` | `"default"` \| `"primary"` \| `"success"` \| `"warning"` \| `"danger"` \| `"info"` | no |  |
| `hint` | `string` | no | Small qualifier under the value (`vs. last quarter`, `excl. tax`) |
| `onClick` | `callable` | no | Callable fired when the card is clicked — the standard KPI drill-down |

### Stats

```
Stats(items, layout?, columns?, align?)
```

KPI strip or grid. Pass `items` as `{label, value, hint?, tone?, spark?}` objects for strip layout, or as `StatCard(...)` nodes when `layout="grid"`.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `object[] | StatCard[]` | **yes** | Stat objects or StatCard nodes when layout=grid |
| `layout` | `"strip"` \| `"grid"` | no | strip = horizontal row; grid = responsive Grid |
| `columns` | `number` | no | Preferred column count for grid layout (1–6) |
| `align` | `"start"` \| `"center"` \| `"end"` | no | Strip alignment (layout=strip only) |

### Sparkline

```
Sparkline(values, tone?, width?, height?, min?, max?, label?)
```

Tiny inline trend chart for KPIs, table cells, and dashboards. Renders an SVG line with a soft fill — use anywhere you would otherwise reach for `LineChart` but a single value series should stay inline with surrounding text. `width`/`height` size it (default 80x24), `min`/`max` fix the scale so a strip of sparklines shares one baseline, and `label` gives screen readers the trend ("7-day trend, up 12%").

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `values` | `number[]` | **yes** |  |
| `tone` | `"primary"` \| `"success"` \| `"warning"` \| `"danger"` \| `"info"` | no |  |
| `width` | `number|string` | no | Chart width — a px number or any CSS length (`100%`, `12rem`). Default 80. |
| `height` | `number|string` | no | Chart height — a px number or any CSS length. Default 24. |
| `min` | `number` | no | Fixed scale minimum. Set `min`/`max` on every sparkline in a strip so their slopes are comparable. |
| `max` | `number` | no | Fixed scale maximum |
| `label` | `string` | no | Screen-reader description of the trend. Without it the chart stays decorative (`aria-hidden`). |

### Tile

```
Tile(label, icon?, value?, description?, tone?, onClick?, href?, selected?)
```

Compact icon + label + optional value tile. Smaller and denser than `StatCard`, ideal for menu grids, quick-action panels, category directories, and category filters. Pair with `Grid` for uniform rows. Pass `href` for a directory tile that links to a route, and `selected` for the on-state of a filter tile.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `label` | `string` | **yes** |  |
| `icon` | `string` | no | Font Awesome icon name shown in a colored disc |
| `value` | `string` | no | Secondary value rendered next to/under the label |
| `description` | `string` | no |  |
| `tone` | `"default"` \| `"primary"` \| `"success"` \| `"warning"` \| `"danger"` \| `"info"` | no |  |
| `onClick` | `callable` | no |  |
| `href` | `string` | no | Render the tile as a link to this URL |
| `selected` | `boolean` | no | Mark the tile as currently applied/current (filter grids, category directories) |

### Progress

```
Progress(value?, max?, label?, tone?, indeterminate?, showValue?, segments?, buffered?)
```

Linear progress bar. `value` is clamped between 0 and `max` (default 100). `indeterminate=true` renders a looping animation when the total is unknown. Provide `segments` to render a segmented progress strip (steps in an onboarding flow), or `buffered` for a secondary buffer indicator (downloads, video buffering) — a segmented bar shows the buffer as pre-lit segments. `label` also becomes the bar's accessible name.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `value` | `number` | no | Current progress; ignored when indeterminate |
| `max` | `number` | no | Upper bound (default 100) |
| `label` | `string` | no | Shown above the bar |
| `tone` | `"primary"` \| `"success"` \| `"warning"` \| `"danger"` \| `"info"` | no |  |
| `indeterminate` | `boolean` | no |  |
| `showValue` | `boolean` | no | Show the numeric value on the right |
| `segments` | `number` | no | Render N equal segments (filled by current step) |
| `buffered` | `number` | no | Secondary value (0..max) drawn behind the bar |

### ProgressRing

```
ProgressRing(value?, max?, label?, caption?, tone?, size?, indeterminate?, icon?)
```

Circular progress indicator. Use for KPIs, quotas, completion rings, and any metric better shown as a circle than a bar. Renders the value (or a custom `label`) inside the ring; pass `icon` for a glyph instead (completion checkmarks). `size` takes `sm|md|lg` or a plain number of px (`size: 40`), so the ring fits a table cell or fills a hero card.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `value` | `number` | no | Current value (ignored when indeterminate) |
| `max` | `number` | no | Upper bound (default 100) |
| `label` | `string` | no | Text shown inside the ring (default "{percent}%") |
| `caption` | `string` | no | Small caption rendered under the ring |
| `tone` | `"primary"` \| `"success"` \| `"warning"` \| `"danger"` \| `"info"` | no |  |
| `size` | `"sm"` \| `"md"` \| `"lg"` | no | `sm\|md\|lg`, or a diameter in px as a number (`size: 40`) |
| `indeterminate` | `boolean` | no |  |
| `icon` | `string` | no | Font Awesome icon rendered inside the ring instead of the label |

### Pagination

```
Pagination(page, totalPages, siblings?, total?, perPage?, perPageOptions?, compact?, onChange?, disabled?, onPerPageChange?)
```

Page navigator with Prev/Next, page numbers, and ellipses. Pass a `$variable` as `page` for two-way binding — clicking a page button sets that state to the new (1-indexed) value. Add `total` to render a "Showing N–M of T" summary, pass `$variable` as `perPage` to expose a per-page selector, or set `compact: true` to drop the page-number row for tight toolbars. For server-side paging pass `onChange` — it receives the new page number.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `page` | `number` | **yes** | Current page (1-indexed); typically a $variable |
| `totalPages` | `number` | **yes** |  |
| `siblings` | `number` | no | Number of page links shown around the current page (default 1, max 6) |
| `total` | `number` | no | Total record count — enables the "Showing N–M of T" summary |
| `perPage` | `number` | no | Bind a `$variable` to expose a per-page selector |
| `perPageOptions` | `number[]` | no | Override the per-page choices (default 10/20/50/100) |
| `compact` | `boolean` | no | Hide page numbers — keep Prev / Next only |
| `onChange` | `callable` | no | Called with the new (1-indexed) page — required when `page` is not a bare `$variable` |
| `disabled` | `boolean` | no | Disable the whole pager (e.g. while a page fetch is in flight) |
| `onPerPageChange` | `callable` | no | Called with the new page size when the per-page selector changes |

### Tree

```
Tree(items, selectedId?, onSelect?, expandedIds?, ariaLabel?, emptyLabel?, checkable?, checkedIds?, onCheck?)
```

Hierarchical tree view. Children must be TreeNode entries. Use for file browsers, nested navigation, category pickers, and any parent/child structure with arbitrary depth. Bind `selectedId` to a `$variable` (and/or pass `onSelect`) for container-level single selection instead of wiring `active` + `onClick` on every node; `expandedIds` opens the named branches (reveal a search hit, expand all). `checkable=true` puts a checkbox on every node and binds `checkedIds` — the standard shape for a folder-sync / permission picker; checking a branch checks its whole subtree and a partly-checked branch renders mixed. The tree implements the standard keyboard model: Up/Down move, Right/Left expand/collapse, Home/End jump, Enter activates, Space checks.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `TreeNode[]` | **yes** |  |
| `selectedId` | `string` | no | Id (or label) of the selected node — bind a `$variable` for two-way selection |
| `onSelect` | `callable` | no | `(nodeId) => void` — fired when a row is activated |
| `expandedIds` | `any[]` | no | Ids (or labels) of branches to open. Additive: it opens branches, it never forces one closed. |
| `ariaLabel` | `string` | no | Accessible name for the tree (`Files`, `Categories`) — needed as soon as a page shows more than one |
| `emptyLabel` | `string` | no | Text shown when there are no items (default `No items`) |
| `checkable` | `boolean` | no | Show a checkbox on every node for multi-selection ("pick the folders to sync", permission trees) |
| `checkedIds` | `any[]` | no | Ids (or labels) of the checked nodes — bind a `$variable` for two-way multi-selection. Checking a branch checks its subtree; a partly-checked branch is reported mixed and is NOT listed here. |
| `onCheck` | `callable` | no | `(checkedIds) => void` — fired with the whole checked-id array whenever a checkbox changes |

### TreeNode

```
TreeNode(label, children?, icon?, expanded?, active?, badge?, onClick?, href?, disabled?, onToggle?, nodeId?, hasChildren?)
```

Single node in a Tree view. When `children` is provided the node renders as an expandable branch with a chevron; otherwise it renders as a leaf. `onClick` fires on click, `href` makes the row a real link, `onToggle` fires with the new open state, and `disabled=true` makes the row inert. Set `hasChildren=true` (with no `children` yet) plus `onToggle` to lazy-load a subtree the first time the branch is opened. `expanded` is the INITIAL open state — a branch the user collapsed stays collapsed across re-renders. Use `active=true` to highlight the current selection, or drive selection from `Tree(selectedId:)` instead. In a checkable Tree it is the node's `nodeId` (or label) that appears in `Tree(checkedIds:)`.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `label` | `string` | **yes** |  |
| `children` | `TreeNode[]` | no |  |
| `icon` | `string` | no | Font Awesome icon shown before the label |
| `expanded` | `boolean` | no | Whether the branch starts open (initial value — the user's own collapse wins afterwards) |
| `active` | `boolean` | no | Highlights the row as the current selection |
| `badge` | `string` | no | Trailing chip (count or status) |
| `onClick` | `callable` | no | Callable fired when the row is clicked |
| `href` | `string` | no | Render the row as a real link (docs sidebars, file paths) so middle-click works |
| `disabled` | `boolean` | no | Renders the row inert — permission-gated or unavailable nodes |
| `onToggle` | `callable` | no | `(open) => void` — fired when the branch expands or collapses. Use it to lazy-load children. |
| `nodeId` | `string` | no | Stable id for `Tree(selectedId:)` / `Tree(expandedIds:)` (defaults to the label) |
| `hasChildren` | `boolean` | no | Render an expandable branch even though `children` is still empty — the lazy-loading half of `onToggle`: open the branch, fetch, re-render with real children. |

### CalendarView

```
CalendarView(value?, month?, events?, view?, firstDay?, onSelect?, onMonthChange?, onEventClick?, maxEventsPerDay?, min?, max?, disabledDates?, hideNav?)
```

Full-month or week calendar grid for scheduling apps — distinct from the form-input `DatePicker`. Pass events as an array of `{date: 'YYYY-MM-DD', title, tone?, time?, id?}` objects. Bind `value` to a `$variable` for the selected date (ISO string) and `month` for the visible month; the built-in ‹ / › / Today controls write both. Use `view="week"` for a single-week strip. `firstDay=1` (Monday) matches most business apps. `onEventClick` makes the event chips clickable, `min` / `max` / `disabledDates` block days.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `value` | `string` | no | Selected ISO date (YYYY-MM-DD); bind a $variable |
| `month` | `string` | no | Visible month — ISO date or YYYY-MM. Bind a $variable to follow the prev/next controls (defaults to `value`, else today) |
| `events` | `object[]` | no | Array of {date, title, tone?, time?, id?} objects |
| `view` | `"month"` \| `"week"` | no |  |
| `firstDay` | `number` | no | 0=Sunday, 1=Monday (default 1) |
| `onSelect` | `callable` | no | Callable fired when a day is clicked; receives the ISO date string |
| `onMonthChange` | `callable` | no | Callable fired when the prev/next/today controls move the grid; receives the new anchor ISO date |
| `onEventClick` | `callable` | no | Callable fired with (eventId, event) when an event chip is clicked; makes the chips real buttons |
| `maxEventsPerDay` | `number` | no | Event chips per day before a `+N more` toggle (default 3) |
| `min` | `string` | no | Earliest selectable ISO date |
| `max` | `string` | no | Latest selectable ISO date |
| `disabledDates` | `string[]` | no | ISO dates that cannot be selected (blackout days) |
| `hideNav` | `boolean` | no | Hide the built-in prev / next / today controls |

### ComparisonTable

```
ComparisonTable(columns, rows, highlightColumn?, featureLabel?, caption?, stickyFirstColumn?)
```

Feature/spec comparison table — generic counterpart of `PricingTable`. Pass `columns` (e.g. plan/product names) and `rows` of `{label, values}` where `values` aligns 1-to-1 with `columns`. Each value can be a boolean (✓/—), a string, or a node. Rows sharing a `group` are kept together under one group header.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `columns` | `string[]` | **yes** | Column headers |
| `rows` | `object[]` | **yes** | Array of {label, values, hint?, group?} entries |
| `highlightColumn` | `number` | no | 0-indexed column to visually emphasise |
| `featureLabel` | `string` | no | Header of the first (row-label) column — default `Feature` |
| `caption` | `string` | no | Table caption; also its accessible name |
| `stickyFirstColumn` | `boolean` | no | Keep the feature labels visible while the plan columns scroll horizontally |

### InfiniteList

```
InfiniteList(items, onLoadMore?, loading?, hasMore?, loaderLabel?, error?, onRetry?, emptyLabel?, rootMargin?, threshold?, retryLabel?)
```

Vertical list that fires `onLoadMore` when the user scrolls near the bottom. Pass already-rendered child nodes as `items`; wire `onLoadMore` to an `action` that awaits a `$mutation` or `$query` (e.g. `await loadMore.invoke()`) and appends to the bound state. Use `loading=true` to show the spinner row, `hasMore=false` to suppress further loads, and `error` + `onRetry` when a page fails.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `Node[]` | **yes** | Already-rendered child nodes |
| `onLoadMore` | `callable` | no | Callable fired when the sentinel scrolls into view |
| `loading` | `boolean` | no |  |
| `hasMore` | `boolean` | no | Default true — set false to hide the sentinel |
| `loaderLabel` | `string` | no | Label rendered while loading (default `Loading…`) |
| `error` | `string` | no | Failure message shown instead of the loader, with a Retry button |
| `onRetry` | `callable` | no | Callable fired by the Retry button (defaults to `onLoadMore`) |
| `emptyLabel` | `string` | no | Message shown when there are no items (default `No results`) |
| `rootMargin` | `string` | no | Prefetch distance for the scroll sentinel (default `200px`) |
| `threshold` | `number` | no | Fraction of the sentinel that must be visible, 0–1 (default 0) |
| `retryLabel` | `string` | no | Label for the retry button (default `Retry`) |

### VirtualGrid

```
VirtualGrid(items, columns?, itemHeight?, gap?, height?, minItemWidth?, onItemClick?, empty?, loading?)
```

Windowed 2-D grid for very large collections (galleries, tiles, thumbnails). Only the visible rows of cells are mounted. Pass pre-rendered nodes (or plain values) as `items`; set `columns`, `itemHeight`, and `gap`. Prefer `minItemWidth` over a fixed `columns` count so the grid reflows on narrow screens (XI.3).

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `any[]` | **yes** |  |
| `columns` | `number` | no | Cells per row (default 4) |
| `itemHeight` | `number` | no | Row height in px (default 120) |
| `gap` | `number` | no | Gap between cells in px (default 8) |
| `height` | `number` | no | Viewport height in px (default 480) |
| `minItemWidth` | `number` | no | Minimum cell width in px — the grid auto-fills and reflows instead of forcing `columns` |
| `onItemClick` | `callable` | no | Receives the clicked item and its index |
| `empty` | `any` | no | Node or text shown when `items` is empty |
| `loading` | `boolean` | no | Show a pending state instead of the empty state |

### Metric

```
Metric(value, label?, gradient?, countUp?, duration?, trend?)
```

A single big-number metric tile: the value (gradient-painted by default), an optional label and an optional `trend` delta. Use inside MetricStrip. Set `countUp` to animate the number in on scroll — currency prefixes, thousands separators and decimals survive the animation.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `value` | `string | number` | **yes** |  |
| `label` | `string` | no |  |
| `gradient` | `boolean` | no | Paint the number with the brand gradient |
| `countUp` | `boolean` | no | Animate numeric value from 0 on scroll-in |
| `duration` | `number` | no | countUp animation ms (default 1000) |
| `trend` | `string` | no | Period-over-period delta, e.g. "+12.5%" or "-3 vs last week" |

### MetricStrip

```
MetricStrip(items, columns?)
```

A responsive row/grid of Metric tiles. `columns` sets the preferred count.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `Metric[]` | **yes** |  |
| `columns` | `number` | no |  |

### Calendar

```
Calendar(month?, year?, selected?, onSelect?, events?, firstDay?, navigable?, onNavigate?, minDate?, maxDate?, disabledDates?, onEventClick?, locale?, weekdayLabels?, monthLabels?)
```

A Google Calendar-style month grid with Today/prev/next navigation. `month` (0–11) and `year` set the view (default current month); `selected` is an ISO date (YYYY-MM-DD) to highlight — bind a $variable for two-way selection; `onSelect(iso)` fires on a day click. `events` mixes bare ISO strings (rendered as dots) and `{date, label, color?, time?}` objects (rendered as colored chips with a '+N more' overflow; `color` is a tone name or CSS color); `onEventClick(event, iso)` fires when a chip is clicked. `minDate`/`maxDate`/`disabledDates` block days from being selected at all (booking windows, blackout days). `onNavigate(year, month)` fires when the user pages months; `navigable=false` hides the toolbar buttons; `locale` (or `weekdayLabels`/`monthLabels`) localises the names. Use for schedules, bookings, and availability calendars.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `month` | `number` | no | 0–11 (default current month) |
| `year` | `number` | no | Full year (default current) |
| `selected` | `string` | no | ISO date to highlight; bind a $variable |
| `onSelect` | `callable` | no | (isoDate) => … |
| `events` | `any[]` | no | ISO strings (dots) and/or {date, label, color?, time?} chips |
| `firstDay` | `number` | no | 0=Sunday (default), 1=Monday |
| `navigable` | `boolean` | no | Show Today/prev/next controls (default true) |
| `onNavigate` | `callable` | no | (year, month) => … after paging |
| `minDate` | `string` | no | ISO date — earlier days are not selectable (e.g. no past bookings) |
| `maxDate` | `string` | no | ISO date — later days are not selectable (e.g. a 90-day horizon) |
| `disabledDates` | `any[]` | no | ISO dates that cannot be selected (holidays, fully-booked days) |
| `onEventClick` | `callable` | no | (event, isoDate) => … when an event chip is clicked (the day cell still handles keyboard selection) |
| `locale` | `string` | no | BCP 47 tag for the weekday/month/day names (e.g. `de-DE`) |
| `weekdayLabels` | `any[]` | no | Exactly 7 labels, Sunday first — overrides `locale` |
| `monthLabels` | `any[]` | no | Exactly 12 month names — overrides `locale` |

### OrderSummary

```
OrderSummary(items, subtotal?, shipping?, tax?, total?, currency?, discount?, locale?, loading?, note?, empty?)
```

An order/cart summary: line items, subtotal, discount, shipping, tax, and a bold total. Pass `items` as {label, amount, qty?} and the named totals. `currency` takes an ISO code ("EUR" — properly localised via `locale`) or a bare symbol prefix. `loading` shows placeholders while an async shipping/tax quote resolves.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `object[]` | **yes** |  |
| `subtotal` | `string | number` | no |  |
| `shipping` | `string | number` | no |  |
| `tax` | `string | number` | no |  |
| `total` | `string | number` | no |  |
| `currency` | `string` | no | ISO code ("USD") or a symbol prefix (default "$") |
| `discount` | `string | number` | no | Coupon/promo line, shown after the subtotal |
| `locale` | `string` | no | BCP-47 tag for number formatting (default: the browser's) |
| `loading` | `boolean` | no | Show placeholders for the named totals |
| `note` | `string` | no | Fine print under the total ('Taxes calculated at checkout') |
| `empty` | `string` | no | Message when `items` is empty |

### Cart

```
Cart(items, onQty?, onRemove?, currency?, footer?, disabled?, loading?, error?)
```

A shopping-cart line list: each item shows image, name, unit price, a quantity stepper, and a remove button, with a running subtotal. `items` is [{ id, name, price, qty, image?, max? }] — `max` caps the stepper at available stock. `onQty(id, qty)` and `onRemove(id)` fire on interaction; omit `onQty` for a read-only receipt view. `disabled` freezes every control (e.g. while checkout submits), `loading` shows placeholder lines, `error` renders a message above the lines. `currency` is an ISO code (default USD).

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `object[]` | **yes** | [{ id, name, price, qty, image?, max? }] |
| `onQty` | `callable` | no | (id, qty) => … |
| `onRemove` | `callable` | no | (id) => … |
| `currency` | `string` | no |  |
| `footer` | `Node` | no | Custom footer (e.g. a checkout button) |
| `disabled` | `boolean` | no | Freeze every stepper and remove button — use while a mutation or the checkout round-trip is outstanding |
| `loading` | `boolean` | no | Show placeholder lines while the cart hydrates (keeps scroll position) |
| `error` | `string` | no | Error message rendered above the lines (a failed quantity update, a stock conflict) |

### PriceTag

```
PriceTag(price, compareAt?, currency?, size?, currencyPosition?, period?)
```

A formatted price with an optional struck-through compare-at price and a discount badge. Numeric values are grouped and given matching decimals (9.5 → $9.50, 1299 → $1,299); pre-formatted strings pass through untouched. `currencyPosition: "suffix"` writes "12,50 €"; `period` adds a trailing unit ("$29 /month").

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `price` | `string | number` | **yes** |  |
| `compareAt` | `string | number` | no |  |
| `currency` | `string` | no | Currency symbol/prefix (default '$') |
| `size` | `"sm"` \| `"md"` \| `"lg"` | no |  |
| `currencyPosition` | `"prefix"` \| `"suffix"` | no | Where the symbol sits (default prefix) |
| `period` | `string` | no | Trailing unit, e.g. "month" → "$29 /month" |

