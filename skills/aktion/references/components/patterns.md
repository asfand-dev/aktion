<!--
  GENERATED by scripts/emit-skill.mjs — do not edit by hand.
  Source of truth: `getComponentCatalog()` + `defaultLibrary.componentGroups`
  Regenerate with `npm run build:skill` (or `npm run build`).
-->

# Patterns

43 components. [← back to the catalogue index](./index.md)

## When to use what

These are the library's own authoring notes — they encode the intended choice
between similar components, which the prop tables below cannot express.

- Patterns are **opinionated composites** that pack an entire UI idiom into one component. Reach for them BEFORE composing equivalent layouts by hand with Card+Stack — the result will look more polished and require fewer tokens.
- `Hero(title, subtitle, primary, secondary, eyebrow?, highlights?, tone?)` — landing-style text-first header. Use `layout="cover"` with `imageSrc`, `height`, and optional `caption` for image-backed hero bands.
- `PageHeader(title, subtitle?, breadcrumbs?, actions?, status?)` — the canonical first child for any dashboard or detail page. If you omit `breadcrumbs`, the component auto-derives `["Home", title]`.
- `TopBar(title?, search?, actions?, sticky?)` — compact top strip for a content surface (panels, dialogs, embedded views). Use `AppShell` when you need a full sidebar; use `TopBar` for narrower headers above scrolling content.
- `SectionHeader(title, subtitle?, eyebrow?, status?, actions?)` — sub-header inside a Card or panel. Use instead of bare `CardHeader` when the section also needs eyebrow / actions / status.
- `Stats(items, layout?)` — KPI strip (`layout="strip"`, default) or responsive grid (`layout="grid"` with StatCard children). Prefer over hand-rolled StatCard rows.
- `Toolbar(left?, right?, center?, searchable?, searchPlaceholder?, searchValue?)` — filter/search/actions row above a list, table, or board. Pass `searchable: true` to auto-mount a `SearchBar` (use `searchValue` to bind it to a `$variable`).
- `EmptyState(title, description?, icon?, illustration?, actions?, action?)` — render this when a list is empty rather than an empty Card. The icon is auto-picked from the title keywords if you omit it (inbox/messages → `inbox`, charts/analytics → `chart-pie`, files/folders → `folder-open`, etc.).
- `Timeline([TimelineItem(...)])` — vertical event feed (audit log, changelog, activity).
- `ActivityLog(entries, variant?)` — purpose-built feed of user actions. Pass `entries` of `{actor, title, description?, time?, icon?, tone?, meta?}`; use `variant="audit"` for security/admin trails with monospace meta.
- `FeatureGrid([FeatureItem(...)])` — feature highlights with iconography.
- `MediaCard(title, imageSrc?, description?, tags?, meta?, actions?, badge?, orientation?, ratio?)` — image + content card. Use for article previews, product cards, project highlights. Pair with `Grid` for uniform card rows.
- `KanbanBoard([KanbanColumn("To do", [KanbanCard(...), ...])])` — task boards.
- `DescriptionList([DescriptionItem("Status", Badge(...)), …])` — detail-page key/value summary. Always preferable to a Stack of Text rows on profile, billing, or metadata panels.
- `StatusDot(label, tone?, pulse?)` — inline status pip. Use in toolbars, list rows, table cells, sidebars.
- `Notification(title, message?, time?, icon?, avatarSrc?, tone?, unread?, actions?)` — inline notification card for notification panels / inboxes (prefer `Banner` for top-of-page announcements).
- `InboxPanel(items, title?, onMarkAllRead?)` — `Notification` cards grouped into Unread / Earlier sections, with a shared mark-all-read action.
- `OnboardingChecklist(items, title?, description?)` — checklist of `{title, description?, done?, action?}` items with progress. Use for product onboarding, setup wizards, getting-started panels.
- `PricingTable([PricingCard(plan, price, period?, description?, features?, action?, badge?, featured?)])` — full pricing page block.
- `LoadingState(title?, description?, tone?, action?)`, `ErrorState(title, description?, action?, secondaryAction?, retryLabel?)`, `SuccessState(title, description?, action?, secondaryAction?)` — full-card empty-state alternatives for asynchronous content states.
- `Tour(steps, current, onFinish?)` and `Spotlight(title?, description?, action?)` are the product-tour primitives. `Spotlight` is a single highlighted call-out; `Tour` walks the user through multiple `{title, description, image?}` steps with Prev/Next/Skip controls and a progress dots indicator.

## Reference

### Hero

```
Hero(title, subtitle?, primary?, secondary?, eyebrow?, highlights?, imageSrc?, caption?, height?, actions?, layout?, tone?, overlay?, align?)
```

Eye-catching landing/marketing header with eyebrow tag, title, subtitle, optional bullet highlights, and primary/secondary CTA buttons. Use `layout="cover"` with `imageSrc` for an image-backed hero band (pass `height`, optional `caption`, and `overlay` to tune the scrim). Default layout shows an optional side illustration. `align="center"` centers the text block.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `title` | `string` | **yes** |  |
| `subtitle` | `string` | no |  |
| `primary` | `Button` | no | Primary CTA — pass a Button(...) |
| `secondary` | `Button` | no | Secondary CTA — pass a Button(...) |
| `eyebrow` | `string` | no | Short uppercase tag above the title |
| `highlights` | `string[]` | no | Bullet items rendered as tag pills |
| `imageSrc` | `string` | no | Illustration or cover background when layout=cover |
| `caption` | `string` | no | Small caption above CTAs (cover layout) |
| `height` | `string` | no | Min-height for cover layout (default 280px) |
| `actions` | `Node[]` | no | CTA row (cover layout; alternative to primary/secondary) |
| `layout` | `"default"` \| `"cover"` | no | default = text-first; cover = image-backed band |
| `tone` | `"default"` \| `"primary"` \| `"success"` \| `"warning"` \| `"danger"` \| `"info"` | no | Accent tone |
| `overlay` | `string | number` | no | Cover scrim strength — 0–1 alpha or 0–100 percent (default 0.62). Use a low value over a light photo, `0` for none. |
| `align` | `"start"` \| `"center"` | no | Text alignment inside the band (default start) |

### PageHeader

```
PageHeader(title, subtitle?, breadcrumbs?, actions?, status?, onCrumbClick?)
```

Page-level header with breadcrumbs, title, subtitle, status tag, and a right-aligned actions row. The canonical first child for any dashboard, settings, or detail page — replaces ad-hoc Stack+Header+Buttons stitching. If `breadcrumbs` is omitted the component auto-derives `["Home", title]` so the page never lacks a trail. Pass `breadcrumbs=false` to opt out.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `title` | `string` | **yes** |  |
| `subtitle` | `string` | no |  |
| `breadcrumbs` | `string[] | {label, to}[] | Breadcrumb | false` | no | Array of strings or `{label, to}` objects (a `to` path renders a router link), a Breadcrumb(...) node, or `false` to suppress the auto-derived trail |
| `actions` | `Node[]` | no | Buttons / NavLinks shown on the right |
| `status` | `Badge` | no | Optional Badge(...) rendered next to the title |
| `onCrumbClick` | `callable` | no | Called with (label, index) when a breadcrumb is clicked — makes string crumbs interactive |

### EmptyState

```
EmptyState(title, description?, icon?, illustration?, action?, actions?)
```

Zero-state placeholder for empty lists, searches, dashboards. Renders a centered icon (or illustration), title, description, and either a single `action` Button or an `actions` row (primary + secondary). Always preferable to an empty Card with raw text.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `title` | `string` | **yes** |  |
| `description` | `string` | no |  |
| `icon` | `string` | no | Font Awesome icon name (defaults to "inbox") |
| `illustration` | `string` | no | Image URL — takes precedence over `icon` when provided |
| `action` | `Button` | no | Single CTA (legacy slot) |
| `actions` | `Node[]` | no | Row of CTA Buttons / Links — preferred over `action` for primary + secondary affordances |

### Timeline

```
Timeline(items)
```

Vertical event timeline. Children must be TimelineItem entries. Ideal for activity feeds, changelogs, and process flows.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `TimelineItem[]` | **yes** |  |

### TimelineItem

```
TimelineItem(title, time?, description?, icon?, tone?, content?, href?, onClick?)
```

Single event on a Timeline. Pass `content` for rich children (Badge, Link, Button) beside the plain-text `description`, and `href`/`onClick` to make the row navigable.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `title` | `string` | **yes** |  |
| `time` | `string` | no | Display label (ISO, relative, etc.) |
| `description` | `string` | no |  |
| `icon` | `string` | no | Font Awesome icon name rendered inside the marker |
| `tone` | `"default"` \| `"primary"` \| `"success"` \| `"warning"` \| `"danger"` \| `"info"` | no |  |
| `content` | `Node[]` | no | Rich children below the description (Badge, Link, Button, …) |
| `href` | `string` | no | Turns the title into a link (changelog entry → PR, commit, release) |
| `onClick` | `callable` | no | Called when the row is clicked |

### ActivityLog

```
ActivityLog(items, variant?, emptyLabel?, onItemClick?, loading?, loaderLabel?)
```

Purpose-built feed of user/system activity. Each entry has `actor`, `title`, `description?`, `time?`, `icon?`, `avatarSrc?`, `tone?`, `href?`, and optional `meta` (IP, browser, request id). An entry's `href` renders its title as a link; `onItemClick` makes every title a button. Use `variant="audit"` to render `meta` in monospace for security/admin trails. Pass items as `{actor, title, description, time, icon, tone, avatarSrc, href, meta}` objects.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `object[]` | **yes** |  |
| `variant` | `"default"` \| `"audit"` | no | audit = monospace `meta` styling for security/admin trails |
| `emptyLabel` | `string` | no | Message shown when `items` is empty (default `No activity yet`) |
| `onItemClick` | `callable` | no | Callable fired with (index, item) when an entry title is activated |
| `loading` | `boolean` | no | Append a loading row while older activity is being fetched |
| `loaderLabel` | `string` | no | Label for the loading row (default `Loading activity…`) |

### FeatureGrid

```
FeatureGrid(items, columns?)
```

Responsive grid of FeatureItem tiles (typically 2–3 columns). Use to highlight product capabilities or page categories.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `FeatureItem[]` | **yes** |  |
| `columns` | `number` | no | Preferred column count (default auto) |

### FeatureItem

```
FeatureItem(title, description?, icon?, tone?, href?, onClick?)
```

Single tile on a FeatureGrid. Pass `href` or `onClick` to make the whole tile a link/button — a category or capability tile is normally the way into that section.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `title` | `string` | **yes** |  |
| `description` | `string` | no |  |
| `icon` | `string` | no | Font Awesome icon name shown in a colored disc |
| `tone` | `"default"` \| `"primary"` \| `"success"` \| `"warning"` \| `"danger"` \| `"info"` | no |  |
| `href` | `string` | no | Render the tile as a link to this URL |
| `onClick` | `callable` | no | Called when the tile is clicked |

### Testimonial

```
Testimonial(quote, author, role?, avatarSrc?, rating?)
```

Quote card with author, role, and optional avatar.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `quote` | `string` | **yes** |  |
| `author` | `string` | **yes** |  |
| `role` | `string` | no |  |
| `avatarSrc` | `string` | no |  |
| `rating` | `number` | no | 0–5 stars |

### ProfileCard

```
ProfileCard(name, role?, avatarSrc?, bio?, tags?, actions?)
```

Compact profile/user card with avatar, name, role, optional bio, social tags, and a row of action buttons. Use for team rosters, contributor lists, and contact panels.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `name` | `string` | **yes** |  |
| `role` | `string` | no |  |
| `avatarSrc` | `string` | no | Avatar image src; falls back to initials |
| `bio` | `string` | no |  |
| `tags` | `string[]` | no |  |
| `actions` | `Node[]` | no | Buttons to render at the bottom |

### Comment

```
Comment(author, body, time?, avatarSrc?, actions?)
```

Single comment / message bubble. Renders avatar, author, timestamp, body, and an optional row of toolbar buttons (reply, like, …).

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `author` | `string` | **yes** |  |
| `body` | `Node[] | Node | string` | **yes** | Comment text, or nodes — Markdown(...), Link(...), Code(...), a Stack of them |
| `time` | `string` | no | Relative or absolute timestamp |
| `avatarSrc` | `string` | no |  |
| `actions` | `Node[]` | no |  |

### Banner

```
Banner(title, message?, action?, icon?, tone?, dismissible?, onDismiss?, href?, onClick?)
```

Full-width announcement banner. Use at the top of a page for promos, release notes, or downtime notices. Pass `dismissible: true` for a close button, or `href`/`onClick` to make the whole band a click target. For inline notices prefer Callout or Alert.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `title` | `string` | **yes** |  |
| `message` | `string` | no |  |
| `action` | `Button` | no |  |
| `icon` | `string` | no | Font Awesome icon name |
| `tone` | `"default"` \| `"primary"` \| `"success"` \| `"warning"` \| `"danger"` \| `"info"` | no |  |
| `dismissible` | `boolean` | no | Show a close button that hides the banner |
| `onDismiss` | `callable` | no | Called when the banner is dismissed (implies `dismissible`) |
| `href` | `string` | no | Make the whole banner a link (release notes → changelog) |
| `onClick` | `callable` | no | Called when the banner itself is clicked |

### Notification

```
Notification(title, message?, time?, icon?, avatarSrc?, tone?, unread?, actions?, author?, onClick?, dismissible?, onDismiss?)
```

Inline notification card with title, message, time, optional avatar, and dismiss/action buttons. Use inside notification panels, inboxes, or activity drawers — for top-of-page announcements prefer `Banner`. Pass `onClick` to make the whole row open the underlying item, `dismissible`/`onDismiss` for a per-item close button, and `author` when `avatarSrc` is set so the avatar carries the actor's name.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `title` | `string` | **yes** |  |
| `message` | `string` | no |  |
| `time` | `string` | no | Relative or absolute timestamp |
| `icon` | `string` | no | Font Awesome icon name shown in a colored disc |
| `avatarSrc` | `string` | no | Avatar URL (alternative to `icon`) |
| `tone` | `"default"` \| `"primary"` \| `"success"` \| `"warning"` \| `"danger"` \| `"info"` | no |  |
| `unread` | `boolean` | no | Highlights the card with an accent |
| `actions` | `Node[]` | no |  |
| `author` | `string` | no | Person the notification is about — supplies the avatar's alt text and initials fallback |
| `onClick` | `callable` | no | Called when the row is clicked — open the item / mark it read |
| `dismissible` | `boolean` | no | Show a close button that removes the notification |
| `onDismiss` | `callable` | no | Called when the notification is dismissed (implies `dismissible`) |

### InboxPanel

```
InboxPanel(items, emptyLabel?, onMarkAllRead?, loading?, loadingLabel?, markAllLabel?, unreadLabel?, earlierLabel?)
```

Grouped notification list — entries are grouped into Unread/Earlier sections, with a count chip on each group header. Pass `items` as `{title, message, time, icon?, tone?, unread?, avatarSrc?, onClick?, actions?}` objects (`action` is also accepted as an alias for `onClick`; `actions` takes Button(...) entries rendered inside the row). Pair with a `SectionHeader` for the panel title (the component does not render its own title to avoid duplication). Set `loading` while entries are still being fetched so the panel does not claim the inbox is empty. Use for top-bar notification trays, activity drawers, and alert center pages.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `object[]` | **yes** |  |
| `emptyLabel` | `string` | no | Text shown when there are no notifications |
| `onMarkAllRead` | `callable` | no | Callable fired by the "Mark all as read" button |
| `loading` | `boolean` | no | Show a busy placeholder instead of the empty state while entries load |
| `loadingLabel` | `string` | no | Busy text (default "Loading…") |
| `markAllLabel` | `string` | no | Label of the mark-all button (default "Mark all as read") |
| `unreadLabel` | `string` | no | Unread group heading (default "Unread") |
| `earlierLabel` | `string` | no | Read group heading (default "Earlier") |

### OnboardingChecklist

```
OnboardingChecklist(items, title?, subtitle?, onDismiss?, onComplete?)
```

Step-by-step product checklist with completion progress at the top. Pass `items` as `{title, description?, done?, onClick?, cta?}` objects (`action` is also accepted as an alias). The progress percentage is computed automatically from `done`; `onComplete` fires once every step is done. An item's `onClick` stays available after it is completed (the button label defaults to "Start", or "Review" for a done step). Use on first-run dashboards, empty workspaces, and "complete your profile" surfaces.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `object[]` | **yes** |  |
| `title` | `string` | no | Heading (default "Getting started") |
| `subtitle` | `string` | no |  |
| `onDismiss` | `callable` | no | Callable fired by the dismiss (×) button — omit to render no dismiss affordance |
| `onComplete` | `callable` | no | Callable fired once, when the last remaining step flips to done |

### MediaCard

```
MediaCard(title, imageSrc?, description?, tags?, meta?, actions?, badge?, orientation?, ratio?, href?, onClick?)
```

Card with a media (image) header followed by title, body, optional tags, footer meta, and an actions row. Use for article previews, product cards, project highlights, gallery items — anywhere a Card needs a leading image. Orient with `orientation="horizontal"` for side-by-side media + content on wide viewports. Pass `href` (or `onClick`) to make the whole card the click target, which is what the hover lift implies.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `title` | `string` | **yes** |  |
| `imageSrc` | `string` | no | Image URL (omit to render a neutral placeholder) |
| `description` | `string` | no |  |
| `tags` | `string[]` | no | Tag pill labels |
| `meta` | `string` | no | Footer meta line (author · date · category) |
| `actions` | `Node[]` | no | Buttons / Links rendered at the bottom |
| `badge` | `string | Badge` | no | Eyebrow string or Badge node shown over the image |
| `orientation` | `"vertical"` \| `"horizontal"` | no |  |
| `ratio` | `string` | no | Media aspect ratio (default 16:9 vertical, 4:3 horizontal) |
| `href` | `string` | no | Make the whole card a link (article preview, product card, gallery item) |
| `onClick` | `callable` | no | Called when the card is clicked |

### TopBar

```
TopBar(title?, subtitle?, left?, center?, right?, sticky?)
```

Compact header strip that pairs a title (or breadcrumb) with search and action slots. Use INSTEAD of hand-rolling a `Stack(direction="row")` above a page. For full SaaS shells use `Navbar` (links) or `AppShell` (sidebar + topbar + content).

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `title` | `string` | no |  |
| `subtitle` | `string` | no |  |
| `left` | `Node[]` | no | Leading slot (breadcrumbs, brand, status, badges) |
| `center` | `Node[]` | no | Centered slot (search bar, segmented control) |
| `right` | `Node[]` | no | Trailing slot (actions, avatar) |
| `sticky` | `boolean` | no |  |

### KanbanBoard

```
KanbanBoard(columns, onCardMove?, draggable?)
```

Horizontal Kanban board. Children must be KanbanColumn entries. The board scrolls horizontally on narrow viewports so columns stay readable. Pass `onCardMove` to enable drag & drop — it is called with (cardTitle, toColumn, fromColumn) so the author moves the data.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `columns` | `KanbanColumn[]` | **yes** |  |
| `onCardMove` | `callable` | no | Called with (cardTitle, toColumn, fromColumn) after a card is dropped on another column |
| `draggable` | `boolean` | no | Make cards draggable without an onCardMove handler (implied by onCardMove) |

### KanbanColumn

```
KanbanColumn(title, items, tone?, actions?, limit?)
```

Single column inside a KanbanBoard. Children must be KanbanCard entries. Pass `actions` for the header's "+ Add card" / overflow menu and `limit` for a WIP limit (the count chip renders "3 / 5" and flags an overflow).

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `title` | `string` | **yes** |  |
| `items` | `KanbanCard[]` | **yes** |  |
| `tone` | `"default"` \| `"primary"` \| `"success"` \| `"warning"` \| `"danger"` \| `"info"` | no | Header accent tone |
| `actions` | `Node[]` | no | Header buttons — add card, column menu |
| `limit` | `number` | no | WIP limit; the count chip becomes "items / limit" |

### KanbanCard

```
KanbanCard(title, description?, tags?, assignee?, tone?, icon?, onClick?)
```

Single card on a Kanban board.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `title` | `string` | **yes** |  |
| `description` | `string` | no |  |
| `tags` | `string[]` | no |  |
| `assignee` | `string` | no | Name shown next to avatar initials |
| `tone` | `"default"` \| `"primary"` \| `"success"` \| `"warning"` \| `"danger"` \| `"info"` | no |  |
| `icon` | `string` | no | Optional Font Awesome icon name shown beside the title |
| `onClick` | `callable` | no | Optional callable fired when the card is clicked |

### SectionHeader

```
SectionHeader(title, subtitle?, eyebrow?, status?, actions?)
```

Compact section header for the top of a Card or panel. Renders a small eyebrow, a title, an optional subtitle, an optional status Tag/Badge, and a right-aligned actions row. Use this inside a Card to introduce a section instead of a bare `CardHeader`.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `title` | `string` | **yes** |  |
| `subtitle` | `string` | no |  |
| `eyebrow` | `string` | no | Short uppercase label above the title |
| `status` | `Badge | Tag` | no |  |
| `actions` | `Node[]` | no | Buttons / Links shown on the right |

### Toolbar

```
Toolbar(left?, right?, center?, searchable?, searchPlaceholder?, searchValue?, onSearch?, searchId?)
```

Horizontal toolbar for filters, search, view modes, and primary actions. Left/center/right slots wrap onto separate rows on narrow viewports so the bar never overflows. Pass `searchable: true` to auto-mount a SearchBar in the left slot (bind `searchValue` to a `$variable`, or handle `onSearch`). Use ABOVE a Table, List, Grid, or Kanban view — never replace `PageHeader` with it.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `left` | `Node[]` | no | Filters / search inputs / chips |
| `right` | `Node[]` | no | Primary action buttons |
| `center` | `Node[]` | no | Centered controls (e.g. SegmentedControl, search bar) |
| `searchable` | `boolean` | no | Auto-mount a SearchBar at the start of the left slot |
| `searchPlaceholder` | `string` | no | Placeholder for the auto-mounted SearchBar |
| `searchValue` | `string` | no | $variable bound to the auto-mounted SearchBar |
| `onSearch` | `callable` | no | Called with the query on every keystroke of the auto-mounted SearchBar |
| `searchId` | `string` | no | Explicit id/name for the auto-mounted SearchBar (defaults to a per-instance unique id) |

### DescriptionList

```
DescriptionList(items, columns?)
```

Compact key/value summary for detail pages — replaces a row of `Text`s with a properly aligned `<dl>`. Children must be DescriptionItem entries. Two columns by default on wide viewports.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `DescriptionItem[]` | **yes** |  |
| `columns` | `number` | no | 1 or 2 (default 2) |

### DescriptionItem

```
DescriptionItem(label, value, icon?)
```

Single row inside a DescriptionList. Renders a small uppercase label on the left and a value (string or arbitrary Node) on the right.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `label` | `string` | **yes** |  |
| `value` | `Node | string` | **yes** |  |
| `icon` | `string` | no |  |

### ActionStripe

```
ActionStripe(label, description?, icon?, value?, href?, disabled?, onClick?, trailing?, target?)
```

Full-width clickable navigation row — leading icon, label + optional description, an optional trailing `value` string or `trailing` node (Switch, Badge, Avatar), and a chevron affordance. Stack them for settings screens, product menus, and drill-down lists. Use `ListItem` instead for a non-interactive content row.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `label` | `string` | **yes** |  |
| `description` | `string` | no |  |
| `icon` | `string` | no | Leading Font Awesome icon name |
| `value` | `string` | no | Trailing value / status text shown before the chevron |
| `href` | `string` | no | Render as a link instead of a button |
| `disabled` | `boolean` | no |  |
| `onClick` | `callable` | no |  |
| `trailing` | `Node` | no | Trailing node before the chevron — Switch, Badge, Avatar (alternative to the string `value`) |
| `target` | `"_self"` \| `"_blank"` \| `"_parent"` \| `"_top"` | no | Link target; `_blank` also sets rel="noopener noreferrer" |

### StatusDot

```
StatusDot(label, tone?, pulse?)
```

Inline status pip + label. Use for compact health/state indicators in toolbars, sidebars, lists, and table cells.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `label` | `string` | **yes** |  |
| `tone` | `"default"` \| `"primary"` \| `"success"` \| `"warning"` \| `"danger"` \| `"info"` | no |  |
| `pulse` | `boolean` | no | Animate the dot for 'live' state |

### PricingTable

```
PricingTable(tiers, columns?)
```

Responsive grid of PricingCard tiers. Items size uniformly across a row and wrap onto multiple rows on narrow viewports. Use as the centerpiece of any pricing or upgrade page.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `tiers` | `PricingCard[]` | **yes** |  |
| `columns` | `number` | no | Preferred column count (default auto) |

### PricingCard

```
PricingCard(plan, price, period?, description?, features?, action?, badge?, featured?, ribbon?)
```

Single pricing tier card with plan name, price, billing period, description, bullet features, and a CTA button. Mark one tier as `featured=true` to highlight it (raises the card and shows a ribbon — "Most popular" unless `ribbon`/`badge` says otherwise). `features` entries may be plain strings or `{label, included: false}` objects so a cheaper tier can show what it does NOT include.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `plan` | `string` | **yes** | Tier name (e.g. 'Pro') |
| `price` | `string` | **yes** | Display price (e.g. '$29') |
| `period` | `string` | no | Billing period (e.g. '/mo') |
| `description` | `string` | no |  |
| `features` | `string[] | {label, included}[]` | no | Bullet list — strings are included features; `{label, included: false}` renders a struck-through/excluded bullet |
| `action` | `Button` | no | Primary CTA — pass a Button(...) |
| `badge` | `string` | no | Eyebrow / badge above the plan name |
| `featured` | `boolean` | no |  |
| `ribbon` | `string` | no | Ribbon text for a featured tier (default "Most popular") |

### LoadingState

```
LoadingState(title?, description?, actions?, icon?)
```

Full-card loading state — large spinner + title + description. Use while a query is in flight or while a long-running tool runs. Pass `actions` for escape hatches (Cancel / Run in background) and `icon` to replace the spinner for a queued/waiting state. For tiny inline loaders prefer `Spinner`; for skeleton placeholders prefer `Skeleton`.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `title` | `string` | no | Default "Loading…" |
| `description` | `string` | no |  |
| `actions` | `Node[]` | no | Buttons rendered under the text (Cancel, Run in background, …) |
| `icon` | `string` | no | Font Awesome icon shown INSTEAD of the spinner (e.g. `clock` for a queued state) |

### ErrorState

```
ErrorState(title?, description?, actions?, icon?)
```

Full-card error placeholder. Pairs a danger icon with title, description, and a row of recovery actions (Retry / Contact support / Go home). Pass `actions` as Button(...) entries.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `title` | `string` | no | Default "Something went wrong" |
| `description` | `string` | no |  |
| `actions` | `Node[]` | no |  |
| `icon` | `string` | no | Font Awesome icon (default `circle-exclamation`) |

### SuccessState

```
SuccessState(title?, description?, actions?, icon?)
```

Full-card success placeholder. Use for confirmation screens ("Order placed", "Payment succeeded", "Account verified") at the end of a flow. Pass `actions` for follow-up CTAs.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `title` | `string` | no | Default "Success" — an empty title falls back rather than rendering an icon-only card |
| `description` | `string` | no |  |
| `actions` | `Node[]` | no |  |
| `icon` | `string` | no | Default `circle-check` |

### Tour

```
Tour(steps, current, open?, onOpenChange?, onComplete?, onSkip?, skipLabel?, backLabel?, nextLabel?, finishLabel?)
```

Product-tour controller — renders the current step's title, description, and a Prev/Next/Skip row. Bind `current` to a `$variable` (0-indexed) to drive the step from your own state; without a binding the component advances itself. Bind `open` to a `$variable` so Skip/Finish can close it (otherwise it closes itself). Pass `steps` as `{title, description, target?}` objects; the optional `target` is a CSS selector that renders alongside the step for designers to reference.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `steps` | `object[]` | **yes** |  |
| `current` | `number` | **yes** | 0-indexed active step — bind a $variable |
| `open` | `boolean` | no | Whether the tour is visible — bind a $variable to control it |
| `onOpenChange` | `callable` | no | Called with the new boolean open state whenever the component opens or closes. |
| `onComplete` | `callable` | no | Fired when the user reaches Finish |
| `onSkip` | `callable` | no | Fired when the user bails out via Skip (falls back to `onComplete` when omitted) |
| `skipLabel` | `string` | no | Default "Skip" |
| `backLabel` | `string` | no | Default "Back" |
| `nextLabel` | `string` | no | Default "Next" |
| `finishLabel` | `string` | no | Default "Finish" |

### Spotlight

```
Spotlight(title, open?, description?, actions?, onClose?, target?)
```

Single-step product highlight — a dimmed full-page overlay with an explainer card, plus a ring around the element named by `target` (a CSS selector). Omit `target` for a centred callout on a plain dim. Use for one-off feature reveals ("Try the new commands menu"). Bind `open` to a `$variable` to control it; the × button, a backdrop click and Escape all dismiss and fire `onClose`.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `title` | `string` | **yes** |  |
| `open` | `boolean` | no | Whether the spotlight is visible — typically a $variable (default true) |
| `description` | `string` | no |  |
| `actions` | `Node[]` | no |  |
| `onClose` | `callable` | no | Callable invoked when the spotlight is dismissed — persist "already seen" here |
| `target` | `string` | no | CSS selector of the element to ring, e.g. "#commands-button" |

### LogoCloud

```
LogoCloud(items, label?)
```

A centered, wrapping row of LogoChip items with an optional label — 'works with' / 'trusted by' bands.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `items` | `LogoChip[]` | **yes** |  |
| `label` | `string` | no |  |

### LogoChip

```
LogoChip(label, icon?, imageSrc?, href?)
```

A single labelled chip for a LogoCloud: a Font Awesome `icon` or a customer wordmark `image`, plus the name. Give it an `href` to link the chip at its integration/partner page.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `label` | `string` | **yes** |  |
| `icon` | `string` | no | Font Awesome name (e.g. brands:react) |
| `imageSrc` | `string` | no | Logo image src URL (wins over `icon`) |
| `href` | `string` | no | Link target — the chip's hover lift already advertises interactivity |

### ProductCard

```
ProductCard(title, image?, price?, compareAt?, currency?, rating?, badge?, action?, onAdd?, href?, onClick?, reviewCount?, soldOut?)
```

An e-commerce product card: image, title, optional rating, a PriceTag, and an add-to-cart action. Pass `price`/`compareAt` directly or a custom `price` node. Give it `href` or `onClick` to make the whole card open the product (the card's hover lift promises it), `rating` + `reviewCount` for credible stars, and `soldOut` to dim it and stop the add button firing.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `title` | `string` | **yes** |  |
| `image` | `string` | no |  |
| `price` | `string | number` | no |  |
| `compareAt` | `string | number` | no |  |
| `currency` | `string` | no |  |
| `rating` | `number` | no | 0–5 stars |
| `badge` | `string` | no | Corner ribbon label (e.g. 'Sale') |
| `action` | `Node` | no | Add-to-cart Button; omit it and pass `onAdd` for the built-in icon button |
| `onAdd` | `callable` | no | Fired by the default add button |
| `href` | `string` | no | Product detail page — makes the whole card a link |
| `onClick` | `callable` | no | Fired when the card is opened (alternative to `href`) |
| `reviewCount` | `number` | no | Number of ratings shown beside the stars |
| `soldOut` | `boolean` | no | Dim the card and disable the add action |

### ShareButtons

```
ShareButtons(url, title?, networks?, showLabels?)
```

A row of social share buttons (twitter|facebook|linkedin|reddit|email|whatsapp|telegram|mastodon) plus a copy-link button, for the given `url`/`title`. `showLabels` renders text beside each icon.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `url` | `string` | **yes** |  |
| `title` | `string` | no |  |
| `networks` | `"twitter"` \| `"facebook"` \| `"linkedin"` \| `"reddit"` \| `"email"` \| `"whatsapp"` \| `"telegram"` \| `"mastodon"` \| `"copy"` | no | Which to show (default twitter, facebook, linkedin, copy) |
| `showLabels` | `boolean` | no | Show a visible label beside each icon |

### AuthorByline

```
AuthorByline(name, avatar?, role?, date?, href?, readingTime?)
```

An author byline: avatar, name, optional role, date and reading time — for articles/blog posts. `href` links the name to the author's page; `date` is emitted as a machine-readable <time>.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `name` | `string` | **yes** |  |
| `avatar` | `string` | no |  |
| `role` | `string` | no |  |
| `date` | `string` | no | Display string or ISO date |
| `href` | `string` | no | Link target for the author name |
| `readingTime` | `string` | no | e.g. "8 min read" |

### CodeWindow

```
CodeWindow(code, file?, language?, status?, preview?, copy?, height?, maxHeight?, showLineNumbers?, highlightLines?)
```

An editor/IDE window chrome (traffic-light dots + filename + status area) around source code. Pass the code as a plain STRING — it renders through a chromeless CodeBlock that fills the pane (no inner border, language label, or copy button), syntax-highlighted via `language` (default: inferred from the `file` extension, else aktion). Set `preview: true` to run the source as a LIVE app beside the code (split view with a pulsing 'Live render' badge); a custom node is also accepted for non-Aktion previews. `copy` adds a copy-to-clipboard button to the title bar, `height`/`maxHeight` cap the pane (long snippets scroll), and `showLineNumbers`/`highlightLines` point the reader at a specific line. The canonical way to show code in docs/marketing.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `code` | `string` | **yes** | Source text (a legacy CodeBlock(...) node is also accepted) |
| `file` | `string` | no | Filename shown in the title bar |
| `language` | `string` | no | Highlight language (default: from the file extension, else aktion) |
| `status` | `Node` | no | A Badge shown at the right of the bar |
| `preview` | `boolean | Node` | no | true → run the source as a live Aktion preview; or a custom node |
| `copy` | `boolean` | no | Show a copy-the-source button in the title bar |
| `height` | `string` | no | Explicit code-pane height (CSS length); overflow scrolls |
| `maxHeight` | `string` | no | Cap the code-pane height (CSS length); overflow scrolls |
| `showLineNumbers` | `boolean` | no | Render a left-side line-number gutter |
| `highlightLines` | `string` | no | Highlight ranges, e.g. "3-5,8" |

### BrowserFrame

```
BrowserFrame(child, url?, height?, clip?)
```

A browser-window chrome (dots + URL bar) wrapping a child — for screenshots/demos of web UIs. `height` fixes the mock viewport and scrolls inside it. The frame does NOT clip its child, so dropdowns/tooltips in the wrapped UI stay visible; set `clip: true` for a hard-edged screenshot look.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `child` | `Node` | **yes** |  |
| `url` | `string` | no |  |
| `height` | `string` | no | Mock viewport height (CSS length); the body scrolls |
| `clip` | `boolean` | no | Clip the child at the frame edge (hides popovers that overflow it) |

### Terminal

```
Terminal(lines, file?, prompt?, height?, maxHeight?)
```

A terminal window rendering monospace lines. Pass `lines` as an array of strings. `prompt` (e.g. "$" or "❯") prefixes each command line with a non-selectable marker — indented lines count as output and get none, so copying the block yields just the commands. `height`/`maxHeight` cap a long log and scroll it.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `lines` | `string[]` | **yes** |  |
| `file` | `string` | no | Title-bar label (default 'bash') |
| `prompt` | `string` | no | Prompt marker for command lines (e.g. "$"); indented lines are treated as output |
| `height` | `string` | no | Explicit body height (CSS length); overflow scrolls |
| `maxHeight` | `string` | no | Cap the body height (CSS length); overflow scrolls |

### ThemeToggle

```
ThemeToggle(light?, dark?)
```

A sun/moon button that toggles the host between light and dark themes — no host glue required. It flips the <aktion-app> `theme` attribute and dispatches a `theme-change` event the host can listen for.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `light` | `string` | no | Theme name when toggled to light (default 'light') |
| `dark` | `string` | no | Theme name when toggled to dark (default 'dark') |

### CopyButton

```
CopyButton(text, label?, copiedLabel?, iconOnly?)
```

A button that copies `text` to the clipboard and confirms it — the label flips to `copiedLabel` (default 'Copied!') with a check icon for a couple of seconds, then reverts, and the confirmation is announced to screen readers. `iconOnly` drops the label for tight corners (code blocks, API-key fields). Bounded, no host glue.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `text` | `string` | **yes** |  |
| `label` | `string` | no | Button label (default 'Copy') |
| `copiedLabel` | `string` | no | Confirmation label (default 'Copied!') |
| `iconOnly` | `boolean` | no | Render the icon alone; `label` becomes the accessible name |

