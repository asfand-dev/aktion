<!--
  GENERATED by scripts/emit-skill.mjs — do not edit by hand.
  Source of truth: `getComponentCatalog()` + `defaultLibrary.componentGroups`
  Regenerate with `npm run build:skill` (or `npm run build`).
-->

# Behaviour wrappers

8 components. [← back to the catalogue index](./index.md)

## When to use what

These are the library's own authoring notes — they encode the intended choice
between similar components, which the prop tables below cannot express.

- Wrappers compose: any built-in component can be made clickable, hoverable, observable, or restyled by wrapping it in one of these primitives — no need for the underlying component to grow another prop.
- `OnClick(child, { onClick, disabled?, stopPropagation? })` makes any component clickable / tappable (touch devices fire `click` too). Use for clickable cards, list rows, media tiles, and any custom layout that needs a tap target without a `<button>` baseline.
- `OnMouse(child, { enter?, leave?, hover?, move?, down?, up?, click?, doubleClick?, contextMenu?, scroll?, wheel?, drag?, drop?, dragStart?, dragEnd?, dragEnter?, dragLeave?, dragOver?, draggable?, passiveScroll? })` attaches any combination of mouse / pointer / drag listeners. Pass `draggable: true` to make the wrapper itself draggable. Scroll / wheel listeners default to `{ passive: true }` for smooth scrolling.
- `OnKeyboard(child, { onKeyDown?, onKeyUp?, onKeyPress?, focusable? })` attaches keyboard listeners. The wrapper is focusable by default; pass `focusable: false` when the child is already focusable (input, button).
- `OnFocus(child, { onFocus?, onBlur? })` tracks focus moving into or out of a subtree (uses bubbling `focusin` / `focusout` so descendants count).
- `OnIntersect(child, { onEnter?, onLeave?, onChange?, threshold?, rootMargin?, once? })` is the IntersectionObserver wrapper — perfect for lazy-load sentinels, infinite-scroll triggers, impression analytics, and reveal-on-scroll animations.
- `OnMount(child, { onMount?, onUnmount? })` is the DOM-ref / lifecycle wrapper. `onMount(node)` fires once after the wrapped element attaches; `onUnmount(node)` fires when it leaves the tree. Use it to measure or focus an element, or to hand a node to an imperative library (chart / map / editor). Stash the node in a `$ref(...)`.
- `Css(child, { style?, class? })` merges raw class tokens and inline styles onto the wrapped child. Reach for it ONLY when the standard component props can't express the styling — prefer `Box`/`Stack`/`Grid` for layout and `$theme(...)` for tokens.
- `Link(label_or_child, { to?, href?, external?, variant? })` is the anchor primitive — accepts either a plain string label or a wrapped component. Use `to` for client-side router navigation and `href` (with `external: true`) for outbound links.

## Reference

### OnClick

```
OnClick(child, onClick, disabled?, stopPropagation?, role?, keyboard?)
```

Make any component clickable. Wraps the child in a transparent span and dispatches `onClick(event)` when the user clicks or taps it. Clicks that land on a self-acting child (a Button, Link, or form control) are left to that child, so nesting is safe. Enter / Space activate it too unless `keyboard: false`. Use to attach click behaviour to components that do not expose an `action` / `onClick` prop (cards, list rows, media tiles, custom layouts).

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `child` | `Node` | **yes** | Component (or array of components) to wrap |
| `onClick` | `callable` | **yes** | Callable invoked on click / tap. Receives the native MouseEvent. |
| `disabled` | `boolean` | no | Skip firing the handler while truthy (also sets aria-disabled and leaves the tab order) |
| `stopPropagation` | `boolean` | no | Call event.stopPropagation() after invoking the handler (default false) |
| `role` | `string` | no | ARIA role for the wrapper (default "button"). Pass "none" when the wrapped element is a list row / table cell whose container owns the semantics. |
| `keyboard` | `boolean` | no | Activate on Enter / Space and expose a tab stop (default true). Pass false when the child is already focusable — e.g. a card containing inputs. |

### OnMouse

```
OnMouse(child, enter?, leave?, hover?, move?, down?, up?, click?, doubleClick?, contextMenu?, scroll?, wheel?, pointerDown?, pointerMove?, pointerUp?, drag?, drop?, dragStart?, dragEnd?, dragEnter?, dragLeave?, dragOver?, draggable?, passiveScroll?)
```

Attach any combination of mouse / pointer / drag listeners to a component. Pass only the props you need — unused events install no handler so the wrapper is essentially free. Each handler receives the native MouseEvent / PointerEvent / DragEvent / WheelEvent. Use for hover tracking, custom drag-and-drop, context menus, scroll-aware UIs.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `child` | `Node` | **yes** | Component to wrap |
| `enter` | `callable` | no | Fired when the pointer enters the element (mouseenter) |
| `leave` | `callable` | no | Fired when the pointer leaves the element (mouseleave) |
| `hover` | `callable` | no | Fired on every mouseover inside the element |
| `move` | `callable` | no | Fired on every mousemove inside the element |
| `down` | `callable` | no | Fired on mousedown |
| `up` | `callable` | no | Fired on mouseup |
| `click` | `callable` | no | Fired on click / tap |
| `doubleClick` | `callable` | no | Fired on double-click |
| `contextMenu` | `callable` | no | Fired on right-click (contextmenu) |
| `scroll` | `callable` | no | Fired when an inner element scrolls (observed on the capture phase, since scroll does not bubble) |
| `wheel` | `callable` | no | Fired on mouse-wheel / trackpad scroll. Not passive, so `event.preventDefault()` works (custom zoom). |
| `pointerDown` | `callable` | no | Fired on pointerdown — use instead of `down` for touch / pen drags (gives `pointerId`, pressure, setPointerCapture) |
| `pointerMove` | `callable` | no | Fired on pointermove |
| `pointerUp` | `callable` | no | Fired on pointerup |
| `drag` | `callable` | no | Fired while the element is being dragged |
| `drop` | `callable` | no | Fired when something is dropped onto the element |
| `dragStart` | `callable` | no | Fired when a drag begins |
| `dragEnd` | `callable` | no | Fired when a drag ends |
| `dragEnter` | `callable` | no | Fired when a drag enters the element |
| `dragLeave` | `callable` | no | Fired when a drag leaves the element |
| `dragOver` | `callable` | no | Fired while something is dragged over the element. Call `event.preventDefault()` to make the element a valid drop target. |
| `draggable` | `boolean` | no | Make the wrapper itself draggable (sets `draggable="true"`) |
| `passiveScroll` | `boolean` | no | Register the `scroll` listener as passive (default true, for scroll performance). Read on the first render. |

### OnKeyboard

```
OnKeyboard(child, onKeyDown?, onKeyUp?, onKeyPress?, focusable?, global?)
```

Attach keyboard listeners to a component. Pass any combination of `onKeyDown`, `onKeyUp`, and `onKeyPress`; each handler receives the native KeyboardEvent. Use for navigation and custom focusable widgets. The wrapper is focusable by default (tabindex="0") so it can be reached via Tab; pass `focusable=false` when the child is already focusable. For an app-wide shortcut (Cmd+K, `?`) pass `global: true`, which listens on the window instead so the keys fire wherever focus is.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `child` | `Node` | **yes** |  |
| `onKeyDown` | `callable` | no | Fired on keydown |
| `onKeyUp` | `callable` | no | Fired on keyup |
| `onKeyPress` | `callable` | no | Fired on keypress |
| `focusable` | `boolean` | no | Make the wrapper focusable via Tab (default true, or false when `global`) |
| `global` | `boolean` | no | Listen on the window rather than the wrapper, so the shortcut fires regardless of focus (command palettes, help overlays). Removed when the component leaves the tree. |

### OnFocus

```
OnFocus(child, onFocus?, onBlur?)
```

Attach focus / blur listeners to a component. Use to track input focus rings, custom focus indicators, or autosave-on-blur flows. Listens for the bubbling `focusin` / `focusout` events, so focus entering or leaving any descendant is observed.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `child` | `Node` | **yes** |  |
| `onFocus` | `callable` | no | Fired when focus enters the element or any descendant |
| `onBlur` | `callable` | no | Fired when focus leaves the element and all descendants |

### OnIntersect

```
OnIntersect(child, onEnter?, onLeave?, onChange?, threshold?, rootMargin?, root?, once?, disabled?)
```

Observe whether a component is visible in the viewport (or a scroll container passed as `root`) using IntersectionObserver. Fires `onEnter` the first time the wrapped element becomes visible, `onLeave` when it leaves, and `onChange({visible, ratio})` for every transition. Use for lazy-load sentinels, infinite-scroll triggers, impression analytics, and reveal-on-scroll animations. Set `disabled: true` once there is nothing left to load.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `child` | `Node` | **yes** |  |
| `onEnter` | `callable` | no | Fired when the element enters the viewport |
| `onLeave` | `callable` | no | Fired when the element leaves the viewport |
| `onChange` | `callable` | no | Fired with `{visible, ratio}` on every transition |
| `threshold` | `number` | no | Visible-ratio threshold 0–1 (default 0.05) |
| `rootMargin` | `string` | no | Root margin as 1–4 px / % lengths (e.g. "0px 0px -64px 0px"). Unit-less values are ignored. |
| `root` | `string` | no | CSS selector for the scroll container to observe inside (e.g. ".rui-scroll-area"). Defaults to the viewport. |
| `once` | `boolean` | no | Disconnect after the first entry (default false) |
| `disabled` | `boolean` | no | Stop firing the callbacks while truthy — use it when the last page of an infinite list has loaded |

### OnMount

```
OnMount(child, onMount?, onUnmount?, deps?)
```

Run imperative code against the wrapped component's rendered DOM node. `onMount(node)` fires once, on a microtask after the child is attached to the DOM — the Aktion way to get a DOM ref (measure an element, focus it, or hand it to an imperative library such as a chart / map / editor). `onUnmount(node)` fires when the component leaves the tree. Pass `deps` to tear down and re-run the pair when the values in it change. Pair with `$ref(...)` to stash the node across renders.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `child` | `Node` | **yes** |  |
| `onMount` | `callable` | no | `(node) => void` — fired once after the wrapped element is attached. |
| `onUnmount` | `callable` | no | `(node) => void` — fired when the wrapped element leaves the tree. |
| `deps` | `any[]` | no | Re-run `onUnmount` + `onMount` whenever a value in this list changes (e.g. `deps: [$docId]` to rebind an editor to another document). |

### Css

```
Css(child, style?, class?)
```

Apply raw CSS class names and / or an inline style string to a wrapped component. The styling is merged onto the rendered child's DOM element — no extra wrapper element is added when the child renders as one element (including an `Svg` root). Several children are each styled individually; a plain-text child gets a wrapping span to carry the styling. Reach for `Css` only when the component's own props cannot express the styling (use `Box`/`Stack`/`Grid` props for layout, `Theme` for tokens, `Styles` + selector classes for sweeping changes).

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `child` | `Node` | **yes** |  |
| `style` | `string` | no | Inline CSS declarations (e.g. "padding: 16px; background: #eef;") |
| `class` | `string | string[]` | no | Class name (space-separated string or array). Tokens must match `[A-Za-z_][A-Za-z0-9_-:/]*`. |

### Link

```
Link(label, to?, href?, external?, variant?, disabled?, onClick?, download?)
```

Anchor link. Accepts either a plain string label or a wrapped component as its positional child. Pass `to` for client-side router navigation (no page reload) or `href` for a regular anchor. `external: true` opens the link in a new tab with `rel="noopener noreferrer"`. `disabled: true` renders a non-navigating link (pagination edges, gated steps). Use to make any component clickable as a link — cards, icons, badges, list rows.

| prop | type / values | required | notes |
| --- | --- | --- | --- |
| `label` | `string | Node` | **yes** | Visible text OR a wrapped component |
| `to` | `string` | no | Router path (preferred). Navigates via the runtime router on click. |
| `href` | `string` | no | Standard anchor href. Used when `to` is omitted. |
| `external` | `boolean` | no | Open in a new tab (`target="_blank"`, `rel="noopener noreferrer"`) |
| `variant` | `"default"` \| `"subtle"` | no |  |
| `disabled` | `boolean` | no | Render as a non-navigating link: no href, out of the tab order, announced as disabled |
| `onClick` | `callable` | no | Ran before navigation (analytics, closing a drawer). Call `event.preventDefault()` inside it to cancel the navigation. |
| `download` | `boolean | string` | no | Download the target instead of navigating. Pass a string to suggest a filename. |

